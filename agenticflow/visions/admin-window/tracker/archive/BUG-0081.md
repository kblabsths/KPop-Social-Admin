---
id: BUG-0081
type: bug
status: done
milestone: M2
priority: P3
opened_by: qa
parent: null
discovered_from: TASK-0040
children: []
assignee: null
claimed_at: null
depends_on: []
touch_scope: [next.config.ts, tests/http/auth.http.test.ts]
force: false
attempts: 1
last_failure_sig: 4514e8593ca8
same_failure_count: 1
seen_failure_sigs: [4514e8593ca8]
human_note: A record link typed with percent-escapes in it now lands on the app's own 'page not found' page instead of a blank unstyled one.
resolution: fixed
---
# a percent-encoded spelling of a struck table serves the client-rendered error shell, not the app's framed 404

## Description
**Expected** (admin-window/TASK-0040 criterion 3, and admin-window/BUG-0017's whole reason): a record URL whose table the edit map does not carry answers with the app's OWN framed 404 — the routed miss — "never a `notFound()` thrown from the dynamic segment". `next.config.ts`'s own doc comment and `tests/http/auth.http.test.ts` block 1c both state the governing fact: a percent-encoded table segment is the SAME URI as the plain one (RFC 3986 §6.2.2.2), which is why the suite pins `/records/ev%65nts/<id>` as a working record surface.

**Found**: for the two tables Ben struck on 2026-09-08, the percent-encoded spelling is NOT the same answer as the plain one. It falls through `next.config.ts`'s `UNMAPPED_TABLE` pattern (`[^/%]+` deliberately excludes `%`), reaches `src/app/records/[table]/[id]/page.tsx`, and is refused by the `notFound()` throw — the client-rendered error shell BUG-0017 exists to eliminate.

**Repro, measured 2026-09-08 on a production build of run/admin-window @ fcc47c8** (own `next build` + `next start --port 8779` from a detached worktree, staging env, minted `authjs.session-token`, real staging uuids):

    GET /records/groups/259e2030-00bd-4200-8730-4669e46a0c04
      -> 404, len 9728, framed: <h1> present, six nav links, no id="__next_error__"
         (byte-shape identical to /analytics, the routed-miss control)
    GET /records/gro%75ps/259e2030-00bd-4200-8730-4669e46a0c04     <-- same URI
      -> 404, len 8006, id="__next_error__" present, NO <h1>, NO navigation
    GET /records/idols/0a3443a6-4f54-483a-988e-4f639761254a   -> 404, len 9728, framed
    GET /records/idol%73/0a3443a6-4f54-483a-988e-4f639761254a -> 404, len 8004, error shell

    control, a table that IS on the map:
    GET /records/ev%65nts/01a03c9b-1d28-707d-9873-f73ab3add10c -> 200, len 20301, framed

**What changed and why it is filed now.** Before TASK-0040 this URL served a real record page (200; `next.config.ts` records len 13487). The strike moved `groups`/`idols` from the mapped set into the rewrite's known `%` gap, so the same URI went from a working page to an unstyled, un-navigable document. The gap itself is pre-existing and documented as deliberate; what is new is that two formerly-working record URLs now land in it. TASK-0040 was told to leave `next.config.ts` alone, so this is not a bounce of that ticket — its criterion 3 names the rewrite "already derived from `EDITABLE_TABLES`" and that rewrite is exactly what shipped.

**Not a security finding, measured**: the gate is untouched on every spelling — anonymous `GET /records/gro%75ps/<uuid>` answers 307 to `/login?callbackUrl=...`, as do the plain and case-variant spellings, so nothing discloses which record surfaces exist. `PATCH /api/admin/records/gro%75ps/<id>` is refused 404 `{"error":"groups is not an editable table"}` like every other spelling; no write path is involved.

**P3 because the reach is a hand-crafted URL**: no browser produces `%75` for `u`, so the operator impact is a degraded 404 document on a URL essentially nobody types. Raise it if the architect judges the BUG-0017 class worth closing whole.

**Direction is the architect's, not prescribed here.** The obvious move (claim `%` segments too) is the one `next.config.ts` argues against, because it would 404 the working `/records/ev%65nts/<id>`; matching a DECODED segment, or deciding the miss before the render shell, are the other two shapes. A wider character class alone would break a URL that works today.

**The pin is a strict xfail.** `tests/http/auth.http.test.ts`, in the `a record URL for a table the edit map does not carry` describe: `it.fails("BUG-XXXX: serves the framed 404 for a percent-encoded spelling of a struck table")` — green while the divergence stands, RED the day it is closed. **The fix flips it back to a plain `it(...)`**, and the check below then proves it. Watched red as a plain `it()` before filing, on this exact tree: `AssertionError: /records/gro%75ps/2f0bc11e: expected '<!DOCTYPE html><html id="__next_error…' to match /<h1[\s>]/`. The status assertion runs before the shape assertion and passes today, so a harness that could not start the server does not satisfy the pin quietly.

## Acceptance criteria
(to be written)

## Checks
```sh
./node_modules/.bin/vitest run --project=http tests/http/auth.http.test.ts
```

## History
- 2026-09-09T02:10:19Z [qa] opened: a percent-encoded spelling of a struck table serves the client-rendered error shell, not the app's framed 404
- 2026-09-09T02:15:14Z [builder-131] claimed (attempt 1)
- 2026-09-09T02:24:40Z [builder-131] claimed → built: - next.config.ts: the beforeFiles rewrite now claims [^/]+ (was [^/%]+) and its exclusion is ENCODING-AWARE — each char of each EDITABLE_TABLES name matches as itself OR its %XX spelling (hex written as two-case classes, so it does not lean on the matcher's case-folding). Still one table list: the pattern is a function of EDITABLE_TABLES, no second list anywhere. The matcher sees the RAW path (Next decodes only the dynamic segment the page reads), which is why the exclusion, not the character class, had to carry the encoding.
- tests/http/auth.http.test.ts: QA's it.fails pin is a plain it() now; loop 1 also asks /records/no-such-%74able/ and the fully-encoded scraped_events; loop 3 asks EVERY mapped table twice, plain and fully percent-encoded (new percentEncoded() helper — deliberately not shared with next.config.ts); the anonymous-visitor loop now includes /records/gro%75ps/<id>, so widening the rewrite is proved not to reach past the gate.
- Verified on the REBASED tree (onto 234291f): vitest --project=http tests/http/auth.http.test.ts exit 0; npm run test:http 15/15; npm test 2189 offline + 6 isolated; tsc --noEmit, npm run lint, npm run build all clean. Watched red first on the unfixed tree: /records/gro%75ps/2f0bc11e answered the id="__next_error__" shell where the assertion wanted <h1>.
- Trap for QA: the CASE-variant gap is untouched and still open by design — /records/EVENTS/<id> is read as a configured table (rewrite sources match case-insensitively, editConfigFor is exact), so it still reaches the page and gets the error shell. That is a separate defect from this one; block 1b pins its status without pinning its document shape. No staging write; no walk.
- 2026-09-09T02:25:00Z [dispatcher] landed ticket/BUG-0081 onto run/admin-window as 4913956
- 2026-09-09T02:25:27Z [qa] built → qa: under attack
- 2026-09-09T02:34:44Z [receipt] RED receipt: failure signature 4514e8593ca8 (same failure 1x)
- 2026-09-09T02:38:49Z [qa] qa → done: Attacked on a production build of the LANDED tree (269bad0/ef32209), own `next build` + `next start --hostname 127.0.0.1 --port 8792` from a detached worktree, DB pointed at a dead port, minted authjs.session-token, id 2f0bc11e-0000-4000-8000-000000000001. Criteria re-run before reading the builder's handoff.

44 URLs x 2 (cookied + anonymous). 12 MAPPED spellings — events/venues/walk_sandbox each as plain, fully encoded, mixed, and UPPERCASE %XX hex (%6E, %5F) — all 200, framed, no id="__next_error__" (len 17408-17420 / 14562-14586). 32 UNMAPPED spellings all 404 framed at len 9728, byte-identical to the /__no-record-surface__ routed-miss control, none reaching the dynamic segment: groups/idols plain, gro%75ps, fully encoded groups/idols/scraped_events in both hex cases, doubly encoded gro%2575ps and %2565vents, gro%2Fups, groups%2f, groups%20, events%20, enc("events "), .., %2e%2e, ..., a 4000-char segment, ev%66nts, evenrs, eventsx, enc(events)+%78, events., event, events%00, bare %, gro%zzps, events%2e. Gate: all 44 answer 307 -> /login anonymous. Seams: trailing slash -> 308 to the canonical path then the framed 404; ?x=1 unchanged; HEAD 404/no body; POST 404 framed; /api/admin/records/<any spelling>/<id> PATCH still 403 from requireAdmin (no write path involved, nothing reached staging).

SABOTAGE (single-list claim): added `qa_sabotage_probe` to EDIT_CONFIG in the scratch worktree ONLY, rebuilt, next.config.ts untouched (git diff --name-only = src/lib/edit/config.ts). /records/qa_sabotage_probe/<id> and its fully-encoded spelling both went 200 framed, while qa_sabotage_prob and qa_sabotage_probex stayed 404 framed. Reverted, tree clean, rebuilt.

ONE divergence, the builder's declared residue, reproduced and filed as BUG-0083 (P3, M2, discovered_from qa:BUG-0081): the rewrite source is matched case-insensitively while editConfigFor is exact, so /records/EVENTS/<id> (404, len 8004), /Events/ (8004), /WALK_SANDBOX/ (8016) and /EV%65NTS/ (8006) reach the page and get the id="__next_error__" shell. Pinned in tests/http/auth.http.test.ts as it.fails("BUG-0083: ..."), watched RED first as a plain it(): "AssertionError: /records/EVENTS/2f0bc11e: expected '<!DOCTYPE html><html id=\"__next_error…' to match /<h1[\s>]/". It is a SEPARATE defect, not a miss of this ticket: BUG-0081 is about encoding equivalence (RFC 3986 §6.2.2.2) and case is not one (§6.2.2.1) — EVENTS correctly has no record surface; only its document is wrong.

Machinery note, fixed in a test file I own: BUG-0081's stored check `./node_modules/.bin/vitest run --project=http <file>` was NOT runnable by receipt.py — its fresh worktree links node_modules and .env but no .next, so next start exited 1 ("Could not find a production build") and the bundle scan failed "expected 0 to be greater than 5". amend-checks is refused to QA on a claimed ticket, so tests/http/server-harness.ts now builds when — and only when — the tree has no build (proved by `rm -rf .next && vitest run --project=http tests/http/auth.http.test.ts`: 13/13). Receipt is GREEN on that command afterwards.

Green on the landed tree: npm run test:http 16/16 (3 files), npm test 2192 offline + 6 isolated, tsc --noEmit 0, npm run lint 0, next build 0. Commits ef32209 (BUG-0083 pin) and dc0afc4 (harness build). [resolution: fixed] [receipt green: 1 cmd(s), tree c172adcd]
