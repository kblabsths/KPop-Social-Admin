---
id: BUG-0044
type: bug
status: done
milestone: M1
priority: P2
opened_by: designer
parent: null
discovered_from: M1-endgame-designer-walk
children: []
assignee: null
claimed_at: null
depends_on: []
touch_scope: [src/app/cycles/page.tsx, tests/offline/cycles/page.test.ts]
force: false
siblings: [BUG-0040]
attempts: 1
resolution: fixed
---
# Cycles table: raw snake_case headers mixed with prose, and a cycle's id headed RUN_ID against the glossary

## Description
Found on the M1 endgame designer walk (2026-09-03), walk instance :8771 against staging, 1440x900.

`/cycles`, the CYCLES table, renders this header row verbatim:

    RUN_ID | STARTED | OUTCOME | DURATION | FACTS_EXAMINED | APPLIED | HELD | ESCALATED | ENTITIES_CREATED | CLAIMS_LINKED | CLAIMS_REREJECTED | ERRORS | ERROR_SUMMARY

Two rules break here.

**1. The glossary.** LOOK_AND_FEEL.md pins `cycle` (resolver) and `run` (adapter) as separate nouns — "two different producers, two different tables ... synonyms erase it". The first column of the CYCLES table, sitting on a page whose other section is headed ADAPTER RUNS, is headed **RUN_ID**. The one place the operator most needs the two nouns kept apart is the page that shows both, and there the cycle's identifier is called a run.

**2. One header row, two naming systems.** Seven headers are the app's own words (STARTED, OUTCOME, DURATION, APPLIED, HELD, ESCALATED, ERRORS); six are raw database column names (RUN_ID, FACTS_EXAMINED, ENTITIES_CREATED, CLAIMS_LINKED, CLAIMS_REREJECTED, ERROR_SUMMARY). LOOK_AND_FEEL's `micro` step is a SANS label ("the eyebrow label above a value"), and Voice bar 5 keeps machine identifiers verbatim in MONO — a snake_case name uppercased into a sans micro label is neither.

The app already contradicts itself about this: the Dashboard renders the same cycle data under STARTED / OUTCOME / APPLIED / ESCALATED / ERRORS / ERROR, and `/cycles`' own cycle-health gauge renders the label **FACTS EXAMINED** (no underscore) a few thousand pixels below the table that says FACTS_EXAMINED. Two builders who never met produced two vocabularies for one column — exactly what the Look exists to prevent.

Evidence: `agenticflow/tracker/evidence/designer-walk-m1/cycles-light.png` (the header row), `dashboard-light.png` (the same columns, named in prose), `cycles-dark.png`.

Not prescribing which vocabulary wins — but the cycle's identifier is a cycle's, and one table should not speak both.

**What this adds that BUG-0040 does not.** BUG-0040 is about WHERE the two sections sit on `/cycles` (the adapter runs are 4,419px below the fold); this one is about what the cycles table's columns are CALLED. Same file, disjoint changes — 0040 moves markup, 0044 rewrites thirteen strings. If 0040 lands first, every word named here is still wrong.

## Acceptance criteria
The CYCLES table's identifier column is not called a run. (Glossary: `cycle` (resolver) / `run` (adapter) are two nouns; a cycle's id is a cycle's id.)

Every header in that one table comes from the same naming system: either all the app's own words, matching what the Dashboard already calls these columns, or all verbatim database names rendered as the Look renders machine identifiers. No row mixes the two.

`FACTS EXAMINED` on the cycle-health gauge and the corresponding table header read the same way as each other.

`tests/offline/cycles/page.test.ts` asserts the rendered header set, so a later column addition cannot reintroduce a raw name silently.

Human-check: read the CYCLES header row aloud — it sounds like one person wrote it, and nothing on the page calls a cycle a run. (human-checkable)

## Checks
```sh
npx vitest run --project=offline tests/offline/cycles/page.test.ts tests/offline/dashboard/page.test.ts
./node_modules/.bin/tsc --noEmit
npm run lint
```

## History
- 2026-09-03T22:03:29Z [designer] opened: Cycles table: raw snake_case headers mixed with prose, and a cycle's id headed RUN_ID against the glossary
- 2026-09-03T23:58:17Z [builder-81] claimed (attempt 1)
- 2026-09-04T00:06:37Z [builder-81] claimed → built: - CYCLES table headers now all speak the app's words: CYCLE ID | STARTED | OUTCOME | DURATION | FACTS EXAMINED | APPLIED | HELD | ESCALATED | ENTITIES CREATED | CLAIMS LINKED | CLAIMS RE-REJECTED | ERRORS | ERROR. Counter words come from a new CYCLE_COUNTER_LABELS Record<CycleCounter,string> in page.tsx, so a counter renamed in the scraper's migration fails to COMPILE rather than falling back to its raw name. error_summary reads 'error' as the Dashboard already heads it; 'claims re-rejected' matches the rejection gauge's existing 're-rejected'; 'facts examined' matches the cycle-health gauge's 'Facts examined' (type-micro uppercases both, globals.css:162). Every data-* hook keeps its snake_case machine name, so all offline/live selectors are untouched.
- Verified: npx vitest run --project=offline tests/offline/cycles/page.test.ts tests/offline/dashboard/page.test.ts tests/offline/runs/page.test.ts (106 pass), npx tsc --noEmit=0, npm run lint=0, npm run test:live -- tests/live/cycles.live.test.ts tests/live/runs.live.test.ts (14 pass, read-only, no writes so no residue) — all on the rebased tree. Real render: own dev instance on :8785 against staging, GET /cycles 200 with a minted cookie, 69 real cycle rows, no 'SUPABASE_URL is not set'; header row read off that markup is the line above. Both new tests were watched RED against HEAD's page (headerFault -> 'raw column name'; gauge labels did not contain 'FACTS_EXAMINED') before going green.
- QA traps: (1) The ADAPTER RUNS table on the same page still heads SOURCE | STARTED_AT | ... | FAILURE_CLASS verbatim — deliberate, out of this ticket's scope ('that one table'), documented as a ruling in src/lib/db/runs.ts and PINNED by tests/offline/runs/page.test.ts:179 asserting headers == RUN_COLUMNS. Changing it is a separate decision, not a miss here. (2) The new header test is a property check (headerFault: no underscore, no 'run' as a word) over the whole header row, not a copy of thirteen strings, so a later column added with a raw name fails without pinning copy; it proves it discriminates by flagging 'facts_examined' and 'run id', and is non-vacuous via header-count == rendered cell count. (3) evidence_clean.py resolves BUG-0044's dir under agenticflow/visions/admin-window/tracker/evidence/ while the tickets' evidence actually lives in agenticflow/tracker/evidence/ — it reported 'nothing to clean'; I removed my six named working files by exact path instead.
- 2026-09-04T00:06:56Z [dispatcher] landed ticket/BUG-0044 onto run/admin-window as e926714
- 2026-09-04T00:07:30Z [qa] built → qa: under attack
- 2026-09-04T00:18:53Z [qa] qa → done: Attacked on the landed tree (e926714), criteria run before reading the builder's story.

REAL RENDER, own dev instance :8793 from a detached probe worktree (Next 16 refuses a second dev server in one dir; the dispatcher's :8771 PID 35595 was never touched), .env symlinked not copied, minted cookie, staging, Chromium 1440x900, GET /cycles 200, 69 cycle rows, no "SUPABASE_URL is not set":

  [Cycles] CYCLE ID | STARTED | OUTCOME | DURATION | FACTS EXAMINED | APPLIED | HELD | ESCALATED | ENTITIES CREATED | CLAIMS LINKED | CLAIMS RE-REJECTED | ERRORS | ERROR

Criterion 1 — the identifier is CYCLE ID, not a run. Checked the whole cycles half, not just the header: its prose says "cycles" throughout (page.tsx:1265-1273), and its deep link is `?cycle=` (CYCLE_FACET, page.tsx:119), the same noun the Dashboard uses (CYCLE_PARAM vs RUN_PARAM, page.tsx:91-92). Nothing on that half calls a cycle a run.
Criterion 2 — one vocabulary, and it is the Dashboard's. Rendered the Dashboard the same way (`/` 200): its cycles table heads STARTED | OUTCOME | APPLIED | ESCALATED | ERRORS | ERROR. Every overlapping column now matches word for word.
Criterion 3 — FACTS EXAMINED. Every visible label on the page containing "FACTS" is that one string; the gauge and the header are no longer two spellings.
Criterion 4 — the test really guards, proved by breaking the code, not by reading it (detached worktree, primary checkout untouched, no stash):
  - hand-added column labelled `checkpoint_after` -> "heads every column in one vocabulary" FAILS, naming it ("checkpoint_after: expected 'raw column name' to be null"). headerFault is applied PER HEADER, so an underscore anywhere fails regardless of the other twelve being clean.
  - gauge label changed to "Facts seen" -> the gauge/header tie FAILS ("expected [...] to include 'FACTS EXAMINED'"). Not vacuous.
  - CYCLE_COUNTER_LABELS really is a compile guard: renaming claims_rerejected in the row type + CYCLE_COUNTERS gives `error TS2561 ... Did you mean 'claims_re_rejected'?` at page.tsx:336; ADDING a ninth counter gives `TS2741: Property 'payloads_skipped' is missing`. It cannot silently fall back to a raw name.
Hooks — parsed every data-* attribute out of the before and after file: 58 vs 59 occurrences, REMOVED none, and the one addition is the string `data-cycle-count` inside the new doc comment (page.tsx:320); the single render hook at :419 is unchanged. No offline or live selector moved. tests/live/cycles.live.test.ts + runs.live.test.ts: 14 pass against staging. tests/offline/runs/page.test.ts: 25 pass. Stored checks green in the receipt.
No staging write was made — this ticket touches a read-only surface, so there was nothing to write and no residue to sweep.

RESIDUAL (not a bug, no path reaches it): headerFault's `/\brun\b/i` misses the plural. I injected `label: "runs"` on the cycles table's identifier column and all 46 tests still passed. It is not a raw column name, so it sits outside criterion 4's guarantee; it becomes a ticket only if someone proposes a cycles-table header containing "run".

FILED: BUG-0064 (P2, M1, discovered-from qa:BUG-0044) — the ADAPTER RUNS table on the same page still heads SOURCE | STARTED_AT | ENDED_AT | OUTCOME | ERROR_SUMMARY | RECORDS_PARSED | CLAIMS_EMITTED | RECORDS_UNLINKED | FAILURE_CLASS, so one screen now heads `error_summary` both ERROR and ERROR_SUMMARY, and the Dashboard already heads the same runs columns SOURCE | STARTED | OUTCOME | ERROR. The builder called that carve-out a ruling; it is not one — DECISIONS.md:445 settles the column SET and its "verbatim" qualifies the error VALUE, and runs.ts:18 says so. It is out of THIS ticket's scope ("that one table") and does not bounce it. Pinned by a strict xfail in tests/offline/runs/page.test.ts, watched red first ("expected [ 'started_at', 'ended_at', ...(5) ] to deeply equal []") and proved strict by de-underscoring runColumns() in the probe tree ("Expect test to fail"). [resolution: fixed] [receipt green: 3 cmd(s), tree 375d791d]
