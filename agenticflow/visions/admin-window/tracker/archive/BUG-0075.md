---
id: BUG-0075
type: bug
status: done
milestone: M1
priority: P2
opened_by: qa
parent: null
discovered_from: BUG-0066
children: []
assignee: null
claimed_at: null
depends_on: []
touch_scope: [src/components/EditableCell.tsx, tests/offline/ui/editable-cell.test.ts]
force: false
attempts: 1
resolution: fixed
---
# Edit cell: a previous save's 1.5s confirmation timer erases the NEXT edit's status — the in-flight 'saving…' vanishes mid-write and a refusal self-erases after 1374ms

## Description
Expected: a status the cell shows belongs to the edit that produced it — the in-flight statement BUG-0066 just landed stands until ITS OWN write answers, and a refusal stands until the operator does something about it. src/components/EditableCell.tsx:229-230 arms `timer.current` on a SUCCESSFUL save (`setTimeout(() => setStatus({kind:"idle"}), 1500)`) and clears it in exactly one place — the next successful save. The `saving` branch (:210), the `failed` branch (:232-234) and the button's own `onClick` (:270-272) never clear it, so a timer armed by an EARLIER save later resets an UNRELATED status to idle.

Found, three measurements on the production build of the landed tree (next build && next start --port 8796, staging data, minted cookie, bundled Chromium, in-page MutationObserver + fetch wrapper for the clock), 2026-09-03, groups 15ff6af9-178a-4f44-ae11-8bdc0c11b296, field short_name:

(1) THE IN-FLIGHT STATEMENT IS ERASED MID-WRITE. Edit 1 (120ms write) lands: 'saved' at +167ms, timer armed for +1667ms. Edit 2 committed at +226ms with a 1.6s write:
    +  229ms  button 'admin-window/BUG-0066 qa p5-second' disabled=True   cell:[status] 'saving…'
    + 1671ms  button 'admin-window/BUG-0066 qa p5-second' disabled=False  cell:(no region in cell)
    + 1834ms  << PATCH answered 200 >>
  For the last 163ms of a write still in flight the cell states no work and the button is enabled again — the exact state BUG-0066 was filed to remove, reached from a second edit instead of a first. The silent window is `1500ms - (gap between the previous save and this commit)`, bounded by the write's own length: commit 200ms after the confirmation on a 3s Railway write and the cell is silent for ~1.3s of it. Re-measured at 8ms in a second run (qa-attack2.py P7), so the width is timing, not the presence of the defect.

(2) A REFUSAL SELF-ERASES, AND THE ROUTE'S OWN WORDS ARE LOST. Edit 1 writes 200 ('saved' at +211ms, timer armed for +1711ms); edit 2 at +331ms is answered 403 with the route's refusal shape `{"error":"short_name is not editable on groups"}`:
    +  341ms  button 'admin-window/BUG-0066 qa p8-A' disabled=False  cell:[alert] 'short_name is not editable on groups'
    + 1715ms  button 'admin-window/BUG-0066 qa p8-A' disabled=False  cell:(no region)
  READABLE FOR 1374ms, then gone. The field has already reverted to its pre-edit value, so what an operator who looked away sees is a cell holding the old value and NOTHING saying their edit was refused — indistinguishable from never having typed. The same refusal raised OUTSIDE a confirmation window persists indefinitely (measured, offline commit: alert still present at +3000ms), which is the correct behaviour and the reason this is a bug and not a design.

(3) It is not tied to a transport: (2) is a real 403 through the app's own client; the offline variant ('Failed to fetch') behaves identically. No lost update was reachable — the racing third commit in qa-attack2.py P7 ordered correctly (PATCHes [200,200,200], cell, database and a reload all read 'p7-C'), so this is a statement-of-work defect, not data loss. That is why it is P2 and not P1.

PRE-EXISTING, not a regression from BUG-0066: `git show a412e9e` touches only the status RENDERING (type export, EditStatus, the render swap); the timer lines are untouched and date from TASK-0004. BUG-0066 met its own bar on the single-edit path and closed on it; this is the second edit.

REPRO (self-contained, restores the row in a finally, sweep after):
  1. Launch a production-like walk instance per STACK.md §5 and mint a cookie into agenticflow/tracker/evidence/BUG-0066/cookie.json.
  2. ( set -a && source .env && set +a && agenticflow/.venv-tools/bin/python agenticflow/tracker/evidence/BUG-0066/qa-attack.py http://localhost:<port> )   → phases P5 and P6 print measurement (1).
  3. ...qa-attack3.py <base> → prints measurement (2) with the readable-for number. instrument.js beside them is the in-page clock; db.mjs reads/restores the one field independently of the app.
  By hand, without scripts: open /records/groups/<id>, edit short_name and press Enter, wait for the green 'saved', and within ~1s click the same cell, type again and press Enter with the network throttled (CDP latency ≥1500ms) — the 'saving…' vanishes and the button re-enables before the PATCH answers.

PINNING, honestly: not pinnable in the offline suite as the component stands — the same wall BUG-0060 and BUG-0066 hit, and for the same reason (tests/offline is environment node with renderToStaticMarkup, no jsdom, STACK.md §4). The timing rule lives in `commit()`'s internal state and no exported unit expresses it. The fix should extract it the way the two before it did, and the criteria below make that the bar rather than a suggestion.

COLLISION: BUG-0069 (focus return) is open on the same file and the same in-flight window. Land order is the dispatcher's; whoever goes second rebases.

## Acceptance criteria
A status the cell is showing is cleared only by the edit that produced it. Concretely, both measured divergences die: (a) a second edit committed inside a previous save's 1.5s confirmation window keeps its statement of work, and keeps its button disabled, for the whole of its own write — no silent, clickable window before the PATCH answers, at any interval between the two edits; (b) a refusal raised inside that window is not erased by the earlier save's timer (measured: readable for 1374ms, then gone), and persists as long as one raised outside such a window does. The confirmation itself must still clear ~1.5s after ITS OWN save, the failure must still name the refusal and revert the field, and the in-flight statement BUG-0066 landed must still appear within 100ms of a commit. THE TIMING RULE IS RENDERED THROUGH A PURE SEAM, as EditStatus and EditField were and for the same reason (tests/offline is environment node with renderToStaticMarkup and no jsdom, STACK.md §4): a rule that lives only inside commit()'s closure is a rule no offline test can reach. Shape is the builder's — a pure reducer over (status, event), an explicit status-with-deadline unit, whatever fits — and tests/offline/ui/editable-cell.test.ts drives it directly to pin that a later status is never retired by an earlier save's clock. Watch those tests fail against today's code before you fix it. Assert the BEHAVIOUR — which status is showing, and until when — never the sentence.

## Checks
```sh
npx vitest run --project=offline tests/offline/ui/editable-cell.test.ts
./node_modules/.bin/tsc --noEmit
npm run lint
```

## History
- 2026-09-04T05:20:09Z [qa] opened: Edit cell: a previous save's 1.5s confirmation timer erases the NEXT edit's status — the in-flight 'saving…' vanishes mid-write and a refusal self-erases after 1374ms
- 2026-09-04T05:25:51Z [builder-106] claimed (attempt 1)
- 2026-09-04T05:37:47Z [builder-106] claimed → built: - WHAT: EditableCell now carries the edit's ordinal in its status. New pure seam (exported, offline-drivable): `EditState`/`EditEvent`/`reduceEdit` + `confirmationDelayMs`. An `elapsed` clock retires a confirmation only if that same edit's save armed it; a refusal is on no clock at all; a superseded write's answer does not overwrite a newer statement; reopening the cell never speaks over a write in flight. The component arms the clock in a useEffect keyed on the state, so React tears it down on every change and on unmount — the `timer` ref is gone, so there is no call site left to forget to clear (that was the bug's shape).
- PROVED. Red first: with only reduceEdit's `elapsed` branch degraded to today's rule ("any clock retires whatever is showing") 5 of the new tests fail, incl. both measured divergences; restored -> 29/29 green.
  Browser, production builds, staging, minted cookie, bundled Chromium, in-page MutationObserver + fetch clock, groups 15ff6af9-178a-4f44-ae11-8bdc0c11b296 / short_name, 2026-09-03 (agenticflow/tracker/evidence/BUG-0075/measure.py <base> before|after):
  BEFORE (7c3207f, :8791) M1 second edit committed 200ms into the window, its write 1967ms: cell silent AND button enabled from +1293ms = 674ms of a live write. M2 403 inside the window: refusal readable 1320ms, then erased (QA measured 163ms / 1374ms; width is timing).
  AFTER (77ede05, :8792) M1 write 1848ms: statement of work and disabled button held to the answer — no silent, clickable window at all; its own confirmation cleared 1504ms after its own save. M2 refusal still on screen at +4000ms (>3956ms).
  Suites on the rebased tree: offline ui+records 277/277, tsc, lint, `npm run test:http` 13/13, `tests/live/edit.live.test.ts` 7/7, residue sweep clean (56 cols, groups.short_name=0). Writes were groups.short_name stamped `admin-window/BUG-0075`, original 'Tuzi' restored in a finally.
- QA TRAPS: (1) reduceEdit returns the SAME state object for a no-op on purpose — that is what makes useReducer bail out and leave a running clock alone; a deep-equal-but-new object would silently re-arm it. (2) `editing` deliberately does NOT clear a `saving` status. (3) BUG-0069 (focus return) is untouched, as instructed. (4) Only src/components/EditableCell.tsx + tests/offline/ui/editable-cell.test.ts changed.
- 2026-09-04T05:38:19Z [dispatcher] landed ticket/BUG-0075 onto run/admin-window as 6575d09
- 2026-09-04T05:38:56Z [qa] built → qa: under attack
- 2026-09-04T05:55:10Z [qa] qa → done: attacked: criteria re-run on the landed tree first; one integrated attack over the pure seam, the effect-keyed clock, and a real browser before/after.

SEAM (offline, +16 tests appended to tests/offline/ui/editable-cell.test.ts, commit 1e91868). The suite fired `elapsed` by hand, which pins the reducer but not the rule the defect lived in: WHICH clock is running, and for how long. `driveCell` composes reduceEdit with confirmationDelayMs over a virtual clock and runs every timeline TWICE — once with the effect's teardown (today's arming), once with a LEAKED clock (the shape the bug had) — so the pins hold against a straggler timeout, not merely against a reducer call. Timelines: second edit inside the window at every gap 0..1600ms (whole live write sampled at 25ms), third rapid edit, refusal at +60s, refusal-then-success, unmount mid-write, straggler events during a running confirmation. Watched red first: restoring the old `elapsed` rule ("any clock retires whatever shows") reddens 11 tests incl. both measured divergences; returning a new-but-equal state for a superseded answer (which silently re-arms the clock — the builder's own trap 1) reddens the straggler test, and nothing else in the suite catches that one. Source restored after each sabotage (git diff src/ empty).

BROWSER (production build of the landed tree, detached worktree under evidence/, :8810, staging + minted cookie, MutationObserver + fetch clock, groups 15ff6af9.../short_name; evidence/BUG-0075/qa-attack.py <base> before|after|dev).
BEFORE 7c3207f: second edit committed 120ms into the window, write 2081ms — cell silent AND button enabled from +1373ms = 708ms of a live write; a REAL 403 from the route ("created_at is not an editable field of groups": the request's field was rewritten, so the words are the server's) readable 1197ms then erased, 0 alerts left; third rapid edit 691ms silent/clickable.
AFTER (landed): no silent instant and no enabled instant anywhere between commit and answer (write 2049ms); refusal still on screen at +5000ms (>4801ms) with the field reverted beside it; third-edit case clean too, PATCHes [200,200,200], cell = DB = p3-C. In-flight statement 1ms after the commit (bar 100ms). Own confirmation cleared 1505ms after its own answer; lone save 1502ms (before: 1503ms). Refusal-then-success: confirmation cleared 1505ms after ITS save, 0 alerts left.
Dev server (React strict-mode double effects) identical: no silent window, refusal >4768ms, confirmation 1506ms — the clock is not double-armed.
Unmount mid-write: client-side nav to /browse 400ms into a 2.2s write — zero pageerrors/console errors, no stray live region on the new page, the write still landed. Double-submit: one Enter = one PATCH; Enter + immediate click elsewhere = one PATCH.
BUG-0069 untouched: focus is INPUT while editing and BODY after the commit on BOTH builds, identical.

SUITES on the landed tree: offline 1950 + isolated 6, offline ui+records 293, tests/live/edit.live.test.ts 7/7, npm run test:http 13/13, tsc, lint. Writes: groups.short_name only, stamped `admin-window/BUG-0075 qa`, original 'Tuzi' restored in a finally after each of three runs; residue sweep clean (56 columns, short_name=0). Only my own pids killed; worktree removed.

RESIDUAL, no path reaches it (not a ticket): commit() reverts `shown`/`draft` on failure BEFORE dispatching `settled` and without the reducer's supersession check, so a superseded write's failure would still revert the displayed value while its status is correctly ignored. Unreachable today — the button is disabled for the whole of `saving` and one Enter yields exactly one PATCH (measured) — it becomes real only if a second commit can start under a live write (a future Save control, or focus-return work). [resolution: fixed] [receipt green: 3 cmd(s), tree b2b88709]
