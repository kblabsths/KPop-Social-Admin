---
id: DEP-0001
type: dep
status: done
milestone: M1
priority: P1
opened_by: architect
parent: null
children: []
assignee: null
claimed_at: null
depends_on: []
touch_scope: [agenticflow/docs/ALLOWED_DEPS.md]
force: false
resolution: fixed
---
# Test runner: vitest + vite-tsconfig-paths

## Description
The repo ships no test framework (ALLOWED_DEPS.md, 'Product shape, for the record'), and acceptance test 1 requires a campaign test suite that is offline by default with staging tests behind an explicit live marker. STACK.md section 4 chooses **Vitest**.

Requested, dev-dependencies only:

- `vitest` (latest 3.x) — the runner. One of the four Next.js documents itself (`node_modules/next/dist/docs/01-app/02-guides/testing/vitest.md`, read 2026-09-01); runs TypeScript/ESM natively and needs one config file. Jest would additionally need `next/jest`, transform config and CJS/ESM mediation for the same result.
- `vite-tsconfig-paths` — so tests resolve this repo's `@/*` alias from `tsconfig.json` instead of hand-written relative paths (ARCHITECTURE section 1.2 bans `../` entirely).

**Deliberately NOT requested** — do not add them: jsdom, @testing-library/*, @vitejs/plugin-react, Playwright, Cypress, any UI kit, any ORM, any Postgres driver. Next's own guide says Vitest cannot render async Server Components; this campaign's answer is architectural (ARCHITECTURE section 5: the page function is the only async component, so a test renders it with `react-dom/server`, already a dependency). Browser screenshots are the walk agent's kit-owned Playwright, not a product dependency.

Install target: `devDependencies` in this repo only. Nothing is installed until this ticket lands the entries in `agenticflow/docs/ALLOWED_DEPS.md`.

## Acceptance criteria
`agenticflow/docs/ALLOWED_DEPS.md` carries a vetted (not grandfathered) entry for `vitest` and `vite-tsconfig-paths`, each with its version range, purpose, date and this DEP id. Identity, health and install-script posture are checked as normal — the intake note records that only two packages in the current 472-package tree carry install scripts, so a new install-script package arriving with these is a finding to state. No other package is added.

## Checks
```sh
bash -c "grep -q '^- vitest ' agenticflow/docs/ALLOWED_DEPS.md"
bash -c "grep -q '^- vite-tsconfig-paths ' agenticflow/docs/ALLOWED_DEPS.md"
```

## History
- 2026-09-02T05:34:20Z [architect] opened: Test runner: vitest + vite-tsconfig-paths
- 2026-09-02T05:53:17Z [toolsmith] open → done: vetted: APPROVED both, dev-only, pinned. vitest (^3.2.7) and vite-tsconfig-paths (^6.1.1) are now the file's first vetted (not grandfathered) entries. Necessity: node:test cannot run TS on node>=20 without extra loader deps, so the stdlib route costs more surface, not less. Identity: registry->repo verified (vitest-dev/vitest 17k stars pushed 2026-09-01; aleclarson/vite-tsconfig-paths 1.6k stars pushed 2026-08-28, MIT); ~99.9M and ~30.5M downloads/week; no near-name collision with any existing entry (checked with supply_gate.py's own allowed_entries/near_matches, both return []). Advisories: both critical Vitest CVEs (GHSA-9crc-q9x8-hgqq, GHSA-5xrq-8626-4rwp) are patched below the approved floor 3.2.7; both concern the Vitest UI/API server, so @vitest/ui stays out and needs its own DEP. Neither requested package nor vite has an install script. FINDING the ticket asked for: a third install-script package DOES arrive -- vitest 3.x -> vite 7.x -> esbuild, whose postinstall runs 'node install.js' to place a platform binary; intake's count of two (sharp, unrs-resolver) becomes three. Judged acceptable (esbuild's standard, widely-audited binary step) but recorded so a FOURTH is a fresh finding. SECOND FINDING: both packages carry caret ranges on 'debug' that span debug@4.4.2, the hijacked-account malware release (GHSA-4x49-vf9v-38px); this repo's lockfile already pins debug 4.4.3, so install lockfile-faithfully and confirm debug did not move to 4.4.2 afterwards. Residuals recorded in the file: vite-tsconfig-paths has a single npm maintainer and 6.1.1 ships without provenance (vitest@3.2.7 has an SLSA attestation), and vite 7 wants node ^20.19 while engines says >=20.0.0. Ranges are the vetting: 3.x/6.x only -- vitest 4.x (different transitive set: vite 8, rolldown, obug) and vite-tsconfig-paths 7.x (alpha, oxc rewrite) are unvetted and each need a new DEP. Tarballs were NOT unpacked: the gate blocked the archive fetch, correctly, and I did not retry -- evidence is registry metadata, GitHub repos and advisory DBs. PROPOSED for BLOCKED_DEPS.md (human-only, I cannot add): vitest-tsconfig-paths (exists on npm, unrelated, last published 2022), vite-test (exists, unrelated, 2021), vite-tsconfig-path (singular, unregistered), vite-tsconfig (name exists with no published version). No other package added; no deadline pressure was present in the ticket. [resolution: fixed]
