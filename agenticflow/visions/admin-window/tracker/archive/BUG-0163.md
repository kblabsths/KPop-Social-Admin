---
id: BUG-0163
type: bug
status: done
milestone: M2
priority: P2
opened_by: qa
parent: null
discovered_from: BUG-0160
children: []
assignee: null
claimed_at: null
depends_on: []
touch_scope: [src/app/claims/page.tsx, src/components/ui/window-line.tsx, src/lib/gauges/pending-claims.ts, tests/offline/claims/page.test.ts]
force: false
attempts: 1
resolution: fixed
---
# /claims: the gauge's own window line names no narrowing, so ?domain= moves every figure on that card under a sentence that says it read the whole table

## Description
Found attacking the landed BUG-0160 fix (qa, 2026-09-10; production build on :8785 against staging, minted cookie, server killed after; commit 3383965 on run/admin-window).

BUG-0160 gave the LIST's window line and the bucket caption the words for a `?domain=` narrowing. The gauge section on the same page — whose read `gaugeFilter` (`src/app/claims/page.tsx:1110`) narrows with the SAME facet — was not touched, and it renders a `ReadWindow` through `WindowLine`'s scan arm (`src/components/ui/window-line.tsx:455-470`), which has no `scope` at all. So every figure on that card is narrowed and its sentence says it read the whole table.

**Measured on staging, the gauge section's own window line (`[data-surface="gauge"] [data-window="pending"]`), byte-identical in all three states:**

    Claims observed since 2026-06-12 20:12 UTC, read to 2026-09-10 20:12 UTC
    — a window of at most 1,000 rows, not the whole table.

| URL | data-window-held | "Claims in this window" | sub |
| --- | --- | --- | --- |
| `/claims` | 877 | 877 | 1 source, 2 domains |
| `/claims?domain=events` | 849 | 849 | 1 source, 1 domain |
| `/claims?domain=groups` | 0 | 0 | 0 sources, 0 domains |

Expected: the sentence stating the window a figure came from names the narrowing that read carried — the rule the list's line one section up now follows ("849 claims in the events domain in all…") and the rule `DrawnWindow.scope` exists for (`window-line.tsx:96-131`, BUG-0114).
Found: `src/app/claims/page.tsx:619-623` renders `<WindowLine gauge={PENDING_WINDOW} window={gauge.window} measured="Claims observed" />` — the scan arm takes `measured` and no scope — so the same 122-character sentence stands over three different reads, and the facet has NO chip row to read it off (`CHIP_FACETS`, BUG-0138). `?source_id=` is narrowed there too and equally unnamed, but its chip shows on screen; `?domain=` shows nowhere on that card.

Worst instance: `/claims?domain=groups` renders "Claims in this window 0", "Buckets holding claims 0 of 5" and, in the age distribution, the NOT-NARROWED emptiness — "No claims in this window / A claim the resolver cannot apply yet appears here, and its wait is measured." — blaming the database for a zero a URL facet produced (LESSONS 3, the two-fact rule `claimsNarrowed` already applies to the list and the bucket table on this page).

Repro:
1. `( set -a && source .env && set +a && SUPABASE_URL="$STAGING_SUPABASE_URL" SUPABASE_SERVICE_ROLE_KEY="$STAGING_SUPABASE_SERVICE_ROLE_KEY" AUTH_URL="http://localhost:8785" npm run build && SUPABASE_URL=... npm run start -- --port 8785 )`
2. `node tests/walk/session-cookie.mts`, add the cookie, GET `/claims` then `/claims?domain=events` then `/claims?domain=groups`.
3. Read `[data-surface="gauge"] [data-window]` on each: identical text, `data-window-held` 877 / 849 / 0.

Pinned RED by `tests/offline/claims/page.test.ts` > "the gauge's window line" > "says what narrowed its own read, where no chip row can say it" (a strict `it.fails` on the run branch — flip it to a plain `it(...)` when you fix it). Its non-vacuity leg reads the query the page actually built: the observations read carried `.eq("domain","venues")`, and the sentence over the figures names nothing.

Not BUG-0161 (the dead-end chips) and not BUG-0162 (the "reach the rest" clause): this is the gauge surface's own sentence.

## Acceptance criteria
1. The gauge section's window line states the narrowing its own read carried: with `?domain=<value>` set, the rendered text of `[data-surface="gauge"] [data-window]` names the domain facet and its value, in the app's own words and in the one spelling `unchippedNarrowings` already owns (`src/lib/claims/filters.ts`) — never a second spelling.

2. It is said exactly where the read carried it: an unnarrowed page renders the sentence it renders today, to the byte, on both tabs; a `?source_id=` narrowing is named on the same rule (its read carries it too).

3. An emptiness the URL produced is not explained as an emptiness the database produced: where the gauge's figures are zero UNDER a narrowing, the card's words say a narrowing emptied it (the two-fact rule of `lib/url/narrowing.ts`, as the list and the bucket table already apply it).

4. Nothing else on the page moves: the list's window line, the bucket caption, the chip rows, every `data-*` hook and every other page's window line stay as BUG-0160 left them.

5. `tests/offline/claims/page.test.ts` > "the gauge's window line" > "says what narrowed its own read, where no chip row can say it" is a plain `it(...)` and green.

## Checks
```sh
cd "/Users/ben-m4/Desktop/Coding/KPOP/kspace Admin" && ./node_modules/.bin/vitest run --project=offline tests/offline/claims/page.test.ts tests/offline/claims/filters.test.ts tests/offline/ui/copy.test.ts tests/offline/absence/pages.test.ts
cd "/Users/ben-m4/Desktop/Coding/KPOP/kspace Admin" && grep -q "says what narrowed its own read, where no chip row can say it" tests/offline/claims/page.test.ts
cd "/Users/ben-m4/Desktop/Coding/KPOP/kspace Admin" && bash -c "rm -rf .next/types && npm run lint && ./node_modules/.bin/tsc --noEmit"
```

## History
- 2026-09-10T20:16:25Z [qa] opened: /claims: the gauge's own window line names no narrowing, so ?domain= moves every figure on that card under a sentence that says it read the whole table
- 2026-09-10T20:31:34Z [builder-243] claimed (attempt 1)
- 2026-09-10T20:55:49Z [builder-243] claimed → built: WindowLine's scan arm now takes a `scope` (through the same `population` helper the drawn arms use), /claims composes it ONCE from the object gaugeFilter hands the read, and the gauge's empty cards take the two-fact rule over the GAUGE's own population — `readClaimCountSince`, a bounded head count of this tab's claims inside the same window with no facet, issued only where a facet can narrow that read.

Verified: `vitest run --project=offline tests/offline/claims/page.test.ts tests/offline/claims/filters.test.ts tests/offline/ui/copy.test.ts tests/offline/absence/pages.test.ts` (514), full offline 3367 passed, lint + tsc clean — all re-run after rebase onto 5043813. Production build on :8785 against staging, minted cookie, server killed after: /claims 'Claims observed since …' held=877 (byte-identical to the landed line); ?domain=events 'Claims observed in the events domain since …' held=849; ?domain=groups held=0 with the card 'No claims in this window in the groups domain / The 'clear filters' chip above clears every filter in this URL, including domain…'; ?source_id= 'matching these filters'; ?bucket=escalated byte-identical to bare (that facet is not in the gauge's read) while the list goes to 0. Each new test watched RED first by sabotaging its behaviour (fact 2 from the whole-view count, scope from the page-wide filter, gaugeEmpty disabled).

Three things to know:
1. /sources' TWO scan window lines (`readAwaitingRowTrend({filter})`) carry a query narrowing and still name nothing — the same defect one page over, NOT fixed here because criterion 4 pins every other page's line; that is why the scan arm's `scope` is optional, documented in window-line.tsx. Worth its own ticket.
2. /claims?tab=standing&domain=groups renders the DATABASE's words on the per-source card, and that is the rule working: staging's standing bucket holds 0 in-window with no facet either (bare ?tab=standing renders the identical card), so no facet is blamed.
3. Outside touch_scope: src/lib/db/claims.ts (+readClaimCountSince, +gte on ClaimQuery) and src/components/gauges/state.tsx (EmptyWords widened string→ReactNode, so a card can set a facet value in the identifier face). lib/gauges/pending-claims.ts NOT touched — a count read there is forbidden by tests/offline/gauges/bounded.test.ts. One landed test was updated, not weakened: 'narrows every count and the window server-side' now asserts ONE unnarrowed unwindowed count AND ONE unnarrowed windowed count.
- 2026-09-10T20:56:12Z [dispatcher] landed ticket/BUG-0163 onto run/admin-window as ba0105c
- 2026-09-10T20:56:32Z [qa] built → qa: under attack
- 2026-09-10T21:10:02Z [qa] qa → done: Attacked on the landed tree (ba0105c), not the builder's story: ran the three stored checks first (515 offline pass, lint+tsc clean), then drove a production build against staging on :8785+1 (port 8786; server killed, walk files swept).

STAGING, the ticket's own three states, [data-surface="gauge"] [data-window]: /claims 'Claims observed since 2026-06-12 21:04 UTC, read to …' held=877 (byte-identical to the pre-fix line the ticket quotes); ?domain=events 'Claims observed in the events domain since …' held=849; ?domain=groups '…in the groups domain…' held=0 with the card 'No claims in this window in the groups domain' + the clear-filters exit. Criteria 1-3 met at the reported repro.

Where I tried to break it and could not:
- facet the read does NOT carry: ?bucket=escalated and ?tab=standing&bucket=escalated leave the line byte-identical to bare, held 877 — and ?domain=events&bucket=agreeing names ONLY the domain (849) while the list names both.
- ?source_id=<uuid> 'matching these filters' (one spelling, the app's own NARROWED_BY_FILTERS); combined with ?domain= it composes 'in the events domain, matching these filters' — the same phrase pair the list's line renders on the same render.
- two-fact, both directions, graded against the DATABASE not the page: read-only counts on staging give pending_claims 877 in window, domain=groups 0, bucket=standing_disagreement 0. So ?domain=groups must blame the facet (it does) and ?tab=standing&domain=groups must NOT (it renders the identical card to bare ?tab=standing — the database's words). Offline, the window-empty-for-everyone case renders the unfaceted words under a facet.
- hostile values: '<img src=x onerror=alert(1)>' renders escaped as text (no raw tag in the HTML); '  events  ' canonicalises so the sentence says exactly what the .eq() carried; a 4,000-char domain is dropped and the sentence then names nothing (read carried no eq — sentence and query agree); ?domain=events&domain=groups takes the first and says 'events' (849).
- refusal path: with the new windowed count erroring, the gauge keeps its window line, figures and cards and the refusal is published beside them naming pending_claims — untested by the suite, so I added ONE test for it in tests/offline/claims/page.test.ts (515 now), committed 878dde0.
- truncation: a window at the 1,000 cap renders scope AND floor clause in one sentence.
- cost: no added depth (the count rides the same Promise.all); /claims 2.24-2.48s warm, narrowed no slower than bare. /sources, /queues, /cycles, / all 200 with their window lines unmoved (criterion 4).

Criterion 5: the pin is a plain it(...) and green. Receipt GREEN on this tree.

Residuals (handoff, not tickets): (1) readClaimCountSince carries gte only — no upper bound — while the scan it is fact 2 for is [since, until]; measured 0 rows with observed_at > now on staging, so unreachable today; a source stamping a future instant would make it reachable. (2) That count is exact and uncapped while the scan it is compared against stops at ROW_CAP=1000; staging's window is at 877/1000, so past the cap a cap-emptied window under a facet would be reported as facet-emptied. (3) /sources' two scan lines still name the narrowing their read carries (builder flagged it too) — worth its own ticket. (4) the offline fixture claimView ignores gte entirely, so only the one hand-scripted answerer exercises the window bound — which is why I drove this over HTTP. [resolution: fixed] [receipt green: 3 cmd(s), tree 07f2d315]
