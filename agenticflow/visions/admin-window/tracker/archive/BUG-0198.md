---
id: BUG-0198
type: bug
status: done
milestone: M3
priority: P3
opened_by: designer
parent: null
discovered_from: designer:endgame-walk
children: []
assignee: null
claimed_at: null
depends_on: []
touch_scope: [src/components/ui/paging.tsx, src/components/claims/paged-claim-list.tsx, src/components/browse/paged-browse-table.tsx, tests/offline/ui/paging.test.ts]
force: false
siblings: [BUG-0196]
attempts: 1
resolution: fixed
---
# The bound-ceiling sentence tells /browse's operator to narrow a view /browse offers no way to narrow

## Description
**The sentence.** `PageMore`'s bound-ceiling arm (`stoppedAtTheCeiling`,
`src/components/ui/paging.tsx:483`) ends:

> This app serves no bound past 100,000 {holds}, so paging stops here and not at
> the end of the set. **Narrow the view and page the smaller set.**

The noun is per-surface — `/browse` correctly reads "events" — but **the next
step is not**, and `/browse` offers nothing to narrow with. Its only parameter
is `columns`, which the page's own comment states *"chooses which COLUMNS render
and never which rows are read"* (`src/app/browse/page.tsx:218-221`, `scope: null`).
I confirmed it on the landed tree at 1440x900: `/browse` renders one chip row —
the column selector — and no facet of any kind; nothing in that URL removes a
row. So on the one surface where the arm's noun is "events", its instruction
tells the operator to do a thing the page does not offer.

**What it fails.** LOOK_AND_FEEL → The Voice → copy bar 3: *"Every error names
what failed, then what to do, with no apology"* — the "what to do" half is the
bar, and a next step the surface cannot honour does not meet it; it is the
wishful fix LESSONS 1 names. And the Register: *"plain, specific"* — advice the
page cannot take is neither. On `/claims` the same sentence PASSES: that surface
has `bucket` and `source_id` chips and narrowing really is how it is worked.

**Reachability, stated honestly, because it decides the priority and not the
verdict.** The arm draws only at `MAX_PAGE_OFFSET + SIZE` = 100,050 rows.
Read-only staging holds **120 events** against 877 claims, so `/browse`'s ceiling
is three orders of magnitude away and no walk, criterion or receipt can drive
that surface into this arm on this data — QA reached it only through its own
`PagingProvider` + `PageMore` probe at a typed `held`. I graded it the way the
architect's M3 walk note asked: against the Voice bars on the reachable surface
(`/claims`, where it passes) and on the component reading for `/browse` (where
it does not). **P3 because it is unreachable, filed because it is wrong.**

**The two ways out, and what each costs** (measured, so the ticket is not
scoped short):

- **A next step true of every surface, or none at all.** Cheapest: no prop, no
  wrapper change, `src/components/ui/paging.tsx` and its test only. The arm's
  other two sentences already stand on their own — they name what stopped the
  paging and that it is not the end of the set, which is copy bar 3's first
  half in full.
- **A per-surface next step.** Needs a new `PageMore` prop (a next-step string,
  or a "this surface can be narrowed" flag) threaded from **both** call sites,
  `src/components/claims/paged-claim-list.tsx:94` and
  `src/components/browse/paged-browse-table.tsx:165` — both of which were
  outside BUG-0178's touch scope, which is exactly why its builder left the gap
  rather than widening.

Both call sites are in this ticket's touch scope so either road is open to its
builder; **a fix that leaves `/browse` telling the operator to narrow an
unnarrowable view is not a fix, whichever road is taken.**

## Acceptance criteria
1. The bound-ceiling arm no longer instructs the operator of a surface to narrow
   a view that surface offers no way to narrow. Asserted on the `/browse`
   wrapper's own reading of `PageMore` at a `held` past
   `MAX_PAGE_OFFSET + SIZE`, in `tests/offline/ui/paging.test.ts`.
2. Copy bar 3's first half is untouched: the arm still names what stopped the
   paging — this app, and its own `MAX_PAGE_OFFSET`, imported and rendered
   through the app's thousands-separated count — and still says, in so many
   words, that this is not the end of the set.
3. The arm still draws **no control** (a press cannot be honoured here), still
   asserts nothing about the read, and still does not duplicate the window
   line's closing clause.
4. `/claims`' reading of the same arm still offers a next step its surface can
   honour, whether that is the sentence it carries today or a per-surface one.
5. Whichever road is taken, `MAX_PAGE_OFFSET` is imported and never retyped, and
   no new wording is pinned byte-for-byte in a test that is not this arm's own.
6. The exhausted arm, the loading arm, the idle arm and the refusal are
   byte-identical on both surfaces: this ticket moves one arm's second sentence
   and nothing else.
7. Human-check: drive `/browse` into the ceiling arm through a component
   reading at a typed `held` and read the sentence aloud — every instruction in
   it is something `/browse` actually offers. **(human-checkable)**
8. **Where the next step lives is RULED (architect, 2026-09-11), because the
   two roads this ticket offers must not meet in the middle.** A default next
   step kept inside `PageMore` and overridden by the surfaces that cannot honour
   it is NOT one of the two roads: a surface added later inherits advice nobody
   checked it can take, which is this ticket's own defect with a longer fuse.
   So either the arm carries a next step true of every surface (or none), and
   the narrowing sentence leaves `src/components/ui/paging.tsx` entirely; or the
   next step is SUPPLIED by the surface, and `/claims`' sentence lives at
   `src/components/claims/paged-claim-list.tsx` with `/browse` supplying none.
   Under both roads the literal `Narrow the view and page the smaller set.` is
   gone from `src/components/ui/paging.tsx` — which is what this ticket's third
   check measures.

## Checks
```sh
npm run lint
npm run build
! grep -qF "Narrow the view and page the smaller set." src/components/ui/paging.tsx
./node_modules/.bin/vitest run --project=offline tests/offline/ui/paging.test.ts tests/offline/ui/primitives.test.ts
npm run test:live -- tests/live/browse.live.test.ts
```
## History
- 2026-09-11T15:29:05Z [designer] opened: The bound-ceiling sentence tells /browse's operator to narrow a view /browse offers no way to narrow
- 2026-09-11T15:29:11Z [designer] Sibling note vs BUG-0196 (stamped for sharing src/components/ui/paging.tsx and tests/offline/ui/paging.test.ts): no overlap, no ordering. BUG-0196 moves the paging REFUSAL's machineWrote arm (~:713) between faces and changes no wording. BUG-0198 changes one sentence of the bound-CEILING arm (stoppedAtTheCeiling, :483), a different function, a different state, and touches no face. In paging.test.ts they grade different arms: 0196 the refusal's two runs, 0198 the ceiling's next step on the /browse reading. Whoever lands second rebases on a file it does not contend for.
- 2026-09-11T15:34:36Z [designer] No screenshot: the arm draws only at MAX_PAGE_OFFSET + SIZE = 100,050 rows and read-only staging holds 120 events. What I did measure in the browser, at 1440x900 on f9165d18: /browse renders exactly one chip row — the column selector — and no facet of any kind, and its window line reads 'a window of at most 50, not the whole catalog'. The screenshot of that is agenticflow/tracker/evidence/M3/designer/browse-1440x900.png. The sentence itself is graded from src/components/ui/paging.tsx:483.
- 2026-09-11T15:42:31Z [architect] criteria amended: Designer's 1-7 unchanged; criterion 8 rules out the third road the two named roads could collapse into — a narrowing sentence kept as PageMore's DEFAULT with /browse opting out, which re-files this ticket for whatever surface is added next. Under both sanctioned roads the literal leaves paging.tsx, which is what the amended third check measures. [was: 1. The bound-ceiling arm no longer instructs the operator of a surface to narrow a view that surface offers no way to na… (1404 chars; full text in git history)]
- 2026-09-11T15:42:38Z [architect] checks amended: Third check REPLACED — the designer's signature grep (! grep -qF 'function stoppedAtTheCeiling(holds: string): string {') was defective in both directions, dry-run on d2610aec: it stays RED under the ticket's own cheapest road (change the sentence, no signature change) so a correct fix could not close, and it greens on a bare signature change that fixes no wording. The replacement pins the thing the ticket is about and is road-agnostic under criterion 8: RED today (the literal is at src/components/ui/paging.tsx:486, dry-run exit 1), green only when the narrowing instruction leaves that file. [was: npm run lint, npm run build, ! grep -qF 'function stoppedAtTheCeiling(holds: string): string {' src/components/ui/paging… (295 chars; full text in git history)]
- 2026-09-11T15:58:53Z [builder-298] claimed (attempt 1)
- 2026-09-11T16:11:45Z [builder-298] claimed → built: Road 2 (criterion 8): the next step is SUPPLIED by the surface. PageMore gains a REQUIRED `nextStep: string | null`; the narrowing sentence now lives at src/components/claims/paged-claim-list.tsx as the exported NARROW_THE_VIEW, /browse passes null. No default in paging.tsx, so a later surface inherits nothing.
- Rendered at a typed held of MAX_PAGE_OFFSET+SIZE through the REAL wrappers (criterion 7): /browse reads 'This app serves no bound past 100,000 events, so paging stops here and not at the end of the set.' — one sentence, no instruction; /claims reads the same half with 'claims' plus 'Narrow the view and page the smaller set.' Both arms draw no control.
- Verified on the rebased tree (e9b64230 on 9b94edf1): npm run lint, npm run build, the grep check (exit 0), vitest --project=offline paging.test.ts+primitives.test.ts, npm run test:live -- tests/live/browse.live.test.ts (6 passed), tsc --noEmit clean, and the whole offline suite (80 files / 3925 tests). New tests observed RED first by simulating the ruled-out road (a default kept in PageMore): 4 of the 5 fail, the stutter one correctly does not.
- QA traps: (a) two files OUTSIDE touch_scope each gained one line because the prop is required — tests/offline/paging/machine.test.ts:517 (nextStep: NARROW_THE_VIEW, a /claims fixture) and tests/offline/browse/page.test.ts:1784 (nextStep: null, a /browse fixture); nothing else there moved. (b) NARROW_THE_VIEW is deliberately NOT re-exported from @/components/claims — a constant out of a client module through a server barrel is BUG-0094; client-boundary.test.ts is green. (c) paging.test.ts's own fixtures are a /claims surface, so its file-level NEXT_STEP is that constant; the ceiling arm is the only arm that reads it and the new criterion-6 test proves the other four are byte-identical either way.
- 2026-09-11T16:12:05Z [dispatcher] landed ticket/BUG-0198 onto run/admin-window as 6636e8a1
- 2026-09-11T16:12:46Z [qa] built → qa: under attack
- 2026-09-11T16:19:12Z [qa] qa → done: attacked: criteria re-run on the LANDED tree (6636e8a1) from the source, through my own PagingProvider + real-wrapper probe, before reading the builder's tests.

DROVE BOTH REAL WRAPPERS at held = MAX_PAGE_OFFSET + SIZE = 100,050 (pageBound refuses it; data-paging="limit"; 0 <button> under either):
- /browse (PagedBrowseTable, its own provider + window line): "This app serves no bound past 100,000 events, so paging stops here and not at the end of the set." — ONE sentence, no instruction of any kind (crit 1, 3, 7). Byte-identical to the widget handed nextStep=null.
- /claims (PagedClaimList): the same half with "claims", plus "Narrow the view and page the smaller set." (crit 4) — and equal, to the byte, to `${widget(claims,null)} ${NARROW_THE_VIEW}`, so the surface's sentence is appended verbatim (crit 2, 6).
- Window lines at that state: /browse "…100,050 events are on screen, and events that arrived before them are not shown."; /claims "…the 100,050 longest-waiting are below — the rest are not shown." Neither closing clause contains the arm's terminal sentence nor is contained by it — BUG-0178 crit 9 holds on both real readings.

CRIT 2/5: count(MAX_PAGE_OFFSET) renders "100,000" off the imported constant; grep -rn over src/+tests/ finds the ceiling sentence pinned NOWHERE outside src/components/ui/paging.tsx, and NARROW_THE_VIEW is imported at all three non-source sites, never retyped.

CRIT 6, harder than the builder's: all four other arms (idle, loading, exhausted-agreeing, exhausted-diverged, refused) rendered under BOTH nouns ("claims" and "events") x BOTH answers (constant / null) — 16 renders, byte-identical in every pair; the ceiling arm is the only one that differs. The landed src diff moves no other arm's JSX.

CRIT 8, where the ruling actually binds: the sourceText assertion catches the one default whose wording we know. I added the pin for every other one — tests/offline/ui/paging.test.ts "cannot be drawn by a surface that has not answered the question": a call site OMITTING nextStep must not compile, so no default can be reintroduced under a different sentence. Proved live in both directions on this tree: with the omission, tsc --noEmit exits 0 (the directive is consumed); supply the prop and tsc exits 2 with TS2578 "Unused '@ts-expect-error' directive" at paging.test.ts:1971. ci_command runs tsc, so it is enforced. Committed as cd8861c7. Suite 71 -> 72 tests.

BUG-0094: NARROW_THE_VIEW is NOT re-exported from src/components/claims/index.ts (barrel re-exports PagedClaimList only) and no server module imports it; client-boundary.test.ts green (20 tests, incl. the real server-import resolution).

GREEN in the primary checkout AND again under receipt.py: lint 0, build 0, the grep 0, vitest offline paging+primitives 0, test:live browse.live 0 (6 passed vs staging). Plus tsc --noEmit 0 and 761 tests across paging/claims/browse/copy/client-boundary.

NOT WALKED, stated honestly: the arm draws only past 100,000 rows and staging holds ~120 events / 877 claims, so no browser walk can reach it on this data — everything above is a component drive at a typed held, which is what crit 7 asks for.

RESIDUALS (neither filed; no criterion or receipt reaches either):
1. nextStep = "" (the empty string the type allows) renders a trailing space: "…end of the set. ". Both real surfaces pass a non-empty constant or null, so it is unreachable; it becomes a ticket the day a surface computes its next step instead of naming a constant.
2. /browse's line and arm overlap SEMANTICALLY at the ceiling (both say there is more not shown) while passing crit 9's structural test — already accepted on BUG-0178; window-line.tsx was out of scope there and here.
BUG-0196's Refusal arm: no defect seen from here, nothing re-filed. [resolution: fixed] [receipt green: 5 cmd(s), tree 4b681a45]
