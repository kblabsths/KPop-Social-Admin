---
id: BUG-0036
type: bug
status: done
milestone: M1
priority: P2
opened_by: qa
parent: FEAT-0002
discovered_from: BUG-0035
children: []
assignee: null
claimed_at: null
depends_on: []
touch_scope: [tests/live/parity.ts, tests/offline/live-guard.test.ts]
force: false
attempts: 1
resolution: fixed
---
# stateOf's excluding filter drops a surface's OWN error card, grading an ERROR surface as ok

## Description
Module: tests/live/parity.ts, stateOf's card scan at :232 (`$(element).closest(excluding).length === 0`), reached from gradeSurface:701 via SurfaceGrade.excluding. Authority: ARCHITECTURE §10 rule 6 (an error is a FAIL) and stateOf's own docstring, parity.ts:199-207 — 'excluding names SUB-SURFACES INSIDE within' and 'A block can never say ok while rendering an error line'.

cheerio's closest() starts at the card itself and walks EVERY ancestor — the graded surface and the page wrapping it included — so the filter is not bounded to sub-surfaces inside `within`. An `excluding` selector that also matches the surface itself (or any ancestor of it) drops every card the surface holds, the ErrorLine among them, and dominantKind([]) answers 'ok'.

REPRO (offline, markup rendered from the real ErrorLine primitive):

    const broken = `<div data-surface="q">${errorLine()}</div>`;
    stateOf(broken, '[data-surface]')                    // 'error'  — correct
    stateOf(broken, '[data-surface]', '[data-surface]')  // 'ok'     — the defect
    assertState(broken, '[data-surface]', 'ok')          // returns; nothing thrown

Expected: 'error' — rule 6 is unconditional and the surface carries an ErrorLine; an exclusion may only silence a card belonging to a PROPER DESCENDANT sub-surface of `within`.
Found: 'ok', and assertState/gradeSurface grade the surface green. Measured 2026-09-02 on run/admin-window 3fdc924: 'AssertionError: expected \'ok\' to be \'error\''.

Reachability, stated honestly: no live test triggers it today — the one caller of `excluding` is tests/live/review-item.live.test.ts:102 (`within: [data-evidence-view]`, `excluding: [data-dial]`), which do not overlap. It is a defect in the SEAM, the same class and the same function as admin-window/BUG-0035, and the trigger is one selector: a test that grades `[data-surface="claims"]` while excluding the generic `[data-surface]` (or any wrapper selector matching an ancestor) makes its own oracle permanently vacuous with no red anywhere. USER-VISIBLE PATH: this classifier is the only staging-facing grade in the campaign; a surface it grades 'ok' while the page rendered an error line is exactly the four vacuous /claims passes admin-window/TASK-0032 was opened to remove — a broken page ships green.

Not to be fixed by dropping `excluding`: review-item needs it. Suggested shape (architect's call): confine the exclusion to elements strictly inside `within` — e.g. exclude a card only when an ancestor matching `excluding` is itself a proper descendant of the surface — and/or refuse (MarkupReadError) when `excluding` matches the surface itself.

PINNED: tests/offline/live-guard.test.ts, describe 'QA: the classifier, attacked', `it.fails(...)` naming this ticket — watched RED first as a plain `it`. THE FIX MUST CONVERT THE PIN TO A PLAIN `it` AND LEAVE IT PASSING; while the defect stands it is green as an expected failure, and the day it is fixed it turns red (XPASS) and sends the reader here.

## Acceptance criteria
(to be written)

## Checks
```sh
./node_modules/.bin/vitest run tests/offline/live-guard.test.ts
```

## History
- 2026-09-02T21:40:19Z [qa] opened: stateOf's excluding filter drops a surface's OWN error card, grading an ERROR surface as ok
- 2026-09-02T21:48:27Z [builder-67] claimed (attempt 1)
- 2026-09-02T21:53:24Z [builder-67] claimed → built: - stateOf (tests/live/parity.ts): the card scan now silences a card only when its NEAREST matching ancestor-or-self is a PROPER DESCENDANT of the graded surface (set membership against surface.find(excluding)); closest() alone walked past the surface into the page. Docstring states the bound.
- QA's pin in tests/offline/live-guard.test.ts is a plain passing it; watched RED first as a plain it (AssertionError: expected 'ok' to be 'error') before the fix. Added 'excludes only PROPER DESCENDANTS, never the surface or a wrapper of it' (surface-as-excluding, wrapper-as-excluding, plus empty/not_provisioned bounds) and a nested-sub-surface case on the existing dial test - both watched red against the pre-fix parity.ts, then green.
- Verified on the rebased tree: vitest run tests/offline/live-guard.test.ts (75 pass), npm test (1644 pass, 53 files), tsc --noEmit 0, npm run lint clean.
Trap for QA: I did NOT make a self-matching 'excluding' a MarkupReadError - the pin asserts stateOf(broken,'[data-surface]','[data-surface]') RETURNS 'error', so a refusal would fail it. A useless exclusion is therefore silent; it can no longer be vacuously green, but it also does not complain. assertState() takes no excluding argument at all (only stateOf/gradeSurface do) - a probe passing a 4th arg will not typecheck. Live callers untouched: review-item's within=[data-evidence-view]/excluding=[data-dial] still excludes (dial is a proper descendant).
- 2026-09-02T21:53:39Z [dispatcher] landed ticket/BUG-0036 onto run/admin-window as 1acf0d6
- 2026-09-02T21:53:55Z [qa] built → qa: under attack
- 2026-09-02T22:00:44Z [qa] qa → done: ATTACKED, then closed on a green receipt (checks re-run on the landed tree 1acf0d6+c81d2bc: vitest tests/offline/live-guard.test.ts, exit 0, 75 pass).

Criteria first, story after. The ticket's own repro is dead: stateOf(`<div data-surface="q">${errorLine()}</div>`, '[data-surface]', '[data-surface]') = 'error'; assertState(..., 'ok') throws.

INTEGRATED ATTACK (16 probes on the stateOf/gradeSurface seam, run offline against markup from the real ui primitives, probe file deleted after transcription). The seam the builder's OWN tests never reach: both of his BUG-0036 cases (SURFACE-as-excluding, [data-page]-as-excluding) leave surface.find(excluding) EMPTY, so they exit through the `subSurfaces.size === 0` shortcut and never exercise the identity-membership branch that is the actual fix. Probed with a NON-empty set:
- Expected 'error', Found 'error': within='[data-surface="claims"]', excluding='[data-surface]', markup = own errorLine() + a genuine nested <div data-surface="dial"> — the self-matching selector with real work to do.
- Expected 'ok', Found 'ok': same shape, error only INSIDE the nested sub-surface — the exclusion still functions, it was not fixed by disabling it.
- Expected 'error', Found 'error': wrapper selector matching an ancestor AND a descendant at once (<main data-page> outside, <div data-page> inside).
- Expected 'error', Found 'error': the surface's own errorLine BESIDE an excluded [data-dial] errorLine — silencing the dial does not silence its sibling.
- Expected 'ok', Found 'ok': [data-dial] nested in [data-dial]; declared-wrapper interplay (data-state="ok" + own error -> 'error'; data-state="empty" + excluded not_provisioned -> 'empty'); exclusion matching nothing -> 'error' (fails SAFE).
- gradeSurface end-to-end: own error -> StateMismatchError; only-the-dial errored with counted=1 -> 'ok'.
- Live-caller reachability verified from the DOM, not the JSX: rendered the real ReviewItemPage with a refused dial read and confirmed [data-dial] is a PROPER DESCENDANT of [data-evidence-view], so review-item.live.test.ts:102's exclusion survives the narrowing. Pinned in tests/offline/review-item/page.test.ts ('keeps the folded records when only the dial's read refuses'): stateOf(markup,'[data-evidence-view]') != 'ok' and stateOf(markup,'[data-evidence-view]','[data-dial]') == 'ok'. A refactor lifting the dial out of the view now reddens instead of silently un-excluding.
- Non-empty-set cases folded into the two existing classifier its (extend, not spawn) — no new files, no new test names. Commit c81d2bc.
Full CI green on my tree: lint + tsc --noEmit + npm test = exit 0, 1646 pass / 53 files (+6 isolated).

TWO RESIDUALS, neither filed (lean depth; run's own tooling, and neither reaches a false green — a wrong verdict is impossible, only a wrong sentence):
1. cardText (parity.ts, reached from gradeSurface:701) ignores 'excluding' and takes .first(). Expected: the error line the GRADED surface rendered ('pending_claims — 57014'). Found: 'observations — 42P01', the EXCLUDED dial's line, when both are in error and the dial is first in document order. Diagnostic misattribution on an already-failing path; trigger = review-item's evidence surface with both reads down on staging.
2. An 'excluding' matching the error CARD itself is a legal proper descendant, so excluding='*' still returns 'ok' on a surface holding only an error line. No plausible author writes it; excluding='div' does NOT reproduce.
No third bounce risk: attempt 1, no churn. [resolution: fixed] [receipt green: 1 cmd(s), tree e4592ef7]
