---
id: BUG-0096
type: bug
status: done
milestone: M2
priority: P2
opened_by: designer
parent: null
discovered_from: designer:early-walk
children: []
assignee: null
claimed_at: null
depends_on: []
touch_scope: [src/app/queues/[reviewItemId]/page.tsx, tests/offline/review-item/page.test.ts]
force: false
attempts: 1
resolution: fixed
---
# The close sits 3,500px below the evidence it closes, so the operator scrolls four screenfuls to act

## Description
Measured on the M2 early walk, 2026-09-08, walk instance on 127.0.0.1:8771 against staging, bundled Chromium, viewport 1440x900, light theme, the one real review item `/queues/01a06287-5b67-7d1f-b4a5-d90aa93cb01a`.

**Expected** — LOOK_AND_FEEL, Key screens: "**Review item detail** — the evidence pair, **and the close beside it**. The operator must be able to decide without opening another tab: contenders, canonical, tiers, ages, and the note field."

**Found** — the close is not beside the evidence; it is under it, past the whole of it. Geometry of `[data-surface]` on that page, read from the rendered document:

- `what_happened`: y=84, height 128
- `evidence`:      y=256, height **3,483**  (106 rows: 91 folded records + the source's trend)
- `close`:         y=**3,781**, height 72
- document height: 3,869

So the close slot's top is 4.2 screenfuls below the fold at 1440x900, and the operator must scroll past 3,483px of evidence to reach the controls that settle the item. Nothing about the evidence needs to be read to the bottom first — it is 91 rows of the same fact folded 700 times, which is exactly the shape this queue is designed to produce (`×700` on the queue row).

Today the slot holds only the not-provisioned card, so the cost is a scroll to read one sentence. **FEAT-0010 builds three action controls into this same slot**, and FEAT-0013/TASK-0059 render a settled item's verdict there — after which the page's whole reason for existing sits below four screenfuls of raw payload. This is filed on the early walk precisely so the placement is settled before the controls land on top of it.

Not a duplicate: FEAT-0010 grades WHAT the close slot contains (criteria 1-4) and says nothing about where it renders; TASK-0059 and BUG-0092 grade the inline verdict block's own contents.

**The fix is not prescribed.** Beside the evidence (the evidence view already runs a `lg:grid-cols-[2fr_1fr]` grid whose right column holds the source dial), above it, or pinned — any of the three satisfies the bar. What the bar forbids is the operator reaching the decision only by exhausting the evidence.

## Acceptance criteria
1. On the staging signal item at 1440x900, the close slot is reachable without scrolling past the evidence: either `[data-surface="close"]` renders at y < 900 in the first viewport, or it precedes `[data-surface="evidence"]` in document order, or the two render in one row of the same grid.
2. Whichever placement is chosen, it holds for all three item shapes (a data_conflict fact item whose evidence is two cards, an entity_link fact item, and the source-pattern signal whose evidence is 100+ rows) — asserted on fixtures in the offline render suite, so a short evidence view and a long one place the close the same way.
3. The evidence view's own anatomy is unchanged: the evidence pair keeps its fixed order (contending claims left, canonical rightmost) and the source-pattern view keeps its table and its dial (LOOK_AND_FEEL, "The evidence pair").
4. The page still answers 200 with `verdicts` absent, the not-provisioned card still names `verdicts`, and nothing throws.
5. Human-check: at 1440x900 on `/queues/01a06287-5b67-7d1f-b4a5-d90aa93cb01a`, the close and the evidence are both on screen at once, or the close comes first. (human-checkable)
6. `npm run lint` and `tsc --noEmit` clean; the targeted offline files and the review-item live suite green.

## Checks
```sh
bash -c 'rm -rf .next/types && npm run lint && ./node_modules/.bin/tsc --noEmit'
./node_modules/.bin/vitest run --project=offline tests/offline/review-item/page.test.ts tests/offline/review-item/close-slot.test.ts
npm run test:live -- tests/live/review-item.live.test.ts
```

## History
- 2026-09-09T05:19:22Z [designer] opened: The close sits 3,500px below the evidence it closes, so the operator scrolls four screenfuls to act
- 2026-09-09T05:20:22Z [builder-160] claimed (attempt 1)
- 2026-09-09T05:22:04Z [designer] Evidence (gitignored, local): agenticflow/tracker/evidence/M2/designer/item-light-1440.png — the full-page render at 1440x900 showing the 3,483px evidence table between the header and the close; review-item-close-slot.png — the viewport at the page's bottom, where the close slot actually is. The geometry in the description is the durable record.
- 2026-09-09T05:31:56Z [builder-160] claimed → built: Close placement: chose ABOVE of the three allowed (beside/above/pinned) — the page now renders data-surface="close" before data-surface="evidence". Decided in the page, once, so it holds for all three shapes; no evidence view's markup, width or grid changed (beside would have nested a page grid around the pattern view's own lg:grid-cols-[2fr_1fr] and risked BUG-0042's overflow, and I have no device lease to walk that width).
- src/app/queues/[reviewItemId]/page.tsx: the close <Section> moved above the Evidence <Section>; docstring anatomy renumbered (1 what happened, 2 the close, 3 evidence) with the reason. No read, prop or component changed — pure order.
- tests/offline/review-item/page.test.ts: new 'the close is reachable without exhausting the evidence' block, +surfaceOrder() reader, +longPatternScript()/FOLDED (104 folded records, crosses ID_CHUNK=100 so the evidence read really chunks). Asserts document order for all 3 shapes x both close states, short-vs-long identical surface sequence, and that the pair/table/dial anatomy is untouched. Red-then-green proved: with page.tsx reverted to HEAD the 3 new tests fail (2 < 1) and the other 70 pass; with the fix, 73 pass.
- Verified on the rebased tree (onto d554b7b): npm run lint clean, tsc --noEmit clean, vitest offline page.test.ts + close-slot.test.ts 107 passed, npm run test:live -- tests/live/review-item.live.test.ts 12 passed against staging (verdicts absent). Full offline suite also green (2599 + 6 isolated).
- Measured at the real surface: dev instance I started on port 8791 (probed free; 8770/8771 untouched), minted cookie, GET /queues/01a06287-5b67-7d1f-b4a5-d90aa93cb01a answered HTTP 200; data-surface order in the delivered HTML = what_happened -> close -> evidence (close at byte 6,278, evidence at 6,914), 91 evidence rows rendered, data-not-provisioned="verdicts" present. Server killed, port free again.
QA note: criterion 5 (pixel geometry at 1440x900) is not walked — I took no emulator/browser lease; the HTTP measurement above is document order, not y-coordinates. Placement is asserted by ORDER, never by a class name or grid literal, so a future 'beside' implementation would have to update these tests deliberately.
- 2026-09-09T05:32:17Z [dispatcher] landed ticket/BUG-0096 onto run/admin-window as 21a2338
- 2026-09-09T05:35:04Z [qa] built → qa: under attack
- 2026-09-09T05:49:20Z [qa] qa → done: attacked in a detached _receipt- worktree at the landed run/admin-window head (f3e3f07), stored checks re-run BEFORE reading the builder's History; all three green (lint+tsc exit 0; offline page.test.ts+close-slot.test.ts 107 passed; live review-item 12 passed against staging, verdicts absent).

Criterion 5, the one the builder did NOT walk, is now WALKED. Production build of this tree, bundled Chromium, viewport 1440x900, bind-probed port 8801 (8770/8771 confirmed still held by their owners before and after; my server killed, 8801 free). Cookie minted, no value printed; staging read-only, no RPC. `[data-surface]` geometry on /queues/01a06287-5b67-7d1f-b4a5-d90aa93cb01a, IDENTICAL in light and dark (prefers-color-scheme contexts):
  what_happened y=84  h=128
  close         y=228 h=72   -> bottom 300, wholly inside the 900px fold
  evidence      y=344 h=3483
  document 3869, 106 evidence rows, HTTP 200, data-not-provisioned="verdicts".
Against the designer's y=3781 the close moved 3,553px up. Criterion 5 holds on BOTH disjuncts, not just the weaker one: close and evidence are on screen at once (evidence top 344 < 900) AND the close comes first. Criteria 1 and 4 measured on the same render.

Criterion 3 checked geometrically, not just structurally: evidence view = source-pattern, exactly one [data-dial] at x=1024 w=400 (the right column of the view's own lg:grid-cols-[2fr_1fr]), the table in its own overflow-x:auto container (clientWidth 798, scrollWidth 1373) — no page-level horizontal scroll (documentElement.scrollWidth 1440 = innerWidth 1440), so the reorder did not resurrect BUG-0042. Staging holds exactly one review item (queues page: 1 link), so the other two shapes are fixture-only, as criterion 2 says.

Attack: I went after the invariant rather than the diff. Enumerated every rendered branch — evidence-unavailable (page.tsx:684), evidence-ok (691), the buckets and sourcesUnavailable legs — all four sit inside the one Evidence Section that follows the close, so no read failure can reorder them. Then the packet's seam: the SETTLED item, whose close slot is the tallest it ever gets — TASK-0059's verdict block renders INSIDE it. A real gap: page.test.ts's placement block renders OPEN items only (`withSettlement` provisions the verdicts table but leaves status open), and verdict-inline.test.ts — the suite's only page-level settled renders — asserts the M1 surfaces exist but never their ORDER. Extended that file (it owns the surface) with two pins over all three log states (absent / no row / the verdict): close before evidence, what_happened before close, one element per name, and item_verdict nested in the close rather than between it and the evidence. WATCHED RED on the pre-fix tree — a second detached worktree at d24b520 with my test file copied in: "log absent: the close precedes the evidence it closes: expected 2 to be less than 1" and "expected 3 to be less than 1", the other 29 green there. Green on the landed tree. Committed as 617640f.

Could not break it: placement holds on every shape, both close states, all three log states, both themes, short and long evidence.

Residual (no path a criterion or receipt drives today, so no BUG under lean depth): once the verdicts table is provisioned (FEAT-0010), an item whose EVIDENCE read fails renders a close that still offers "Supply a different value" and "Keep current & settle" — keep_current is unconditional (conflict-actions.tsx:135) — while the one-per-claim choose_claimed_value controls are silently absent (built from `evidence` at conflict-actions.tsx:102, which page.tsx:630-633 makes [] on a failed read), and the read-failure card now renders BELOW the close instead of above it. Pre-existing to TASK-0049; the reorder is what puts the operator's controls before the failure notice. Trigger for a ticket: the verdict log landing while the evidence read can still fail.

Green from this worktree: lint clean, tsc --noEmit clean, full offline suite 69 files / 2612 tests, isolated 6, live review-item 12. [resolution: fixed] [receipt green: 3 cmd(s), tree a3c13cbc]
