---
id: BUG-0232
type: bug
status: done
milestone: M3
priority: P3
opened_by: qa
parent: null
discovered_from: BUG-0231
children: []
assignee: null
claimed_at: null
depends_on: []
touch_scope: [src/components/claims/claim-list.tsx, src/lib/url/narrowing.ts, src/components/claims/bucket-table.tsx]
force: false
attempts: 1
resolution: fixed
---
# Three comments outside /claims still say the app issues a head: true count; the reads they describe are countRead

## Description
BUG-0231 closed the last 'head count' paraphrase in src/app/claims/page.tsx. Sweeping the rest of src/ for the same class (the sweep BUG-0231's file-scoped criteria could not make) finds three more sentences that describe reads THIS APP ISSUES TODAY as `head: true` counts. No actual `head: true` query exists anywhere in src/ — `grep -rn 'head: true' src` returns comment lines only (verified 2026-09-15 at b2d0e432).

FINDING 1 — src/components/claims/claim-list.tsx:76-77.
Expected (ARCHITECTURE §4.3 + BUG-0210): the list's `held` is `readClaimCount` -> `countRead`, a GET with `{ count: "exact" }` over `limit 0` (src/app/claims/page.tsx:1346 and :1649 `held: listCount.data`; src/lib/db/claims.ts:367-379; src/lib/db/result.ts:1430).
Found: "the read is a window now and `held` is a `head: true` count of its own" — present tense, naming the retired mechanism as the shipped one.

FINDING 2 — src/lib/url/narrowing.ts:45-46.
Expected: where fact 2 costs a query it is `readClaimCount(populationFilter)` -> `countRead` (src/app/claims/page.tsx:1378), same spelling everywhere.
Found: "where it does cost a query, that query is a bounded `head: true` count and never a row read".

FINDING 3 — src/components/claims/bucket-table.tsx:47-51.
Expected (src/lib/db/result.ts:1441-1455, admin-window/BUG-0224): the no-count arm is NOT reached only by a query written without the count option — 'the premise was false: a HOST answering 404 with zero bytes reaches it too'. The arm is the one rule for an answer this app cannot grade (`unreadableAnswer`).
Found: "a database that gave no count at all is exactly the response a query written without `{ head: true, count: "exact" }` returns" — stale on both counts: it states the false premise BUG-0224 removed, and it spells the required option with `head: true`, which ARCHITECTURE §4.3 retired.

SECONDARY, same class, test comments (no assertion depends on them): tests/offline/review/review-items.test.ts:111 and tests/offline/queues/page.test.ts:209 both say 'A head count returns no rows at all'.

NOT IN SCOPE, and must survive untouched — these say why the app does NOT use `head: true` and are true: src/lib/db/result.ts:1399 and :1405, src/lib/db/verdict.ts:251 and :256, src/lib/db/claims.ts:354, src/lib/db/review-items.ts:207, tests/live/parity.ts, tests/http/absence.http.test.ts, ARCHITECTURE.md §6 trap 12. A blanket ban on the literal across the repo is the wrong fix (LESSONS 4).

REPRO (static, no server), from the repo root:
  grep -rn 'head: true' src
  grep -n 'held: listCount.data' src/app/claims/page.tsx
  sed -n '74,79p' src/components/claims/claim-list.tsx
  sed -n '43,47p' src/lib/url/narrowing.ts
  sed -n '46,52p' src/components/claims/bucket-table.tsx
  sed -n '1441,1456p' src/lib/db/result.ts

IMPACT: prose only, no behaviour change, no user-visible defect. Filed because this class has now cost DEBT-0022 and BUG-0231 one lane each by being fixed file-by-file, and the next lane reading claim-list.tsx re-learns a mechanism the code retired. Fix is three sentences at their owners plus two test comments. No new test — grading prose with machinery is banned; the checks are the same grep form DEBT-0022 and BUG-0231 shipped, and each is RED on the current tree (verified). Run ./node_modules/.bin/vitest run --project=offline tests/offline/claims/page.test.ts before pushing (226 green at b2d0e432).

## Acceptance criteria
1. Every comment in src/ that describes a count read THIS APP ISSUES names the spelling the code has — `countRead`, `{ count: "exact" }` over `limit 0` — in the vocabulary src/app/claims/page.tsx:681-682 already uses. Concretely: claim-list.tsx's `held` sentence, narrowing.ts' fact-2 sentence, and bucket-table.tsx's no-count sentence each state their read as it is spelled today, and none of the three says the app issues a `head: true` count.
2. bucket-table.tsx's sentence additionally states the no-count arm as src/lib/db/result.ts:1441-1455 does: an answer this app cannot grade, whatever produced it — not 'the response a query written without the option returns'. Its surrounding rule (a refusal is rendered as a refusal, in that bucket's own row) is kept.
3. The contrastive mentions listed in the description (result.ts, verdict.ts, claims.ts, review-items.ts, tests/live/parity.ts, tests/http/absence.http.test.ts, ARCHITECTURE.md) are untouched: they explain why the app does NOT use head: true and are true.
4. The two test comments naming 'a head count' (tests/offline/review/review-items.test.ts:111, tests/offline/queues/page.test.ts:209) say what the stub actually returns; no assertion changes.
5. Prose only: no behaviour change, no new test, no sentence a user reads is touched. tsc --noEmit 0, npm run lint 0, tests/offline/claims/page.test.ts green with nothing rewritten.

## Checks
```sh
! grep -q 'head: true' src/components/claims/claim-list.tsx
! grep -q 'head: true' src/lib/url/narrowing.ts
! grep -q 'head: true' src/components/claims/bucket-table.tsx
grep -q 'countRead' src/components/claims/claim-list.tsx
./node_modules/.bin/tsc --noEmit
npm run lint
```

## History
- 2026-09-15T06:54:52Z [qa] opened: Three comments outside /claims still say the app issues a head: true count; the reads they describe are countRead
- 2026-09-15T06:56:49Z [builder-332] claimed (attempt 1)
- 2026-09-15T07:01:26Z [builder-332] claimed → built: Prose only, 5 files, no runtime line touched (git diff --name-only aff89621..HEAD = the 3 scoped src files + the 2 named test comments). claim-list.tsx `held` now reads "readClaimCount (src/lib/db/claims.ts) through countRead, which is { count: \"exact\" } over `limit 0` and never a `head` request"; narrowing.ts fact-2 the same spelling; bucket-table.tsx states the no-count arm as result.ts does since admin-window/BUG-0224 — "an answer this app cannot grade, whatever produced it (unreadableAnswer)", with its refusal-in-its-own-row rule kept. The two stub comments say what the stub returns; no assertion changed.
Verified on the REBASED tree (HEAD 1ee804ff on aff89621): all four ticket checks green; ./node_modules/.bin/tsc --noEmit 0; npm run lint 0; ./node_modules/.bin/vitest run --project=offline tests/offline/claims/page.test.ts tests/offline/queues/page.test.ts tests/offline/review/review-items.test.ts = 406 passed (226 + 129 + 51). Every exempted contrastive mention (result.ts, verdict.ts, claims.ts, review-items.ts, tests/live/*, tests/http/absence, ARCHITECTURE.md) is untouched — confirmed by the file list above; no repo-wide ban added.
For QA: (a) the two test-comment edits are outside touch_scope, which lists only the 3 src files — criterion 4 requires them. (b) amend-checks is refused to a builder, so these presence-of-fix checks were run by hand and are each RED at HEAD^ (git show HEAD~1:<path>) and green now, worth adding to the checks block: grep -q countRead in each of narrowing.ts, bucket-table.tsx, review-items.test.ts, queues/page.test.ts; grep -q unreadableAnswer in bucket-table.tsx; ! grep -qi "a head count" over the two test files. (c) same class, NOT in this ticket and still stale by my read: tests/offline/claims/population.ts:262,280, tests/offline/claims/read.test.ts:383 and tests/offline/live-guard.test.ts:1194 describe app reads as head counts — left alone deliberately rather than widened past the ticket. (d) worktrees have no node_modules; I symlinked the primary checkout's for tsc/vitest and removed it before handoff.
- 2026-09-15T07:01:53Z [dispatcher] landed ticket/BUG-0232 onto run/admin-window as 0485e414
- 2026-09-15T07:02:35Z [qa] built → qa: under attack
- 2026-09-15T07:10:13Z [qa] qa → done: attacked: verified every criterion on the LANDED tree (13e188f8) by reading the code first, then grading each rewritten sentence against it.

C1 — each of the three sentences names the read the code has. `countRead` IS `db.from(o).select("*", { count: "exact" }).limit(COUNT_ROWS=0)` (src/lib/db/result.ts:1395-1432) and never head-shaped; claim-list.tsx's `held` chain checks out end to end (page.tsx:1346/1442-1445 -> :1649 `held: listCount.data`; `readClaimCount` -> `readCount(countRead)` at claims.ts:367-379); narrowing.ts' fact-2 read is page.tsx:1384 `readClaimCount(populationFilter)`; bucket-table's per-bucket reads are page.tsx:1350. Vocabulary matches page.tsx:681-682 verbatim.
C2 — bucket-table now says "an answer this app cannot grade, whatever produced it (`unreadableAnswer`, the no-count arm of `readCount`)". Graded against result.ts:1447-1479: `unreadableAnswer` IS what `if (!isCount(count))` returns, and BUG-0224's false premise ("the response a query written without the option returns") is gone. The refusal-in-its-own-row rule and "each is its own read" survive.
C3 — exempted contrastive mentions untouched, proved by the changed-file list (`git diff --name-only aff89621..HEAD` = the 3 src files + 2 test files + tracker only): result.ts, verdict.ts, claims.ts, review-items.ts, tests/live/*, tests/http/absence, ARCHITECTURE.md all unchanged, and each still holds its true "why NOT head: true" sentence. No lint rule, no prose-grading test, no repo-wide ban added — nothing outside those 5 files changed.
C4 — the two stub comments describe the leg the stub answers (`data: null` + a real count); no assertion touched.
C5 — PROSE ONLY, two mechanical proofs: (a) `esbuild --minify` output byte-identical between aff89621 and HEAD for all five files (tsx/ts loaders); (b) `git diff -U0 aff89621..HEAD -- src tests` has 0 changed lines that are not comment lines. tsc --noEmit 0, npm run lint 0 (receipt). tests/offline/claims/page.test.ts 226 green AND untouched; queues/page.test.ts + review-items.test.ts 180 green.

Builder's un-amendable checks re-run by me at HEAD: all 11 green, and each is presence-of-fix not vacuous — `countRead` count was 0 in all five files at aff89621 and 1+ now. Out-of-touch_scope test edits judged LEGITIMATE: criterion 4 names those two files and lines explicitly; touch_scope listed only the src trio. No scope abuse.

Broke it (filed BUG-0233, P3): BUG-0232's own sweep grepped the LITERAL `head: true`, so it could not see the same claim spelled "HEAD count" in prose. Sweeping both spellings finds TEN more live sites, one of them in src/ — src/app/queues/page.tsx:704-705 "one HEAD count per shape OF EACH KIND THE URL NARROWS", where the reads are `readReviewQueues` -> `countQuery` -> `countRead` (review-items.ts:215, :273). That is a counterexample to C1's universal head ("every comment in src/"), though outside this ticket's touch_scope and outside its own concrete enumeration, which is why this closes rather than bounces. Two of the ten now CONTRADICT the lines this ticket rewrote, in the same doc block 24 and 35 lines away (review-items.test.ts:87 vs :111, queues/page.test.ts:174 vs :209). BUG-0233 sweeps all ten in ONE pass with the bar stated positively (LESSONS 13), lists the code/contrastive sites that must survive (three of them hold `head: true` in CODE, so a blanket ban is refused), and carries four presence-of-fix checks I watched RED at HEAD.

Residual, not filed: the stub comments say a `countRead` "returns no rows at all" — a real PostgREST `limit=0` GET returns an empty ARRAY with a body (that body is the point of BUG-0210), while the stub answers `data: null`. `readCount` reads only `{count, error}`, so nothing depends on it; within tolerance for prose. [resolution: fixed] [receipt green: 6 cmd(s), tree 805cb08c]
