---
id: BUG-0152
type: bug
status: done
milestone: patch
priority: P3
opened_by: qa
parent: null
discovered_from: DEBT-0011
children: []
assignee: null
claimed_at: null
depends_on: []
touch_scope: [src/components/evidence/evidence-pair.tsx, tests/offline/ui/evidence-pair.test.ts]
force: false
attempts: 1
resolution: fixed
---
# The canonical card's provenance line draws a blank source as an empty identifier box, announcing no absence — the claim line beside it draws the dash

## Description
Found while re-checking DEBT-0011 (QA r3, attack on the new ProvenanceSegment seam).

Expected: a provenance value with nothing visible in it draws the app's own absence element, exactly as the CLAIM LINE one card to its left already does for the same class of value — `ClaimValue` (`src/components/evidence/evidence-pair.tsx:112-115`) passes `claim.source` through `isAbsent`/`orDash` and renders `<span aria-label="no value">` (admin-window/BUG-0151 and its pinned test "draws a %s source as the app's absence element, not an empty isolated box"; the absence rule is BUG-0134).

Found: `ProvenancePart` (`src/components/evidence/evidence-pair.tsx:139-157`) wraps `segment.identifier` in `<Identifier muted>` UNCONDITIONALLY, with no absence guard. Measured offline (vitest, `tests/offline/ui/evidence-pair.test.ts`, cheerio over the rendered markup, 2026-09-09):

  provenance: [{ identifier: "" }, { identifier: "official", after: "at apply" }, "applied 3d ago"]
  renders    <span class="type-data text-ink-secondary"><span dir="ltr" class="type-data text-ink-secondary"></span> · <span dir="ltr" class="type-data text-ink-secondary">official</span> at apply · applied 3d ago</span>

  i.e. an EMPTY isolated box, `[aria-label="no value"]` count 0, and the line reads " ·  at apply · applied 3d ago" — a silent gap where the winning source belongs. Identical on all three shapes `isAbsent` calls empty: "" , "   ", and "​‮" (ink-less).

Repro (offline, no device, no network):
  1. cd "kspace Admin"
  2. ./node_modules/.bin/vitest run --project=offline tests/offline/ui/evidence-pair.test.ts
  3. The three `it.fails` cases "draws a {empty,blank,ink-less} provenance value as the app's absence element, not an empty isolated box" pass as EXPECTED FAILURES — flip `it.fails.each` to `it.each` and they go red with `expected [ "", "official" ] to not include ""`.

Reachability: `canonicalCard` (`src/app/queues/[reviewItemId]/page.tsx:339`) builds `{ identifier: source }` from `canonicalSideOf`'s `sources.get(decision.source_id)?.source ?? decision.source_id` (`src/lib/db/review-item.ts:530-533`) — the source's own NAME out of the scraper-owned `sources` registry, which is `string` with no blankness guard. A registry row whose name is empty or whitespace produces this.

NOT a regression from DEBT-0011: the pre-sweep line concatenated the same empty string into the sentence and drew the same nothing, so criterion 3 (rendered text unchanged) holds. What DEBT-0011 changed is that the gap now has a BOX around it, and that the sibling line on the same card gained the guard this one did not. P3: two lines of one card disagree about how they announce a missing value, on a path that needs a blank registry name to reach.

Fix shape (for the builder, not binding): route `segment.identifier` through the same `isAbsent`/`orDash` pair `ClaimValue` uses — an absent value is the app's own absence element, never a machine identifier's box, so it must NOT be wrapped. Then convert the `it.fails.each` in `tests/offline/ui/evidence-pair.test.ts` to a strict `it.each`.

## Acceptance criteria
(to be written)

## Checks
```sh
./node_modules/.bin/vitest run --project=offline tests/offline/ui/evidence-pair.test.ts
```

## History
- 2026-09-09T22:52:57Z [qa] opened: The canonical card's provenance line draws a blank source as an empty identifier box, announcing no absence — the claim line beside it draws the dash
- 2026-09-09T23:11:02Z [builder-227] claimed (attempt 1)
- 2026-09-09T23:18:09Z [builder-227] claimed → built: Fix: `ProvenancePart` now renders `segment.identifier` through the SAME guard the claim line uses — `ClaimValue` renamed `MachineValue` (one helper, both secondary lines of the card; no second guard written). Present value -> `<Identifier muted>`; absent per `isAbsent` -> `orDash`'s `[aria-label="no value"]` dash, unwrapped, so no empty `dir="ltr"` box is left standing. `before`/`after` app words still render beside the dash.
Verified (rebased onto run/admin-window @ e8bed10): red-then-green — flipping QA's three `it.fails` to `it.each` first went red (`expected [ "", "official" ] to not include ""`, and the '   ' / ink-less twins), then green after the guard; `./node_modules/.bin/vitest run --project=offline tests/offline/ui/evidence-pair.test.ts` 23/23; `npm test` 3195 offline + 6 isolated pass; `npm run lint` and `tsc --noEmit` exit 0; `npm run test:live -- tests/live/review-item.live.test.ts` 12/12 against staging (read-only).
For QA: present-value markup is byte-identical — the whole 1,515-byte `EvidencePair` render of a 4-segment provenance line (plain, `after`, string, `before`) diffed clean against a detached checkout of the pre-fix commit. Two traps: (1) the three `it.fails` are now strict `it` and their docblock rewritten to state the landed behaviour, so a regression reddens rather than silently passing as an expected failure; (2) `ClaimValue` no longer exists by that name — the test's prose reference was updated with it. Diff is the guard, its two call sites, the rename, and doc text; no behaviour outside the absent-identifier arm.
- 2026-09-09T23:20:23Z [dispatcher] landed ticket/BUG-0152 onto run/admin-window as 0f68c9e
- 2026-09-09T23:21:48Z [qa] built → qa: under attack (qa-0152, lean depth)
- 2026-09-09T23:36:01Z [qa] qa → done: attacked (lean, P3): criteria re-run on the LANDED tree from a detached _receipt- worktree before reading the builder's story; stored check verbatim exit 0 (23/23, now 33/33 with my pins).

Criteria, each graded:
- absent segments draw the labelled absence element, no empty isolated box: measured on all five absent shapes and BOTH other placements — "songkick · — at apply · applied 3d ago" (tier, app words AFTER) and "… · the claim it applied is now —" (dead-claim status, app words BEFORE); [dir] boxes = ["songkick"] only, [aria-label="no value"] count 1, no <span dir="ltr"></span> anywhere. The app's words beside the dash still render (the fix's second half).
- present values unchanged: healthy 4-segment line "songkick · official at apply · applied 3d ago", boxes ["songkick","official"], 0 absence elements; " x " keeps its visible content verbatim inside its box. I did not redo the builder's byte-diff against the pre-fix commit (release-tier move); behaviour re-measured instead.
- one guard: `isAbsent` is asked in exactly one place on these two lines (`MachineValue`, evidence-pair.tsx:128); no `ClaimValue` reference survives anywhere in src/, tests/, contracts/ or docs/ (grep, 0 hits) — the rename's prose is clean. Rather than grepping source shape, I pinned the property BEHAVIOURALLY: the claim line and the provenance line of one card, fed the same absent value, draw the very same absence element (compared element-to-element, so a token change moves both).

Attack (one, across the batch's surface):
- null and undefined at runtime through `identifier: string` — the row type lies (tier_at_apply / observations.status are declared string over scraper-owned columns): both draw the dash, no box. Pre-fix they would have drawn an empty box.
- a source literally named "—" -> the dash (isAbsent's EM_DASH arm), same as the claim line: consistent, not a finding.
- all identifier segments absent at once -> "— · — at apply · applied 3d ago", 2 dashes, 0 boxes, no dangling separator.
- reachability driven end to end through the REAL page (`ReviewItemPage`, stubbed reads, blank `sources.source`): claim line "— · official · 9d ago" and provenance "— · trusted at apply · applied 8d ago", 0 empty boxes. The fix works on the path the ticket named, not just in the component.
- production build (exit 0) + the staging review item over HTTP on my own bind-probed port 8793 (read-only GET, own PID, killed; port confirmed closed): 200, 313,728 bytes, 0 empty isolated boxes. Weak evidence by construction — staging's one review item is a per-source item, so it renders no canonical card at all ("contender" count 0); the offline page render above is what actually covers the path.

New tests (committed c760e3c, both watched RED first against an unconditional-wrap stand-in line: dashes=0, boxes=["songkick",""]): tests/offline/ui/evidence-pair.test.ts +2 it.each x5 — the app's words beside an absent value in both other placements, and the two lines' one absence element. Extended the file that owns the surface; no new file.

Filed BUG-0154 (P3, my one lean filing): upstream of this fix — a `sources` row whose NAME is blank ("   ") passes `sourceLabel`'s `??` and reaches the evidence table as a BLANK cell ("" between "TWICE 5TH WORLD TOUR" and "official", sibling row reads "bandsintown") and an anchor whose whole visible content is whitespace, while the two evidence-pair lines correctly dash the same value — one row's absence, three renderings. Pinned strict `it.fails` in tests/offline/review-item/page.test.ts, watched red as a plain `it` ("blank cells: expected [ \"\" ] to deeply equal []"). Not BUG-0152's fault and not its scope: BUG-0152 decides how absence is DRAWN, BUG-0154 decides what should be handed to it.

Residuals (no path a criterion drives; not tickets): (1) `{ before: "" }` renders a stray leading space — `before` is only ever the app's hardcoded words, so unreachable; (2) `CardValue` (evidence-pair.tsx:92) wraps `orDash(value)` INSIDE `<Identifier>`, the inverse category error `MachineValue`'s own doc names — the dash still renders labelled and in disabled ink, so no user-visible divergence; (3) `canonicalSideOf` (review-item.ts:530) retypes `sourceLabel`'s `??` instead of importing it (LESSONS 5) — behaviour identical today, and it is named in BUG-0154's fix shape.

Verified from the worktree on the landed tree: npm run lint 0, tsc --noEmit 0, npm test 3210 offline + 6 isolated pass (3195 before, +14 mine, +1 expected-failure pin). Receipt GREEN. Judgment: the fix meets the ticket's goal — a provenance value with nothing visible in it now announces its absence in the app's one element, in every placement and on the real page, and the healthy line is unchanged. [resolution: fixed] [receipt green: 1 cmd(s), tree 74e37cc2]
