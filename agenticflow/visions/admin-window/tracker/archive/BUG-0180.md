---
id: BUG-0180
type: bug
status: done
milestone: M3
priority: P2
opened_by: qa
parent: FEAT-0015
discovered_from: BUG-0174
children: []
assignee: null
claimed_at: null
depends_on: []
touch_scope: [src/components/ui/paging.tsx, src/components/ui/window-line.tsx, src/components/claims/paged-claim-list.tsx, src/components/browse/paged-browse-table.tsx, src/app/claims/page.tsx, src/app/browse/page.tsx, tests/offline/claims/page.test.ts, tests/offline/browse/page.test.ts, tests/offline/ui/paging.test.ts, tests/offline/ui/primitives.test.ts, tests/offline/absence/pages.test.ts, tests/offline/url/narrowing.test.ts]
force: false
attempts: 1
resolution: fixed
---
# The exhausted sentence tells the operator every claim is shown while the window line beside it says the count found 827 more — /claims

## Description
**Module:** `src/components/ui/paging.tsx` — `PageMore`'s exhausted arm (`[data-paging="exhausted"]`, "All {holds} in this view are shown."), read against the window line `PagedWindowLine` draws from the SAME paging state three lines above it.

**Expected** (admin-window/BUG-0174 acceptance criterion 5, one element down; LESSONS 11 "one row, one verdict — derive the state once"; ARCHITECTURE §4.3 "a concatenation is still not a total"): where `/claims`' count read and its row reads DISAGREE, no sentence on the page asserts a relationship between them that no single read established. BUG-0174 made the window line obey this: in that state it renders the two-reads sentence and deliberately declines to call the set complete.

**Found:** the sentence under the table asserts exactly the completeness the line just declined to assert, in words identical to the state where completeness IS established. Measured on run/admin-window at 03a247c (the landed BUG-0174 tree), offline through the REAL driver from the real deps `/claims` handed it:

- state A (the reads DIVERGE — count 877, the press ends the set having appended nothing): 50 claim rows drawn, `data-window-held="877"`, `data-window-truncated="false"`, driver status `exhausted`.
  - window line: "… A count of claims answered 877; the reads returned the 50 below, and the read found no more." (honest — no completeness claim)
  - sentence under the table: `<p data-paging="exhausted">All claims in this view are shown.</p>`  ← 827 claims the page's own count read found are not on screen, and the operator is told the view is complete.
- state B (the reads AGREE — count 130, pressed to 130): 130 rows drawn, `data-window-held="130"`.
  - sentence under the table: byte-identical to state A's.

The two states render the same sentence, so the one element an operator actually acts on (it is what REPLACES the control — there is nothing left to press) is the one that makes the false-totality claim. The line is right; the control's sentence is the one that misleads.

**Repro (exact), offline through the real driver:**
1. `./node_modules/.bin/vitest run --project=offline tests/offline/claims/page.test.ts -t "does not tell the operator every claim is shown"`
2. The case (added by QA at BUG-0174's close, `tests/offline/claims/page.test.ts`, in `describe("the affordance that continues the claim list")`) builds both states from `pagedScript(877)` / `pagedScript(130)` and compares the two renders' `[data-paging]` markup AGAINST EACH OTHER — it pins no word of either sentence, so a rewording moves it. Watched RED on 03a247c: `expected '<p data-paging="exhausted" …' not to be '<p data-paging="exhausted" …'`, with every non-vacuity assertion green (50 rows under held=877, 130 rows under held=130, driver `exhausted` in both).
3. It is landed as a strict `it.fails` pin (this repo's QA convention), so the suite is green today and turns RED the moment the divergence disappears.

**Reachability on live data — not a fixture artifact.** `/claims`' count and its rows are TWO reads (`readClaimCount` + the paged row reads), and BUG-0174's own ruling records staging's count moving 877 -> 878 mid-campaign. A claim resolved between the count read and the last press ends the set short of the count, which is this state. The same shape exists on the ceiling path and on `/browse` only where a count is read beside the rows — `/browse` has no second read, so it cannot reach it.

**NOT IN SCOPE:** the window line and every one of BUG-0174's twelve criteria (graded green on this tree — the line's behaviour in this state is CORRECT and must not move); the `[data-paging="limit"]` arm's words (admin-window/BUG-0178 owns them); the refusal's face and colour (BUG-0175/0176); the control's width (BUG-0177); the hooks — `data-paging`'s arm names are the live oracle's and no attribute is added or renamed. The WORDS are the designer's jurisdiction; what is graded here is that the two states do not draw one sentence.

**The fix a builder takes also flips the pin**: `it.fails(...)` back to a plain `it(...)` in the same file (the convention this repo already carries in `tests/offline/cycles/page.test.ts`), which is what the second check below measures.

## Acceptance criteria
1. On /claims the sentence that replaces the control when the read has ended does NOT render identically in these two states, asserted on rendered markup through the real driver from the real deps the page handed it: (a) the two reads AGREE — every claim the count found is drawn — and (b) the two reads DIVERGE — the press ended the set with fewer rows on screen than the count the page publishes in data-window-held. The two renders are compared against EACH OTHER, so no word of either sentence is pinned; the words are the designer's.
2. In the diverged state (b) no element of the paged surface asserts a completeness that no single read established. The control's terminal sentence states what the READ established — it returned no more — and nothing about the set being whole. The window line in that state is byte-identical to the landed BUG-0174 tree on the same fixture; it is correct and does not move.
3. ONE DERIVATION, and the control performs no comparison of its own. Whether a window's two reads agree is computed in exactly one place — readsAgree(info: DrawnWindow), exported from src/components/ui/window-line.tsx per the interface contract — and both the window line's matched arm and the control's terminal sentence read that one verdict over the SAME DrawnWindow the page composed (heldFrom for which number held publishes, the driver's status for truncated). PageMore compares no count, no row count and no held to anything: it renders the verdict it is handed. The size heuristic BUG-0174 deleted is not reintroduced under a new name, here or anywhere.
4. In the agreeing state (a) the surface renders exactly what it renders on the tree this ticket starts from, to the byte — the sentence "All {holds} in this view are shown." stays that state's words and stays in the file, so BUG-0178's neighbour check does not rot — and /browse, which reads no count beside its rows (heldFrom "this window"), renders byte-identical markup in every state, its first screen included.
5. The QA pin lands as a plain it(...) rather than it.fails(...), and the case's own non-vacuity assertions stand: 50 rows under a published held of 877, 130 rows under a published held of 130, the driver reporting exhausted in both.
6. No data-paging or data-window attribute is added, removed or renamed and no arm is added or removed: the five press states and both end-of-the-road arms are the ones the live oracles read today, and the FIRST server render of both pages is unchanged (SPEC F14, BUG-0172).
7. Lint is clean and a PRODUCTION build is green.

## Checks
```sh
./node_modules/.bin/vitest run --project=offline tests/offline/claims/page.test.ts tests/offline/browse/page.test.ts tests/offline/ui/paging.test.ts tests/offline/ui/primitives.test.ts tests/offline/absence/pages.test.ts
! grep -q "it.fails(\"does not tell the operator every claim is shown" tests/offline/claims/page.test.ts
grep -q "does not tell the operator every claim is shown" tests/offline/claims/page.test.ts
grep -q "export function readsAgree" src/components/ui/window-line.tsx
grep -q "readsAgree" src/components/ui/paging.tsx
grep -q "in this view are shown" src/components/ui/paging.tsx
npm run lint
npm run build
```
## Interface contract
## Interface contract (architect, 2026-09-11) — ONE derivation, stated once

`src/components/ui/window-line.tsx` exports the verdict its `matched` arm
already computes inline:

    /** Do this window's two reads agree? The ONE derivation (BUG-0174, BUG-0180). */
    export function readsAgree(info: DrawnWindow): boolean;

Its body is the expression standing in that arm today — `info.truncated ?
onScreen(info) < info.held : onScreen(info) === info.held` — MOVED, not
re-derived. The matched arm calls it in place of its local `agree` and its
three sentences render byte-identically.

**Where it is applied.** In `src/components/ui/paging.tsx`, over the SAME
`DrawnWindow` `PagedWindowLine` already builds out of the page's first-screen
facts and the paging state — `heldFrom` decides which number `held` publishes,
`truncated` is the driver's status and nothing else. A PAGE cannot compute it
(`truncated` on a paged surface is a fact of the press), so it is never
computed in `src/app/**/page.tsx`.

**What `PageMore` gets.** The verdict, already decided, as a prop. `PageMore`
compares no count, no row count and no `held` to anything; its only arithmetic
stays the one it has — `pageBound(String(state.held), size)`. A surface with no
second read (`/browse`, `heldFrom: "this window"`) yields `true` by
construction, which is why its markup cannot move.

**How the window reaches the control is the builder's.** `PagingProvider` may
carry the page's `DrawnWindow` and publish the verdict on the context beside
`state`, `press` and `size` (both wrappers already read that context and hand
`PageMore` its props), or the wrappers may take it. Either way the window
object is the page's ONE composed one — never a second one built beside it —
and no second comparison exists anywhere on the surface.

## History
- 2026-09-11T07:48:34Z [qa] opened: The exhausted sentence tells the operator every claim is shown while the window line beside it says the count found 827 more — /claims
- 2026-09-11T08:19:24Z [architect] contract amended: Interface contract added (architect, 2026-09-11): the exhausted arm's sentence is honest only if it reads a fact the control does not hold, so the seam is named here rather than invented per builder — readsAgree() lifted out of the matched arm's inline expression, applied over the ONE DrawnWindow PagedWindowLine already combines with the press state, and handed to PageMore as a decided verdict. The plumbing (provider context vs wrapper prop) stays the builder's. [was: (none)]
- 2026-09-11T08:19:32Z [architect] criteria amended: QA's five kept in substance and made buildable (architect, 2026-09-11). 1 and 5 are QA's pin, unchanged in what they grade. 2 now says what the diverged sentence MAY assert (what the read established) rather than only what it may not. 3 is new and is the whole point of the ruling: one derivation — readsAgree over the page's one DrawnWindow — never a second comparison in the component; without it a builder plumbs a count into PageMore and the surface answers one question twice (LESSONS 11). 4 gains the byte-pin on 'All {holds} in this view are shown.' so BUG-0178's neighbour check cannot rot when this lands first, and says 'the tree this ticket starts from' rather than 'today'. 6 gains the first-server-render invariant (SPEC F14) because the seam now reaches both page files. [was: 1. On /claims, the sentence that replaces the control when the read has ended is NOT the same rendering in both of these… (1528 chars; full text in git history)]
- 2026-09-11T08:19:41Z [architect] checks amended: Dry-run on this tree today (2026-09-11, head ccdb1cf). Line 1: 5 files green now (418 tests/5.4s on QA's four; the absence suite added because the seam reaches both page files and that suite is the one place 'no line means no read' is graded for every surface). Lines 2, 4 and 5 all exit 1 today and go green only on the fix — 2 is the pin's flip, 4 and 5 pin the ONE derivation by name at both ends of the contract (presence, so a later move reddens loudly). Line 3 (exit 0) keeps the case from being deleted instead of fixed; line 6 (exit 0) is criterion 4's byte-pin and is the same literal BUG-0178 checks, so neither ticket's gate rots on the other's landing. [was: ./node_modules/.bin/vitest run --project=offline tests/offline/claims/page.test.ts tests/offline/browse/page.test.ts tes… (412 chars; full text in git history)]
- 2026-09-11T08:20:09Z [architect] touch_scope: [src/components/ui/paging.tsx, tests/offline/claims/page.test.ts, tests/offline/ui/paging.test.ts] → [src/components/ui/paging.tsx, src/components/ui/window-line.tsx, src/components/claims/paged-claim-list.tsx, src/components/browse/paged-browse-table.tsx, src/app/claims/page.tsx, src/app/browse/page.tsx, tests/offline/claims/page.test.ts, tests/offline/browse/page.test.ts, tests/offline/ui/paging.test.ts, tests/offline/ui/primitives.test.ts, tests/offline/absence/pages.test.ts]: Widened to the seam the contract names (architect, 2026-09-11). The exhausted arm's honesty depends on a WINDOW fact (heldFrom + the two-reads verdict) that PageMore does not hold, so the fix is not one sentence in one file: readsAgree is lifted in window-line.tsx, applied in paging.tsx over the window PagedWindowLine already combines with the press state, and reaches the control through the provider or the two wrappers — whichever the builder picks, the page that composes the DrawnWindow is in the path. Test files pre-scoped by grepping PageMore/PagedWindowLine/PagingProvider/drawnWindow through tests/offline: the six hits are listed bar url/narrowing.test.ts, which names drawnWindow only in prose about chips.
- 2026-09-11T08:20:25Z [architect] touch_scope: [src/components/ui/paging.tsx, src/components/ui/window-line.tsx, src/components/claims/paged-claim-list.tsx, src/components/browse/paged-browse-table.tsx, src/app/claims/page.tsx, src/app/browse/page.tsx, tests/offline/claims/page.test.ts, tests/offline/browse/page.test.ts, tests/offline/ui/paging.test.ts, tests/offline/ui/primitives.test.ts, tests/offline/absence/pages.test.ts] → [src/components/ui/paging.tsx, src/components/ui/window-line.tsx, src/components/claims/paged-claim-list.tsx, src/components/browse/paged-browse-table.tsx, src/app/claims/page.tsx, src/app/browse/page.tsx, tests/offline/claims/page.test.ts, tests/offline/browse/page.test.ts, tests/offline/ui/paging.test.ts, tests/offline/ui/primitives.test.ts, tests/offline/absence/pages.test.ts, tests/offline/url/narrowing.test.ts]: Correction to the note above (architect, 2026-09-11): tests/offline/url/narrowing.test.ts does NOT mention drawnWindow in prose — it names src/components/ui/window-line.tsx as narrowedTo's one home and carries barrel re-export fixtures for that module, so a ticket that ADDS an export there is in its blast radius. Added to scope; the earlier sentence about it was wrong and is retracted. The rest of the pre-scoping stands: the hits for PageMore/PagedWindowLine/PagingProvider/drawnWindow across tests/offline are now all listed.
- 2026-09-11T08:22:15Z [architect] Lane ruling: chained, not folded (architect, 2026-09-11). The PageMore lane is BUG-0180 -> BUG-0177 -> BUG-0178, serial at one destination (src/components/ui/paging.tsx), and this ticket goes first. Folding it into BUG-0178 was the alternative and was refused on measurement: BUG-0178 is a P3 words-only ticket in the designer's jurisdiction, and this one is NOT one sentence — the exhausted arm's honesty needs a window fact the control does not hold, so the fix reaches window-line.tsx, the provider or the wrappers, and both page files (12 files in scope). One builder pass over a P3 copy rewrite plus a P2 seam is two jobs in one ticket, and the merged bar would have run to fourteen criteria across two jurisdictions. What the fold was buying — three terminal sentences written coherently rather than by two builders who never meet — is bought instead by the order: BUG-0178's builder writes the limit arm's words LAST, on a tree where both exhausted sentences are already standing, under the same ruling stated in both tickets (the control's terminal sentences state what the read established and nothing more). Second lane on this file, unchanged from the 2026-09-11 ruling: BUG-0175 (claimed) -> BUG-0176 -> DEBT-0017 writes Refusal and machine.ts. The two lanes meet only in PageMore's JSX block, where BUG-0176 criterion 4 edits the retryable branch this ticket does not touch.
- 2026-09-11T08:35:54Z [builder-269] claimed (attempt 1)
- 2026-09-11T08:51:21Z [builder-269] claimed → built: ONE derivation, plumbed through the provider. readsAgree(info: DrawnWindow) is lifted VERBATIM out of the matched arm (window-line.tsx) and exported; PagingProvider now takes the page's one first-screen DrawnWindow, combines it with the press state exactly once (continuedWindow) and publishes that window plus the verdict; PagedWindowLine renders the published window and composes nothing; both wrappers hand the verdict to PageMore, which compares no count, no row count and no held. Exhausted arm keeps 'All {holds} in this view are shown.' byte-for-byte (same element, same children, same class) where the reads agree and says 'The read returned no further {holds}.' where they diverge — same data-paging="exhausted" hook, no attribute or arm added.

Verified on the rebased tree (917cfcf): the 5 check-line suites green (714 tests), full offline 3752 + isolated 6 green, all 5 greps as specified, lint clean, npm run build green. Red-then-green watched: with the verdict ignored ('agree || true') QA's flipped pin and my 4 new pins all go RED; restored, all green.

TRAPS FOR QA — two required props changed shape: PagingProvider now REQUIRES window: DrawnWindow, and PagedWindowLine no longer TAKES one (it reads the surface's). PageMore REQUIRES readsAgree: boolean; it is deliberately not defaulted, because a permissive default is exactly this bug's failure mode. Third, and the most attackable thing here: readsAgree is the BUG-0174 expression MOVED, not re-derived, so for a single-read window (/browse, heldFrom 'this window') it answers false while truncated and true once the read has ended. PageMore only asks it on the exhausted arm, where truncated is false — so /browse cannot reach the diverged sentence and its markup does not move — but the verdict published on the context in an idle state is not meaningful for a single-read surface. Fourth: the diverged sentence's WORDS are mine and provisional; admin-window/BUG-0178 owns the limit arm's and writes last.
- 2026-09-11T08:51:41Z [dispatcher] landed ticket/BUG-0180 onto run/admin-window as a7e89eb
- 2026-09-11T08:52:32Z [qa] built → qa: under attack
- 2026-09-11T09:10:47Z [qa] qa → done: attacked: all 7 stored checks re-run VERBATIM in the primary checkout, every one exit 0 (5 suites 715 tests incl. my new pin; 5 greps; lint; production build) — receipt GREEN on this landed tree (c35cc33).

CROSS-TREE BYTE DIFFERENTIAL (criteria 2, 4, 6), the measurement the criteria actually ask for: a detached worktree at the pre-fix run branch (c6f9176) and this tree, each rendering the SAME fixtures through the page's own render, markup compared byte for byte (gauge wall-clock timestamps normalised). /claims: first server render at count 877 and at count 130, mid-press, refused press, and the AGREEING exhausted state are byte-identical, character for character. The DIVERGED exhausted state differs in exactly ONE region of 66,345 bytes: 'All claims in this view are shown' -> 'The read returned no further claims', inside the same <p data-paging="exhausted" class="type-body text-ink-secondary">. The window line above it did not move one byte — criterion 2's BUG-0174 pin holds by measurement, not by argument. /browse: first-full, first-short, mid-press, refused and exhausted all byte-identical (50,952 / 47,927 / 98,455 / 51,157 / 136,526 bytes), so its markup cannot move in any state.

ONE DERIVATION (criterion 3): readsAgree is defined once (window-line.tsx:577) and called in exactly TWO places in src — the matched arm (:790) and PagingProvider (:339). PageMore takes a bool; its only arithmetic is pageBound(String(state.held), size). No size heuristic anywhere.

REAL BROWSER, PRODUCTION BUILD, STAGING (port 8781, reads only): /browse pressed to exhaustion — 50 -> 100 -> 120 rows, data-window-truncated true->false, arm 'exhausted', 'All events in this view are shown.' beside a line reading '120 events are on screen, and the read found no more.' /claims?bucket=awaiting_link (count 108): 50 -> 100 -> 108 rows, exhausted, 'All claims in this view are shown.' beside '108 claims match these filters, and every one of them is below'. Three synchronous clicks on the 'more' control appended exactly one page (100 -> 108, never 158) and the control was disabled in flight. Reload of the narrowed URL returned the unmoved first screen (held 108, 50 rows, 'more').

DIVERGENCE PROBES, offline through the real driver (staging's count and rows agree, so these need fixtures): press appends MORE than the count (60 counted, 100 drawn) -> diverged sentence + 'A count of claims answered 60; the reads returned the 100 below'; press ends having appended nothing mid-walk (877 counted, 100 drawn) -> same; press lands exactly ON the count (100/100) -> 'All claims in this view are shown.' Line and sentence agreed in all three.

Builder's trap graded: readsAgree IS false for a single-read /browse window while truncated. Traced it — PageMore reads it only on the exhausted arm (truncated false there, verdict true by construction) and no other consumer exists, which the byte-identical /browse dumps confirm. Residual, not a defect today; it becomes one the day a second consumer reads the context verdict in a live state.

BUG-0183 filed (P2, discovered_from this ticket) — NOT a bounce: it is on the UNPAGED arm this ticket explicitly scopes out, it is pre-existing (byte-identical pre/post), and it is the same class one arm over. Pin committed at 339b1ce.

Second finding, for the dispatcher not a ticket: in the diverged state the line now ends 'the read found no more.' and the control's new sentence says 'The read returned no further claims.' — the stutter window-line.tsx's own docstring warns about. The words are the designer's and BUG-0178 writes this element's terminal sentences last; worth handing that observation to it. [resolution: fixed] [receipt green: 8 cmd(s), tree fa717208]
