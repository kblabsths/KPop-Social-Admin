---
id: TASK-0020
type: task
status: done
milestone: M1
priority: P2
opened_by: architect
parent: FEAT-0001
children: []
assignee: null
claimed_at: null
depends_on: [TASK-0019]
touch_scope: [docs/build_judgments.md]
force: false
attempts: 1
resolution: fixed
---
# Compile docs/build_judgments.md for the milestone-close review

## Description
Module: `docs/build_judgments.md` — a review artifact, not product code. Authority: `contracts/admin-build.md`, 'Reviewed after the build': *'at most fifteen, rewritten whole at each milestone close, each entry naming the contract location that was silent'*; M1 EC14.

Runs **last** in the milestone. Collect every judgment the milestone's tickets recorded — each builder records theirs as a ticket comment while the work is fresh, so this is a compile, not an archaeology dig — and write the file **whole** (never appended to):

- At most **fifteen** entries.
- Each entry names **the contract location that was silent** (file and section), what was decided, and why that reading rather than another.
- A judgment that turned out to fork the build is not an entry here — it is a blocked ASK ticket for Ben. This file is for the small readings that had to be made to keep moving, not for decisions that should have been questions.
- Cross-check against the ASK tickets: nothing that is an open question may appear here as a settled judgment.

Also produce the milestone's **cross-directory report** line for the close: every scraper-side commit during M1 is either a declared handoff or a noted minor edit, and no ticket's touch scope named a scraper path. The expected count for M1 is **zero** scraper-side commits — a campaign is running in that repo, so everything there is a handoff.

## Acceptance criteria
[EC14] `docs/build_judgments.md` exists, was rewritten whole at this close, and carries at most fifteen entries, each naming the contract file and section that was silent. No entry restates an open ASK ticket as a settled decision. The cross-directory report states the number of scraper-side commits made during M1 (expected: zero) and confirms no ticket's touch scope named a scraper path.

## Checks
```sh
bash -c "test -f docs/build_judgments.md"
test "$(grep -c '^## ' docs/build_judgments.md)" -le 15
bash -c "grep -qi contract docs/build_judgments.md"
```

## History
- 2026-09-02T05:43:11Z [architect] opened: Compile docs/build_judgments.md for the milestone-close review
- 2026-09-02T19:14:02Z [builder-59] claimed (attempt 1)
- 2026-09-02T19:23:03Z [builder-59] claimed → built: Wrote docs/build_judgments.md whole (new file): 15 entries, Ben's 7 rulings first (env names; groups/idols live writes; dials deferred past M2; resolver-owned display list; pre-cutover provenance line; the nine runs columns; desktop-only + both themes), then 8 architect/QA judgments. Every entry carries a '*Silent at*'/'*Contradictory at*' line naming a contract file + section, '*Decided by*' (Ben/architect/QA/builder) and '*Recorded*' (DECISIONS.md paragraph, ARCHITECTURE section, campaign-qualified ticket). Sources read: TASK-0021..0025/0029/0031, DECISIONS.md (23 paragraphs), ARCHITECTURE 12, and the ruled-judgment blocks in TASK-0002/0004/0011/0015, BUG-0016/0017/0030.
- Proof, all on the rebased tree (50dad3c, rebased onto run/admin-window 600da15): the three stored checks verbatim -> exit 0/0/0, entry count 15. Negative control watched: appending a 16th '## ' entry to a scratch copy takes check 2 to exit 1, so the count check really counts entries (scratch file deleted). A script re-parsed all 15 blocks and asserted each has a silence marker + a named contract file + decider + recorded-at: 15/15 OK. Every section citation was checked against the file it names (fixed two before commit: ARCHITECTURE gauges is 8 not 6; DECISIONS holds 23 paragraphs not 21).
- Cross-directory report, re-measured at handoff: scraper-side commits by this campaign = ZERO (git log --all --grep=admin-window in ../kspace Scraper -> 0; its 260 commits since 2026-09-01 are its own resolver campaign); no ticket's touch scope names a scraper path (0 of 75 ticket files match Scraper or ../); one handoff outstanding, TASK-0031.
TRAPS FOR QA. (1) Entries are '## ' headings on purpose so the stored count check is load-bearing; the sibling scraper repo's edition uses a numbered list, under which grep -c '^## ' would be vacuously 0. The two trailing sections (routed questions, cross-directory report) are '###' for the same reason - do not promote them. (2) Nothing open is stated as settled: TASK-0031 appears ONLY in 'Questions routed rather than decided'; TASK-0029, TASK-0032 and BUG-0027 are named as ruled-but-not-landed with 'open at this close'. Verify against the board if it moved. (3) I checked landed code before asserting it: runs.ts carries the nine columns, ErrorLine.reading is required, next.config.ts holds the rewrite, ROW_CAP=1000 - but config.ts has no 'display' field and no ui primitive carries data-state, which is why entries 4/13/14 say the build has not landed. (4) No test added - touch_scope is this one doc and a doc-shape test would pin presentation.
- 2026-09-02T19:23:26Z [dispatcher] landed ticket/TASK-0020 onto run/admin-window as 3254db3
- 2026-09-02T19:24:02Z [qa] built → qa: under attack
- 2026-09-02T19:32:03Z [qa] qa → done: ATTACKED THE CONTENT, not the shape. Stored checks re-run verbatim on the landed tree (3254db3): exit 0/0/0; receipt GREEN. Own parse of the file (not the builder's script): 15 '## ' blocks, each with a Silent/Contradictory-at line naming a contract FILE + SECTION, a Decided-by and a Recorded-at; the two trailing sections are '###', so the count check is load-bearing. One commit for the file (50dad3c), so 'rewritten whole' holds.

CITATION AUDIT — I opened every cited location and checked it does NOT decide the point. admin-build.md Ground rules (E1 quotes both halves verbatim: STAGING_* refusal vs 'never repointed'; the doc never says which names the APP reads), tests 2/3/5/10/11 + 13 (E2/E14: parity is required, fixture creation unmentioned), test 9 (E8), 'Tests that must pass' names no runner (E11). admin-observability §5 'per-source awaiting_row trend against its pattern threshold' (E3) and 'server-side queries, not database views' (E9/E10 — no db-max-rows anywhere); §4 'the counts as columns' with adapters.md absent from contracts/ and no contract naming records_parsed/claims_emitted/records_unlinked/failure_class (E6 verified by grep); §8 covers only the M2 override form (E4) and 'per-field provenance shows at the edit surface' (E5); §6 (E12); §10 'nothing is re-encoded from scraper YAML' (E3). LOOK_AND_FEEL bar 5, bar 1 (1440x900), bar 12, and 'counts sit in fixed positions' (l.267-268) all verbatim (E5/E7/E13).

POINTERS RESOLVE: DECISIONS.md holds exactly 23 dated paragraphs, and all 23 map (12 to E8-E15, 5 to Ben's, 6 unmapped by design); ARCHITECTURE §4.1/§4.3/§5/§8/§10/§12 each say what is claimed (§12 carries the settled five-question paragraph AND the OPEN-CLAIMS-COST marker, exactly one open item); violation-ledger classes 4-5 exist (E15). Landed-code claims spot-checked myself: no STAGING_ under src/, DB_URL read by nothing, ROW_CAP=1000 + readCount refusal (result.ts:404-434), TABLE_ABSENT/COLUMN_ABSENT code sets (result.ts:61,67), middleware is 'export { auth as middleware }', next.config.ts:89-91 beforeFiles rewrite, nine runs columns (runs.ts:69-88), ErrorLine.reading required, Empty holds/filledBy, config.ts has NO display field (so E4's 'not landed' is true).

BEN'S SEVEN ARE HIS: compared word-for-word against the dispatcher relays on TASK-0021 (env + pg-driver half left to architect), TASK-0022 (groups/idols write-and-sweep; resolver tables never), TASK-0023 (the nine columns, listed identically), TASK-0024 (16:28 ruling + 18:50 standing rule + 18:56 M2 extension), TASK-0025 ('keep the current rendering'), TASK-0029 (display lists verbatim), and the 2026-09-02 digest ('desktop-only; both light and dark stay'). Contrast figures in the routed section (3.22/3.20/3.06/4.33/4.39/5.25/7.21/2.79) match TASK-0004's History exactly.

NOTHING OPEN SOLD AS SETTLED: the only blocked ASK is TASK-0031 and it appears solely in 'Questions routed rather than decided'; TASK-0029 (claimed), TASK-0032 (open), BUG-0027 (open) are each flagged 'open at this close' and their board states match.

CROSS-DIRECTORY RE-MEASURED BY ME in ../kspace Scraper: git log --all --grep=admin-window = 0; every commit prefix since 2026-09-01 is that repo's own (resolver/, qa:, architect:, tracker:); its RUNNING marker is present. 75 ticket files, 0 touch_scope entries matching Scraper or '../'.

ATTACK — the missing sixteenth. Weighed every judgment source: the 6 unmapped DECISIONS paragraphs (shapeOf total, scraper read-only, offline .ts tests, .next clear, pure domain leaves, tsconfig exclude) and the judgment blocks on TASK-0002/0004, BUG-0005/0016/0017/0029/0030. Best candidate is shapeOf defaulting to data_conflict_fact — rejected: migration 20260901000002 CHECK-constrains queue to two values, so the branch is unreachable and it is recorded as ARCHITECTURE §6 trap 11; it does not displace any of the fifteen. 'Scraper read-only' fails the entry test outright — §10 is explicit, not silent. The three candidates I was told to test are already in: count ?? 0 -> refusal IS E9, the notFound()/rewrite IS E12, verdicts absent-vs-empty is E8 + tables.ts:11-13. Verdict: these are the right fifteen.

TWO RESIDUALS, neither a bug, neither reachable from a criterion. (1) Preamble overreaches: 'Every one arrived through a blocked ASK ticket' is true of six of Ben's seven — E7 (desktop-only + both themes) arrived as a taste call in the 2026-09-02 digest with no ASK ticket, which E7's own Recorded line honestly reflects by citing DECISIONS.md alone. (2) Cross-directory sub-clause 'no commit message in that repo names this campaign or this repo': commit 0db3e6eb (2026-08-11, Ben, scraper intake) has 'kspace/kspace-Admin repo boundary' in its body — three weeks before M1 and outside the measured window, so the load-bearing zero is untouched; its '260 commits since 2026-09-01' now reads 262, ordinary drift in a live sibling campaign. [resolution: fixed] [receipt green: 3 cmd(s), tree a6f95d2a]
