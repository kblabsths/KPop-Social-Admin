---
id: DEBT-0012
type: debt
status: done
milestone: patch
priority: P3
opened_by: architect
parent: null
children: []
assignee: null
claimed_at: null
depends_on: [BUG-0141]
touch_scope: [src/lib/db/review-items.ts, tests/offline/review/review-items.test.ts]
force: false
attempts: 1
resolution: fixed
---
# readPopulation issues all three shape counts on every narrowed /queues URL, including the two the URL's own kind cannot render

## Description
M2 structure walk finding (architect, 2026-09-09), measured on run/admin-window.

`readPopulation` (`src/lib/db/review-items.ts:203-231`) maps over `SHAPES` — all three of them — and issues one `readCount` per shape, unconditionally:

    const answered = await Promise.all(
      SHAPES.map((shape) => readCount(T.reviewItems, (client) => countQuery(client, shape), db)),
    );

It is called from `readReviewQueues` (:291) whenever `narrows(filter)`. On `/queues?kind=signal` only `entity_link_source_pattern` can render a row, and on `?kind=decision` only the other two — so one or two of the three counts are read, folded into a `population` entry, and thrown away by a page that renders no block of that kind.

They are `head: true` counts issued in parallel, so the cost is bounded: two extra round trips on a `?kind=`-narrowed URL, none on the others. No number on screen is wrong — this is waste, not a defect, which is why it is P3 debt and not a bug.

It is filed because it is the same shape as the two costs M2 did measure and did file: BUG-0138 (`/claims` reading the whole population every request, ~14 sequential round trips, 2.9-3.8s warm) and BUG-0139 (`/sources` reading runs once per registered source, ~9 round trips, 2.0-2.3s warm). A read that answers questions the URL did not ask is the class those two belong to, and this is the third instance.

## Acceptance criteria
**Amended 2026-09-09 (architect) — criteria 1 and 5 only.** Criterion 1's
original direction ("a count per shape OF THAT KIND, and no others") was
BACKWARDS, and the builder measured it before implementing it. Criteria 2, 3, 4
and 6 are unchanged.

1. **`readPopulation` issues a count only for the kinds whose population the
   page's rendering actually CONSULTS — the kinds the URL NARROWS, which is the
   opposite of the kind it NAMES.** `isSurfaceNarrowed`
   (`src/lib/url/narrowing.ts`) ANDs fact 1 (structural: can any facet of this
   URL remove a row of this kind at all) with fact 2 (this kind's whole-table
   population), so for a kind whose fact 1 is false no count could change a
   word its block renders — that is the read this ticket is about. Both blocks
   ALWAYS render (`src/app/queues/page.tsx` maps `KINDS`), and the kind a URL
   EXCLUDES is exactly the one whose zero has to be explained: its rows are all
   gone from the filtered read, which is precisely the count BUG-0133 made
   load-bearing. So `/queues?kind=signal` skips the SIGNAL count and issues the
   DECISION ones. The set is one call of the same predicate the page asks per
   block — `isNarrowedBeyond(filter, narrowingOfKind(kind))` per kind — declared
   once beside that predicate in `src/lib/review/queue-filters.ts` and imported
   by `src/lib/db/review-items.ts` rather than retyped, so the set the read
   ISSUES counts for and the set the page CONSULTS cannot come apart.
   A kind that was not counted answers a declared THIRD state, distinct from a
   refusal (nothing failed, nothing to report) and from a counted zero (a zero
   is a claim about the table, and this state makes none); its block falls back
   to fact 1 alone, which is `false` by construction for exactly those kinds.

2. The bare `/queues` still costs exactly ONE query — an unnarrowed read IS the
   whole table, so its own rows are the population (the property the function's
   own docstring states today).

3. Every population figure and every empty-state arm on `/queues` is identical
   to today's for every facet combination the existing suites cover, including
   the four states BUG-0129/0131/0133 settled and the per-kind refusal reporting
   BUG-0135 landed. A kind whose counts were not read renders exactly what it
   renders now, because the page never showed them.

4. A refusal of one shape's count still says nothing about the others, and is
   still carried per kind as its own `DbResult` reported beside the rows —
   BUG-0135's ruling, unchanged. The third state is NOT a refusal and reports
   nothing.

5. **The saving is graded, not asserted, and the numbers are measured rather
   than authored.** A test counts the reads issued against the stub client for a
   bare `/queues`, a `?kind=`-narrowed URL and a non-kind-narrowed URL, and
   names WHICH count legs make up each number — not merely how many, because the
   stub answers per-shape counts BY POSITION and a URL that skips a shape would
   otherwise let a count answer the wrong leg. `review_items` reads = 1 row leg
   + one count leg per shape of a narrowed kind (decision has two shapes, signal
   one). Measured on run/admin-window, 2026-09-10:
   - bare `/queues` — **1** (no count issued at all; criterion 2)
   - `?kind=decision` — **2** (was 4): the signal kind's single shape
   - `?kind=signal`, `?queue=entity_link`, `?shape=entity_link_source_pattern` —
     **3** (was 4): the decision kind's two shapes. The last two IMPLY the
     signal kind, so they narrow it no further than it narrows itself.
   - `?status=open`, `?source_id=<a carried source>`, `?queue=data_conflict`,
     `?shape=data_conflict_fact` — **4**, unchanged: no kind's own narrowing
     implies a status or a source, and the decision kind spans both queues and
     both fact shapes, so BOTH blocks have a zero to explain and all three
     counts are load-bearing.

6. `npm run lint` and `tsc --noEmit` clean.

## Checks
```sh
./node_modules/.bin/vitest run --project=offline tests/offline/review tests/offline/queues
bash -c "rm -rf .next/types && npm run lint && ./node_modules/.bin/tsc --noEmit"
grep -q "kindsNarrowedBy(filter)" src/lib/db/review-items.ts
grep -q "kind: \"not_asked\"" src/lib/db/review-items.ts
```
## History
- 2026-09-09T18:58:30Z [architect] opened: readPopulation issues all three shape counts on every narrowed /queues URL, including the two the URL's own kind cannot render
- 2026-09-10T00:08:11Z [builder-230] claimed (attempt 1)
- 2026-09-10T00:26:24Z [builder-230] claimed → built: DONE, but criterion 1's DIRECTION is inverted by measurement — see bullet 2.

- `readPopulation` now counts only the shapes of the kinds `kindsNarrowedBy(filter)` names (new export in src/lib/review/queue-filters.ts, one call of `isNarrowedBeyond(filter, narrowingOfKind(kind))`, imported so lib/db does not retype it). Other kinds answer `{kind:"not_asked"}` — a third `KindPopulation` state, distinct from a refusal and from a counted 0. The page reports nothing for not-asked (nothing failed) and falls back to fact 1, which is `false` BY CONSTRUCTION for exactly those kinds, so no figure and no empty-state arm moves. Beyond touch_scope: queue-filters.ts (the predicate) and src/app/queues/page.tsx (one line: not-asked is not a refusal).

- THE INVERSION. The kind the URL NAMES is the one whose count can be skipped; the kind it EXCLUDES is the one BUG-0133 needs a count for — its rows are all gone from the filtered read, and BOTH blocks always render (page.tsx:697 maps KINDS). Criterion 1's literal reading (count only the named kind's shapes), measured on this tree: 17 offline tests red, incl. tests/offline/queues/page.test.ts "does not blame a filter for a zero on a queue that holds nothing anyway (admin-window/BUG-0133)" — its `kind=signal` case flips the decision block from `nothing open` to `nothing open in this filtered view` and `data-empty` from `queue` to `narrowing`: BUG-0133's defect returns. So criterion 1's 2nd sentence and criterion 5's numbers need amending to what landed. What landed reddens 1 test: the lib pin asserting BOTH counts on a narrowed URL, in touch_scope, rewritten.

- Measured `review_items` reads per URL (stub call log; pinned by "issues exactly the reads each URL shape needs"): bare 1; `?kind=decision` 4->2; `?kind=signal` / `?queue=entity_link` / `?shape=entity_link_source_pattern` 4->3; `?status=` / `?source_id=` / `?queue=data_conflict` / `?shape=data_conflict_fact` 4 (unchanged: both blocks have a zero to explain).

Verified, rebased tree: stored checks green (602 in tests/offline/review+queues; page.test.ts's 116 UNTOUCHED), lint + tsc clean, grep check passes, `npm test` 3250+6, `npm run test:live -- tests/live/queues.live.test.ts` 14 green vs staging (read-only). Red-then-green: the 4 new/changed assertions fail with `asked = KINDS`.

TRAP: withLegs/tableHolding script per-shape counts BY POSITION, so a test whose URL skips a shape must name them (`withLegs(rows, rows, DECISION_SHAPES)`) or a count answers the wrong leg.
- 2026-09-10T00:26:51Z [dispatcher] landed ticket/DEBT-0012 onto run/admin-window as b7da394
- 2026-09-10T00:29:46Z [architect] criteria amended: Criterion 1's direction was inverted and the builder measured it: count the kinds the URL NARROWS, not the kind it names. Both blocks always render, so the EXCLUDED kind is the one whose zero BUG-0133 needs a population for; the literal original reddens BUG-0133's own pin (verified independently by architect-amend-0012: flipping kindsNarrowedBy to its negation reddens 21 offline tests, incl. 'does not blame a filter for a zero on a queue that holds nothing anyway' with data-empty queue->narrowing). Criterion 5 now carries the measured per-URL read counts. Criteria 2/3/4/6 unchanged. [was: 1. `readPopulation` issues a count only for the shapes whose kind the caller will render. A URL naming one kind costs on… (1160 chars; full text in git history)]
- 2026-09-10T00:29:54Z [architect] checks amended: Replaced the absence check (! grep SHAPES.map) with two PRESENCE checks: the absence form greens an INVERTED implementation too (shapesOfKind(filter.kind).map passes it), so it graded no direction at all, and a negated absence check silently greens when code merely moves (ARCHITECTURE Common violations / BUG-0035 class). kindsNarrowedBy(filter) pins the amended direction; not_asked pins criterion 1's third state. Both dry-run exit 0 on run/admin-window @ landed b7da394; the two suite checks are unchanged and green (602 offline). [was: ./node_modules/.bin/vitest run --project=offline tests/offline/review tests/offline/queues, bash -c "rm -rf .next/types … (234 chars; full text in git history)]
- 2026-09-10T00:32:21Z [qa] built → qa: under attack
- 2026-09-10T00:46:44Z [qa] qa → done: Attacked on the landed tree (205963c) in a detached _receipt- worktree. Stored checks re-run VERBATIM before reading the builder's story: all four exit 0. All six (amended) criteria verified; full offline 3252 pass, lint + tsc clean, live queues 14 pass vs staging read-only.

MEASURED, not asserted. Exhaustive probe over all 216 URLs the facet vocabulary spells (kind x queue x shape x status x source, incl. absent): reads = 1 (bare, x1), 2 (?kind=decision, x1), 3 (x7), 4 (x207). The ticket's table names 9 of those 216; the other 4 three-read URLs are combinations of signal-implying facets and are all correct. Criteria 2 and 5 hold exactly as written. No URL where narrows() is true yields zero counts: a filter within BOTH kinds' implied narrowings is unspellable (kind cannot be both), so >=1 count always issues.

Criterion 1's last clause PROVEN, not sampled: page.tsx:327 binds ownNarrowing = narrowingOfKind(kind) and :371 falls back to isNarrowedBeyond(filter, ownNarrowing) — byte-identical expression and arguments to kindsNarrowedBy's filter predicate (queue-filters.ts:429), over the same filter object the page hands readReviewQueues (:685). A not_asked kind whose block reads as narrowed is therefore impossible by construction, not by luck. ReviewItemFilter's 5 fields are exactly NARROWING_FACETS, so narrows() and isNarrowedBeyond see the same field set.

not_asked rendering, driven on all 8 skipping URLs: zero population sub-surfaces, no counted zero, correct arm every time. Union exhaustiveness attacked by adding a 4th KindPopulation variant: tsc reddens at page.tsx:419 — the negative-space refusal classification (ok||not_asked ? undefined : refusal) cannot silently absorb a new state. Sabotage rounds: negating kindsNarrowedBy reds 21 offline tests; criterion 1's ORIGINAL direction (count the kind the URL names) reds 10 incl. BUG-0133's own pin — independently reproducing the builder's and architect's measurement. Reporting not_asked as a refusal reds 4. Reading not_asked as a counted 0 reds nothing, and provably cannot: isSurfaceNarrowed short-circuits on fact 1 = false, so the two arms are equal — a theorem, not a gap.

ONE DEFECT FOUND, in test code, fixed by me (product source untouched): tests/offline/queues/page.test.ts's tableHolding scripted all three counts by POSITION while the read now issues fewer. Expected: the signal population on /queues?kind=decision is the entity_link_source_pattern count. Found: it is the data_conflict_fact count (page.test.ts:176-189, stub dispenses positionally). Over a signals-only table that reads 0, and the signal block renders data-empty="queue" where the truth is "narrowing" — BUG-0133's forbidden conflation, manufactured by the fixture and invisible because page.test.ts:1647 asserts the figure but not the arm. Latent today (no assertion wrong), and the builder's "page.test.ts's 116 UNTOUCHED" is exactly what hid it. Watched red ("expected 'queue' to be 'narrowing'"), then fixed the fixture and pinned both arms on one render; also extended the page cost pin to the skipping URLs and added the 216-URL exhaustive lib pin. Committed d5b16d4.

Staging walk substituted, stated honestly: I did NOT stand up a production build on my own port. The vetted staging path here is the guard-enforced live suite (STAGING_* + SERVICES.md declaration); .env's bare SUPABASE_* is NOT guaranteed staging (reset-sandbox reads that pair), and I may not read .env to check, so pointing a server I start at it risked production. Ran tests/live/queues.live.test.ts instead: 14 green against real PostgREST, incl. "counts each kind's whole-queue population, and it is the census the database holds" and "renders a faceted URL with no block reporting a count it could not make" — this ticket's exact surface. Both themes not walked and not load-bearing: the theme is pure @media (prefers-color-scheme) in globals.css with no dark: classes and no data-theme attribute, so server markup is theme-identical, and CSS conformance is the designer's jurisdiction.

Residual (no ticket): a 6th field added to ReviewItemFilter without adding it to NARROWING_FACETS would make narrows() true, kindsNarrowedBy empty and both blocks fall back to fact 1 = false, so a block that facet emptied would claim its own zero. Pre-existing — isBlockNarrowed was already fact-1-gated, so DEBT-0012 changed the read count, not the rendering. filters.test.ts:577 pins the list's contents but not that the filter has no extra key. Trigger for a ticket: the next facet added to /queues. [resolution: fixed] [receipt green: 4 cmd(s), tree 8d196fc7]
