---
id: BUG-0088
type: bug
status: done
milestone: M2
priority: P2
opened_by: qa
parent: FEAT-0009
discovered_from: qa:TASK-0046
children: []
assignee: null
claimed_at: null
depends_on: []
touch_scope: [agenticflow/tracker/for-human/M2-handoff-settle-review-item.md, tests/offline/handoff/settle-review-item.test.ts]
force: false
attempts: 1
resolution: fixed
---
# choose_claimed_value adopts ANY observation in the ledger, not one of the item's evidence claims

## Description
**Expected** — `contracts/admin-observability.md` §7 spells the `data_conflict` answer as "**choose a claimed value** (one tap per evidence card)": the value adopted is one of the ITEM'S OWN evidence claims. The artifact says the same of itself, in the comment on this very arm (`M2-handoff-settle-review-item.md:526-528`): "the admin ADOPTS a claimed value, which means re-asserting it in their own voice. The value is read off the claim rather than re-sent by the dashboard, so the two cannot differ." The function's own stated posture (line 146) is "What it refuses is a decision that would write something other than what it says."

**Found** — the `choose_claimed_value` arm reads the observation by PRIMARY KEY ALONE, with no predicate binding it to the item or to the fact:

    agenticflow/tracker/for-human/M2-handoff-settle-review-item.md:537-540

        select claim.value
          into v_claim
          from public.observations as claim
         where claim.observation_id = (v_value ->> 'observation_id')::uuid;

Measured on the landed tree (commit c9b8133, run/admin-window), by extracting that statement's WHERE clause with the suite's own reader and asserting it names any binding:

    AssertionError: expected ' claim.observation_id = (v_value ->> …' to match
      /v_item\.evidence|claim\.domain|claim\.entity_id|claim\.field/
    + Received: " claim.observation_id = (v_value ->> 'observation_id')::uuid"

So ANY `observation_id` in the ledger is adopted. Its value is then re-ingested through the gate under the admin source for the DECISION's `domain`/`entity_id`/`field` (lines 581-589) and applied with `tier_at_apply = 'admin'` and `admin_locked = true` (lines 657-667) — `contracts/resolver.md` §8's admin stickiness, which no scraper claim can ever displace. The wrong value lands on the RIGHT row (the new observation carries the decision's own fact identity), so nothing downstream detects it.

The gate is not the backstop: it validates the adopted value against the TARGET field's declaration only, so any same-shaped foreign value passes. Concretely, on the installed events v3 / venues v2 schemas (`kspace Scraper/supabase/migrations/20260829000004`), adopting a `venues.name` claim (string, 1..200) into `events.title` (string, 1..300) validates and applies, admin-locked, forever.

**The asymmetry is the tell.** The artifact already refuses the analogous mismatch on the other side — lines 463-479 refuse a decision whose `domain`/`entity_id`/`field` differ from the item's, with its own message ("item % is about a different fact"). It guards item↔decision and leaves claim↔decision unguarded. And the binding datum the contract names is already in hand: `v_item.evidence` is read three statements later, at line 644, for the rejection set.

**Repro (offline, no database — the artifact is a text handoff nothing here executes):**

    ./node_modules/.bin/vitest run --project=offline tests/offline/handoff/settle-review-item.test.ts

The pin is landed as `it.fails(…)` (strict xfail, house convention — cf. BUG-0087): it goes RED the day the arm is bound, which is the signal to flip it back to `it`. Watched red as a plain `it` before pinning; the failure text above is that run.

**The fix is a predicate, not a redesign.** Either binding satisfies the pin and the contract: constrain the read to `claim.observation_id = any (v_item.evidence)` (the contract's own words, and it also refuses an override adopting a claim — `override` cannot fill `observation_id` today, so the arm is item-only in practice), or constrain the claim's own fact triple to the decision's `v_domain`/`v_entity_id`/`v_field`. Raise `KS027` on a miss, in the shape the neighbouring refusals already use, and add the citation to §4 of the note so Ben reads the choice rather than inherits it.

**Why it is filed and not waved through**: this artifact is pasted by hand into a repo this campaign may not enter, and the defect it would install is permanent by design (`admin_locked`). It is one predicate before the paste and an unpicked, unprovenanced canonical value after it.

## Acceptance criteria
(to be written)

## Checks
```sh
./node_modules/.bin/vitest run --project=offline tests/offline/handoff/settle-review-item.test.ts
bash -c 'grep -q "it(\"adopts a claimed value only from a claim bound to the fact being settled\"" tests/offline/handoff/settle-review-item.test.ts'
```

## History
- 2026-09-09T04:07:18Z [qa] opened: choose_claimed_value adopts ANY observation in the ledger, not one of the item's evidence claims
- 2026-09-09T04:11:15Z [builder-148] claimed (attempt 1)
- 2026-09-09T04:21:12Z [builder-148] claimed → built: - SQL: the `choose_claimed_value` read is now bound four ways — the id sent, `claim.observation_id = any (v_item.evidence)`, and `claim.domain/entity_id/field` = the decision's — and a miss raises one KS027 naming the claim, the item and the fact (replacing 'no observation %'). §4 of the note gains item 5 (heading now 'five things') stating the choice and the looser/stricter escapes; §5 lists the new grader assertion. No .sql file, no SQL executed, still one fenced block.
- Grader: QA's `it.fails` flipped to `it` (same test name the stored check greps) and strengthened to require BOTH bindings; second pin asserts the miss is refused with a KS code naming evidence + observation_id + review_item_id. gradeSettle gains adopted_claim_unbound:<part>, adoption_miss_unrefused/uncoded/unnamed, proved on three doctored blocks (PK-only read → 4 findings; guard test removed → unrefused; predicates reordered with any(...) unspaced → grades clean).
- Why both bindings, for QA: evidence membership alone is insufficient because a per-source review_item's subject is a SOURCE (domain/entity_id/field null, evidence spans fields, sibling 20260901000002), so it would still permit one field's value into another; entity_id is compared strictly, so an unlinked claim (entity_id null) is refused by design — that is the one over-refusal risk and the note tells Ben how to loosen it. Product code needs no change: conflict-actions.tsx already builds one control per evidence card from the item's own fact.
- Verified on the rebased tree (HEAD 345d2e1 on run/admin-window): vitest offline tests/offline/handoff/ 66 passed; tsc --noEmit exit 0; npm run lint exit 0; full offline suite in a detached _receipt- worktree under evidence/ exit 0 (66 files / 2450 tests + isolated 6). No .env opened, no port bound.
- 2026-09-09T04:21:37Z [dispatcher] landed ticket/BUG-0088 onto run/admin-window as 85c4cfc
- 2026-09-09T04:22:18Z [qa] built → qa: under attack
- 2026-09-09T04:39:44Z [qa] qa → done: attacked (lean, one integrated pass). Stored checks re-run verbatim on the landed tree before reading the builder's story: both exit 0; receipt GREEN.

BOUND READ, against the sibling's installed shapes (read-only): review_items.evidence is `uuid[] not null default '{}'` (20260901000002:48) and v_item is `public.review_items%rowtype`, so `= any (v_item.evidence)` is well-typed against `claim.observation_id uuid`; observations.domain/field are NOT NULL and entity_id is nullable (20260818000000:3818), so the fact triple compares as intended. Builder's "membership alone is insufficient" confirmed at source: 20260901000002:11-14 and its column comment :87 — a per-source item carries domain/entity_id/field ALL NULL, and the earlier item-vs-decision guard (:463) only compares parts the item NAMES, so without the triple a per-source item's evidence would still let one field's value into another.

OVER-REFUSAL, chased: v_entity_id can never be null in this arm (:375 refuses a value-carrying verdict missing any of the three), so `claim.entity_id = v_entity_id` refuses a claim whose entity_id is null. For a data_conflict that is unreachable — a per-fact item's evidence is the weighing lane's claims for one fact key (resolver/cycle.py:_the_item), which carry that entity_id; unlinked claims have no fact and key the entity_link queue. Stated for Ben twice (SQL comment "a claim with no canonical row yet has no fact identity", §4 item 5).

NO PARTIAL WRITE: the refusal at :566 precedes every write in the body — ingest_observation :607, confirmed_matches :643, review_items :786, verdicts :793 — and the block carries no `exception when` handler, so nothing catches and continues. The `for update` at :440 is a lock, not a write.

GRADER, doctored four ways the shipped fixtures do not cover: two of three fact parts bound -> ["adopted_claim_unbound:field"]; evidence only -> the other three findings; the read deleted -> ["no_adoption_read"]. Correct.

FILED BUG-0093 (P2), the one finding: KS027/KS028 are ALREADY the sibling's. `tools/staging/harness_objects.sql:2026,:2121` raises them; `tests/helpers/ks_codes.py:122,:128` names them LEASE_HELD_BY_ANOTHER / LEASE_NOT_LIVE; and the sibling's own witness says why a migrations-only grep — the one §3's citation row cleared these codes with — misses exactly them ("resolver/BUG-0044's two lease codes were the first", test_codes_named_once.py:29-38). On paste, test_no_code_the_migrations_raise_is_left_unnamed_by_the_harness goes red on the unnamed KS029/KS030. Pinned `allocates codes the sibling has not already taken`, watched RED as a plain it (expected [ 'KS027','KS028' ] to deeply equal []), landed it.fails in commit 263f35d. NOT a bounce of this ticket: the allocation predates the fix, spans 20 KS027 sites, and renumbering is a whole-file change this ticket's scope never named.

RESIDUALS (not tickets): (a) the grader accepts a refusal whose `raise exception` is downgraded to `raise notice` — errcode is still present, so adoption_miss_unrefused stays silent; trigger = any future edit to that raise. (b) The shared reader keeps comments inside `$$…$$` verbatim (documented), so commenting out all four predicates grades clean — same trigger. (c) A legal respelling (`in (select unnest(...))`) grades adopted_claim_unbound:evidence — only bites if the artifact is respelled. (d) §4 item 5's loosening recipe says drop all three fact predicates, but the one over-refusal it names is loosened by dropping the entity_id line alone.

VERIFIED: supabase/migrations/ still two files, no .sql added on the branch, no SQL executed, sibling tree clean. tsc 0, eslint 0, offline 67 files/2493 tests and isolated 6/6 green in a detached `_receipt-qaBUG0088` worktree, removed after. (The primary checkout's `npm run lint` reports 338 errors — every one of them from a stale `.worktrees/_receipt-qaBUG0086` left by another lane, not mine to delete.) [resolution: fixed] [receipt green: 2 cmd(s), tree 859d9d3e]
