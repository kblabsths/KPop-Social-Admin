---
id: BUG-0128
type: bug
status: done
milestone: M2
priority: P2
opened_by: qa
parent: null
discovered_from: BUG-0124
children: []
assignee: null
claimed_at: null
depends_on: []
touch_scope: [src/components/review/shape-views.tsx, tests/offline/review-item/page.test.ts]
force: false
attempts: 1
resolution: fixed
---
# The entity_link fact lede still claims every claim the record holds is below

## Description
Found while attacking BUG-0124 (QA, 2026-09-09). BUG-0124 retired the source-pattern lede's completeness claim; the SAME claim stands one shape over, on the same block, and the fix's new header sentence now sits above it.

**Expected** (BUG-0124 criterion 2 and the designer's Expected: LOOK_AND_FEEL Voice bar 6 — 'A screen may not assert a completeness its own read cannot support'): a shape's lede says what the block below HOLDS and claims no total. Admin reads the ids in `review_items.evidence` and resolves each one; it never reads a record's claims, so 'every claim held by [the record]' is a population this page has not looked at.

**Found**, `src/components/review/shape-views.tsx:291` (`StuckFactEvidence`), rendered verbatim on the `entity_link` FACT shape:
'This record cannot link or be created. **Every claim held by it is below**, with what each one is waiting for.'

The page contradicts it out of its own mouth on a fixture where one evidence id resolves to nothing. Repro, offline, no database:

1. `cd '/Users/ben-m4/Desktop/Coding/KPOP/kspace Admin'`
2. In `tests/offline/review-item/page.test.ts`, render `stuckScript` with the item overridden to `reviewItemEntityLink({ evidence: [ID.observationB, '01920000-0000-7000-8000-000000000999'], folded_count: 700 })` — i.e. two evidence ids, one of which names no observation. That is exactly the pinned test 'no shape's lede claims a completeness its read cannot support > does not hold on the entity_link fact shape' now in that file.
3. The one page then carries, simultaneously:
   - lede: 'Every claim held by it is below, with what each one is waiting for.'
   - accounting: '1 of 2 evidence ids resolved to a claim, in the order they folded in.'
   - unresolved line: '1 of this item's evidence ids names no claim this database holds: 01920000-…-000000000999'
   - header (BUG-0124's new sentence): 'Each fold is a re-ask of this item, not a record: 700 folds over the 2 evidence ids it carries, listed below.'

Measured by running that test with `it.fails` removed:
`AssertionError: expected 'This record cannot link or be created…' not to match /\b(every|all|complete|entire)\b/i` — received 'This record cannot link or be created. Every claim held by it is below, with what each one is waiting for.' The conflict and source-pattern ledes pass the same guard.

This is the reading failure both M2 user-sims reported (a completeness claim beside a fold count that cannot be reconciled), on the shape BUG-0124's criteria did not name. It is pre-existing — `git show cab3efd` did not touch this lede — and is filed rather than folded into BUG-0124, whose own criteria are met.

The pin is landed strict: `it.fails("does not hold on the entity_link fact shape (BUG-0128)")` in `tests/offline/review-item/page.test.ts`. Fix the lede and it turns red — that is the signal to fold the shape into the passing per-shape test above it and delete the pin.

## Acceptance criteria
1. The `entity_link` fact shape's lede (`StuckFactEvidence`, src/components/review/shape-views.tsx) claims no completeness this page's read cannot support: it says what the block below holds — the claims behind this item's evidence ids, with what each one is waiting for — and asserts nothing about claims the page never read. Same register, no apology, no hedging word.
2. The conflict and source-pattern ledes are unchanged, and no figure enters any lede.
3. The strict pin 'does not hold on the entity_link fact shape' in tests/offline/review-item/page.test.ts is folded into the passing per-shape test ('holds on the conflict and source-pattern shapes' becomes all three shapes) and the pin is deleted — the rule is graded once, for every shape.
4. `npm run lint` and `tsc --noEmit` clean; the offline suite green.

## Checks
```sh
bash -c 'rm -rf .next/types && npm run lint && ./node_modules/.bin/tsc --noEmit'
./node_modules/.bin/vitest run --project=offline tests/offline/review-item/page.test.ts
```

## History
- 2026-09-09T14:16:29Z [qa] opened: The entity_link fact lede still claims every claim the record holds is below
- 2026-09-09T14:20:07Z [builder-194] claimed (attempt 1)
- 2026-09-09T14:20:09Z [qa] Parent cleared to null (it was filed with --parent BUG-0124, a BUG, which blocked that ticket's close); the link is carried by discovered_from: BUG-0124, which is the accurate relation.
- 2026-09-09T14:28:13Z [builder-194] claimed → built: - Lede (shape-views.tsx, StuckFactEvidence): 'Every claim held by it is below' is gone; the entity_link FACT lede now reads, transcribed off the rendered markup: 'This record cannot link or be created. The table below holds the claims behind this item's evidence ids, with what each one is waiting for.' Same register, no hedge, no figure, and the population is the one the accounting sentence under it accounts for — matching BUG-0124's source-pattern wording. Conflict and source-pattern ledes byte-unchanged (the pattern lede re-read off the same render: 'One source, many records stuck the same way. The table holds the claims behind this item's evidence ids; …').
- Test (page.test.ts): QA's strict pin is DELETED and the shape folded into the per-shape test, now 'holds on all three shapes' over unfiltered SHAPED — the completeness rule is graded once, for every Shape. The pin's harder fixture survives as a passing test, 'names the population on the fact shape where an evidence id resolved to nothing' (2 evidence ids, 1 orphan): it re-asserts the page's own read (accounting [1,2], the unresolved id) and then that the lede names 'evidence ids' and carries no digit. It does not re-run CLAIMS_A_TOTAL, so the ban still has exactly one grading site.
- Verified red-then-green, all on the REBASED tree (7c00266, run/admin-window at 482d65f): with the source at HEAD the two new/folded tests fail for the right reasons ('stuck: expected … not to match /\b(every|all|complete|entire)\b/i' and 'expected … to contain evidence ids'); with the fix they pass. Before I touched the test file I watched the pin itself go red on the fixed source ('Error: Expect test to fail') — that is the signal the ticket named. Stored checks exit 0 (lint + tsc --noEmit; page.test.ts 101/101); npm test 2899 offline + 6 isolated; npm run test:live -- tests/live/review-item.live.test.ts 12/12 against staging, read-only.
- For QA: staging's one open item is the source-pattern shape, so the entity_link fact lede is not reachable on the walk instance today — offline render is the only surface it has. The pre-fix source was read with 'git show HEAD:…' (no stash). Worktree carries symlinked node_modules and .env (gitignored).
- 2026-09-09T14:28:32Z [dispatcher] landed ticket/BUG-0128 onto run/admin-window as 205f756
- 2026-09-09T14:29:14Z [qa] built → qa: under attack
- 2026-09-09T14:38:59Z [qa] qa → done: Attacked offline on the landed tree (run/admin-window @ da85544), in my own detached worktree. No walk: staging's one open item is the source-pattern shape, so the entity_link fact lede has no live surface today — offline render is the whole surface it has, and I claim no browser evidence.

Criteria, re-run before reading the builder's story:
1. MET. Lede transcribed off the rendered markup: "This record cannot link or be created. The table below holds the claims behind this item's evidence ids, with what each one is waiting for." No completeness word on any of the three shapes (CLAIMS_A_TOTAL over unfiltered SHAPED), no digit in it, and the population it names is the one the accounting sentence under it accounts for.
2. MET. `git diff 482d65f da85544 -- src/` is exactly two hunks in shape-views.tsx (the doc comment and that one Lede); the conflict and source-pattern ledes are byte-identical, and no figure entered any lede.
3. MET. The strict pin is gone; the rule is graded once, over unfiltered SHAPED. The 2-id-1-orphan fixture survives as a passing test that re-reads the page's own accounting ([1,2]) and unresolved id.
4. MET. Stored checks verbatim: lint+tsc exit 0; page.test.ts 101/101 (104/104 with my additions). Full offline from my worktree: 71 files / 2902 passed, plus 6 isolated.

Attack, one pass over the batch surface:
- Curly apostrophe (BUG-0100's rule): NOT a blind spot, measured. copy.test.ts strips HTML entities before its PROSE_CHARS gate and U+2019 is in that class, so the lede is prose the scanner reads — glossaryViolationsIn over the new lede with claims->observations returns "1: observations - say claim" in BOTH the &rsquo; and the literal-curly spelling.
- Widening CLAIMS_A_TOTAL: measured, DECLINED, with the numbers so nobody re-runs it. "each" stands in 2 of 3 shipped ledes ("Each contending claim stands against...", "with what each one is waiting for"), scoped to the rows below — banning it would redden approved copy. "whole", "full", "any", "only" occur in no lede on any shape, and a ban word the corpus never exercises is the vacuous guard this repo's own LESSONS 3 warns about.
- A fourth Shape: SHAPED is HAND-LISTED. Record<Shape,...> forces a fourth VIEW to compile, nothing forced a fourth SWEEP entry, so the newly folded per-shape rule would have kept grading three while the comment claimed otherwise. Closed on my side of the line: new guard "the per-shape sweeps grade every Shape the app declares" (SHAPED length == SHAPES.length; the three fixtures' shapeOf == SHAPES).
- Zero evidence ids resolving: BROKE IT -> BUG-0130 filed. With one orphan id and nothing resolved, the fact view renders 0 <table> elements and an Empty card reading "No claims behind this item's evidence ids", under a lede saying "The table below holds the claims..." — the page contradicting itself again, one state over. The source-pattern lede does the same (BUG-0124's wording, evidence: []), the conflict lede names no table and is clean. Pinned strict: it.fails "holds when no evidence id resolves (admin-window/BUG-0130)" in tests/offline/review-item/page.test.ts, watched red first on this exact tree ("stuck: expected 'This record cannot link...' not to match /\btable\b/i"; both shapes red in the scratch run), with a passing non-vacuity test beside it proving the healthy state renders the table its lede names.

Verdict: this ticket's criteria are met and its goal is met — the fact lede now states the population the page actually read. BUG-0130 is the state neither this ticket's nor BUG-0124's criteria drove, it lands on BOTH ledes, and bouncing this one would have fixed half of it. Tests committed at d516303; receipt green. [resolution: fixed] [receipt green: 2 cmd(s), tree 2ab9c0d8]
