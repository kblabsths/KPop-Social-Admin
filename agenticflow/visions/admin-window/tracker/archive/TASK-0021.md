---
id: TASK-0021
type: task
status: done
milestone: M1
priority: P0
opened_by: architect
parent: FEAT-0002
children: []
assignee: null
claimed_at: null
depends_on: []
touch_scope: [src/lib/db/client.ts, tests/live, agenticflow/docs/SERVICES.md]
force: false
resolution: already_done
---
# ASK BEN: which Supabase env names does the APP read at runtime, and what do the campaign's live tests read?

## Description
**Contract silence.** ARCHITECTURE marker `OPEN-ENV`. Two rules the campaign must obey at once appear to point at different names:

- `contracts/admin-build.md` ground rule: *'Live work targets the staging project through `STAGING_SUPABASE_*` names ... an unset name is a refusal, never a fallback.'*
- `contracts/admin-build.md` ground rule: *'The deployed Railway service is never repointed by this campaign, and every push to `main` must leave the app deployable against whatever project the service targets.'*

The deployed service reads `SUPABASE_URL` / `SUPABASE_SERVICE_ROLE_KEY` (`src/lib/supabase.ts`, today). If the app's own code starts reading `STAGING_*`, the deploy breaks; if the live tests read `SUPABASE_*`, 'staging only' stops being structural.

Four things to settle, one answer:

1. **Which names does the app read at runtime?** (Recommend: `SUPABASE_URL` / `SUPABASE_SERVICE_ROLE_KEY`, unchanged — the service is never repointed.)
2. **How do the live tests point the app at staging?** (Recommend: `tests/live/setup.ts` is the one file that reads `STAGING_SUPABASE_URL` / `STAGING_SUPABASE_SERVICE_ROLE_KEY`, refuses when either is unset, and sets the process env for the live run only. Product code never mentions a `STAGING_` name. This keeps 'no fallback' true: the app has no knowledge of staging, and the tests have no fallback to the app's names.)
3. **Do the live tests get anything beyond URL + service key?** Acceptance test 2 says 'direct SQL on staging', but PostgREST cannot run SQL and the `supabase` CLI is an undeclared service the remote gate refuses. (Recommend: no database connection string. 'Parity' becomes: the page renders through `lib/db`, and the test issues its own independent PostgREST count — two paths to one number. If you want literal SQL, say so and name the connection-string variable; it would be a new secret name and a new dependency.)
4. **`agenticflow/docs/SERVICES.md` declares no service.** The remote gate refuses an undeclared service's CLI, and the live-test guard is designed to refuse a staging host that is not the one declared there. (Recommend: add a `## supabase` section naming the staging project as the target agents may touch. Names only — no key, ever.)

Also, for the record: **`.env` today carries `SUPABASE_URL` and `SUPABASE_ANON_KEY` and no service-role key**, so a local dev run or a walk cannot read anything through the service-role client. Whether that gets a local service-role key is part of this answer. No value of any of these names appears in this ticket, and none may appear in any transcript, commit or note.

**Blocks nothing structural**: TASK-0002 builds `client.ts` reading the app's existing names and TASK-0003 builds the live guard, both written so that this answer changes those two files and nothing else. What it does block is any live-marked test actually running.

## Acceptance criteria
(human-checkable) Ben answers all four questions on this ticket. The architect then removes the `OPEN-ENV` marker from ARCHITECTURE's open-questions list and amends TASK-0002 / TASK-0003 if the answer differs from the recommendation. If the answer adds a `## supabase` declaration, `agenticflow/docs/SERVICES.md` carries it.

## Checks
```sh
bash -c "! grep -q OPEN-ENV agenticflow/docs/vision/ARCHITECTURE.md"
bash -c "! grep -rn STAGING_SUPABASE_DB_URL src tests | grep -qvE \"^[^:]*:[0-9]+:[[:space:]]*(\*|//|/\*)\""
bash -c "! grep -rn \"\\bfrom .pg.\\|require(.pg.)\\|postgres.js\" src tests"
```
## History
- 2026-09-02T05:45:08Z [architect] opened: ASK BEN: which Supabase env names does the APP read at runtime, and what do the campaign's live tests read?
- 2026-09-02T05:45:08Z [architect] open → blocked: ASK BEN: the app reads SUPABASE_* and the live tests read STAGING_SUPABASE_*, but the contract names only one and no fallback is allowed. Four sub-questions, one answer; the Recommend line is on the ticket. Blocks every live-marked test; blocks no foundation work.
- 2026-09-02T15:55:14Z [dispatcher] Ben 2026-09-02: .env now carries STAGING_SUPABASE_URL, STAGING_SUPABASE_SERVICE_ROLE_KEY, STAGING_SUPABASE_ANON_KEY, STAGING_SUPABASE_DB_URL (old SUPABASE_* names removed locally; the deployed Railway service keeps reading SUPABASE_URL/SUPABASE_SERVICE_ROLE_KEY — src/lib/db/client.ts unchanged). SERVICES.md declares the staging project ref; the live guard passes and tests/live/harness.live.test.ts ran green against staging. Remaining half for the architect: STAGING_SUPABASE_DB_URL exists as a NAME, so direct-SQL parity is possible — rule whether it needs a pg driver DEP or stays two-PostgREST-paths. The anon key is unused by Admin.
- 2026-09-02T16:41:50Z [architect] checks amended: The env question is settled, so its second half gets a permanent structural pin: STAGING_SUPABASE_DB_URL exists as a name in .env and the ruling is that NOTHING in this repo reads it (parity stays two PostgREST paths, no pg driver). The new check is the code-lines form ARCHITECTURE §10 requires, and it was dry-run on today's tree: the name appears in no file under src/ or tests/, so it passes now and reddens the day someone imports a Postgres driver by the back door. The original marker check is unchanged and also dry-run green. [was: bash -c "! grep -q OPEN-ENV agenticflow/docs/vision/ARCHITECTURE.md"]
- 2026-09-02T16:42:07Z [architect] blocked → open: UNBLOCKED and ANSWERED — for QA to close as already_done; no code change is required by the answer. Ben settled three of the four sub-questions on 2026-09-02 (History above): the app keeps reading SUPABASE_URL / SUPABASE_SERVICE_ROLE_KEY (src/lib/db/client.ts unchanged, Railway never repointed); the live suite reads the four STAGING_SUPABASE_* names, in tests/live/setup.ts alone; SERVICES.md declares the staging project ref and the guard passes.

THE ARCHITECT'S HALF — STAGING_SUPABASE_DB_URL: parity stays TWO POSTGREST PATHS. No pg-driver DEP. What ARCHITECTURE §10 requires is two INDEPENDENTLY WRITTEN paths to one number, not two transports, and tests/live/parity.ts already gives that: independentClient() plus a count query the test writes itself, sharing nothing with lib/db but the database. A pg driver would add a second credential SHAPE to the live suite (a DSN with an embedded password — the exact shape lib/db/result.ts's redactor had to be taught to scrub), a supply-gate DEP, a pooling question, and a transport whose timeout and role semantics differ from the one the app actually speaks; parity proven over a transport the app never uses proves less, not more. The two things direct SQL would genuinely buy — EXPLAIN ANALYZE and real aggregates — are the scraper repo's, which owns the schema and already has a SQL prompt; that is exactly where the pending_claims diagnosis went (TASK-0031). Door closed and now pinned by this ticket's checks: no pg / postgres.js dependency, no direct-Postgres connection from product code or tests, and STAGING_SUPABASE_DB_URL stays a name nothing under src/ or tests/ reads.

Recorded in DECISIONS.md (2026-09-02) and the OPEN-ENV marker is out of ARCHITECTURE §12. All three stored checks dry-run green on today's tree.

ONE THING FOR BEN, not a ticket: I listed the NAMES in .env today (names only, no values, nothing printed) and it carries SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY as well as the four STAGING_ ones — the History note said the old names had been removed locally. Nothing in the campaign depends on it either way; the live suite sets the app's names itself in-process. But a local 'npm run dev' or a designer's walk reads the app's names, whichever project they point at, so it is worth a glance.
- 2026-09-02T16:48:26Z [qa] open → done: Closed on Ben's ruling + the architect's write-up; no code change was required and none was made. Receipt GREEN, all three stored checks re-run verbatim on the landed tree (00a53c8).

VERIFIED, not taken on trust:
- The ruling is RECORDED where it must be: DECISIONS.md:304 "the app reads SUPABASE_*, the live suite reads STAGING_*, and parity stays two PostgREST paths" (the DB_URL half at :312-326); OPEN-ENV appears nowhere in ARCHITECTURE (check 1); SERVICES.md:35 carries a `## supabase` section naming the staging project ref (ref only — I read no `.env`, printed no value, and the ref is not a credential).
- The app still reads its own names: src/lib/db/client.ts is unchanged, so Railway is not repointed. The four STAGING_* names are read in tests/live/setup.ts alone; product code mentions no STAGING_ name.
- THE PINS ARE NOT VACUOUS — positive controls run. Check 2 (`STAGING_SUPABASE_DB_URL` read by nothing) exits 0 today; substituting `STAGING_SUPABASE_URL`, a name that IS read under tests/, makes the same command exit 1, so it reddens on a real hit rather than always passing. Check 3 (no pg / postgres.js) exits 0; substituting `from .cheerio.`, an import that exists under tests/, makes it exit 1. The door the ruling closed is genuinely bolted.
- Cross-check against today's live evidence: the two-PostgREST-paths parity works in practice — tests/live/review-item.live.test.ts ran 4/4 green against staging in this lane, its independentClient sharing nothing with lib/db but the database.

RESIDUAL for Ben, restating the architect's note because it is still true and this ticket is where he will look: `.env` carries the app's SUPABASE_* names as well as the four STAGING_ ones (names listed, no value read or printed), though the History said the old names had been removed locally. Nothing in the campaign depends on it — the live suite sets the app's names in-process — but `npm run dev` and a designer's walk read the app's names, whichever project they point at. [resolution: already_done]
