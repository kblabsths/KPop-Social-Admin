---
id: BUG-0144
type: bug
status: done
milestone: M2
priority: P2
opened_by: qa
parent: null
discovered_from: DEBT-0008
children: []
assignee: null
claimed_at: null
depends_on: []
touch_scope: [src/app/claims/page.tsx, tests/offline/claims/page.test.ts]
force: false
attempts: 1
resolution: fixed
---
# /claims states 'every bucket ... with every claim in it' over a pending_claims read that refused

## Description
Found while attacking admin-window/DEBT-0008 (the two-fact empty-state rule). Measured on run/admin-window at 8729765, offline and on a production build against staging.

Repro A (offline, exact):
  cd '/Users/ben-m4/Desktop/Coding/KPOP/kspace Admin'
  ./node_modules/.bin/vitest run --project=offline tests/offline/claims/page.test.ts -t 'says nothing about bucket counts'
That case is an it.fails pin, so it reports PASS while the defect stands; flip its 'it.fails(' to 'it(' in tests/offline/claims/page.test.ts to watch it red (measured red today at the first assertion, params {}).

Repro B (production build, staging, in a browser): launch per STACK.md section 5's production-like block on a bind-probed port, open /claims. Measured 2026-09-09 on port 8815, both colour schemes, when the server's SUPABASE_URL was unset (any refusal of the pending_claims read does it — permission denied, a transport failure, or the view outgrowing ROW_CAP, which staging is 877/1000 of): the Buckets section renders "pending_claims - SUPABASE_URL is not set / Reload to try the read again.", zero [data-bucket] rows and zero [data-bucket-claims] hooks, and DIRECTLY BELOW it the paragraph "Every bucket the classification view can hold, with every claim in it - nothing above narrows these counts. A bucket with no claims is a real zero."

Expected: the caption below the bucket table describes counts the read produced, so - like the list's own window line one Section down, which this page already drops entirely on a read that did not happen (src/app/claims/page.tsx:698, "{claims.kind === 'ok' ? <WindowLine .../> : null}"; ARCHITECTURE.md section 4.3, "a window line states a read that HAPPENED"; admin-window/BUG-0063, admin-window/BUG-0070) - it may not stand over a refused read. LOOK_AND_FEEL bar 13 is the same rule for a sentence: it has to be true in every state that renders it, and this one asserts the table lists every bucket with every claim in it while the table lists none.

Found: src/app/claims/page.tsx:660-681 renders the caption for every claims.kind except not_provisioned, so both arms stand over the error state. admin-window/DEBT-0008 (02dc0f5) additionally made the arm chosen under a refusal the UNNARROWED one for every URL - population and rendered are both 0 there, so isSurfaceNarrowed is false whatever the URL carries - and its clause "nothing above narrows these counts" is then also false about the URL: on /claims?bucket=escalated with the read refused, the page applied the bucket facet (no dropped-parameters line, the chip renders active) and the sentence denies it. Before DEBT-0008 that URL got the "under the filters above" arm, so the change is what makes the sentence contradict the chip bar; the deeper defect - a sentence about counts standing where there are no counts - predates it and is what this ticket is against.

Not in DEBT-0008's criteria (they own the EMPTY state, not the refused one) and its own pins are green, so DEBT-0008 closed done and this is its own ticket.

Note for whoever takes it: not_provisioned already replaces the whole surface, so the shape is there. Dropping the caption on kind === "error" is one condition; keep both healthy arms exactly as they are (tests/offline/claims/page.test.ts "says what the bucket figures are figures of" and the DEBT-0008 block "which emptiness this is" pin them, and the staging walk shows both arms reachable: ?bucket=escalated takes the unnarrowed arm, ?domain=events&source_id=... takes the narrowed one).

## Acceptance criteria
When the pending_claims read refuses (kind 'error'), the Buckets section on /claims renders neither arm of the bucket-table caption: with the read refused and with any URL (bare, and with a facet the page applies), the paragraph the healthy page renders below the table is absent, while the refusal card and the table's own state line stay exactly as they are. Both healthy arms are unchanged, byte for byte: the unnarrowed arm over a bare URL and over a facet that removes no row of that table, the narrowed arm over one that does. Pinned by flipping the it.fails pin 'says nothing about bucket counts a refused read never produced' in tests/offline/claims/page.test.ts back to a plain it(...), with every other case in that file still green.

## Checks
```sh
./node_modules/.bin/vitest run --project=offline tests/offline/claims tests/offline/absence
npm run lint
```

## History
- 2026-09-09T20:03:56Z [qa] opened: /claims states 'every bucket ... with every claim in it' over a pending_claims read that refused
- 2026-09-09T20:11:35Z [builder-213] claimed (attempt 1)
- 2026-09-09T20:21:19Z [builder-213] claimed → built: Gated the /claims bucket-table caption on `claims.kind === "ok"` (src/app/claims/page.tsx) — the same rule the list's window line one Section down already follows (ARCHITECTURE §4.3). BucketTable, its StateOf line and both healthy arms are untouched; the diff is the gate plus its comment.
- Flipped QA's it.fails pin '[admin-window/BUG-0144]' back to it() and widened it to both directions: caption present on every read that RETURNED (rows bare / source facet / bucket facet, and an EMPTY ok view — an empty read is still a read), neither arm on permission denied, transport failure, the view outgrowing ROW_CAP (count 4096 > rows), or the view absent — each bare and under ?bucket=escalated, with droppedParams still 0 so the facet really was applied. Absence is checked over EVERY <p> in the buckets surface, not just the last one, so moving the sentence elsewhere in the section does not sneak past.
- Red-then-green both times: original pin red at page.test.ts:1551 (params {}), rewritten pin red at 'permission denied {}' against the pre-fix page restored with `git checkout --`, green after.
- Verified on the rebased tree (onto 91d7632): stored checks (offline claims+absence 466 passed), npm run lint clean, tsc --noEmit clean, npm test 3060+6 passed, npm run test:live tests/live/claims.live.test.ts 9 passed.
- Observed at the real surface, production build against staging on port 8820 (killed; port closed): healthy /claims and /claims?bucket=escalated each render 5 [data-bucket] rows, 5 [data-bucket-claims] hooks, no data-state, and the caption paragraph; with SUPABASE_URL unset both URLs render data-state="error", 0 bucket rows, 0 count hooks, and the only paragraph in the buckets surface is 'pending_claims — SUPABASE_URL is not set / Reload to try the read again.'
- Trap for QA: bucketCaption() in the test file reads the LAST <p> of [data-surface="buckets"], which in a refusal is the refusal card's own text — a test asserting only 'said !== whole' there can pass vacuously; use the all-paragraphs form. Nothing forces a truncation error against live staging, so ROW_CAP is pinned offline only.
- 2026-09-09T20:21:37Z [dispatcher] landed ticket/BUG-0144 onto run/admin-window as 6d3de21
- 2026-09-09T20:22:18Z [qa] built → qa: under attack
- 2026-09-09T20:35:07Z [qa] qa → done: Attacked on the landed tree (6d6c0e5), criteria re-run before the builder's story. Stored checks verbatim: offline claims+absence 466 passed, npm run lint clean; receipt GREEN (both exit 0).

Criteria, each clause driven:
- Refused read, neither arm: permission denied, transport failure, view outgrowing ROW_CAP (count 4096 > 11 rows) and readComplete's FIFTH refusal the pin missed — a read that came back with a null count / no count key — each bare AND under ?bucket=escalated. Every one: data-state="error" on the buckets surface, 0 [data-bucket], 0 [data-bucket-claims], droppedParams 0, and neither arm in ANY <p> of the surface. Also ?bucket=in_window (parked, dropped line = 1) and ?zzz=1: still no caption.
- Refusal card + table state line unchanged: the only paragraph in the surface is the pending_claims card, verbatim.
- Both healthy arms byte-for-byte: the diff (7bd006d) wraps the <p>, BUCKET_CAPTION untouched.
- not_provisioned still replaces the surface; standing tab renders no buckets section at all in ok/error/not_provisioned.

Attack, on the batch's combined surface — the arm the pin only grades as membership:
- Which arm the bucket facet takes. Criterion says the UNNARROWED arm over "a facet that removes no row of that table"; the pin only asserts said ∈ {whole, narrowed}, so a page deciding the arm from the URL again passes it. Measured correct on all five renderable buckets (caption === bare caption, figures === bare figures 10, chip current, dropped 0).
- A source/domain facet the WHOLE VIEW carries (every claim moved onto one source, then onto one domain): unnarrowed arm, chip active, dropped 0 — the row fact, not the chip. Opposite direction (?source_id=first over the real population: sum 10 -> 5) takes the narrowed arm.
- Empty-but-ok: all 5 bucket rows still drawn as real zeros, caption present, unnarrowed arm; the source/domain facet is DROPPED there (empty vocabulary) so the sentence and the dropped-params line agree.
- ok read with registry or observations refused; count < rows; count === rows exactly.

Both pin directions watched RED against wrong implementations (throwaway detached worktree, reverted with git checkout, product source untouched): caption ungated again -> 2 failures (the fix's own pin + my null-count pin); bucketsNarrowed from the URL alone -> 3 failures (both my arm pins + DEBT-0008's "blames no facet when the whole view holds nothing").

Real surface: production build (npm run build) in Chromium via agenticflow/.venv-tools, bind-probed port 8840 (never 8770/8771), sentinel DB on 127.0.0.1:1 and a throwaway AUTH_SECRET — no credential read, .env never opened. 6 renders (light + dark x /claims, ?bucket=escalated, ?source_id=<uuid>): buckets data-state="error", 0 bucket rows, 0 count hooks, 0 window lines, and 0 paragraphs matching /with every claim in it|under the filters above/ anywhere in the document. Server killed (own PID only), port closed.

3 pins committed as 929bc60 (extends tests/offline/claims/page.test.ts; no new file). bucketCaption() now THROWS instead of reading the state card's text on a refused surface — the last-paragraph trap the fix leaves behind, which would let a future `!== whole` pin pass vacuously. tsc --noEmit clean, npm test 3079+6 passed from the receipt worktree.

Judgment: the fix meets the ticket's goal — the caption follows the read, both healthy arms and both refusal renderings are honest, and the sentence is now true in every state that renders it. No BUG filed. Residual (not a ticket, and out of this ticket's scope by its own criterion "the refusal card stays exactly as it is): a transport-failure card prints the full undici stack including the server's absolute filesystem paths into the page — deliberate per errorMessage()'s "everything it said" policy and secret-scrubbed, but unreadable to an operator; it would earn a ticket if LOOK_AND_FEEL ever bounds what a state card may say. [resolution: fixed] [receipt green: 2 cmd(s), tree 423d9a93]
