---
id: TASK-0052
type: task
status: done
milestone: M2
priority: P1
opened_by: architect
parent: FEAT-0011
children: []
assignee: null
claimed_at: null
depends_on: [TASK-0040]
touch_scope: [tests/live/edit.live.test.ts, tests/live/sweep.ts, tests/live/residue.live.test.ts]
force: false
attempts: 1
resolution: fixed
---
# The live edit round trip moves to walk_sandbox: five coercions, the NOT NULL refusal, and the sweep

## Description
**Narrowed 2026-09-08 by the architect**, after Ben's strike and TASK-0040's landing: TASK-0040 owns inverting the groups/idols write case, re-aiming the http rewrite tripwire and re-expressing the residue floor. What is left for this ticket is the half TASK-0040 does not do — **the live edit suite needs a real write round trip, and after the strike there is exactly one table it may write.**

**`walk_sandbox` is present on staging with rows** — Ben pasted the DDL on 2026-09-08 and builder-118 measured it on 2026-09-09 (`residue.live.test.ts` swept it as 5 of 5 mapped tables; the row-count floor passed; columns `sandbox_id, label, note, tally, is_flagged, observed_on, created_at`). M2 precondition 3 is satisfied, so the write half is exercisable rather than skipped.

**What this ticket does:**
1. **One write round trip on `walk_sandbox`**, in `tests/live/edit.live.test.ts`: PATCH one mapped column of one seeded fixture row (`00000000-0000-4000-8000-000000000001`), assert it landed by re-reading, restore the prior value in a `finally`, and let the residue sweep run after (acceptance test 13, M2 EC12).
2. **The typed coercions the sandbox exists to walk**, one assertion each and each with its absent case: `label` (text), `note` (nullable text — clear it to the unqualified dash, then fill it), `tally` (integer), `is_flagged` (boolean), `observed_on` (nullable date).
3. **The refusal path is a pass, not a failure**: clearing a NOT NULL column (`label`, `tally`, `is_flagged`) is refused by the database (23502) and the surface must report the refusal without claiming the save landed — assert the refusal, assert the value is unchanged on re-read.
4. **The absence branch stays and stays honest**: if the table is ever absent again, the write half skips on the absence code read through the existing `codeOf`/`objectIsAbsent` idiom with a stated note — an absent table is not an error and is not a silent pass either.
5. **The state kind before the number** (ARCHITECTURE §10): every assertion names the kind it expects, read structurally from `data-state`; `error` is always a FAIL.

**Out of scope, because TASK-0040 owns it**: the groups/idols refusal assertions, the http tripwire, the residue floor's re-expression. Do not touch them here.

**The interim walk-write exception is dead** — it permitted one field of one `groups`/`idols` row precisely because the direct path existed. Note that consequence on TASK-0037, which owned the retirement on a later schedule; this ticket does not edit STACK.

## Acceptance criteria
1. `tests/live/edit.live.test.ts` performs one write round trip against `walk_sandbox` on staging — write, re-read, restore in a `finally` — and passes; it writes no other table on any path.
2. Each of the five mapped sandbox columns is exercised with its own assertion, including the nullable pair cleared to the unqualified dash and refilled.
3. Clearing a NOT NULL sandbox column asserts the database's refusal (23502) and asserts the value unchanged on re-read; the surface never claims the save landed.
4. If `walk_sandbox` is absent, the write half skips on the absence code with a stated note and the suite still passes; that branch is exercised at least once against a stubbed absence so it is not dead code.
5. Every assertion names its expected state kind before comparing anything; `error` fails the suite.
6. `tests/live/residue.live.test.ts` passes afterwards and reports no campaign residue.
7. The consequence for the interim walk-write exception is recorded as a comment on TASK-0037; this ticket does not edit STACK.md.

## Checks
```sh
bash -c 'rm -rf .next/types && npm run lint && ./node_modules/.bin/tsc --noEmit'
npm run test:live -- tests/live/edit.live.test.ts
npm run test:live -- tests/live/residue.live.test.ts
```

## History
- 2026-09-09T01:19:43Z [architect] opened: The live edit suite after the strike: prove the refusal, write only walk_sandbox, sweep
- 2026-09-09T01:25:16Z [architect] description amended: Narrowed after TASK-0040 took the groups/idols inversion, the http tripwire and the residue floor. What remains is the walk_sandbox write round trip, which is newly exercisable: Ben pasted the DDL 2026-09-08 and builder-118 measured the table present with rows on 2026-09-09. [was: **Why this exists, and why it is early.** `tests/live/edit.live.test.ts` writes one field of a real `groups`/`idols` row… (3134 chars; full text in git history)]
- 2026-09-09T01:25:16Z [architect] criteria amended: Same narrowing: the refusal assertions and the sweep floor move to TASK-0040; the sandbox round trip, its five coercions and the NOT NULL refusal path are what this ticket proves. [was: 1. `tests/live/edit.live.test.ts` no longer writes `groups` or `idols` on any path: a grep of the file finds no PATCH or… (1065 chars; full text in git history)]
- 2026-09-09T01:25:16Z [architect] title amended: Retitled to what the ticket now does. [was: The live edit suite after the strike: prove the refusal, write only walk_sandbox, sweep]
- 2026-09-09T01:25:16Z [architect] depends_on: [] → [TASK-0040]: Depends on the strike-removal ticket, which owns the same live file's groups/idols half; worktrees isolate builds, not landings (ARCHITECTURE §13.9).
- 2026-09-09T02:14:54Z [builder-130] claimed (attempt 1)
- 2026-09-09T02:33:37Z [builder-130] claimed → built: - tests/live/edit.live.test.ts (+~400 lines, all inside the existing 'the walk sandbox' describe): each of the five mapped columns written through submitFieldEdit onto the PATCH route — the CELL's own path, so 'the surface never claims the save landed' is a SaveOutcome and not an inferred status — then read back by this file's own client, TYPE first (tally '4242' -> number 4242, is_flagged 'true' -> boolean, observed_on '2026-03-04' -> date string); note/observed_on cleared to the app's own EM_DASH with no qualifier and refilled; label/tally/is_flagged cleared answers {ok:false} carrying Postgres 23502 ('null value in column "tally" of relation "walk_sandbox" violates not-null constraint … (23502)') with the whole row unchanged on re-read. Every render goes through sandboxMarkup, which asserts the state kind ok before any value is compared; a sibling test proves that oracle on two fixtures (seeded row = ok, unseeded uuid = empty, and sandboxMarkup rejects it). Absence: sandboxSkip reads the table and skips with a stated note on PGRST205/42P01 via codeOf/ABSENCE_CODES; any other error still throws. Undo is resetSandbox in a finally on every write test.
- OUTSIDE touch_scope, deliberately: src/app/records/[table]/[id]/page.tsx gains data-surface='fields' on the Fields section (one attribute + comment). ARCHITECTURE §10 forbids addressing a graded surface by position; this page had no name. Nothing else on the page changed.
- Verified on the rebased tree (run/admin-window @269bad0): npm run lint + tsc --noEmit clean; npm test 2192+6 pass; npm run test:live 91/91 including edit.live 20/20 and residue.live clean (walk_sandbox label=0 note=0, 3 of 3 mapped tables). Red-then-green observed for each new claim by mutation: tally stored as string -> 'came back as number'; 23502 -> 23503 -> fails printing the real message; data-surface removed -> 'markup carries no surface'; the absence read pointed at a non-existent table -> 13 skipped with the note, file still passes.
- QA traps: the write tests share row …0001 and reset ALL THREE rows in a finally, so do not run them beside a live walk on the sandbox. The 23502 refusal text carries Postgres's 'Failing row contains (…)' dump — that is the database's own words, by design. Criterion 7 recorded as a comment on TASK-0037; STACK.md untouched.
- 2026-09-09T02:34:51Z [dispatcher] landed ticket/TASK-0052 onto run/admin-window as 721bdee
- 2026-09-09T02:35:23Z [qa] built → qa: under attack
- 2026-09-09T02:49:49Z [qa] qa → done: ATTACKED, nothing broke. Criteria re-run on the landed tree (721bdee) in my own detached worktree under the gitignored evidence dir (node_modules/.env symlinked, never opened); receipt GREEN, 3/3 checks exit 0.

C1 WIRE-LEVEL, not inferred: I ran edit.live under a fetch spy wrapping globalThis.fetch in the live project's setup, logging every non-GET request. 37 mutating requests in the whole file, ALL of them /rest/v1/walk_sandbox (13 PATCH sandbox_id=eq....0001, 12 POST, 12 DELETE from the reset). Zero PATCH/POST/DELETE to any other table. Corroborated by full-table sha256 over every row of groups(1759)/idols(7234)/events(120)/venues(130)/field_provenance(1712)/review_items(1)/event_performers(136) before and after: byte-identical, and still identical after my entire attack campaign. Only walk_sandbox's hash moved (created_at, re-seeded by the reset).
C2 5/5 columns, own test each + the nullable pair cleared to bare EM_DASH and refilled; DB null asserted independently before the dash is read, so the dash assertion is not vacuous (a literal em dash typed into note stores as text - see residual 2).
C3 23502 with the row unchanged: verified, and the whole REQUIRED loop reddens when the constant is moved to 23503 ('expected "null value in column label ..." to contain 23503').
C4 absence branch DRIVEN, not just read: I stubbed sandboxSkip's own read in a scratch copy and ran the file on five error shapes. PGRST205 -> 13 skipped with the stated note, file passes. 42P01 -> same. 42501 -> 13 FAIL 'that is a refusal about this run, not an absent table, so it is a failure and not a skip'. A code-less 500 -> same failure. null error -> 20/20 pass. Skips only on the two absence codes; widening ABSENCE_CODES to 42501 reddens the pure-fixture test.
C5 ok-first oracle proved load-bearing: disarming assertState inside sandboxMarkup makes 'grades this page's ok state apart from its emptiness' RED ('promise resolved instead of rejecting'). Measured the two fixtures myself: seeded row = no state card + 5 aria-labelled controls; unseeded uuid AND a non-uuid id = data-state="empty" + 0 controls.
C6 residue GREEN after the run [label=0 note=0, 3 of 3 mapped tables]; I planted 'admin-window QA planted residue' into walk_sandbox.note and it went RED naming 'walk_sandbox.note: 1 row(s)'; reset -> GREEN. Sandbox left byte-identical to sandbox-fixture.ts over all 6 mapped columns.
C7 honest: builder-130's 2026-09-09T02:33 comment on TASK-0037 states the consequence; the ticket commit touches 2 files and STACK.md is not one of them.
data-surface="fields": exactly ONE data-surface on the record page in every state it draws (ok/empty/non-uuid). Rendering the same row against the pre-change page.tsx and diffing: the ONLY difference in the whole markup is ' data-surface="fields"' on one <section>. Removing it reddens 10 tests. Offline slice around the changed page 742/742 green.
ATTACK (one integrated pass, 19 absurd inputs through submitFieldEdit onto the route): tally 9999999999999 -> 22003, 4242.7 -> 22P02, '' -> 23502, 1e3 -> 22P02; is_flagged 'maybe' -> 22P02 ('yes'/'1' coerce to true, Postgres semantics); observed_on 2026-02-30 -> 22008, 'not a date' -> 22007; label with a NUL byte -> 22P05. EVERY refusal left the row unchanged and EVERY ok:true value equalled what the DB kept. <script> stored verbatim, escaped in the render. Non-uuid/quote/1 OR 1=1 ids all render the graded empty state, no throw.
RESIDUALS (not filed, lean depth): (1) concurrent double-submit on one field is last-write-wins and the loser's cell claims ok with its OWN value while the DB holds the other (measured: both ok, stored='concurrent A'); no criterion reaches it and the app has no optimistic concurrency by design - architect's call. (2) a literal em dash typed into note is stored and then drawn identically to absence. (3) the pre-existing TASK-0037 test in this file compares page content without naming a state kind (C5's letter); it cannot pass vacuously - it demands 5 edit controls, which only the ok state draws. (4) submitFieldEdit does not trim, EditableCell does, so '  padded  ' is storable through the test path but not from the UI. (5) page.tsx is outside touch_scope; disclosed by the builder, one attribute, provably no other markup change. [resolution: fixed] [receipt green: 3 cmd(s), tree 6a5bdfa5]
