---
id: BUG-0161
type: bug
status: done
milestone: M2
priority: P2
opened_by: designer
parent: null
discovered_from: designer:relook-claims
children: []
assignee: null
claimed_at: null
depends_on: []
touch_scope: [src/app/claims/page.tsx, src/components/claims/filter-bar.tsx, src/lib/claims/filters.ts, tests/offline/claims/filters.test.ts, tests/offline/url/narrowing.test.ts]
force: false
siblings: [BUG-0160, BUG-0162]
attempts: 1
resolution: fixed
---
# /claims: a ?domain= that matches nothing is a dead end — every chip carries it forward and the empty card names an exit that does not exit

## Description
Found on the scoped re-look of `/claims` after BUG-0138 landed (designer, 2026-09-10; production build, own instance on 8783 against staging; evidence `agenticflow/tracker/evidence/M2/designer/claims-domain-zzz-deadend.png`).

**Measured on staging, `/claims?domain=zzz`:** all five bucket rows read `0`, the window line says the read found nothing, and the empty card says

    No claims matching these filters
    Widen a filter above; the 'all' chip on any row shows everything again.

Every anchor on that page carries `domain=zzz` forward, **including both `all` chips**:

    all (bucket)     -> /claims?domain=zzz
    all (source_id)  -> /claims?domain=zzz
    ticketmaster     -> /claims?source_id=01a05782-…&domain=zzz
    awaiting_row     -> /claims?bucket=awaiting_row&domain=zzz

So the one action the empty state names returns the operator to the same zeroed page, and there is no control on the screen that clears the narrowing that emptied it. The only exits are the sidebar's Claims link or editing the URL by hand — and the screen never says the word `domain`, so a person cannot know that is what to edit (that invisibility is BUG-0160; this ticket is the exit, which stays broken even once the narrowing is named).

This is new since BUG-0138: before it, `?domain=` was reported as a dropped parameter and the page rendered unnarrowed. The builder recorded the change of shape in the ticket's History ("`?domain=<anything nothing carries>` now NARROW and render NOTHING_MATCHED + window held 0"); what the ruling did not carry was the exit.

Violates LOOK_AND_FEEL copy bar 4 ("every empty and not-provisioned state names what the surface holds **and what fills it**") — the named filler does not fill it — and the Interaction principle for emptiness; the Voice register ("plain, specific") does not survive an instruction that cannot be followed.

Provenance checked: `git log` on `src/app/claims/page.tsx`, `src/components/claims/filter-bar.tsx`, `src/lib/claims/filters.ts` returns ticket-bearing commits only (5f08227 BUG-0138, 02dc0f5 DEBT-0008, cfe3e06 DEBT-0009). Not a human-lane divergence.

## Acceptance criteria
1. **The exit the empty card names works.** From `/claims?domain=<value nothing carries>`, the single control the card names clears **every** narrowing the page applied — including one with no chip row — and lands on a page that renders rows. Whatever that control is (an `all` chip that drops all facets, or a clear-narrowing control the card links to), the card's words and the control agree.

2. **A narrowing with no rendered control is not left un-clearable.** No reachable state of `/claims` renders zero rows with no on-screen control that undoes the narrowing responsible.

3. **The chip rows keep their present job for the facets they do render**: an inactive `bucket`/`source_id` chip still composes with the other facet, `all` still means "this facet, unset", and `in_window` still appears nowhere (LOOK_AND_FEEL bar 3).

4. The `NOTHING_MATCHED` / population distinction BUG-0138 preserved does not regress: `?tab=standing&domain=events` still renders "nothing here yet", not "nothing matched".

**Human-check (human-checkable):** open `/claims?domain=zzz`, click only what the empty card tells you to click, and arrive at a page showing claims.

## Checks
```sh
cd "/Users/ben-m4/Desktop/Coding/KPOP/kspace Admin" && ./node_modules/.bin/vitest run --project=offline tests/offline/claims/filters.test.ts tests/offline/claims/page.test.ts tests/offline/url/narrowing.test.ts tests/offline/absence/pages.test.ts tests/offline/ui/copy.test.ts
cd "/Users/ben-m4/Desktop/Coding/KPOP/kspace Admin" && grep -q "the empty card names an exit that clears every narrowing the page applied" tests/offline/claims/filters.test.ts
cd "/Users/ben-m4/Desktop/Coding/KPOP/kspace Admin" && bash -c "rm -rf .next/types && npm run lint && ./node_modules/.bin/tsc --noEmit"
```

## History
- 2026-09-10T19:37:26Z [designer] opened: /claims: a ?domain= that matches nothing is a dead end — every chip carries it forward and the empty card names an exit that does not exit
- 2026-09-10T19:37:37Z [designer] Sibling of BUG-0160 (same walk, same file). What this one adds that BUG-0160 does not: BUG-0160 is about the page never SAYING a domain narrowing is in force; this one is about there being no CONTROL that undoes it. Naming the narrowing does not create an exit, and building an exit does not name it — but a builder taking both at once will land them in one pass, and should: the sentence that names the narrowing and the control that clears it belong on the same line of the filter bar.
- 2026-09-10T20:05:13Z [builder-241] claimed (attempt 1)
- 2026-09-10T20:19:54Z [builder-241] claimed → built: The exit: `clearNarrowing` (src/lib/claims/filters.ts) builds the href from the EMPTY filter — total over CLAIM_FACETS, so it drops the chipless `domain` too; keeps the tab (a control the tab strip already undoes); null where nothing narrowed. The filter bar draws it as its own row (`data-clear-narrowing`, eyebrow 'narrowing', chip 'clear filters') on EVERY narrowed state, not just the emptied one — a narrowing with no control is un-clearable while rows still show. The empty card's second line is now one sentence that interpolates CLEAR_LABEL (CLEARED_BY in the leaf), so card and chip cannot drift; the old 'Widen a filter above…' + 'remove it from the URL' pair is gone from /claims (/queues keeps its own copy, out of scope).
Verified: the three ticket checks on the rebased tree (5 files / 526 offline tests, grep OK, lint 0, tsc 0), plus full `npm test` 3356 pass. New tests seen RED first by sabotage: exit clearing only chip facets → 4 leaf failures; `clear={null}` on the page → 7 page failures. Walked on a production build against staging (own instance, port 8786, minted cookie, killed after): /claims?domain=zzz → 0 claim rows, all five buckets 0, row reads 'narrowing clear filters', card reads "No claims in the zzz domain" / "The 'clear filters' chip above clears every filter in this URL, including domain, which has no chip row of its own.", exit href /claims → 50 rows, awaiting_link 108, awaiting_row 769, no clear row. ?tab=standing&domain=zzz → exit href /claims?tab=standing; ?domain=zzz&source_id=… → /claims.
For QA: the exit row renders whenever hasNarrowingFacet is true — including the population-empty 'nothing here yet' case (?tab=standing&domain=events over an empty standing tab), where the card deliberately still blames nothing and names no exit; that pairing is pinned. `clearNarrowing` is registered in tests/offline/url/narrowing.test.ts's OWNER map. BUG-0162's window-line sentence untouched.
- 2026-09-10T20:20:16Z [dispatcher] landed ticket/BUG-0161 onto run/admin-window as 6287d80
- 2026-09-10T20:20:42Z [qa] built → qa: under attack
- 2026-09-10T20:34:01Z [qa] qa → done: attacked: criteria re-run on the landed tree (6287d80/ea133ec) before reading the builder's note, then driven over HTTP on a production build against staging (own instance :8793, minted cookie, both PIDs killed; ports 8770/8771/8772/8790 untouched, no staging write of any kind).

Human-check, run as written: GET /claims?domain=zzz -> 200, 0 claim rows, all five bucket rows 0, card hook data-empty=narrowing reading 'No claims in the zzz domain / The clear filters chip above clears every filter in this URL, including domain, which has no chip row of its own.' The only control it names renders as <a href="/claims"> inside [data-clear-narrowing] (same Chip primitive and classes as an inactive facet chip, real link, keyboard-reachable); following it -> 200 with 50 rows and no clear row left. C1 met.

C2, hunted across every zero-row state I could reach: narrowed-empty (exit present), narrowed-with-rows (/claims?domain=events -> 50 rows, gauge 849 vs 877 unnarrowed, exit -> /claims), standing tab narrowed away (?tab=standing&domain=zzz -> exit /claims?tab=standing; card stays the population one, correctly, since staging's standing tab holds 0 either way), and offline against four refused reads (sources denied, claims transport failure, pending_claims absent from the schema cache, observations denied) -- the exit row renders in all of them because it hangs off hasNarrowingFacet, not off the read. Un-clearable state not found. Filed no bug.

C3: bucket and source_id chip rows unchanged -- 'all' still drops only its own facet and carries the other plus domain (/claims?domain=zzz for both), the exit row carries no data-facet and sets no parameter, and in_window appears nowhere in the served HTML. C4: ?tab=standing&domain=<anything> keeps data-empty=claims, never 'narrowing'.

Boundary/malformed pass on the facet the card interpolates, over HTTP: <script> tag (escaped, no raw script in the body), 3000-char value, Arabic, zero-width, NUL, embedded quote, newline, RTL override, repeated ?domain=a&domain=b, ?domain=%20%20, ?bucket=in_window beside a real narrowing, ?bucket=<renderable>&tab=standing (ignored and named on the dropped-parameter line). All 200; every refused value renders the unnarrowed page with no exit; every applied one gets an exit to the bare path.

Added one pin the landed tests lacked (tests/offline/claims/page.test.ts, commit b8ad482, watched red when its premise is broken): 'is on the screen while the narrowing still leaves rows to draw' -- C2's non-empty half, which the shipped cases only graded on emptied pages.

Residual, not filed because it is already BUG-0163 (open): on this same URL the gauge section's figures are narrowed (877 -> 849 -> 0) under a window line naming no narrowing, and its age card renders the population-flavoured emptiness. BUG-0163's criterion 3 covers it; nothing here regressed it, and it is out of this ticket's exit scope. Second residual, one line: /queues still carries the old 'Widen a filter above' copy the builder scoped out -- dispatcher's call whether it earns a ticket.

Receipt green on the landed tree: 530 offline tests over the five named files, grep OK, lint 0, tsc 0. [resolution: fixed] [receipt green: 3 cmd(s), tree 4750cc30]
