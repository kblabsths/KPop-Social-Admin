---
id: BUG-0015
type: bug
status: done
milestone: M1
priority: P2
opened_by: designer
parent: FEAT-0001
discovered_from: TASK-0005
children: []
assignee: null
claimed_at: null
depends_on: []
touch_scope: [src/components/shell/shell.tsx, tests/offline/shell]
force: false
attempts: 1
resolution: fixed
---
# Frame: a hovered nav item is pixel-identical to the active one, so two items read as current

## Description
Early walk of M1 (real app on 8771, 1280x900, light and dark). LOOK_AND_FEEL's Frame rule gives the active nav item one rendering of its own: 'Active item = chrome-inverse fill (gray-200 / gray-700) with primary text' — and the token layer says so in its own comment (--color-chrome-inverse, 'active nav item'). The shipped sidebar hands that exact rendering to hover as well. Measured on / with the pointer over 'Queues': the hovered item computes background rgb(229,231,235) and colour rgb(30,41,57); the active 'Dashboard' computes background rgb(229,231,235) and colour rgb(30,41,57). Identical, to the byte. Dark theme is the same story on the gray-700 pair. So while the pointer is anywhere in the sidebar the Frame shows TWO items dressed as current, and the operator's only cue to where they are is the one they are not touching — see agenticflow/tracker/evidence/designer/frame-nav-hover.png, where Dashboard (active) and Queues (hovered) are indistinguishable. The 'Sign out' button carries the same hover pair, so hovering it dresses a sign-out control as a seventh, active nav item. This matters more than a cosmetic nit because the sidebar is on all six pages and it is the only persistent statement of location the window has; the Feel's repeat-use principle ('nothing moves between visits, counts sit in fixed positions') rests on that statement being unambiguous, and bar 10's one-click investigation is navigated from here. Provenance checked: shell.tsx is 5cf4199, admin-window/TASK-0005 — ticket-bearing, so this is a violation, not a human-lane divergence. Hover is a state, not a claim of place; the Look reserves chrome-inverse for the claim.

## Acceptance criteria
1. On every page, an item under the pointer is visibly distinct from the active item: their computed background and colour pair are not equal, in light and in dark.
2. The active item keeps exactly the rendering LOOK_AND_FEEL names — chrome-inverse fill with primary text — and no non-active item, including the sign-out control, is given that fill in any state.
3. Hovering the sign-out control does not render it as a nav item.
4. The 120ms colour transition and the accent focus ring are unchanged; keyboard focus stays visible on every item (bar 9), and the focus ring is not the thing carrying the hover distinction.
5. tests/offline/shell asserts the active item's classes and a non-active item's hover classes are not the same set, so the two states cannot silently converge again.
Human-check: with the pointer resting on 'Queues' while on the Dashboard, exactly one sidebar item reads as 'you are here'. (human-checkable)

## Checks
```sh
bash -c "! grep -q 'hover:bg-chrome-inverse' src/components/shell/shell.tsx"
./node_modules/.bin/vitest run tests/offline/shell
./node_modules/.bin/tsc --noEmit
```

## History
- 2026-09-02T10:08:03Z [designer] opened: Frame: a hovered nav item is pixel-identical to the active one, so two items read as current
- 2026-09-02T10:53:48Z [builder-29] claimed (attempt 1)
- 2026-09-02T11:03:48Z [builder-29] claimed → built: - Hover no longer wears the active rendering: a non-active nav item hovers to `bg-surface` + `text-ink` (the surface half of the Look's sanctioned surface/chrome row-hover pair — the sidebar is already chrome, so `hover:bg-chrome` would be a no-op; no new colour, tokens only). `bg-chrome-inverse` is now spent on the active item alone.
- Sign out is the Look's secondary `Button` (hairline border, transparent fill, primary text) instead of a nav-shaped button, so no state dresses it as a seventh item. Reused the existing `ui/button` primitive rather than hand-styling; note it has no hover fill of its own (no button in this app does — a Button-primitive change is out of scope here).
- `Sidebar` is now exported so tests/offline/shell renders the real Frame and asserts the states cannot reconverge: set-under-pointer != active set, nothing hovers into the active fill, Sign out carries no nav hover class, every control keeps transition-colors and sets no focus/outline class.

Verified on the rebased tree (onto run/admin-window @6ef5117): grep guard ok; `vitest run tests/offline/shell` 22 passed; `tsc --noEmit` clean; `npm run lint` exit 0; `npm test` 726 passed (32 files); `npm run test:http` 8 passed. Saw the two new guards red first against the old classes, then green.

Browser measurement, Chromium 1280x900, production CSS + the component's real markup, both schemes (criteria 1/2/4): light hovered Queues bg rgb(255,255,255)/color rgb(30,41,57) vs active Dashboard bg rgb(229,231,235)/rgb(30,41,57) — was byte-identical, now the fills differ. Dark hovered rgb(16,24,40) vs active rgb(54,65,83). Sign out under the pointer: bg rgba(0,0,0,0), 1px rgb(209,213,220) border (light) / rgb(54,65,83) (dark) — never a nav fill. transition-duration 0.12s on colour/background-color everywhere; Tab focus ring 2px solid rgb(152,16,250) light / rgb(194,122,255) dark at 1px offset on all seven controls.

QA trap: the focus ring transitions too, so read outline-color >120ms after Tab — sampled immediately it reports a mid-transition blend (rgb(47,38,84)), not accent. The light-theme hover fill is a deliberately quiet step (gray-50 -> white); the loud half of the hover cue is the ink lifting gray-600 -> gray-800.
- 2026-09-02T11:04:06Z [dispatcher] landed ticket/BUG-0015 onto run/admin-window as e944b72
- 2026-09-02T11:04:53Z [qa] built → qa: under attack
- 2026-09-02T11:11:09Z [qa] qa → done: attacked: re-measured the Frame myself in Chromium (kit playwright) against a production build I started on 8774 with a minted session cookie, 1280x900, both schemes, before reading the builder's numbers. Criteria re-run verbatim first: grep guard exit 0, vitest tests/offline/shell 23 passed, tsc clean; receipt GREEN.

C1 (hovered != active, every page, both schemes) — light: hovered Queues bg rgb(255,255,255)/ink rgb(30,41,57) vs active Dashboard bg rgb(229,231,235)/rgb(30,41,57); dark: rgb(16,24,40) vs rgb(54,65,83). Pairs differ in both. Enumerated all six fills while the pointer sat on Queues: exactly one item computes the active fill (Dashboard), the hovered one computes surface, four are transparent — same on the 404. The transition path is safe too: light interpolates gray-50 -> white and dark gray-800 -> gray-900, i.e. AWAY from the active fill, so there is no mid-transition frame where two items collide.
C2 — attacked the inverse of the filed bug, which the builder's guards did not cover: pointer ON the active item. Light stays rgb(229,231,235)/rgb(30,41,57), dark rgb(54,65,83)/rgb(229,231,235) — the claim of place survives the pointer. Same with focus+hover together on the active item. bg-chrome-inverse appears exactly once in src/ (shell.tsx:56) plus its two token declarations.
C3 — Sign out under the pointer: bg rgba(0,0,0,0), border 1px solid rgb(209,213,220) light / rgb(54,65,83) dark, colour unchanged; it is a <button>, no href, no aria-current, and carries none of the nav hover classes. Never a nav fill.
C4 — every one of the seven controls reports transition-duration 0.12s on colour/background-color; full Tab traversal in both schemes gives all seven outline 2px solid rgb(152,16,250) light / rgb(194,122,255) dark at 1px offset (sampled >300ms after the key, past the ring's own 120ms blend). Keyboard focus on a non-active item leaves its background at resting rgba(0,0,0,0) — the ring is not carrying the hover distinction.
C5 — the offline guards assert set inequality, not colour literals, so a token change cannot redden them. I added one test to the same file (a2d8cec, watched-with-a-control): the active item's under-pointer set must equal its resting set. Without it, a hover utility written on the shared half of the className would flip the ACTIVE item to the hover fill and leave the sidebar with no item claiming the place — the mirror image of this bug, and every existing guard passes in that state.
Module boundary: Sidebar's new export is imported only by tests/offline/shell/shell.test.ts; no product file picked it up.
Also drove: 404 (/no-such-surface-here, status 404) — sidebar renders six links, zero lit, hovering Dashboard fills it with surface only; client-side click nav /->/queues moves the fill correctly; /login renders no aside. Full offline suite 727 passed, lint 0, tsc 0 on the landed tree. Server I started killed, 8774 released.
Residuals (not this ticket's criteria, no path from them reaches a criterion): (a) /records/groups/<id> renders 200 inside the Frame with zero items lit — the edit surface makes no claim of place; (b) a nested 404 (/queues/2f0bc11e) also lights nothing even though isNavItemActive('/queues/<id>','/queues') is true, because Next serves the prerendered not-found; (c) Sign out now has no pointer feedback at all in either scheme — LOOK_AND_FEEL defines no button hover and every Button in the app behaves this way, so it is a Look silence for the designer, not a defect. [resolution: fixed] [receipt green: 3 cmd(s), tree d867f489]
