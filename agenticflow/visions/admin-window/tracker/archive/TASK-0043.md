---
id: TASK-0043
type: task
status: done
milestone: M2
priority: P1
opened_by: architect
parent: FEAT-0009
children: []
assignee: null
claimed_at: null
depends_on: []
touch_scope: [agenticflow/tracker/for-human]
force: false
resolution: already_done
---
# ASK Ben: which registered sources row is the admin voice, and does it exist on staging

## Description
**A contract silence, filed blocked. No builder resolves it by choosing** (acceptance doc ground rule; ARCHITECTURE §12 marker `ADMIN_SOURCE_IDENTITY`).

**The question.** Every value-carrying verdict and every override writes an **admin-tier observation through the gate**. The gate refuses an unregistered source: `ingest_observation` raises **KS007** — *"observation rejected: source \"%\" is not registered"* — after looking the name up in `public.sources` (read 2026-09-08 from `kspace Scraper/supabase/migrations/20260821000003_the_gate_refuses_empty_values_and_future_observations.sql`). `source_tier` already carries the value `'admin'` (`20260818000000`), so the TIER exists; what does not exist here is knowledge of the ROW.

So, for Ben:
1. **Is there a `sources` row for the admin voice on staging today?** If yes: what is its `source` name, and its `lifecycle` (the gate reads that too)?
2. **If not, who creates it?** It is a registry fact and a row in a scraper-owned table, so it is a handoff either way — either Ben inserts it, or the `settle_review_item` artifact carries an idempotent insert for it and Ben applies that. Whichever he prefers, the campaign does not write it from here.

**Why it blocks.** `settle_review_item`'s value-carrying branches cannot name a source without this answer, and inventing a name is precisely the move SPEC F9 forbids ("never a signature invented to make the artifact compile"). The branches that carry no value — `keep_current`, `settle`, `fixed`, `wont_fix` — do not need it, but they live in the same file as the ones that do, so the artifact waits.

**What is NOT blocked by it**: the decision envelope, the `verdicts` table artifact, the classifier, the call seam, and every UI surface's absent-case behaviour. M2's critical path is arranged so this answer is needed once, late.

**Deliverable of this ticket**: the question written to `agenticflow/tracker/for-human/M2-admin-source.md` in Ben's terms — the KS007 refusal quoted with its file, the two options, and the consequence of each — and this ticket left blocked until he answers. When he answers, record it here, remove the `ADMIN_SOURCE_IDENTITY` marker from ARCHITECTURE §12 (architect only), and unblock the function artifact.

## Acceptance criteria
1. **Ben's answer is recorded on this ticket** (2026-09-08), in the four registry values the gate reads: the admin voice is a `sources` row named `admin`, tier `admin`, lifecycle `active`, kind `registered`. Staging holds no such row today — read-only census the same day: `ticketmaster` plus two test-harness sources, nothing else — so Ben took the SECOND of the two shapes this ASK offered: the `settle_review_item` handoff artifact carries an idempotent insert for the row, and he installs the registration and the function in one paste.
2. The `ADMIN_SOURCE_IDENTITY` marker appears nowhere in `agenticflow/docs/vision/ARCHITECTURE.md`, History included — **the marker's absence is what closes the question** — and §9.2's first installed-schema fact now states the answer, with its date, where it stated the question.
3. `agenticflow/docs/DECISIONS.md` carries the dated paragraph: the four values; the installed shape the insert is legal against (`sources_source_key UNIQUE (source)`, the three enums, the `^[a-z0-9_]+$` CHECK), read from the sibling's migrations; why `do nothing` and never `do update`; and the doors it closes.
4. **The answer reached the ticket that consumes it.** TASK-0046 requires the idempotent `insert ... on conflict (source) do nothing` inside the artifact's one fenced block, and the name spelled ONCE as `ADMIN_SOURCE` in the pure leaf `src/lib/verdict/decision.ts`, with the artifact's own offline test asserting the SQL's source literal against that constant.
5. **Nothing in this campaign creates the row.** No agent applies SQL, no ticket's touch scope names a sibling path, and the sibling repo stays untouched — TASK-0046's stored checks hold that by absolute path.
6. (human-checkable) Nothing further is owed: this ASK needed no code, the answer was Ben's and the recording was the architect's, and both have landed. QA closes it `blocked -> done` with resolution `already_done` — the architect has no such edge.

## Checks
```sh
bash -c '! grep -q ADMIN_SOURCE_IDENTITY agenticflow/docs/vision/ARCHITECTURE.md'
bash -c 'grep -qE "ADMIN_SOURCE = .admin." agenticflow/docs/vision/ARCHITECTURE.md'
bash -c 'grep -qF "the admin voice is a registered sources row named" agenticflow/docs/DECISIONS.md'
bash -c 'grep -rqF "on conflict (source) do nothing" agenticflow/visions/admin-window/tracker/'
```
## History
- 2026-09-09T01:14:53Z [architect] opened: ASK Ben: which registered sources row is the admin voice, and does it exist on staging
- 2026-09-09T01:15:00Z [architect] open → blocked: Blocked on Ben: the gate refuses an unregistered source (KS007), so the admin voice must be named before settle_review_item's value-carrying branches can be authored. Question filed for him; inventing a source name is the one move SPEC F9 forbids.
- 2026-09-09T03:26:56Z [architect] BEN'S ANSWER, 2026-09-08 — recorded verbatim in its parts. The admin voice is a `sources` row named `admin`, tier `admin`, lifecycle `active`, kind `registered`. He took the second shape this ASK offered: the `settle_review_item` handoff artifact (TASK-0046) carries an idempotent insert for the row, so he installs the registration and the function in ONE paste. Staging holds no such row today — verified read-only the same day: `ticketmaster` plus two test-harness sources, nothing else — which is why the insert exists at all. Checked against what is INSTALLED, read read-only from /Users/ben-m4/Desktop/Coding/KPOP/kspace Scraper/supabase/migrations/20260818000000_the_schema_arrives_as_one_snapshot.sql: `sources_source_key UNIQUE ("source")` is the conflict target `on conflict (source) do nothing` needs (sources_pkey is source_id, so the ON CONFLICT target must be the unique on source); `source_kind` carries 'registered' and `source_lifecycle` carries 'active' (line 87-106); `source_tier` already carried 'admin'; and the `sources_source_shape` CHECK '^[a-z0-9_]+$' admits the name. `on conflict ... do nothing` is the sibling's own idiom (20260901000005, 20260901000006) — and `do nothing` rather than `do update` is deliberate: a second paste, or a row Ben inserted himself, must never have its lifecycle or tier rewritten from Admin's copy of it. WHAT I DID WITH IT: the ADMIN_SOURCE_IDENTITY marker is gone from ARCHITECTURE.md (§12 and the 2026-09-08 History line both — an ASK's structural check is the string's absence from that FILE); §9.2's fact 1 now states the answer instead of the question; DECISIONS.md carries the dated paragraph; TASK-0046 gained criteria 9-11 and four checks requiring the insert, the single ADMIN_SOURCE spelling in the pure leaf, and the artifact test's two couplings; and TASK-0046's dependency on this ticket is dropped, because the edge waited on the ANSWER, which has landed. NOTHING FURTHER IS OWED HERE.
- 2026-09-09T03:27:18Z [architect] criteria amended: Re-aimed at the ANSWERED state, because the old criteria graded a deliverable the run never produced and a marker that must now be gone. Criteria 1-3 of the old bar asked for agenticflow/tracker/for-human/M2-admin-source.md; that note was never written — the question reached Ben through the blocked sweep's own for-human/TASK-0043.md stub and the digest, and he answered it on 2026-09-08 — and old check 3 asserted the marker was PRESENT, which contradicts old criterion 4 (the marker's absence is what closes the question). The new bar grades what the closure actually is: the answer recorded, the marker gone from ARCHITECTURE.md, the ruling in DECISIONS.md, and the answer landed in TASK-0046's criteria. [was: 1. `agenticflow/tracker/for-human/M2-admin-source.md` exists, states the question in one screen, quotes the KS007 refusa… (730 chars; full text in git history)]
- 2026-09-09T03:27:18Z [architect] checks amended: Replaces the three old checks, one of which asserted the marker was PRESENT and so could never pass on a closed question. All four dry-run at exit 0 on this tree as of 2026-09-08, and each is a presence-of-the-fix except the marker one, which is written in the sanctioned '! grep -q' form. The last one is path-tolerant across tickets/ and archive/, so it survives TASK-0046 being archived. [was: bash -c 'test -f agenticflow/tracker/for-human/M2-admin-source.md', bash -c 'grep -q KS007 agenticflow/tracker/for-human… (248 chars; full text in git history)]
- 2026-09-09T03:27:51Z [architect] CLOSURE ROUTING — the ticket is answered and its bar is green, but the architect has no closing edge. Measured 2026-09-08: 'ticket.py transition TASK-0043 done --as architect --resolution already_done' -> ERROR: blocked -> done is not permitted for role architect (the (blocked, done) edge is QA's; wont_fix is strategist/human/toolsmith). Same shape as admin-window/TASK-0023 on 2026-09-02, which QA closed already_done after the architect's record landed. The ticket therefore stays blocked for exactly one action: QA runs receipt.py and closes it blocked -> done with --resolution already_done. All of its stored checks were dry-run VERBATIM on this tree today and every one exits 0. It is NOT waiting on Ben, and nothing downstream waits on it — TASK-0046 and TASK-0054 no longer carry the dependency edge, because the edge waited on the answer, not on the paperwork.
- 2026-09-09T03:33:16Z [qa] blocked → done: Attacked, not typed. Criteria re-run BEFORE the architect's story, on this checkout. C1 the answer's four registry values are recorded verbatim (source=admin, tier=admin, lifecycle=active, kind=registered) and I re-verified the premise myself rather than trusting the census in History: read-only select on staging sources returned exactly 3 rows — test_harness (registered/trial/untrusted), test_harness_control (cited/trial/untrusted), ticketmaster (registered/active/official); admin-named or admin-tier rows: 0. So the insert is genuinely needed and is not a no-op dressed as work. C2 ADMIN_SOURCE_IDENTITY appears nowhere in ARCHITECTURE.md (grep across the whole file, History included, exit 0 on the '! grep' form); I also read Sec 12 to check the question was not re-posed in other words — it states 'No question is open' and gives the one-line answer without the marker, so the grep is not passing on a rename. Sec 9.2 fact 1 now states the answer with its 2026-09-08 date where it stated the question. C3 DECISIONS.md carries the dated paragraph with the four values, the installed shape, and the do-nothing-not-do-update rationale. C4 the requirement reached the consumer: TASK-0046 criteria 9-11 carry the insert inside the one fenced block, ADMIN_SOURCE spelled once in the pure leaf, and both offline-test couplings. C5 verified by measurement, not by assertion: sibling kspace Scraper working tree porcelain = 0 lines, 0 commits matching admin-window, Admin's supabase/migrations still holds its 2 pre-existing files, no .sql tracked outside it, and ADMIN_SOURCE/'admin'-as-source-literal appears nowhere in src or tests yet. ADVERSARIAL EXTRA (the thing that would actually cost Ben a session): I re-read the installed sources DDL to see whether the recorded insert would fail at his paste. It will not — public.sources NOT NULL columns without a default are exactly source/kind/tier (lifecycle defaults 'candidate', source_id/created_at/updated_at default), so the 4-column insert covers every one; source_kind has 'registered' (line 87-90), source_lifecycle has 'active' (100-106), source_tier has 'admin' (116-122); sources_source_key UNIQUE (source) at line 4625 is a valid ON CONFLICT target and sources_pkey is source_id, so naming (source) is required, not stylistic; sources_source_shape CHECK '^[a-z0-9_]+$' admits 'admin'; the only trigger on the table is sources_touch_updated_at, BEFORE UPDATE, which an ON CONFLICT DO NOTHING never fires. Could not break it. No code was owed here and none was written; the answer was Ben's, the recording is the architect's, both landed. Closing already_done as the routing History names. Residual for the digest (not a bug, no path drives it yet): the run's answer-recording is sound, but see my TASK-0044 note for a registry-pattern seam that TASK-0054 inherits. [resolution: already_done]
