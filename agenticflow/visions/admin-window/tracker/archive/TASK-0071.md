---
id: TASK-0071
type: task
status: done
milestone: M3
priority: P2
opened_by: architect
parent: FEAT-0016
children: []
assignee: null
claimed_at: null
depends_on: [TASK-0070, BUG-0174, BUG-0183, BUG-0186]
touch_scope: [src/app/claims/page.tsx, src/components/claims/bucket-table.tsx, src/components/claims/tabs.tsx, tests/offline/claims/page.test.ts, tests/offline/gauges-ui, tests/live/claims.live.test.ts]
force: false
attempts: 1
resolution: fixed
---
# Two figures on /claims never silently disagree: each says whether it is a window or a total

## Description
**Modules:** `src/app/claims/page.tsx`, `src/components/claims/**` (the bucket table and the gauge cards' labels), `tests/offline/claims/page.test.ts`, `tests/live/claims.live.test.ts`.

**The defect (SPEC F15, M3 EC7).** Two sets of per-bucket figures stand on one page and are read two different ways: the bucket table's counts are `head: true, count: "exact"` reads of the WHOLE narrowing (`readClaimCount`), and the Claims tab gauge's bucket distribution is computed over a WINDOW of at most 1,000 rows. Past 1,000 claims they diverge with nothing on screen saying so. Staging is at 877 — this is live within months, not theoretical.

**The ruling: LABEL, do not equalise.** Making them equal has only two forms and both are worse. Capping the head counts to the window would make the page's totals wrong rather than windowed. Scanning the whole view to fill the gauge is the ~14-round-trip, 2.9-3.8 s read BUG-0138 removed and Ben complained about by name. So: **each figure says, in RENDERED TEXT, what kind of fact it is** — a window's, or the narrowing's total — and no sentence on the page asserts a relationship between two figures that no single read established (LESSONS 2).

The page already owns part of this vocabulary and it is the vocabulary to extend, not to reinvent: `LIST_COUNT_EYEBROW` ("Matching claims"), `POPULATION_EYEBROW` ("Whole-view count") and `GAUGE_POPULATION_EYEBROW` ("Window population"). The gauge's distribution card and the bucket table's caption are what lack it.

**What must be true of the rendered HTML** (asserted on markup, never on the read — M3 EC7):

- On a fixture holding MORE than 1,000 claims, for every bucket that appears in both places, either the two figures are equal, or each is labelled as the kind of fact it is, in text a reader sees — not in an attribute alone, and not in a comment.
- The same fixture produces no rendered sentence asserting a relationship between two figures that no single read established (for example "N of M", a percentage, or a difference, computed across the two).
- The labelling holds on the standing tab as well as the pending one, and under a `?source_id=` / `?domain=` narrowing, where both figures move.
- The window line above the gauge keeps saying exactly what it says today — this ticket adds no window line and moves no figure.

**Voice:** the words are the designer's jurisdiction, not this ticket's criteria. Use the app's existing nouns (a window is a window, a total is a total) and keep the sentences short and quiet; a walk grades the copy, the checks grade the structure.

**No new surface, no new figure** (M3 EC10): the sidebar still holds exactly six links, `src/app` gains no page route, and no figure is added to the page — this ticket makes the ones already there say which kind they are.

## Acceptance criteria
On a fixture holding more than 1,000 claims, the Claims tab gauge's bucket figures and the head counts above them are either equal or each labelled in rendered text as the kind of fact it is — window versus total — asserted on rendered HTML for every bucket that appears in both places, on both tabs, and under a source_id and a domain narrowing. That fixture produces no rendered sentence asserting a relationship between two figures no single read established. On a fixture below the window, where the two are equal, the page renders no contradiction and no extra apology. The window line, the row set and every existing figure are unchanged from the pre-ticket tree on the same fixtures. No figure and no surface is added: the sidebar holds exactly six links and src/app gains no page route. /claims' live parity suite is green, its oracles addressed by data-surface and deciding the state kind from data-state before comparing a number.

A FACET'S OWN TABLE STAYS UNNARROWED BY ITSELF AND SAYS SO (architect ruling 2026-09-11, folding QA's product residual 5 from TASK-0077's close — the second face of this ticket's own shape: two figures on one page under one narrowing disagreeing in KIND). `/claims?bucket=<x>` draws the bucket table's WHOLE distribution — measured on staging 2026-09-11: awaiting_link 108, awaiting_row 770, three buckets at 0, identical to the bare page — beside a list that facet really narrowed, under a caption whose unnarrowed arm says "nothing above narrows these counts" while the bucket chip stands active above it, and `?source_id=<x>` narrows the same table. The FIGURES ARE RIGHT AND DO NOT MOVE: `bucketStats` drops the bucket facet on purpose (narrowing this table to one bucket answers a question nobody asked), and the counts under every bucket URL stay byte-identical to the bare page's. What ends is the page denying a facet it applied: where a bucket facet is in force, the bucket caption says that these counts answer for EVERY bucket and that the filter above does not narrow them. That clause states this TABLE's own scope and nothing else — it never names the bucket as what narrowed these counts, never takes the blaming arm `?source_id=` takes over a view it really narrows, and says nothing about the list below or about why the list is empty (LESSONS 2 and 3: a sentence claims only the scope its read had, and an empty surface is explained from two facts). The bare page — no facet at all — still renders the sentence it renders today, to the byte, and so does a source or domain facet the whole view carries anyway (BUG-0160's two pinned arms are unmoved).

EXACTLY THREE EXISTING EXPECTATIONS CHANGE, ALL IN tests/offline/claims/page.test.ts, ALL NAMED HERE, and each is rewritten to grade the INVARIANT it was written for rather than byte-identity with the bare caption: (1) it("does not blame the bucket facet, which removes no row of that table") — keep the chip-is-active, nothing-dropped and counts-equal-the-bare-page assertions verbatim, and replace `expect(bucketCaption(markup)).toBe(bareCaption)` with the arm rule: the caption is NOT the blaming arm the `?source_id=` render takes, and no clause of it names the bucket value; (2) it("blames no facet when the whole view holds nothing") — the empty card, the empty hook and the five real zeros are unchanged, and the caption expectation becomes the same arm rule; (3) it("says nothing about bucket counts a refused read never produced [admin-window/BUG-0144]") — its bucket-facet rows grade membership in the page's OWN arms, extended to the third arm the same copy-independent way, with both directions of its rule standing: a read that did not happen still carries NO arm, in any state or URL. Every arm in these tests is read off the app, never typed — this file pins no sentence of the page, and a rewording moves the tests with it. A new plain `it("says the bucket table answers for every bucket when a bucket facet is set")` carries the rule with its twin (LESSONS 8): the bucket-facet render, and the bare render whose caption is unchanged.

## Checks
```sh
./node_modules/.bin/vitest run --project=offline tests/offline/claims tests/offline/gauges-ui
./node_modules/.bin/vitest run --project=offline tests/offline/ui/copy.test.ts tests/offline/shell
grep -qF 'it("says the bucket table answers for every bucket' tests/offline/claims/page.test.ts
test "$(find src/app -name "page.tsx" | wc -l)" -eq 9
npm run lint
npm run build
npm run test:live -- tests/live/claims.live.test.ts
```
## History
- 2026-09-10T21:44:17Z [architect] opened: Two figures on /claims never silently disagree: each says whether it is a window or a total
- 2026-09-11T05:52:20Z [architect] depends_on: [TASK-0070] → [TASK-0070, BUG-0172]: Architect ruling 2026-09-11, destination collision (ARCHITECTURE §13.9): BUG-0172 rewrites src/app/claims/page.tsx's list window line and the paged wrapper it sits above; this ticket writes figures on the same page. BUG-0172 lands first - it is a truth defect, this is labelling work. Partition while both are open: BUG-0172 owns the window line and the paging wrapper, this ticket owns the bucket table, the gauge cards and their labels.
- 2026-09-11T05:54:01Z [architect] Two partitions to respect (architect, 2026-09-11). (1) src/app/claims/page.tsx: BUG-0172 now lands before this ticket and rewrites the list window line plus the paged wrapper it sits above - this ticket owns the bucket table, the gauge cards and their labels, and its criterion 'the window line keeps saying exactly what it says today' is read against the tree BUG-0172 leaves, where the line's first-screen render is byte-identical by that ticket's own criterion 4. (2) tests/live/claims.live.test.ts: TASK-0077 owns claimsFromDatabase, the two censuses, refuseTruncated and the cap constants, the identity proof's window and the paged-walk case; this ticket's live work stays in the figure-labelling cases.
- 2026-09-11T07:14:12Z [architect] depends_on: [TASK-0070, BUG-0172] → [TASK-0070, BUG-0174]: Re-chained (architect, 2026-09-11): BUG-0172 is closed, and its successor on the same destinations is BUG-0174 - which now also writes src/app/claims/page.tsx (both paged pages must state heldFrom on the first-screen facts they compose once). Truth defect first, then contract work with a declared behaviour of zero. TASK-0070 edge unchanged.
- 2026-09-11T07:36:56Z [architect] criteria amended: Architect ruling 2026-09-11: QA's product residual 5 from TASK-0077 (a bucket facet leaves the bucket table's distribution whole beside a list it narrowed) is folded here rather than filed - it is this ticket's own shape (two figures on one page disagreeing in kind under one narrowing), on surfaces already in this touch scope, and a separate BUG would collide at the same destination (the bucket caption in src/app/claims/page.tsx). Ruling: the table stays unnarrowed by its own facet AND says so; the three existing expectations it invalidates are named in the criteria. [was: On a fixture holding more than 1,000 claims, the Claims tab gauge's bucket figures and the head counts above them are ei… (940 chars; full text in git history)]
- 2026-09-11T07:37:09Z [architect] checks amended: One check added with the folded criterion (architect 2026-09-11): the presence grep for the new named case. Dry-run on run/admin-window at ea80d78: the grep is RED today (the case does not exist), the page-count check reads 9 and is GREEN. Every other check is unchanged. [was: ./node_modules/.bin/vitest run --project=offline tests/offline/claims tests/offline/gauges-ui, ./node_modules/.bin/vites… (330 chars; full text in git history)]
- 2026-09-11T09:20:38Z [architect] depends_on: [TASK-0070, BUG-0174] → [TASK-0070, BUG-0174, BUG-0183]: Truth defect before contract work (architect ruling 2026-09-11, 13.9): BUG-0183 rewrites src/app/claims/page.tsx's listWindow composition (it states drawn and continues there) and this ticket writes figures and captions on the same page. BUG-0183 lands first - it is a truth defect on the window line, this is labelling work - and this ticket's criterion 'the window line keeps saying exactly what it says today' is then read against the tree BUG-0183 leaves, where the unpaged line names the rows it drew. Partition while both are open: BUG-0183 owns the window line and the window composition, this ticket owns the bucket table, the gauge cards and their labels. TASK-0070 and BUG-0174 edges unchanged.
- 2026-09-11T10:16:05Z [architect] depends_on: [TASK-0070, BUG-0174, BUG-0183] → [TASK-0070, BUG-0174, BUG-0183, BUG-0186]: Chained behind BUG-0186 (architect, 2026-09-11): shares src/app/claims/page.tsx and tests/offline/claims/page.test.ts, and its own criterion says the window line keeps saying exactly what it says today — that baseline must be the corrected line, not the one that describes a 50-row screen as 30. Ordered anyway through TASK-0070; the edge is stated so the collision is legible.
- 2026-09-11T11:44:00Z [builder-281] claimed (attempt 1)
- 2026-09-11T12:12:06Z [builder-281] claimed → built: Two faces, both on /claims. (1) Each set of per-bucket figures now carries an unconditional rendered eyebrow saying which KIND of fact it is: the bucket table 'Total counts', the gauge's bucket/source trend tables 'Window counts', published as data-figures on the region and data-figures-label on the words. No figure moved, no sentence relates the two, and the same words stand whether the two agree or not. (2) Where a bucket facet is in force the bucket caption now states this table's own scope ('these counts answer for every bucket; the bucket filter above does not narrow them') instead of denying the facet; it names no bucket value and never takes the ?source_id= blaming arm. Bare and source/domain arms byte-unmoved.
Verified: all 8 ticket checks green on the rebased tree (f-checks 1-6 + npm run test:live tests/live/claims.live.test.ts, 17/17). Full offline project 3843/3843. Red-then-green done: against HEAD's page.tsx the 3 rewritten + 2 new offline cases fail, and EXACTLY the three named expectations failed when the product changed - nothing else in the suite moved. Observed on the real surface: production build on port 8783 against staging, cookie-minted GET /claims -> labels 'Total counts'/'Window counts', bucket counts 0/108/769/0/0; GET /claims?bucket=awaiting_row -> counts byte-identical (0/108/769/0/0) with the new caption clause; GET /claims?tab=standing -> window region only, no total region.
For QA: staging (877 claims) is BELOW the 1,000 cap, so the two figures AGREE there - the divergence is graded offline on a new 3,600-claim fixture whose instants are derived from Date.now() (documented exception to this file's fixed-instant rule: the gauge window is [now-90d, now), so fixed instants would fall out of it). tests/offline/claims/page.test.ts 'two figures on one page never silently disagree'. Judgement call worth attacking: the criterion's 'the bare page still renders the sentence it renders today, to the byte' is honored literally - the kind-label went into a NEW eyebrow element, not into the bucket caption, so BUG-0160's two pinned arms and the bare caption are unchanged. Second judgement: with ?bucket=X AND a source facet that really narrows, the blaming arm stands (true of the source) and the every-bucket clause is ADDED to it rather than replacing it. Touch scope respected; no shared gauge component gained a hook.
- 2026-09-11T12:12:28Z [dispatcher] landed ticket/TASK-0071 onto run/admin-window as ebe30005
- 2026-09-11T12:13:01Z [qa] built → qa: under attack
- 2026-09-11T12:26:09Z [qa] qa → done: Attacked on the landed tree (ebe30005), criteria re-run before reading the builder's note. All 8 stored checks GREEN in the primary checkout and again under receipt.py (offline claims+gauges-ui 336/336, copy+shell 64/64, presence grep, page count 9, lint, build, live claims 17/17).

DROVE IT MYSELF, in a real browser (Playwright/chromium, production build on my own port 8785 against staging, my PID, killed; reads only) and offline on the over-cap fixture:
- bare /claims: labels "Total counts"/"Window counts" both VISIBLE (is_visible true), counts 0/108/769/0/0, window rows identical (877 < the 1,000 cap, so the two agree); caption byte-identical to the pre-ticket bare arm.
- ?bucket=awaiting_row: counts byte-identical to bare (0/108/769/0/0) and the caption's denial is REPLACED by the every-bucket clause, naming no bucket value.
- ?source_id= and ?domain=: both labels stand; ?domain=events 0/108/741/0/0, its caption unmoved (no chip clause).
- ?tab=standing, ?tab=standing&source_id=, ?tab=standing&domain=: one window region, no total region to disagree with it.
- Offline over-cap (3,600 claims, four URLs incl. bucket+source and bucket+domain): totals 720/240/360 vs window 200 per bucket — genuinely divergent — with both regions labelled in every one.
- Dropped/bogus bucket values (nonsense, the parked in_window, uppercased, trailing space, empty, repeated param) all fall back to the bare caption: no every-bucket clause claiming a facet the page did not apply.
- Refusal states (permission denied, table missing, transport failure): caption absent (BUG-0144 holds), table draws the refusal line.

Two builder calls, GRADED:
1. Kind-label in a NEW eyebrow rather than the caption — CORRECT, and it is what makes the byte-identity criterion literally true: the `nothingNarrows` gate only gained `|| bucketFaceted`, false on the bare page, so the bare and source/domain arms are unmoved (confirmed on staging and by the unchanged BUG-0160 pins). It is real visible text through the page's own Eyebrow primitive, not an attribute alone, and it is unconditional, so agreement and divergence are one screen.
2. ?bucket=X with a genuinely narrowing SOURCE facet keeping the blaming arm + appended clause — CORRECT for the source: the arm is true of it and the clause disambiguates.

But call 2's shape breaks with a DOMAIN: /claims?bucket=awaiting_row&domain=events renders "…in the events domain in it UNDER THE FILTERS ABOVE — these counts answer for every bucket; the bucket filter above DOES NOT NARROW THEM", with the only active chip being awaiting_row and counts (0/108/741/0/0) identical to ?domain=events. Expected (BUG-0160's pinned rule): no filter-above claim, since domain draws no chip. Found: both claims in one sentence. Filed BUG-0191 P2 with a strict it.fails pin, watched RED as a plain it. Not TASK-0071's defect — page.tsx:469's gate is byte-unchanged by this diff, so the false clause predates it; the ticket only made it audible. Criteria met, so this closes.

Residuals (no ticket): (a) the "Total counts" eyebrow also stands over the Buckets surface in its error state, where the table holds five dashes and a refusal line — labels the TABLE, not counts, and BUG-0144's arm rule is about the caption, but a reader taking it as a claim about those dashes is the trigger that would make it a ticket; (b) the criterion's "on both tabs … and under a source_id and a domain narrowing" is graded by one standing-tab case on the small fixture — I drove the narrowed standing URLs myself and the label stands, so behaviour holds and coverage is one URL narrower than the sentence. [resolution: fixed] [receipt green: 7 cmd(s), tree d2a96484]
