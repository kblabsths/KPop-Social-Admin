---
id: FEAT-0003
type: feat
status: done
milestone: M1
priority: P1
opened_by: visionary
parent: null
children: [BUG-0025, BUG-0026, TASK-0009]
assignee: null
claimed_at: null
depends_on: [FEAT-0001, FEAT-0002]
touch_scope: []
force: false
resolution: already_done
---
# Dashboard — the breakfast view

## Description
VISION: "Six pages — Dashboard, Queues, Claims, Sources, Cycles & runs, Browse ... each showing real staging rows whose numbers match what the database says", answering the operator questions "did anything happen last night, what needs me, who keeps being wrong". SPEC F3. Contracts read 2026-09-01: admin-observability.md §4 (Dashboard), §1, §6 (the kind is derived in code from the shape — no column carries it); resolver.md §6 (resolution_runs columns) and §11 (review_items columns; severity comes from the registry). The attention summary keeps decision and signal counts separate, each with open count, max severity and oldest age; severity ranks by the registry low/high alone, with no formula (the severity formula is parked). Last night cycles and runs render from resolution_runs and the adapter framework runs, with error_summary surfaced as error lines. Everything on the page links into the pages below — the dashboard is the entry to the investigation path, never a dead end.

## Acceptance criteria
M1 EC3, EC4. Acceptance test 2: the page renders real staging data and its parity check asserts the rendered dashboard counts against direct SQL on staging. Decision and signal counts render as separate figures, each with open count, max severity and oldest age. From the Dashboard, an attention count opens Queues and a cycle line opens Cycles and runs (human-checkable on the close-review screenshots). Acceptance test 9: with the resolver tables absent the page returns 200 with its not-provisioned marker. No severity formula and no parked-section surface is rendered. `npm run lint` and `npm run build` exit 0.

## Checks
```sh
npm run lint
npm run build
```

## History
- 2026-09-02T05:17:31Z [visionary] opened: Dashboard — the breakfast view
- 2026-09-02T12:48:25Z [qa] QA parent review, 2026-09-02 (landed tree, run/admin-window b98367f). Verdict: FEAT-0003 stays OPEN on ONE criterion. Its only child TASK-0009 is done on a green receipt (tree 697a9c3d) after an eight-case attack; I graded the PARENT's criteria, not the child's, and did not re-attack the page.

Met, with what covers each:
- 'Decision and signal counts render as separate figures, each with open count, max severity and oldest age' — TASK-0009's cheerio assertions: two sibling <a> of one parent, same tag and class, ?kind=decision / ?kind=signal, severity word verbatim with no digit beside it, oldest age with the absolute in title. Extra corners pinned there: an all-settled population renders a real 0 without borrowing a severity word or an age, and a tie on one instant renders exactly one age per card.
- 'From the Dashboard, an attention count opens Queues and a cycle line opens Cycles and runs' — src/app/page.tsx:115 queueHref -> /queues?kind=<kind>, lineHref -> /cycles?cycle=<run_id> / ?run=<run_id>, both encodeURIComponent'd; both target routes exist in the build route table (○ /queues, ○ /cycles), so no link is a dead end today. They are placeholders that ignore the param — honouring ?kind= is FEAT-0004's, and this criterion asks only that the count OPENS Queues.
- 'Acceptance test 9: resolver tables absent -> 200 with its not-provisioned marker' — TASK-0009's offline cases render populated / empty / not-provisioned with nothing thrown, the marker names the missing table, and the resolution_runs-absent-beside-present-review_items case renders a card containing no digit at all.
- 'No severity formula and no parked-section surface' — TASK-0009 check 6 (code-lines-only grep for recommendations|incidents|agent_runs) exit 0 in its receipt; no score or rank on the surface.
- 'npm run lint and npm run build exit 0' — re-run by me just now on this tree: lint exit 0, build exit 0, route table shows ƒ / (dynamic, so the page is not a build-time-prerendered frozen error).

UNMET, and it is why this stays open:
- 'Acceptance test 2 / M1 EC4: the page renders real staging data and its parity check asserts the rendered dashboard counts against direct SQL on staging.' Expected: M1 EC4's machine-checkable, 'the live-marked parity tests, one per page, green' (M1.md:61). Found: npm run test:live exits 1, 4 suites failed to collect with LiveGuardError 'STAGING_SUPABASE_URL and STAGING_SUPABASE_SERVICE_ROLE_KEY are not set' — tests/live/dashboard.live.test.ts is WRITTEN and correct in shape (it counts through its own createClient and imports no lib/db read, so the parity is two real paths) but has never rendered a staging row. That is the open env-names ASK, not a build defect: no builder work is owed on FEAT-0003, and nothing here should be re-tickets as a BUG. It clears the moment the staging names exist and that suite runs green.

Ruling on the human-checkable clause, asked explicitly: the above-the-fold-at-1440x900 clause and the walked link chain are close-review screenshot work (TASK-0009 was honest that it was NOT walked — no service-role key in local .env, STACK section 5). Had that been the ONLY gap I would have closed FEAT-0003 and carried the clause into close review, since a criterion the ticket itself marks human-checkable is graded at the milestone close, not by me. It is not the only gap, so the point is moot: the live parity criterion holds the parent open on its own.
- 2026-09-02T12:49:43Z [dispatcher] open → blocked: Every criterion met by TASK-0009 except acceptance test 2 / M1 EC4: the live staging parity test (tests/live/dashboard.live.test.ts) has never run because STAGING_SUPABASE_URL / STAGING_SUPABASE_SERVICE_ROLE_KEY are absent from .env and SERVICES.md declares no target (TASK-0021, on Ben's panel). QA closes this from blocked (--resolution already_done) once npm run test:live is green.
- 2026-09-02T16:18:36Z [qa] blocked → done: LIVE parity pass, 2026-09-02 — the one criterion that held this blocked is now MET, measured, not assumed.

Acceptance test 2 / M1 EC4 ("the page renders real staging data and its parity check asserts the rendered dashboard counts against direct SQL on staging"): `./node_modules/.bin/vitest run --project=live tests/live/dashboard.live.test.ts` -> exit 0, "Tests 7 passed (7)", 2.44s. Ran twice, identical. Staging guard active (tests/live/setup.ts: both STAGING_ names present, host equals the target declared in SERVICES.md), so this is the declared staging project and nothing else.

Not vacuous — I checked the population the parity ran against before signing. Counts read by my own client, independently of the page: review_items 1 (queue=entity_link, source_id NOT NULL, status=open -> 1 signal, 0 decisions, 0 settled), resolution_runs 28, runs 5. So the cycles table, the runs table and both attention figures were compared against real rows; the decision figure is a real rendered 0 with 0 in the database, which is the assertion, not a skip. Every fallback branch in that file (`countsRendered` false -> assert the table name) is a not-provisioned path and none of them was taken: the table-shape assertions ("the two figures plus the settled ones are the table", "no more cycles than its window", ordering by started_at desc, per-row applied/escalated/errors and source/outcome/error_summary equality) all executed against rendered markup.

Receipt: `receipt.py FEAT-0003` GREEN on this landed tree — `npm run lint` exit 0, `npm run build` exit 0.

The other criteria were graded on 2026-09-02 in this ticket's History and stand unchanged; I re-ran nothing of TASK-0009's offline work and re-attacked nothing already certified there. `already_done` is the honest resolution: no builder work was owed on FEAT-0003 — the criterion was unverifiable until staging became reachable, and the first run of it is green.

Attacked, and what I found elsewhere (NOT this ticket's): the live pass is red on four of the seven other page suites. queues 3 failed / 4 passed, review-item 2/2, claims 2/4, sources 1/5; cycles 6/6 and browse 4/4 green. The dashboard is the only page whose live parity is clean end to end. BUG-0024 filed for the most severe (review-item evidence always empty — `observations.entity_type` was dropped by the schema owner and the app still selects it). The rest are recorded per-TASK and in my handoff. None of them touches the Dashboard's reads: dashboard.ts reads review_items, resolution_runs and runs with column lists that all exist on staging (verified column by column against the live schema). [resolution: already_done]
