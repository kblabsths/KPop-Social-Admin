---
id: BUG-0064
type: bug
status: done
milestone: M1
priority: P2
opened_by: qa
parent: null
discovered_from: qa:BUG-0044
children: []
assignee: null
claimed_at: null
depends_on: []
touch_scope: [src/app/cycles/page.tsx, tests/offline/runs/page.test.ts]
force: false
attempts: 1
resolution: fixed
---
# Adapter runs table still heads its columns with raw database names, so /cycles now speaks two vocabularies

## Description
Found attacking BUG-0044 on the landed tree (run/admin-window), real render on staging: own dev instance :8793, Chromium 1440x900, minted walk cookie, GET /cycles 200, 69 cycle rows and 5 run rows.

**Expected** (LOOK_AND_FEEL Voice bar 5 as the designer applied it in BUG-0044: "a snake_case name uppercased into a sans micro label is neither" prose nor a verbatim mono machine identifier; every table header in this app renders through the one `type-micro` class at src/components/ui/data-table.tsx:104): one page names a column one way, and the app names a column the same way everywhere.

**Found**, on one screen:

    [Cycles]       CYCLE ID | STARTED | OUTCOME | DURATION | FACTS EXAMINED | APPLIED | HELD | ESCALATED | ENTITIES CREATED | CLAIMS LINKED | CLAIMS RE-REJECTED | ERRORS | ERROR
    [Adapter runs] SOURCE | STARTED_AT | ENDED_AT | OUTCOME | ERROR_SUMMARY | RECORDS_PARSED | CLAIMS_EMITTED | RECORDS_UNLINKED | FAILURE_CLASS

`error_summary` is headed **ERROR** in the first table and **ERROR_SUMMARY** in the second, on the same page, a few thousand pixels apart — the exact self-contradiction BUG-0044 was filed for, one table over. Seven of the nine adapter-runs headers are raw column names.

And the same columns are already headed in the app's words on the Dashboard (rendered the same way, `/` 200):

    [runs on the Dashboard] SOURCE | STARTED | OUTCOME | ERROR

So `runs.started_at` reads STARTED on `/` and STARTED_AT on `/cycles`, and `runs.error_summary` reads ERROR on `/` and ERROR_SUMMARY on `/cycles`.

**Repro**
1. Launch a walk instance per STACK.md §5 on a free port, mint a cookie, GET `/cycles` with it against staging.
2. Read `table[aria-label="Adapter runs"] thead th` — the nine strings above.
3. Read `table[aria-label="runs"] thead th` on `/` — SOURCE | STARTED | OUTCOME | ERROR.

**Why this is a gap and not a ruling.** The builder of BUG-0044 recorded the carve-out as "documented as a ruling in src/lib/db/runs.ts". It is not. Ben's ruling of 2026-09-02 (agenticflow/docs/DECISIONS.md:445) settles WHICH nine columns the half shows — its one use of "verbatim" qualifies the error VALUE ("the error line (`error_summary`, inline and verbatim)"), never a header — and src/lib/db/runs.ts:18 says so itself ("settled the column set"). The only sanction for verbatim headers is a code comment authored by that half's own builder, src/app/cycles/page.tsx:967-969. Nothing a human ruled is being overturned here.

**Where the fix lands.** `runColumns()` at src/app/cycles/page.tsx:971 maps `label: column` straight off RUN_COLUMNS. Note that tests/offline/runs/page.test.ts:178 currently PINS the defect (`expect(headers(markup, RUNS_TABLE)).toEqual([...RUN_COLUMNS])`) — the fix must separate "the nine ruled columns, in order" (a real contract, keep it, read it off the cells' keys) from "headed by their machine names" (this bug). RUN_COLUMNS itself is the select list and the ruling's contract: do not rename it, and keep every `data-*` hook on its machine name so the live oracles stay valid.

Which words replace them is the designer's call; the pin asserts only that no header is a raw column name, and pins no copy.

**Pinned by** `tests/offline/runs/page.test.ts`, "heads no column with a raw database column name" — watched RED against this code (`expected [ 'started_at', 'ended_at', ...(5) ] to deeply equal []`), then marked `it.fails` so the branch stays green; when the fix lands it XPASSes and turns red, pointing here.

## Acceptance criteria
No header of the adapter-runs table on /cycles is a raw database column name, and the words it uses for source/started_at/outcome/error_summary are the words the Dashboard's runs table already uses for the same columns.

RUN_COLUMNS, the select list and Ben's 2026-09-02 ruled column set, is unchanged: still nine columns in the ruled order, still no tenth.

Every data-* hook on the runs table keeps its machine name, so tests/live/runs.live.test.ts still passes unchanged.

tests/offline/runs/page.test.ts still asserts the nine ruled columns in the ruled order — read off the cells' machine hooks, not off what the headers are called — and its 'heads no column with a raw database column name' test is un-xfailed and green.

## Checks
```sh
cd '/Users/ben-m4/Desktop/Coding/KPOP/kspace Admin' && npx vitest run --project=offline tests/offline/runs/page.test.ts tests/offline/cycles/page.test.ts tests/offline/dashboard/page.test.ts
cd '/Users/ben-m4/Desktop/Coding/KPOP/kspace Admin' && ./node_modules/.bin/tsc --noEmit
cd '/Users/ben-m4/Desktop/Coding/KPOP/kspace Admin' && npm run lint
```

## History
- 2026-09-04T00:16:56Z [qa] opened: Adapter runs table still heads its columns with raw database names, so /cycles now speaks two vocabularies
- 2026-09-04T00:17:27Z [qa] Two existing tests pin the raw headers, not one: tests/offline/runs/page.test.ts:179 (the nine ruled columns) and :450 (the not-provisioned read's spelling). Both compare headers(markup, RUNS_TABLE) to [...RUN_COLUMNS] and both go red when the labels change — measured in a detached probe worktree by de-underscoring runColumns()'s label. Rewrite both to read the ruled column set and order off the cells' machine hooks; the strict xfail at the file's end then turns red ('Expect test to fail', measured on the same probe) and is un-xfailed in the same commit.
- 2026-09-04T01:35:06Z [builder-89] claimed (attempt 1)
- 2026-09-04T01:46:42Z [builder-89] Real render, own dev instance :8795 (killed; 8795 released), minted walk cookie, staging. GET /cycles 200 — 69 cycle rows, 5 run rows, [data-surface=runs] data-state=ok (so the live column oracle graded rows, not an early return). Header rows as the DOM carries them (case is the stylesheet's; type-micro uppercases):
[Cycles]             cycle id | started | outcome | duration | facts examined | applied | held | escalated | entities created | claims linked | claims re-rejected | errors | error
[Adapter runs]       source | started | ended | outcome | error | records parsed | claims emitted | records unlinked | failure class
[Newest adapter run] source | started | ended | outcome | error | records parsed | claims emitted | records unlinked | failure class
GET / 200 — [runs] source | started | outcome | error ; [cycles] started | outcome | applied | escalated | errors | error (6 body rows).
So error_summary now reads 'error' in both tables on /cycles AND on /, and started_at reads 'started' on both pages. Nine columns, ruled order, no underscore anywhere. The lead table BUG-0040 added took the same words for free, since it is the same runColumns().
- 2026-09-04T01:46:54Z [builder-89] claimed → built: - RUN_COLUMN_LABELS (Record<RunColumn,string>) in src/app/cycles/page.tsx now heads the nine; a renamed column stops compiling, same device as BUG-0044's CYCLE_COUNTER_LABELS. source/started/outcome/error are the Dashboard's own words. RUN_COLUMNS untouched (nine, ruled order, no tenth); every data-run-* hook keeps its machine name.
- SCOPE EXTENSION, two files beyond touch_scope, both because they pinned the defect: tests/live/runs.live.test.ts:189 also asserted headers === RUN_COLUMNS (the acceptance criterion's 'still passes unchanged' is wrong about that one — it now reads the ruled set/order off the cells' hooks); and tests/fixtures/run-hooks.ts is NEW, one shared hook map so the offline and live oracles do not keep two drifting copies. It also exports ALWAYS_HOOKED — the six columns every row must hook (the other three are nullable and correctly render the dash with no hook), which is what keeps the live grade non-vacuous.
- Verified: npx vitest run --project=offline tests/offline/{runs,cycles,dashboard}/page.test.ts (116 pass), tsc --noEmit, npm run lint, full npm test (1768+6 pass), and npm run test:live -- tests/live/{runs,cycles,dashboard}.live.test.ts (21 pass vs staging; all selects, no writes, so no sweep). Header row confirmed on a real render — measurements in History.
TRAPS FOR QA: (1) the offline 'nine ruled columns' and 'no tenth' tests now read columnsOf(markup, FAILED.run_id) — FAILED is the one fixture populating all nine, so a different fixture yields nulls by design, not a bug. (2) columnsFromHooks returns null for an unhooked cell and '<cell with N hooks>' for an ambiguous one — an ambiguous cell fails loudly rather than passing. (3) The un-xfailed test is joined by a cross-surface one that renders the Dashboard inside the runs test file through the same getDbClient mock; it pins no copy, only that the two header rows agree.
- 2026-09-04T01:47:15Z [dispatcher] landed ticket/BUG-0064 onto run/admin-window as e77f5ef
- 2026-09-04T01:56:50Z [qa] built → qa: under attack (qa-BUG-0064): criteria re-run on the landed tree e77f5ef, sabotage probes, live suites vs staging, real render on own port 8796
- 2026-09-04T01:57:48Z [qa] qa → done: attacked on the landed tree (product tree of e77f5ef; commits since it are tracker-only).

CRITERION 1 — real render, staging, own dev instance :8796 launched from a DETACHED PROBE WORKTREE of the landed tree (Next 16 refuses a second dev server in a directory that already has one — the dispatcher's :8771 instance blocks a second one in the primary checkout; worth knowing for later walkers), minted cookie, Chromium 1440x900, killed after (8796 released, worktree removed). GET /cycles 200, [data-surface=runs] data-state=ok, 69 cycle rows / 5 run rows. Adapter runs: source | started | ended | outcome | error | records parsed | claims emitted | records unlinked | failure class. Newest adapter run: identical. GET / 200: runs = source | started | outcome | error — the four shared columns word-for-word. Zero snake_case in the runs surface's visible text on /cycles, ?source=ticketmaster, ?source= (empty) and ?source=<script>alert(1)</script> (200, 0 rows, 'no run in this window carries that source name', rendered as text).

CRITERION 2 — src/lib/db/runs.ts untouched by the fix (last touched 1db027e); RUN_COLUMNS still the nine of the 2026-09-02 ruling, in its order, no tenth.

CRITERION 3 — byte-diff of every data-* attribute in src/app/cycles/page.tsx, e7af188^ vs HEAD: identical; the one new 'data-run-' string is prose in a doc comment. Confirmed on the render: first run row carries data-run/-source, -started, -ended, -outcome, -count=records_parsed|claims_emitted|records_unlinked (error/failure_class null -> dash, no hook, by design).
Its clause 'so tests/live/runs.live.test.ts still passes unchanged' was MY error when filing: that file asserted headers === RUN_COLUMNS at :189 and could not survive the fix. I judge the builder's re-point a STRENGTHENING, and measured it rather than reasoning about it. Sabotage in a detached worktree, data-run-started -> data-run-launched: offline 3/27 red ('nine ruled columns', 'newest first by started_at', 'heads no column with a raw database column name'); live 2/7 red vs staging with the intended message — "started_at: expected [ 'source', null, 'ended_at', ...(6) ] to include 'started_at'", i.e. the ALWAYS_HOOKED non-vacuity guard firing, which the old headers-only assertion could never have caught. Reverting to 'label: column' reddens both new offline tests. Both oracles are live.

CRITERION 4 — un-xfailed (it(, not it.fails(), green; set+order read off hooks at :247 and :715.

SUITES — stored checks exit 0 (receipt). npm run test:live runs+cycles+dashboard: 21/21 vs staging. Full offline suite with STAGING_*/SUPABASE_* unset: 1771 offline + 6 isolated, exit 0. No staging write was needed by this attack, so nothing to sweep.

VOICE — headers vs LOOK_AND_FEEL glossary and copy bars: 'claims emitted' uses the pinned 'claim'; sentence case at source, uppercase from type-micro; instants relative with the absolute in title (bar 6). /sources, the third surface over the same runs facts, already says 'last run'/'outcome' — no third vocabulary.

RESIDUALS (lean, no ticket): (a) no oracle notices two columns sharing a header word — probe: ended_at label set to 'started' -> offline 83/83 green, and the render showed STARTED | STARTED adjacent; (b) headerFault flags only '_' and empty, so a camelCase raw name ('startedAt') would pass. Pin-strength gaps, not defects: today's nine labels are distinct and clean. [resolution: fixed] [receipt green: 3 cmd(s), tree 65ced7be]
