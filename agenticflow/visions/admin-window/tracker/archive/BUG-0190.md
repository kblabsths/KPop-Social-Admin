---
id: BUG-0190
type: bug
status: done
milestone: M3
priority: P2
opened_by: qa
parent: null
discovered_from: BUG-0188
children: []
assignee: null
claimed_at: null
depends_on: []
touch_scope: [tests/offline/review/one-place.test.ts, tests/offline/edit/config.test.ts, tests/offline/records/entity-picker.test.ts, tests/probe-area.ts]
force: false
attempts: 1
resolution: fixed
---
# A mirror tree under tests/.probes left by a KILLED run is never swept, and a later run that draws its pid walks the corpse as its own probe

## Description
Found attacking BUG-0188 on the landed tree (run/admin-window @ 3d5b99d, primary checkout, offline vitest, no device, no network, 2026-09-11). BUG-0188's own criteria are green here (all four stored checks exit 0; both of its literal repros are dead). This is the SAME defect class in the OTHER probe area — the one BUG-0188's builder named as out of scope with "no measured consequence" (BUG-0188 History, 2026-09-11T11:42:47Z). It has one, and it is measured below.

**Expected** (`tests/offline/review/one-place.test.ts:246-248`, the case's own comment): "The mirror is the whole world the scan saw, so nothing below is an accident of the real tree" — `expect([...walked].sort()).toEqual([ACTION_PROBE, ROUTE_PROBE].sort())`.

**Found — a false red, in a lane that planted nothing.** The mirror trees these self-guards walk are named from `process.pid` ALONE — `tests/.probes/m2-close-${process.pid}` (`tests/offline/review/one-place.test.ts:214`), `m2-one-call-${process.pid}` (:354), `inline-${process.pid}` (:269), `admin-locked-${process.pid}` (`tests/offline/edit/config.test.ts:1517`), `one-call-${process.pid}` (:2025), `strings-${process.pid}-…` (:1666), and the same idiom in `tests/offline/records/entity-picker.test.ts:2221`. Each is removed only in a case's `finally`, so a run KILLED mid-case leaves the whole tree on disk, and NOTHING sweeps it: `tests/.probes/` is gitignored (`.gitignore:74`) and ESLint-ignores the area (`eslint.config.mjs`, BUG-0030), so neither `git status` nor the suite ever names it. pids are recycled, so a later worker drawing the dead run's number walks the corpse's files as if it had planted them — and every case compares the walk with `toEqual`.

MEASURED on this tree, with the ticket-BUG-0188 band repro pointed at this area (one process plants `tests/.probes/m2-close-<n>` and `m2-one-call-<n>` for n = pid+1..pid+80, each holding `src/lib/review/corpse-of-a-killed-run.ts` = `"use server";`, then the run happens on the SAME command line):
  `./node_modules/.bin/vitest run --project=offline tests/offline/review/one-place.test.ts` -> **2 failed | 14 passed (16)**, exit 1
    - `the M2-close guard itself > reports a server action and a close-named route while they exist`
    - `the one-call-site and close-route rules, guarding themselves > tells the one sanctioned call from a second one, and a delegating route from a direct one`
  at `tests/offline/review/one-place.test.ts:248`:
    `AssertionError: expected [ 'src/app/api/settle/route.ts', …(2) ] to deeply equal [ 'src/app/api/settle/route.ts', …(1) ]`, the extra being `+ "src/lib/review/corpse-of-a-killed-run.ts"`.
  With the band removed, the same command is 16 passed. The band stands in for what time does for free: one killed run plus enough runs.

This is not BUG-0188's fix failing — `src/.probes` is now sweep-and-entropy clean, verified. It is the same hazard one directory over, and the transient half of it survives: the false red above costs a builder or QA lane a bounce and points at a file that is invisible in `git status`.

**Repro (deterministic, no pid band — the pin below IS it):**
 1. `cd "/Users/ben-m4/Desktop/Coding/KPOP/kspace Admin"`
 2. `./node_modules/.bin/vitest run --project=offline tests/offline/review/one-place.test.ts -t "dead run that had this pid"`
 3. Currently: **1 failed | 16 skipped (17)**, exit 1 (watched RED as a plain `it` before this ticket was filed; landed as `it.fails` so the suite stays green while the divergence stands).

**PIN** (landed by qa on run/admin-window, `it.fails` while the divergence stands — **THE FIX FLIPS IT TO A PLAIN `it`**): `tests/offline/review/one-place.test.ts`, describe "the M2-close guard itself", `"is blind to a mirror tree left behind by a dead run that had this pid"`. It plants the corpse at the path a DEAD run with this pid wrote to (`tests/.probes/m2-close-<pid>`), never at the guard's own `probeBase`, so a base this run alone can name is what makes it pass — not a change to where the corpse goes. Non-vacuous: it asserts the corpse is on disk before the walk.

**Fix shape (not binding):** `tests/probe-area.ts` already holds both halves generically and BUG-0188's builder offered them ("the helper is generic if you want them moved"): name each mirror base with this run's pid AND `RUN_ID` (`probeDirFor`-style), and sweep dead-pid siblings under `tests/.probes/` at module load (`sweepDeadProbeDirs` takes a parent/base, so it needs a small generalisation rather than a second copy — LESSONS 5: one shared spelling, imported, never retyped). `PROBE_PARENT` is currently hard-coded to `src/.probes`.

**Not in scope:** `src/.probes` — BUG-0188 fixed it and it holds under attack (dead pids swept, live/foreign/unparseable kept, lint/tsc/build green on a corpse, 5/5 concurrent pairs).

## Acceptance criteria
(to be written)

## Checks
```sh
./node_modules/.bin/vitest run --project=offline tests/offline/review/one-place.test.ts -t "dead run that had this pid"
./node_modules/.bin/vitest run --project=offline tests/offline/review/one-place.test.ts tests/offline/edit/config.test.ts tests/offline/records/entity-picker.test.ts
npm test
```

## History
- 2026-09-11T11:56:51Z [qa] opened: A mirror tree under tests/.probes left by a KILLED run is never swept, and a later run that draws its pid walks the corpse as its own probe
- 2026-09-11T12:17:50Z [builder-283] claimed (attempt 1)
- 2026-09-11T12:30:42Z [builder-283] claimed → built: Both BUG-0188 answers now cover tests/.probes too, parameterised in tests/probe-area.ts rather than copied: probeDirFor(label, parent), mirrorDirFor(label) (absolute base under new MIRROR_PARENT), sweepDeadProbeDirs(base, area), sweepDeadMirrorDirs(). The three guard suites take their mirror bases from mirrorDirFor and call sweepDeadMirrorDirs() once at module scope; labels with a digit are refused by the existing LABELLED rule, so m2-close/m2-one-call became close-guard/close-one-call/close-inline, and config's per-plant strings base is now strings-<pid>-<run id>-<n> (still a direct child, so the sweep reads its pid).
QA's it.fails pin is a plain it and was WIDENED, not just flipped: it plants three corpse spellings (the pre-fix m2-close-<pid> it measured, close-guard-<pid>, and close-guard-<pid>-<other uuid> — the shape a dead run of the FIXED code leaves), so entropy, not the rename, is what makes it pass. New probe-area cases live with the existing ones in tests/offline/db/layering.test.ts (OUTSIDE touch_scope, deliberately — that file owns probe-area's tests): mirror naming, dead-pid sweep incl. the legacy pid-only name, live/ppid/unreadable kept, and each area swept on its own.
Verified on the rebased tree (run/admin-window @ 507e61c8): both stored checks exit 0; npm test 3848+8 passed exit 0; lint and tsc exit 0. Watched red first — pin as a plain it before the fix: 1 failed, extra file src/lib/review/corpse-of-a-killed-run.ts. Three mutations redden the right cases (entropy dropped -> 5 red; mirror sweep pointed at src/.probes -> 3 red; liveness check dropped -> 3 red). Band repro on the fixed tree: 480 corpse trees planted around the run's pid (6 labels x pids+1..+80), then the same command line -> 169 passed exit 0 AND 0 survivors under tests/.probes. Traps for QA: toolchain.test.ts and ui/copy.test.ts still hand-spell mirror bases — both already carry entropy so neither has the defect, and the area-wide sweep now clears their corpses; a corpse whose pid was recycled by a live stranger survives by design (probe-area docstring).
- 2026-09-11T12:31:08Z [dispatcher] landed ticket/BUG-0190 onto run/admin-window as 438aae77
- 2026-09-11T12:31:33Z [qa] built → qa: under attack
- 2026-09-11T12:41:32Z [qa] qa → done: attacked on the landed tree (run/admin-window @ 7ff1e2be, primary checkout, offline vitest, no device, no network). Criteria re-run BEFORE reading the builder story: all three stored checks exit 0 (pin: 1 passed/16 skipped; three guard suites: 169 passed; npm test: offline+isolated green) — receipt GREEN.
One integrated attack across both probe areas:
1. DEAD-PID CORPSES (pids 90000-90012, none live): the six pre-fix pid-only names the ticket measured (m2-close-, m2-one-call-, inline-, admin-locked-, one-call-, strings-<pid>-7) plus seven fixed-shape names (close-guard/close-one-call/close-inline/source-tree/copy-spaces/copy-glossary-<pid>-<uuid>, strings-<pid>-<uuid>-3), each holding src/lib/review/corpse-of-a-killed-run.ts = "use server". One run of six planting suites: 244 passed exit 0, all 13 swept.
2. KEEP CASES, same run: pid 1 (EPERM->alive), a live sleeper I spawned, pid 0, an unsafe-integer pid, a no-digit name, a stray FILE — all 6 survived. src/.probes in the same run: dead credential-/leaf-import- swept, live/foreign/no-pid kept — DEBT-0018 + BUG-0188 guarantees hold.
3. RECYCLED PID: 960 corpse trees (12 spellings x pid+1..pid+80 around the planter pid), vitest on the SAME command line -> 198 passed exit 0; the run workers drew band pids and graded no corpse, so entropy and not the rename is what does the work. Litter converges 163 -> 6 (my 6 deliberate keepers) on the next run, stable after.
4. KILLED RUN MID-SCAN: SIGKILL to my own pgid the instant a mirror tree appeared left close-guard-96440-<uuid> on disk; the next run of the three suites swept it, 169 passed exit 0. The ticket scenario end to end.
5. HAND-SPELLED BASES: source-tree-/copy-spaces-/copy-glossary- corpses swept at dead pids AND ungraded at recycled band pids (toolchain + copy green inside 3).
6. DESTRUCTIVE-RISK PROBE: a symlink named close-guard-90000-aaaa pointing at a real directory is skipped by both sweeps (Dirent.isDirectory() false); target untouched — rmSync recursive never follows it.
7. BLAST RADIUS with corpses on disk: git status clean (both areas gitignored), npm run lint 0, tsc --noEmit 0, npm run build 0 with a syntactically broken .ts and a stray page.tsx in src/.probes and a broken .ts in tests/.probes.
8. CONCURRENCY: 5/5 simultaneous pairs of the five planting suites in one checkout, 198 passed on both sides every pair.
Out-of-scope edit graded and ACCEPTED: tests/offline/db/layering.test.ts already owns probe-area tests; the new cases extend it (no new file, no duplicate pin), run on fixture checkouts rather than the shared area, and each asserts the corpse on disk before the sweep.
Every corpse I planted and my sleeper removed; both areas empty, tree clean.
RESIDUAL (not a ticket): toolchain.test.ts:303 and ui/copy.test.ts:43,246 still hand-spell <label>-<pid>-<uuid> and never call sweepDeadMirrorDirs themselves, so a run of only those two files leaves a killed run corpse until a sweeping suite runs — npm test always runs one, and entropy keeps it ungraded. Same class, third area: toolchain.test.ts:187 plants tsconfig-program-probe-<uuid> under agenticflow/tracker/evidence/, which no sweep clears — measured harmless (tsc 0 and lint 0 with an ill-typed probe.ts left there). [resolution: fixed] [receipt green: 3 cmd(s), tree 431cf223]
