---
id: BUG-0168
type: bug
status: done
milestone: M3
priority: P1
opened_by: qa
parent: FEAT-0015
discovered_from: TASK-0064
children: []
assignee: null
claimed_at: null
depends_on: []
touch_scope: [src/lib/paging/machine.ts, src/components/ui/paging.tsx, tests/offline/ui/paging.test.ts, tests/offline/paging/machine.test.ts]
force: false
attempts: 1
resolution: fixed
---
# A short non-exhausted page ends paging silently: held leaves the bound grid and PageMore draws 'no further rows'

## Description
Found attacking TASK-0064 (landed b292796), offline, no route needed.

**Repro** (`tests/offline/ui/paging.test.ts`, describe "a page that arrives short of the window", first case — currently pinned `it.fails`, campaign admin-window/BUG-NNNN; flip it to `it` to see it red):

1. A surface with window `size = 50` whose first screen held 50 rows: `{rows: [], held: 50, status: "idle", refusal: null}`.
2. One press. The answer is a legal `PageAnswer`: `{kind: "ok", rows: <30 rows>, offset: 50, exhausted: false}` — i.e. the server says it served a short page AND that the set continues.
3. `requestPage` (src/lib/paging/machine.ts, the `ok` arm) returns `{held: 80, status: "idle", rows: 30}` — it ends in `exhausted` only when `answer.exhausted` is true OR `answer.rows.length === 0`.
4. Render `PageMore` with that state, `size = 50`.

**Expected:** the operator can still reach the rest of the set, or is told the set is complete. Either ending is honest, and the pinning assertion accepts both (control drawn, or `data-paging="exhausted"`).

**Found:** `pageBound("80", 50)` refuses (80 is not a multiple of the window), so `drawsControl` is false in src/components/ui/paging.tsx and the markup is exactly:
`<div class="flex flex-col gap-2"><p data-paging="limit" ...>This view shows no further rows.</p></div>`
— zero controls, and a sentence that contradicts the answer the operator just received. `held` is off the grid `pageBound` enforces and nothing ever puts it back, so paging is dead for the life of the view; the rest of the set needs a reload. That is the exact complaint FEAT-0015 exists to answer ("not being able to load all claims if I want to is a huge oversight").

**Also:** machine.ts already claims the fixed behaviour in prose. The `ok` arm comment reads "A short or empty page is the end of the set, whichever of the two said so first" while the condition tests only `rows.length === 0`. Code and comment disagree; one of them is the bug.

**Two honest fixes, architect to rule — the pin accepts either:**
(a) the driver treats a short page as the end of the set (what its own comment already says), or
(b) the route contract is full-or-exhausted and the driver refuses a short non-exhausted page OUT LOUD (a refusal, `held` unmoved, the control retained) rather than silently dropping the affordance.
What must not stand is the third outcome now shipping: the affordance disappears and the surface says nothing is wrong.

**Not a bounce of TASK-0064.** Its criteria specify the `limit` arm exactly as built, and the widget implements them; the defect is the driver letting `held` leave the grid the bound guard requires. Filed before TASK-0066 lands the route on purpose: the route either has to guarantee full pages or this has to be fixed, and the route is being written now.

**Non-vacuity:** the second case in the same describe is the must-NOT-flag fixture — the identical path with a FULL window (50 rows, `exhausted: false`) leaves `held = 100`, draws the control, and is green today.

## Acceptance criteria
RULED (b), the route contract: a paged answer is full-or-exhausted and the driver refuses anything else out loud — see the ruling comment on this ticket and DECISIONS.md 2026-09-10. requestPage reads deps.size (it never did) and grades the ok arm against it on four shapes, each driven offline against a recording stub: rows.length === size with exhausted false appends in the order received, grows held by size, clears the refusal and stays idle; rows.length <= size with exhausted true appends its rows and ends exhausted, the zero-row case behaving exactly as it does today; rows.length < size with exhausted false is REFUSED — the row list comes back identical in length, members and order, held is unchanged so the next press asks for the same bound, status returns to idle, the refusal names the route, and fetchJson was called exactly once; a page LONGER than the window is refused the same way, on any value of exhausted (QA drove 60 rows for a 50 window and it left held at 110). The invariant that kills the class is asserted as a property over that whole matrix and named as such in the test: after any requestPage, either pageBound(String(state.held), deps.size).kind === "ok" or state.status === "exhausted" — held leaves the bound grid only on the final page. PageMore's five arms are unchanged with one case added: an exhausted state whose held is NOT on the grid (the legitimate final short page, e.g. held 80 with size 50) draws the exhausted sentence and data-paging="exhausted", never the limit arm — the limit arm keeps its own case (held = MAX_PAGE_OFFSET + size) and still never says the set is complete. The pin in tests/offline/ui/paging.test.ts, describe 'a page that arrives short of the window', is a plain it() that passes and no it.fails marker remains in that file; because the ruled ending appends nothing, its two non-vacuity assertions are re-expressed against the ANSWER the stub served (30 rows, exhausted false) rather than against the resulting state, and the test says in one line why — an assertion rewritten silently is how a pin stops proving what it was filed for. Its must-NOT-flag sibling (the same path at a full window) stays a plain it and stays green. The window stops being supplied twice with nothing reconciling the copies (QA residual 4): usePageRows returns the size it was handed alongside state and press, so a surface spells the number once and the widget's size prop is fed from the hook rather than retyped, and one offline case drives a consumer-shaped probe proving the value the driver checked against and the value PageMore rendered are the same one. Nothing else moves: the double-press proof, pressing()'s identity arms, fetchJson's Error contract and the four other PageMore arms are green unchanged; machine.ts is still a pure leaf importing only ./bounds and tests/offline/db/layering.test.ts is green over it; the token and primitives guards are green; exactly one fetch( stands under src/components; no route handler, page or component outside src/components/ui/paging.tsx is touched; and the six live parity suites for the two surfaces that will consume this are unchanged.

## Checks
```sh
./node_modules/.bin/vitest run --project=offline tests/offline/ui/paging.test.ts tests/offline/paging/machine.test.ts
./node_modules/.bin/vitest run --project=offline tests/offline/ui/primitives.test.ts tests/offline/ui/tokens.test.ts tests/offline/db/layering.test.ts
! grep -q "it.fails" tests/offline/ui/paging.test.ts
test "$(grep -rn "fetch(" src/components | wc -l)" -eq 1
npm run lint
npm run build
```
## History
- 2026-09-11T00:04:11Z [qa] opened: A short non-exhausted page ends paging silently: held leaves the bound grid and PageMore draws 'no further rows'
- 2026-09-11T00:04:47Z [qa] The "BUG-NNNN" placeholder in the description is this ticket: admin-window/BUG-0168. The pin is LANDED on run/admin-window as tests/offline/ui/paging.test.ts, describe "a page that arrives short of the window", marked it.fails (vitest's strict xfail: measured — a passing it.fails body reports "Expect test to fail" and the file goes red). So the branch suite is GREEN today with the defect standing, and turns RED the day the behaviour is fixed, which is the signal to flip it.fails back to it. The second case in the same describe is the must-NOT-flag fixture and runs as a normal it.
- 2026-09-11T00:18:10Z [architect] criteria amended: Ruled and written (architect, 2026-09-10). See the ruling comment for the reasoning, the contract TASK-0066 now owes, and the two things a builder will otherwise get wrong (the pin's non-vacuity assertions, and the exhausted-vs-limit ordering on a legitimate off-grid final page). QA residual 4 (PageDeps.size never read) is folded in here because it is the same defect one layer over: the window supplied twice and reconciled nowhere is what let held leave the grid in the first place, and this ticket is the one that makes the driver read it. [was: (to be written)] [was: (to be written)]
- 2026-09-11T00:18:20Z [architect] checks amended: Two added lines, the four QA wrote kept verbatim (architect, 2026-09-10). (1) '! grep -q it.fails tests/offline/ui/paging.test.ts' — the criteria require the pin flipped back to a plain it, and nothing stored graded that: an it.fails file is GREEN exactly while the defect stands, so every other line here can pass with the bug in the tree. Measured today: one it.fails at tests/offline/ui/paging.test.ts:612, so this line is red now and green the day the fix lands. (2) the fetch-count ratchet from TASK-0064, because this ticket edits the one module that holds the repo's only network call and a builder reaching for a second fetch to 'ask the server whether it is exhausted' is the obvious wrong turn; measured on this tree: exactly 1, so the line is green today and stays a ratchet. Line 1 re-measured green today (61 passed, 651ms). [was: ./node_modules/.bin/vitest run --project=offline tests/offline/ui/paging.test.ts tests/offline/paging/machine.test.ts, .… (298 chars; full text in git history)]
- 2026-09-11T00:18:46Z [architect] RULING (architect, 2026-09-10): **(b) — the route contract is full-or-exhausted, and the driver refuses a short continuing page OUT LOUD.** Recorded in DECISIONS.md (2026-09-10, 'a paged answer is full-or-exhausted') and in ARCHITECTURE §4.3 kind 3, which gains the bullet 'Full-or-exhausted, on BOTH sides of the wire'.

**Why not (a), which the driver's own comment already claims.** (a) is cheaper and it is what the code was trying to say — but it makes the CLIENT infer the end of a set from a row count. A page that arrives short for any reason other than the set ending (a truncating proxy, a stale deploy, a route mid-rewrite, a mangled body) then renders as 'the set is complete', which is a false totality claim on the one surface that exists because Ben said 'not being able to load all claims if I want to is a huge oversight'. §4.3 exists to make exactly that impossible: an answer is complete or it is refused, never silently partial. The refusal arm costs the operator one retry; the inference arm costs them rows they will never know were missing. And (b) is not a new rule — §4.3 already said 'Complete-or-refuse holds INSIDE a page … never a half-filled ok'; what was missing was its client half.

**What (b) buys beyond this bug.** `held` grows by exactly one window on every continuing page, so `pageBound(String(held), size)` is ok BY CONSTRUCTION until the last page — the invariant in criterion 3. That closes the class, not the instance: residual 5 (usePageRows has no bound guard of its own, so a second consumer can press past the ceiling) and any future surface wiring the widget slightly differently can no longer re-open it, because the driver itself refuses to leave the grid.

**The contract TASK-0066 owes, and why it is chained behind this** (its criteria are amended the same day): the route derives `exhausted === rows.length < CLAIM_WINDOW` from the read it just made, and NEVER emits a short-and-continuing or over-long page. Both sides then hold the same rule and the refusal arm is unreachable in normal operation — which is the point: it exists for foreign data on a wire, like `isPageAnswer`'s arm, not for our own route's ordinary answers.

**Two traps, both of which will otherwise cost a bounce:**
1. **The pin's own non-vacuity assertions contradict this ending.** `tests/offline/ui/paging.test.ts:612-624` asserts `expect(next.rows).toHaveLength(30)` before it checks the markup — true under (a), FALSE under (b), where the refused page appends nothing. The pin is fix-agnostic in its markup assertion and not in its setup. Re-express the two setup assertions against the ANSWER the stub served (30 rows, `exhausted: false`) and say in one line why they moved; do not weaken the markup assertion, and do not touch the must-NOT-flag sibling below it.
2. **Order the arms so `exhausted` wins over `limit`.** A legitimate FINAL page is short: `held` 80 with `size` 50, `status: "exhausted"` — off the grid, and honestly so. `PageMore` must read that as the exhausted sentence, never as the limit arm, or this ticket ships the same wrong sentence it was filed to remove. The limit arm's own case (`held = MAX_PAGE_OFFSET + size`, an IDLE state) is unchanged.

**QA residual 4 is folded in here** because it is this defect one layer up: `PageDeps.size` was never read by `machine.ts`, so the window was supplied twice — once to the driver, once to `PageMore` — and reconciled nowhere. Under this ruling the driver must read it (it is the number the invariant is checked against), and `usePageRows` hands the same value back out so a surface spells it once. TASK-0067 and TASK-0069 take the size from the hook rather than retyping the constant; noted on both.

**Not folded in, deliberately:** residuals 1 (`not_provisioned` names its object twice) and 2 (`isPageAnswer` accepts an empty `message`/`reason`) — they are refusal-wording defects that become reachable only when TASK-0067 wires a page, and they belong to whoever meets them there, not to a driver-contract fix. Residual 3 ('Press it again' beside a disabled control) is the designer's call, as QA said.
- 2026-09-11T00:20:04Z [architect] priority: P2 → P1: Scheduling ruling (architect, 2026-09-10): P2 -> P1. QA filed it P2 correctly for its own blast radius (one widget arm), but the ruling makes it the ticket that WRITES the full-or-exhausted contract, and TASK-0066 and TASK-0067 — both P1, both the M3 critical path — now depend on it. A P2 gating two P1s is a scheduler telling the dispatcher to run the important work last. Nothing about the bar or the scope changes.
- 2026-09-11T00:20:59Z [builder-252] claimed (attempt 1)
- 2026-09-11T00:32:15Z [builder-252] claimed → built: RULED (b) built on branch ticket/BUG-0168 (e8f1d0c, rebased on ccd4873). (1) requestPage now reads deps.size: full window + continuing appends and grows held BY THE WINDOW; exhausted:true appends whatever it carries and ends exhausted; short-and-continuing and over-long (either exhausted value) are REFUSED naming the route, rows/held/order untouched, idle, one fetch. Empty page is deliberately UNCHANGED (exhaustion, whatever the flag says) per the criteria's zero-row clause. (2) usePageRows returns size; PageMore's prop is fed from it. PageMore's arms are untouched - exhausted already preceded limit, so the off-grid final page (held 80, size 50) was already right and now has a case pinning it. (3) The pin is a plain it(); its two non-vacuity assertions moved onto the ANSWER the stub served (30 rows, exhausted false) with a one-line why, markup assertion and must-NOT-flag sibling untouched.

Verified on the rebased tree: all six stored checks green (70 paging tests, 139 guard tests, no it.fails, exactly 1 fetch(, lint 0, build 0); full npm test 3506+6 green; tsc --noEmit clean. Red-then-green measured by swapping HEAD's machine.ts+paging.tsx back in: 6 of the new/changed assertions fail pre-fix (including the flipped pin and the INVARIANT property), all pass after.

Three things QA should know. (a) Three PRE-EXISTING fixtures served a short continuing page, which the ruling makes illegal, so they became full windows, each with a one-line why: machine.test.ts 'is cleared by the next press that succeeds' and the double-press non-vacuity half, and ui 'a press after the answer lands asks for the NEXT bound' - whose bounds are now 50 then 100, not 50 then 51. Behaviour graded is unchanged; the fixtures are just legal answers now. (b) I added one guard the criteria did not ask for: a deps.size that is not a positive integer refuses out loud, because NaN passes BOTH the short and over-long comparisons and would otherwise append rows against a window pageBound can never honour (it has its own two-fixture case). (c) The invariant property is asserted over a real window by construction; with a nonsense size every bound is refused, so the (b) guard is what keeps that case honest rather than the property.

Touch scope respected exactly: 4 files, no route/page/component outside src/components/ui/paging.tsx.
- 2026-09-11T00:32:36Z [dispatcher] landed ticket/BUG-0168 onto run/admin-window as ba48ab0
- 2026-09-11T00:33:01Z [qa] built → qa: under attack
- 2026-09-11T00:40:55Z [qa] qa → done: attacked: criteria re-run on the landed tree (ba48ab0) before reading the builder's story. Six stored checks green in the primary checkout, then 6/6 exit 0 under receipt.py (70 paging tests, 139 guard tests, no it.fails, exactly 1 fetch(, lint 0, build 0).

Criteria graded clause by clause against the landed files: four ok-arm shapes each on the recording stub - machine.test.ts:279 (full+continuing appends in order, +SIZE, idle, refusal cleared, 1 request), :298 (exhausted true over 0/1/SIZE rows), :319 (empty page unchanged), :331 (short+continuing REFUSED, rows identical in length/members/order, held unmoved, route named, 1 request), :353 (over-long refused on both exhausted values). INVARIANT property at :394 = 12 shapes x 3 start states = 36 presses, both endings observed. Arm ordering: ui:95 exhausted at held 80 / size 50 -> data-paging="exhausted" with a non-vacuity assertion that pageBound really refuses that bound; ui:114 keeps the limit arm at MAX_PAGE_OFFSET+size and never claims completion; ui:131 must-NOT-flag draws the control one window below. Pin at ui:710 is a plain it(); its two non-vacuity assertions now read the ANSWER (30 rows, exhausted false) with the one-line why, sibling ui:729 untouched. I read every removed line of the diff: only the three illegal fixtures moved, and ui:508 now serves offset: 999, so "the answer's own offset never reaches the state" is pinned harder than before, not weaker. ui:553 is discriminating, not decorative (the same 7-row answer LANDS at window 7 and is REFUSED at window 8). Touch scope: 4 files, no route/page/component outside paging.tsx, tests/live untouched.

My attack, 9 probes driving the landed modules then rendering PageMore. Ceiling: held 100000 + a full continuing page -> held 100050, idle, pageBound refused, limit arm (the ruled limit case, honest sentence). Foreign rows [null, undefined, 7, "x", ...] append verbatim -> held 100, rows[0] === null, control drawn. A server repeating a window -> 100 rows, 51 distinct ids. Caller mutating the served array afterwards does NOT poison state (50 rows, first r0): the spread copies. All four refusal arms (short / not_provisioned / error / refused) -> held 50, control retained, role=alert, retry advice matches a drawable control. Over-long at 51/500/10000 rows -> refused, held 50, nothing appended. Windowless size NaN/0/-50/2.5 -> refused, 0 rows, exactly 1 request, no control drawn.

The builder's beyond-criteria guard earns its place: without it NaN passes BOTH served > size and served < size, so a page would append against a window pageBound can never honour - the same dead-paging this ticket removed, through a second door. Keep it.

Residuals, none reachable from a shipped path (grep across src: nothing imports initialPage/usePageRows outside the widget; only the barrel's PageMore re-export). (1) initialPage's held is unguarded: initialPage(30, true) at size 50 draws the limit arm with no control, and presses from there leave held 80 then 130, idle, limit forever - "ok BY CONSTRUCTION" is an induction whose BASE is unchecked. Trigger: TASK-0067/0069 wiring a surface whose first screen can report an off-grid held with more=true. (2) machine.ts:206-209's prose invariant is false at the ceiling (measurement above); the property test starts only from sub-ceiling states. (3) PageMore's size is still an independent prop - driver window 50 / held 100 renders limit at widget size 30 and a control at 100; the hook makes the right wiring available, nothing forces it. (4) isPageAnswer validates only Array.isArray(rows) - the architect's deferred residual-2 class, the route's to own. (5) machine.test.ts:200's title still says "grows the bound by the row count"; it grows by the WINDOW now, and its full-window fixture hides the drift.

Verdict: ruled (b) is built, the class is closed for every path a press can take, and the sentence the ticket was filed about is gone. [resolution: fixed] [receipt green: 6 cmd(s), tree 0cc525e8]
