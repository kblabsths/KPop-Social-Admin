---
id: TASK-0025
type: task
status: done
milestone: M1
priority: P2
opened_by: architect
parent: FEAT-0008
children: []
assignee: null
claimed_at: null
depends_on: []
touch_scope: [src/app/records/[table]/[id]/page.tsx]
force: false
resolution: already_done
---
# ASK BEN: what does the edit surface show for provenance on groups and idols, which have none?

## Description
**Contract silence.** ARCHITECTURE marker `OPEN-PROVENANCE`. SPEC named gap 3.

Spec section 8 says *'per-field provenance shows at the edit surface'*, and LOOK_AND_FEEL quality bar 5 makes it a bar: *'provenance shows at the fact ... and every editable field'*. But `groups` and `idols` are **pre-cutover**: they edit directly, *'legal and unprovenanced as the ownership rules allow'* (spec section 8), so **no `field_provenance` row exists for them** and none will until catalog maintenance cuts them over (ROADMAP queue item 9).

So the two rules meet: the bar says show provenance at every editable field; the data says there is none to show.

**Recommend: render nothing in the provenance slot for a pre-cutover table** — no line, no placeholder, no 'unknown'. SPEC calls this 'the honest read' and asks for confirmation before shipping it. The alternative worth considering is one `micro` label per record (not per field) saying the table is not yet resolver-owned, so the operator knows the absence is structural rather than a lookup that failed — LOOK_AND_FEEL's Interaction principle that an empty state and an unprovisioned state must never share a rendering points that way. What must not happen is a per-field 'no provenance' line on every field of every record, which is noise the operator learns to ignore.

TASK-0018 builds everything else on the edit surface and leaves this slot empty until the answer lands.

## Acceptance criteria
(human-checkable) Ben confirms the empty slot, or names the one line to show. The architect then removes the `OPEN-PROVENANCE` marker from ARCHITECTURE and amends TASK-0018's criteria.

## Checks
```sh
bash -c "! grep -q OPEN-PROVENANCE agenticflow/docs/vision/ARCHITECTURE.md"
```

## History
- 2026-09-02T05:45:08Z [architect] opened: ASK BEN: what does the edit surface show for provenance on groups and idols, which have none?
- 2026-09-02T05:45:09Z [architect] open → blocked: ASK BEN: LOOK_AND_FEEL requires provenance at every editable field, but groups/idols are unprovenanced by construction. Recommend rendering nothing in that slot; confirm before shipping.
- 2026-09-02T16:28:24Z [dispatcher] Ben 2026-09-02 (ruling): keep the current rendering — the provenance slot reads 'no provenance recorded (pre-cutover table)' for groups/idols. Architect: close this ASK.
- 2026-09-02T16:41:41Z [architect] blocked → open: UNBLOCKED and ANSWERED — for QA to close as already_done; nothing is left to build. Ben's ruling, 2026-09-02: KEEP the landed rendering — the provenance slot on a pre-cutover table reads 'no provenance recorded (pre-cutover table)'. Absence with its reason beats a blank, and it means every table's provenance slot is filled with something true (the resolver-owned tables get real per-field provenance under TASK-0029). TASK-0018's criteria need no amendment: what it built is what Ben confirmed. Architect's part done: the OPEN-PROVENANCE marker is out of ARCHITECTURE §12 (this ticket's stored check passes — dry-run 2026-09-02) and the ruling is a dated paragraph in DECISIONS.md.
- 2026-09-02T16:49:04Z [qa] open → done: Closed on Ben's ruling ("keep the current rendering"); nothing was left to build and nothing was built. Receipt GREEN (OPEN-PROVENANCE appears nowhere in ARCHITECTURE, re-run verbatim on 00a53c8).

VERIFIED THE BEHAVIOUR BEN CONFIRMED IS THE BEHAVIOUR THAT IS LANDED — the structural facts, not the words (copy is the designer's call and I pin none of it):
- The per-field provenance slot on a pre-cutover table renders NOTHING: no line, no placeholder, no "unknown". src/app/records/[table]/[id]/page.tsx has no per-field provenance element on the pre_cutover branch.
- The absence is explained ONCE PER RECORD, not per field, by `regimeNote(config)` at page.tsx:71-78, branching on `config.regime === "pre_cutover"` — which is exactly the shape the ASK recommended and the noise it warned against ("a per-field 'no provenance' line on every field of every record") is not there.
- The distinction is made by the same enum the WRITE path uses (`Regime` in src/lib/edit/config.ts:39, :80, :97, :188), so the note cannot drift out of step with which tables actually edit directly.
- Ben's phrasing on the ruling ("no provenance recorded (pre-cutover table)") is a description of that state, not a literal in the source; the landed sentence says the same thing in the app's voice. I checked the behaviour, not the wording, and TASK-0018 needs no amendment.

RESIDUAL, one line, not a ticket: the docblock at page.tsx:64-69 still calls this "the open question ... nothing is invented until it is answered" and cites TASK-0025 as open. It is answered now. Stale comment only — no behaviour depends on it; worth a sentence the next time that file is touched. [resolution: already_done]
