---
id: BUG-0193
type: bug
status: done
milestone: M3
priority: P2
opened_by: qa
parent: FEAT-0016
discovered_from: BUG-0192
children: []
assignee: null
claimed_at: null
depends_on: [BUG-0192]
touch_scope: [src/lib/claims/filters.ts, src/app/claims/page.tsx, tests/offline/claims/page.test.ts]
force: false
resolution: already_done
---
# The standing tab's own bucket is subtracted with the URL's, so the chip clause is decided off the other tab's population

## Description
**Expected** (BUG-0192's own rule and its own named hazard 1): on the standing tab, the family subtraction drops the chip facets the URL carried, NEVER the tab's own bucket — "dropping that would compare this tab's rows against the other tab's population". `withoutChipFacets`' docstring states the same guarantee: "a facet a SURFACE merged in of its own accord survives it."

**Found** at `src/lib/claims/filters.ts:177-188`: the subtraction decides FROM `asked` but deletes FROM `applied`, and for `bucket` those are two different facets on the standing tab. `listFilterOf` merges the tab's own `bucket = standing_disagreement` into the applied filter and the URL's `?bucket=` is dropped; when the URL carried a `?bucket=` anyway, `asked.bucket !== undefined` is true, so `delete widened.bucket` removes the TAB's bucket. The widened count is then a count of the whole view, not of this tab's subset, and the attribution answer is decided off the wrong set.

```ts
// src/lib/claims/filters.ts:177
export function withoutChipFacets(applied: ClaimsFilter, asked: ClaimsFilter): ClaimsFilter {
  const widened: ClaimsFilter = { ...applied };
  for (const facet of CHIP_FACETS) {
    if (asked[facet] !== undefined) delete widened[facet];   // deletes the TAB's bucket too
  }
  return widened;
}
```

**Repro (exact), offline, on this landed tree:**

1. `/claims?bucket=escalated&source_id=<SOURCE.third>&domain=groups` (the buckets tab; all three facets applied, dropped-parameter line empty).
2. Click the tab "Standing disagreements". Its href is written by `tabLinks`/`claimsQuery` off `filter`, which on the buckets tab still carries `bucket`, so the click lands on `/claims?bucket=escalated&source_id=<SOURCE.third>&domain=groups&tab=standing`. The state is ONE CLICK away, not a hand-typed URL.
3. On that tab the source chip removes NOT ONE row: the standing subset in `domain=groups` is exactly claim `…909` whether or not `source_id=SOURCE.third` is set (measured on `tests/offline/claims/population.ts`).

Expected (the ticket's rule): no clause points at the chip bar, and the sentence is the one the same page renders without the parameter it dropped.
Actual, measured 2026-09-11 by rendering the page (`tests/offline/claims/page.test.ts`):

```
without ?bucket=  : "… so it holds all the claims in the standing_disagreement bucket, in the groups domain the read found."
with    ?bucket=  : "… so it holds all the claims in the standing_disagreement bucket, in the groups domain, matching these filters the read found."
```

The recorded stub calls name the cause — the widened `head:true` count issued for the `?bucket=` render is

```
select("*",{head:true,count:"exact"}).neq("bucket","in_window").eq("domain","groups")
```

with the tab's `eq("bucket","standing_disagreement")` gone, against `…eq("bucket","standing_disagreement").eq("domain","groups")` on the render without `?bucket=`. It is a count of the OTHER tab's population, and every other count this tab issues carries the tab's bucket.

**Blast radius:** the list's window line, the empty card (`NOTHING_MATCHED.filters`) and — on a URL whose source chip also survives into `filter` — the bucket caption's `underTheFilters`, i.e. exactly the clauses BUG-0192 landed. The gauge leg is NOT affected: `gaugeWithoutChipFilter` spreads `populationFilter` first (`src/app/claims/page.tsx:1178`), so the tab's bucket is put back; the list leg has no such repair.

**Not reachable on staging TODAY, and why it is still a bug:** staging holds 0 standing_disagreement claims (measured 2026-09-11 on a production build at `/claims?tab=standing`: "the read happened and found no claims in the standing_disagreement bucket at all"), so the standing surface's population is 0 and `isSurfaceNarrowed` answers false in every state, hiding this. The first standing disagreement the resolver produces arms it.

**A fix must not simply subtract from `asked`**: the widened filter has to stay the APPLIED read with only the facets the URL CONTRIBUTED removed — i.e. drop a chip facet only where `applied[facet] === asked[facet]` (or hand the surface's own facets in), so the tab's bucket survives and the count stays inside this tab's subset.

**The pin** is already on the branch as a strict xfail: `tests/offline/claims/page.test.ts`, `it.fails("claims no filter above on the standing tab when the chip set removed no row of it")`, inside `describe("a narrowing with no chip row")`. It reaches the state by FOLLOWING the tab link off the markup, asserts the read (every `head:true` count of the standing tab carries the tab's bucket), asserts the sentence (no `NARROWED_BY_FILTERS`, and byte-equal to the line without the dropped parameter), and is non-vacuous the other way (a source that really removes a standing row still earns the clause). Watched red as a plain `it(...)` on this tree, twice — once failing on the sentence, once on the read shape. **The fixing builder flips `it.fails` back to `it`** (this repo's own convention, e.g. `tests/offline/claims/page.test.ts:734`), which is what the check below then grades.

## Acceptance criteria
The family subtraction widens the APPLIED read by only the facets the URL contributed: a chip facet is dropped from the widened filter only where the applied read took it FROM the URL, so a facet a surface merged in of its own accord (the standing tab's bucket = standing_disagreement, merged by listFilterOf) survives it. Graded on the app, not on the function: at /claims?bucket=escalated&source_id=<SOURCE.third>&domain=groups&tab=standing — one click from the buckets tab, since claimsQuery carries bucket into the tab href — every head:true count the standing tab issues carries eq(bucket, standing_disagreement), the widened one included, and the window line carries no chip clause, byte-equal to the line the same page renders without the ?bucket= parameter it dropped. Non-vacuous the other way: a source that really removes a standing row still earns the clause. The existing strict-xfail pin at tests/offline/claims/page.test.ts (it.fails("claims no filter above on the standing tab when the chip set removed no row of it"), in describe("a narrowing with no chip row")) is flipped back to a plain it() and passes; it is the grader, and no word of it is weakened to make it pass. Everything BUG-0192 landed stays landed: the gauge leg keeps its one answer without relying on gaugeWithoutChipFilter's spread order for correctness, and the offline claims and url suites, lint and build are green.

## Checks
```sh
./node_modules/.bin/vitest run --project=offline tests/offline/claims/page.test.ts -t "claims no filter above on the standing tab"
./node_modules/.bin/vitest run --project=offline tests/offline/claims tests/offline/url
npm run lint
npm run build
```

## History
- 2026-09-11T14:08:52Z [qa] opened: The standing tab's own bucket is subtracted with the URL's, so the chip clause is decided off the other tab's population
- 2026-09-11T14:11:38Z [architect] milestone: (none) → M3: Architect ruling 2026-09-11: M3. This is FEAT-0016's own ticket surface (the chip clause BUG-0192 landed), not a new front, and it is the twin of BUG-0192's reopen — it closes on the same lane, in the same milestone, or not at all.
- 2026-09-11T14:11:43Z [architect] depends_on: [] → [BUG-0192]: Serial at the destination (architect, 2026-09-11): same three files, same one-line fix. BUG-0192 is reopened on this exact defect and its next builder (a fresh spawn — the original is past the resume cap) carries the fix in its scope, so dispatch must never offer both into src/lib/claims/filters.ts at once. BUG-0193 is not a second lane; it is the twin that the QA re-check of BUG-0192 closes.
- 2026-09-11T14:11:55Z [architect] TWIN OF THE BUG-0192 REOPEN — expected to close already_done, not to be built (architect ruling, 2026-09-11).

QA filed this alongside reopening BUG-0192 on the SAME defect: BUG-0192's own named hazard 1, unimplemented — `withoutChipFacets(applied, asked)` (src/lib/claims/filters.ts:177) decides from `asked` but deletes from `applied`, so on the standing tab the tab's merged `bucket = standing_disagreement` is deleted along with the URL's and the widened count compares this tab's rows against the other tab's population. Same files, same fix, same pin.

WHO FIXES IT: the next BUG-0192 builder, inside BUG-0192's scope. That ticket's bounce note (2026-09-11T14:09:29Z) carries the full criteria — the fix shape ('drop a chip facet only where applied[facet] === asked[facet]', or hand the surface's own facets in), the strict-xfail pin at tests/offline/claims/page.test.ts (`it.fails("claims no filter above on the standing tab when the chip set removed no row of it")`, committed b53e1e8c, watched red twice as a plain `it`), and the instruction to flip `it.fails` back to `it`. Nothing is to be re-derived here.

WHO CLOSES IT: the QA lane that re-checks BUG-0192. When that re-check is green, run this ticket's Checks on the same tree and close it `already_done` citing BUG-0192's landed commit. Do NOT dispatch a builder to it: `depends_on: [BUG-0192]` is set so it is never offered into the same files concurrently, and a second builder in filters.ts would be two tickets writing one destination for no gain.

IF the BUG-0192 fix does NOT satisfy this ticket's Checks (the xfail still red, or the read-shape assertion fails), this ticket stops being a twin and becomes the live lane: keep it open, re-dispatch it on its own, and say so in its History.
- 2026-09-11T14:12:08Z [architect] criteria amended: Filled the placeholder (architect, 2026-09-11). QA filed this with '(to be written)' beside a complete, measured repro; this is that repro's bar restated as the ticket's rule, so the already_done close has something to cite and a builder taking it over (the branch where BUG-0192's fix does not satisfy it) is not left deriving the criteria from a Description. It ADDS no requirement beyond BUG-0192's bounce note — same fix shape, same pin, same suites. Checks unchanged and not re-dry-run: they are QA's, measured on the landed tree 253fa5a6 at filing. [was: (to be written)]
- 2026-09-11T14:46:56Z [qa] open → done: Closed already_done off BUG-0192's landed re-fix (2c928a80, builder commit 0f0a433c) - the twin closure the architect's 2026-09-11 note prescribed, not a no-op: I ran THIS ticket's four Checks on the landed tree and computed its own receipt (BUG-0193.json, all four exit 0), and I graded its criteria myself rather than inheriting BUG-0192's verdict.

CRITERION BY CRITERION on 8774a63a:
1. 'A chip facet is dropped only where the applied read took it FROM the URL' - src/lib/claims/filters.ts:188-200: the loop skips any facet present in  (tabFacetsOf, filters.ts:378) and otherwise requires applied[facet] === asked[facet]. Both call sites are handed tabOwn (page.tsx:1155 list, 1193 gauge).
2. 'Graded on the app, not the function', at /claims?bucket=escalated&source_id=<SOURCE.third>&domain=groups&tab=standing: the recorded stub calls show 5 head:true counts and EVERY one carries eq(bucket, standing_disagreement), the widened one ({bucket:standing_disagreement, domain:groups}) included. The read shape is byte-identical to the same state without the ?bucket= parameter - so is the window line, and it carries no chip clause.
3. Non-vacuous the other way: in my 128-state sweep the standing tab earned the clause in every state where the source really removes a standing row, and in none where it does not (90 of 128 states clause-bearing overall).
4. 'The strict-xfail pin is a plain it() and no word of it is weakened': git diff b53e1e8c..HEAD -- tests/offline/claims/page.test.ts is exactly  ->  plus the countFacets helper hoisted to describe scope; no assertion byte changed. It passes (check 1, exit 0).
5. 'The gauge keeps its one answer without relying on gaugeWithoutChipFilter's spread order for correctness': the gauge subtraction now receives tabOwn itself (page.tsx:1193), so the bucket survives the subtraction rather than being restored by the ...populationFilter spread ahead of it; the spread is untouched, as the architect required.
6. Suites, lint, build green (receipt).

Beyond the criteria I swept all 128 states of both tabs and 18 malformed URLs against fixture-computed arithmetic and found no state where the page attributes a narrowing to a chip that removed nothing - full evidence in BUG-0192's close note. My sweep is pinned at tests/offline/claims/page.test.ts (commit 8774a63a) and was watched red against the defective subtraction.

NOT REACHABLE ON STAGING and still correctly fixed: I counted it myself - pending_claims on ubfjjqlvnpnoborczbdb holds 0 standing_disagreement claims, so this tab's population is 0 there and isSurfaceNarrowed answers false in every state. The offline render-plus-recorded-calls pins are this defect's only live surface; no walk claimed. [resolution: already_done]
- 2026-09-11T14:47:11Z [qa] CORRECTION to two clauses my close note lost to shell backtick expansion (the facts are unchanged): (1) criterion 1 reads "the loop skips any facet present in OWN (tabFacetsOf, src/lib/claims/filters.ts:378) and otherwise requires applied[facet] === asked[facet]"; (2) criterion 4 reads "git diff b53e1e8c..HEAD -- tests/offline/claims/page.test.ts is exactly it.fails( changed to it( plus the countFacets helper hoisted to describe scope; no assertion byte changed."
