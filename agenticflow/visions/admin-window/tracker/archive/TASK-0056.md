---
id: TASK-0056
type: task
status: done
milestone: M2
priority: P1
opened_by: architect
parent: FEAT-0010
children: []
assignee: null
claimed_at: null
depends_on: [TASK-0049, TASK-0055]
touch_scope: [src/components/review/close/link-actions.tsx, tests/offline/review-item/link-actions.test.ts]
force: false
attempts: 1
resolution: fixed
---
# The entity_link fact item's two actions: link to an existing entity, or settle

## Description
**SPEC F10 / spec §7, the `entity_link` FACT item** (`source_id` null — the source-pattern item with `source_id` set is the SIGNAL and takes dispositions instead; `lib/review/shapes.ts` is the one place that distinction is made, §11). Its close slot offers exactly two controls:

1. **Link to an existing entity** — the picker of FEAT-0012, rendered inside the close slot. Produces `action: "link_entity"` carrying the chosen entity's id as `value.ref`; `settle_review_item` writes the confirmed match, which the resolver's next cycle reads to unlock the waiting claims.
2. **Settle** — leave it held. The item settles; the claim keeps waiting in its bucket. Produces `action: "settle"` with `value` null.

Each is exactly one call. There is no "create the entity" control here or anywhere: entity creation is the resolver's, and the picker offers only rows that exist.

**Fixtures, not staging**: no `entity_link` FACT item exists on staging (the one row there is the source-pattern signal). This is graded offline against the fixture; "built, not walkable on this data" is the honest grade, exactly as M1 graded the same gap.

**The absence is the normal case and is graded FIRST.** `verdicts` and `settle_review_item` do not exist on staging and will not until Ben installs the handoffs — that is what `main` deploys against. The surface renders its not-provisioned state naming the missing object, offers no control it cannot perform, and nothing throws. **No Admin-side workaround** for the absent schema may be written (spec §10's one forbidden move).

## Acceptance criteria
1. With `verdicts` present (stub) and an `entity_link` fact fixture: the close slot contains exactly two controls — the picker's link action and "settle" — and no others; no create affordance exists on any path.
2. The link action's submission is exactly one call carrying `action: "link_entity"`, the item's id, and the chosen entity as `value.ref`; "settle" is exactly one call carrying `action: "settle"` and a null `value`. Both asserted against the recording stub, and `decisionRefusals` returns `[]` for each.
3. The shape distinction is read from `lib/review/shapes.ts`: a fixture with `source_id` set renders the SIGNAL's dispositions and not these two, and a fixture with `source_id` null renders these two and not the dispositions — both asserted, so a mis-shaped item cannot silently take the wrong action set.
4. With `verdicts` absent: neither control renders and the not-provisioned card names the missing object.
5. `npm run lint`, `tsc --noEmit`, the targeted offline file and the review-item live suite are green.

## Checks
```sh
bash -c 'rm -rf .next/types && npm run lint && ./node_modules/.bin/tsc --noEmit'
./node_modules/.bin/vitest run --project=offline tests/offline/review-item/link-actions.test.ts
npm run test:live -- tests/live/review-item.live.test.ts
```

## History
- 2026-09-09T01:20:51Z [architect] opened: The entity_link fact item's two actions: link to an existing entity, or settle
- 2026-09-09T05:32:55Z [builder-164] claimed (attempt 1)
- 2026-09-09T05:52:08Z [builder-164] claimed → built: Filled the seeded entity_link FACT shape: linkActions returns the F12 picker's link_entity (chosen row -> value.ref, no value) and settle (null value); linkableFact is the one 'is there anything to link' predicate; linkNotice renders WHY the picker is withheld. Outside touch_scope, stated plainly: actions.ts gains ActionSpec.chooses (the window, as data) beside supplies + decisionValue routes a chosen row into ref and a typed one into value + closeRefusal/refusalWords gain ref_required; form.tsx gains ChosenControl rendering the SHARED PickerPanel (entity-picker.tsx untouched — BUG-0097 owns it); slot.tsx points NOTICE_BY_SHAPE.entity_link_fact at linkNotice; lib/db/records.ts gains readLinkChoices (fact->table via registryField, then the same read the record picker makes) and the queues page calls THAT rather than editConfigFor, so tests/offline/edit/config.test.ts's five-reader guard is respected not widened; tests/fixtures/rows.ts gains venueWindow() because two suites now need one window. Three pre-existing tables that encoded the empty stub were amended, not weakened: close-slot.test.ts OFFERED entity_link_fact []->['settle'], its notice test now passes a linkable input per shape plus a non-vacuous counter-case; conflict-actions.test.ts likewise; page.test.ts stuck []->['settle'] and its dead zero-button branch became a live 'nothing rests disabled' assertion. TRAP FOR QA: the picker is NOT offered on the shipping reviewItemEntityLink fixture — its entity_id is null, and settle_review_item raises on a value-carrying decision missing entity_id, so an item opened before its canonical row exists gets settle alone plus a rendered reason. That is deliberate (same branch conflict-actions takes) and staging has no fact-shaped row at all, so this is graded on fixtures: built, not walkable on this data. Verified on the rebased tree (50ffa81 on 6bd99a1): npm run lint + tsc --noEmit clean; vitest --project=offline tests/offline/review-item/link-actions.test.ts 30/30; npm test 2659+6 pass; npm run test:live -- tests/live/review-item.live.test.ts 12/12 against staging read-only (the close's not_provisioned path re-proved there). Red-then-green observed twice: neutering the link control reddens 18/30, and routing the chosen row into the value slot instead of ref reddens 3/30.
- 2026-09-09T05:52:26Z [dispatcher] landed ticket/TASK-0056 onto run/admin-window as 6e6a273
- 2026-09-09T05:52:53Z [qa] built → qa: under attack
- 2026-09-09T06:05:46Z [qa] qa → done: Attacked on the LANDED, integrated tree (detached _receipt worktree at run/admin-window 6a93776; node_modules and .env symlinked, .env never opened). Stored checks re-run VERBATIM before reading the builder's story: lint+tsc exit 0; link-actions.test.ts 30/30; review-item.live.test.ts 12/12 against staging read-only. Receipt GREEN 3/3 on the tree including my pin. Whole gauntlet from my worktree: npm test (offline+isolated) exit 0, test:http 17 passed exit 0, test:live exit 0.

CRITERIA. (1) Exactly two controls on a complete-fact fixture with a window, and $("button") length 2 with no form/input/select at rest; the create affordance is excluded structurally (every button is one of the two actions or one of the window's own rows), not by word search. (2) decisionValue(link, venueId) = {domain events, entity_id event, field venue, observation_id null, value null, ref venueId} — one filled slot, and decisionRefusals returns [] for it and for settle's null value; misfiling the chosen row into value trips invariant 6 (reference_field_not_scalar) since events.venue is in REFERENCE_FIELDS. settle carries value null and is refused nothing. One POST to settlePath, one call to FN.settleReviewItem. (3) shapeOf turns on source_id alone; the signal fixture gets fixed/wont_fix and neither of these, the fact fixture the reverse. (4) verdicts absent/refused: neither control, StateOf names settle_review_item; re-proved live on staging (the close's not_provisioned path, "never settles anything by rendering the page"). (5) all green above.

ATTACK (one integrated pass over the batch's surface). Held: ref_required fires before any DB call on null/""/"   "/U+200B and NOT on a real row or on settle (closeRefusal, then decisionRefusals value_payload_missing behind it); a row the read did not return cannot be submitted (optionFor, asked again at the one call site that sends); an empty window still renders the picker and its own labelled emptiness; a refused venues read costs the picker only, names T.venues, and leaves settle standing; the note is optional on both actions and a blank one travels as null; no path offers creation. BROKE: BUG-0102 (P2, filed, pinned strict). ChosenControl hands PickerPanel a hardcoded status idle (form.tsx:285), so BUG-0097's busy rule (entity-picker.tsx:353 -> :283) never fires; open and disabled are independent state in CloseForm (form.tsx:403,405 — nothing clears `choosing` when a sibling control starts settling), so the picker stays LIVE for the whole of another settlement and a row clicked there hits settle's in-flight guard (form.tsx:334) which returns without setState — the choice is dropped silently, then the form is replaced by SettledNotice. Expected [] live options, Found all three. Observed RED as a plain it before pinning; landed as it.fails (admin-window/BUG-0102) in tests/offline/review-item/link-actions.test.ts, commit f10f237.

VERDICT: done, not a bounce. Every criterion is met and the ticket's goal — the two actions, their wire bodies, the shape distinction, the not-provisioned state — is met on the integrated tree; BUG-0102 is a second-order interaction between two controls in the shared close form (TASK-0049's frame as much as this ticket), P2 by user impact, and the campaign already handles this shape of finding that way (BUG-0097 from TASK-0055, same picker, that ticket closed done).

BUILDER DECISIONS, judged. (a) Gating the picker on a COMPLETE fact is correct, not a dodge: settle_review_item refuses a value-carrying decision with a null entity_id (M2 handoff, reference arm), so an offered link would be an action the function refuses; the absence is RENDERED with its cause and the complete-fact arm is graded on its own fixture. (b) readLinkChoices in lib/db/records.ts rather than widening editConfigFor's five-reader pin is the right side of that guard, and the page imports linkableFact instead of re-spelling it. Out-of-scope touches: all necessary (a client control cannot live in a server shape module; the payload router and ref_required belong on the shared spec), and the three pre-existing tables were STRENGTHENED, not weakened — I diffed them: close-slot gained a non-vacuous counter-case, page.test.ts's dead zero-button branch became a live "nothing rests disabled" assertion.

RESIDUAL (no ticket, lean cap): linkableChoices gates on linkableFact alone, not on the SHAPE (page.tsx:531), so a data_conflict item about events.venue with entity_id set also reads the venue window — a bounded but wasted round trip whose refusal conflictNotice then ignores. Trigger for a ticket: page latency on the conflict queue, or a shape that starts reading choices. [resolution: fixed] [receipt green: 3 cmd(s), tree 603fd8fd]
