# Digest 2026-09-15 (covers 2026-09-12 → 2026-09-15; the run's closing digest)

## Key items
- **Campaign ready for your verification.** M3 SHIPPED at tag `m3` (commit 672c6536, CI green, 76 tickets archived). The strategist's retro (M3.md, "Retro" section; your copy at `for-human/M3-retro-for-ben.md`) rules the vision **satisfied in build**: the verdict UI is built and walked, both handoffs are installed on staging (2026-09-11) and tracked in the scraper repo (`d1c1b3ba`, 2026-09-13). "Reviewed" is your word. Walk the app on `run/admin-window` (your instance: http://localhost:8771); file fixes as inbox notes (they run as a patch run), or tell me to finish and I merge it to main. No M4 was planned: it is costed both ways in ROADMAP.md and waits on your page-windows answer.
- **Run stopped on the vision-satisfied condition.** The run-end full-suite gate (`ci_check.py --force`) result is recorded under "Everything else". RUNNING is deleted at the end of this close; the watchdog will not relaunch.
- **Your items, in order:** (1) restore staging event `01a03c9b-1d28-707d-9873-f73ab3add10c` — two statements in `for-human/BUG-0215-staging-residue.md`; it is the only red live file (`residue.live`); (2) a LAUNCH.md security line: `requireAdmin()` grants on an alien host row set (needs a non-database host + a valid session; conditions and fix shape in M3.md §D); (3) page windows yes/no — `for-human/M3-paging-shape-for-ben.md`; blocks BUG-0221 only; (4) say "reviewed" on the §9 artifacts, or say what is wrong with them; (5) the §7 acceptance run is now DUE (you installed the function four days ago): TASK-0084/0085/0086 are filed and held `blocked` because a live proof consumes a real `review_items` row and appends a `verdicts` row this app cannot delete, on a staging project the entity-linking campaign is writing — they start on your word; (6) the sibling KS-code admission rule — `for-human/M3-sibling-code-admission-for-ben.md` (nothing blocked on it); (7) the eight product judgments in `for-human/M3-usersim-judgment.md` (two partly answered by your paging rulings); (8) `.env.example` still modified in your tree.
- **Curator filed three machinery proposals** — the ticket gate is blind to every Bash write (0 fires in 292, URGENT: `2026-09-15-urgent-ticket-gate-is-blind-to-bash-writes.md`), the check birth gate should refuse the four defect shapes a detached-worktree receipt cannot grade (`2026-09-15-birth-gate-refuses-checks-the-receipt-cannot-grade.md`), and `ci_check.py` is called by no scheduler so the run-branch tip goes unmeasured (`2026-09-15-nothing-measures-the-run-branch-tip.md`) — superseding two earlier proposals (receipt-evidence-paths, qa-pins-ungated) and leaving three standing. Nine files in `agenticflow/tracker/proposals/`; install between runs.
- **What changed in the product since the 09-11 digest (all on the run branch, all QA-closed):** paging cannot draw a duplicate or drop a row when the first screen re-renders under kept client state (BUG-0216/0222), and the paged pages hand the client data, not a function (BUG-0226 — the P1 that took both pages down for one lane); every host answer is graded as a PostgREST answer (ARCHITECTURE §4.1 bar; BUG-0224/0227/0228/0229/0230/0234): missing, foreign, alien-row, miscounted and signed-zero answers refuse naming the object instead of an empty card, a developer sentence or a 500; settlement readiness conjoins the `verdicts` table and the `settle_review_item` function by reading PostgREST's schema description, never by calling (BUG-0223); the live suites read which settlement world they are in and never apply an override (BUG-0215), and exclude an entity only when their own keyed read proves it vanished (BUG-0219); the paging control is withdrawn on a not-provisioned answer (BUG-0211); `authored` is required on the error arm so `tsc` is the standing guard (DEBT-0021); the sibling-reading KS-code guard compares declared code→meaning pairs (BUG-0213/0220/0225) and lives in an opt-in `handoff` vitest project outside `npm test` (TASK-0080), so your scraper edits can no longer redden Admin's default suite (verified under the scraper's own KS033 allocation).
- **Your two LOOK_AND_FEEL rulings are installed** (TASK-0079/BUG-0217/BUG-0218): Feel bar 14 (nothing the operator must find sits below a long list — both paged surfaces fail it by position today) and the self-retiring paging clause on bar 11.
- **Contract movements (architect, ARCHITECTURE/DECISIONS/LESSONS):** §4.1 "What a PostgREST ANSWER is" (row 28); §11 count-read vocabulary — a comment names the symbol the code calls, never an install state (row 29); §9.2 ratified (a function's existence is a READ of the schema description); §10 input ownership — sibling-reading checks are opt-in; EC1 amended (live tier graded on the settled tree at the close + targeted `test:live` on new in-scope tickets); LESSONS rewritten whole at 15 lines.

### Still waiting (unchanged)
- `.env.example` modified in the working tree — yours (since 2026-09-04).
- Stale `for-human/` notes TASK-0031/0034/0043/0044 — all resolved; delete at will.

## Verify it yourself
- [ ] `git tag -l m3` → present; `python3 agenticflow/scripts/ci_check.py` → green (or "skipped: app tree unchanged")
- [ ] `http://localhost:8771/claims` → 200 (was 500 for one lane on 2026-09-14); press "more" repeatedly → 877 distinct claims, no console error, the overlap sentence appears at most once
- [ ] `http://localhost:8771/browse` → 200; two presses → 120 events
- [ ] a record page for a resolver-owned event → the override control is offered (both §9 objects installed) — `/records/events/01a06573-e943-7f4e-8db9-db7a5742560e`
- [ ] `npm run test:handoff` → green (12 tests) with the scraper checkout present; skipped, never vacuously green, without it
- [ ] `npm run test:live` → exactly one red file, `residue.live` naming `events.title: 1 row(s)`, until you restore the row; then green
- [ ] `docs/build_judgments.md` → 8 entries, names itself the M3 edition, §9 artifacts stated INSTALLED with instants

## Everything else
- attention UI: http://127.0.0.1:8770/ (your walk instance http://localhost:8771 runs `next dev` on the primary checkout at HEAD)
- run-end gate: see the line appended below by the close
- rail telemetry since the 09-11 digest: 0 usage throttles, 0 breaker trips, 0 compactions, 0 stalled claims; 2 background commands killed by macOS memory pressure (system-wide: Chrome/VS Code, not factory processes); 1 bounce (BUG-0222 → BUG-0226, the client-boundary P1), 1 reopen chain closed already_done; 11 artifact_gate fires (agents reaching for run.yaml/DIGEST), 5 evidence_budget fires (notes over 4,000 chars, counted not refused)
- spawn economy since the 09-11 digest (spawn_log out-tokens): builder 22 returns / 591k; qa-adversary 24 / 521k; architect 2 / 86k; strategist 2 / 59k; curator 1 / 27k; designer 1 / 10k. Two lanes per ticket is the whole bill; the retro names the four one-property-at-a-time chains (42 of 72 M3 bugs) as the cost driver and promoting classes at N=2 as the fix.
- counts since the 09-11 digest: ~35 tickets opened (BUG-0212..0236, TASK-0079..0086, DEBT-0021/0022), ~33 closed done (25 via QA lanes incl. 2 already_done twins; the rest docs/architect-routed); open now: BUG-0221 (blocked on you), BUG-0235, BUG-0236, TASK-0082, TASK-0083 (patch lane, small), TASK-0084/0085/0086 (blocked on you); BUG-0234 — see the close line below
- staging facts: `verdicts` holds exactly 2 rows (the pre-BUG-0215 test's, 2026-09-11T23:26/23:27Z); the entity-linking campaign keeps writing staging (71 of 72 review items are its; a row vanished mid-run once, BUG-0219); production untouched throughout
- residue in the primary checkout at close: none expected after the last QA lane; the curator noticed two `tests/offline/db/zz-qa-attack-probe*.test.ts` probes mid-lane — the close line below records whether they were cleaned
- incidents worth the curator's eye are in its three proposals; the full dispatcher incident log for the run is folded into M3.md's ledger sections

### Close line (dispatcher, 2026-09-15 08:2xZ)
- run-end gate `python3 agenticflow/scripts/ci_check.py --force` → **green** (full suite on the run-branch tip, after BUG-0234 landed)
- BUG-0234 closed done by QA on a green receipt (all three admission legs verified on loopback and a production build; nothing filed); its two probe files were removed by the lane — the primary checkout carries no residue
- open at close: BUG-0235, BUG-0236, TASK-0082, TASK-0083 (patch lane, small; run as a patch run when you file the next inbox note or say "continue"), BUG-0221 + TASK-0084/0085/0086 (blocked on your answers)
- markers: CLOSING then RUNNING deleted; the watchdog will not relaunch. To resume: `/ship` (patch lane) or `/ship finish` to merge `run/admin-window` to main after your verification
