# Digest 2026-09-04 — M1 shipped, M2 defined, awaiting roadmap sign-off

## Key items
- **Where the app stands vs the vision:** the read slice and the safe edit surface are SHIPPED (tag `m1`, 26cec8d): six pages, six gauges, review item + evidence, Browse, groups/idols editable through one config map, every number parity-checked against staging. INTENTIONALLY ABSENT until M2: every verdict action, settle, the verdicts log, the events/venues override path, the reference picker, both handoff migrations. Dials are absent by Ben's ruling and stay absent through the campaign.
- **M1 closed clean:** 129 tickets (124 M1, 5 patch), all done; every endgame walk bug fixed and QA-closed; foreground CI green; run branch and tag pushed to origin.
- **Endgame verdicts:** designer walk 11 bugs, two user-sims 4 bugs + retro notes, verifier 12/13 criteria (the 13th, live parity, went green once the oracle class was fixed), architect root-cause pass → LESSONS.md (6 classes) + ARCHITECTURE §13 (page tickets must carry their live suite).
- **M2 defined by the strategist** (2527c1c): FEAT-0009..0013 — the verdict slice, the campaign's LAST milestone; patch lane TASK-0037..0039. Sign-off block at `tracker/for-human/M2-roadmap.md`. **Nothing builds until Ben answers the fork question there.**

## Waiting on Ben (≤15)
1. Roadmap sign-off: "this is the build order — does it match what you want to see working first?"
2. Fork: groups/idols have no door (nothing / Browse entry point / name lookup).
3. Small question: show the group row's own `source_*` provenance columns on the record page (one patch ticket) or keep the 2026-09-02 wording.
4. Paste `walk_sandbox` DDL into staging (`for-human/TASK-0034.md`) — retires the interim groups/idols walk-write exception.
5. Two run.yaml knobs proposed: `qa_batch_patience 0→2`, `compact_threshold_bytes 16000→20000`.
6. `.env.example` edit is uncommitted in the working tree (Ben's); say if it should land on the run branch.
7. `M1-contract-gaps.md`: `groups.updated_at` never moves on an Admin edit (trigger = scraper schema); local `AUTH_SECRET` is the `.env.example` placeholder — rotate.
8. Stale panel note `for-human/TASK-0031.md` is Ben's to delete (ticket done).

## Factory discussion points (for the curator)
- `builders: 1` for the whole M1 burn-down: ~25 tickets serialised; the cap question was asked three times and never answered — a run.yaml value that should surface in the attention UI.
- Two lanes found their detached worktree deleted mid-run by another lane's cleanup under `agenticflow/.worktrees/`; lanes now use the gitignored evidence dir. Cleanup should leave non-ticket worktrees alone.
- Port probing: `/dev/tcp` under zsh reports every port free; a stranger's IPv4 listener on 8790 answered `bad token` to a lane whose Next bound IPv6. Rule adopted: real bind on 127.0.0.1, ::1 and 0.0.0.0.
- `evidence_clean.py` resolves paths against the primary checkout, so it is a no-op from inside a worktree.
- Next's production JSX transform drops a last-child inter-element space that vitest keeps (BUG-0045/0059): offline markup tests cannot see it; a repo-wide source rule now guards it.
- One QA lane grepped `.env` for names (self-reported; a project ref, no secret, reached the transcript). Two QA evidence notes overran `qa_evidence_max_chars`.
- The architect's first spawn for the walk-session ticket died to an API 529 before writing; respawned fresh.
- Staging's newest adapter run is a `test_harness_ticketmaster` row from the scraper campaign's own harness, now leading /cycles for everyone.
