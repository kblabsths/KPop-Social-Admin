# Digest 2026-09-09 — the vision amendment, routed

## Key items
- **Ben's 2026-09-08 strike is routed as removal, not as new work.** "groups/idols edit directly within it" is gone from VISION, so the direct catalog write path comes out: **TASK-0040** (patch, P1) removes `groups`/`idols` from `EDIT_CONFIG` — after it no catalog table is writable from Admin at all until FEAT-0011's override path lands — and **TASK-0041** (patch, P1) withdraws the interim groups/idols walk-write exception from the walk docs. Both stamped `discovered_from: inbox:2026-09-09-vision-amendment.md`. **Nothing was licensed by the strike**: no new surface, no compensating feature, no milestone. The "do not re-implement direct edits" rule is the architect's to state (DECISIONS + ARCHITECTURE, 2026-09-08); my tickets cite it and do not duplicate it.
- **TASK-0040 is sequenced behind that architect ruling and there is no ticket ID to depend on** — what `Regime` means once the walk sandbox is the only `pre_cutover` entry is his call, and it was not yet in the repo when this was filed. The ticket says in its own body: do not start until the ruling is in `DECISIONS.md`. A builder claiming it without checking is the failure mode to watch.
- **Reconciled, not silently dropped:** SPEC F8 keeps its shipped text and gains an amendment note (it shipped; it is retired); `milestones/M1.md` gains the same note (EC10's pass stays true — the record changed, not the grading); ROADMAP records both of Ben's answers; `for-human/M2-roadmap.md` no longer asks a question he has answered. SPEC **named gap 8** ("the human edit with no fingerprint") is **closed by removal** — the un-attributed write path is being deleted rather than instrumented, which is the one fix this repo could never make (a trigger is schema).
- **Two open tickets amended, none cut.** FEAT-0011 keeps the override half for resolver-owned tables (its `vision_trace` quoted the struck sentence and now quotes what survives; a new criterion bars it from restoring any direct write path). FEAT-0012 loses its display half — linking `idols.group_id` to a group record, "what ends the idol-group islands" — because neither table has a record surface left; its picker half survives and still traces to VISION. No ticket was purely struck scope, so nothing was `wont_fix`'d.
- **TASK-0037 (paste the walk sandbox, then prove it) gained real urgency.** With the interim exception withdrawn, `walk_sandbox` is the walkers' ONLY write surface; until Ben pastes it, a save-path walk has no write surface and states that in its report as a narrowed walk. It is still blocked on Ben and still P2 — **a strategist cannot set priority; the architect should look at P1.**
- **TASK-0038 carried a checksum that would have failed every close:** its stored check pinned VISION.md's pre-amendment hash. Re-pinned to the amended file. Any other stored check pinning a frozen doc's hash has the same failure mode after an amendment.

## Waiting on Ben (≤15)
1. **Paste `walk_sandbox`** into staging (`for-human/TASK-0034.md`) — now the only write surface a walk can have.
2. `.env.example` is still modified in the working tree (yours); say if it should land on the run branch. Untouched here.
3. FEAT-0011 still needs your `events`/`venues` editable-column list before it builds (SPEC named gap 7) — unchanged by the amendment.

## Factory discussion points (for the curator)
- **An editor's YAML formatter reflowed `agenticflow/run.yaml` at 18:03 today and broke the parser.** Prettier-style output (`key:\n  value # comment`) is not the flat `key: value` the header requires; `lib_ticket.load_run_config()` then read `builders`, `gated`, `product_dir`, `ci_command` and `ci_scope` as empty — CI gate silently disabled — and crashed outright on the int cast, taking `ticket.py new` with it. It was repaired within a couple of minutes (values kept: `builders: 4`, `qa_batch_patience: 2`, `compact_threshold_bytes: 20000`), so nothing here worked around it. Two asks: put `agenticflow/run.yaml` in `.prettierignore`, and make `load_run_config()` refuse loudly on an unparseable int rather than tracebacking through whatever command happened to call it.
- `ticket.py set-priority` correctly refused the strategist ("only the architect or human rules a priority"). Right rule; it means priority consequences of an amendment have to travel as a ticket comment and a digest line, which is slower than the amendment itself. Worth a curator look at whether the strategist should be able to re-rank at a boundary.
