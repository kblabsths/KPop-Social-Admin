
## Raw history compacted 2026-09-02T11:45:28Z
- 2026-09-02T05:38:10Z [architect] opened: Gauge cards: the figure card, the trend table and the distribution view
- 2026-09-02T05:39:07Z [architect] checks amended: aligned the layering check with TASK-0004's spelling so it catches every import form of @/lib/db, not just one [was: npm run lint, ./node_modules/.bin/tsc --noEmit, npm run build, ./node_modules/.bin/vitest run tests/offline/gauges-ui, b… (260 chars; full text in git history)]
- 2026-09-02T07:54:57Z [architect] checks amended: false-RED, measured: 'git diff --quiet $(git merge-base HEAD main)..HEAD -- package.json' exits 1 on the run branch today, because TASK-0001 legitimately added vitest and vite-tsconfig-paths to package.json — the merge-base is main, so the diff reports the whole branch's work, not this ticket's. No builder could turn it green. Intent kept ('no charting or UI dependency'), now pinned as a named-library absence in package.json (dry-run: exit 0). Any dependency at all is already gated by the supply-chain hook plus a toolsmith DEP ticket against ALLOWED_DEPS, which is the real enforcement. Other checks verbatim. [was: npm run lint, ./node_modules/.bin/tsc --noEmit, npm run build, ./node_modules/.bin/vitest run tests/offline/gauges-ui, b… (231 chars; full text in git history)]
- 2026-09-02T07:55:05Z [architect] criteria amended: criteria follow the check amendment: the sentence 'package.json dependencies are unchanged' asserted a branch-wide diff this ticket does not control (TASK-0001 already changed package.json on the run branch). The bar is now what this ticket can prove — no chart library, and no DEP ticket filed. [was: Three components exist, are pure and synchronous, import nothing from `src/lib/db`, and render the four states through t… (525 chars; full text in git history)]
- 2026-09-02T07:56:06Z [architect] Test-idiom ruling (DECISIONS.md 2026-09-02): the offline include glob stays `tests/offline/**/*.test.ts` — no `.tsx`, no JSX, no jsdom. Component tests build elements with `createElement` and assert emitted markup. Import the landed shared helper `tests/offline/ui/markup.ts` (`h` = createElement, `render` = renderToStaticMarkup, `classesOf`, `tagsOf`, `textOf`) instead of writing your own render helper; see tests/offline/ui/primitives.test.ts for the pattern. This adds no work and no scope — it names the helper you reuse.
- 2026-09-02T08:47:25Z [dispatcher] Relayed from QA on TASK-0007 (residual): src/lib/gauges/index.ts re-exports PENDING_CLAIM_BUCKETS, which contains in_window. Never surface that constant as filter options or labels — in_window renders nowhere in the UI (spec §4; FEAT-0005 check pins it under src/app and src/components).
- 2026-09-02T09:10:08Z [dispatcher] Relayed from QA on BUG-0009 (residual): resolveBounds floors a fractional limit — limit: 0.5 passes the > 0 guard, floors to 0, and the query goes out as .limit(0) (empty window, blank card). Any page mapping a query-string limit through Number() needs an integer guard before it reaches a gauge.
- 2026-09-02T11:19:24Z [builder-31] claimed (attempt 1)
- 2026-09-02T11:33:29Z [builder-31] claimed → built: - src/components/gauges/{gauge-card,trend-table,distribution,state}.tsx + index.ts: three pure sync components, plain props, no lib/db import, all four states through the ui primitives (state.tsx holds the one rule: Empty/NotProvisioned are cards and REPLACE the surface; Loading/ErrorLine are lines and render inside, header intact). Distribution's "chart" is DataTable + a CSS bar of two token fills; the only inline style in any of them is the bar's data-driven width %, pinned by a test.
- Verified on the rebased tree: npm run lint (0), tsc --noEmit (0), npm run build (0), vitest run tests/offline/gauges-ui (21 passed), both greps (no @/lib/db in src/components, no chart library in package.json). Full npm test 779 passed. No dependency added; no DEP filed. Tests render REAL aggregate output (aggregateCycleHealth, aggregateQueueHealth) and were seen red first (3 failures on wrong fixture arithmetic + an HTML entity) before green.
- Two additive edits BEYOND touch_scope, deliberate: (1) StatCard gained `floor` — renders "at least" in sans-secondary beside the mono figure so window.truncated is visible without a second card anatomy in a second file; (2) lib/format gained `duration(seconds)` on relativeAge's unit ladder — every gauge distribution is seconds and the app has exactly one formatting module. Tests for both went into the files that own those surfaces (primitives.test.ts, format.test.ts), not into mine.
- QA traps worth knowing: GaugeCard's props are a 3-arm union — a value that can be null will not COMPILE without `absent`, which is how "em dash with its reason, never 0" is enforced; a floor/tone on an absent figure is suppressed on purpose. TrendTable/Distribution force rows=[] whenever `state` is set (stale rows under a loading line would be a lie). Nothing here surfaces PENDING_CLAIM_BUCKETS or in_window — labels come only from props. The integer-limit residual (BUG-0009) is a page concern; no query-string parsing exists in these components.
- 2026-09-02T11:33:47Z [dispatcher] landed ticket/TASK-0008 onto run/admin-window as cbafd26
- 2026-09-02T11:34:29Z [qa] built → qa: under attack
- 2026-09-02T11:40:00Z [qa] qa → reopened: BUG-0018 filed. Attacked on the landed tree (cbafd26), all six stored checks re-run verbatim and green: lint 0, tsc --noEmit 0, build 0 (after rm -rf .next/types), vitest tests/offline/gauges-ui 21 passed, no @/lib/db under src/components, no chart-library name in package.json. Criteria graded individually: three components exist, are sync and hook-free; the only lib/db reach is `import type { Spread }` via lib/gauges (type-only, erased); all four states go through the ui primitives (state.tsx), Empty/NotProvisioned replacing the surface and Loading/ErrorLine inside it with the header intact; the not-provisioned card names the missing object verbatim; no DEP filed and package.json untouched by cecaab4.

Attack (one pass, real aggregate output, not hand shapes): all-null Spread from spread([],4) -> six dash rows, zero bars, no digit outside the percentile labels; QueueHealth with settlesMeasurable:false -> settled column all dashes, and the one `0` in the table is an honest opened:0 week, not a fabricated settle; truncated window (limit 1000, truncated true, items 1000) -> "at least" beside a mono type-figure 1,000; negative duration -30s renders negative with no bar, 0 draws no bar, 120 does; a Distribution driven from a bucket list cannot leak in_window (labels come only from props; no gauge component mentions PENDING_CLAIM_BUCKETS or in_window).

What broke: GaugeCard decides absence with its own `value === null` (gauge-card.tsx:93-105) rather than `isAbsent` (lib/format.ts, the single definition BUG-0004 exists to enforce). StatCard dashes anything isAbsent accepts, so the two paths disagree. Expected (the component's own docblock and its sibling test 'never qualifies or colours a figure it does not have'): no floor qualifier and no palette colour on a dash. Found, with value = duration(aggregateCycleHealth(unfinished).duration.p50) i.e. the em-dash STRING this ticket's own new helper returns for a null percentile: <span class=type-body>at least</span> + <span class="type-figure text-broken">—</span>, and no reason line, because the third union arm is unreachable through a non-null string. Same markup for NaN and Infinity. The three-arm union is therefore structural only against a literal null, not against the app's formatters. Latent today (grep GaugeCard src/app -> no match), user-visible at the first gauge page.

Pins committed 2b724d5 in tests/offline/gauges-ui/cards.test.ts, both watched red as plain it() first, now it.fails (strict xfail, BUG-0018); full offline suite 781 passed on the branch.

Residuals, not tickets: (1) a surface state on GaugeCard replaces the whole card, so the gauge's own micro label disappears in the empty/not_provisioned case — an operator sees which table is missing but not which knob; trigger for a ticket would be the six-card dashboard grid landing with two unprovisioned gauges. (2) TrendTable/Distribution with rows:[] and no state render a headers-only table with an empty tbody and no empty state — reachable when a page hands AwaitingRowTrend.series:[] without mapping it to a state; the four-states duty currently sits entirely on the caller.
