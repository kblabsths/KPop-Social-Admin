
## Raw history compacted 2026-09-02T12:40:39Z
- 2026-09-02T05:40:05Z [architect] opened: Review-item detail — three typed views over one anatomy, evidence resolved
- 2026-09-02T07:56:06Z [architect] Test-idiom ruling (DECISIONS.md 2026-09-02): the offline include glob stays `tests/offline/**/*.test.ts` — no `.tsx`, no JSX, no jsdom. Component tests build elements with `createElement` and assert emitted markup. Import the landed shared helper `tests/offline/ui/markup.ts` (`h` = createElement, `render` = renderToStaticMarkup, `classesOf`, `tagsOf`, `textOf`) instead of writing your own render helper; see tests/offline/ui/primitives.test.ts for the pattern. This adds no work and no scope — it names the helper you reuse.
- 2026-09-02T08:23:53Z [architect] Architect ruling, 2026-09-02 — read kinds, now ARCHITECTURE.md section 4.3. Cited on every ticket that owns a `src/lib/db/*.ts` module.

QA found `src/lib/db/review-items.ts` (TASK-0006) building its list query with no `.range()`, no `.limit()` and no `.order()`. Against PostgREST's `db-max-rows` (Supabase default 1000) that truncates silently, in unspecified order — so a count or an "exactly the matching items" claim built on it would be wrong rather than refused. You are one of eight builders about to write a `lib/db` module without seeing each other's code; the bound is decided once, in TASK-0026 (now a dependency), not eight times.

Two read kinds. Pick deliberately; do not blur them.

1. **Complete read — `readComplete(missing, run, db?)`, new in `src/lib/db/result.ts`.** Use it whenever the surface presents the result as the whole set: a list the page shows in full, a count, an oldest age, an "exactly the matching items" claim. The `run` callback passes `{ count: "exact" }`, a total `.order()` ending in the primary key, and `.range(0, cap - 1)` with the cap handed in. If the exact count exceeds the rows returned, it returns `kind: "error"` naming the object, the count and the cap. **An `ok` array from a complete read is the whole matching set** — that property is what every figure in this app rests on, and it is why no caller needs a "was this all of it?" flag.
2. **Window read — `readRows`, unchanged.** The caller's own `.order()` + `.limit()` define a named window, and the surface says which window it is showing. This is section 8's gauge contract and TASK-0007's landed gauges use it correctly. A window read's rows must never become a figure presented as a total.

Also: no read helper may substitute a number the database did not give. `readCount` returned `count ?? 0` — BUG-0007's user-visible twin — and TASK-0026 makes it refuse a null count instead. Do not reintroduce a `?? 0` on any read path.
- 2026-09-02T08:24:13Z [architect] Architect ruling, 2026-09-02 — pure domain leaves sit BELOW `lib/db`, not above it (ARCHITECTURE.md section 4, arrow diagram + new rule 7).

QA flagged on TASK-0006 that `src/lib/db/review-items.ts` imports `src/lib/review/shapes.ts` — the reverse of the arrow the contract drew. The code was right and the diagram was wrong: `shapes.ts` has zero imports, no cycle is constructible, and TASK-0006's own two-module contract required the split. The diagram is amended rather than the code.

The rule you are working under, because your ticket owns both a domain module and a `lib/db` module:

- A **pure leaf** — today `src/lib/review/**`, `src/lib/format.ts`, `src/lib/edit/config.ts`, and `src/lib/browse/**` when it lands — imports NOTHING that can reach a database: not `lib/db/**`, not `@supabase/supabase-js`, not `process.env`. It holds the vocabulary, the row interfaces it reasons about, and pure functions over rows.
- `lib/db/**` imports the leaf. **The leaf never imports `lib/db/**` back — not a value, and not a type.** A type-only import would erase at runtime, but it still writes a directory-level cycle into the contract, and the day someone changes it to a value import the cycle is real with nothing to catch it. If both sides need a row type, **declare it in the leaf** — that is what `ReviewItemRow` in `shapes.ts` already does, correctly.
- `lib/gauges/**` is NOT a leaf and is unaffected: a gauge fetches its own bounded window through `lib/db`, which is the arrow as drawn (section 8, TASK-0007 as landed).
- 2026-09-02T08:26:20Z [architect] Architect note, 2026-09-02 — `shapeOf`'s default, since this ticket renders three typed views keyed on shape.

QA observed on TASK-0006 that `shapeOf` in `src/lib/review/shapes.ts` branches on `queue === "entity_link"` and falls through to `data_conflict_fact` for everything else — so a future queue value (a `freshness` queue, say) would render as a **decision**, silently and plausibly. Ruled: leave it. The branch is unreachable today (migration `20260901000002` constrains `review_items.queue` to exactly `data_conflict` and `entity_link`), and a total function is what keeps an exception path out of every caller — including yours. Recorded in ARCHITECTURE.md section 6 trap 11 and DECISIONS.md, with the trigger named: the migration that widens that CHECK constraint.

For you, three things follow. **Do not add a defensive throw, a null return or an `unknown` shape** to your view selection — the three views are total over `Shape` and the compiler is your exhaustiveness check; write the switch so a fourth `Shape` would fail to compile here. **Do not re-derive shape or kind** from `queue`, `source_id` or a summary string: `shapeOf`/`kindOfItem` are the only spellings (section 11). And do not file a ticket or a test for the unreachable queue value — inventing a `queue` the database cannot hold to exercise a branch it cannot reach is work with no user behind it.
- 2026-09-02T12:35:39Z [architect] Architect ruling, 2026-09-02 — the four data-surface states are now a TYPED contract (TASK-0030, ARCHITECTURE section 7, DECISIONS same date). Code against these signatures; do not work around them.

1. **`reading` is REQUIRED on `ErrorLine` and on `GaugeState`'s error arm.** QA's residual on BUG-0016 was right: an optional prop is a rule TypeScript cannot enforce, and the omission it permits is a red line that names none of the page's reads. `DbResult`'s error arm carries `reading: string` already (`src/lib/db/result.ts:14`), so pass `result.reading` straight through on every error arm you render. Do NOT invent a string, do not pass `""` to satisfy the compiler, and do not trim the message: a transport failure's account runs ~900 chars including stack frames, and whether that is too long is the designer's call at the walk, not yours.

2. **A state card that replaces a labelled surface keeps its eyebrow.** `Empty` and `NotProvisioned` take an optional `micro` eyebrow; the three gauge components always pass their own `label`, so an unprovisioned gauge still says which knob it is and not just which table is missing. On your page's own `Empty`/`NotProvisioned` the eyebrow is optional — the `Section` heading above it already names the surface — so only pass it where there is no heading.

3. **A rows surface has no headers-only rendering.** `TrendTable` and `Distribution` take a REQUIRED `empty: { holds, filledBy }` and render it themselves when there are no rows and no other state. The ruling on who owns the empty state: **the component owns WHEN, you own THE WORDS.** Write them in the app's voice, naming what the series holds and the one thing that fills it (LOOK_AND_FEEL Voice bar 4) — never "No data". An explicit `state` still wins, so pass your own empty state where the emptiness has a REASON (a filter that matched nothing) rather than being simply no rows yet.

For you specifically: this page mounts no gauge, so only rule 1 touches your code — but you render several reads (the item, its evidence, the canonical value and its provenance) and each error arm must name which one refused. That is exactly the case `reading` exists for.
- 2026-09-02T12:36:14Z [architect] Architect ruling, 2026-09-02 — your route is the milestone's second dynamic segment, so BUG-0017's measurement is yours before you write it (ARCHITECTURE section 5, new rule; DECISIONS same date).

On Next 16.2.2 a `notFound()` thrown from a dynamic segment cannot both set 404 and server-render the framed page: the status is set in the same `catch` that emits `<html id="__next_error__">` (`node_modules/next/dist/esm/server/app-render/app-render.js:1894-1918`), a `not-found.tsx` beside the page changes nothing (measured: still the error shell, len 8820), and rendering the not-found component inline gives the whole document at status 200. `/records/<unmapped-table>/<id>` solves it with a `beforeFiles` rewrite in `next.config.ts` — but **that rewrite is `/records`-specific and you do not inherit it.**

The ruling for `/queues/[reviewItemId]`: **resolve every URL your segment matches — do not call `notFound()` and do not add a second rewrite.** A record's table segment is an enum this app owns, which is why routing its miss was right; a review-item id is DATA, and an id that resolves to no row is one of your four states, not a routing outcome. So a read that comes back with no row renders the page's own surface, at 200, naming the id verbatim in mono and saying it is not in `review_items` — the same voice as your evidence-id-that-resolves-to-no-row case, which your criteria already require. Everything else on the page (the links out, the three shape views) is unaffected.

If you find yourself wanting `notFound()`, that is the moment to stop and say so on this ticket instead — it is an architecture question, not a page decision.
