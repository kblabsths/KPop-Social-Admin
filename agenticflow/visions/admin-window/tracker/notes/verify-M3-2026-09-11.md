# Verifier run log — M3, campaign `admin-window`, 2026-09-11

Tree: `run/admin-window` at `bed26500`. Repo `/Users/ben-m4/Desktop/Coding/KPOP/kspace Admin`.
Read first, and only: `agenticflow/docs/vision/SPEC.md` (F14–F16 plus the cross-cutting
section and F5/F7 they extend), `agenticflow/tracker/milestones/M3.md`, `README.md`,
`agenticflow/docs/STACK.md` §5. Tickets were read only at grading time, and only for the
two criteria that grade ticket metadata (EC1's second bullet, EC10's third).

Evidence: `agenticflow/tracker/evidence/M3/verifier/` (gitignored). Every number below is in
this file in text, because the directory is not.

## How the app was launched, from the docs alone

README §"Running it" points at STACK.md §5, which carries the launch line verbatim. It
worked first time, with no guesswork:

    npm run build                                   # exit 0
    ( set -a && source .env && set +a && \
      SUPABASE_URL="$STAGING_SUPABASE_URL" SUPABASE_SERVICE_ROLE_KEY="$STAGING_SUPABASE_SERVICE_ROLE_KEY" \
      AUTH_URL="http://localhost:8775" npm run start -- --port 8775 )
    node tests/walk/session-cookie.mts               # one line of JSON, no value on either stream
    node tests/walk/reset-sandbox.mts (same prefix)  # "deleted 3, seeded 3, read back and verified"

Port 8775 bind-probed free before use (8770/8771/8772/8790 left alone; 8770, 8771 and 8790
were confirmed still listening on their original PIDs after my instances were killed).
Two instances of my own: 8775 (staging) and 8776 (pointed at a local PGRST205 stub on
8777). All three were started by me and all three were killed by me.

## What was walked

1. **Six pages, authenticated, production build against staging.** Dashboard, Queues, a
   review item, Claims, Sources, Cycles & runs, Browse, a record page.
2. **Paging on /claims to exhaustion**, in Chromium 1440x900, counting presses, requests
   and distinct row ids.
3. **Paging on /browse to exhaustion**, same instrumentation.
4. **Five refusal arms** on the paging control (a real 400 from the handler, a forged
   bound, a `not_provisioned` answer, an `error` answer, a dead transport).
5. **The whole app against a database answering PGRST205 to every read** — a 46-line
   python stub on 127.0.0.1:8777, a second app instance on 8776 pointed at it.
6. **The save path**: the `walk_sandbox` record page — edit, save, reload, restart the
   server, read it back; then the required-column refusal.
7. **The gate**: every page and both route handlers, unauthenticated, on seven methods.
8. **Direct REST counts against staging**, as an oracle independent of the app.

## Measurements

### Paging, /claims (877 claims)

First screen: 50 rows, 50 distinct ids, **zero** requests to the route handler before any
press. Window line, verbatim:

> Oldest first — the longest-waiting claim at the top; a claim whose instant is unknown
> sorts last. A window of at most 50 rows, not the whole view. 877 claims in all; the 50
> longest-waiting are below — the rest are not shown.

`[data-window]` attributes: `data-window-held="877"`, `data-window-limit="50"`,
`data-window-truncated="true"`. Control label: **"Show the next 50 claims"**.

17 presses, **17** requests, exactly **one per press**, offsets 50, 100, 150 … 850, each an
explicit `?offset=` on `/api/admin/claims/rows`. Rows after each press: 100, 150, 200 …
850, 877. Final state: **877 rows, 877 distinct `data-claim` ids, zero duplicates**, which
equals the head figure the page states (877) and equals the database's own count
(`content-range: 0-0/877` on `pending_claims`, read straight over REST).

Final window line, verbatim:

> … 877 claims in all, and every one of them is below — the read found no more.

`data-window-truncated="false"`, `[data-paging="exhausted"]` reads **"All claims in this
view are shown."**, and the control is **gone** (`[data-paging="more"]` absent). Zero
console errors or warnings for the whole session. A reload returns to 50 rows and the URL
never changed — which is the 2026-09-10 amendment as written.

### Paging, /browse (120 events)

First screen 50 rows, `held="50"`, `truncated="true"`. 2 presses, 2 requests, rows 100 then
120. Final: **120 rows, 120 distinct**, equal to `event_listings`' own count
(`content-range: 0-0/120`). Final line: "The newest events by arrival, newest first. 120
events are on screen, and the read found no more."; exhausted sentence "All events in this
view are shown."; control absent.

### The five refusal arms on /claims (rows before → after)

| arm | rows | window line | what it said |
| --- | --- | --- | --- |
| real 400 (offset rewritten to 51) | 50 → 50 | unchanged | "the \`offset\` must be a multiple of 50, the window this surface pages by" + "Press it again to ask for the same rows." |
| forged 400 | 50 → 50 | unchanged | reason as sent, plus the retry sentence |
| `{"kind":"not_provisioned","missing":"pending_claims"}` | 50 → 50 | unchanged | "pending_claims isn't in this database yet — it arrives with the scraper repo's migrations." and **no** retry sentence |
| `{"kind":"error","reading":"pending_claims","message":…}` | 50 → 50 | unchanged | "pending_claims — permission denied for table pending_claims" + retry sentence |
| transport aborted | 50 → 50 | unchanged | "/api/admin/claims/rows — Failed to fetch" + retry sentence |

No arm half-filled the list; every arm named its object. The `not_provisioned` arm leaves
the control drawn and enabled (BUG-0211).

### The gate

Unauthenticated, no cookie: `/`, `/claims`, `/browse`, `/queues`, `/sources`, `/cycles`
and **both** route handlers answer **307** to `/login?callbackUrl=…`. Both handlers answer
307 on GET, POST, PUT, DELETE, PATCH, HEAD and OPTIONS — the redirect happens before any
handler body. Built client bundles (19 chunks under `.next/static`): **zero** files
containing `SERVICE_ROLE`, `service_role`, `STAGING_SUPABASE`, `supabase.co` or `eyJ`.

### Forged bounds, authenticated (all 400, reason and bound named, never clamped)

| sent | answer |
| --- | --- |
| (absent) | "the request named no \`offset\`, and a page must say how many rows it already holds" |
| `49` | "the \`offset\` must be at least 50: the first 50 rows are the screen the server already rendered" |
| `-1`, `abc`, `1e9` | "the \`offset\` must be a plain decimal number — no sign, no spaces, no leading zero, no decimal point and no exponent" |
| `100001`, `999999999` | "the \`offset\` must be a multiple of 50, the window this surface pages by" |
| `100050`, `150000` | "the \`offset\` must be at most 100000" |
| `850` | 200, `rows=27`, `exhausted=true` (850+27 = 877) |
| `100000` | 200, `rows=0`, `exhausted=true` |

### Warm server time, over HTTP, production build, staging

`/claims`: n=10, **min 1.117 s, median 1.235 s, max 1.392 s — zero samples over 1.4 s**
(a separate first-of-session sample read 1.658 s cold). `/queues` 0.104–0.130 s;
review item 0.674–0.885 s; `/browse` 0.332–0.423 s; `/` 0.087–0.097 s; `/sources`
0.201–0.224 s; `/cycles` 0.102–0.119 s. Against TASK-0062's recorded post-change numbers
(/claims 1.714 s, review item 1.301 s, /queues 0.107 s), nothing regressed and /claims is
now under the bar TASK-0074 was given.

### Against a PGRST205 database (stub on 8777, app on 8776)

All eight surfaces answer **200** and carry `data-state="not_provisioned"`. `/claims` and
`/browse` carry **zero** `data-paging` occurrences and zero "Show the next" — the
affordance is not drawn at all. The route handlers themselves answer **403 Forbidden**
(`requireAdmin()` reads `admin_allowed_emails`, which is also absent, and fails closed) —
correct, and the UI never calls them because no control exists.
One finding here: `/claims` also renders a `data-state="error"` line whose text is
"pending_claims — the query returned no count, so the number of rows is unknown; a count
read requires { head: true, count: "exact" }." → BUG-0210.

### The save path

`/records/walk_sandbox/00000000-0000-4000-8000-000000000001`, note cell: "A note a walker
may rewrite." → typed "verifier M3 walk 2026-09-11" → Enter → cell shows the new value →
reload shows it → **server killed and relaunched** → still there. Clearing `label` (NOT
NULL) is refused with the database's own words ("null value in column …") and the old
value still stands. Sandbox reset afterwards (deleted 3, seeded 3, verified) and the
residue sweep run: "16 column(s) scanned across 3 of 3 mapped table(s) … [label=0, note=0]",
6 tests passed.

### Independent oracle (direct REST, read-only)

`pending_claims` 877 · `event_listings` 120 · `review_items` 72 · `resolution_runs` 69 ·
`observations` 2595 · `walk_sandbox` 3 · `verdicts` **404 PGRST205** ·
`/rpc/settle_review_item` **absent** from the PostgREST OpenAPI root ·
`pending_claims?select=observed_at` **200**.

### EC11's residue question, answered with evidence

`review_items` holds 72 rows; 71 were opened 2026-09-11T06:13Z. Every one of those 71 has
`queue = "entity_link"` and a **non-null `external_ref`** (e.g. `K8vZ917qpo7`), and
`external_ref` is the column the SIBLING repo added today in
`supabase/migrations/20260911000002_a_review_item_can_name_a_reference.sql` (Ben's commit
43505768, "migration 2 of 6"). The 72nd, from 2026-09-02, has `external_ref` null.
Newest `resolution_runs` 2026-09-03, newest `observations` 2026-09-03 — both untouched.
This campaign has no insert path to `review_items` at all and writes only `walk_sandbox`.
Those 71 rows are the `entity-linking` campaign's, not M3's, and I did not touch them.

## Findings filed

BUG-0207 (P0), BUG-0208 (P1), BUG-0209 (P1), BUG-0210 (P2), BUG-0211 (P3), TASK-0078 (P1).
Full grading, clause by clause, is in `agenticflow/tracker/for-human/M3-verifier.md`.
