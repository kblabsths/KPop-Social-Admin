# Verifier run log — M2, campaign `admin-window`, 2026-09-09

Walked by the verifier against the dispatcher's walk instance
`http://localhost:8771` (`next dev`, branch `run/admin-window`), pointed at the
**staging** project `ubfjjqlvnpnoborczbdb.supabase.co` (the target declared in
`agenticflow/docs/SERVICES.md`). Driven with playwright from
`agenticflow/.venv-tools` (bundled chromium **151.0.7922.34**), viewport
1440×900 unless a step says 1280. Auth: one session cookie minted with
`node tests/walk/session-cookie.mts`, piped into the browser context in memory —
never written to disk, never printed. No `.env` was opened, printed or grepped.
I started no server and killed no process; port 8770 was never touched.

Sandbox discipline: `node tests/walk/reset-sandbox.mts` run **before** the walk
(`deleted 3, seeded 3, read back and verified`) and **again after** it (same
line). The only table this walk wrote is `public.walk_sandbox`. Verified after
the closing reset: all three seed rows byte-identical to
`tests/walk/sandbox-fixture.ts`; `events.title` of `01a03c9b-…add10c` still
`XG`; `venues.name` of `01a03c9b-…787aa` still `OVO Arena Wembley`; the newest
row in `observations` is still ticketmaster's from 2026-09-03. **Zero residue.**

Read before walking, and nothing else: `agenticflow/docs/vision/SPEC.md`,
`agenticflow/tracker/milestones/M2.md`, `contracts/admin-build.md`,
`contracts/admin-observability.md`, `agenticflow/docs/vision/VISION.md` (with
its 2026-09-08/09 amendment). Tickets were read only afterwards, and only the
three I was told not to refile plus a duplicate search before filing.

Evidence frames live in `agenticflow/tracker/evidence/M2/verifier/`
(gitignored); every number below is transcribed here in text because that
directory does not survive.

---

## 1. Getting in, the way a new operator would

- `GET /` unauthenticated → **307** to
  `/login?callbackUrl=http%3A%2F%2Flocalhost%3A8771%2F`, which renders
  "KPop Social Admin / Sign in to access the admin dashboard / Sign in with
  Google". `01-login-unauth.png`.
- The README (`README.md`) is **stale**: it still describes "scraper operations
  (raw scraped events, scraper runs, reconciliation review)", `.env.local`, and
  port 3000. It is not a spec surface and nothing it describes is reachable
  (every one of those routes 404s, §6 below), so I did not file it; the live
  instructions a walker needs are in `agenticflow/docs/vision/STACK.md` §5 and
  they are exact — I launched nothing, but the reset and mint blocks there ran
  verbatim first time. Recorded as a doc-drift observation for the campaign
  close, not a BUG.
- Authenticated `/` → 200. Sidebar holds **exactly six** links: Dashboard `/`,
  Queues `/queues`, Claims `/claims`, Sources `/sources`, Cycles & runs
  `/cycles`, Browse `/browse`. `02-dashboard.png`.

## 2. Dashboard (F3)

Observed: `OPEN DECISIONS 0` with "No open decisions…" and `OPEN SIGNALS 1 /
high / oldest 7d ago` — the two kinds counted separately, each a link
(`/queues?kind=decision`, `/queues?kind=signal`). CYCLES panel: 6 rows, all
"succeeded", window line "a window of at most 6, not a count of the cycles that
exist… The window filled its cap". RUNS panel: 5 rows, ticketmaster, two
`failed` with `error_summary` surfaced inline — "refusing to run:
KSPACE_BOT_USER_AGENT is not set" and "refusing to run: source ticketmaster has
no row in sources". Zero console errors, zero page errors.

Every cycle row links to `/cycles?cycle=<id>`; every run row links to
`/cycles?run=<id>` — **the run half of that promise is broken, BUG-0142** (§7).

## 3. Queues, both blocks, both tabs, the facet URLs (F4, EC8)

`/queues` 200. Two blocks of equal standing: **DECISION QUEUE** (`OPEN
DECISIONS 0`, "No decisions waiting") and **SIGNAL QUEUE** (`OPEN SIGNALS 1`,
"1 high, 0 low", "oldest 7d ago") with the one item on staging. Chips: kind
(all/decision/signal), queue (all/data_conflict/entity_link), shape
(all/data_conflict_fact/entity_link_fact/entity_link_source_pattern), status
(all/open/settled). QUEUE HEALTH gauge per queue with age percentiles and an
opens/settles-per-week table whose settle cells are dashes, explained: "Settles
per week are not measurable yet: no column records when an item was closed".
`03-queues.png`.

Facets walked, item links observed:

| URL | items rendered |
| --- | --- |
| `/queues?kind=decision` | none |
| `/queues?kind=signal` | `/queues/01a06287-5b67-7d1f-b4a5-d90aa93cb01a` |
| `/queues?shape=entity_link_source_pattern` | the same one |
| `/queues?queue=data_conflict` | none |
| `/queues?status=settled` | none |

**Verdict log is a tab, not a seventh page**: clicked "Verdict log" from
`/queues`, URL became `/queues?tab=verdict_log`, sidebar still six links, and
the panel rendered `data-state="not_provisioned"` reading exactly:
`verdicts isn’t in this database yet — it arrives with the scraper repo's
migrations.` `04-verdict-log-tab.png`.

## 4. The review item rendered (F4, F10, EC5, EC9)

`/queues/01a06287-5b67-7d1f-b4a5-d90aa93cb01a`, reached by clicking the row.
Section order measured from bounding boxes: `WHAT HAPPENED` y=84 → **`THE
CLOSE` y=254** → `EVIDENCE` y=342. **The close slot is above the evidence.**
`05-review-item-detail.png`, `06-close-slot.png`.

Three counts, all distinct and all rendered: `asked again ×700` (folds),
`769` stuck records in the source's window, and `91 of 91 evidence ids resolved
to a claim, in the order they folded in`.

Evidence table columns, left to right: **RECORD / FACT / VALUE / SOURCE / TIER
NOW / OBSERVED / PAYLOAD** — record and fact are both present, values resolve
(e.g. record `Z598xZbpZAdAF`, fact `venues.timezone`, value
`Europe/Amsterdam`, source ticketmaster, tier `official`, observed 9d ago,
payload `sha256/7b/7b5079…`). Per-source dial beside it renders the
awaiting-row trend with the honest line "No pattern threshold is readable from
here — the dial is a source-registry setting in the scraper repo, so the trend
is drawn without its line."

The close: `data-state="not_provisioned"`, whole text
`verdicts isn’t in this database yet — it arrives with the scraper repo's
migrations.` **Zero controls** — 0 buttons other than the shell's Sign out, 0
inputs, 0 textareas, 0 selects. No dead control anywhere on the page. The words
"recommend"/"recommendation" do not appear.

Naming note, graded not filed: SPEC F10 says this card names
`settle_review_item`; it names `verdicts`. That is a **recorded design
decision**, not a slip — `DECISIONS.md` 2026-09-08 ("a surface reads the
`verdicts` TABLE to know whether it may offer an action"): PostgREST cannot
introspect a function without calling it, so the surface probes the table and
names what it probed. EC9 asks for "the missing object (`verdicts`,
`settle_review_item`)" and one of the two is named. The API layer names the
other correctly (§8). Recorded as a **design limit + SPEC wording drift** for
the human, not a bug.

Links out of the item: `Its claims ticketmaster` →
`/claims?source_id=01a05782-…`, `Its source ticketmaster` →
`/sources?source_id=01a05782-…`, and each evidence row's source likewise.

## 5. Claims, Sources, Cycles & runs, Browse (F5, F6, F7)

**Claims** `/claims` 200. Tabs `Buckets` / `Standing disagreements`
(`/claims?tab=standing`). Buckets and counts as rendered:
`standing_disagreement 0`, `awaiting_link 108`, `awaiting_row 769`,
`escalated 0`, `agreeing 0` — sum 877, and the window line reads "877 claims in
all; the 50 longest-waiting are below". `in_window` is **not** a bucket, not an
empty bucket, not a filter option; visible text contains it zero times.
`07-claims.png`.

- Dropped-parameter line, quoted verbatim: `/claims?bucket=in_window` →
  "The URL carries bucket, which this page did not apply: nothing below is
  narrowed by it." Same line for `?bucket=nonsense` and for
  `?source_id=not-a-uuid`.
- Window line, verbatim: "Oldest first — the longest-waiting claim at the top;
  a claim whose instant is unknown sorts last. A window of at most 50 rows, not
  the whole view. 877 claims in all; the 50 longest-waiting are below — narrow
  with the filters above to reach the rest."
- Narrowing works: `?bucket=awaiting_link` → "108 claims match these filters",
  every row `awaiting_link`; `?bucket=awaiting_link&domain=venues` → the honest
  empty "the read happened and found no claims matching these filters at all",
  with the bucket table showing `awaiting_row 28` for the domain.
- One measured nuance, **not** a defect: under `/claims?bucket=in_window` the
  literal `in_window` occurs **3 times in the raw HTML** — all three inside
  Next's RSC flight payload echoing the URL the operator typed
  (`"claims?bucket=in_window"`, `"q":"?bucket=in_window"`,
  `__PAGE__?{"bucket":"in_window"}`), zero times in rendered text and zero
  times in any bucket, chip or option. EC2's machine check "zero occurrences in
  rendered HTML" holds for every URL the app itself emits; a hand-typed
  adversarial URL makes the framework echo it. Worth knowing before someone
  reports it as a regression.
- BUG-0138 (population read / page latency) is open and blocked on Ben; I did
  not refile it. Observed for the record: `/claims` felt slow but rendered
  correctly.

**Sources** `/sources` 200. Three registry rows — `test_harness` (registered,
trial, untrusted), `test_harness_control` (cited, trial, untrusted),
`ticketmaster` (registered, active, official, checkpoint
`2026-08-31T20:28:47+00:00`, last run 8d ago succeeded) — with lifecycle, tier,
checkpoint, last run, outcome, note, and per-row links `review items` / `runs`.
Gauges beside them: AWAITING-ROW TREND (`769` claims in window, from 1 source,
`ticketmaster 769 / 2 days with a claim`) and SETTLED VALUES (`0` re-rejected,
`0` sources). Explicit no-dial line: "No threshold line is drawn. The
per-source `stuck_pattern` dial lives only in the scraper repo's source
registry, and where Admin may read it is an open question — so no default is
substituted here." `08-sources.png`. BUG-0139 (read shape) and BUG-0140 (uuid
spelling) are in flight; not refiled. The `review items` link is broken in a
way neither covers — **BUG-0141** (§7).

**Cycles & runs** `/cycles` 200. `NEWEST ADAPTER RUN` lead row, then CYCLES
newest first, 69 of at most 200, with every count as its own column
(`FACTS EXAMINED / APPLIED / HELD / ESCALATED / ENTITIES CREATED / CLAIMS
LINKED / CLAIMS RE-REJECTED / ERRORS`) and `error_summary` inline (observed:
"canonical write refused: public.venues holds no row a78dc1c7-…"). A `skipped`
cycle renders as `skipped` (0.6s) and unfinished ones as `died` with `—`
duration; the legend row carries `still running` too. ADAPTER RUNS: 5 rows with
`RECORDS PARSED 325 / CLAIMS EMITTED 2,122 / RECORDS UNLINKED 134` and
`FAILURE CLASS config` on the two failures. `09-cycles.png`.
`/cycles?source=test_harness` narrows the runs half and says so in one
sentence; `/cycles?cycle=<id>` marks exactly one row
(`data-row-marked="true"` ×1) and names it; `/cycles?cycle=not-a-uuid` says the
id is not in the window. `/cycles?run=<id>` does neither — BUG-0142.

**Browse** `/browse` 200. One curated view, RECENT EVENTS, newest first
("a window of at most 50, not the whole catalog. The window filled its cap"),
columns TITLE / STARTS (UTC) / VENUE / DESCRIPTION / POSTER / SOURCES /
ARRIVED, with SOURCES read from the provenance join (rows show `ticketmaster`
or `—`). Column selector walked as a workflow: headers began
`[TITLE, STARTS (UTC), VENUE, DESCRIPTION, POSTER, SOURCES, ARRIVED]`; clicking
`Description` → URL `?cols=title,starts_at,venue,poster,sources,arrived` and
headers `[TITLE, STARTS (UTC), VENUE, POSTER, SOURCES, ARRIVED]`; clicking it
again restored all seven. `/browse?cols=title,bogus_column` renders `[TITLE]`
alone — the configured set and no more. Each title links to
`/records/events/<id>`. `11-browse.png`. No second view, no table browser, no
SQL box anywhere in the app.

## 6. Records: events, venues, walk_sandbox (F11, F12, F8-retirement)

**events** — `/records/events/01a03c9b-1d28-707d-9873-f73ab3add10c` (reached
from Browse, then by id). Regime note verbatim: "events is resolver-owned: its
values change through the resolution pipeline, never by a direct edit. An edit
here is recorded as an admin override — a claim at the admin tier, applied
through the pipeline and logged — and the pipeline then leaves that field
alone." Read-only reason verbatim: "No field here can be edited: an edit is
recorded through the resolution pipeline, and what records it is not present in
this database. Every value below is read-only until it is." Plus the
`data-state="not_provisioned"` card naming `verdicts`. Drawn-columns note:
"These 6 columns are the ones Admin works with for events…". Zero edit
controls. `12-record-event.png`, `15-record-event-provenance.png`.

**Per-field provenance renders, and it is real.** On that record:
`title XG → ticketmaster, applied 7d ago`, `poster_url → ticketmaster, applied
7d ago`, `starts_at → ticketmaster, applied 7d ago`, `venue_id → ticketmaster,
applied 7d ago`. I checked the negative case too: the first two records I
opened showed `—` everywhere, and direct SQL confirmed they genuinely carry
zero `field_provenance` rows, exactly as the page's own line says ("A — in
Provenance means no field provenance is recorded for that field"). Staging
holds **1712** provenance rows across `events.{title,starts_at,poster_url,
venue,status,ticket_url,event_type,time_precision}` (108 each) and
`venues.{name,city,country,address,timezone,latitude,longitude}` (115-125
each); `admin_locked` is true on **0** of them, which is right — nothing has
overridden anything.

**Reference as a link (F12 display half)**: `venue_id` renders as
`accesso ShoWare Center` / `01a03f78-a82b-…` linking to
`/records/venues/01a03f78-a82b-…`; clicked it and landed on that venue's own
record, 200. `13-record-event-venue-link.png`, `14-record-venue.png`.

**venues** — same regime note with "venues", same read-only reason, drawn-columns
note "These 5 columns…", fields `venue_id / name / city / country / address`,
provenance `ticketmaster, applied 7d ago` on the four value fields.

**groups / idols have no door.** `/records/groups`, `/records/idols`,
`/groups`, `/idols` all **404**, and the PATCH route answers
`404 {"error":"groups is not an editable table"}` /
`{"error":"idols is not an editable table"}`. No listing, no search box, no link
anywhere in the app.

**walk_sandbox** — `/records/walk_sandbox/00000000-0000-4000-8000-000000000001`.
Regime note verbatim: "walk_sandbox is a staging fixture table, edited
directly: a value changed here is written to it as it stands and reaches no
catalog record. No field provenance is recorded for it, so no source is shown
beside a value." Drawn-columns note: "These 6 columns are the ones Admin works
with for walk_sandbox…". `16-sandbox-record-before.png`.

The edit cell, walked as a workflow:

- **Distinguishable before touch**: `sandbox_id` is bare text; `label`, `note`,
  `tally`, `is_flagged`, `observed_on` are `<button>`s with
  `aria-label="<field> of walk_sandbox"` and a hairline underline
  (`underline decoration-hairline decoration-1 underline-offset-2`).
- **Opens on press with its value selected**: clicking `note` focused an
  `<input>` whose value was `A note a walker may rewrite.` with selection
  `[0, 28]` of length 28 — the whole value, so a straight retype replaces.
  `17-edit-cell-open.png`.
- **Keyboard reaches it**: Tab focused the `label` cell and Enter opened the
  same editor with its value selected.
- **Escape retires**: after Escape, 0 inputs on the page and the cell read
  `A note a walker may rewrite.` again — no write.
- **Blur commits**: typing `BLURTEST2` then clicking the heading fired exactly
  one `PATCH /api/admin/records/walk_sandbox/…` → **200**, the cell read
  `BLURTEST2`, and after a full reload it still read `BLURTEST2`. Persisted to
  the database; survives a reload. `18-blur-shows-unsaved.png` (the moment of
  blur) and `19-after-reload-true-value.png` (after reload). The payload is one
  typed field: `{"field":"label","value":"probe label"}`.
- **Refusal, two halves, inside the container**: typing `not-a-number` into
  `tally` and pressing Enter → HTTP **500**, value stayed `7`, and the cell
  rendered a `role="alert"` box with exactly two spans —
  `type-data`: `invalid input syntax for type integer: "not-a-number" (22P02)`
  and `type-body`: `Type a whole number, like 7.` The box is
  `absolute left-full … max-w-xs wrap-break-word` inside the value span; I
  measured every descendant against the cell's box (x=459.3, w=716.4) and
  **nothing overflowed it**. `20-refusal-tally.png`. Observation, not filed:
  the route answers **500** for a foreseeable user typo (and logs a console
  error); the screen behaviour is honest, the status code is not the one I'd
  expect — worth a line in build judgments.
- **Long-token wrap**: saved a 152-character unbroken token into `note`. At
  1280px the cell's button measured `scrollWidth 862 == clientWidth 862`,
  height 36 (i.e. it wrapped onto a second line), and
  `document.scrollWidth == clientWidth == 1280` — no horizontal overflow of the
  cell or the page, editor open or closed. `21-long-token-wrap.png`,
  `22-long-token-1280.png`, `23-editor-long-token.png`.
- `is_flagged` renders as a text cell reading `false` (scalar → cell, per
  spec §8). Noted, not filed: a boolean edited by typing is a shape the spec
  permits but an operator may not expect.

## 7. Server-side refusals, forged (F8's survivor, F11, EC6)

Forged PATCHes issued from the authenticated page context with the UI's own
envelope. **I sent no value that would change a catalog row**: the two mapped
cases carry the row's current value, and both were refused before reaching the
database.

| request | status | body |
| --- | --- | --- |
| `events.bogus_col` | **403** | `bogus_col is not an editable field of events` |
| `events.event_id` | **403** | `event_id is not an editable field of events` |
| `events.title` (= current value `XG`) | **503** | `settle_review_item is not present in this database`, `"missing":"settle_review_item"` |
| `venues.name` (= current value) | **503** | same |
| `venues.latitude` | **403** | `latitude is not an editable field of venues` |
| `walk_sandbox.created_at` | **403** | `created_at is not an editable field of walk_sandbox` |
| `walk_sandbox.sandbox_id` | **403** | `sandbox_id is not an editable field of walk_sandbox` |
| `groups.name` | **404** | `groups is not an editable table` |
| `idols.stage_name` | **404** | `idols is not an editable table` |
| `review_items.status` | **404** | `review_items is not an editable table` |
| `observations.value` | **404** | `observations is not an editable table` |

So: a column absent from the map is refused server-side, not merely hidden; a
mapped `events`/`venues` column is refused **with the missing object named**
while the function is absent, and never written directly; no direct write path
to `review_items` exists from Admin. This is where `settle_review_item` is
named correctly, which is why §4's card is a wording question and not a
knowledge gap.

Confirmed after the walk: `events.title` still `XG`, `venues.name` still
`OVO Arena Wembley`, newest `observations` row still 2026-09-03.

## 8. Absent addresses, retired addresses, and the gate

Retired routes, all **404** with "No page at this address / Analytics, Database
and Data management were retired with the old dashboard, and nothing replaced
their URLs. The window is the six pages in the sidebar." — `/overview`,
`/analytics`, `/database`, `/data-management`, `/scrapers`, `/review`,
`/dashboard`, `/events`, `/groups`, `/idols`, plus `/records` and
`/records/walk_sandbox` (no table listing) and `/records/bogus_table/<uuid>`.
`25-retired-404.png`.

Bad ids answer **200** with an honest `data-state="empty"` and, notably, tell
the two failure modes apart. Verbatim:

- `/records/events/not-a-uuid` → "No events record at this address / The
  address bar does not hold an id: events ids are uuids, 32 hexadecimal digits
  usually written in five hyphenated groups. Browse lists recent events, and an
  event's record links to its venue. Check the id in the address bar."
- `/records/events/<well-formed but unknown uuid>` → "No events record with
  that id / Browse lists recent events…"
- `/queues/not-a-uuid` → "No review item at this address / …review_items ids
  are uuids…"
- `/queues/<unknown uuid>` → "No row with that id in review_items"
- `/records/walk_sandbox/not-a-uuid` → "…Admin has no walk_sandbox listing:
  such a record is reached by its id alone."

`24-not-a-uuid.png`. Zero page errors on all of them.

**The gate** (EC11): unauthenticated GETs to `/`, `/queues`,
`/queues?tab=verdict_log`, `/queues/<id>`, `/claims`, `/claims?tab=standing`,
`/sources`, `/cycles`, `/browse`, `/records/events/<id>`,
`/records/venues/<id>`, `/records/walk_sandbox/<id>` **all** redirect to
`/login?...callbackUrl=...`. An unauthenticated `PATCH` to
`/api/admin/records/walk_sandbox/<id>` is an opaque redirect (never reaches the
handler; the row was unchanged).

**No key in anything the browser gets**: I collected every html/js/json
response across ten pages — **31 assets** — and searched them for
`SERVICE_ROLE`, `service_role`, `STAGING_SUPABASE`, `ubfjjqlvnpnoborczbdb`,
`supabase.co`, `eyJhbGciOi`, `AUTH_SECRET`. **Zero hits.** (This is the dev
server; the built-bundle scan is the suite's.)

**Sign-out**: clicked `Sign out` in the shell → landed on `/login`; the session
cookie was gone (only `authjs.csrf-token` and `authjs.callback-url` remained);
revisiting `/queues` redirected to `/login?callbackUrl=…%2Fqueues`.
`26-after-signout.png`.

## 9. The chained investigation (spec §4: "an investigation never leaves the app")

Dashboard → `OPEN SIGNALS` card → `/queues?kind=signal` → the item row →
`/queues/01a06287-…` → `Its claims ticketmaster` →
`/claims?source_id=01a05782-…` ("877 claims match these filters") → a claim's
record link → `/records/events/01a03f78-69bb-…` → its venue link → the venue's
record. Browser Back twice returned to the item. No step left the app, no step
errored. `32-item-to-claims.png`.

## 10. What could not be walked, honestly

- **The two fact-shaped review items.** `review_items` holds exactly **1** row
  on staging (`entity_link`, `open`, ticketmaster). The `data_conflict` and
  `entity_link` fact views, and therefore EC5's three-shape control inventory,
  are "built, not walkable on this data" — the grade M2 itself pre-authorises.
- **Every §7 settlement end to end** (tests 6-8, EC5-EC7's write halves).
  `verdicts` answers `PGRST205` and `settle_review_item` is absent by design;
  the deferred patch run owns those. What I *could* grade — that no dead
  control is offered and that the refusal names the function — passed.
- **A settled item's inline verdict** (F13's second half) — needs a settled
  item and the table; neither exists.
- **A server restart.** The walk instance is the dispatcher's and I may not
  stop it; the persistence claim is carried instead by a full page reload plus
  a direct database read of the row I wrote.
- **Dark theme** was rendered (`28-dashboard-dark.png`, `29-record-dark.png`)
  with zero errors, but judging it is the designer's, not mine.

## 11. Filed

- **BUG-0141** (P1) — `/queues` ignores the `source_id` the Sources page links
  it with; another source's item renders as that source's review items.
- **BUG-0142** (P2) — the Dashboard's run links land on `/cycles?run=<id>`,
  which marks nothing and says nothing.

Not refiled, as instructed: BUG-0138, BUG-0139, BUG-0140.
