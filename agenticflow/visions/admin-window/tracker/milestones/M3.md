# M3 — completeness of the read slice

Campaign `admin-window`. Slice definition: `agenticflow/docs/vision/ROADMAP.md`.
Behavior: `agenticflow/docs/vision/SPEC.md` (F14–F16, added 2026-09-10).
North star: `agenticflow/docs/vision/VISION.md`, frozen — every criterion below
traces to it and none extends it.

**Why M3 exists.** Through the M2 close this roadmap said there was no M3.
**Ben overruled that on 2026-09-10**: *"not being able to load all claims if I
want to is a huge oversight"*, paging on Claims and Browse is a next-milestone
item, and the milestone's shape is *"as long as everything is complete is good."*
M3 is that ruling and its two direct neighbours — nothing else.

**M3 carries zero schema, in this repo and in the sibling.** No migration is
written, authored or installed by anything here. The one migration installed on
staging (`pending_claims.observed_at`, 2026-09-10) was Ben's act on a handoff
this campaign authored in M2; M3 consumes it and does not extend it.

**M3 opens no new front.** No new page, no new nav item, no search, no second
Browse view, no door onto `groups`/`idols`, no dial, no responsive work. If a
ticket in M3 adds a surface, it is mis-authored — report it.

---

## Preconditions (human-owned)

Unchanged from M2, and nothing new:

1. `STAGING_SUPABASE_URL` and `STAGING_SUPABASE_SERVICE_ROLE_KEY` in `.env`
   (names only in `.env.example`).
2. The staging Supabase project declared in `agenticflow/docs/SERVICES.md`.
3. **`public.walk_sandbox` pasted into staging** from
   `agenticflow/tracker/for-human/TASK-0034.md`. Still not present; until it is,
   the interim walk-write exception stands and every M3 walk is narrower than it
   should be.

Not preconditions, and not blockers for M3: Ben's review of the two §9 handoff
artifacts, and their install. Those belong to the campaign's satisfaction
sentence and to the deferred patch run, not to this milestone's work.

---

## The first ticket of the milestone is a contract amendment, not a page

`ARCHITECTURE.md` §4.3 currently reads *"Paging is not the answer to a cap and
none is built: nothing in the spec asks for it, and complete-or-refuse means
there is never a partial page to continue."* The spec now asks for it (SPEC
F14). §4 rule 1 forbids a component fetching; §5 makes the page function the
only `async` component on a route.

**The architect amends those three contracts before any page changes, and states
in the amendment where the boundary now is.** A builder that finds a contract in
its way files a blocked question; it never interprets one. A page diff that
lands before the amendment is a defect regardless of whether it works.

---

## The tickets

| SPEC | ticket | priority | depends on |
| --- | --- | --- | --- |
| F16 — the concurrent second leg | **FEAT-0014** | P1 | — (first) |
| F14 — paging on Claims and Browse | **FEAT-0015** | P1 | FEAT-0014 |
| F15 — windowed figures tell the truth | **FEAT-0016** | P2 | — |

The architect decomposes these; the contract amendment named above is the first
ticket cut from FEAT-0015 and lands before any page diff.

Patch-lane residuals routed at the same close, not M3 work and not gating it:
**BUG-0164** (`/queues` carries the copy BUG-0161 replaced), **TASK-0060**
(the last two source-name renderers, which closes that class),
**DEBT-0016** (`newestFirst` exported twice), **TASK-0061** (README describes
the retired dashboard).

---

## Exit criteria

### EC1 — the repo's bars are green, and the live tier is in the gate

`npm run lint` and `npm run build` each exit 0 with zero errors, and the
campaign's test suite passes. The suite stays **offline by default**: with no
`STAGING_SUPABASE_*` names set it still passes, with staging tests skipped
behind the explicit live marker rather than failing or falling back.

- Machine-checkable: `npm run lint` → exit 0; `npm run build` → exit 0;
  `./node_modules/.bin/vitest run --project=offline` and
  `--project=isolated` each exit 0 with the staging names unset, and again with
  them set.
- Machine-checkable: for every ticket closed in M3, if its `touch_scope` matches
  `src/app/**` or `src/components/ui/**` then its `## Checks` block contains an
  `npm run test:live` invocation. Zero exceptions. (Carried from M2 EC1;
  `npm test -- <path>` is not a legal check — LESSONS 12, BUG-0142.)

  **AMENDED — EC1 live-tier ruling (architect, 2026-09-11), from BUG-0209's
  measurement.** The clause above is replaced, not waived, by these two:

  1. **On the settled tree, at the close:** `npm run test:live` exits 0, and the
     verifier records its file and test counts. That is the guard. It runs on
     the tree the milestone ships, where a live regression is actually visible.
  2. **Forward-looking, on authoring only:** a ticket *authored after
     2026-09-11T21:30Z* whose `touch_scope` names `src/app/**` or
     `src/components/ui/**` carries a **targeted** live invocation in its
     `## Checks` — `npm run test:live -- tests/live/<page>.live.test.ts`, the
     form 22 of M3's tickets already use — not the whole tier.

  A ticket that has already CLOSED keeps the bar it closed under. Three reasons,
  each measured rather than argued: (a) the per-ticket grep has never held in any
  milestone of this campaign — in-scope tickets carrying `test:live` are M1 6/62,
  M2 14/51, M3 22/38 (builder-309, over `tickets/` + `archive/`), so it grades
  record-keeping practice nobody has ever followed, not code; (b) it caught
  nothing — the live tier was run at HEAD over the 14 exceptions and is green, 11
  files / 121 tests, so the gap was an unguarded lane and not a missed defect;
  (c) the repair costs more than the risk and destroys evidence — `amend-checks`
  DELETES the receipt that proved a ticket at its close, so greening history
  means deleting 14 honest green receipts and re-earning each against a
  network-dependent tier, after which 14 closed receipts redden forever on any
  future live flake (BUG-0208 is exactly that flake). A closed ticket's `## Checks`
  are the bar it was graded against; rewriting them after the grade is the one
  edit this factory must never make.

  BUG-0210 and BUG-0211 were authored before this ruling and are not retroactively
  obliged; BUG-0210 is under QA grade as this is written, and amending its checks
  would delete the receipt that lane is earning — which is the same harm, live.

### EC2 — M1 and M2 are still green

Every M1 and M2 exit criterion that was PASS at its close is PASS at the M3
close. M3 changes read paths three shipped pages depend on; a regression on any
shipped surface fails M3.

- Machine-checkable: the six live parity suites green (`npm run test:live`), one
  per page; the retired-route 404 set still 404; the sidebar still exactly six
  links; `in_window` still zero occurrences in rendered text across all surfaces.
- Machine-checkable: the whole absence proof still passes — every surface 200
  with `data-state="not_provisioned"` against a database answering `PGRST205` to
  every read, including the new route handler's own surface behavior.

### EC3 — the paging contract is amended before the first page diff

- Machine-checkable: `ARCHITECTURE.md` §4.3's sentence "Paging is not the answer
  to a cap and none is built" is **gone or superseded in place**, and §4.3, §4
  rule 1 and §5 each carry a dated amendment naming what paging may and may not
  do.
- Machine-checkable: the amendment's commit is an ancestor of every commit that
  touches `src/app/claims/page.tsx`, `src/app/browse/page.tsx`, or any file
  under the new route handler's directory, in M3.

### EC4 — the operator can reach every claim, and every event, past the window

- Machine-checkable: against staging (877 claims), a paged walk of `/claims`
  starting from the first screen reaches a claim whose position is beyond the
  first window's last row, and the total number of distinct claim ids reachable
  by paging equals the exact count the page's own head figure states. No id
  appears twice; no id is skipped.
- Machine-checkable: the same walk on `/browse` reaches an event beyond the
  first window's last row.
- Machine-checkable: each "more" request is **one** request to the route
  handler carrying an explicit bound, asserted in the offline suite against a
  recording stub; the count of requests issued equals the number of times the
  affordance was pressed, and pressing nothing issues none.
- Machine-checkable: the first server-rendered screen of `/claims` and
  `/browse` is byte-identical in its window line, its figures and its row set to
  what it renders today, on the same fixture. Paging changes what happens after
  the first screen, never the first screen.

### EC5 — a paging affordance is never drawn where it cannot be honoured

- Machine-checkable: on a fixture where the set is exhausted by the first
  window, the affordance is absent and the surface says the set is complete;
  on a fixture with more rows, it is present.
- Machine-checkable: pointed at a database answering `PGRST205`, `/claims` and
  `/browse` render `data-state="not_provisioned"` and the affordance is absent —
  zero occurrences of its control in the rendered HTML.
- Machine-checkable: a refused page request never yields a partially extended
  list — the rendered row count after a refusal equals the row count before it,
  and the refusal names the object, on two fixtures (one that must refuse, one
  that must not — LESSONS 8).

### EC6 — the route handler is gated exactly as the pages are

- Machine-checkable: an unauthenticated request to the paging route handler
  redirects or is refused before the handler body runs, on every method it
  accepts; the assertion is the same one the pages already carry.
- Machine-checkable: a scan of the built client bundles finds no service-role
  key, no staging URL, and none of the literals `SERVICE_ROLE`, `service_role`,
  `STAGING_SUPABASE`. A forged request naming a bound outside the handler's
  allowed range is refused with the reason named, never clamped in silence.

### EC7 — no two figures on one page silently disagree

- Machine-checkable: on a fixture holding more than 1,000 claims, the Claims tab
  gauge's bucket figures and the head counts above them are either equal, or
  each is labelled as the kind of fact it is (window vs. total) in rendered
  text — asserted on rendered HTML, not on the read.
- Machine-checkable: the same fixture produces no rendered sentence asserting a
  relationship between two figures that no single read established.

### EC8 — every window line names the narrowing its read carried

- Machine-checkable: `/sources`' two scan-window lines name the narrowing their
  read carried, for a narrowed URL and for an unnarrowed one; `/sources` and
  `/sources?<facet>=<value>` do not render byte-identical window lines when the
  facet narrows the read.
- Machine-checkable: `readClaimCountSince` and the scan it is printed beside
  carry the same bounds — asserted at the read, and on the rendered page for a
  fixture where an unbounded upper edge would change the number.

### EC9 — the read layer is concurrent and still refuses correctly

- Machine-checkable: `readRowsByIds` issues its chunks concurrently, bounded;
  asserted against a stub that records issue order and concurrency, on an id set
  spanning at least three chunks.
- Machine-checkable: the three properties it holds today survive, each on its
  own fixture: an empty id set issues **zero** requests; a chunk that errors
  yields the first non-`ok` result unchanged (never a half-filled `ok`); a
  missing table reaches the page as `not_provisioned` naming that table.
- Machine-checkable: `/claims` warm server time, measured over HTTP against a
  production build on the walk instance, is **at or under 1.4 s** — recorded as a
  before/after pair with the commands in the evidence note. `/queues` and the
  review item are measured in the same pass and neither regresses.

### EC10 — no schema, no new surface, no sibling write

- Machine-checkable: this repo's `supabase/migrations/` holds exactly the two
  app-owned files it held at the M2 close.
- Machine-checkable: **CORRECTED 2026-09-11 (architect), from the verifier's
  report that this clause was unfalsifiable as written.** The old form,
  `git -C "../kspace Scraper" log --all --grep=admin-window`, is a substring
  grep over commit MESSAGES: it matches any sibling commit that merely mentions
  this campaign, and it did — Ben's own `43505768` in the sibling's
  `entity-linking` campaign names us in its message and touches only sibling
  files. A clause that a stranger can redden by talking about us proves nothing.
  The falsifiable form, anchored on the factory's own commit stamp (every commit
  this campaign makes begins `admin-window/<TICKET>`):
  `test "$(git -C "../kspace Scraper" log --all --format=%s | grep -c '^admin-window/')" -eq 0`
  — plus, unchanged, no ticket closed in M3 has a `touch_scope` naming a path
  outside this repo (measured: 0 of 329).
- Machine-checkable: the sidebar holds exactly six links; `src/app` gains no
  page route; the strings `search`, `dial`, `threshold` introduce no new
  operator-facing control (asserted on rendered HTML across all surfaces).

### EC11 — zero residue

- Machine-checkable: the live sweep runs green and staging carries no M3
  leftovers after the final run. Walks write only to `walk_sandbox` once it
  exists; until then the interim note-restore-sweep is followed and the sweep
  command's output is recorded.
- Machine-checkable: no `.env` value appears in any commit, ticket, receipt or
  evidence file.

### EC12 — the campaign's own bars

- `docs/build_judgments.md` rewritten whole for M3, at most eight entries, each
  naming the contract location that was silent.
- Screenshots of both paged surfaces, at the first screen and after paging.
- The cross-directory report: the three handoffs' state (filed / reviewed /
  installed) restated, including whether the installed migration is still
  untracked in the sibling.
- Every contract gap hit in M3 is a blocked ticket, not a silent call.

### EC13 — the campaign's stop condition is restated

At the M3 close the verifier states, in one paragraph, whether VISION's
satisfaction sentence is met — *"the verdict UI is built and both handoffs are
complete and reviewed"* — and what remains is Ben's review of the two §9
artifacts plus the deferred patch run. **M3 is not planned to be followed by an
M4.** A recommendation to continue is a recommendation to Ben.

---

## Deliberately not graded here

- **Search.** Ben's ruling 2026-09-10: a vision addition, out of scope unless he
  runs `/ship revise`. The evidence for it is carried in ROADMAP, not in a
  ticket.
- **Tests 6, 8 and test 7's override half.** Still the deferred patch run's,
  after Ben installs the two §9 migrations. Unchanged by M3.
- **The two fact-shaped review items and the verdict log's rows.** Still "built,
  not walkable on this data" until staging holds them.
- **`DrawnWindow.scope`'s comma-joined string.** Unreachable today; a constraint
  on F15, not a criterion (M2 retro ruling 4).
- **`runs` retention and the `runs` row-cap horizon.** Scraper-side; a handoff
  for Ben, sequenced together, not M3 work.

---

## Walk notes for the M3 endgame — recorded, NOT filed (architect, 2026-09-11)

The M3 early walk (`agenticflow/tracker/for-human/M3-early-walk.md`) deliberately
recorded two things as non-tickets (1 and 2 below); item 3 was added at
BUG-0175's close, from a QA residual the architect ruled rather than filed.
They are parked here so the endgame walker
and the strategist find them, and so the next walker does not re-file them.

**1. ~~A proposed bar for the endgame — the designer's proposal, in the designer's
words.~~ — SUPERSEDED by Ben's ruling of 2026-09-11, now LOOK_AND_FEEL Feel bar
14 ("nothing the operator must find sits below a long list"); recorded by
TASK-0079, not open, not re-proposed.** *"A control that extends a list is still on screen after it acts."*
Measured on `/claims`: the button's top moves 434 → 2,084 with the viewport 900
tall and `scrollY` unchanged, and the document grows 2,814 → 4,464 — so a press
puts the control ~1,650px below the fold, and paging `/claims` to the end is 17
presses and 17 scrolls. The designer's reason for NOT filing it stands: this is
normal "load more" behaviour, no bar in LOOK_AND_FEEL covers it, and filing it
mid-milestone would be inventing taste. **No bar is invented here.** ~~It is a
LOOK_AND_FEEL addition, which is the designer's document and Ben's call: if it is
accepted at the M3 endgame it becomes a bar first, and a ticket only if the paged
surfaces then fail it.~~ **Ben ruled it himself on 2026-09-11 and wrote a
stronger rule than the one proposed; it is Feel bar 14, and the `/claims` fix it
implies waits on his open paging-shape decision.** Recorded with it, from the same walk: by the time
`/browse` exhausts, the window line BUG-0174 corrects sits ~4,000px above the
operator who paged, so that fix's readership is whoever reloads or shares the
URL. The continuation-line alternative was rejected in writing under BUG-0172
(it leaves the false sentence where the operator reads it first, and it breaks
the one-`[data-window]` rule) and is not reopened.

**2. A measurement trap for the next walker, not a defect.** Tailwind v4's
`transition-colors` includes `outline-color`, so a computed-style read taken
immediately after Tab returns the MID-TRANSITION value — the walk's first read
said gray-800 and the accent focus ring was nearly filed as missing. **Read the
ring ~200 ms after focus.** As built it is correct: `#9810fa`, 2px, 1px offset,
and Enter pages. Recorded here rather than in `agenticflow/docs/LESSONS.md` on
purpose: LESSONS is the BUILDER's briefing, rewritten whole each close and put in
front of every builder by `ticket.py packet`, and a walker's measurement
technique has no builder application — it would cost a line every builder reads
to serve the one agent who never reads that file.

**3. The paging refusal's em dash at narrow widths — recorded, NOT a defect
(architect ruling, 2026-09-11, off QA's close of BUG-0175).** QA measured, on a
production build against staging: below ~900px the em dash that separates the
identifier from the reason heads a wrapped line with nothing to its left; at
1440px it sits inline with an 8px gap. The mechanism is known and is a
consequence of a change QA graded CORRECT: BUG-0175's builder moved the em dash
into the SANS span so `dir="ltr"` isolates exactly the identifier, and
`Refusal`'s container is `flex flex-wrap items-baseline gap-2`
(`src/components/ui/paging.tsx`), so the sans span wraps as one flex item and
takes its leading em dash to the head of the new line. The 8px is `gap-2`.

**Ruled: not a violation, and not to be filed.** LOOK_AND_FEEL names a minimum
supported width, and ~900px is below it. `agenticflow/docs/vision/LOOK_AND_FEEL.md:133-140`:
*"Viewport: the frame is desktop-only, 1280px wide and up. Supported range is
1280px to no upper bound; the design target is 1440x900 (bar 1). ... Below
1280px the app is unsupported in M1, and nothing a narrow viewport does is a
violation"* — Ben ruled that 2026-09-02, and it already covers a worse
measurement (the Dashboard's attention counts wrapping one word per line at
390px, BUG-0050, recorded and not a defect). A ~900px reading grades nothing.

**What the endgame walk should do instead: measure it at 1,280 — the supported
FLOOR — and only then.** The two measurements in hand are 1440 (inline, fine)
and ~900 (unsupported, ungraded); the boundary between them is untested, and
1280 is the only width at which this can be a defect. If the em dash heads a
wrapped line at 1280 on any refusal arm, THAT is filable, against
LOOK_AND_FEEL's Voice/Typography rather than against a width bar. Measure after
BUG-0176 lands, not before: its criterion 13 changes what this line is made of
(the not-provisioned arm renders the object as an `<Identifier>` node inside an
extracted clause), so a reading taken today describes markup that is about to
change on one of the five arms.

**4. "The 0 longest-waiting are below" over an empty card — recorded, NOT filed
(architect ruling, 2026-09-11, off QA's close of BUG-0183 and the filing of
BUG-0186).** QA measured on `/claims`: a window whose COUNT read answers 900
while its row read comes back with none renders "900 claims in all; the 0
longest-waiting are below — the rest are not shown." above the empty card
(reproduced through the real component on a988b00: cap 50, held 900, drawn 0).

**Ruled: not a truth defect, so not a BUG-0186 criterion.** Both numbers are
facts a read established — 900 is the count read's, 0 is the rows the page drew
— and no clause asserts a relationship between them that neither read
established, which is the rule BUG-0174/BUG-0183/BUG-0186 enforce. What is
wrong with it is that it RANKS an empty set and then apologises for withholding
the rest above a card that already says the read found nothing: a wording
problem, and wording is the designer's jurisdiction (criteria and DEBT never pin
UI copy). BUG-0186 leaves this line byte-identical on purpose (its criterion 4a
pins it) so that the endgame walker grades one sentence rather than a moving
one.

**What the endgame walk should do: grade it against the Voice bars at 1440x900,
after BUG-0186 lands**, and if it fails, file it as a Voice defect naming the
empty-card duplication — not as a window-line truth defect. The count clause
itself is sound; a candidate the designer may prefer is to drop the ranking
clause when the window drew no rows at all, which is a sayability rule the
window line can carry without any new read.

**The pin that fix will redden, and who carries it (architect ruling,
2026-09-11, off QA's close of BUG-0186).** That sentence is byte-pinned in
exactly ONE place: `tests/offline/ui/primitives.test.ts`, in the case *"leaves
every state but the two the ruling moves byte-identical"* — line 1709 for the
`0` variant QA flagged, line 1706 for the `37` one, line 1748 for the paged
arm's. Measured 2026-09-11 on run/admin-window: `grep -rn longest-waiting tests
src` returns three byte pins, all in that file; `tests/offline/claims/page.test.ts`
already grades this line digit-masked (`shapeOf`, `replace(/[\d,]+/g, "#")`)
and pins no word of it. BUG-0186's criterion 4 mandated those pins to prove the
ticket moved two arms and nothing else, and QA has since measured that proof
cross-tree (one-file differential, exactly 2 reds) — so the pins have SPENT
their purpose and now only hold the designer's own copy still.

**Ruled: the Voice BUG this walk files carries
`tests/offline/ui/primitives.test.ts` in its `touch_scope`, and its criteria
say the pin is converted to a STRUCTURAL assertion — the numbers, the hooks and
the presence of the two facts, with no word of the sentence pinned — in the
same lane as the wording change.** Both halves in one ticket; a walker that
files the wording fix without that scope line hands its builder a red it is not
allowed to touch. No separate de-pin DEBT is filed ahead of the walk, on
purpose: it would cost a builder lane to save one scope line, and it would land
in `tests/offline/ui`, which is inside **TASK-0072**'s touch scope (open, M3,
and TASK-0073 depends on it) — two tickets writing one destination for no gain.
If the walk grades the sentence GOOD and files nothing, the pins simply stand,
blessed.

**5. The bound-ceiling sentence's next step is claims-shaped on `/browse` —
recorded, NOT filed (architect, 2026-09-11, off QA's close of BUG-0178).** The
limit arm of `PageMore` (`src/components/ui/paging.tsx:483`,
`stoppedAtTheCeiling`) ends *"Narrow the view and page the smaller set."* The
noun is per-surface (`holds`, so `/browse` correctly reads "events"), but the
NEXT STEP is not: `/browse` offers no facets at all. QA confirmed the mechanism
on the landed tree — `/browse`'s only parameter is `columns`, which "chooses
which COLUMNS render and never which rows are read"
(`src/app/browse/page.tsx:215-221`, `scope: null`) — so the sentence tells the
operator of that page to do a thing the page does not offer.

**Reachability, measured, and it is why nothing is filed:** the arm draws only at
`MAX_PAGE_OFFSET + SIZE` = 100,050 rows, and read-only staging holds **120
events** (against 877 claims). `/browse`'s ceiling is three orders of magnitude
away; no criterion, no receipt and no walk can drive that surface into this arm
on this data. QA reached it only through its own `PagingProvider` +
`PageMore` probe at a typed `held`, which is a component reading, not a page
reading. It is not test-pinnable either without grading prose, which criteria and
DEBT may not do (wording is the designer's jurisdiction).

**What the endgame walk should do: grade the sentence against the Voice bars at
1440x900, on the ONE surface where it is reachable (`/claims`, ceiling
100,000 claims) and on the component reading for `/browse`, and file a Voice BUG
only if it FAILS** — the fact that a next step is unreachable advice on a
surface the operator cannot reach is a defect only if the bars say it is. If it
passes, this note is the blessing and the next walker does not re-file it.

**The cost, if it is filed, so the walk files it with its scope right:** a
per-surface next step needs a NEW PROP on `PageMore` (a next-step string or a
"this surface can be narrowed" flag) threaded from **both** call sites —
`src/components/claims/paged-claim-list.tsx:94` and
`src/components/browse/paged-browse-table.tsx:165` — both of which were outside
BUG-0178's touch scope, which is exactly why its builder left the gap rather
than widening. A Voice BUG here therefore carries `src/components/ui/paging.tsx`
AND both wrappers AND `tests/offline/ui/paging.test.ts` in its touch scope; one
without the wrappers hands its builder a sentence it cannot make per-surface.
Cheaper alternative the designer may prefer, and which needs no prop: a next
step true of every surface, or none at all — the arm's other two sentences
already stand on their own.

early_walk: 2026-09-11 fired

---

## Design walk — 2026-09-11 (designer, M3 endgame)

The running app walked against `agenticflow/docs/vision/LOOK_AND_FEEL.md`, all
three parts, on a production build of `f9165d18` served on the walker's own
bind-probed port 8796 against staging; 1440x900 and 1280x900, light and dark;
`walk_sandbox` reset before and after and the residue sweep green. Full report:
`agenticflow/tracker/for-human/M3-endgame-walk.md`.

### Per-rule verdict — The Look

| rule | verdict | measurement |
| --- | --- | --- |
| Palette — five colours, named jobs; state colour is the ramp's dark step | **PASS** | every state ink measured against its real backdrop on 8 surfaces x 2 themes x 2 widths; dark-theme paging renders window line `#99a1af`, refusal `#ff6467`, not-provisioned `#4a5565` |
| Typography — five steps, nothing outside them | **PASS** | leaf-text sweep over 8 surfaces x 2 themes x 2 widths returns exactly `11/16 mono`, `12/18 sans`, `10/14 sans uppercase`, `14/20 sans 600 uppercase`, `20/24 mono 600`. Weights **400 and 600 only** |
| Typography — **mono is the machine's words, sans is the app's** | **FAIL → BUG-0196 (P2)** | four sentences authored in `src/lib/db/result.ts` (`documentInstead` :389, `serialisedInstead` :410, `framesInstead` :427, `REFUSED_WITHOUT_WORDS` :575) render in Geist Mono 11px red. Two renderers carry it: `ui/error-line.tsx:46` (state 4, every page) and `ui/paging.tsx:713`. The document clause's own content says "these are not the database's words". Same rule, same family as BUG-0175 |
| Spacing / density — the scale, and nothing outside it | **PASS** | table cells `6px 8px` on server-rendered AND client-appended rows; paging control `6px 12px`; radii across every surface: `4px` and nothing else |
| 1px hairlines, never shadows | **PASS** | **zero** `box-shadow` on any element of any surface, either theme, either width |
| Viewport — 1280 to no upper bound, no horizontal page scroll | **PASS** | no page scrolls horizontally at 1280 or 1440, either theme, before or after paging. **The M3 walk note's open question is answered**: at the supported floor of 1280 no paging-refusal arm wraps — longest arm ends at x=1249 against a right edge of 1264, `<p>` height 18px, one line box — so the em dash never heads a wrapped line at or above 1280. Not filable; the ruling stands |
| Data table — hover fill, no zebra, `—` for null, scroll inside the border | **PASS** | and the client-appended rows are structurally identical to the server's: same links, same step, same padding, row 50 indistinguishable from row 0 |
| Buttons — 4px, 6/12, one primary, disabled keeps its label | **PASS** | paging control `px-3 py-1.5`, `rounded-control`, secondary; disables **4ms** after the press at opacity 0.5 / `not-allowed` with the label unchanged. (Nit, not filed: no button in the app has a hover state at all — pre-existing, and the Look does not require one) |
| Inputs and inline edit — resting underline, preselect, accent border | **PASS** | `walk_sandbox` note cell: `text-decoration: underline` at rest, opens selected `0..28`, `1px rgb(152,16,250)` border, 4px radius, mono; Escape reverts; a real save landed and survived a reload |
| The four states, mandatory on every data surface | **PASS** | all four reached on the paged surfaces, including the paged leg notes: a failed leg renders red with the database's words plus the fix, an absent leg renders the app's one not-provisioned clause, and the rows still append |
| Motion — 120ms colour, nothing else | **PASS** | `transition-duration: 0.12s`; no skeleton, no counter, no pulse |

### Per-rule verdict — The Feel

| bar | verdict | measurement |
| --- | --- | --- |
| 1 — the question is answered above the fold at 1440x900 | **PASS** | Dashboard 210 · Queues 283 / 485 · Claims' full bucket table 396 · Sources 230 · Cycles 140 · Browse 206 |
| 2 — two queues of equal standing | **PASS** | unchanged by M3 |
| 3 — `in_window` appears nowhere | **PASS** | zero occurrences in rendered text on 11 surfaces, including when forced into the URL |
| 4 — absence is honest | **PASS** | the paged not-provisioned arm is gray, names its object once, and offers no press |
| 5 — provenance at the fact | **PASS** | unchanged by M3 |
| 6 — severity is the registry's word | **PASS** | unchanged by M3 |
| 7 — a settlement resolves in place | **not walkable on this data** (`verdicts` absent) |
| 8 — an irreversible write states its subject first | **not walkable on this data** |
| 9 — keyboard reaches everything, visibly | **PASS** | 8–94 Tab stops per page across six pages, **zero ringless**; the paging control takes `#9810fa` 2px at 1px offset and Enter pages |
| 10 — the investigation never leaves the app | **PASS** | and it survives paging: appended claim rows carry the same `/records/events/<id>` and `/sources?source_id=<id>` links the server's rows do |
| 11 — state lives in the URL | ~~**bar is STALE, app is right — no ticket**~~ → **CLOSED 2026-09-11** | a press changes no URL, a reload returns to the first 50 rows, Back leaves the app. That is ARCHITECTURE.md's dated 2026-09-10 amendment, traced to SPEC F14 and Ben's choice of on-demand client fetching. Recorded as a proposed bar-11 clause for the next revision (report §Proposals 2), NOT filed and NOT edited mid-walk. **Closure (2026-09-11, admin-window/BUG-0217):** the proposed clause was landed by TASK-0079 — LOOK_AND_FEEL bar 11 now carries the paging exception verbatim (Ben's ruling of 2026-09-11), so bar 11 is no longer stale and this surface is graded against the window line and the held count, not the address bar. The struck verdict stands as what the walk saw on its own day; it is not the standing verdict. |
| 12 — both themes, clean console | **PASS** | only ink under 4.5:1 anywhere is the disabled `—` (2.60:1 light, 2.35:1 dark), exempt by job. **Zero** console errors or warnings on load across 8 surfaces x 2 themes x 2 widths, presses included |
| 13 — every windowed list states its window, and the line follows the READ | **PASS**, the milestone's own bar, tried hardest | first screen / continued / exhausted / did-not-fill all correct on both paged surfaces; `held` stays **877** on `/claims` through 17 presses (the count read; paging reads no claim into it); 13 forced refusal arms each left the line's `outerHTML` byte-identical and appended zero rows |
| Zeroes principle | **PASS** | `/cycles`' four health gauges render the empty arm under a window line naming its interval, while the table above shows 69 cycles outside that window. Neither figure pretends to be the other |

### Per-rule verdict — The Voice

| rule | verdict | measurement |
| --- | --- | --- |
| Register — the pipeline's engineer reading you its own logbook | **FAIL x2 → BUG-0197 (P3), BUG-0198 (P3)** | "the **0** longest-waiting are below — the rest are not shown" over a card that says the read found nothing; and "Narrow the view and page the smaller set" on `/browse`, which has no facets (`src/app/browse/page.tsx:218-221`, `scope: null`) |
| Glossary — one name per concept | **PASS** | every banned synonym swept across 11 surfaces: zero hits in the app's own words. Every apparent hit is the database's text (`events.status`, a review item's `what_happened`, an artist named "Next Level K-Pop") |
| Copy bar 1 — a verb plus its object | **PASS** | "Show the next 50 claims" / "Show the next 50 events" |
| Copy bar 2 — an action keeps its name | **PASS** | the label does not change under load |
| Copy bar 3 — names what failed, then what to do | **PASS except BUG-0198** | every refusal arm carries both halves in the right faces (BUG-0196 is which face, not whether) |
| Copy bar 4 — empty and not-provisioned states name what fills them | **PASS** | on every arm reached, including the paged leg note |
| Copy bar 5 — sentence case; identifiers verbatim in mono | **PASS** | |
| Copy bar 6 — ages relative, scheduled times absolute, counts carry their noun; a record page's value column is verbatim | **PASS** | record page shows `2027-05-01T19:00:00+00:00`; `/browse` renders the same instant `2027-05-01 19:00` under a `STARTS (UTC)` header |

### Ledger — what this walk produced

- **Filed: 3.** BUG-0196 (P2, Look/Typography, app-wide error path) · BUG-0197
  (P3, Voice, one window-line arm, carries the byte-pin conversion) · BUG-0198
  (P3, Voice, the ceiling arm's next step).
- **Rules graded: 31** (11 Look, 12 Feel, 8 Voice). **Passed 26**, failed 3,
  not walkable on this data 2, stale-bar-not-a-failure 1.
- **Walk notes closed: 3 of 5.** Note 2 (the `transition-colors` measurement
  trap) confirmed and used. Note 3 (the em dash at narrow widths) **measured at
  1280 and closed — not a defect, do not re-open**. Note 4 (the empty-card
  sentence) **graded FAIL → BUG-0197, with the three byte pins in its scope**.
  Note 5 (`/browse`'s ceiling next step) **graded FAIL → BUG-0198**, scoped with
  both wrappers as the note predicted.
- **Walk note 1 (the control leaving the viewport) is NOT closed and is Ben's.**
  Re-measured on the landed tree, and the two paged surfaces now disagree:
  `/claims`' control goes viewport y=434 → **2,066** with `scrollY` unchanged
  (1,166px below a 900px fold; 2,084 on presses 2 and 3), while `/browse`'s
  stays at **852** because the browser's scroll anchoring holds it. 17 presses
  and 17 scrolls against 2 presses and none. Proposed as a bar in the report;
  no bar invented here and no ticket filed.
- **Not re-filed, verified still open:** BUG-0164, TASK-0060, DEBT-0020.
- **Not filed, recorded:** mixed apostrophe glyphs in the app's own prose
  (U+2019 and U+0027 six words apart in one sentence, 8 curly sites in `src/`) —
  no rule in LOOK_AND_FEEL covers glyphs, so it is a proposed Voice line for the
  next revision, not a BUG. And: no button in this app has a hover state.
- **Provenance checked before filing all three:** every file named returns
  ticket-bearing commits only. No human-lane divergence, no reconciliation.

## ~~For Ben — two endgame decisions the walk left open, and neither is a ticket~~ — BOTH RULED by Ben, 2026-09-11 (architect, 2026-09-11; closed by designer, TASK-0079)

> **Closed 2026-09-11 (designer, TASK-0079).** Ben walked `/claims`' paging on
> his own instance and ruled on both items the same day. Item 1's proposal is
> **superseded** by a stronger rule of his own and is now **LOOK_AND_FEEL Feel
> bar 14**; item 2 is **granted** and bar 11 now carries the dated paging clause.
> Neither is open, neither is a ticket, and neither is re-proposed. Struck, not
> deleted, so the record of how each reached him survives.

The M3 endgame walk filed three bugs and deliberately stopped short of two
things it had no authority to decide. Both are recorded here, both are Ben's,
and neither is being worked: `LOOK_AND_FEEL.md` is human-owned, and inventing
or retiring a bar is not the architect's call any more than it was the
designer's.

**1. A proposed Feel bar: "a control that extends a list is still on screen
after it acts." — SUPERSEDED, see the note below.** No such bar exists today,
so nothing in M3 failed — but the
two paged surfaces now DISAGREE about it, measured on the landed tree at
1440x900: `/claims`' control goes viewport y=434 → **2,066** with `scrollY`
unchanged (**1,166px below a 900px fold**; 2,084 on presses 2 and 3), which is
17 presses and 17 scrolls; `/browse`'s stays at **852** because the browser's
scroll anchoring happens to hold it. Two surfaces, one interaction, opposite
outcomes, and no rule that says which is right. ~~If Ben adopts the bar, it is a
LOOK_AND_FEEL amendment plus one ticket against `/claims`; if he does not, the
disagreement is recorded here and `/claims` is not a defect.~~ The campaign will
not invent the bar to justify a fix.

**Ruled 2026-09-11 (Ben, recorded by TASK-0079).** He wrote a stronger rule than
the one proposed — *"There should not be elements at the bottom of a list this
long. We should put them at the top or on a different page, depending on the
importance…"* — and it is **Feel bar 14** now: not "still on screen after it
acts" but "never below the rows in the first place". The proposal is closed; no
walker re-raises it. The measurements above live in bar 14's own trace. The
`/claims` ticket this implies is deliberately **not** filed, because the fix is
whichever paging shape Ben picks (`tracker/for-human/M3-paging-shape-for-ben.md`).

**2. LOOK_AND_FEEL bar 11 ("state lives in the URL") is STALE against
ARCHITECTURE's dated 2026-09-10 paging amendment. — ANSWERED, see the note
below.** Paging changes no URL, a
reload returns to the first screen, and Back leaves the app — which is the
amendment as written, traced to SPEC F14 and to Ben's own choice of on-demand
client fetching. The app is right and the bar's text is not; the walk graded it
"bar is stale, app is right — no ticket", which is the correct verdict and not
a licence to edit the vision. ~~Until Ben rules, bar 11 is graded against the
2026-09-10 amendment rather than against its own sentence, and every walk that
touches it says so.~~ ~~The cheap fix, if Ben wants one, is a clause on bar 11
naming paging as the exception and what replaces the URL for it.~~

**Ruled 2026-09-11 (Ben, recorded by TASK-0079).** He took the cheap fix. Bar 11
now carries one dated clause naming paging as its exception in the model that
ships today, and naming what stands in for the URL — the **window line** and the
paging state's **held count**. The clause retires itself the day page position
enters the URL, and bar 11's original sentence is untouched. No walk narrates
"bar is stale, app is right" again, and bar 11 is no longer graded against
ARCHITECTURE's amendment in place of its own text.

### Lane ordering on `src/lib/db/result.ts` (architect ruling, 2026-09-11)

BUG-0196 is `depends_on: [DEBT-0020, BUG-0199]` and BUG-0199 is
`depends_on: [DEBT-0020]` — one serial lane on the account's derivation, truth
before cosmetics. DEBT-0020 and BUG-0199 are the same defect seen twice (a
transport failure states its cause sentence twice on every shape node actually
produces) and carry identical touch scopes; BUG-0196 restructures the very
derivation that defect lives in. **Consequence worth seeing:** an M3 P2 now
waits on a `patch`-milestone ticket. Their milestones were left as filed
because DEBT-0020 is already in flight with its original builder resuming on
BUG-0199's bounce, so the wait is short and promoting a deliberately-deferred
debt item into M3's close accounting would cost more than it buys. If that lane
stalls, promoting both to M3 is the lever.

BUG-0197 and BUG-0198 are dispatchable NOW, in parallel with each other, with
TASK-0060 (`src/components/sources/**`, `tests/offline/sources/**` — disjoint)
and with the result.ts lane (disjoint). They share files with BUG-0196 only —
`primitives.test.ts` with BUG-0197, `paging.tsx` + `paging.test.ts` with
BUG-0198 — and BUG-0196 is now chained behind the result.ts lane, so it arrives
after them and rebases on files it does not contend for: different functions
(`stoppedAtTheCeiling` :483 vs the `Refusal` machine arm ~:713) and different
test cases, as the designer's two sibling notes measured.

---

## Verifier verdict — 2026-09-11 (M3 endgame)

Walked on a production build of `bed26500` served on my own bind-probed port 8775 against
staging `ubfjjqlvnpnoborczbdb`, Chromium 1440x900, with a second instance on 8776 pointed
at a local PGRST205 stub on 8777; all three processes mine, all three killed. Launched from
README → STACK.md §5 verbatim, first try. Sandbox reset before and after; residue sweep
green. Full report: `agenticflow/tracker/for-human/M3-verifier.md`. Run log:
`agenticflow/tracker/notes/verify-M3-2026-09-11.md`.

**Verdict: M3 does not close green — EC1 and EC12 are FAIL; the other eleven pass.**
Filed: **BUG-0207 (P0)**, BUG-0208 (P1), BUG-0209 (P1), BUG-0210 (P2), BUG-0211 (P3),
TASK-0078 (P1).

### EC1 — the repo's bars are green, and the live tier is in the gate — **FAIL**

> "`npm run lint` and `npm run build` each exit 0 … and the campaign's test suite passes …
> offline by default … for every ticket closed in M3, if its `touch_scope` matches
> `src/app/**` or `src/components/ui/**` then its `## Checks` block contains an
> `npm run test:live` invocation. Zero exceptions."

Repo root. `npm run lint` **exit 0**; `npm run build` with all four Supabase names unset
**exit 0**. `vitest --project=offline` with the staging names unset: **exit 1** —
`1 failed | 3968 passed`, the red being
`tests/offline/handoff/settle-review-item.test.ts > allocates no code the sibling's tree
holds today`, `expected [ 'KS029','KS030','KS031','KS032' ] to deeply equal []`;
`--project=isolated` red for the same cause. Cause established next door read-only: Ben
copied this campaign's own two handoff migrations into `../kspace Scraper/supabase/
migrations/` at **11:59:31 today** and added the four codes to `tests/helpers/ks_codes.py`
at **11:59:48** — after QA declared dry. The guard cannot tell our own artifact from a
collision → **BUG-0207 (P0)**. Offline-by-default itself holds: identical 3968/1 with the
names set and unset. Ticket clause: **14 exceptions** against "zero" — BUG-0168, 0171,
0172, 0174, 0175, 0176, 0177, 0178, 0180, 0183, 0186, 0191, 0193, TASK-0076 → **BUG-0209**.

### EC2 — M1 and M2 are still green — **PASS** (two findings named against it)

> "the six live parity suites green (`npm run test:live`) … retired-route 404 set still
> 404; the sidebar still exactly six links; `in_window` still zero occurrences in rendered
> text … every surface 200 with `data-state="not_provisioned"` against a database answering
> `PGRST205`, including the new route handler's own surface behavior."

All ten live parity files pass (**120 tests**); the command itself exits 1 on a 60 s
timeout in `edit.live.test.ts` under eleven-file parallelism (45,627 ms solo, green alone,
25 passed) → **BUG-0208**. Nine retired routes **404** authenticated. Sidebar, read from
`nav a` standing on `/`: **exactly six** — Dashboard, Queues, Claims, Sources, Cycles &
runs, Browse. `in_window`: zero in rendered text on six surfaces and on four URLs that
force it (its one occurrence is inside Next's RSC flight payload, not text). Absence proof:
eight surfaces **200 + `not_provisioned`**; `/claims` and `/browse` draw **zero**
`data-paging` nodes; the handler called directly answers 403, failing closed on the absent
allowlist. One thing the stub-based offline proof cannot see: `/claims` also renders
"pending_claims — the query returned no count … a count read requires { head: true, count:
"exact" }", because a real PostgREST HEAD for a missing table carries no body → **BUG-0210**.

### EC3 — the paging contract is amended before the first page diff — **PASS**

> "§4.3's sentence … is gone or superseded in place, and §4.3, §4 rule 1 and §5 each carry
> a dated amendment … the amendment's commit is an ancestor of every commit that touches
> `src/app/claims/page.tsx`, `src/app/browse/page.tsx`, or any file under the new route
> handler's directory, in M3."

§4.3 read kind 3 is headed "AMENDED 2026-09-10" with a "Superseded, in place." blockquote
carrying the old sentence; the dated entry names §4.3, §4 rule 1 and §5 and states the
boundary ("the server decides the size, validates the bound and refuses out loud; the
client decides only WHEN to ask") plus what it does not license. Ancestry checked with
`git merge-base --is-ancestor` from `5e8742de`: **every** M3-era commit on
claims/page.tsx (15), browse/page.tsx (6), both route dirs (3 + 2) and `src/lib/paging` (11)
is a descendant; every non-descendant is dated before the amendment and is not a paging diff.

### EC4 — every claim and every event reachable past the window — **PASS**

> "a paged walk of `/claims` … reaches a claim … beyond the first window's last row, and
> the total number of distinct claim ids … equals the exact count the page's own head
> figure states. No id appears twice; no id is skipped … the same walk on `/browse` …
> each 'more' request is one request … pressing nothing issues none … the first
> server-rendered screen … is byte-identical in its window line, its figures and its row
> set."

Sidebar → Claims → `/claims`, pressing "**Show the next 50 claims**" **17** times without
leaving the page: rows 50 → 877, **877 distinct `data-claim` ids, zero duplicates**, equal
to the head figure "**877 claims in all**" and to the database's own count (877, read over
REST). **17 presses, 17 requests**, offsets 50…850, one per press, **0 before the first
press**. Exhausted line: "877 claims in all, and every one of them is below — the read
found no more." + "All claims in this view are shown."; control gone. Sidebar → Browse:
**2 presses, 2 requests, 120 distinct rows**, equal to `event_listings`' 120. First screen:
server HTML and pre-press DOM carry the same line, the same `held="877"` / `limit="50"` /
`truncated="true"` and the same 50 ids; appended rows land below. Stated for the record:
the first screen's *words* did change during M3 by dated ruling (BUG-0172/0174/0183/0186/
0191/0204), so "byte-identical to what it renders today" cannot be read literally against
the M2 tree; graded on what it protects — paging adds and never rewrites the first screen.

### EC5 — no affordance where it cannot be honoured — **PASS** (all three clauses)

> "on a fixture where the set is exhausted by the first window, the affordance is absent
> and the surface says the set is complete … pointed at a database answering `PGRST205` …
> zero occurrences of its control in the rendered HTML … a refused page request never
> yields a partially extended list."

Standing on `/claims?domain=venues`: 28 rows, **no control**, "The window did not fill — 28
of at most 50 — so it holds all the claims in the venues domain the read found."
`?bucket=agreeing` and `?bucket=standing_disagreement`: 0 rows, no control. On the PGRST205
instance: `/claims` and `/browse` carry **0** `data-paging` and **0** "Show the next". Five
refusal arms driven on the live surface (real 400, forged 400, `not_provisioned`, `error`,
dead transport): **50 rows before, 50 after, every time**, window line byte-unchanged,
each naming its object. Outside these clauses: after a `not_provisioned` answer the control
stays drawn and enabled → **BUG-0211 (P3)**.

### EC6 — the route handler is gated exactly as the pages are — **PASS**

> "an unauthenticated request … redirects or is refused before the handler body runs, on
> every method it accepts … no service-role key, no staging URL … a forged request naming
> a bound outside the handler's allowed range is refused with the reason named, never
> clamped in silence."

Both handlers, no cookie, seven methods each: **307 → `/login?callbackUrl=…` on all 14**,
the same answer the six pages give. 19 built chunks: **0** files containing `SERVICE_ROLE`,
`service_role`, `STAGING_SUPABASE`, `supabase.co` or `eyJ`. Forged bounds (authenticated):
`100050`/`150000` → "the `offset` must be at most 100000"; `49` → "must be at least 50";
`-1`/`abc`/`1e9` → "must be a plain decimal number…"; `100001` → "must be a multiple of
50"; absent → "the request named no `offset`…". Each echoes the bound **as sent**; none
clamped.

### EC7 — no two figures silently disagree — **PASS**

> "the Claims tab gauge's bucket figures and the head counts above them are either equal,
> or each is labelled as the kind of fact it is (window vs. total) in rendered text."

Staging holds 877, so the >1,000 divergence is unreachable live; the labelling is explicit
on one screen: the page's table is headed "**Total counts**" ("…with every claim in it —
nothing above narrows these counts"), the gauge's is headed "**Window counts**" under
"Claims observed since … read to … — a window of at most 1,000 rows", with its card reading
"**Claims in this window** 877". Equal case also holds: 108 + 769 = 877.

### EC8 — every window line names its narrowing — **PASS**

> "`/sources`' two scan-window lines name the narrowing their read carried … do not render
> byte-identical window lines when the facet narrows the read … `readClaimCountSince` and
> the scan it is printed beside carry the same bounds."

`/sources`: "Claims observed since 2026-06-13 19:09 UTC, read to 2026-09-11 19:09 UTC — a
window of at most 1,000 rows". `/sources?source_id=01a01808-…`: "Claims observed **from
source_id 01a01808-…** since …". Not byte-identical. The count beside the scan is labelled
"Awaiting-row claims **in this window** 769 / from 1 source" and declares the same bounds.

### EC9 — concurrent and still refusing — **PASS**

> "`readRowsByIds` issues its chunks concurrently, bounded … a missing table reaches the
> page as `not_provisioned` naming that table … `/claims` warm server time … at or under
> 1.4 s … `/queues` and the review item … neither regresses."

Warm `/claims` over HTTP against the production build, **n=10: min 1.117 s, median
1.235 s, max 1.392 s, zero samples over 1.4 s** — the bar TASK-0062 missed (1.714 s) and
handed to TASK-0074 is met. `/queues` 0.104–0.130 s (vs 0.107 s); review item
0.674–0.885 s (vs 1.301 s). The naming property observed live on four objects:
"pending_claims / sources / observations / verdicts isn't in this database yet".

### EC10 — no schema, no new surface, no sibling write — **PASS in substance**

> "`supabase/migrations/` holds exactly the two app-owned files … `git -C "../kspace
> Scraper" log --all --grep=admin-window` returns zero commits … no ticket … outside this
> repo … sidebar exactly six links; `src/app` gains no page route; `search`, `dial`,
> `threshold` introduce no new operator-facing control."

Two migration files, last touched by `b5666901` (pre-campaign). **62 M3 tickets, zero**
with a touch_scope outside this repo. Nine `page.tsx` routes — the M2 set unchanged; the two
new files are route handlers. `search` **0** occurrences on all six surfaces; `dial` and
`threshold` occur only in explanatory prose and in a review item's own database text;
**zero `<input>` elements** on any surface. **The sibling-grep clause is reported back as
mis-authored**: it returns **one** commit, `43505768` — Ben's own `entity-linking` work,
which merely *names* "the admin-window Admin campaign" in its message. No commit in the
sibling was made by this campaign; a substring grep over messages cannot answer this
question and should not be certified as if it had.

### EC11 — zero residue — **PASS**

> "the live sweep runs green and staging carries no M3 leftovers after the final run …
> no `.env` value appears in any commit, ticket, receipt or evidence file."

Sandbox reset before and after; one real save made and swept; `residue.live.test.ts`
**6 passed** — "16 column(s) scanned across 3 of 3 mapped table(s) … walk_sandbox:
[label=0, note=0]". **The 71 review_items rows opened today are NOT M3's**, established by
reading them rather than assuming: all 71 carry `queue="entity_link"` and a non-null
`external_ref` (`K8vZ917qpo7` and siblings), and `external_ref` is the column the scraper
repo added **today** in its own commit `43505768`
(`20260911000002_a_review_item_can_name_a_reference.sql`); the 72nd row (2026-09-02) has it
null. This campaign has no insert path to `review_items` and writes only `walk_sandbox`;
`settle_review_item` is not installed. They are the `entity-linking` campaign's live work.
Secrets: searched every tracked file, the whole tracker tree and the evidence dir by value
(never printed), plus `git log -S` on the service-role key across all refs — **no commit
and no tracked file carries any `.env` value**. The one match is `AUTH_SECRET`, whose local
value is the placeholder `.env.example` has carried since commit `dcc168eb`; nothing leaked
outward. (For Ben, not a campaign defect: that placeholder means locally minted session
cookies are forgeable by anyone with the repo — a `LAUNCH.md` line, not a ticket.)

### EC12 — the campaign's own bars — **FAIL** (first bullet only)

> "`docs/build_judgments.md` rewritten whole for M3, at most eight entries … Screenshots of
> both paged surfaces … The cross-directory report … Every contract gap hit in M3 is a
> blocked ticket."

`docs/build_judgments.md` still opens "this is the **M1 edition**, written 2026-09-02" and
holds **15** entries; `git log` on it returns one commit, the M1 one → **TASK-0078 (P1)**.
Screenshots: present, mine and the designer's (both dirs gitignored, so the numbers live in
the run log). **Cross-directory report, established today against both trees and the live
database:** `pending_claims.observed_at` — file `20260910000001_a_pending_claim_carries_its
_instant.sql`, now **tracked** in the sibling (it was untracked at the M2 close), and
**installed** (`select=observed_at` answers 200); `verdicts` —
`20260908000001_the_verdict_becomes_a_row.sql`, written into the sibling **11:59 today**,
**untracked**, **not installed** (404 PGRST205); `settle_review_item` —
`20260908000002_the_verdict_settles_the_item.sql`, same timestamp, **untracked**, **not
installed** (absent from the RPC list), with `tests/helpers/ks_codes.py` modified there to
add KS029–KS032 — which is what turned our offline suite red. Blocked-ticket bullet: passes
on available evidence (BUG-0179, BUG-0187, TASK-0062 each carry a blocked question and a
ruling); I found no silent call.

### EC13 — the stop condition — stated

VISION's satisfaction sentence is met on two clauses of three: **the verdict UI is built**
(the review item's close slot renders, and on this data it renders the honest absence
"verdicts isn't in this database yet — it arrives with the scraper repo's migrations"),
**both handoffs are complete** (three artifacts, all three now physically in the sibling;
one applied and committed there, two placed today and unapplied), and **"reviewed" is Ben's
word to say** — the 11:59 copy is the evidence he is mid-review. What remains is what this
file already predicted: Ben's review and install of the two §9 artifacts and the deferred
patch run for tests 6–8, plus the six tickets this walk filed, of which **BUG-0207 must land
first** because until it does every builder's `npm test` is red for reasons unrelated to
their diff. **Recommendation to Ben: do not open an M4.** Five of the six findings are patch
work; M3's own promise — paging that reaches all 877 claims and all 120 events, windowed
figures that no longer lie, and `/claims` answering in 1.2 s instead of 2.3 — is built and
walkable today.

---

## M3 root-cause pass and endgame rulings (architect, 2026-09-11)

45 M3 bugs classified from titles and closing History lines. Full rules are in
`agenticflow/docs/LESSONS.md` (rewritten whole, 13 lines) and
`agenticflow/docs/DECISIONS.md` (six dated entries, same date);
`agenticflow/docs/vision/ARCHITECTURE.md` carries the contract amendments and
Common-violations rows 25 and 26.

**The classes, by what would have prevented each.**

1. **The paged sentence — 16 bugs, the milestone's largest class** (BUG-0168,
   0172, 0174, 0178, 0180, 0183, 0186, 0191, 0192, 0193, 0194, 0197, 0198,
   0201, 0202, 0204). A SEMANTICS corner nobody briefed, found one lane at a
   time: on a paged surface a press is a read, so a server-rendered sentence
   about rows is a constant over changing state. Prevented by a brief, now
   LESSONS 2 — which also carries the two corners under it (a clause naming a
   subject claims that subject's EFFECT, and an unpaged fallback arm is still a
   claim).
2. **The account derivation on `src/lib/db/result.ts` — 9 bugs in one chain**
   (BUG-0170/173/179/181/182/185/187/196/200). A DESIGN nobody wrote: seven
   consecutive QA lanes each anchored one more true question to a derivation
   whose bar was never stated positively. Fixed as design at BUG-0187 and
   recorded as LESSONS 13 — state the property that makes the next instance
   impossible, in the criteria, before fixing the instance.
3. **The machine's face — 4 bugs** (BUG-0175, 0184, 0196, 0200). The app's own
   sentences wearing the mono face reserved for what the database said. Folded
   into LESSONS 6.
4. **Zero over an empty window — 4 bugs** (BUG-0169, 0197, 0203, 0206). Ruled
   rather than patched: the split is figure-versus-block, both current
   behaviours are correct, and the rule is now ARCHITECTURE §8. No ticket.
5. **The cross-repo handoff guard — 3 bugs in one day** (BUG-0207 → 0212 →
   0213). A text-matching oracle for identity. Ruled at BUG-0213 (declared
   `code → meaning` pairs, no corpus matching), promoted to a rule in §10,
   LESSONS 4.
6. **Ordinary mistakes, no class**: BUG-0166/167/171/176/177/188/189/190/195/
   199/205/208/210/211 — paging bounds, sweep hygiene, a cache header, a
   timeout, a type error on a QA pin. Nothing to brief.

**Three machinery gaps, none of them this campaign's code to fix** (each has a
proposal filed; the curator and Ben own them): a QA-committed pin lands on the
run branch without `ci_command` (BUG-0195); `receipt.py` grades in a detached
worktree at HEAD, so a check naming a gitignored evidence path can never be
green and a check naming a tracker file is red until the tracker is committed
(TASK-0062); and the `## Checks` fence is split one command per line, so a
multi-line heredoc is RED whatever the ticket delivered — which cost TASK-0078
a bounce and made BUG-0209's own check unrunnable. Both of those checks were
**verifier-authored**, as was EC10's unfalsifiable sibling grep: three defective
gates from one role in one walk. The authoring rules are LESSONS 12; the
tooling fixes are the human's.

**Recorded, NOT filed — with the trigger that would file them.**

- **BUG-0205's residual.** The record page for a MISSING row still says "an edit
  here is recorded as an admin override" — a promise about a row that is not
  there. P3. Trigger: a reader reaching that page for a real absent id.
- **BUG-0201/0202's residual.** A repeated URL key read two ways in one request
  (the page joins, `droppedParams` takes the first value). Promoted to a RULE
  in ARCHITECTURE §8 and Common violations row 26, not ticketed: today the only
  visible effect is a missing explanatory line on a hand-built URL. Trigger:
  the two readings producing different ROWS, or a third instance.
- **The `/cycles` empty-window sentence said three times per panel.** One rule
  broken (the surface's window line owns the sentence), not three defects;
  ARCHITECTURE §8. Trigger: the next ticket that touches those panels carries
  it as a criterion.
- **`answersFor`, the free-form sentence prop on the shared window-line
  primitive.** It stays free-form: an enum of approved sentences is the
  copy-oracle QA is right to refuse to build. Its guard is the effect-versus-
  presence rule (§4.3), tested where the sentence is COMPOSED, never in the
  primitive. Its one stale comment is filed as DEBT-0022.
- **The 570-vs-569 sibling commit count** in `docs/build_judgments.md`. One
  digit; no conclusion in the report turns on it.

**Filed here, both P3, both patch-run work:** DEBT-0021 (make `authored`
required on the error arm, so the compiler replaces a regex that lives inside a
closed ticket's checks) and DEBT-0022 (three one-line truth residuals: a comment
naming a defect that is fixed, a second spelling of "unnamed source" in the
standing-disagreements gauge, and a count of 1 that reads as a plural).

---

## Routed from Ben's 2026-09-11 paging note — NOT M3 work, and hooks for the M3 retro (strategist, 2026-09-11)

Ben walked `/claims` paging on his own instance and filed four rulings
(`tracker/inbox/2026-09-11-ben-paging-rulings.md`, routed and deleted). Nothing
here is an M3 criterion or a gate on its close; it is recorded so the retro and
the next walker find it.

- **BUG-0216 (P1, patch lane)** — the page at offset 50 returned a claim id the
  first 50 already held (duplicate React key on his dev log), so shipped paging
  can repeat a row and, per React's own warning, omit another. **Retro hook:**
  EC4 grades exactly this ("No id appears twice; no id is skipped") and PASSED
  on ONE run against a production build. A single-run distinct-count proof over
  a table whose 817-row tie straddles every window boundary is the weakest kind
  of proof this milestone shipped, and the retro should say what that costs.
  Note for whoever takes the ticket: the inbox note's likely cause (a missing
  tiebreaker) is ALREADY implemented at `src/lib/db/claims.ts:328-329`, and
  `/browse` carries the same shape — the mechanism is unknown.
- **TASK-0079 (P2, patch lane, designer)** — Ben ruled the two LOOK_AND_FEEL
  questions this milestone left open. "For Ben" item 1's proposed bar is
  SUPERSEDED by his own stronger bar (nothing the operator must find at the
  bottom of a long list); "For Ben" item 2's bar-11 clause is authorised. The
  designer writes both; no src/ fix is filed from either, because the fix shape
  depends on the item below.
- **Page windows (20/50/100) replacing append-on-press: STOPPED for Ben**, as
  milestone-sized — it rewrites SPEC F14 rather than extending it and reopens
  EC4, EC5 and EC7. `tracker/for-human/M3-paging-shape-for-ben.md`. No M4 is
  planned from it; only Ben converts a note into a milestone.

## M3 close queue — architect rulings (2026-09-12)

Everything queued for me since the root-cause pass, ruled in one pass. Each item
says what was decided and where it is recorded; the ones that needed no action
say so, so the dispatcher can clear them.

### Ruled into work

- **BUG-0221 (the paged seam) — SPLIT, and half of it is Ben's.** The SKIP is
  `blocked` on his paging-shape answer: §4.3 kind 3 already declines the
  snapshot promise, and its honest fix (keyset) is the same layer as page
  windows, which are positional by construction — ruling keyset would close the
  door on his open option. The DUPLICATE (one id drawn twice, React's duplicate
  key, the line in his log) is **BUG-0222**, P2 patch, buildable today, fixed in
  the client driver so it survives either shape. Recorded in DECISIONS.md
  (2026-09-12), in `for-human/M3-paging-shape-for-ben.md` (new final section),
  and in ARCHITECTURE's History as a deliberate NON-amendment.
- **The handoff guards leave the every-builder suite — TASK-0080.** Cut by input
  ownership: guards over OUR artifact stay in `tests/offline`, guards that READ
  `kspace Scraper` move to `tests/handoff/**`, a fifth opt-in vitest project
  outside `npm test` and therefore outside `ci_command`. Four triggers in one
  file in one campaign, twice at the moment the campaign SUCCEEDED. Rule written
  into ARCHITECTURE §10.
- **The sibling-code reader — BUG-0225** (P3, depends on TASK-0080, filed from
  QA's unfiled BUG-0220 residuals: CRLF, one-line compound bodies, a bracket
  reset that can INVENT a declaration). Fourth ticket on one reader, so it is
  filed as the class's bar stated positively, not as a fifth instance
  (LESSONS 13). BUG-0220 itself untouched — a QA lane is grading it.
- **`readSettlementReadiness` asks only about the table — BUG-0223** (P3 patch).
  A half-installed world offers an override whose save 503s; readiness becomes
  the conjunction of every object the save calls, and all four worlds get graded
  offline. P3 because the window is an install window and both ends of it are
  honest; filed because nothing grades that state and the next install will hit
  it again.
- **BUG-0210's named residual — BUG-0224** (P2 patch). A host that 404s with
  zero bytes (wrong `SUPABASE_URL`, a proxy) makes `/claims` print a developer
  sentence AND an empty card over a read that failed. Filed as the positive
  property: a read the app cannot classify is a refusal naming its object —
  never a zero, never an empty card, never a sentence about our own call site.
- **`docs/build_judgments.md` — TASK-0081** (P3, M3): §9 artifacts described as
  "not installed" (false since 2026-09-11, measured) and BUG-0210 described as
  patch-lane (it is M3). Two factual corrections, dated; the eight-entry cap and
  every judgment stay as they are.
- **`src/app/claims/page.tsx`'s `head: true` prose — folded into DEBT-0022** by
  `amend-scope`, as ruled at the endgame: same class as that ticket's other
  three residuals (a comment describing a mechanism that no longer exists), one
  more criterion, one more check, destinations still disjoint.

### Ruled into the contract and the briefing

- **A stored check that cannot be RED — promoted at 2** (Common violations row
  27; rule written into ARCHITECTURE §1.2; LESSONS 12 rewritten). `git -C
  "../kspace Scraper" …` and `git status --porcelain -- src tests` are both
  VACUOUS in a receipt worktree — the sibling path does not exist there and the
  tree is always clean. Reach a sibling by ABSOLUTE path; assert a diff as `git
  diff --name-only <merge-base>..HEAD -- <paths>`. **No proposal filed**: this
  is an authoring defect, not a machinery defect — `receipt.py`'s detached
  worktree at HEAD is documented and correct, and the two checks were written
  without being dry-run where they would run.
- **FEAT-0011 criterion 2 — NOT amended, deliberately.** That ticket is closed
  and was graded in a world where the §9 settlement path was absent from
  staging; rewriting the bar a ticket was already graded against is the edit
  this factory must not make (the same rule that decided the BUG-0209 EC1
  ruling four days ago). What changed is the WORLD, and the tree already caught
  up: BUG-0215 made the live tier READ which settlement world it is in, and the
  absence arm is graded offline on fixtures from here on. BUG-0223 completes the
  picture by grading the two mixed worlds nobody had.

### Recorded, no ticket

- **BUG-0216's guard is latent in production.** Measured on this tree: `src/`
  contains **zero** `router.refresh()` call sites (three comments and one test
  mention it; nothing calls it). The re-render path Ben hit was `next dev`'s
  Fast Refresh after a route module compiled on demand. So the `after` bound
  BUG-0216 added is correct and cheap insurance, not a live fix — and the
  production instance of that surface's defect is the population moving
  (BUG-0221/BUG-0222), not the first screen re-rendering. Worth knowing before
  anyone prices a paging change off Ben's log.
- **`LOOK_AND_FEEL` bar 11's non-trigger list names `?columns=`; the parameter
  is `cols`.** P3 cosmetic, in a designer-owned file, so a note rather than an
  edit or a ticket — for the designer's next pass on that bar (it is already in
  their lane via TASK-0079/BUG-0217).
- **Untracked residue in the primary checkout**: `tests/offline/handoff/__probe.test.ts`
  (BUG-0213's repro probe, pointing at a scratch replica that no longer exists)
  is collected by the offline glob THERE but invisible to receipts, which run at
  HEAD — which is why EC11's zero-residue measurement could not see it. Deleting
  it is TASK-0080 criterion 5.
- **BUG-0209's scan residual 1** (a bare-directory `touch_scope` — `[src/app,
  …]` — escapes the scan's trailing-slash regex) is recorded and NOT repaired:
  the check is stored on a closed ticket and `amend-checks` deletes the green
  receipt that ticket earned. The rule that stands is the EC1 ruling's prose;
  the regex fix rides the next scan anyone authors.

### No action — clear these

- **BUG-0219** (venue-provenance live cases reddening under a concurrent writer
  on shared staging): already filed, correctly, as its own P2. The class — live
  proofs against a moving population — is a retro item for the strategist, not
  an architect ruling; nothing to decide until the retro prices it.
- **TASK-0079's bar-11 clause trigger already being true, and M3.md:492 still
  grading bar 11 STALE with no closure note**: the designer's lane (TASK-0079 /
  BUG-0217). `LOOK_AND_FEEL` and the design walk's verdicts are theirs to edit;
  I would be writing in someone else's file.
- **`agenticflow/DIGEST.md:50-51` still pricing bar 14's consequence at one
  surface**: the strategist's M3 close digest. Not a contract, not a ticket.
