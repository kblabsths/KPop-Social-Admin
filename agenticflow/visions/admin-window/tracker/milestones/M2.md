# M2 — the verdict slice, and the campaign's last milestone

Campaign `admin-window`. Slice definition: `agenticflow/docs/vision/ROADMAP.md`.
Behavior: `agenticflow/docs/vision/SPEC.md` (F9–F13, added 2026-09-04).
Acceptance authority: `contracts/admin-build.md`, read 2026-09-01 — its test
numbers are cited as **[test N]**.

**M2 carries zero *installed* schema.** It authors two migrations as handoff
artifacts and installs neither. No migration lands in this repo, no DDL runs
from app code, and no surface may assume `verdicts` or `settle_review_item`
exists. If a criterion below appears to require installed schema, it is
mis-authored — report it, do not install anything.

**The absence is the normal case.** `main` deploys against a database without
those two objects for the whole of M2 and beyond, until Ben installs them. Every
surface M2 builds renders its not-provisioned state there, offers no action it
cannot perform, and does not throw. That is not a fallback; it is the graded
behavior.

**No dials.** Nothing in M2 renders, reads, hand-copies, or is shaped like a
threshold control. Ben builds dials himself after this campaign closes.

---

## Preconditions (human-owned)

M2 cannot reach its live criteria, or its walks, without these. None is a
ticket; all are Ben's.

1. `STAGING_SUPABASE_URL` and `STAGING_SUPABASE_SERVICE_ROLE_KEY` in `.env`
   (names only in `.env.example`). An unset name is a refusal, never a fallback;
   no secret value ever appears in a file, transcript, ticket, or evidence note.
2. The staging Supabase project declared in `agenticflow/docs/SERVICES.md`.
3. **`public.walk_sandbox` pasted into staging** from the ready-to-copy block in
   `agenticflow/tracker/for-human/TASK-0034.md`. Until it exists, the interim
   walk-write exception stands (one field of one existing `groups`/`idols` row,
   noted, restored, swept) and every M2 walk is narrower than it should be.
   Proof of the landed table is TASK-0037, blocked on this.
4. **Ben's answers to the two fork questions** in
   `agenticflow/tracker/for-human/M2-roadmap.md`, before M2 builds.

---

## Exit criteria

### EC1 — the repo's bars are green, and the live tier is in the gate [test 1]

`npm run lint` and `npm run build` each exit 0 with zero errors, and the
campaign's test suite passes. The suite stays **offline by default**: with no
`STAGING_SUPABASE_*` names set it still passes, with staging tests skipped
behind the explicit live marker rather than failing or falling back.

Additionally, and new for M2 (ARCHITECTURE §13.1, DECISIONS 2026-09-03): every
M2 ticket whose touch scope includes a page under `src/app/**` or a shared
render primitive under `src/components/ui/**` carries that page's live suite in
its `## Checks`.

- Machine-checkable: `npm run lint`; `npm run build`; the suite command run once
  with the staging names unset (green, live tests skipped) and once with them set
  (green, live tests run).
- Machine-checkable: for every ticket closed in M2, if its `touch_scope` matches
  `src/app/**` or `src/components/ui/**` then its `## Checks` block contains a
  `npm run test:live` invocation. Zero exceptions.

### EC2 — M1 is still green [tests 1, 2, 3, 4, 5, 7-pre, 9, 10, 11, 12, 13]

Every M1 exit criterion that was PASS at the M1 close is PASS at the M2 close.
M2 rewrites pages M1 shipped; a regression on a shipped surface fails M2.

- Machine-checkable: the six live parity suites green (`npm run test:live`),
  one per page; the retired-route 404 set still 404; the six-link nav still six
  links; `in_window` still zero occurrences in rendered HTML across all
  surfaces.

### EC3 — the `verdicts` migration is authored complete and installable by hand

One migration file, exact content, target path under
`kspace Scraper/supabase/migrations/`, and the apply command Ben runs there,
carried on a blocked handoff ticket in this repo.

- Machine-checkable: the artifact exists as a single fenced SQL block on the
  handoff ticket; the ticket names its target path and its apply command; the
  table it creates carries exactly spec §7's seven columns
  (`verdict_id`, `review_item_id`, `actor`, `action`, `observation_id`, `note`,
  `created_at`) with the stated types, nullability, defaults, FKs, and the
  `action` CHECK covering §7's action names plus `override`; the SQL contains
  `enable row level security` and **zero** `create policy` statements; the SQL
  contains no `json`/`jsonb` column and no `alter table` on any canonical table.
- Machine-checkable: `git -C "../kspace Scraper" status --porcelain` is
  unchanged by this campaign, and `git -C "../kspace Scraper" log --all
  --grep=admin-window` returns zero commits. The migration is **not** written
  into the sibling by anything here.

### EC4 — `settle_review_item` is authored complete, against the installed schema

One migration file, same handoff shape as EC3.

- Machine-checkable: the function is the **only** statement in the artifact that
  writes `verdicts` or sets `review_items.status`; it calls `apply_resolution`
  for the canonical write; it carries the standard revoke pair; it raises on
  `wont_fix` with a null or empty note; its whole body is one transaction (no
  `commit`, no autonomous-transaction construct, no `dblink`).
- Machine-checkable: every identifier the artifact calls or reads
  (`apply_resolution`, the gate function, `observations`, `review_items`, and
  their columns) appears in `kspace Scraper/supabase/migrations/*.sql` with a
  matching arity. A citation that does not resolve there is a blocked question
  for Ben on the ticket, not an invented signature — and an unresolved citation
  fails this criterion.
- Human-checkable at review: Ben reads both artifacts. "Complete and reviewed"
  is VISION's bar and it is his, not the campaign's.

### EC5 — every §7 action exists in the UI as one typed decision [toward test 6]

Standing on a review-item detail with the function present (stub or installed):

- `data_conflict` decision item offers **choose a claimed value** (one control
  per evidence card), **supply a different value**, **keep current & settle**.
- `entity_link` fact item offers **link to an existing entity** and **settle**.
- Signal item offers **`fixed`** and **`wont_fix`**, and `wont_fix` is refused
  by the form without a note.
- Machine-checkable: for each of the three shapes, the rendered close slot
  contains exactly the controls listed above and no others; each control's
  submission produces exactly **one** call to `settle_review_item` with one typed
  decision payload, asserted in the offline suite against a recording stub.
- Machine-checkable: `grep` over `src` finds **zero** writes to `review_items`
  or `verdicts` other than through the one `settle_review_item` call site — the
  same shape of guard `tests/offline/db/one-place.test.ts` already applies, and
  it ships with one input it must flag and one it must not (LESSONS 3).

### EC6 — the override half writes only as an observation [toward test 7]

- `events` and `venues` are in the map and edit through the **override** regime:
  an admin-tier observation through the gate, applied via `apply_resolution`,
  `admin_locked` provenance, an `override` row in `verdicts` with a null
  `review_item_id`.
- Machine-checkable: no `.update(`, `.insert(`, `.upsert(`, `.delete(` anywhere
  in `src` targets `events` or `venues`; the only mutating path for those two
  tables is the `settle_review_item` call. Proved by the structural guard, on
  two fixtures.
- Machine-checkable: a forged PATCH naming a column absent from the map is
  refused server-side with 403 for `events` and `venues` as it already is for
  `groups` and `idols`; a forged PATCH naming a mapped `events` column while the
  function is absent is refused with the reason named, never a direct write.
- Machine-checkable: per-field provenance renders at the field for a
  resolver-owned record, read from `field_provenance`; a pre-cutover record keeps
  the rendering Ben confirmed 2026-09-02.

### EC7 — a reference field is a picker, and a reference renders as a link [toward test 8]

- Machine-checkable: a `kind: reference` field renders a picker, never a text
  input; its submission payload carries the chosen entity's id and its confirmed
  match, asserted against the recording stub; no code path writes a reference
  field's value as a string.
- Machine-checkable: on an `idols` record the group renders as a link resolving
  to that group's record page (200 authed), and on a `groups` record its idols
  are reachable in one click. On the same pages, **no ids or keys are editable**
  — the map is unchanged by this criterion.

### EC8 — the verdict log is visible, and it is not a seventh page

- Machine-checkable: the sidebar holds **exactly six** links, unchanged from M1;
  the verdict log is reachable as a tab, and its URL is a facet of an existing
  page, not a new nav item.
- Machine-checkable: with `verdicts` present (stub), the tab renders rows newest
  first carrying actor, action, item link, observation link, note and time; a
  settled item's detail renders its own verdict inline.

### EC9 — every M2 surface is honest when the schema is absent [test 9]

Pointed at a database lacking `verdicts` and `settle_review_item` — which is
staging today and `main`'s deploy target until Ben installs them:

- Machine-checkable: the review-item close slot, the verdict-log tab, and the
  `events`/`venues` edit surface each render a `data-state="not_provisioned"`
  card naming the missing object (`verdicts`, `settle_review_item`); every page
  answers 200; zero page errors; no control is offered that would call a missing
  function.
- Machine-checkable: the whole M1 absence proof still passes — all seven
  surfaces 200 with `data-state="not_provisioned"` against a database answering
  `PGRST205` to every read.

### EC10 — no schema, no workaround, no sibling write

- Machine-checkable: `supabase/migrations/` in this repo holds exactly the two
  app-owned files it held at the M1 close; `grep -rniE "create (or replace )?(view|table|function)|alter table|drop table" src supabase tests` returns zero hits outside the two handoff artifacts' own ticket bodies.
- Machine-checkable: zero `json` / `jsonb` columns proposed anywhere; zero new
  canonical columns beyond `verdicts`.
- Machine-checkable: no ticket closed in M2 has a `touch_scope` naming a path
  outside this repo; the sibling's git log carries zero `admin-window` commits.

### EC11 — auth holds and no key leaks [test 12]

- Machine-checkable: an unauthenticated request to every **new** M2 page, tab,
  and route redirects to login; a scan of the built client bundles finds no
  service-role key, no staging URL, and none of the literals `SERVICE_ROLE`,
  `service_role`, `STAGING_SUPABASE`.

### EC12 — zero residue [test 13]

- Machine-checkable: the live sweep runs green and staging carries no M2
  leftovers — no observation, no `verdicts` row, no changed catalog value, after
  the final run. Walks write only to `walk_sandbox` once it exists; until then,
  the interim exception's note-restore-sweep is followed and the sweep command is
  run and its output recorded.
- Machine-checkable: no `.env` value appears in any commit, ticket, receipt, or
  evidence file.

### EC13 — the campaign's own bars

- `docs/build_judgments.md` rewritten whole for M2, at most fifteen entries, each
  naming the contract location that was silent.
- Screenshots of every changed surface and of both edit flows, captured at close.
- The cross-directory report: every sibling commit is a declared handoff or a
  noted minor edit; no ticket's touch scope named a sibling path; both §9
  handoffs filed and their state (filed / reviewed / installed) stated.
- Every contract gap hit in M2 is a blocked ticket, not a silent call. Named
  gaps 6, 7 and 8 in SPEC are the ones already visible.

### EC14 — the campaign's stop condition is stated

At the M2 close the verifier states, in one paragraph, whether VISION's
satisfaction sentence is met — "the verdict UI is built and both handoffs are
complete and reviewed" — and the run stops for Ben's verification. **There is no
M3.** A recommendation to continue is a recommendation to Ben, not a milestone
this campaign plans for itself.

---

## Deliberately not graded here

- **Tests 6, 8 and test 7's override half.** They need the two migrations
  installed; they are the deferred patch run's, after Ben installs. A criterion
  above that seems to demand a live §7 settlement is mis-read: EC5–EC7 grade the
  UI's payload and its refusals, never a completed settlement on staging.
- **The two fact-shaped review items.** Staging holds one item, an `entity_link`
  source-pattern signal. Their views are built and graded on fixtures; "built,
  not walkable on this data" is an honest grade, as it was in M1.
- **The door to groups and idols.** ROADMAP question 1, Ben's alone.
- **The `groups.updated_at` fingerprint.** SPEC named gap 8; needs a trigger,
  which is schema, which this repo may never carry.

early_walk: 2026-09-08 fired

---

## Verifier verdict — 2026-09-09

Walked against the walk instance on `http://localhost:8771` (staging
`ubfjjqlvnpnoborczbdb`), playwright/chromium 151, minted cookie, sandbox reset
before and after (`deleted 3, seeded 3, read back and verified`, both times).
Full verdict, criterion by criterion with the route walked for each:
**`agenticflow/tracker/for-human/M2-verifier.md`**. Run log with every command,
quote and measurement: **`agenticflow/tracker/notes/verify-M2-2026-09-09.md`**.

**Shippable, with two defects filed: BUG-0141 (P1), BUG-0142 (P2).** Neither
touches the verdict slice, the schema footprint, the gate or the absence
contract; both are navigation-honesty defects on links the app itself emits.

| criterion | grade | the route walked, and what it showed |
| --- | --- | --- |
| EC1 | not graded here (QA's) | did not re-run the suite; observed zero page/console errors across ~40 loads on 10 routes |
| EC2 | **PASS** | 10 retired addresses → 404 with the retirement sentence; sidebar exactly six links; `in_window` zero in rendered text (3 raw-HTML echoes exist only inside Next's RSC payload when the operator types the URL) |
| EC3 | **PASS** (structural) | verdicts artifact: target path + `supabase db push`, seven columns, action CHECK over §7's names + `override`, `enable row level security`, `create policy` ×0, `jsonb` ×0 |
| EC4 | **PASS** (structural) | settle artifact: target path + apply command, `wont_fix` refused inside the function, no `commit`/`dblink`/autonomous construct |
| EC5 | **built, not walkable** | staging holds exactly ONE review item (entity_link/open). On its detail: WHAT HAPPENED y=84 → **THE CLOSE y=254** → EVIDENCE y=342, and the close offers 0 buttons, 0 inputs, 0 selects |
| EC6 | **PASS** | forged PATCH: unmapped column → 403 naming it; mapped `events.title` / `venues.name` → **503 `settle_review_item is not present in this database`**, no write (title still `XG`); provenance renders `ticketmaster, applied 7d ago` at title/poster_url/starts_at/venue_id and at the venue's four fields |
| EC7 | link half **PASS**; picker half not walkable; **second bullet mis-authored** | `venue_id` renders as a link and resolves to that venue's record (200). `groups`/`idols` have no record surface — Ben's 2026-09-08 no-door ruling — so the idols↔groups clause cannot be graded as written |
| EC8 | **PASS** | pressed the **Verdict log** tab from `/queues` → `/queues?tab=verdict_log`, sidebar still six links; rows-with-stub half needs the table |
| EC9 | **PASS** | close slot, verdict-log tab and both record edit surfaces each render `data-state="not_provisioned"`; every page 200; zero dead controls |
| EC10 | **PASS** | this repo's `supabase/migrations/` still holds exactly its two app-owned files; sibling `git status --porcelain` empty; `log --grep=admin-window` → 0 commits |
| EC11 | **PASS** | 12 unauthenticated addresses all redirect to `/login`; unauth PATCH never reaches the handler; 31 served assets scanned, zero hits for `SERVICE_ROLE`/`service_role`/`STAGING_SUPABASE`/the staging host/`eyJhbGciOi`/`AUTH_SECRET`; Sign out clears the session and re-gates `/queues` |
| EC12 | **PASS** | sandbox back to fixture byte-for-byte; `events.title` `XG`, `venues.name` `OVO Arena Wembley`, newest observation still 2026-09-03; no secret in any artifact |
| EC13 | partly (32 frames captured) | build judgments / cross-directory report are the campaign's close |
| EC14 | **stated** | see the closing paragraph of `M2-verifier.md` |

**Design limit, recorded not filed:** the not-provisioned card names
**`verdicts`** where SPEC F10 says `settle_review_item`. Ruled in DECISIONS
2026-09-08 — a function cannot be probed without calling it, so the surface
probes the table and names what it asked about; the API layer, which can fail
on the call, does name `settle_review_item` (503 above). If anything moves, it
is SPEC F10's sentence.

## Structure walk — 2026-09-09 (architect, M2 close)

The `src/` tree walked against `agenticflow/docs/vision/ARCHITECTURE.md` §3
(module map) and §4 (dependency direction), on run/admin-window, with
BUG-0141 in flight in a separate worktree and excluded from every count.

### Per-rule verdict

| rule | verdict | measurement |
| --- | --- | --- |
| §4 rule 1 — `components/**` never imports `lib/db/**`, never fetches | **PASS** | `grep -rn 'from "@/lib/db' src/components` → **0**; every component takes plain props |
| §4 rule 2 — only `lib/db/**` imports `@supabase/supabase-js` | **PASS** | `from "@supabase/supabase-js"` → **13** files: the 12 under `lib/db/**` plus the named `lib/supabase.ts` exemption. Five further files mention the package **in a docstring quoting this rule** (`browse/views.ts`, `edit/config.ts`, `gauges/gauge.ts`, `gauges/index.ts`, `verdict/decision.ts`) — a word-keyed scan would have called five leaves violations (ledger row 4's lesson, applied to this walk) |
| §4 rule 3 — only `lib/db/client.ts` reads `process.env` for credentials | **PASS**, exemption unchanged | exactly **two** readers of `process.env.SUPABASE_*` in `src/`: `lib/db/client.ts:38-39` and `lib/supabase.ts:3-4`, the one named `CARRIED_OVER` entry in `tests/offline/db/layering.test.ts`. No new second reader |
| §4 rule 4 — only `lib/db/tables.ts` spells a table or view name | **PASS** | the four most-spelled names (`review_items`, `pending_claims`, `resolution_runs`, `event_listings`) as literals under `src/app` + `src/components` → **0**; `T.*` / `FN.*` throughout, and the two file-local identifier components render `T.reviewItems` and `config.table`, never a literal |
| §4 rule 5 — `lib/**` never imports `app/**` or `components/**` | **PASS** | `grep -rn 'from "@/app\|from "@/components' src/lib` → **0** |
| §4 rule 6 — no `src/app/components/` | **PASS** | does not exist |
| §4 rule 7 — leaves sit BELOW `lib/db/**` and never import it back | **PASS on the invariant; the rule's wording was too narrow and is amended** | no leaf imports `lib/db/**`, `@supabase/supabase-js` or `process.env`. But two leaves import a third: `lib/format.ts` and `lib/claims/filters.ts` both import `@/lib/verdict/decision`, which itself imports nothing. §4's diagram drew every leaf as `-> (nothing)`. **Ruled: a leaf may import a leaf, the leaf layer is a DAG, no cycles at any depth including type-only** — same precedent as ledger row 1 (the rule was over-broad, not the code). Those edges are what stops `visibleContent` being answered twice, which is four M2 bugs |
| §4 rule 8 — `next.config.ts` imports the leaf, only the leaf | **PASS** | `lib/edit/config.ts` has **0** import statements, so the build host's one arrow still lands on a file with no module graph behind it |
| §3 module map — the map matches the tree | **FAIL, amended** | five modules the tree holds were absent from the map (`lib/verdict/decision.ts`, `lib/cycles/state.ts`, `lib/sources/names.ts`, `lib/db/verdict.ts`, `lib/gauges/index.ts`), as were the two non-component modules at the components root (`edit-cell-layout.ts`, `edit-refusal.ts`). All recorded. `lib/url/` is the map's one forward entry (BUG-0141) and is now marked as such |
| §4.3 — the four states, and no silently partial read | **FAIL on two surfaces → DEBT-0008** | BUG-0133's two-fact rule lives only in `isBlockNarrowed` (`lib/review/queue-filters.ts`). `src/app/claims/page.tsx:647` (and :232, :681) and `src/app/sources/page.tsx:200` decide from the URL alone, so a facet over an empty set says "nothing matched". Ledger row 14 promoted at 3 → §4.3 gains the rule |
| §11 — machine identifiers verbatim in mono | **structurally FAIL → DEBT-0011** | the rule is hand-spelled `type-data text-ink` **44 times across 24 files**; `src/components/ui/` has fifteen primitives and none is the identifier; two byte-identical file-local components (`TableName` at `records/[table]/[id]/page.tsx:277`, `ReviewItems` at `queues/[reviewItemId]/page.tsx:174`). Rendered output is correct today — this is the structure that produced BUG-0112 → 0120 → 0121, one route at a time. Ledger row 16 promoted at 3 |
| §11 — contract vocabulary is the app's vocabulary, in code | **FAIL → DEBT-0010** | `narrowedTo` exported twice with unrelated types (`components/ui/window-line.tsx:279` joins scope phrases, `lib/db/runs.ts:198` canonicalises the `?source=` facet) and imported by adjacent pages; `isNarrowed` exported twice, and the queues one means two things by arity — its default `within = {}` has **no production caller**, only `tests/offline/queues/filters.test.ts:85,102,143`. Ledger row 18 promoted at 3 |
| §4 rule 7, read forwards — a pure function belongs in the leaf layer | **FAIL → DEBT-0009** | `isRecordId`/`canonicalRecordId` (`lib/db/records.ts:167`) touch no client, no env, no table name, and rule 7 therefore bars every leaf from importing them — which forced the BUG-0141 ruling to hand `source_id` into the leaf as an argument, and left `/claims?record_id=` still carrying BUG-0140's defect one route from where it was fixed. Ledger row 17 promoted at 2 |
| read shape — a read answers what the URL asked | **noted at 3, deliberately NOT promoted → DEBT-0012 (P3)** | BUG-0138 (~14 round trips), BUG-0139 (~9), and `readPopulation` (`lib/db/review-items.ts:203`) issuing all three shape counts on a `?kind=`-narrowed URL. A contract rule here would pin a read shape five of Ben's open rulings may not want (below). Ledger row 19 |
| edit surface — the two inline-edit widgets share their contracts | **FAIL → DEBT-0013** | `EditableCell` and `records/entity-picker` share the status clock (BUG-0111) and the page's one refusal slot (BUG-0119) but not focus: `grep -c '\.focus()'` is **2** in the cell and **0** in the picker. The picker never focuses the panel it opens nor restores focus when it closes, and `PICKER_HINT` promises an unqualified "Escape cancels" that only fires while focus is inside the panel (handler bound at `:643`). LOOK_AND_FEEL bar 9 |

### Walk observation that is NOT a violation, and got no ticket

The evidence tables carry up to **7** columns (`recordColumn` … `payloadColumn`,
`src/components/review/shape-views.tsx:525-532`) inside `DataTable`'s
`overflow-x-auto`, so `observed` and `payload` sit off-screen at common widths
with no affordance saying columns exist there. LOOK_AND_FEEL explicitly
sanctions that scroll — "Tables that exceed their width scroll horizontally
*inside their own border*; the page does not" — and no bar requires a hint, so
filing a DEBT ticket against it would be the architect writing design.
**Routed to the designer's walk jurisdiction**, recorded here so it is not
lost, and NOT counted in any ledger row.

### Filed

DEBT-0008 (M2, P1, behind BUG-0141) · DEBT-0009 (M2, P2, behind DEBT-0008) ·
DEBT-0010 (M2, P2, behind BUG-0141) · DEBT-0011 (patch, P2) ·
DEBT-0012 (patch, P3) · DEBT-0013 (patch, P2).
Ledger promotions: rows 14 (→3), 16 (3), 17 (2), 18 (3); row 19 recorded at 3
and held.

### Housekeeping

**BUG-0142's first check was defective and could never pass.** `npm test` is
`vitest run --project=offline && vitest run --project=isolated`, so npm appends
a path filter to the SECOND command; the isolated project's include glob
(`tests/isolated/**/*.isolated.test.ts`) matches nothing under
`tests/offline/cycles` and exits 1 under any filter. Measured 2026-09-09:
`npm test -- tests/offline/cycles` → **EXIT 1**;
`./node_modules/.bin/vitest run --project=offline tests/offline/cycles` →
**EXIT 0, 2 files / 101 tests**. Amended via `amend-checks`. No other open or
archived ticket carries the form (BUG-0141's `npm run test:live -- <file>` is
a single-project script and is correct).

## Retro notes for the strategist — Ben's open rulings for the next milestone

These are decisions Ben has taken or deferred that shape the next milestone's
scope. Recorded here because they are read-shape rulings and the architect
declined to write any of them into ARCHITECTURE §4.3 in advance (ledger row 19):
a contract rule authored before the answers lands pins a shape the answers may
not want.

1. **Paging past the window, on Claims and Browse** — through **on-demand
   client fetching against a route handler**, not a server round trip per page
   and not an unbounded read. This is the first thing in the campaign that
   crosses §4's "no fetching in components" line and §5's one-async-boundary
   rule, so it needs a design pass before decomposition, not a ticket.
2. **Search** — no surface has one today. Scope, which fields, and whether it
   narrows the same way the facets do (and therefore feeds the two-fact rule
   §4.3 just gained) are all open.
3. **Retention on `runs`** — scraper-side, so it is a sibling-repo change and
   therefore a HANDOFF artifact for Ben, never an edit from this repo. See
   LESSONS 9: it gets replayed against the sibling's real state before it is
   handed over.
4. **The `runs` row-cap horizon on `/sources`** — what `/sources` should say
   once `runs` outgrows `ROW_CAP`, given complete-or-refuse. Interacts with (3):
   retention may make the horizon moot, so sequence them together.
5. **The `/sources` and `/claims` count-read follow-ups** — the residue of
   BUG-0138 and BUG-0139. DEBT-0012 is the third instance of the same shape and
   is filed at P3 against `/queues`; all three want one answer, not three.
6. **BUG-0138's pending A/B decision** (ticket is `blocked`, milestone M2) —
   `observations.observed_at` reached through the `pending_claims` view. This is
   the one on the critical path: DEBT-0008 and DEBT-0009 both land in
   `src/app/claims/page.tsx`, and whichever arm Ben picks decides whether
   `/claims`' population fact (the second fact §4.3 now requires) stays free or
   becomes a read of its own.
