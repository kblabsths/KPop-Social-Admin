---
id: TASK-0009
type: task
status: built
milestone: M1
priority: P1
opened_by: architect
parent: FEAT-0003
children: []
assignee: builder-36
claimed_at: 2026-09-02T12:14:19Z
depends_on: [TASK-0005, TASK-0006, TASK-0007, TASK-0008, TASK-0026]
touch_scope: [src/app/page.tsx, src/lib/db/dashboard.ts, tests/offline/dashboard, tests/live/dashboard.live.test.ts]
force: false
attempts: 1
---
# Dashboard — the breakfast view

## Description
Module: `src/app/page.tsx` + `src/lib/db/dashboard.ts`. Authority: spec section 4 (Dashboard) and section 1; section 6 (the kind is derived in code); `contracts/resolver.md` section 6 (`resolution_runs` columns) and section 11 (`review_items`); SPEC F3; M1 EC3, EC4; acceptance tests 2 and 9; LOOK_AND_FEEL 'Key screens — Dashboard' and quality bar 1.

Renders, above the fold at 1440x900 with no scrolling:

1. **The attention summary, decision and signal counts kept separate** — two counts side by side, each with open count, **max severity** and **oldest age**. The kind comes from `lib/review/shapes.ts` (TASK-0006); no column carries it. Severity is the registry's `low` / `high` verbatim — no score, no rank, no formula.
2. **Last night's cycles and runs** — recent `resolution_runs` and the adapter framework's `runs`, with `error_summary` surfaced verbatim as error lines. A cycle still running (`ended_at` null) and a `skipped` cycle each read as such.
3. **Everything links**: each attention count opens `/queues` filtered to that kind; each cycle or run line opens `/cycles`; an error line reaches the row that produced it. The Dashboard is the entry to the investigation path and never a dead end.

For the adapter `runs` half, show only what the Dashboard needs — source, when, outcome, error line. Which columns the **Cycles & runs page** shows for adapter runs is a separate blocked question (TASK-0015); do not answer it here.

No parked-section surface is rendered: no recommendations, no incidents, no agent runs, no operator, no free-form ticket box.

Every page ticket in this milestone carries the same standing rules (ARCHITECTURE sections 4, 5, 7):
- The page function is the **only** async component on the route; every child is a pure synchronous component taking plain props. That is what lets the tests render `await Page(props)` with `react-dom/server`.
- Read only through `src/lib/db/**`; never build a Supabase client in a page; never spell a table name outside `src/lib/db/tables.ts`.
- Render all four states from the `ui` primitives: loading, empty, **not provisioned** (naming the missing table, gray, never a zero), error (the database's own message). A missing backing table must not throw and must not take the rest of the app down.
- Use tokens and primitives, never raw hex, arbitrary type sizes or ad-hoc radii.
- **State lives in the URL**: every filter, sort and tab is a `searchParams` value and survives the back button.
- Voice: buttons are a verb plus their object; ages are relative with the absolute in the title attribute; scheduled times are absolute UTC with the zone stated once in the column header; machine identifiers render verbatim in mono; empty and not-provisioned states name what the surface holds and what fills it.
- **Nothing settles anything in M1.** No verdict action, no settle control, no fixed/won't-fix, no scaffolding toward them.

## Acceptance criteria
[tests 2, 9; EC3, EC4] Decision and signal counts render as two separate figures, each with open count, max severity and oldest age, and each links to `/queues` filtered to that kind. Recent cycles and runs render newest first with `error_summary` inline. Offline tests render the page function against stub clients and assert: the populated case, the empty case, and the not-provisioned case (200-equivalent markup naming the missing table, nothing thrown). A live-marked parity test asserts every rendered dashboard number against a count the test issues itself. No severity formula, score or computed rank exists. No parked-section surface renders. (human-checkable) The whole view answers 'did anything happen last night, what needs me' without scrolling at 1440x900, at the close-review screenshots.

## Checks
```sh
npm run lint
./node_modules/.bin/tsc --noEmit
npm run build
./node_modules/.bin/vitest run tests/offline/dashboard
bash -c "test -f src/lib/db/dashboard.ts"
bash -c "! grep -rqE 'recommendations|incidents|agent_runs' src/app"
```

## History
- 2026-09-02T05:40:05Z [architect] opened: Dashboard — the breakfast view
- 2026-09-02T08:23:10Z [architect] depends_on: [TASK-0005, TASK-0006, TASK-0007, TASK-0008] → [TASK-0005, TASK-0006, TASK-0007, TASK-0008, TASK-0026]: Adds TASK-0026 (complete reads). Every lib/db list read in this milestone goes through the one readComplete helper — an ok row set is the whole matching set or the read refuses. Without the edge, eight builders who cannot see each other's code each invent a bound; see ARCHITECTURE.md section 4.3.
- 2026-09-02T08:23:40Z [architect] Architect ruling, 2026-09-02 — where the Dashboard's numbers come from, so you do not decide it here.

`readReviewAttention` (TASK-0006) feeds your attention summary's open count, max severity and oldest age. QA found its underlying read unbounded — no `.range()`, no `.limit()`, no `.order()` — so against PostgREST's `db-max-rows` (Supabase default 1000) those three figures would have been WRONG rather than refused: a silent truncation in unspecified order, rendered as a confident number on the one screen whose whole job is "did anything happen last night, what needs me".

TASK-0026 (now a dependency) fixes it in one helper, not here: `readComplete` returns `kind: "error"` — naming the object, the exact count and the cap — whenever the row set was truncated, so **an `ok` result is the whole matching set** and `readReviewAttention` is correct by construction. Its signature does not change.

What that means for `src/lib/db/dashboard.ts`, which is yours:

- **Every list read you write goes through `readComplete`** (ARCHITECTURE section 4.3): `{ count: "exact" }`, an explicit total `.order()` ending in the primary key, `.range(0, cap - 1)`. That covers your `resolution_runs` and `runs` reads. Do not hand-roll a bound and do not call `readRows` for a set you present as complete.
- **`readRows` is the WINDOW read and stays as it is** — the gauges use it correctly (a named, bounded, ordered window whose card states it). Your "last night's cycles and runs" is naturally a window: order newest-first and limit it, and let the section heading say what window it shows. Either helper is legitimate here; what is illegitimate is a number presented as a total that came from a partial set.
- **No figure on this page may be `rows.length` of a read that could have been truncated**, and none may be a `count ?? 0` fallback: BUG-0007's rule (a helper never substitutes a number the database did not give) now applies on the user-visible path too — `readCount` refuses a null count as part of TASK-0026.
- Nothing about your page's markup, links or layout changes. This is entirely about which helper the reads use.
- 2026-09-02T11:28:22Z [dispatcher] Relayed from QA on BUG-0016 (residual): ErrorLine's reading prop is optional, so TypeScript will not catch a page rendering result.message without naming the read. Every error arm the Dashboard (and Queues/Claims/Sources/Cycles) renders must pass reading — the DbResult error arm now carries it. Also: a transport failure's red line carries the client's full details incl. stack frames (~900 chars); the designer will judge the copy at the walk, do not trim it silently.
- 2026-09-02T12:14:19Z [builder-36] claimed (attempt 1)
- 2026-09-02T12:32:08Z [builder-36] claimed → built: - Built `src/lib/db/dashboard.ts` (three reads, reported separately) + `src/app/page.tsx`. Attention: decision/signal open counts as two figures of EQUAL standing (same container, same element, same class), severity `low`/`high` verbatim from the registry, oldest age relative with the absolute in `title`, each card linking to `/queues?kind=<kind>`. Counts come from `readReviewAttention` (readComplete) — a truncated set refuses, never renders a number. Cycles + adapter runs: newest first, `error_summary` inline VERBATIM and linked to its row; a running cycle (`ended_at` null) and a `skipped` cycle each read as such (`data-outcome` is the test seam). Four states per read: absent table -> NotProvisioned naming it, never a 0; every error arm passes `reading` to ErrorLine and carries the client's account untrimmed (asserted: "Caused by" + "makeNetworkError" reach the markup).
- Verified on the rebased tree (rebased onto run/admin-window): `npm run lint`, `./node_modules/.bin/tsc --noEmit`, `npm run build` (exit 0; `/` is `f` = dynamic in the route table), `./node_modules/.bin/vitest run tests/offline/dashboard` (38), `npm test` (825/825), `npm run test:live` refuses by name as designed. Red-then-green observed: collapsing both queue hrefs and dropping the cycles read's order+limit reddens 5 of the new tests.
- Traps / decisions for QA: (1) the adapter-runs half IS rendered, per this ticket's own text (source, when, outcome, error line only); the blocked OPEN-RUNS question is the CYCLES page's column set and is untouched — the read lives in `dashboard.ts`, not `runs.ts`, and reads 6 columns, so `claims_emitted` etc. cannot leak (pinned). (2) Window read (`readRows`, newest-first + limit 6) for cycles/runs, per the architect's ruling — legitimate only because NO figure on the page is computed over those rows; every number is a column of its own row. (3) Two URL seams the sibling page tickets must honor: `?kind=decision|signal` on /queues, `?cycle=<run_id>` / `?run=<run_id>` on /cycles; both degrade to opening the page, never a dead end. (4) `export const dynamic = "force-dynamic"` on `/` — without it Next prerenders the route at build time (no credential) and ships a frozen error page; /queues,/claims,/cycles,/sources are still static placeholders and will need the same when they gain reads. (5) NOT WALKED: no service-role key in local `.env` (STACK section 5), so the 1440x900 above-the-fold bar is budgeted (~730px: 2 cards + 2 six-row tables, error cells `whitespace-nowrap` so a long summary scrolls inside the table's border instead of growing the page), not measured in a browser.
- 2026-09-02T12:32:27Z [dispatcher] landed ticket/TASK-0009 onto run/admin-window as 67bc523
