---
id: BUG-0203
type: bug
status: done
milestone: M3
priority: P2
opened_by: designer
parent: null
discovered_from: usersim:marisa
children: []
assignee: null
claimed_at: null
depends_on: []
touch_scope: [src/components/cycles/cycle-health.tsx, tests/offline/cycles/page.test.ts]
force: false
attempts: 1
resolution: fixed
---
# CYCLE HEALTH renders four calm zeros over a window that holds nothing, two inches under 69 cycles it drops

## Description
Filed from BOTH M3 user-sim walks, independently, 2026-09-11 — the only finding the two strangers hit in the same words.

Marisa (`agenticflow/tracker/for-human/M3-usersim-marisa.md`), who built the pipeline:

> "**CYCLE HEALTH says: cycles in this window `0`, facts examined `0`, writes `0`, errors `0`, '0 cycles reported one,' 'No cycles in this window.'** That block sits maybe two inches under a table listing 69 cycles, five of which died and one of which carries the error `canonical write refused: public.venues holds no row a78dc1c7-…`. The caption does say *'Cycles started since 2026-09-04 17:01 UTC'* and my cycles are all from 09-02/09-03, so the zeros are arithmetically correct. That doesn't help. A wall of `0`s under a heading that says HEALTH reads as *good news* at a glance, and it's the opposite of good news."

Tomas (`…/M3-usersim-tomas.md`), who came to catch the app lying and did not:

> "It is arithmetically correct and its window is named precisely. It still reads, at a glance, as 'everything is fine, nothing is happening' stacked on top of 'here are 69 cycles and some database errors'. If I were checking this page in a hurry I would take away the wrong impression and the page would not have lied to me to do it."

**Expected** — LOOK_AND_FEEL → The Feel → Interaction principles → Zeroes: *"a zero states what it excludes whenever the excluded set is not empty … and never stands bare beside the rows it silently drops."* And the four states: a surface that can render rows renders the **Empty** state — *"naming what the surface will hold and the one thing that fills it"* — rather than a figure derived from a set with no members.

**Found** — `src/components/cycles/cycle-health.tsx` composes `empty` (`holds: "cycles in this window"`, `filledBy: "The resolver wakes on its cron and files a row, and the window fills."`) at `health.cycles === 0` and hands it **only to the two `Distribution`s**. The four `GaugeCard`s above them render regardless, each with a derived sub-line: `0 ran longer than the 15m cadence`, `0 held, and a held fact writes nothing`, `0 applied, 0 escalated, 0 created`, `0 cycles reported one`. So one panel answers the same emptiness two ways in two inches: its distributions say the window holds nothing, its four figures report measurements of it. The panel already owns the honest words and does not use them where the eye lands first.

My own M3 endgame walk graded the Zeroes principle PASS on this exact panel (`agenticflow/tracker/milestones/M3.md`, Design walk, Feel table) on the reasoning that each figure names its own window and neither pretends to be the other. That reasoning is right about truth and wrong about the bar: the bar is about a zero standing beside rows it drops, and 69 such rows are on the same screen. Two document-blind strangers read it the way the bar predicts. This is the walk correcting itself, which is what the blind walks are for.

**What this ticket may NOT do, and why it is written here rather than left to the builder:**
- it may not state how many cycles fall outside the window — that relates two reads and M3 EC7 forbids a rendered sentence asserting a relationship no single read established;
- it may not colour, rank or judge staleness — no staleness threshold exists in this app and DECISIONS 2026-09-02 forbids inventing one (whether the window should widen when the pipeline is stale is Ben's, recorded in the judgment note, not this ticket's);
- it may not change the window line above, which is correct and names its interval.

Provenance checked before filing: `git log src/components/cycles/cycle-health.tsx` returns ticket-bearing commits only (BUG-0116, BUG-0110, BUG-0055, DEBT-0004). Not a human-lane divergence.

## Acceptance criteria
1. Where the cycle-health read found **no cycles in its window** (`health.cycles === 0`), the panel does not render the four `GaugeCard` figures or their derived sub-lines. In their place stands the panel's Empty state, built from the `EmptyWords` the panel already composes — what the surface holds ("cycles in this window") and the one thing that fills it ("The resolver wakes on its cron and files a row, and the window fills.") — so the same emptiness is stated once, not answered two ways on one screen.
2. The `WindowLine` above is untouched: it still names the interval the read carried ("Cycles started since …"), and it is still the first thing under the heading.
3. Where the window holds **one or more** cycles, the panel is byte-identical to what it renders today: four cards, all four sub-lines, both distributions, the duration note and the newest-error line.
4. No new read, no figure from another read, and no sentence relating this window to the table above it (M3 EC7). No colour change, no tone change, no severity, no staleness word: the gray Empty state, as the four-states rule draws it.
5. The two `Distribution`s keep their own empty arms exactly as they are; this ticket adds the missing arm above them and changes nothing below.
6. The offline suite grades it at the surface: a test named `renders no health figure where the window holds no cycles` (that string, so the check below can find it) asserts, on a fixture whose health window is empty, that the four figures are absent and the empty words are present — and, on a fixture with cycles, that all four are present with their sub-lines.
7. Human-check: at 1440x900 on staging, `/cycles` shows no `0` health figure above a table of 69 cycles; what stands there says the window holds no cycles and what would fill it. **(human-checkable)**

## Checks
```sh
npm run lint
npm run build
grep -q 'renders no health figure where the window holds no cycles' tests/offline/cycles/page.test.ts
./node_modules/.bin/vitest run --project=offline tests/offline/cycles/page.test.ts tests/offline/gauges/cycle-health.test.ts
npm run test:live -- tests/live/cycles.live.test.ts
```

## History
- 2026-09-11T17:24:31Z [designer] opened: CYCLE HEALTH renders four calm zeros over a window that holds nothing, two inches under 69 cycles it drops
- 2026-09-11T17:26:03Z [designer] Two independent strangers, same panel, same reading, quoted in the description; that is why this outranks my own endgame PASS on the Zeroes principle. Measured on staging read-only at judgment time (PostgREST, no writes): newest resolution_runs.started_at 2026-09-03T11:22:34Z, 69 cycles on record, gauge window opens 2026-09-04 — so every cycle the table shows falls outside it and the four zeros are exactly as reachable today as the sims found them. No instance started; no screenshot.
- 2026-09-11T17:44:55Z [builder-303] claimed (attempt 1)
- 2026-09-11T17:57:56Z [builder-303] claimed → built: - cycle-health.tsx: at health.cycles === 0 the four GaugeCards and their derived sub-lines are replaced by the panel's own ui/Empty, built from the EmptyWords it already composed (no eyebrow — the Section heading names the surface; no tone, no staleness word). WindowLine, both Distributions, the duration note and the newest-error line are untouched, and the cycles>0 path is unchanged.
- Two files beyond touch_scope, both consequences of the same rule flip: tests/live/cycles.live.test.ts (its health gradeSurface demanded a labelled figure at a counted zero — there is none now; a counted zero is this surface's EMPTY state, and it asserts no figure stands beside the words) and two pins inside the in-scope offline file that encoded 2026-09-10's 'figures still read a real 0 at a counted zero' for this gauge: the real-0 pin now runs over a window that HOLDS a cycle with zeroed counters (LESSONS 7 intact), and the state case now expects health=empty / latency=ok. Latency's half is untouched.
- Decision (mine, either was defensible): the new card carries NO data-gauge-block marker. The marker means 'a block's card, not the read behind the figures'; with no figures left, this card IS that read, so the surface grades empty rather than ok-with-nothing-in-it. Marking it would have kept the old 'ok' but made the live oracle grade a figureless surface green.
- Verified on the rebased tree: npm run lint, npm run build, tsc --noEmit, ./node_modules/.bin/vitest run --project=offline tests/offline/cycles/page.test.ts tests/offline/gauges/cycle-health.test.ts (194 pass), full npm test (3959+8 pass), npm run test:live -- tests/live/cycles.live.test.ts (7 pass; instrumented once to confirm it takes the new empty branch on staging — window since 2026-09-04T17:55Z, 0 cycles in it). New test watched fail on the pre-fix component (figureLabels still contained 'Cycles in this window'), then pass.
- Observed at the surface: production build on scratch port 8781 (bind-probed; my PID, killed), /cycles with a minted cookie against staging — health panel renders window line then 'No cycles in this window / The resolver wakes on its cron…', zero occurrences of the four figure labels, 3 [data-state=empty] cards inside the surface (1 its own + the 2 distributions'), table above holds 69 cycle rows.
- Trap for QA: that one sentence now reads three times in the panel — the distributions already take the panel's words through their state prop, and criterion 5 forbade changing them. If that repetition is wrong, it is a designer call, not this ticket's.
- 2026-09-11T17:58:19Z [dispatcher] landed ticket/BUG-0203 onto run/admin-window as 506512ae
- 2026-09-11T17:59:00Z [qa] built → qa: under attack
- 2026-09-11T18:10:27Z [qa] qa → done: Attacked on the landed tree (506512ae + my pin), criteria re-run before reading the builder's story.

Criteria 1-6 verified: over emptyWindows() the four figure labels are gone from figureLabels(markup) and readNumber throws for each; one [data-state=empty] card of the surface's own stands in their place (two distinct non-empty lines); the WindowLine is still the first element under the heading and still carries data-window-since; over a window holding one cycle all four figures and all four sub-lines return with no card of the surface's own; both Distributions keep their own empty arms. Criterion 7 (human-checkable) measured at the server: rendered src/app/cycles/page.tsx against staging through the live harness at 2026-09-11 18:03 UTC (read-only, no writes) — cycle_health surface state `empty`, ZERO health figures, the words "No cycles in this window / The resolver wakes on its cron…", and 69 cycle rows in the table above it. That is the exact state both user-sims filed from, and it is gone.

Judged the builder's two declared deviations, both SOUND. (a) tests/live/cycles.live.test.ts beyond touch_scope: forced by the rule flip — emptyAtZero now defaults true so the surface is expected `empty` at a counted zero, and `figure:` had to go because gradeSurface's figure check only fires at counted 0, exactly where no figure now exists. The edit is a net STRENGTHENING: the empty arm now asserts readNumber("Cycles in this window") throws, so the case is not vacuous, and the counted>0 parity assertion is untouched. Re-run against staging in the empty state: 7 pass. (b) LESSONS 7 pins: the "real 0 at a counted zero" assertion was not deleted but moved to the only grain where it can still hold for this gauge — a window that HOLDS one cycle whose counters are all zero, four labels each reading a real 0 — and LATENCY_COUNTS keep the original empty-window form. Intact. (c) No data-gauge-block marker on the new card: correct by the marker's own contract (state.tsx: it means "a block's card, not the read behind the figures"); with no figures left the card IS that read, so the surface grades `empty` rather than a false `ok`, and I confirmed HEALTH is graded in exactly one live case which the builder kept non-vacuous.

Probed for the seams: health.cycles===0 is the only trigger and aggregateCycleHealth sets cycles=rows.length, so every other counter and latestError are provably zero/null in that branch (no hidden non-zero figure, no orphan "Newest cycle carrying errors" line — measured on the real staging page too). The panel takes no facet (readCycleHealth({now}) in page.tsx:317), so ?source=/?cycle= cannot reach it; faceted render still names every surface once. No new read, no colour, no tone, no staleness word, no cross-window sentence (EC7) in the rendered text. Full offline suite 3961 pass, isolated 8 pass, lint/tsc/build clean.

BUG-0206 FILED from the same attack, not against this ticket: the panel DIRECTLY BELOW — RESOLUTION LATENCY — still renders "Applies in this window 0", "Unset by a human decision 0", "Applies with no claim found 0" and a dash over a window that holds nothing, on the same screen, two inches under the same 69-row table. Measured on staging: that window holds 0 of the table's 1,712 field_provenance rows, so its zeros exclude 1,712 rows and name none. Same code shape as the bug just fixed (latency.tsx composes the EmptyWords and hands them only to its Distribution and TrendTable). Pinned as a strict it.fails in tests/offline/cycles/page.test.ts, watched RED as a plain it() on this tree first, committed as 239e77c4.

Residuals, not tickets: (1) the panel's two sentences now render three times over an empty window — the surface card plus both Distributions, which take the panel's words through their state prop; criterion 5 forbade this ticket from touching them, and repetition of copy is the designer's call, so it is noted rather than bounced. (2) A CycleHealth value with cycles===0 and latestError!==null would render the empty card above an error line; no path produces one (rows.length is the count), so it is a shape note, not a defect. [resolution: fixed] [receipt green: 5 cmd(s), tree 79e79ff4]
