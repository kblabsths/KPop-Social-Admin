---
id: BUG-0133
type: bug
status: built
milestone: null
priority: P2
opened_by: qa
parent: null
discovered_from: BUG-0131
children: []
assignee: builder-200
claimed_at: 2026-09-09T15:37:34Z
depends_on: []
touch_scope: [src/app/queues/page.tsx, src/lib/review/queue-filters.ts, src/lib/db/review-items.ts, tests/offline/queues/page.test.ts]
force: false
attempts: 1
---
# A queue block blames a filter for a zero its own empty queue produced

## Description
Found attacking BUG-0131 (QA, 2026-09-09), on the surface that fix owns, and it is the state STAGING IS IN RIGHT NOW. BUG-0131 is correctly fixed; this is the same rule at the trigger its structural derivation cannot reach.

**Expected** — LOOK_AND_FEEL, the four states ("a table with no rows and a filter that matched nothing are different states and never share a rendering"), as BUG-0129/BUG-0131 restated it: a block whose rendered set is identical with and without a URL facet may not tell the reader that facet is why it is empty, nor scope its open figure by it.

**Found** — `src/app/queues/page.tsx:305`, `narrowed = isNarrowed(filter, narrowingOfKind(kind))`. `narrowingOfKind` is derived from the SHAPE REGISTRY (`src/lib/review/queue-filters.ts:243-290`), so it answers "can a row of this kind ever carry another value?" — never "does this table hold any row of this kind at all?". When a block's own queue is EMPTY, no facet can have removed a row from it, yet every facet outside the kind's implied set still flips the block to the filtered rendering.

MEASURED, production build of run/admin-window @ e071191 against STAGING (own bind-probed port 8805, minted `authjs.session-token`, 1440x900, read-only — nothing written; server killed after, both PIDs mine; both themes text-identical, zero console errors/warnings). Staging holds 1 open signal and **0 decision items**. DECISION block, sha of `[data-queue="decision"]` innerHTML (first 16 hex):

| URL | decision items | sha | open sub-line |
| --- | --- | --- | --- |
| `/queues` | 0 | `b987b62ded9a4296` | `nothing open` + `No decisions waiting / The resolver files one when...` |
| `/queues?kind=decision` | 0 | `b987b62ded9a4296` (identical — BUG-0129, correct) | same |
| `/queues?shape=data_conflict_fact` | 0 | `73d3bf06c1b8931b` | `nothing open in this filtered view` + `No items matching these filters / Widen a filter above; the 'all' chip on any row shows everything again.` |
| `/queues?queue=data_conflict` | 0 | `73d3bf06c1b8931b` | same |
| `/queues?shape=entity_link_fact` | 0 | `73d3bf06c1b8931b` | same |
| `/queues?status=open` | 0 | `73d3bf06c1b8931b` | same |
| `/queues?kind=signal` | 0 | `73d3bf06c1b8931b` | same |

Ten of the twelve URLs walked put the decision block in the filtered rendering. Every one of those chips is one click from this page's own chip row. Clicking the "all" chip the card names shows **the same zero** — the advice is false, and a whole-queue zero is dressed as a scoped one: the exact two states LOOK_AND_FEEL forbids sharing a rendering.

REPRO (offline, no database, from the repo root):
1. `./node_modules/.bin/vitest run --project=offline tests/offline/queues/page.test.ts -t "holds nothing anyway"`
2. It is landed as `it.fails` and therefore PASSES while the defect is live. Drop `.fails` to see it red — measured: `shape=data_conflict_fact sub-line: expected 'nothing open in this filtered view' to be 'nothing open'`. Same signal BUG-0129's and BUG-0131's own pins used.

Offline measurement behind that pin (`renderQueues(SIGNALS_ONLY, ...)`, a script holding the fixture's 3 signal rows and no decision row):
- `/queues` decision block: `nothing open` + `No decisions waitingThe resolver files one when sources disagree about a fact, or when a record cannot link.`
- `?shape=data_conflict_fact`, `?queue=data_conflict`, `?shape=entity_link_fact`, `?status=open`, `?kind=signal`: all `nothing open in this filtered view` + `No items matching these filtersWiden a filter above; ...`, with `idsIn(...,"decision")` `[]` in every case including the unfiltered one.

NOT A REGRESSION of BUG-0131, and BUG-0131 must not be reopened for it: its own rule is correct and unbroken. Re-driven on this tree, offline and on the walk — 98 multi-facet URLs x both blocks x populated/empty all render identically to unfiltered exactly when every facet value the URL names is implied by the block's kind, and differently otherwise; the signal block's sha is `8161f3e9b728cb99` on `/queues`, `?kind=signal`, `?shape=entity_link_source_pattern`, `?queue=entity_link`, `?kind=signal&shape=entity_link_source_pattern` and the three-facet spelling alike, and `?status=open` correctly keeps its scope claim.

NOTE FOR THE FIX (not a prescription): the page cannot answer this from what it reads today — `listReviewItems(filter)` (`src/app/queues/page.tsx:534,558`) applies the URL filter at the database, so `result.data` never contains the rows the facet removed. Deciding "did this facet actually empty this block" needs the block's own unfiltered population (its kind's count with no URL facet) beside the filtered one. Whatever shape the fix takes, it must not weaken the structural rule: `?status=settled` on a decision queue that HOLDS open rows, and `?kind=signal` on a decision queue that holds decisions, must keep naming their scope (both re-measured green on this tree).

## Acceptance criteria
(to be written)

## Checks
```sh
cd "/Users/ben-m4/Desktop/Coding/KPOP/kspace Admin" && ./node_modules/.bin/vitest run --project=offline tests/offline/queues/page.test.ts tests/offline/queues/filters.test.ts
cd "/Users/ben-m4/Desktop/Coding/KPOP/kspace Admin" && bash -c "rm -rf .next/types && npm run lint && ./node_modules/.bin/tsc --noEmit"
```

## History
- 2026-09-09T15:34:27Z [qa] opened: A queue block blames a filter for a zero its own empty queue produced
- 2026-09-09T15:37:34Z [builder-200] claimed (attempt 1)
- 2026-09-09T15:52:03Z [builder-200] WALK, production build of ticket/BUG-0133 rebased onto run/admin-window @ cc74be1, against STAGING (own bind-probed port 8805 — 8770/8771 untouched; minted `authjs.session-token`; 1440x900; read-only, nothing written, no sandbox reset needed since no save path was walked; server killed after, PID mine; zero console messages and zero page errors in both themes).

Staging still holds 1 open signal and 0 decision items. DECISION block, sha256 of `[data-queue="decision"]` innerHTML (first 16 hex), same method as the filing measurement:

| URL | decision items | sha | open sub-line |
| --- | --- | --- | --- |
| /queues | 0 | b987b62ded9a4296 | nothing open |
| /queues?kind=decision | 0 | b987b62ded9a4296 | nothing open |
| /queues?shape=data_conflict_fact | 0 | b987b62ded9a4296 | nothing open |
| /queues?queue=data_conflict | 0 | b987b62ded9a4296 | nothing open |
| /queues?shape=entity_link_fact | 0 | b987b62ded9a4296 | nothing open |
| /queues?status=open | 0 | b987b62ded9a4296 | nothing open |
| /queues?kind=signal | 0 | b987b62ded9a4296 | nothing open |

All seven are byte-identical to the unfiltered rendering, and b987b62ded9a4296 is the SAME sha the filing measured for /queues — the unfiltered rendering did not move; the five URLs that read 73d3bf06c1b8931b before now read it too. Decision block, verbatim, every URL: figure `Open decisions` `0`; sub-line `nothing open`; card `No decisions waiting` / `The resolver files one when sources disagree about a fact, or when a record cannot link.` No filter blame anywhere.

The true half, on the POPULATED signal block (1 open signal), same run:
- /queues, ?kind=signal, ?queue=entity_link — state=ok, 1 item, `1 high, 0 low` `oldest 7d ago`, unscoped.
- ?kind=decision and ?status=settled — state=empty, 0 items, `nothing open in this filtered view` + `No items matching these filters` / `Widen a filter above; the 'all' chip on any row shows everything again.` The structural rule is intact.

Both themes (color_scheme light and dark, Chromium): every extracted string identical, on all six URLs x both blocks.
- 2026-09-09T15:53:58Z [builder-200] claimed → built: A block's read now carries its own population, so 'my queue is empty' and 'the filter matched nothing' are two facts, not one guess.

- `readReviewQueues` (src/lib/db/review-items.ts) returns the filtered rows BESIDE each kind's whole-table count. Two complete reads (ARCHITECTURE §4.3 kind 1) and only when the URL carries a facet — the bare /queues still reads once and is its own population. Either leg refusing is the page's refusal, so no population is ever a number the app substituted. `isBlockNarrowed(filter, within, {rendered, population})` (queue-filters.ts, pure) is the four-state decision: BUG-0131's structural narrowing AND rendered!==population (rendered is a subset, so equal sizes mean the same set). page.tsx reads through both.
- Verified: red-then-green on QA's pin (dropped `.fails`; observed red first with QA's exact message, 'expected nothing open in this filtered view to be nothing open'). `./node_modules/.bin/vitest run --project=offline tests/offline/queues/page.test.ts tests/offline/queues/filters.test.ts tests/offline/review/review-items.test.ts` 166 pass; `npm run lint`, `tsc --noEmit`, `npm test` (2951+6) and `npm run test:live -- tests/live/queues.live.test.ts` (9) all green on the rebased tree (HEAD 2686116 on run 22a2447). Walk transcribed in History: decision block sha256[:16] b987b62ded9a4296 on all 7 URLs — identical to the unfiltered sha the filing measured; populated signal block still scopes under ?kind=decision / ?status=settled. Mutation-checked both new pin sets (reverting isBlockNarrowed to the old rule reddens 5; making the population read the filtered one reddens 3).
- QA MUST KNOW: I INVERTED two landed BUG-0131-era assertions that asserted the defect on an EMPTY table — page.test.ts 'blames the filters on an empty table when a facet beside the kind is set' (now 'blames NO filter...') and the empty half of the EVERY-value sweep. Both said a facet may be blamed when the table holds nothing; that is exactly what this ticket says is wrong, and their populated halves (which carry the true half of the rule) are untouched. Beyond touch_scope: tests/offline/queues/filters.test.ts and tests/offline/review/review-items.test.ts — the files that own the two modules I extended (no new per-ticket test file). KNOWN BOUNDARY, deliberately not changed: a facet that COULD remove rows of a kind but happens to remove none from a populated block still reads scoped only if the counts differ — i.e. that case is now also unscoped; the structural rule alone can never make a scope claim the counts refuse.
- 2026-09-09T15:54:33Z [dispatcher] landed ticket/BUG-0133 onto run/admin-window as f39f8c4
