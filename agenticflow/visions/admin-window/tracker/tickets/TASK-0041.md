---
id: TASK-0041
type: task
status: done
milestone: patch
priority: P1
opened_by: strategist
parent: null
discovered_from: inbox:2026-09-09-vision-amendment.md
children: []
assignee: null
claimed_at: null
depends_on: []
touch_scope: [agenticflow/docs/vision/STACK.md, agenticflow/tracker/for-human, agenticflow/docs/DECISIONS.md]
force: false
attempts: 1
human_note: The walk instructions no longer tell anyone to edit a real groups or idols row — that permission is written up as withdrawn, with your 2026-09-08 amendment named as the reason.
resolution: fixed
---
# Retire the interim groups/idols walk-write exception from the walk docs

## Description
**The interim walk-write exception is dead, and the docs still teach it.** Ben's amendment of 2026-09-08 struck "groups/idols edit directly within it" from VISION: *"admin edits catalog tables only through the observation pipeline; do not re-implement direct edits."* `STACK.md` §5 step 3's first branch still instructs a walker, whenever `walk_sandbox` is absent from staging, to *"edit one field of one existing `groups`/`idols` row, note the original, restore it in a `finally`, and run the residue sweep"* — and `agenticflow/tracker/for-human/TASK-0034.md` §1 tells Ben the same thing in his own copy. A walker who reads either today writes a real row of a catalog three repos share, under a rule the human has withdrawn.

**The retirement is now the AMENDMENT's, not the paste's.** The old wording made the exception end when `public.walk_sandbox` appears. It ends now, whether or not the table exists, and that is the whole substance of this ticket.

**What is true after it, and must be said plainly:** until Ben pastes the sandbox table, **a save-path walk has no write surface at all.** That is the honest state — a walker states it in the walk report as a narrowed walk and moves on. It is not a licence to write `events` or `venues` (never was), not a licence to insert or delete anywhere, and not a reason to build a workaround, a fixture writer, or a second sandbox. TASK-0037 proves the sandbox once he pastes; that ticket now carries the whole of the save-path walk, which is why it went to P1.

**Retire in place, do not delete.** Replace the interim branches with a dated retirement line naming the 2026-09-08 amendment, so a walker who half-remembers the old permission finds its withdrawal rather than silence. Keep everything the retirement does not touch: the absence test itself (run `tests/walk/reset-sandbox.mts`, read which refusal it gave — that is still how a walker learns which state staging is in, and nothing here flips on a date), the sandbox's paste-ready SQL block and its uuid ids byte-identical, and the two-branch structure of step 3.

**Say the rule once.** The "do not re-implement direct edits" rule belongs to the architect's 2026-09-08 entry in `agenticflow/docs/DECISIONS.md` and its ARCHITECTURE rule, written the same day. Cite it. Only if that entry does not itself retire the walk exception may this ticket add one dated line to DECISIONS superseding the 2026-09-03 ruling — one line, never a restatement of the rule.

**Do not touch:** `.env.example`, port 8771, the sandbox SQL, `VISION.md`, or any code. This is a documentation reconciliation and nothing else.

## Acceptance criteria
1. No branch of `STACK.md` §5 instructs any walker to write a `groups` or `idols` row, under any state of staging; the withdrawn instruction is replaced by a dated retirement line naming the 2026-09-08 amendment, not deleted without trace.
2. `STACK.md` §5 states plainly that until `public.walk_sandbox` exists on staging a save-path walk has no write surface, and that the walk report says so — with the standing bans intact on both branches: never `events` or `venues`, never an insert, never a delete, never a workaround.
3. The absence test still governs which state a walker is in — run the reset tool, read its refusal — and nothing in the file flips on a date; the sandbox SQL block, its uuid ids and the launch/URL lines are byte-identical.
4. `agenticflow/tracker/for-human/TASK-0034.md` §1 records the same retirement in Ben's terms and states that the paste is now the only way a save-path walk gets a surface; the note asks him for nothing new.
5. The rule is stated once: this ticket cites the architect's 2026-09-08 DECISIONS/ARCHITECTURE entry rather than restating it, and adds at most one dated superseding line to `DECISIONS.md` if that entry does not already retire the 2026-09-03 exception.
6. Any command line the edit changes was pasted into a fresh shell and run before it was written down (LESSONS class 6).
7. No file under `src/` or `tests/` changes; `.env.example` and port 8771 are untouched; `agenticflow/docs/vision/VISION.md` is byte-identical before and after.
8. `npm test` green.

## Checks
```sh
bash -c 'npm test'
bash -c 'test "$(shasum agenticflow/docs/vision/VISION.md | cut -d" " -f1)" = 4b934d15c8e415b1745b0473ec89ea8f0fbbd5d2'
```

## History
- 2026-09-09T01:07:49Z [strategist] opened: Retire the interim groups/idols walk-write exception from the walk docs
- 2026-09-09T01:14:01Z [builder-119] claimed (attempt 1)
- 2026-09-09T01:22:15Z [builder-119] claimed → built: Doc-only, 2 files, no src/ or tests/ touched.
- STACK.md §5 (real path agenticflow/visions/admin-window/docs/STACK.md): step 3's absence branch no longer tells a walker to edit a groups/idols row. It now carries a dated 'RETIRED 2026-09-08' paragraph naming Ben's vision amendment (quoted from VISION.md's Amendments list) and citing the architect's 2026-09-08 DECISIONS entry ('direct catalog editing is struck...') rather than restating the rule, then states plainly: no write surface until walk_sandbox exists, so the walk is narrowed to reads/rendering and the walk report says exactly that. Bans repeated on both branches (groups/idols, events/venues, insert, delete, workaround). Three-way branching kept; only the retired sub-bullets and their residue-sweep code block were removed (the sweep command still lives in §5's table, re-captioned, command cell unchanged). The caveats section's two state bullets and closing paragraph rewritten the same way.
- for-human/TASK-0034.md §1 rewritten in Ben's terms (allowlist closed + exception retired by his amendment; the paste is now the only way a save-path walk gets a surface; nothing new asked). The header 'nothing is blocked on it' line updated to say what the paste is now FOR. §2's SQL block, uuids and the walk URL untouched.
Verified on the rebased tree (onto a06f556): npm test exit 0 (58 files/2042 + isolated 6), VISION.md shasum check exit 0, npm run lint exit 0. Programmatic check: every fenced block in both files is byte-identical to HEAD except the one removed residue-sweep block; uuid/URL/port-8771 occurrence counts unchanged; VISION.md byte-identical. No command line in the docs was changed; I re-ran 'env -u SUPABASE_URL -u SUPABASE_SERVICE_ROLE_KEY node tests/walk/reset-sandbox.mts' today to confirm the retained branch-2 text (exit 1, unset-name refusal, MODULE_TYPELESS warning) still holds.
TWO THINGS FOR QA: (1) I added NO line to DECISIONS.md — the architect's 2026-09-08 entry already retires the exception ('the interim walk-write exception dies with the path it depended on'), so criterion 5's 'at most one' is zero. That entry is NOT yet on run/admin-window (it was uncommitted in the primary checkout when I read it); my citations are anchored on VISION.md's landed amendment line so nothing dangles if it lands late. (2) §5's table still names tests/live/residue.live.test.ts; if TASK-0040 deletes that file, that row needs a follow-up edit — I left the command cell untouched deliberately to avoid colliding with TASK-0040.
- 2026-09-09T01:22:34Z [dispatcher] landed ticket/TASK-0041 onto run/admin-window as 7eeee66
- 2026-09-09T01:23:05Z [qa] built → qa: under attack
- 2026-09-09T01:34:45Z [qa] qa → done: attacked: criteria driven on the landed tree before reading the builder's story.

AC1/AC3 — the write authorisation is genuinely dead. All nine `groups`/`idols` mentions left in STACK.md (lines 316, 319, 325, 335, 360, 423, 431, 435, 441) are either the dated RETIRED narrative or a ban; not one is an instruction. Both branches of step 3 AND both caveat bullets carry the standing bans (never groups/idols, never events/venues, never insert, never delete, never a workaround), so there is no branch a walker can land on that leaves the door ajar. The absence test still governs structurally ("Run step 3 first, read what it said, take the matching branch").

AC6 — I ran step 3's block VERBATIM from the primary checkout, since that one command is what the edited paragraph turns on:
  ( set -a && source .env && set +a && SUPABASE_URL=... SUPABASE_SERVICE_ROLE_KEY=... node tests/walk/reset-sandbox.mts )
exit 0: 'walk_sandbox reset on ubfjjqlvnpnoborczbdb.supabase.co (declared staging target "ubfjjqlvnpnoborczbdb"): deleted 3, seeded 3, read back and verified against tests/walk/sandbox-fixture.ts.' The documented MODULE_TYPELESS_PACKAGE_JSON warning appeared on stderr exactly as §5 predicts. No value printed; .env never opened or grepped. The sanctioned write left the table in its canonical three-row seeded state, so no cleanup was owed.

That run is also the finding: the SUCCESS branch is the live one, and three sentences in these two files still assert absence in the present tense (STACK.md:307-311 "Measured 2026-09-04: this is the branch staging is on ... walk_sandbox: not present"; for-human/TASK-0034.md:1 and :5). Filed as BUG-0078 (P2) with the exact repro; its three pins were watched red before filing. NOT bounced onto this ticket: the retirement it was asked for is complete and correct, the stale paragraph is text the ticket explicitly told the builder to keep, and the drift is Ben's 2026-09-08 paste landing after the strategist wrote the premise (corroborated at DECISIONS.md:920, same-day entry).

AC5 — the builder's own flag was that the cited architect entry was not yet on the branch. It is now: `git show HEAD:agenticflow/docs/DECISIONS.md` at 7031bed carries "## 2026-09-08 — direct catalog editing is struck", so the citation resolves and no rule is restated anywhere in STACK.md. Zero DECISIONS lines added by this ticket, which is the correct reading of "at most one".

AC4 — the for-human note's §1 states the retirement in Ben's terms and asks nothing new (its one remaining ask, the paste, is BUG-0078's subject).

AC7 — verified by diff, not by claim: 7eeee66 touches exactly 2 files, neither under src/ nor tests/; .env.example is not in it (its working-tree modification belongs to another lane); VISION.md shasum 4b934d15... unchanged; the 8771 lines are untouched.

§5's residue-sweep row names tests/live/residue.live.test.ts, which exists and is git-tracked today. TASK-0040 is in flight and may delete it, which would leave that row naming a missing file — flagged, deliberately not fixed here.

RECEIPT GREEN on the landed tree: npm test exit 0, VISION shasum exit 0. [resolution: fixed] [receipt green: 2 cmd(s), tree 5e3bbaf8]
