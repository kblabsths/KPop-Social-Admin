---
id: BUG-0012
type: bug
status: done
milestone: null
priority: P2
opened_by: qa
parent: FEAT-0006
discovered_from: BUG-0010
children: []
assignee: null
claimed_at: null
depends_on: []
touch_scope: [src/lib/db/gauges.ts, src/lib/gauges/resolution-latency.ts, tests/offline/gauges]
force: false
attempts: 1
resolution: fixed
---
# Resolution latency counts a verdict unset as an apply the join lost

## Description
`ProvenanceApplyRow` types `field_provenance`'s two nullable keys as non-null, and the latency aggregate then reports every verdict unset as a broken join.

**Authority.** Scraper migration `20260901000005` §1 drops NOT NULL on both columns and states the reader contract in the column comments: `source_id` — 'null on a verdict unset, the one row shape whose authority is a human verdict rather than a winning observation'; `observation_id` — 'null on a verdict unset. A reader takes a null here as "no applied observation"'. `contracts/data-model.md`, Per-field provenance, says the same. `src/lib/gauges/resolution-latency.ts:74-79` documents `unmatchedApplies` as 'applies whose observation was not in the second fetch ... a large value means the join, not the resolver, is what the reader is looking at'.

**Found — the type.** `src/lib/db/gauges.ts:167-168`: `source_id: string; observation_id: string;` against columns that admit null. `readProvenanceApplies` (same file, 331-346) filters on `applied_at` alone, so verdict-unset rows are in the window's row set like any other and the type is a lie about rows the read really returns. Compare `src/lib/browse/rows.ts:60`, which types the same column `string | null` after admin-window/BUG-0010.

**Found — the aggregate.** `src/lib/gauges/resolution-latency.ts:121-127`: `observedAt.get(row.observation_id)` misses for a null key, so `unmatchedApplies += 1`. Measured with one measurable apply (600s) plus one verdict unset (both keys null):
- Expected: `unmatchedApplies` 0 — no observation was lost; the unset never named one.
- Found: `unmatchedApplies` 1, and the unset is also counted in `applies` (2) and `byDomain[0].applies` (2), i.e. inside the population the doc calls 'exactly the population of measurable latencies'.
The latency percentiles themselves are unaffected (`overall.count` 1, `unmeasurable` 1).

**Repro.** `tests/offline/gauges/resolution-latency.test.ts` > 'a verdict unset in the apply window' > 'is not counted as an apply whose observation the join lost' — landed as `it.fails` (strict pin, admin-window/this ticket). Flip it back to `it(` as part of the fix; with the pin left as `it.fails` a correct fix turns it red, which is the signal working, not a second bug. Watched red before pinning: `AssertionError: expected 1 to be +0` at line 286.

**Impact.** The gauge is not yet on a page — `src/app/cycles/page.tsx` is still the placeholder Empty and nothing in `src/` calls `readResolutionLatency` — so the defect reaches an operator the day the Cycles page wires gauge 2, and every verdict unset in the window then reads as a join failure the operator is invited to investigate. That is why it is P2 and not P1.

**Note for the fix.** Scope is the nullability, not the gauge's design: type both columns `string | null`, and decide the unset's place explicitly — either keep it out of the apply population at the read (`.not("observation_id", "is", null)`, which also keeps `applies` honest) or count it as its own kind. It must not land in `unmatchedApplies`. `tests/fixtures/rows.ts` `FieldProvenanceRow` types both columns non-null too and is the reason the pin needs a cast; widening it is part of this fix.

## Acceptance criteria
(to be written)

## Checks
```sh
./node_modules/.bin/vitest run tests/offline/gauges/resolution-latency.test.ts
```

## History
- 2026-09-02T09:54:53Z [qa] opened: Resolution latency counts a verdict unset as an apply the join lost
- 2026-09-02T10:29:27Z [builder-26] claimed (attempt 1)
- 2026-09-02T10:38:23Z [builder-26] claimed → built: FIX (commit 7baa465, rebased on run/admin-window):
- `ProvenanceApplyRow.source_id`/`.observation_id` now `string | null` (migration 20260901000005's column comments quoted as the contract), plus `namesNoObservation()` beside the row shape so the null check has ONE spelling. `FieldProvenanceRow` in tests/fixtures/rows.ts widened to match (tsc --noEmit 0 across all consumers — browse page/read tests already spread-override `source_id: null`, so nothing broke).
- Decided in TypeScript, NOT by filter: `aggregateResolutionLatency` splits the window's decisions before measuring. An unset is in neither `applies`, `byDomain[].applies`, `unmatchedApplies`, nor the spreads' `unmeasurable` — it is counted as its own `verdictUnsets`, overall and per domain. No `.not("observation_id","is",null)` at the read, deliberately: filtering server-side would make that count an unavoidable fabricated 0. Window/truncation untouched (still `readRows` + BUG-0009's clamp, decided on rows the server returned).
- AUDIT of the other five gauges: zero further defects. Against the scraper migrations, `field_provenance` is the only table in any gauge read whose `source_id`/`observation_id` admit null; `observations` (both NOT NULL, 20260818000000:3819/3826), `pending_claims` (projects them straight off `live_pending_claim`), `sources` (PK) and `resolution_runs` (has neither column) are correctly typed. The one other nullable of that family — `review_items.source_id` (20260901000002:41) — is ALREADY `string | null` and is `shapeOf()`'s whole discriminator (src/lib/review/shapes.ts:44,123). Nothing changed outside resolution latency.

VERIFIED on the rebased tree: `vitest run tests/offline/gauges/resolution-latency.test.ts` 20 pass (stored check); `vitest run tests/offline/gauges` 186 pass (was 178 here / 177 at QA); `npm run lint` 0; `tsc --noEmit` 0; `npm test` 709 pass / 32 files; `npm run build` 0.
RED-THEN-GREEN, watched not assumed: with src/lib/gauges/resolution-latency.ts reverted to HEAD (tests unchanged), 7 of the 8 unset cases fail — QA's own pin body reproduces the ticket's signature verbatim, `AssertionError: expected 1 to be +0`. Restored, 20 pass.

FOR QA:
- QA's `it.fails` flipped to a plain `it(`; every pre-existing test name in that file is unchanged (diff of the it/describe titles), 8 cases added, and `grep -rn 'it.fails|it.skip|it.todo|PIN admin-window' tests/offline/gauges` returns nothing.
- Naming trap I hit: the landed guard tests/offline/review/one-place.test.ts:121-123 reddens on ANY `function/const/let/class` name containing `verdict` — my first name `isVerdictUnset` broke `npm test` (not the gauge suite). Renamed to `namesNoObservation`, the same compliance TASK-0007 made for `…RejectionStamps`; the spec noun survives in the OUTPUT field `verdictUnsets` (a property line, not a declaration, and singular so `/verdicts/` misses it). Anyone extending this must check `npm test`, not just tests/offline/gauges.
- Judgement call worth attacking: a domain whose window held ONLY unsets is still listed in `byDomain` with `applies: 0` + `verdictUnsets: n` and null latencies, rather than vanishing. That 0 is a measured count of applies, and the sibling field explains it; dropping the row would hide the unsets from the per-domain breakdown a page renders.
- Files touched beyond touch_scope: tests/fixtures/rows.ts only (the ticket's own 'Note for the fix' names widening it as part of this fix).
- 2026-09-02T10:38:43Z [dispatcher] landed ticket/BUG-0012 onto run/admin-window as 6c83999
- 2026-09-02T10:39:27Z [qa] built → qa: under attack
- 2026-09-02T10:44:23Z [qa] qa → done: ATTACKED, no defect found; closed on my own verification, not the builder's story.

CRITERIA (the ticket's Expected/Found is its bar; the criteria block reads 'to be written'). Stored check re-run VERBATIM on the landed tree 6c83999: './node_modules/.bin/vitest run tests/offline/gauges/resolution-latency.test.ts' exit 0, 20 pass (25 with mine). Expected 'ProvenanceApplyRow ids string|null': found src/lib/db/gauges.ts:179-181, both columns, comments quoting migration 20260901000005:83-84. Expected 'unmatchedApplies 0 for an unset, and the unset out of applies/byDomain[].applies/unmeasurable': found resolution-latency.ts:183-202 splits before measuring; my own aggregate call over 1 apply (600s) + 1 unset gives applies 1, unmatchedApplies 0, overall.count 1, unmeasurable 0, verdictUnsets 1. Fixture widened (tests/fixtures/rows.ts:151-152) so no test casts. No it.fails/it.skip/PIN left in tests/offline/gauges (grep empty).

AUTHORITY CHECKED MYSELF, not taken from the handoff. Migration 20260901000005:83-88 drops NOT NULL on both and its column comments name observation_id as the READER'S discriminator ('a reader takes a null here as no applied observation'); contracts/data-model.md:207-210 says the same; the unset insert (same file:485-497) writes both null, the apply insert (:715-723) takes both off the claim. So namesNoObservation()'s single-column test is contract-backed, not a guess. Spec §5 line 202 is 'observed_at -> outcome latency percentiles, per domain' — a row naming no observation is outside that population, so counting it as its own verdictUnsets rather than as a lost join is the spec's reading. No server-side .not(...is null): pinned at the read, and the count is therefore real.

AUDIT CLAIM RE-VERIFIED against the scraper migrations, column by column, not trusted: field_provenance source_id/observation_id nullable (20260901000005:83-84) -> now string|null; observations observation_id/source_id/observed_at/domain all NOT NULL (20260818000000:3819-3826), entity_id IS nullable but no gauge read selects it; pending_claims.entity_id nullable via observations (20260901000004:106) -> already string|null (db/gauges.ts:230); review_items source_id/domain/entity_id/field all nullable (20260901000002:41-44) -> all four already |null (review/shapes.ts:44-47); sources.source_id and resolution_runs.run_id NOT NULL. The one other module reading field_provenance (src/lib/db/browse.ts:84-85) does not select observation_id. Claim holds.

MY ATTACK (5 cases added to tests/offline/gauges/resolution-latency.test.ts, the file that owns this surface; commit 690e0f0): (1) half-null row source_id set + observation_id null -> unset; (2) reverse half-null -> apply, latency measured — the schema has NO check tying the pair, so both are reachable rows and the discriminator is now pinned both ways; (3) 6-row multi-domain mix: applies+verdictUnsets = rows read, sum(byDomain.applies) = applies, sum(byDomain.verdictUnsets) = verdictUnsets, count+unmeasurable = applies overall and per domain; (4) all-unset window issues NO second query (tablesRead [field_provenance]) yet reports verdictUnsets 1, applies 0 — 0 is honest here, a measured count of applies, not a ratio the null-not-zero policy would forbid; (5) all-unset window filled to limit 3 still reports window.truncated true — truncation stayed on rows the server returned, so BUG-0009's repair survives the split. All 5 pass; lint 0, tsc --noEmit 0, npm test 715 pass / 32 files. verdictUnsets survives one-place.test.ts:126-137 because /verdicts/ is case-sensitive and the declaration regex grades names, not property lines.

RESIDUALS (no ticket, lean): (a) ResolutionLatencyRows.applies is the raw rows INCLUDING unsets while ResolutionLatency.applies is a count EXCLUDING them — documented at resolution-latency.ts:63-70, but a page that renders data.applies.length would over-count by the unsets; trigger = the Cycles page reading rows instead of the aggregate. (b) No CHECK constraint ties field_provenance's two nullable columns; only apply_resolution writes the table today, so a half-null row is schema-legal but unreachable — a second writer would make it a scraper-repo handoff, never an Admin edit. [resolution: fixed] [receipt green: 1 cmd(s), tree e33f9516]
