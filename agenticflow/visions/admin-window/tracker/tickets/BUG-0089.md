---
id: BUG-0089
type: bug
status: qa
milestone: M2
priority: P2
opened_by: qa
parent: FEAT-0010
discovered_from: TASK-0051
children: []
assignee: builder-149
claimed_at: 2026-09-09T04:15:14Z
depends_on: []
touch_scope: [src/components/review/close/actions.ts, src/lib/verdict/decision.ts, tests/offline/review-item/signal-actions.test.ts, tests/offline/verdict/decision.test.ts]
force: false
attempts: 1
---
# A wont_fix note of invisible characters passes all three note guards and settles the item

## Description
**Expected** (spec §7 / SPEC F10 / TASK-0051 criterion 2): `wont_fix` is the one action whose note is the contract — "the note is required: why the condition stands" — and a blank one is refused by the form, by `decisionRefusals`, and by `settle_review_item`. Three guards, deliberately.

**Found:** all three test blankness the same way, and none of them tests it by *visible content*. `closeRefusal` (`src/components/review/close/actions.ts:184`) and `present()` (`src/lib/verdict/decision.ts`, invariant 3) both use `String.prototype.trim()`, which strips the Unicode WhiteSpace set and U+FEFF but NOT the Cf format characters; the SQL artifact uses `v_note ~ '^[[:space:]]*$'` (`agenticflow/tracker/for-human/M2-handoff-settle-review-item.md:295`), whose POSIX class does not match them either. So a note of U+200B (zero-width space), U+2060 (word joiner) or U+00AD (soft hyphen) — the characters an operator gets by PASTING out of a web page or a PDF — is graded as a written note by every guard.

Measured on the landed tree (65e91ef, re-checked on 51c0c3a), `tests/offline/review-item/zz-qa-probe.test.ts` (a scratch probe, deleted; the pins below are its permanent form):

    closeRefusal(wont_fix, note):
      " " NBSP        -> note_required   (refused)
      "　"             -> note_required   (refused)
      "﻿" BOM         -> note_required   (refused)
      "​" ZWSP        -> null            (ACCEPTED)
      "⁠" word joiner -> null            (ACCEPTED)
      "­" soft hyphen -> null            (ACCEPTED)
      "  ​  "         -> null            (ACCEPTED)

    POST /api/admin/review-items/{id}/settle {action:"wont_fix", note:"​", value:null}
      -> 200, and the scripted database recorded 1 call to settle_review_item
         carrying p_decision.note = "​"
      (the four trim()-blank spellings above: 400 note_required, 0 database calls)

**Repro (exact):**
1. Open a signal review item's detail with `verdicts` installed (offline: `tests/offline/review-item/signal-actions.test.ts`'s `closeMarkup({kind:"ok"})`).
2. Paste a single zero-width space into the note field — nothing appears.
3. Press "Close as won't fix".
4. Expected: the form refuses and names the reason, sending nothing. Actual: the request is sent, the route answers 200, and `settle_review_item` is called once with a note of `"​"`.

**Why it is not cosmetic:** the item settles and `verdicts.note` — the permanent record of why the condition stands — holds a string nobody can read. The verdict log then renders it as content, because `isAbsent()` (`src/lib/format.ts:242`) also uses `trim()`: `verdict-log.tsx:171` takes the `isAbsent` branch only for a trim()-blank note, so an invisible one draws a blank `td` with **no em dash** — exactly the rendering admin-window/BUG-0085 was filed to remove.

**Pinned RED before filing**, then flipped to strict `it.fails` in commit 51c0c3a:
- `tests/offline/verdict/decision.test.ts` — "flags a note whose every character is invisible on wont_fix"; observed red: `"​": expected [] to include 'note_required'`.
- `tests/offline/review-item/signal-actions.test.ts` — "refuses a note whose every character is invisible" and "puts no invisible-only note on the wire"; observed red: `expected [ { …(2) } ] to deeply equal []`.

**The fix is one shared blankness test, not three** — the same identifier (`note_required`) already spans the two TS guards, so the predicate belongs beside them once and the SQL's regex widened to match. Flip the three `it.fails` pins back to `it(...)` when it lands. Whether the SQL half is a scraper-repo handoff or an edit to the artifact note in this repo is the architect's call.

## Acceptance criteria
(to be written)

## Checks
```sh
./node_modules/.bin/vitest run --project=offline tests/offline/verdict/decision.test.ts tests/offline/review-item/signal-actions.test.ts
```

## History
- 2026-09-09T04:07:41Z [qa] opened: A wont_fix note of invisible characters passes all three note guards and settles the item
- 2026-09-09T04:15:14Z [builder-149] claimed (attempt 1)
- 2026-09-09T04:26:37Z [builder-149] Where the shared definition landed, and why (the dependency direction, as the ticket asked). `hasVisibleContent` / `visibleContent` live in `src/lib/verdict/decision.ts`. `format.ts` imports them; it cannot be the home, because `decision.ts` must import NOTHING (ARCHITECTURE §4 rule 7, pinned literally by tests/offline/db/layering.test.ts's "imports nothing, in any leaf") while `format.ts` already imports React. A third module both could import would break that same pin for decision.ts, and I would not weaken it. So the only direction a SINGLE definition can point is format.ts -> decision.ts, and that is what landed; the function carries the reasoning in place.

Ink-less is White_Space + Cf + Cc + the four Hangul fillers (U+115F/1160/3164/FFA0), removed ANYWHERE in the string, not only at its ends. Boundary drawn on purpose and stated in the docstring: an assigned printable character is content even when it looks empty — U+2800 BRAILLE PATTERN BLANK stays content, and is a fixture on the must-NOT-flag side in both test files.

For the architect, two things I did not touch. (1) The SQL artifact's `v_note ~ '^[[:space:]]*$'` (M2-handoff-settle-review-item.md) is still trim()-equivalent — third guard, BUG-0088's builder's file, untouched here by instruction; a POSIX class cannot express Cf, so it needs `regexp_replace(v_note, '[[:space:]\u200B-\u200D\u2060\u00AD\uFEFF]', '', 'g')` or an equivalent, and that is an artifact edit + a scraper handoff, not this ticket. (2) `EditableCell.tsx:523` still decides its own blank with `draft.trim()`, so an invisible-only cell value is written to a catalog column as content rather than as null — same defect class, different surface, outside this touch scope and worth its own ticket.
- 2026-09-09T04:26:52Z [builder-149] claimed → built: One definition of blank, by VISIBLE content: `hasVisibleContent`/`visibleContent` in `src/lib/verdict/decision.ts` (ink-less = White_Space+Cf+Cc+Hangul fillers, stripped anywhere in the string). `present()`, `closeRefusal`, `settleBody` and `format.ts`'s `isAbsent` all read it — no copies. Home choice + the two sites I did NOT touch are in the History comment above (SQL artifact is BUG-0088's; EditableCell.tsx:523 still trims).

Verified: the three QA pins flipped `it.fails` -> `it` and RE-OBSERVED RED against the pre-fix tree in a detached worktree — 8 failures, incl. the surface one ('\u200b\u2060\u00ad' rendered as content instead of the em dash in the queues verdict log) — then all green after. On the rebased tree: `vitest run --project=offline tests/offline/verdict/decision.test.ts tests/offline/review-item/signal-actions.test.ts` = 80 passed; full offline 2483 passed + isolated 6 passed + tsc + lint all exit 0 (receipt from a detached _receipt- worktree, removed). Re-rebased onto e545244 after the run branch moved; review-item/format/queues/ui/db re-run there = 738 passed.

Traps for QA: (a) blankness widened for `actor` and `review_item_id` too, not just the note — invisible-only now refuses `actor_required`/`review_item_required`; (b) an OPTIONAL invisible-only note is not refused, it is sent as null (`settleBody`), so `fixed` still settles; (c) a note WITH visible content travels byte-identical — this is a blankness test, not a sanitiser; (d) U+2800 braille blank is content BY RULING (docstring + must-not-flag fixtures), not an oversight. Beyond touch_scope: `src/lib/format.ts`, `tests/offline/format.test.ts`, `tests/offline/queues/verdict-log.test.ts` — the ticket named format.ts's isAbsent as part of the one definition, and the log is where the blank cell was visible.
- 2026-09-09T04:27:15Z [dispatcher] landed ticket/BUG-0089 onto run/admin-window as 92655c4
- 2026-09-09T04:37:28Z [qa] built → qa: under attack
