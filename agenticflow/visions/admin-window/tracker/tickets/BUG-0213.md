---
id: BUG-0213
type: bug
status: open
milestone: null
priority: P2
opened_by: qa
parent: null
discovered_from: BUG-0207
children: []
assignee: null
claimed_at: null
depends_on: []
touch_scope: [tests/offline/handoff/settle-review-item.test.ts]
force: false
siblings: [BUG-0212]
---
# The attribution corpus carries a 2-line paste block, so the sibling allocating ITS next code reddens the offline suite

## Description
QA re-check of admin-window/BUG-0207, 2026-09-11, run/admin-window at d5d36383 (landed).

BUG-0207's fix attributes a spelling next door to us only when it sits inside a verbatim, CONTIGUOUS copy of one of this campaign's paste blocks ('a one-line idiom is not an identity; 776 lines of it in order are', unattributedSpellings/HANDOFF_BLOCKS, tests/offline/handoff/settle-review-item.test.ts:296-345). Measured: of the three code-bearing blocks in M2-handoff-settle-review-item.md, one is 776 lines (the sql migration), one is 25 (ks_codes.py entries) and ONE IS TWO LINES - section 1a's registry paste, the wrapped tail of the sibling's tests/live_safety/test_codes_named_once.py list:
    "KS024", "KS025", "KS026", "KS027", "KS028",
    "KS029", "KS030", "KS031", "KS032",
Two lines is not an identity either, and the second one is the line the sibling's NEXT allocation lands on: that list is written 6, 6, 6, 5, 5, 4 entries to a line (sibling tests/live_safety/test_codes_named_once.py:342-347, read-only), so KS033 goes on the line that has room.

Expected (BUG-0207's own goal: the guard fires on a collision, not on a legitimate next-door event - its WHY IT MATTERS is 'npm test is red for every builder ... precisely BECAUSE the campaign's satisfaction condition is being met'): the sibling adding a code of its own to its registry list leaves the offline suite green; it claims none of our four meanings.
Found: the 2-line block stops matching, the line is reported as a FOREIGN spelling of KS029-KS032, and 'allocates no code the sibling's tree holds today' fails - exit 1, npm test red for every builder in this repo.

REPRO (no write next door; a scratch replica of the sibling, built from read-only copies):
 1. Copy '../kspace Scraper' {supabase/migrations/20260908000001*.sql, 20260908000002*.sql, tests/helpers/ks_codes.py, tests/live_safety/test_codes_named_once.py, tools/staging/harness_objects.sql} into <scratch>/replica at the same relative paths.
 2. Copy tests/offline/handoff/settle-review-item.test.ts to tests/offline/handoff/__probe.test.ts with SIBLING_ROOT rewritten to <scratch>/replica.
 3. Baseline: ./node_modules/.bin/vitest run --project=offline tests/offline/handoff/__probe.test.ts -t 'allocates no code the sibling' -> exit 0 (replica faithful).
 4. In the replica, allocate the sibling's next code the way its own list is written: change '"KS029", "KS030", "KS031", "KS032",' to '"KS029", "KS030", "KS031", "KS032", "KS033",' in tests/live_safety/test_codes_named_once.py and append a KS033 entry to tests/helpers/ks_codes.py.
 5. Re-run step 3 -> exit 1: AssertionError expected [ 'tests/live_safety/test_codes_named_once.py: "KS029", "KS030", "KS031", "KS032", "KS033",' ] to deeply equal [].

The same class as BUG-0212 at a different scale, and it is live: the sibling is mid-campaign next door (its newest migrations are dated 20260911, three days after this campaign's handoffs).

PIN (committed, it.fails / strict xfail so the branch stays green): tests/offline/handoff/settle-review-item.test.ts > 'does not call the sibling's own next code a claim on ours'. Watched red first as a plain it(): 'expected [ "\"KS029\", \"KS030\", \"KS031\", \"KS032\", \"KS033\"," ] to deeply equal []'. The fixer flips it.fails back to it().

Not in scope for the fixer: attribution by path (BUG-0207 rejected it, correctly), re-deriving TAKEN_NEXT_DOOR from the scan, or editing the note's section 1a purely to make the block longer without making attribution itself honest.

## Acceptance criteria
(to be written)

## Checks
```sh
env -u STAGING_SUPABASE_URL -u STAGING_SUPABASE_SERVICE_ROLE_KEY ./node_modules/.bin/vitest run --project=offline tests/offline/handoff/settle-review-item.test.ts
./node_modules/.bin/tsc --noEmit
npm run lint
```

## History
- 2026-09-11T20:53:49Z [qa] opened: The attribution corpus carries a 2-line paste block, so the sibling allocating ITS next code reddens the offline suite
- 2026-09-11T20:53:56Z [qa] vs BUG-0212 (its sibling stamp): BUG-0212 was the LINE corpus letting a stranger's 'using errcode = KS029' pass as ours — a silent MISS, now fixed and pinned. BUG-0213 is the opposite direction of the same fix: the BLOCK corpus contains a two-line block, so a next-door edit that claims none of our meanings is reported as a foreign spelling — a loud FALSE POSITIVE that reddens npm test for every builder. Different arm, different failure mode, same helper (unattributedSpellings/HANDOFF_BLOCKS).
