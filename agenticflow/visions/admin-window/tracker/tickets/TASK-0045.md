---
id: TASK-0045
type: task
status: qa
milestone: M2
priority: P1
opened_by: architect
parent: FEAT-0009
children: []
assignee: builder-135
claimed_at: 2026-09-09T02:36:56Z
depends_on: [TASK-0042]
touch_scope: [agenticflow/tracker/for-human/M2-handoff-verdicts.md, tests/offline/handoff/extract.ts, tests/offline/handoff/verdicts.test.ts]
force: false
attempts: 2
---
# The verdicts table, authored complete as a handoff artifact Ben installs by hand

## Description
**SPEC F9, first half.** Author `verdicts` as exact migration content in `agenticflow/tracker/for-human/M2-handoff-verdicts.md`, carrying: the single fenced `sql` block; the **target path** it takes in `kspace Scraper/supabase/migrations/` (the sibling's own naming convention — a `YYYYMMDDNNNNNN_` prefix and a sentence-shaped name, e.g. `20260908000001_the_verdict_becomes_a_row.sql`); the **apply command** Ben runs there (`supabase db push`, from the scraper repo, which is the only repo that pushes migrations — root CLAUDE.md); and a **citation table** naming, per identifier the SQL calls or references, the sibling migration file that defines it.

**Where the artifact lives, and why it is not a `.sql` file** (DECISIONS 2026-09-08). It is a single fenced `sql` block inside a markdown note under `agenticflow/tracker/for-human/`. A file with a `.sql` extension in this repo is a migration-shaped object a glob, a mis-run `supabase db push`, or an agent looking for "the migrations" can apply; `supabase/migrations/` stays at exactly the two app-owned files and no `.sql` exists anywhere else in the tree. It is also the shipped precedent Ben consumes (TASK-0031, TASK-0034 were pasted from exactly this place) and the digest routes `for-human/` notes to him.

**Author against what is INSTALLED, never against what a contract implies** (SPEC F9). Read `kspace Scraper/supabase/migrations/*.sql` by ABSOLUTE path — `/Users/ben-m4/Desktop/Coding/KPOP/kspace Scraper/supabase/migrations/` — because a receipt runs in `agenticflow/.worktrees/<name>` where `../kspace Scraper` resolves inside this repo (ARCHITECTURE §1.2). An identifier that does not resolve there with matching arity is a BLOCKED QUESTION FOR BEN on this ticket, never a signature invented so the file reads well.

**Nothing here applies anything.** No agent runs either artifact, no ticket's touch scope names a sibling path, and the sibling's working tree and git log stay untouched — two of this ticket's stored checks are exactly that, by absolute path.

**The table — spec §7's seven columns, verbatim, and nothing else:**

| column | type | constraints / default |
| --- | --- | --- |
| `verdict_id` | uuid | PK, `default public.uuid_generate_v7()` |
| `review_item_id` | uuid | nullable, FK -> `public.review_items(review_item_id)` |
| `actor` | text | not null |
| `action` | text | not null, CHECK over the eight action names |
| `observation_id` | uuid | nullable, FK -> `public.observations(observation_id)` |
| `note` | text | nullable (the FUNCTION requires it on `wont_fix`, not the table) |
| `created_at` | timestamptz | not null, `default now()` |

- **The `action` CHECK's value set is `VERDICT_ACTIONS`** from `src/lib/verdict/decision.ts` — the same eight, spelled the same way. This is how SPEC gap 6 is closed by construction: the offline test imports that constant and compares it to the set parsed out of the SQL, so a divergence is a red rather than a discovery at install time.
- **RLS on, ZERO policies**, client roles hold nothing: `alter table public.verdicts enable row level security;` plus the revoke that matches the sibling's house style for a resolver-owned table (read `20260901000002_the_review_item_opens_once_per_subject.sql` §4 and copy its shape — including its reasoning about TRUNCATE, which RLS does not govern).
- **Zero json/jsonb columns. Zero new canonical columns. No `alter table` on any vetted table.** The values a verdict rejected are NOT columns here — they are rejection stamps on the observations, written by the function.
- Column comments in the sibling's voice: it is the only documentation the table will have, and Ben reads this file rather than the code.
- The two nulls are STRUCTURAL and the comments say so: a null `review_item_id` is what makes a row an override; a null `observation_id` is what makes it settle-only.

**The grading instrument** (DECISIONS 2026-09-08 — no psql, no dry-run, no SQL parser; a DEP for one file is not proportionate and SPEC F9 says the bar is a REVIEW bar). This ticket builds it, and the function's artifact reuses it:
- `tests/offline/handoff/extract.ts` — reads a for-human note, asserts it contains **exactly one** fenced `sql` block, returns its text plus its statements. Shared; the next artifact imports it.
- `tests/offline/handoff/verdicts.test.ts` — the structural assertions above, plus balance (even `$$` count, `begin`/`end` matched), plus the `VERDICT_ACTIONS` equality. **Two fixtures, as every guard must have** (LESSONS 3): a doctored block it MUST flag (one with a `create policy`, or a ninth action in the CHECK) and the real one it must NOT.

## Acceptance criteria
1. `agenticflow/tracker/for-human/M2-handoff-verdicts.md` exists and carries exactly one fenced `sql` block, the target path under `kspace Scraper/supabase/migrations/`, the apply command Ben runs there, and a citation table (identifier | what it is | sibling migration file that defines it).
2. The SQL creates `public.verdicts` with exactly the seven columns above, with the stated types, nullability, defaults and both FKs, and no eighth column.
3. The `action` CHECK's value set is exactly `VERDICT_ACTIONS` from `src/lib/verdict/decision.ts`, asserted by importing the constant — not by a hand-copied literal list in the test.
4. The SQL contains `enable row level security` and ZERO `create policy` statements; it revokes from the client roles the way the sibling's own `review_items` migration does.
5. The SQL contains no `json` or `jsonb` column, no `alter table` on any canonical table, no `commit`, no `dblink`, and no autonomous-transaction construct; `$$` occurrences are even and `begin`/`end` are balanced.
6. `tests/offline/handoff/verdicts.test.ts` proves itself on TWO inputs: a doctored block it must flag (a `create policy` line, and separately a ninth action in the CHECK) and the shipped block it must not.
7. Nothing applies it: `supabase/migrations/` in this repo still holds exactly its two files, no `.sql` file exists outside that directory, the sibling's working tree is clean and its log carries zero `admin-window` commits (stored checks, absolute paths).
8. (human-checkable) Ben reads the artifact and calls it complete. That is VISION's bar and it is his.

## Checks
```sh
bash -c 'rm -rf .next/types && npm run lint && ./node_modules/.bin/tsc --noEmit'
./node_modules/.bin/vitest run --project=offline tests/offline/handoff/verdicts.test.ts
bash -c 'test "$(git ls-files "supabase/migrations/*.sql" | wc -l)" -eq 2'
bash -c 'test "$(git ls-files "*.sql" | grep -v "^supabase/migrations/" | wc -l)" -eq 0'
bash -c 'test "$(git -C "/Users/ben-m4/Desktop/Coding/KPOP/kspace Scraper" status --porcelain | wc -l)" -eq 0'
bash -c 'test "$(git -C "/Users/ben-m4/Desktop/Coding/KPOP/kspace Scraper" log --all --grep=admin-window --oneline | wc -l)" -eq 0'
```

## History
- 2026-09-09T01:16:09Z [architect] opened: The verdicts table, authored complete as a handoff artifact Ben installs by hand
- 2026-09-09T01:58:36Z [builder-128] claimed (attempt 1)
- 2026-09-09T02:14:13Z [builder-128] Citation resolution, measured 2026-09-08 from the worktree by absolute sibling path (DECISIONS 2026-09-08 instrument 2). `amend-checks` is architect-only, so these are recorded here rather than stored on the ticket; both exit 0 and neither writes anything:

(a) every migration file cited in §3 of the note exists in the sibling — resolved 6 of 6:
bash -c 'd="/Users/ben-m4/Desktop/Coding/KPOP/kspace Scraper/supabase/migrations"; n=0; for f in $(awk "/^## 3\\./,/^## 4\\./" agenticflow/tracker/for-human/M2-handoff-verdicts.md | grep -oE "[0-9]{14}_[a-z0-9_]+\\.sql" | sort -u); do test -f "$d/$f" || exit 1; n=$((n+1)); done; test "$n" -ge 4'

(b) the three identifiers the SQL actually calls or references resolve there:
bash -c 'd="/Users/ben-m4/Desktop/Coding/KPOP/kspace Scraper/supabase/migrations"; grep -rqE "uuid_generate_v7.\\(\\) RETURNS" "$d" && grep -rqE "review_items_pkey primary key \\(review_item_id\\)" "$d" && grep -rqE "observations_pkey. PRIMARY KEY ..observation_id" "$d"'

The target file the note names (20260908000001_the_verdict_becomes_a_row.sql) is deliberately excluded from (a): it does not exist yet and must not — that is the whole point of the handoff.
- 2026-09-09T02:14:36Z [builder-128] claimed → built: - `agenticflow/tracker/for-human/M2-handoff-verdicts.md`: ONE fenced sql block (spec §7's seven columns verbatim, both FKs to the INSTALLED review_items/observations PKs, RLS on + zero policies, the review_items-style client revoke, notify pgrst), plus target path `20260908000001_the_verdict_becomes_a_row.sql`, the `supabase db push` line Ben runs in the scraper repo, a rollback, and a §3 citation table (identifier | what it is | defining sibling migration).
- `tests/offline/handoff/extract.ts` (shared with the settle_review_item artifact): one tokenizer walk -> one fenced sql block per note, comments stripped, literals emptied, statements split, forbidden-construct scan, $$ and begin/end balance. Not a SQL parser and never claims one.
- `tests/offline/handoff/verdicts.test.ts`: one grader, run on the shipped block (must report nothing) and on FIVE doctored notes (create policy, ninth action, jsonb column, commit, service_role write grant) plus a clean-prose fixture where those words appear only in a comment and a string literal. The action set is compared to VERDICT_ACTIONS IMPORTED from src/lib/verdict/decision.ts, never a copied list.

Verified on the rebased tree (onto 9d92e15): `npm run lint` + `tsc --noEmit` clean; `vitest run --project=offline tests/offline/handoff/verdicts.test.ts` 17/17; `npm test` 60 files / 2156 offline + 6 isolated green; all four stored 'nothing applies it' checks green (two app migrations, no stray .sql, sibling tree clean, sibling log free of admin-window).

TWO THINGS QA SHOULD PROBE:
1. ONE deliberate divergence from the sibling's house grant line: `grant select` alone to service_role, not select/insert/update/delete. Reason in the SQL comment and note §4 — settle_review_item is the only writer (contract §7), it is security definer like apply_resolution (20260901000005), and the deferred proof run's 'grant introspection proving verdicts is written by settle_review_item alone' only passes this way. It COUPLES the two artifacts: the function ticket must not assume a table write grant.
2. Two indexes (created_at desc, review_item_id) beyond the seven columns — not required by the contract, flagged to Ben in note §4 as droppable lines.

Citation resolution was measured by absolute sibling path (6/6 files, 3/3 identifiers); `amend-checks` is architect-only so both commands are in this ticket's preceding comment rather than stored checks.
- 2026-09-09T02:15:03Z [dispatcher] landed ticket/TASK-0045 onto run/admin-window as 8c04a36
- 2026-09-09T02:15:31Z [qa] built → qa: under attack
- 2026-09-09T02:23:20Z [qa] qa → reopened: BUG-0082 filed (P1). Attacked on the landed tree (ba26a22), all work in a detached worktree under evidence/, removed after; no SQL executed anywhere.

STORED CHECKS, all six re-run verbatim, all exit 0: lint+tsc clean; verdicts.test.ts 17/17; supabase/migrations still 2 files; zero .sql outside it; sibling tree clean; sibling log free of admin-window. Full suite on the pinned commit 234291f: 61 files / 2189 offline + 6 isolated, green.

CRITERIA. 1,3,5,6,7 met. 2 met against the contract: seven columns, exact types (uuid/text/timestamptz), both nullables structural, both FKs resolve to the INSTALLED PKs — review_items_pkey(review_item_id) at 20260901000002:52 and observations_pkey(observation_id) at 20260818000000:4570, both uuid, types match. uuid_generate_v7() resolves zero-arg RETURNS uuid at 20260818000000:2973. CHECK's eight literals equal VERDICT_ACTIONS by import, not by copy. No json column, no alter table off its own table, no commit/dblink, 69242 0/0, begin/end 0/0. notify pgrst present (0 of 32 sibling files use it; harmless, delivered at commit). Indexes collide with nothing in the sibling (0 hits) and are droppable as §4 says. source_tier is referenced 0 times — nothing to resolve.

CRITERION 4 IS WHY THIS BOUNCES. It is literally satisfied — the client revoke copies review_items — but the artifact's own guarantee is not. Expected (M2-handoff-verdicts.md:64-72 and §4 item 1): 'service_role holds SELECT and nothing else', with FEAT-0009's second half coupled to it. Found: line 142 revokes anon+authenticated only; line 143 GRANTs select to a role that already holds ALL, because kspace Scraper/supabase/migrations/20260818000000...sql:6850 is 'ALTER DEFAULT PRIVILEGES ... GRANT ALL ON TABLES TO service_role'. A grant cannot narrow. The sibling measured this exact ACL — 20260821000001...sql:30, 'service_role=arwdDxtm/postgres ... target: service_role=r/postgres' — and fixed the identical 'one write path' design with an explicit revoke at :44-46. So verdicts installs writable (incl. TRUNCATE, the very privilege §4's own comment says the revoke exists for) by the key the Admin dashboard holds and already uses on this table (src/lib/db/verdict.ts:210). Silent: db push succeeds. The builder flagged this grant line for QA and asserted the deferred grant-introspection proof 'only passes this way' — it is the reverse.

ATTACK ON THE GRADER (it held). Four doctorings the five shipped fixtures do not cover, each driven through the real grader end-to-end: a SECOND fenced sql block -> throws 'found 2', whole file red; create policy inside a dollar-quoted string -> create_policy, 5 failures (dollar bodies survive emptyLiterals by design); column renamed actor->decided_by -> column_set + 5 failures; action misspelled wont_fix->wontfix -> action_set + 3 failures. The comment/literal fixture stays clean; a policy inside a plain single-quoted literal is correctly NOT flagged. The one blind spot is the BUG's: service_role_write_grant (verdicts.test.ts:239) reads GRANT statements only, so it certifies an ACL that is the opposite of what it grades.

PINNED: verdicts.test.ts carries it.fails('PIN BUG-0082...'), committed as 234291f. Watched red as a plain it (1 failed | 17 passed), and watched it XPASS into red when I doctored the revoke in — strict both ways.

RESIDUALS, not tickets: the §1 rollback 'drop table public.verdicts;' is correct and takes the indexes/comments with it, but is not idempotent (consistent with the file's declared forward-only style) and carries no notify pgrst, so a rollback leaves PostgREST's cache stale and Admin sees a 42P01 rather than the not_provisioned card. Worth one line in §1 when BUG-0082 is fixed.
- 2026-09-09T02:36:56Z [builder-135] claimed (attempt 2)
- 2026-09-09T02:40:21Z [builder-135] claimed → built: ZERO-DIFF re-proof, no change invented. The bounce reason (BUG-0082: service_role kept its inherited ALL because a GRANT cannot narrow) was fixed on the run branch at d215afc/99b058f in these same three files; ticket/TASK-0045 carries no commits of its own and rebases to be byte-identical to the run tip (HEAD == run/admin-window == dc0afc4, same tree 7438ca8). Rebased twice — the run branch advanced mid-verification (dc0afc4, tests/http harness, untouched by me) and everything below was re-run on that final tree.

- All six stored checks exit 0 on dc0afc4: lint + tsc --noEmit clean; verdicts.test.ts 21/21 (was 18 pre-fix); supabase/migrations still exactly 2 files; zero .sql outside it; sibling kspace Scraper tree clean and its log free of admin-window. Full offline suite 61 files / 2215 tests + 6 isolated, green. No SQL executed anywhere; the sibling was only read.
- QA's strict pin is gone as a pin: no it.fails/skip remains in tests/offline/handoff/verdicts.test.ts — the ACL posture is now a plain passing assertion plus four doctored fixtures (pre-fix grant-only block, re-grant after revoke, missing client revoke, unmodellable privilege statement). Watched it red honestly: deleting the service_role revoke line from the shipped block turns the file red 7 failed | 14 passed; restored with git checkout, green again 21/21.
- Note §4 item 1 matches the SQL verbatim: artifact lines 159-161 are 'revoke all ... from anon, authenticated' / 'revoke insert, update, delete, truncate, references, trigger, maintain ... from service_role' / 'grant select ... to service_role', and that privilege list is privilege-for-privilege the sibling's own 20260821000001 lines 44-46. Zero create policy.

QA note: the only residual QA flagged last round (rollback lacking a PostgREST reload) is also fixed in d215afc — §1's rollback now carries notify pgrst, 'reload schema'.
- 2026-09-09T02:40:37Z [dispatcher] nothing to land: ticket/TASK-0045 has no commits and a clean worktree — on to QA as-is
- 2026-09-09T02:40:59Z [qa] built → qa: under attack (fix-scoped re-check, attempt 2)
