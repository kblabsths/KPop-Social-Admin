---
id: BUG-0113
type: bug
status: open
milestone: M2
priority: P2
opened_by: qa
parent: null
discovered_from: BUG-0108
children: []
assignee: null
claimed_at: null
depends_on: []
touch_scope: [src/components/claims/bucket-table.tsx, tests/offline/claims/page.test.ts, tests/fixtures/link-spelling.ts]
force: false
---
# The five bucket links on /claims are drawn as links only in the DOM: the Badge inside re-inks the words and hides the underline, so the rendered pixels are unchanged

## Description
BUG-0108 converted `src/components/claims/bucket-table.tsx`'s anchor to `IN_PAGE_LINK`. The anchor's computed style now satisfies criterion 1 — but the anchor's whole body is a `<Badge>`, and a Badge sets its own ink (`TONE.neutral = "text-ink"`, `src/components/ui/badge.tsx:15`) on an `inline-block` box with its own `bg-chrome` fill. CSS gives the descendant's ink priority over the inherited accent, and the badge's own background paints over the ancestor's underline. Result: the five bucket names on `/claims` render at rest EXACTLY as they did before BUG-0108 — the fix is invisible on the surface it was filed for.

**Expected** (BUG-0108 criterion 1): "Every anchor inside `main` on ... `/claims` (both tabs) ... renders at rest as this app spells a link: accent ink plus an underline, no hover required." And LOOK_AND_FEEL's bar-10 clause: a thing that navigates says it navigates without the pointer.

**Found**, measured in bundled Chromium on a `next build` of run/admin-window at facd060, served on 127.0.0.1:8901 against staging, viewport 1440x900, mouse parked at (0,0), 400ms after load:

| element | computed | rendered |
| --- | --- | --- |
| `main a[data-bucket]` (the anchor) | `color: rgb(152, 16, 250)`, `text-decoration-line: underline` | — |
| its `<span class="type-data inline-block rounded-control bg-chrome px-2 py-0.5 text-ink">` (the glyphs the reader sees) | `color: rgb(30, 41, 57)`, `background-color: rgb(249, 250, 251)`, `text-decoration-line: none`, `display: inline-block` | — |
| crop of the whole anchor, light theme | 0 pixels within 40 of accent `rgb(152,16,250)` | sha1 of the crop is **byte-identical** to the same crop with `style.textDecorationLine='none'` forced on the anchor |
| the same, dark theme | 0 pixels within 40 of accent `rgb(194,122,255)` | byte-identical, 5/5 buckets |
| control — a plain `/browse` title anchor, same treatment | 1000 accent pixels at rest -> 443 with the decoration removed | not identical (the underline is 557 visible pixels) |

All five buckets (`standing_disagreement`, `awaiting_link`, `awaiting_row`, `escalated`, `agreeing`), both themes: `identical_without_underline: true`, `accent_px_at_rest: 0`. Raw measurements: `agenticflow/tracker/evidence/BUG-0108/walk1.json`, `walk2.json` (script `walk2.py`).

These five are the buckets tab's primary navigation — clicking a bucket narrows the claim list — so this is the exact bar-10 cost BUG-0108 was filed to remove, on the page the ticket said carried the most routes off it.

**Repro**
1. `next build && next start` on a free port with staging credentials; sign in with a minted walker cookie.
2. Open `/claims` at 1440x900, do not move the mouse.
3. `getComputedStyle(document.querySelector('main a[data-bucket] span')).color` -> `rgb(30, 41, 57)`; `.textDecorationLine` -> `none`.
4. Screenshot the anchor's bounding box; set `a.style.textDecorationLine='none'`; screenshot again. The two PNGs are byte-identical.

**Why the builder's stated reason does not hold.** The handoff says re-inking the chip is "a shape/colour change criterion 6 forbids". Criterion 6 forbids a change to "ink weight, row height, cell padding or table density" and says explicitly "this changes colour and decoration only" — the badge's ink colour is the one thing criterion 6 leaves open. A fix is available inside the criteria as written (give the badge the link's ink when it is the body of a link, or drop the badge for the bucket name). Note for whoever takes it: `src/components/ui/badge.tsx:4` states "A badge is never interactive" — a bucket name that navigates may not be the right thing to draw as a badge at all, and that judgment belongs with the designer before a builder picks an ink.

**Pin.** `tests/offline/claims/page.test.ts`, "draws the bucket link so the WORDS carry the link's ink, not just the anchor" — a strict `it.fails` pin (it XPASSes red the day this is fixed). It asserts through `expectLinkSpellingReachesTheGlyphs` in `tests/fixtures/link-spelling.ts`, which reads `IN_PAGE_LINK` rather than repeating any class literal. Watched red before pinning: `expected [ 'text-ink' ] to deeply equal []` on `standing_disagreement`.

## Acceptance criteria
1. At rest on `/claims`, in both themes, at 1440x900, with the pointer off the table, each of the five bucket links renders with the app's link spelling on the GLYPHS a reader sees: the accent ink and the underline are visible, not merely computed on the anchor. Measured the way this bug was: a crop of the anchor must NOT be byte-identical to the same crop with the anchor's `text-decoration-line` forced to `none`, and must hold accent-coloured pixels.
2. `tests/offline/claims/page.test.ts`'s pin "draws the bucket link so the WORDS carry the link's ink, not just the anchor" is a plain `it(...)` and green — the `it.fails` marker is removed as part of the fix, never left XPASSing.
3. Nothing else on the surface moves: no change to row height, cell padding or table density on the buckets table (criterion 6 of BUG-0108 still binds).
4. `npm run lint` and `tsc --noEmit` clean; the claims offline suite and the repo-wide link guard green.

## Checks
```sh
bash -c 'rm -rf .next/types && npm run lint && ./node_modules/.bin/tsc --noEmit'
./node_modules/.bin/vitest run --project=offline tests/offline/claims/page.test.ts tests/offline/ui/link-spelling.test.ts
npm run test:live -- tests/live/claims.live.test.ts
```

## History
- 2026-09-09T09:29:09Z [qa] opened: The five bucket links on /claims are drawn as links only in the DOM: the Badge inside re-inks the words and hides the underline, so the rendered pixels are unchanged
- 2026-09-09T09:36:36Z [designer] RULING (a): the bucket is not a badge. Drop the <Badge> wrapper and draw the bucket name as a bare in-page link — <a href={row.href} data-bucket={row.bucket} aria-current={...} className={IN_PAGE_LINK}>{row.bucket}</a> — with NO chip, no fill, no extra span around the words. No type class is added: the DataTable cell already carries `type-data px-2 py-1.5 text-ink` (src/components/ui/data-table.tsx:169), so the bucket stays mono as a machine identifier and the anchor's own text-accent + underline reach the glyphs. This is exactly how the app's other navigating labels are drawn: queue-list.tsx:124 (item summary), registry-table.tsx:37 (source name), evidence-cells.tsx:93/149, claims/page.tsx:390 — all bare anchors with IN_PAGE_LINK, none wearing a chip. bucket-table.tsx is the sole outlier in src/, and the fix is to stop being one.

WHY NOT (b), a link-flavoured badge variant. Two reasons, both from the file. (1) badge.tsx's contract is the design language's, not the component's: LOOK_AND_FEEL, Chips and badges — 'A *badge* is not interactive'. A badge that navigates contradicts the rule the whole component exists to carry, and 'bucket' appearing in that rule's parenthetical is about the bucket that CLASSIFIES a claim row (claim-list.tsx:135), not one that navigates. (2) A link-flavoured variant would be a second spelling of a link. links.ts is explicit that the app has exactly one — 'a second spelling is a second answer to "does this text go somewhere", and BUG-0099 is what the second answer cost'. I will not add the second answer to close a bug caused by one. Filter-chip flavour (the Look's one interactive chip) is rejected for the same reason plus a sharper one: its inactive state is chrome fill + secondary text, which at rest is indistinguishable from the neutral bucket badges on the same page — that is BUG-0113 again with a different class list.

NOTHING NEW SITS BESIDE IT. The count and age already have their own columns (claims, oldest, sources); no badge is added anywhere. The row keeps aria-current and data-bucket unchanged.

NOT AN INCONSISTENCY TO CHASE: claim-list.tsx:135 keeps its non-interactive bucket Badge. Same noun, different job — badge classifies, link navigates — and that split is now written into LOOK_AND_FEEL so a future walker reads it as the rule working rather than as drift.

DOC CHANGE (committed with this ruling): LOOK_AND_FEEL.md 'Chips and badges' now ends with 'A badge never sits inside a link, and a link never wears one' plus the one-noun-two-jobs clause and a walkable form — 'no anchor inside main contains a chip-filled span'. Provenance-checked first: bucket-table.tsx's history is ticket-bearing (TASK-0012, BUG-0108), not the human's interactive lane, so this is a violation to fix, not a divergence to reconcile.

ARCHITECT — CRITERIA AMENDMENT NEEDED. Criterion 3 as written ('no change to row height, cell padding or table density', inherited from BUG-0108 criterion 6) forbids the fix it is attached to: removing the chip removes its inline-block box (py-0.5 = 2px each side on a 16px type-data line box), so the buckets table's rows lose about 4px of height. That shrink is the intended consequence of removing a chip that should not have been there, not a density change. Amend criterion 3 to: the table's own density is unchanged — cell padding stays 8/6 (px-2 py-1.5), the column set, order and alignment are unchanged, and no other cell's rendering moves; the row-box height may shrink by the removed chip's vertical padding, and nothing else on /claims changes. Criteria 1, 2 and 4 stand as written and the fix satisfies 1 directly: with no chip, the accent ink and underline are on the glyphs a reader sees.

Also worth folding into the pin or the repo-wide link guard (architect's call, not a blocker): assert no anchor in the rendered tree contains a descendant carrying the badge's chip classes — that turns this ruling into something a test fails on the day someone reaches for a badge inside a link again.
