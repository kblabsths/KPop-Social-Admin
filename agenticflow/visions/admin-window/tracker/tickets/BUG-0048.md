---
id: BUG-0048
type: bug
status: built
milestone: M1
priority: P3
opened_by: designer
parent: null
discovered_from: M1-endgame-designer-walk
children: []
assignee: builder-98
claimed_at: 2026-09-04T03:54:56Z
depends_on: []
touch_scope: [src/components/browse/browse-table.tsx, tests/offline/browse/page.test.ts]
force: false
siblings: [BUG-0047]
attempts: 1
---
# Browse: the poster cell is a 24x24 thumbnail that navigates out of the app to the CDN

## Description
Found on the M1 endgame designer walk (2026-09-03), walk instance :8771 against staging, 1440x900.

The POSTER column on `/browse` renders `<a href="https://s1.ticketm.net/dam/a/..."><img …></a>`. Measured: the image is **24x24 CSS px**, and the anchor carries no `target`, so a click replaces the Admin tab with a raw image on the Ticketmaster CDN.

Two things against the file:

1. **Feel bar 10 — "The investigation never leaves the app."** Every other link on the page is a real in-app URL (`/records/events/<id>`). The poster is the one cell that navigates the operator out, with the browser's Back button as the only way home.

2. **24x24 cannot do the job the column exists for.** The column's own source comment says "Spot-verification is looking at the thing: a poster that belongs to" — at 24px the operator can see that an image EXISTS, not whether it is the right one. The Look sets no image rule (my taste fills the gap, licensed by VISION's "Browse (v1 view: recent events) ... whose numbers match what the database says" and the walk's spot-verification purpose), so I am not legislating a size — only recording that the present one answers a different question than the column asks.

The alt text is correct and present (the event title), so the accessibility side is clean.

Evidence: `agenticflow/tracker/evidence/designer-walk-m1/browse-light.png` (the POSTER column, all 50 rows).

Lowest priority of the walk: nothing is broken, nothing is wrong, and an operator who never clicks a poster never meets it.

**What this adds that BUG-0047 does not.** 0047 is the STARTS column's zone suffix and the row wrapping it causes; this is the POSTER column's link target and size. Same file, different column renderer, no shared line.

## Acceptance criteria
Clicking a poster does not navigate the Admin tab away from the app. Either the poster is not a link, or it opens outside the current tab — the operator's place in the browse list survives the click.

The poster is large enough that "is this the right poster for this event" is answerable from the row, or the column is honest about being a presence indicator. Say in the ticket History which of the two was chosen and the rendered size.

The `alt` stays the event title.

`tests/offline/browse/page.test.ts` asserts the poster cell's rendered markup — the anchor's target behaviour and the image's alt — so this cannot regress silently.

Human-check: on `/browse`, click a poster; you are still in Admin, on the same list, at the same scroll position. (human-checkable)

## Checks
```sh
npx vitest run --project=offline tests/offline/browse/page.test.ts tests/offline/browse/views.test.ts
./node_modules/.bin/tsc --noEmit
npm run lint
```

## History
- 2026-09-03T22:05:10Z [designer] opened: Browse: the poster cell is a 24x24 thumbnail that navigates out of the app to the CDN
- 2026-09-04T03:54:56Z [builder-98] claimed (attempt 1)
- 2026-09-04T04:07:21Z [builder-98] claimed → built: Chose the FIRST option of the two the criteria offer: the poster is not a link at all (no anchor, no target), and it is enlarged to do its job rather than demoted to a presence indicator. src/components/browse/browse-table.tsx only; alt unchanged (event title, or event_id when the row has none).

- Rendered size, measured on staging at 1440x900 (48 posters over 50 rows): a 96x56 CSS px box, object-contain, every cell identical. 96x56 is measured, not taste — staging's posters are 16:9 landscape banners (2426x1365), so a box of that shape wastes no row height and contain never crops the artwork being verified; visible content ranges 96x54 (16:9) to 45x56 (the one near-portrait). Rows 68px vs 45px for a posterless row; POSTER column 112px; table still 1214px, no horizontal scroll. `max-w-none` is load-bearing: Tailwind preflight caps img at max-width:100%, which in an auto-layout table silently shrank a plain `w-16` request to a 60px column (that intermediate letterboxed the banner to 60x34 — do not drop the class).
- Human-check driven for real on a detached instance (port 8781, my pid, killed; staging via the STACK.md launch line + minted cookie): clicked a poster at scrollY 1200 — url /browse -> /browse, scrollY 1200 -> 1200, tabs 1 -> 1, table still 50 rows. 0 anchors in any poster cell; 63 anchors on the page, 0 off-app. Light and dark clean, no console error or warning. No writes: residue.live.test.ts scans clean.
- Verified on the REBASED tree (onto 91d5294): the ticket's three checks green (page+views 87 passed, tsc, lint), full offline project 58 files / 1881 passed, and tests/live/browse.live.test.ts 5 passed.

Two traps for QA. (1) I narrowed page.test.ts's `hrefs()` helper from `[href]` to `a[href]`: React 19 hoists every `<img src>` into a `<link rel=preload as=image href=CDN>` in the head, so a bar-10 assertion over `[href]` fails on markup that navigates nowhere. Those preloads are still emitted and are not a defect. (2) I deliberately did NOT assert the box size in a test — that is a presentation literal and the walk owns it; the test pins only 'img present, no anchor in the cell, every anchor in-app'. Red-then-green observed: the anchor assertion fails against HEAD's pre-fix cell.

Capability removed on purpose: there is no longer any way to open the full-resolution poster from Browse. The criteria sanction that, and 96x56 answers the column's question; if the full image is wanted later it belongs on /records/events/<id>, in-app, not as an outbound anchor.
- 2026-09-04T04:07:47Z [dispatcher] landed ticket/BUG-0048 onto run/admin-window as 17aa0ce
