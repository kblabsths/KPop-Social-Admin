---
id: TASK-0050
type: task
status: done
milestone: M2
priority: P1
opened_by: architect
parent: FEAT-0010
children: []
assignee: null
claimed_at: null
depends_on: [TASK-0049]
touch_scope: [src/components/review/close/conflict-actions.tsx, tests/offline/review-item/conflict-actions.test.ts]
force: false
attempts: 1
resolution: fixed
---
# The data_conflict item's three verdict actions, one typed decision each

## Description
**SPEC F10 / spec §7, the `data_conflict` decision item.** Its close slot offers exactly three controls and no others:

1. **Choose a claimed value** — ONE control per evidence card, so the operator picks the observation they believe. Produces `action: "choose_claimed_value"` with `value.observation_id` set to that card's observation and `value.value` null.
2. **Supply a different value** — the operator's own value, admin tier. Produces `action: "supply_value"` with `value.value` set and `value.observation_id` null. The input is the shared edit cell's behaviour, not a second widget spelling.
3. **Keep current & settle** — the disagreement is fine as it stands; canonical holds and the rejections still land. Produces `action: "keep_current"` with `value` null.

Each control's submission is **exactly one** POST to the settle route and therefore **exactly one** call to `settle_review_item`. There is no client-side multi-step settlement and no second write path.

**The evidence cards already exist** (`src/components/review/evidence-cells.tsx`, `evidence/evidence-pair.tsx` — the app's signature block). This ticket adds a control to each card; it does not restyle the card, re-read the evidence, or change how a value is rendered (§11: machine identifiers render verbatim in mono; a uuid is resolved to a name or a link — LESSONS 5).

**Fixtures, not staging**: no `data_conflict` item exists on staging, so this is graded offline against `tests/fixtures/rows.ts`'s conflict fixture. "Built, not walkable on this data" is the honest grade, exactly as M1 graded the same gap. A criterion demanding a live conflict row is mis-authored.

**The absence is the normal case and is graded FIRST.** `verdicts` and `settle_review_item` do not exist on staging and will not until Ben installs the handoffs — that is the database `main` deploys against for the whole of M2. Build and grade the not-provisioned path before the happy path, and never offer a control that would call a missing function.

**No Admin-side workaround for the absent function may be written** — spec §10's one forbidden move: no queued write, no pending-overrides table, no retry buffer, no flag-guarded direct write.

## Acceptance criteria
1. With `verdicts` present (stub) and a `data_conflict` fixture: the close slot contains exactly one "choose this value" control per evidence card, exactly one "supply a different value" control and exactly one "keep current & settle" control — and no other control.
2. Each control's submission produces EXACTLY ONE call to `settleReviewItem`, asserted per control against the recording stub, carrying one typed decision whose `action`, `review_item_id`, `actor`, `note` and `value` are exactly as specified above; `decisionRefusals` returns `[]` for each of the three payloads.
3. "Choose a claimed value" carries the observation id of the card it sits on — asserted with a two-card fixture, so a control wired to the wrong card fails.
4. With `verdicts` absent: none of the three controls renders, the slot shows the not-provisioned card, and the evidence cards render exactly as M1 shipped them.
5. The empty and null cases render honestly (LESSONS 1): an evidence card whose value is null shows the dash with no qualifier, and an item with zero evidence rows renders its labelled empty state rather than a bare slot.
6. `npm run lint`, `tsc --noEmit`, the targeted offline file and the review-item live suite are green.

## Checks
```sh
bash -c 'rm -rf .next/types && npm run lint && ./node_modules/.bin/tsc --noEmit'
./node_modules/.bin/vitest run --project=offline tests/offline/review-item/conflict-actions.test.ts
npm run test:live -- tests/live/review-item.live.test.ts
```

## History
- 2026-09-09T01:18:18Z [architect] opened: The data_conflict item's three verdict actions, one typed decision each
- 2026-09-09T03:25:03Z [builder-139] claimed (attempt 1)
- 2026-09-09T03:43:15Z [builder-139] claimed → built: - `conflictActions` now returns spec §7's three: one `choose_claimed_value` per evidence card (that card's `observation_id`, `value`/`ref` null), one `supply_value`, one payload-less `keep_current`. An item whose fact triple (`domain`/`entity_id`/`field`) is not whole offers `keep_current` ALONE — a `VerdictValue` cannot be built without all three.
- BEYOND touch_scope, and deliberately: the frame had no way for a control to carry a value the OPERATOR types, so `actions.ts` gains `ActionSpec.supplies` (data: the fact as `domain.field`) + `decisionValue()` merging the typed scalar at submission, and `form.tsx` renders that one control as the shared `EditableCell` (the ticket's "the shared edit cell's behaviour, not a second widget spelling"). No new request path: still one POST, one `settle_review_item`. I read FEAT-0011's "override form" as the RECORD surface's item-less `override`; if it also owns this control, this is the collision to arbitrate. Also amended `tests/offline/review-item/close-slot.test.ts` and `page.test.ts`, which pinned "every shape offers nothing" — both now carry a per-shape table; the other two shapes still expect []. TASK-0051 will want the same two tables (rebase point).
- Traps for QA: the supply control is a CELL, so the slot renders 4 buttons for 3+1 controls — `[data-close-action]` (on the wrapping span for supply) is the control set, and every button sits inside one. An empty/whitespace cell is refused locally as `value_required` and sends NOTHING; `refusalWords` is now total over both identifiers, so no raw identifier reaches copy. Nothing renders while `verdicts` is absent — `CloseSlot` still asks readiness once, this module never asks.

Verified on the rebased tree (onto 63645c1): `npm run lint` 0, `tsc --noEmit` 0, `vitest --project=offline tests/offline/review-item/ tests/offline/absence/` 419 pass, `npm test` 2325 + 6 pass, `npm run test:http` 16 pass, `npm run test:live -- tests/live/review-item.live.test.ts` 9 pass (read-only; `apply_resolution` never called). Red-then-green observed: against the seeded empty-list stub 15 of the new file's 25 tests fail; re-wiring every choose control to card one reddens the observation-id test alone.
- 2026-09-09T03:43:35Z [dispatcher] landed ticket/TASK-0050 onto run/admin-window as 748d37d
- 2026-09-09T03:44:14Z [qa] built → qa: under attack
- 2026-09-09T03:59:45Z [qa] qa → done: Attacked on the LANDED, integrated tree (detached worktree at 5e99b3a, re-verified and landed at 137797c; product code of this ticket untouched by TASK-0051/TASK-0046 in between). Criteria run before reading the builder's handoff.

Criteria, each verified by me:
1. N+2 controls — now pinned at the DOM, not just at the list: for N=0,1,2,3 evidence cards `[data-close-action]` is exactly [choose×N, supply_value, keep_current], and keep_current stands ALONE (with the note field) on each of the three incomplete fact triples.
2. One POST -> one settle_review_item per control: recording stub shows calls==1, functionsCalled()==[settle_review_item], tablesRead()==[] for all four controls; args are exactly {p_decision}; decisionRefusals []. Route refusals answer 400 with zero DB calls (close-slot.test.ts:607), 503 names the absent function after the one call.
3. Observation-id wiring: strengthened past the builder's two-card test — three cards, two of them label-identical (same source, same value), so nothing but positional wiring passes, plus a distinct-id count of 3.
4. verdicts absent: no control renders, not-provisioned card names `verdicts`, evidence unchanged — offline AND on staging (live suite 9/9; staging genuinely lacks `verdicts`, so the absence arm is the one actually exercised). apply_resolution never invoked; the live file makes no .rpc call at all. Read-only throughout.
5. Null claim -> dash, no qualifier; zero evidence -> labelled empty state.
6. lint 0, tsc 0, targeted file green.

New attacks that held (all green, landed in a656349, 8 tests added to tests/offline/review-item/conflict-actions.test.ts — the file that owns this surface, no new file):
- the shared ActionSpec is not mutated by submission: two settlements with two typed values carry their own and spec.value.value stays null (a server-built spec backs every press);
- refusalWords is total over every identifier closeRefusal can actually produce (drove all specs × notes × supplied values: exactly {note_required, value_required}, neither rendered raw);
- EditableCell trims and maps blank -> null, so the whitespace path is refused locally and sends nothing.

Broke it once — BUG-0087 (P1, filed at FEAT-0010, already claimed): a data_conflict on a REFERENCE field (events.venue; EDIT_CONFIG.events.reference = venue_id/venues) is offered the free-text supply_value control like any scalar. Measured body: value.value="The Forum, Inglewood", ref=null, decisionRefusals []. Spec §7 calls this control "the override form, §8" and §8 requires a picker carrying the chosen entity "instead of writing text"; decision.ts's own PAYLOAD_SLOTS says a reference "never" arrives as a supply_value. Reachable per resolver.md §6 steps 1-2. Pinned it.fails(strict) ×2, watched red as plain it() first. NOT bounced here: the fix is the architect's one-widget-or-two ruling against TASK-0054's in-flight override editor (which keeps venue_id display-only), and it reaches outside this ticket's touch_scope. This ticket's own goal — three typed actions, one decision, one call — is met.

Residuals, no ticket: (a) the settle route's 400 body embeds raw refusal identifiers in `error`, which submitSettlement shows verbatim, so an operator can still read "value_payload_ambiguous" — TASK-0049 surface, the route already returns a `refusals` array nobody branches on; (b) the close slot offers supply_value for events columns the edit map excludes (status, event_type, …) — probably correct (registry, not allowlist, is the authority there) but it is the same question BUG-0087 asks. [resolution: fixed] [receipt green: 3 cmd(s), tree a1a2eec9]
