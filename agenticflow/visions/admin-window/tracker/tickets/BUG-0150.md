---
id: BUG-0150
type: bug
status: done
milestone: null
priority: P3
opened_by: qa
parent: null
discovered_from: BUG-0148
children: []
assignee: null
claimed_at: null
depends_on: []
touch_scope: [src/components/queues/verdict-log.tsx, tests/offline/queues/verdict-log.test.ts]
force: false
attempts: 1
resolution: fixed
---
# The verdict log's own observation id and action are the same missed call site BUG-0148 fixed one tab over — outside the identifier primitive

## Description
Found attacking admin-window/BUG-0148 on the landed run branch (03774e0), which put the review-item close block's unlinked observation id through the shared `Identifier` primitive.

**Expected** (DEBT-0011 criterion 2, the rule BUG-0148 was filed and fixed under): "Every call site that renders a machine identifier — a table or view name, a column name, a status/shape/bucket value, **an id** — imports the primitive." Criterion 4: the primitive is where the ARCHITECTURE §7 / Common-violations row 15 bidi isolation is applied ONCE, so every identifier gets it.

**Found**: `src/components/queues/verdict-log.tsx` renders the SAME column BUG-0148 just fixed, on the other tab, outside the primitive:

- `:143` — `<span data-verdict-observation={row.observationId}>{row.observationId}</span>`, the unlinked arm of the observation-id column (`verdicts.observation_id`, an id the database produced). Not an anchor; the linked arm at `:145-151` is the sanctioned anchor exception, this one is not.
- `:114` — `<span data-verdict-action={row.action}>{row.action}</span>`, the action column, whose own comment calls it a machine identifier "verbatim, in the table's own mono cell (§11)". Its twin in the review-item block (`review/close/slot.tsx:281`) already goes through `Identifier`.

Neither spells the face by hand, which is why DEBT-0011's one-owner sweep (`tests/offline/ui/primitives.test.ts`) is green on them: the mono face arrives from `DataTable`'s own cell, `src/components/ui/data-table.tsx:169` (`"type-data px-2 py-1.5 text-ink"`). The isolation arrives from nowhere.

**The table is not one of DEBT-0011's stated boundaries, and that is measured rather than argued**: `src/components/cycles/run-columns.tsx:73` (`source`) and `:185` (`failure_class`) are `Column.cell` bodies in the SAME `DataTable` that already render through `<Identifier>`. So the run table's cells are identifiers and the verdict log's are not, in one app.

**No user-visible harm today, and this ticket says so honestly.** A `<td>` is a block box, so a bidi override inside a cell can only reorder that cell's own content, and each of these cells holds exactly one value and no app-authored word. What is wrong is the same thing BUG-0148 was filed for: the face is coupled to an ancestor by inheritance rather than owned, the isolation is absent, and a future rule hung on the primitive (the next `unicode-bidi`, the long-identifier wrap BUG-0105 had to solve alone) will not reach these two cells. P3 for exactly that reason — completeness of the sweep, not a defect an operator can hit.

**Repro (offline, exact, on the landed tree)**: flip the strict pin `renders the log's machine values through the one identifier primitive, like the run table's cells` in `tests/offline/queues/verdict-log.test.ts` from `it.fails(` to a plain `it(`, then `./node_modules/.bin/vitest run --project=offline tests/offline/queues/verdict-log.test.ts`. Watched RED before filing: `1 failed | 23 passed`, failing at `expect(observation.attr("dir"))` with `an id the database produced is isolated here too: expected undefined to be 'ltr'` — AFTER `expect(primitive.attr("dir")).toBeDefined()` and the cell's own length/anchor assertions have passed, so the pin is not vacuous. As landed it is `it.fails` and the file is green at 24 passed; the day both cells go through the primitive the XPASS turns the file red and sends the reader here.

The pin types no class literal and no attribute value: the isolation marker is read off `Identifier`'s own render, so a restyle of the primitive moves the assertion with it.

**Fix shape**: wrap both cell bodies in `<Identifier data-verdict-observation=… >` / `<Identifier data-verdict-action=… >` (the hooks are already `data-*`, which the primitive's prop type carries), leave the two anchor cells (`item`, and the linked observation arm) exactly as they are, and flip the pin to a plain `it(`. Expect a markup delta of `dir="ltr"` plus the primitive's class pair on those two spans; the `td` already inks them identically, so nothing on screen moves.

## Acceptance criteria
(to be written)

## Checks
```sh
./node_modules/.bin/vitest run --project=offline tests/offline/queues/verdict-log.test.ts tests/offline/ui/primitives.test.ts
npm run lint
```

## History
- 2026-09-09T22:07:02Z [qa] opened: The verdict log's own observation id and action are the same missed call site BUG-0148 fixed one tab over — outside the identifier primitive
- 2026-09-09T23:04:04Z [builder-226] claimed (attempt 1)
- 2026-09-09T23:09:41Z [builder-226] claimed → built: Both named cell bodies now render through `Identifier`; diff is those two sites plus the test file.

- `verdict-log.tsx`: action (:114) and the UNLINKED observation arm (:143) wrap in `<Identifier>` carrying their existing `data-verdict-action` / `data-verdict-observation` hooks. The item anchor and the LINKED observation arm are byte-untouched (DEBT-0011's sanctioned anchor exception). Import added to the existing `@/components/ui` line — no new module.
- FACE, stated explicitly: `Identifier` renders `type-data text-ink`; `DataTable`'s td is `type-data px-2 py-1.5 text-ink` (data-table.tsx:169). Both tokens were INHERITED and are now OWNED by the span — same pair, so nothing on screen moves. Non-muted, matching `run-columns.tsx`:73/:185 in the same table.
- Tests: QA's `it.fails` pin flipped to plain `it`. Added one pin beside it — a U+202E RIGHT-TO-LEFT OVERRIDE mid-id renders verbatim on screen and on the hook, is isolated (`dir` read off the primitive's own render, never a literal), sits inside exactly ONE `td` of its row, every sibling cell is byte-identical to the same row with a clean id, and the isolated set in the whole log is exactly {action, hostile id} — no column label or the dash-note line.

Verified, red-then-green, on the rebased tree (d4ed803 on run/admin-window):
- RED, pre-fix source restored from HEAD (never `git stash`): `vitest run --project=offline tests/offline/queues/verdict-log.test.ts` -> `2 failed | 23 passed`, both at `expected undefined to be 'ltr'`, and both AFTER their verbatim/unlinked/length assertions passed, so neither is vacuous.
- GREEN after: stored checks `vitest run --project=offline tests/offline/queues/verdict-log.test.ts tests/offline/ui/primitives.test.ts` -> 125 passed (25 + 100). `npm run lint` clean, `tsc --noEmit` exit 0, `npm test` -> 72 files / 3190 offline + 6 isolated, all passing.

For QA:
- No HTTP/live observation: `verdicts` is on neither staging nor production (the tab's graded normal case is the not-provisioned card), so there is no verdict row to drive. The offline pin renders the REAL page function (`renderToStaticMarkup(await QueuesPage(...))`), which is the strongest surface available here.
- `VerdictLine.action`'s doc (:36-38) still says "in the table's own mono cell" — left as-is deliberately, it stays true (the td keeps `type-data`) and the ticket scoped the diff to the two call sites.
- Worktree carries gitignored `node_modules` and `.env` symlinks to the primary checkout; the runner needs them.
- 2026-09-09T23:10:00Z [dispatcher] landed ticket/BUG-0150 onto run/admin-window as 05afbdf
- 2026-09-09T23:10:27Z [qa] built → qa: under attack
- 2026-09-09T23:19:09Z [qa] qa → done: FIX IS RIGHT AND THE SHAPE IS EXACTLY WHAT THE TICKET NAMED. Stored checks re-run VERBATIM in a detached `_receipt-BUG-0150` worktree at the landed tip 5eda7ee, BEFORE reading the handoff: `vitest run --project=offline tests/offline/queues/verdict-log.test.ts tests/offline/ui/primitives.test.ts` -> 125 passed (25+100), exit 0; `npm run lint` exit 0. Receipt green on a2a9f81 (my tests in tree). From my worktree: `rm -rf .next/types && npm run lint` 0, `tsc --noEmit` 0, `npm test` -> 72 files / 3194 offline + 6 isolated, all passing (3190 before my 4).

Diff read against the claim (d4ed803): two cell bodies plus the barrel import and comments, nothing else in src. verdict-log.tsx:124 action and :161 the UNLINKED observation arm render `<Identifier>` with their existing `data-*` hooks; the item anchor (:137) and the LINKED arm (:166) are byte-untouched. Parity claim verified, not taken: run-columns.tsx:73 and :185 are non-muted `<Identifier>` in the same DataTable, so the log's cells and the run table's cells now agree in one app. No stale `it.fails` left in the file (the two hits are doc prose); QA's pin is a plain strict `it` and passes non-vacuously.

MY ATTACK — the BOUNDARY between the two converted cells and the two exempt ones, committed as 4 tests in the file that owns the surface (a2a9f81), no new file, no class or attribute literal (the isolation marker is read off `Identifier`'s own render):
1. Resolved observation arm: exactly ONE element carries the hook, it IS the anchor, and it has zero child elements — no primitive nested inside it, which is what a next sweep would reach for. Same render carries the unresolved arm one row down, asserted isolated: the exception discriminates rather than exempting the column.
2. Hook integrity per row, all four §7 row shapes: actor and action exactly 1, item and observation <=1 and both render somewhere. This is the claim every reader depends on — `cellsOf` here and the live oracle's `[data-verdict-action]` map (tests/live/queues.live.test.ts:491) both do `row.find(hook).attr(...)`, which answers from the FIRST match and would silently read a wrapper.
3. The half the builder's own hostile fixture did not drive: U+202E in the ACTION (he hostile-tested only the observation id). Verbatim on screen and on the hook, isolated, override inside exactly one td, every sibling cell byte-identical to the same row with a clean action.
4. Same containment for the LINKED id — stated as containment (block-box td), not as `dir` being absent, so the pin survives a revisit of the anchor exception.
All four hold. Adjacent suites addressing the same hooks stay green: verdict-inline 34, evidence-pair 23, primitives 100 (one-owner guard).

OTHER `verdicts` VALUES, decided against the doc, no miss: `actor` is an email from the settle route, `note` is the admin's prose (clamped, title-carried), `created_at` renders as the app's own relative words with the raw instant absent by test — none is a "table/view name, column name, status/shape/bucket value, or id" (DEBT-0011 criterion 2), none is snake_case machine text, and the sibling surface leaves actor a bare span too (review/close/slot.tsx:287) after that sweep's three attempts and two rendered audits found UNEXPLAINED = 0. `verdict_id` never reaches markup (DataTable rowKey only). Nothing filed.

RESIDUALS, not tickets. (1) `clamped` (lib/format.ts:325) returns `${kept}${ELLIPSIS}`, so a clamped note renders foreign text glued to the app's own ellipsis inside ONE span (verdict-log.tsx:195) — a U+202E note would reorder that one character. Not reachable by criterion 2 (prose, not an identifier; `Identifier` would wrongly give it the mono face) and contained to its td; it earns a ticket only if §7 is ever read to cover app punctuation appended to producer text. (2) The linked arms are un-isolated by design (DEBT-0011's anchor exception); harm is contained by the block-box td, measured in markup by pin 4. (3) Not observed over HTTP: `verdicts` is on neither staging nor production, so the live selectors above are unchanged-by-construction (attribute selectors, span->span) rather than driven. [resolution: fixed] [receipt green: 2 cmd(s), tree 8ecfa61f]
