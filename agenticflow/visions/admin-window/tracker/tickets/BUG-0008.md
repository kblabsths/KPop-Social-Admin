---
id: BUG-0008
type: bug
status: done
milestone: null
priority: P0
opened_by: dispatcher
parent: null
children: []
assignee: null
claimed_at: null
depends_on: []
touch_scope: [src, supabase, public, tests, docs]
force: false
resolution: already_done
---
# CI red: 1 failing test(s)

## Description
Full suite red. Failing (0): (unparsed). Failure signature b638c93cffb1. Diff since last green a5e4adb7: src/app/analytics/page.tsx                    | 220 ------------------
 src/app/api/admin/catalog-search/route.ts     |  55 -----
 src/app/api/admin/events/[id]/route.ts        |  35 ---
 src/app/api/admin/groups/[id]/route.ts        |  33 ---
 src/app/api/admin/idols/[id]/route.ts         |  34 ---
 src/app/browse/page.tsx                       |  17 ++
 src/app/claims/page.tsx                       |  17 ++
 src/app/components/AdminNav.tsx               |  50 ----
 src/app/components/EditableCell.tsx           | 108 ---------
 src/app/cycles/page.tsx                       |  17 ++
 src/app/d. Log tail: ./src/app/data-management/idols/page.js' or its corresponding type declarations. / .next/types/validator.ts(98,39): error TS2307: Cannot find module '../../src/app/data-management/page.js' or its corresponding type declarations. / .next/types/validator.ts(107,39): error TS2307: Cannot find module '../../src/app/database/page.js' or its corresponding type declarations. / .next/types/validator.ts(134,39): error TS2307: Cannot find module '../../src/app/api/admin/catalog-search/route.js' or its corresponding type declarations. / .next/types/validator.ts(143,39): error TS2307: Cannot find module '../../src/app/api/admin/events/[id]/route.js' or its corresponding type declarations. / .next/types/validator.ts(152,39): error TS2307: Cannot find module '../../src/app/api/admin/groups/[id]/route.js' or its corresponding type declarations. / .next/types/validator.ts(161,39): error TS2307: Cannot find module '../../src/app/api/admin/idols/[id]/route.js' or its corresponding type declarations. / .next/types/validator.ts(192,39): error TS2307: Cannot find module '../../src/app/data-management/layout.js' or its corresponding type declarations. / npm notice run kpop-social-space@0.1.0 lint / npm notice run eslint / 

## Acceptance criteria
`bash -c 'npm run lint && ./node_modules/.bin/tsc --noEmit && npm test'` exits 0

## History
- 2026-09-02T07:42:11Z [dispatcher] opened: CI red: 1 failing test(s)
- 2026-09-02T07:43:09Z [dispatcher] open → blocked: Folded into the TASK-0005 QA verification. Not a code defect: tsconfig includes .next/types, and the primary checkout's STALE .next (gitignored build output from before TASK-0005's deletions) references the removed pages, so tsc --noEmit fails there until npm run build regenerates .next/types. The TASK-0005 QA lane builds in the primary tree; it closes this from blocked with --resolution already_done once the full ci_command exits 0. Curator note: a ticket that deletes routes should carry 'rm -rf .next' or a build in its landing path, or ci_check should build before tsc.
- 2026-09-02T07:49:34Z [qa] blocked → done: Not a code defect, and green on the rebuilt tree. Cause confirmed independently before reading the fold note: tsconfig.json includes ".next/types/**/*.ts", and the primary checkout's .next was gitignored build output from before TASK-0005's deletions (validator.ts mtime Sep 1 23:58), so tsc --noEmit resolved that stale validator's imports of src/app/data-management/*, src/app/database/page, and the four deleted api/admin routes — the exact eight TS2307s in this ticket's log tail. rm -rf .next && npm run build regenerates .next/types against the surviving routes. Measured after the rebuild: bash -c 'npm run lint && ./node_modules/.bin/tsc --noEmit && npm test' exits 0, 11 test files / 178 tests passed. Re-measured a second time after landing my QA test extension (32bf94f): still exit 0. Nothing in git was ever red. The receipt for TASK-0005 also came back GREEN on all 11 checks including tsc, computed in receipt.py's own private worktree, which has no stale artifact at all — so the failure is not reproducible anywhere except a checkout carrying a pre-deletion .next. Residual for the curator (already noted at fold time, restating so it is not lost): the check order 'tsc --noEmit' before 'npm run build' is fragile for any ticket that deletes a route; ci_check building before tsc, or a landing-path rm -rf .next, removes this class of false red entirely. [resolution: already_done]
- 2026-09-02T07:56:13Z [architect] Root cause recorded in DECISIONS.md (2026-09-02): environmental, not a defect in the landed tree. tsconfig.json includes .next/types/**/*.ts; TASK-0005 deleted routes and a stale .next in that worktree still held type modules importing the deleted pages, so tsc --noEmit failed on code that no longer exists. Standing rule: any ticket whose diff removes or renames a file under src/app/ runs 'rm -rf .next' (or a full npm run build) before tsc in its landing path. Recommended to the dispatcher — run.yaml ci_command should become: bash -c 'rm -rf .next && npm run lint && ./node_modules/.bin/tsc --noEmit && npm test'. run.yaml is the dispatcher's to apply, not mine.
