---
id: TASK-0053
type: task
status: qa
milestone: M2
priority: P2
opened_by: architect
parent: FEAT-0011
children: []
assignee: builder-138
claimed_at: 2026-09-09T02:55:40Z
depends_on: [TASK-0052, TASK-0040]
touch_scope: [src/components/EditableCell.tsx, src/components/records/field-editor.tsx, tests/offline/ui/editable-cell.test.ts]
force: false
attempts: 1
---
# The shared edit cell's two affordances: visible before it is touched, and open with the value selected

## Description
**Two affordances the M1 user-sim walks earned** (SPEC F11), on the shared edit cell, and they apply to **every** regime that has a write path — the sandbox today, `events`/`venues` when their allowlist lands.

1. **An editable value is distinguishable from a read-only one BEFORE it is touched.** A stranger found the edit affordance by tabbing, not by looking, and said a mouse-first colleague would read the whole page as read-only. The affordance is a **1px hairline underline on the value** — not colour, not weight (LOOK_AND_FEEL; colour is reserved and weight is the type scale's). It is a resting state: no hover, no focus, no click required to see it.
2. **A click-to-edit cell opens with its existing value selected**, so a straight retype replaces and an arrow key still appends. A stranger wrote a wrong value into the shared catalog because the caret sat at the end and nothing was selected.

**Scope discipline.** This ticket changes the cell's resting rendering and its open behaviour. It does not change what is editable, the write path, the refusals, the provenance line, or any page's layout. `src/components/EditableCell.tsx` keeps its name and its place at the components root (§11: the one PascalCase file, the one old component that re-earned its place).

**Use the tokens, never raw values** (LOOK_AND_FEEL, `app/globals.css`'s `@theme`): the hairline is a token'd border colour at the token'd width, and it passes the existing contrast test in both themes.

## Acceptance criteria
1. A cell whose column is editable renders a 1px hairline underline in its RESTING state — asserted in the offline render suite on markup with no hover, focus or click applied — and a read-only cell renders none. Both fixtures, in both themes.
2. The affordance is drawn from the design tokens: no raw hex, no raw px colour, in the changed lines; the contrast test still passes for both themes.
3. Opening a cell selects its existing value: the rendered input carries the select-on-open behaviour, asserted structurally rather than by a comment, and an empty cell opens with an empty input and no selection error.
4. Neither change alters what is editable: `decideEdit`'s answers are byte-identical before and after (the edit-config guard still passes), and no page's column set moves.
5. `npm run lint`, `tsc --noEmit`, the targeted offline file, and the record page's live suite are green.

## Checks
```sh
bash -c 'rm -rf .next/types && npm run lint && ./node_modules/.bin/tsc --noEmit'
./node_modules/.bin/vitest run --project=offline tests/offline/ui/editable-cell.test.ts tests/offline/ui/contrast.test.ts tests/offline/ui/tokens.test.ts
npm run test:live -- tests/live/edit.live.test.ts
```

## History
- 2026-09-09T01:19:43Z [architect] opened: The shared edit cell's two affordances: visible before it is touched, and open with the value selected
- 2026-09-09T01:25:16Z [architect] depends_on: [TASK-0052] → [TASK-0052, TASK-0040]: Adds the strike-removal ticket: the edit cell's affordances are graded on the record page, which TASK-0040 rewrites.
- 2026-09-09T02:55:40Z [builder-138] claimed (attempt 1)
- 2026-09-09T03:07:44Z [builder-138] claimed → built: Both affordances land in the shared control only; `field-editor.tsx` needed no change and `EDIT_CONFIG`/`decideEdit` are untouched (criterion 4 is a no-diff).

- RESTING_AFFORDANCE = `underline decoration-hairline decoration-1 underline-offset-2` on the resting button, so it covers the em-dash an absent editable value renders as. Hairline, not accent: accent ink + underline is this app's link at rest (`components/cycles/links.ts`).
- `selectOnOpen`, a MODULE-LEVEL ref callback on the input and the textarea alike, calling `select()` — no `setSelectionRange(0, length)`, so a textarea's newlines cannot be selected short. Module-level is load-bearing: an inline arrow would re-select after every keystroke.

Walked on a production build at 127.0.0.1:8791 (free-bind probed on 127.0.0.1/::1/0.0.0.0; 8790 was already taken), staging `walk_sandbox`, reset before AND after, server pid mine and killed. Computed style on the real cell: light underline/rgb(209,213,220)=hairline/1px/offset 2px, ink rgb(30,41,57), weight 400, 11px; dark underline/rgb(54,65,83)=hairline/1px/2px. The read-only `sandbox_id` line on the same table: text-decoration-line `none`, both themes. Absent `note` (row ...0002): em dash, underlined, rgb(209,213,220), 1px. Open: INPUT selectionStart 0 / selectionEnd 17 over 'First sandbox row', activeElement true; ArrowRight collapses to 17/17 (still appends); Escape restores 'First sandbox row' with no write; a straight retype replaced it, Enter saved and it survived a reload. Textarea on 'line one\nline two\n\nline four': 0..28 of length 28, all 3 newlines inside the selection. Zero console errors/warnings in either theme.

Green after rebase onto run/admin-window: lint, tsc --noEmit, vitest offline ui/{editable-cell,contrast,tokens} 126 passed, records suites 250 passed, `npm run test:live -- tests/live/edit.live.test.ts` 20 passed, `npm run test:http` 16 passed, full `npm test` from a detached worktree of the rebased HEAD 2232 offline + 6 isolated.

Trap for QA: text-decoration is inherited, so a test that asks only the innermost element whether it is underlined calls the underlined em-dash span un-underlined — the new `underliningClasses` helper walks self-or-ancestor. Red-then-green: 10 of the new tests fail against the pre-change component.
- 2026-09-09T03:08:04Z [dispatcher] landed ticket/TASK-0053 onto run/admin-window as e2b7467
- 2026-09-09T03:16:09Z [qa] built → qa: under attack
