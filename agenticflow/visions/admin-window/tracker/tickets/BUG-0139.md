---
id: BUG-0139
type: bug
status: built
milestone: M2
priority: P1
opened_by: architect
parent: null
discovered_from: human:ben-walk
children: []
assignee: builder-207
claimed_at: 2026-09-09T18:03:23Z
depends_on: []
touch_scope: [src/lib/db/sources.ts, src/app/sources/page.tsx, tests/offline/sources/page.test.ts, tests/offline/sources/read.test.ts, tests/offline/sources/names.test.ts, tests/offline/sources/population.ts, tests/offline/absence/pages.test.ts, tests/offline/absence/blank-cells.test.ts, tests/offline/absence/surfaces.ts, tests/offline/gauges/bounded.test.ts, tests/offline/ui/primitives.test.ts, tests/live/sources.live.test.ts, tests/live/parity.ts]
force: false
attempts: 2
---
# /sources reads runs once per registered source and then waits for both gauges: ~9 sequential round trips, 2.0-2.3 s warm

## Description
Found by **Ben, walking the M2 endgame instance himself**, 2026-09-09. Warm server times he measured on the walk instance: `/sources` **2.0-2.3 s** (and `/claims` 2.9-3.8 s, admin-window/BUG-0138), against `/` 0.2 s and `/queues` 0.3 s.

**Where the time goes: depth, not a slow query.** `src/app/sources/page.tsx` (~131-145) awaits three composed reads one after another, and the first of them is itself a loop:

1. `listSources()` (`src/lib/db/sources.ts:255-270`) — `readSources()`, then **one `readLastRun` per registered source, in a `for` loop, each awaited**. Three sources today is four sequential round trips; a registry of thirty is thirty-one.
2. `readAwaitingRowTrend({ filter })` — two sequential legs, the second chunked.
3. `readRejectionStampGauge()` — two more.

MEASURED read-only against staging today (architect; `agenticflow/tracker/evidence/architect-perf-M2/perf-probe.mjs` / `perf-probe.tsv`): `sources` holds **3** rows, `runs` holds **5**; one of today's per-source last-run reads takes **110 ms**. ~9 sequential round trips at 110-380 ms each is the 2.0-2.3 s. None of the three composed reads needs anything from the other two.

## Ben's ruled fix shape (2026-09-09)

`listSources` folds the per-source last-run loop into **ONE PostgREST request** ("sources with runs embedded, ordered by start time, limited to one per source, **or the equivalent single query you rule honest under §4.3; state the exact chain**"), the trend and rejection gauges run **concurrently** with it, each leg reports separately as today, and the pin is the stub's call log — exactly 1 request for the sources+runs leg regardless of source count — plus byte-identical rendering on the existing fixtures.

## The embed does not exist; here is the chain I rule instead

**Measured today: there is no relationship to embed.** `sources` and `runs` have no foreign key — deliberately, so a run against an unregistered source can still write its row (migration `20260829000001`'s own column comment; ARCHITECTURE §6 trap 6), and PostgREST refuses both spellings:

    sources.select("…, runs(run_id)").limit(5)          PGRST200  111 ms  "Could not find a relationship between 'sources' and 'runs'"
    sources.select("…, runs!source(run_id)").limit(5)   PGRST200   89 ms  same

so `runs.order=…&runs.limit=1` has nothing to hang off. Aggregates are refused as well (**PGRST123**, "Use of aggregate functions is not allowed"), so there is no `distinct on (source)` and no group-by to lean on either — which is what `readLastRun`'s own docstring already says.

**The equivalent single query, ruled honest under §4.3 — the exact chain:**

    db.from(T.runs)
      .select(LAST_RUN_COLUMNS, { count: "exact" })
      .order("source",     { ascending: true })
      .order("started_at", { ascending: false })
      .order("run_id",     { ascending: false })   // uuid v7: ties break in time's own direction
      .range(0, cap - 1)                            // through readComplete, cap = ROW_CAP

through `readComplete`, then fold in TypeScript: the newest run for a source NAME is the **first row carrying that name** in that order. Measured: **159 ms, one request, count 5 of 5**.

Why this is honest rather than a window pretending to be a total (§4.3, and the exact objection `readLastRun` was written to answer): `readComplete`'s `ok` array is the WHOLE `runs` table or the read refuses, so "this source has never run" is a real absence in a complete set and never a row that fell off the end of a window. That is the one thing the dash may not mean here, and a complete read is the only bounded shape that preserves it.

Why **unnarrowed** rather than `.in("source", names)`: the narrowed form needs the registry's names first, which puts the sequential round trip straight back, and above 100 names it would have to chunk — at which point "exactly 1 request" stops being true. Unnarrowed, the runs read depends on nothing and goes into the same `Promise.all` as the registry read. Runs belonging to no registered source are simply never matched by the fold.

## The cost of this ruling, stated plainly

`runs` is "**one row per invocation of the adapter run loop**" (migration `20260829000001`'s table comment) and **has no retention policy**. Five rows today because the crons are not yet running in anger; three sources on an hourly cron is ~72 rows/day, so `ROW_CAP` (1000) is roughly **two weeks of production cron**. On the day the log outgrows the cap this page stops rendering the registry and renders the honest refusal instead — "the database holds N rows matching this read and it is capped at 1000" — where today's per-source `limit 1` reads can never truncate. §4.3 sanctions exactly that trade ("when a table genuinely outgrows ROW_CAP the app says so with the real number, and raising the cap or narrowing the filter is then a deliberate decision with evidence behind it"), and criterion 3 below makes the refusal a tested, legible state rather than a surprise — but it is a new failure mode, it is weeks away rather than years, and the fix for it is not Admin's: either a retention policy on `runs` or a per-source seek, both in the scraper repo. **Recommended for the M3 list beside the paging ruling below**, not for this ticket.

## Recorded for the NEXT milestone, not this one (Ben's ruling, 2026-09-09)

**Paging past the window, on Claims and on Browse, through on-demand client fetching against a route handler** — Ben: *"not being able to load all claims if I want to is a huge oversight."* Deliberately NOT a ticket now and no part of this fix; written here so the strategist picks it up at the M2 retro. Whoever plans it should know ARCHITECTURE §4.3 currently says "paging is not the answer to a cap and none is built" — that ruling amends the contract, and the amendment is the architect's to make when the milestone starts, never a builder's.

## Acceptance criteria
1. **One runs request, whatever the registry holds.** `listSources` issues exactly two requests — `readSources()` and one `readLastRuns()` — and never a request per source. The offline pin is the stub's own call log, named `one runs request, whatever the registry holds`: rendering `/sources` against a stub scripted with 3 sources and against one scripted with 300 records **the same number of calls**, with exactly one `.from(runs)` in each. Both fixtures are stated; the 300-source one is the fixture that would have caught the loop.

2. **The fold is exact, and the dash still means what it meant.** The newest run for a source is the first row carrying its `source` NAME in the chain's order (`source asc, started_at desc, run_id desc`); `lastRun` is `null` — rendered as the table's dash — when the complete set holds no run for that name, and a run whose `source` matches no registered source is matched by nobody and renders nowhere. The offline suite states all four fixtures: a source with several runs (the newest wins, and a tie on `started_at` breaks on the uuid v7 `run_id`), a source with exactly one, a source with none (dash, never a zero, never "no runs" as a word of ours), and a run whose name is in no row of the registry (present in the read, absent from the page). A run still in flight keeps its null `ended_at` and null `outcome` and renders the dash for each.

3. **Complete-or-refuse, and the refusal is legible.** The runs read goes through `readComplete` with `{ count: "exact" }`, the total order above and `.range(0, cap - 1)`; a scripted `{ data: 1000 rows, count: 1500 }` makes the read refuse naming **`runs`** and the real number, and the page renders the error state — it must not render a registry whose last-run column is quietly wrong. Both fixtures: the complete read (`ok`, every row) and the truncated one (refusal). A null count is a refusal, never a zero (§4.3, common violations row 2).

4. **Each leg still reports separately, exactly as today.** `not_provisioned` from the registry read names `sources` and from the runs read names `runs`, so the card says which object is absent rather than blaming the other; the first refusal is returned as the read's refusal, as today — this ticket changes the number of requests, not the composed read's semantics.

5. **The three composed reads run concurrently.** `listSources()`, `readAwaitingRowTrend({ filter })` and `readRejectionStampGauge()` are issued together (`Promise.all` over independent reads) — the filter each takes is derived from `searchParams`, never from another read's rows. Each keeps its own `DbResult` and its own Section: a refused trend gauge renders its own state card with its own eyebrow and leaves the registry table and the rejection gauge untouched, and the same in every other combination. No leg's refusal removes another leg's rows (§4.1, common violations row 14).

6. **The rendering does not move.** On the existing fixtures the page is byte-identical: the same source chips in the same order, the same registry columns and cells, the same paragraph under the table ("State, not configuration…"), both gauges' window lines with their `data-window`, `data-window-held` and `data-window-truncated` hooks unchanged, and the two emptinesses still telling themselves apart — `data-empty="registry"` when the registry holds nothing and `data-empty="narrowing"` when a chip matched nothing. A null `checkpoint` or `note` still renders the dash with no qualifier. A window line stands on every `ok` read including an empty one, and on no refused read (§4.3, common violations row 11) — graded for every surface at once in `tests/offline/absence/pages.test.ts`.

7. **The live oracle grades the state before the number.** `tests/live/sources.live.test.ts` reads the state kind from `data-state` first — `empty` is a pass with a 0, `error` is always a FAIL (common violations row 6) — addresses every surface by its `data-surface` name and never by position (row 8), and counts the same narrowing the surface renders (row 7).

8. **Measurably faster, and the number is recorded.** Warm server time for `/sources` on the walk instance, by STACK.md's own launch recipe, is stated in the handoff with the before (2.0-2.3 s, Ben's measurement) and the after. The bar is the shape, not a threshold: at most 6 requests per render, and the registry+runs leg is 2 of them however many sources are registered.

## Checks
```sh
cd "/Users/ben-m4/Desktop/Coding/KPOP/kspace Admin" && ./node_modules/.bin/vitest run --project=offline tests/offline/sources/page.test.ts tests/offline/sources/read.test.ts tests/offline/sources/names.test.ts tests/offline/absence/pages.test.ts
cd "/Users/ben-m4/Desktop/Coding/KPOP/kspace Admin" && bash -c "rm -rf .next/types && npm run lint && ./node_modules/.bin/tsc --noEmit"
cd "/Users/ben-m4/Desktop/Coding/KPOP/kspace Admin" && grep -q readLastRuns src/lib/db/sources.ts
cd "/Users/ben-m4/Desktop/Coding/KPOP/kspace Admin" && grep -q 'one runs request, whatever the registry holds' tests/offline/sources/page.test.ts
cd "/Users/ben-m4/Desktop/Coding/KPOP/kspace Admin" && grep -q 'Promise.all' src/app/sources/page.tsx
cd "/Users/ben-m4/Desktop/Coding/KPOP/kspace Admin" && npm run test:live -- tests/live/sources.live.test.ts
```

## Interface contract
(architect ruling, 2026-09-09)

`src/lib/db/sources.ts` — the module keeps its job as the ONE place `sources`
and `runs` are matched by NAME (§6 trap 6: there is no key to join on, by
design) and stops doing it one source at a time.

    /** Every run the table holds, newest first within each source NAME — ONE
     *  request. A COMPLETE read (§4.3 kind 1): the `ok` array is the whole
     *  table or the read refuses naming `runs` with the real number, which is
     *  what keeps "this source has never run" an absence in a complete set
     *  rather than a row that fell off a window. Unnarrowed on purpose: it
     *  needs no name from the registry, so it runs CONCURRENTLY with it, and
     *  `.in(names)` above 100 names would have to chunk. */
    export function readLastRuns(
      db?: SupabaseClient,
    ): Promise<DbResult<LastRunRow[]>>;

    /** The newest run per source NAME, from that complete set — a pure fold,
     *  no read. `null` for a name the set holds no row for. */
    export function lastRunBySource(
      runs: readonly LastRunRow[],
    ): ReadonlyMap<string, LastRunRow>;

    /** Unchanged signature, two requests inside it now instead of 1 + N. */
    export async function listSources(
      db?: SupabaseClient,
    ): Promise<DbResult<SourceState[]>>;

    // GONE: readLastRun (the per-source `limit 1` seek and its loop).
    // KEPT unchanged: SourceRow, LastRunRow, SourceState, SourcesFilter,
    //      SourceNameRow, readSources, readSourceNames, selectSources.

The exact chain `readLastRuns` builds, which is the ruling and not a suggestion:

    client.from(T.runs)
      .select(LAST_RUN_COLUMNS, { count: "exact" })
      .order("source",     { ascending: true })
      .order("started_at", { ascending: false })
      .order("run_id",     { ascending: false })   // uuid v7 — ties break in time's direction
      .range(0, cap - 1)                            // cap handed in by readComplete

`src/app/sources/page.tsx` — one composition, three independent legs:

    const [sources, trend, rejections] = await Promise.all([
      listSources(),
      readAwaitingRowTrend({ filter }),
      readRejectionStampGauge(),
    ]);

`filter` is derived from `searchParams` and the registry's ids as today, so the
narrowing the trend gauge takes must be computed BEFORE the `Promise.all` from
the URL alone, or the concurrency is lost again. Where the URL names a
`source_id` the registry turns out not to hold, the page renders exactly what it
renders today (`data-empty="narrowing"`), and the gauge legs are unaffected —
each leg keeps its own `DbResult` and its own Section state, and no leg's
refusal removes another leg's rows (§4.1, common violations row 14).

## History
- 2026-09-09T17:10:32Z [architect] opened: /sources reads runs once per registered source and then waits for both gauges: ~9 sequential round trips, 2.0-2.3 s warm
- 2026-09-09T17:10:50Z [architect] contract amended: architect ruling 2026-09-09: the embed Ben named does not exist (no FK between sources and runs, PGRST200 measured both spellings), so the honest single query is a complete-or-refuse read of the whole runs table, unnarrowed, ordered source/started_at/run_id, folded newest-per-name in TypeScript. [was: (none)]
- 2026-09-09T17:19:26Z [architect] priority: P2 → P1: Architect, 2026-09-09: dispatch order at the builder cap of 1. BUG-0139 goes first. It is on the critical path - BUG-0138 (/claims 2.9-3.8s) is blocked on it and inherits its ruled read shape, so BUG-0137 dispatching first idles the whole perf chain behind it; BUG-0137 blocks nothing. BUG-0139 is also the ticket Ben found by walking the M2 endgame himself, with a ruled fix shape and a measured probe already in evidence. No depends_on edge added between them: the two share no file and a false edge would outlive this cap.
- 2026-09-09T17:22:24Z [builder-205] claimed (attempt 1)
- 2026-09-09T17:46:59Z [builder-205] claimed → built: - `readLastRuns` reads the whole run log in ONE complete-or-refuse request (source asc / started_at desc / run_id desc, {count:"exact"}, range 0..cap-1), `lastRunBySource` folds newest-per-NAME, `listSources` issues its two legs in one Promise.all and keeps first-refusal semantics; `readLastRun` is gone. The page issues listSources + readAwaitingRowTrend({filter}) + readRejectionStampGauge() in one Promise.all.
- Verified: the 4 stored checks + `npm test` (2991+6 green) + `npm run test:live -- tests/live/sources.live.test.ts` (7 green, staging read-only), all re-run after rebase onto run/admin-window. Red-then-green observed: with `git show HEAD~1:` restoring the two src files, 9 page cases and 8 read cases fail (incl. the concurrency barrier, which times out because the sequential page never issues read 2).
- MEASURED, production build of this tree on my own bind-probed port 8791 against staging, warm (5 requests each, curl time_total): /sources BEFORE (pre-fix tree, same build recipe) 1.71-1.92 s; AFTER 1.44-1.62 s. Same instance: / 0.10-0.14 s, /queues 0.09-0.12 s, and `/sources?source_id=<a real source>` 0.09-0.14 s. Requests per render: the offline fixture render costs 6 (pinned), of which the registry+runs leg is 2 for 3 sources AND for 300. The residual ~1.4 s is NOT this ticket's leg: the page states `data-window-held="877"`, and the awaiting-row gauge's second leg chunks those 877 ids at ID_CHUNK=100 into 9 sequential requests - the same gauge /claims uses (admin-window/BUG-0138). Narrowing collapses it to 0.1 s, which is the control.

TWO THINGS QA SHOULD KNOW.
(1) ONE rendering moved, and it is the one the contract names. The filter now comes from the URL alone, so `?source_id=<well-formed uuid the registry lacks>` renders `data-empty="narrowing"` with 0 source rows (was: the whole registry). Measured on the production build: 200, 3 empty states, no error card. A value that is NOT a uuid still narrows nothing and renders all 3 rows, unchanged - through `isRecordId` (admin-window/BUG-0065's one grammar), because handing `nobody` to the gauge's query is refused by Postgres itself: measured read-only on staging, `22P02 invalid input syntax for type uuid: "nobody"` (agenticflow/tracker/evidence/BUG-0139/uuid-probe.mjs). Without that guard "the filter from searchParams alone" turns a hand-typed URL into an error card.
(2) `tests/offline/absence/pages.test.ts` needed one refinement, declared as data (COMPOSED_READS): with the WHOLE database absent, /sources now asks for `runs` as well as `sources` and reports the first refusal, so it names `sources` only - two object names in one mono span would break LOOK_AND_FEEL state 3. The first member is still required by name, and `runs` absent on its own is still named (matrix above, and the page suite's own case).
- 2026-09-09T17:47:38Z [dispatcher] landed ticket/BUG-0139 onto run/admin-window as cd6d267
- 2026-09-09T17:48:24Z [qa] built → qa: under attack
- 2026-09-09T18:01:53Z [qa] qa → reopened: BUG-0140 filed (P2, discovered_from BUG-0139): the fix moved the narrowing to the URL alone but left the comparison a JS string compare, so /sources now DENIES a registered source named in a uuid spelling Postgres and this app own isRecordId both accept.

Expected (tests/offline/records/page.test.ts WELL_FORMED_IDS, the app own established rule: "Every spelling of a uuid Postgres itself accepts ... canonical, uppercased, and with the hyphens left out. Each names the SAME row"; and this ticket contract: a source_id "the registry turns out not to hold" is what renders data-empty="narrowing"): all three spellings render the registered source row.
Found, production build of c11c03d against staging, port 8793, cookie-authed: ?source_id=01a05782-8e7f-752c-ae60-3ce4c51962f6 renders that row; ?source_id=01A05782-8E7F-752C-AE60-3CE4C51962F6 and ?source_id=01a057828e7f752cae603ce4c51962f6 render 0 rows + data-empty="narrowing". src/app/sources/page.tsx:123-126 -> src/lib/db/sources.ts:349 (===) vs src/lib/db/gauges.ts:347 (.eq, uuid). Read-only staging: sources .eq source_id returns count=1 for canonical, upper, hyphen-less AND upper-hyphen-less. Not pre-existing: git show d43b954^:src/app/sources/page.tsx:108-113 matched against the ids already read, so an unmatched spelling narrowed nothing and showed the whole registry. Pinned it.fails in tests/offline/sources/page.test.ts (commit eaa0745), watched red first.

WHAT I ATTACKED AND COULD NOT BREAK (all 8 criteria hold otherwise):
- stored checks re-run on the landed tree BEFORE reading the handoff: 4 offline files 363 green; lint + tsc clean; npm run test:live sources 7 green; full offline 2992 green.
- fold, against an INDEPENDENT read of runs (evidence/BUG-0139-qa/probe.mjs, read-only): rendered data-source-last-run=01a05978-1875-74e8-a93d-3d6b7d6aaa89, -at=2026-08-31T20:17:17.761328+00:00, outcome=succeeded == my own newest-per-name fold; the two test_harness sources hold no run and render the dash only. run_id version nibble measured = 7, so the started_at tie-break really is time-directed.
- runs truncated at the cap (count 1500 / 1000 rows): registry reports data-read-failed="runs" and BOTH gauges still render their windows (held 4 / 5) - no leg refusal removes another leg rows. Empty registry + runs absent: same, names runs.
- unregistered run name (eventbrite) folded and rendered nowhere; source with zero runs = dash; in-flight run keeps null outcome.
- URL abuse, live: source_id= empty, =nobody, =%27%3B--, comma-joined pair, 5000 chars, repeated facet both orders -> all 200, no error card, no 22P02, first value wins. No injection reaches PostgREST.
- 10 concurrent /sources renders: identical rows and identical last-run id.
- both colour schemes in headless chromium: 3 rows visible, 3 surfaces, chip click narrows to 1 row in each.
- warm, my own bind-probed port 8793, 5 samples: /sources 1.42-1.53 s (Ben before: 2.0-2.3 s), / 0.08-0.10 s, /queues 0.08-0.11 s. Note for BUG-0138, NOT filed here: narrowing does NOT collapse the residual - /sources?source_id=<ticketmaster> is still 1.33-1.39 s, and data-window-held="877" is unchanged by the narrowing, so the 9-chunk gauge cost survives the filter the builder measured it away with.
- 2026-09-09T18:03:23Z [builder-207] claimed (attempt 2)
- 2026-09-09T18:15:12Z [builder-207] claimed → built: Reopen fixed; admin-window/BUG-0140 is fully covered by this commit (said so on that ticket too — QA can close it already_done).

- `canonicalRecordId` (src/lib/db/records.ts, beside `isRecordId` and built ON it — one uuid grammar, no second lowercasing spelled at a call site) reduces every spelling that predicate accepts to the one Postgres prints, null for a non-id; `filterFrom` (src/app/sources/page.tsx) asks for it once where the narrowing is made, so the query's uuid compare and the JS compares in selectSources/selectClaims agree. Diff is the comparison and its docs: nothing about readLastRuns/lastRunBySource/the Promise.all moved.
- Tests: QA's `it.fails` pin is now a plain `it(` with its name kept, over 5 spellings (canonical/upper/hyphen-less/upper-hyphen-less/mixed) of a manySources id — the population's own ids are ALL DIGITS, so an uppercase spelling of one is the same string and the loop would pass vacuously; the guard asserts each spelling differs. It also asserts the narrowed by-day and by-week gauge figures, the active chip and the row link. Opposite direction pinned: an id the registry lacks renders data-empty="narrowing" in every spelling. Unit coverage of the new helper went into tests/offline/records/page.test.ts beside WELL_FORMED_IDS (that file owns isRecordId's grammar) — both files are outside this ticket's touch_scope, as is src/lib/db/records.ts.
- Verified on the rebased tree (1d0db7d on run/admin-window): both tickets' stored checks, lint, tsc --noEmit, npm test (2999 offline + 6 isolated), npm run test:live -- tests/live/sources.live.test.ts (7, staging read-only). Red-then-green watched twice: the un-fixed page failed the pin on the UPPERCASE spelling only (canonical passed); dropping `.toLowerCase()` from the new helper reddens both new cases.
- HTTP, production build against staging, own bind-probed port 8795 (never 8770/8771), cookie-authed, warm — all 200, 0 error cards: canonical, UPPERCASE, hyphen-less and mixed spellings of 01a05782-… each render exactly data-source="01a05782-8e7f-752c-ae60-3ce4c51962f6"; absent id uppercased -> data-empty="narrowing", 0 rows; ?source_id=nobody -> all 3 rows. Warm /sources 1.59-2.29 s over 5 samples on this machine (narrowed to the uppercase id: 1.70-2.03 s) — slower than QA's 1.42-1.53 s on the same code path, and NOT this diff (a string canonicalisation): data-window-held="877" is unchanged and narrowing still does not collapse it, which is the gauge-chunk residual QA already flagged for admin-window/BUG-0138. The walked tree and the rebased tree share every file under src/ and tests/ (git diff --stat empty).
- 2026-09-09T18:15:42Z [dispatcher] landed ticket/BUG-0139 onto run/admin-window as c8e1908
