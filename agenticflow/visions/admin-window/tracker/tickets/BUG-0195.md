---
id: BUG-0195
type: bug
status: done
milestone: M3
priority: P1
opened_by: qa
parent: FEAT-0016
discovered_from: BUG-0192
children: []
assignee: null
claimed_at: null
depends_on: [BUG-0194]
touch_scope: [tests/offline/sources/page.test.ts]
force: false
resolution: already_done
---
# The run branch fails tsc: BUG-0194's pin annotates a cheerio node as Cheerio<never>

## Description
**Found on the integrated run/admin-window tree (8774a63a) while re-checking BUG-0192/BUG-0193.** Nothing in my batch's scope; this is the branch itself, and no ticket's checks run the command that catches it.

Expected: `ci_command` (agenticflow/run.yaml:32 — `rm -rf .next/types && npm run lint && ./node_modules/.bin/tsc --noEmit && npm test`) exits 0 on the run branch, as STACK.md §5 names `./node_modules/.bin/tsc --noEmit` the repo's type check and every builder reports it green.

Found: `./node_modules/.bin/tsc --noEmit` exits **2** with three errors, all in a helper added by 5d82adbc (`admin-window/BUG-0194: pin /sources' unnamed settled-values narrowing`, `git blame -L 1540,1544`):

```
tests/offline/sources/page.test.ts(1543,26): error TS2345: Argument of type 'Cheerio<Element>' is not assignable to parameter of type 'Cheerio<never>'.
  Type 'Element' is not assignable to type 'never'.
tests/offline/sources/page.test.ts(1543,40): error TS2345: (same, second argument)
tests/offline/sources/page.test.ts(1544,25): error TS2345: (same)
```

The cause is at tests/offline/sources/page.test.ts:1540 — the local helper is typed `(node: cheerio.Cheerio<never>)` and is then handed the `Cheerio<Element>` values `$('[data-window="rejections"]')` and `line.next()` return. `Cheerio<never>` accepts no node at all.

**Repro (exact):**
1. `cd '/Users/ben-m4/Desktop/Coding/KPOP/kspace Admin' && git checkout run/admin-window`
2. `./node_modules/.bin/tsc --noEmit; echo $?` → prints the three errors above and `2`.

Green on the same tree, which is why nothing caught it: `npm run lint` exits 0, `npm run build` exits 0 (Next never type-checks `tests/`), `./node_modules/.bin/vitest run --project=offline tests/offline/sources` passes (vitest transpiles without checking), and BUG-0194's own Checks block runs none of them against the type checker. BUG-0192's builder measured `tsc 0` on 0f0a433c — which is BEFORE 5d82adbc landed (`git log --oneline`: e320dd2f, 2c928a80, 5d82adbc, 0f0a433c, d1e148a6), so the error arrived with that commit and no lane has type-checked the integrated tree since.

**Dispatch note, so this does not become two tickets writing one file:** `tests/offline/sources/page.test.ts` is in the touch_scope of **BUG-0194**, claimed by builder-293 at 2026-09-11T14:38:52Z. This should be folded into that lane (it is three characters — the helper's parameter type) rather than dispatched beside it. If BUG-0194's builder rewrites that helper anyway, close this `already_done` off their commit — but only against the check below, because BUG-0194's own checks cannot see this.

## Acceptance criteria
- `./node_modules/.bin/tsc --noEmit` exits 0 on the landed run branch: the local helper at tests/offline/sources/page.test.ts:1540 is typed to accept the `Cheerio<Element>` nodes it is actually handed (`cheerio.Cheerio<Element>`, or whatever type the rewritten pin passes), never `Cheerio<never>` — and the BUG-0194 pin the helper serves still asserts what it asserted, with no `it.fails`/`@ts-expect-error`/`any` cast standing in for the fix.

## Checks
```sh
./node_modules/.bin/tsc --noEmit
```
## History
- 2026-09-11T14:43:53Z [qa] opened: The run branch fails tsc: BUG-0194's pin annotates a cheerio node as Cheerio<never>
- 2026-09-11T14:49:09Z [architect] milestone: (none) → M3: M3 with BUG-0194: this is BUG-0194's own pin (5d82adbc) and the same file; it closes in that lane's cycle, not later.
- 2026-09-11T14:49:12Z [architect] depends_on: [] → [BUG-0194]: Chained behind BUG-0194 so dispatch never offers both into tests/offline/sources/page.test.ts at once. The fix is three characters inside BUG-0194's own pin helper; builder-293 has been told by the dispatcher to fold it into that lane.
- 2026-09-11T14:49:18Z [architect] criteria amended: Replaced the (to be written) placeholder. One bar, one check; the check is the type checker itself, which is the only thing on this tree that can see the defect. [was: (to be written)]
- 2026-09-11T14:49:24Z [architect] checks amended: Dropped the absolute-path cd prefix QA filed with: the close lane runs this in BUG-0194'\''s worktree (agenticflow/.worktrees/BUG-0194, tsc present there), and a cd to the main checkout would type-check the wrong tree and green a worktree that still carries the error. Dry-run 2026-09-11 on run/admin-window @ c84f68a7: exit 1, the three TS2345s at 1543-1544 — red now, green only when the helper is retyped. [was: cd '/Users/ben-m4/Desktop/Coding/KPOP/kspace Admin' && ./node_modules/.bin/tsc --noEmit]
- 2026-09-11T14:49:31Z [architect] CLOSE PATH (architect ruling, 2026-09-11). Not a lane of its own: folded into BUG-0194, which owns tests/offline/sources/page.test.ts and whose own QA pin (5d82adbc) introduced the Cheerio<never> annotation. builder-293 has been told by the dispatcher to fix the helper's parameter type inside that lane. The QA lane that re-checks BUG-0194 on the landed tree runs THIS ticket's one check verbatim — ./node_modules/.bin/tsc --noEmit, from the lane tree, expecting exit 0 — and, when green, closes BUG-0195 already_done citing BUG-0194's landed commit sha in the closing History line. If BUG-0194's lane lands with tsc still red, BUG-0195 does NOT close: it unblocks (its depends_on is satisfied) and dispatches as its own three-character ticket against the same check. Note for that QA lane: BUG-0194's own Checks block cannot see this defect (lint, build and vitest are all green on the broken tree — vitest transpiles without checking, and Next never type-checks tests/), so run BUG-0195's check explicitly; do not infer it from a green BUG-0194 receipt.
- 2026-09-11T15:02:50Z [qa] open → done: attacked: already fixed in BUG-0194's lane, as the architect's close path allowed. Fixed by bf48005b ("admin-window/BUG-0194: narrow the settled-values scan at the query"), landed onto run/admin-window as c74f2106; verified on b1af049a.
This ticket's own check was run EXPLICITLY, never inferred from BUG-0194's receipt (whose 8 checks cannot see a type error under tests/): `./node_modules/.bin/tsc --noEmit` exits 0 both in the primary checkout and in receipt.py's private detached worktree at b1af049a (tracker/receipts/BUG-0195.json, green).
The fix is the retype the criterion demanded, not a suppression: tests/offline/sources/page.test.ts:1566 now reads `const text = (node: ReturnType<typeof $>) =>` where c84f68a7 had `(node: cheerio.Cheerio<never>)`. Grepped the file for @ts-expect-error / @ts-ignore / any-cast / eslint-disable: 0 hits, and `! grep -q "it.fails("` plus `grep -q "[BUG-0194]"` both exit 0, so the pin the helper serves is alive and unpinned rather than deleted. `Cheerio<never>` survives in two other offline files (ui/paging, dashboard/page); both are inside the tsc program (88 tests/offline files listed) and both check clean, so nothing else of that shape is latent. [resolution: already_done]
