---
id: TASK-0020
type: task
status: built
milestone: M1
priority: P2
opened_by: architect
parent: FEAT-0001
children: []
assignee: builder-59
claimed_at: 2026-09-02T19:14:02Z
depends_on: [TASK-0019]
touch_scope: [docs/build_judgments.md]
force: false
attempts: 1
---
# Compile docs/build_judgments.md for the milestone-close review

## Description
Module: `docs/build_judgments.md` — a review artifact, not product code. Authority: `contracts/admin-build.md`, 'Reviewed after the build': *'at most fifteen, rewritten whole at each milestone close, each entry naming the contract location that was silent'*; M1 EC14.

Runs **last** in the milestone. Collect every judgment the milestone's tickets recorded — each builder records theirs as a ticket comment while the work is fresh, so this is a compile, not an archaeology dig — and write the file **whole** (never appended to):

- At most **fifteen** entries.
- Each entry names **the contract location that was silent** (file and section), what was decided, and why that reading rather than another.
- A judgment that turned out to fork the build is not an entry here — it is a blocked ASK ticket for Ben. This file is for the small readings that had to be made to keep moving, not for decisions that should have been questions.
- Cross-check against the ASK tickets: nothing that is an open question may appear here as a settled judgment.

Also produce the milestone's **cross-directory report** line for the close: every scraper-side commit during M1 is either a declared handoff or a noted minor edit, and no ticket's touch scope named a scraper path. The expected count for M1 is **zero** scraper-side commits — a campaign is running in that repo, so everything there is a handoff.

## Acceptance criteria
[EC14] `docs/build_judgments.md` exists, was rewritten whole at this close, and carries at most fifteen entries, each naming the contract file and section that was silent. No entry restates an open ASK ticket as a settled decision. The cross-directory report states the number of scraper-side commits made during M1 (expected: zero) and confirms no ticket's touch scope named a scraper path.

## Checks
```sh
bash -c "test -f docs/build_judgments.md"
test "$(grep -c '^## ' docs/build_judgments.md)" -le 15
bash -c "grep -qi contract docs/build_judgments.md"
```

## History
- 2026-09-02T05:43:11Z [architect] opened: Compile docs/build_judgments.md for the milestone-close review
- 2026-09-02T19:14:02Z [builder-59] claimed (attempt 1)
- 2026-09-02T19:23:03Z [builder-59] claimed → built: Wrote docs/build_judgments.md whole (new file): 15 entries, Ben's 7 rulings first (env names; groups/idols live writes; dials deferred past M2; resolver-owned display list; pre-cutover provenance line; the nine runs columns; desktop-only + both themes), then 8 architect/QA judgments. Every entry carries a '*Silent at*'/'*Contradictory at*' line naming a contract file + section, '*Decided by*' (Ben/architect/QA/builder) and '*Recorded*' (DECISIONS.md paragraph, ARCHITECTURE section, campaign-qualified ticket). Sources read: TASK-0021..0025/0029/0031, DECISIONS.md (23 paragraphs), ARCHITECTURE 12, and the ruled-judgment blocks in TASK-0002/0004/0011/0015, BUG-0016/0017/0030.
- Proof, all on the rebased tree (50dad3c, rebased onto run/admin-window 600da15): the three stored checks verbatim -> exit 0/0/0, entry count 15. Negative control watched: appending a 16th '## ' entry to a scratch copy takes check 2 to exit 1, so the count check really counts entries (scratch file deleted). A script re-parsed all 15 blocks and asserted each has a silence marker + a named contract file + decider + recorded-at: 15/15 OK. Every section citation was checked against the file it names (fixed two before commit: ARCHITECTURE gauges is 8 not 6; DECISIONS holds 23 paragraphs not 21).
- Cross-directory report, re-measured at handoff: scraper-side commits by this campaign = ZERO (git log --all --grep=admin-window in ../kspace Scraper -> 0; its 260 commits since 2026-09-01 are its own resolver campaign); no ticket's touch scope names a scraper path (0 of 75 ticket files match Scraper or ../); one handoff outstanding, TASK-0031.
TRAPS FOR QA. (1) Entries are '## ' headings on purpose so the stored count check is load-bearing; the sibling scraper repo's edition uses a numbered list, under which grep -c '^## ' would be vacuously 0. The two trailing sections (routed questions, cross-directory report) are '###' for the same reason - do not promote them. (2) Nothing open is stated as settled: TASK-0031 appears ONLY in 'Questions routed rather than decided'; TASK-0029, TASK-0032 and BUG-0027 are named as ruled-but-not-landed with 'open at this close'. Verify against the board if it moved. (3) I checked landed code before asserting it: runs.ts carries the nine columns, ErrorLine.reading is required, next.config.ts holds the rewrite, ROW_CAP=1000 - but config.ts has no 'display' field and no ui primitive carries data-state, which is why entries 4/13/14 say the build has not landed. (4) No test added - touch_scope is this one doc and a doc-shape test would pin presentation.
- 2026-09-02T19:23:26Z [dispatcher] landed ticket/TASK-0020 onto run/admin-window as 3254db3
