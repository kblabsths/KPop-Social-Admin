---
id: BUG-0167
type: bug
status: built
milestone: M3
priority: P2
opened_by: qa
parent: FEAT-0014
discovered_from: TASK-0074
children: []
assignee: builder-260
claimed_at: 2026-09-11T05:27:25Z
depends_on: [TASK-0075]
touch_scope: [src/lib/db/gauges.ts, src/lib/gauges/pending-claims.ts, tests/offline/gauges/pending-claims.test.ts, tests/live/claims.live.test.ts]
force: false
attempts: 1
---
# the gauge's two legs truncate on different orders, so at the row cap a tie on the boundary instant drops claims the id list kept

## Description
**Expected** (TASK-0074 acceptance criterion 2): "The claim set is UNCHANGED" — the windowed claims leg selects the same claims the id-list join did.
**Found**: at the row cap the two sets differ. The windowed shape returned 2 claims where the id-list join returned 3.

**Why.** The two legs of `fetchPendingClaims` (src/lib/gauges/pending-claims.ts:224) carry the SAME cap (`PENDING_CLAIMS_DEFAULTS.limit` = `GAUGE_ROW_CAP` = `ROW_CAP` = 1000, src/lib/db/result.ts:55) but NOT the same order:

- claims leg — `readPendingClaimsInWindow` (src/lib/db/claims.ts:181) orders `observed_at asc, observation_id asc`. A TOTAL order, and its own test asserts that is the point: 'so which claims are inside a truncated window is decided by the database and not by a tie'.
- observations scan — `readPendingObservations` (src/lib/db/gauges.ts:355) orders `observed_at` ALONE. No tiebreak, so `limit` over rows tied on the boundary instant returns an ARBITRARY subset.

`fetchPendingClaims` then intersects the claims with the scan's ids (pending-claims.ts:236). When the two legs cut the tie differently the intersection is smaller than either leg, and smaller than what `.in(<the scan's ids>)` returned before this ticket. The old shape could not diverge: it asked for exactly the ids the scan returned.

**Reachability is near, not theoretical.** TASK-0074's own census: staging holds 877 live pending claims in the 90-day window and 877 pending observations — 88% of the 1000 cap, 123 rows of headroom. The live check that proves the two shapes agree (tests/live/claims.live.test.ts, 'the windowed claims read and the id-list join select the same claims') asserts BOTH legs came back under their cap and says so in its own message: 'a truncated read would make this a question about where two windows were cut'. So the regime this ticket is about is the one that live proof deliberately excludes, and it is the regime the view enters at 1000 rows.

**Repro (exact), pinned in the suite.** tests/offline/gauges/pending-claims.test.ts, test 'PIN admin-window/BUG-0167: at the cap, a tie on the boundary instant costs claims the id list kept' — currently `it.fails`, i.e. it is RED against the landed code and `it.fails` is what keeps the suite green. Four claims share one instant (2026-08-01T00:00:00Z), ids …c01–c04, cap 3:
- the scan came back with {c02, c03, c04} — a legal answer for `order by observed_at limit 3` over four tied rows;
- the claims leg, ordered totally, took {c01, c02, c03};
- `fetchPendingClaims` returns {c02, c03}; `readPendingClaimRows(idsOf(scan))` returns {c02, c03, c04}.
Watched red before the pin was applied: `AssertionError: expected [ …(2) ] to deeply equal [ …(3) ]`, the missing id being …c04.

**User-visible effect** once the view crosses 1000 rows in the window: /claims' and /sources' pending-claims figures lose an arbitrary handful of claims that the scan itself saw, and the number moves between renders because the tie subset the database picks is arbitrary per query. The window is flagged `truncated` in that state and the page calls its figures a floor, which is why this is P2 and not P1 — but it is a lower floor than the read actually had in hand, and a nondeterministic one.

**The remedy that makes the identity unconditional** (proposed, not prescribed): give the scan the same total order the claims leg has — `.order("observation_id", { ascending: true })` after its `observed_at` order in `readPendingObservations`. With one total order and one cap over a population the claims are a SUBSET of, the claims below the scan's cutoff are exactly the claims leg's first N, so the intersection is the id-list join's set in every state, ties included. Whatever shape is chosen, flip the pin from `it.fails` to `it` — a strict pin, so it reddens the day the divergence returns.

**Not in scope**: CHUNK_FANOUT, the per-bucket counts and seeks, paging, and anything rendered.

## Acceptance criteria
The pin `PIN admin-window/BUG-0167: at the cap, a tie on the boundary instant costs claims the id list kept` in tests/offline/gauges/pending-claims.test.ts is flipped from `it.fails` to a plain `it` and passes, with its BUG id spelled in the test name — the windowed claims leg and the id-list join select the same claims when both legs truncate at the same cap and the boundary instant is tied. The identity holds for at least one more shape than the tie: add a cap fixture where the boundary instant is NOT tied and the populations differ (a pending observation the view holds no claim for, so the claims leg reaches further in time than the scan) and assert the two shapes still agree. Nothing about the ticket's own guarantees moves: the two legs are still issued together (the in-flight-order test stays green and still fails against the sequential shape), the observations refusal still wins when both legs refuse, no refusal becomes a half-filled gauge or a zero, and nothing rendered changes on /claims, /sources, /queues or the review item — their offline page suites and tests/live/claims.live.test.ts are green unchanged. The live set-equality check keeps its own cap guard: it must still refuse to compare when either leg came back at 1000, since a green there would then be about the cap and not about the shapes. That check arrives on this branch already bounded to one snapshot instant by TASK-0075 (DECISIONS 2026-09-10, the identity-proof ruling): keep both cap guards AND both upper edges — a leg here that drops the `.lt("observed_at", asOf)` bound puts the 877-vs-879 scraper race back into this ticket's own proof. (human-checkable) /claims warm server time over HTTP against a production build does not regress past 1.4 s (QA measured 1.134 s median, 7 warm samples, on the tree this bug was found on); state the median you measured in the closing History entry.

## Checks
```sh
./node_modules/.bin/vitest run --project=offline tests/offline/gauges/pending-claims.test.ts
./node_modules/.bin/vitest run --project=offline tests/offline/gauges/bounded.test.ts tests/offline/gauges/standing-disagreements.test.ts
./node_modules/.bin/vitest run --project=offline tests/offline/claims/page.test.ts
! grep -q "it.fails" tests/offline/gauges/pending-claims.test.ts
npm run lint
npm run build
npm run test:live -- tests/live/claims.live.test.ts
```
## History
- 2026-09-10T23:25:49Z [qa] opened: the gauge's two legs truncate on different orders, so at the row cap a tie on the boundary instant drops claims the id list kept
- 2026-09-11T00:17:05Z [architect] depends_on: [] → [TASK-0075]: Destination collision + a flaky check, one edge (architect, 2026-09-10). BUG-0167 declares tests/live/claims.live.test.ts in scope and runs it as a stored check; TASK-0075 rewrites that file's identity proof to read a bounded snapshot (DECISIONS 2026-09-10). Running BUG-0167 first would (a) collide at the same destination and (b) grade a one-line ordering fix with a suite that reddened 3 of 5 runs for builder-250. TASK-0075 first, then this. No other edge added: the tie-break itself is independent of everything open.
- 2026-09-11T00:17:05Z [architect] checks amended: One added line, the rest verbatim (architect, 2026-09-10): the criteria require the PIN to be flipped from it.fails to a plain it, and nothing stored graded that — a builder could have left the xfail in place and greened every line, because an it.fails file is green precisely while the defect stands. Measured on this tree: one it.fails at tests/offline/gauges/pending-claims.test.ts:507, so the new line is RED today and green the day the pin is flipped, which is the presence-of-the-fix form. Line 1 re-measured green today (34 passed, 556ms). [was: ./node_modules/.bin/vitest run --project=offline tests/offline/gauges/pending-claims.test.ts, ./node_modules/.bin/vitest… (397 chars; full text in git history)]
- 2026-09-11T00:17:26Z [architect] criteria amended: CONFIRMED COMPLETE as QA wrote them, with two edits and no change of bar (architect, 2026-09-10). QA's criteria are machine-checkable end to end except one clause, which is now marked: the 1.4 s warm-time guard cannot be a stored check (it needs a production build over HTTP against staging), so it carries (human-checkable) and its number goes in the closing History, exactly as TASK-0074's did. Second edit: the live set-equality check now arrives pre-bounded by TASK-0075, so this ticket keeps BOTH the cap guards and the snapshot upper edges rather than reverting one while editing the other. The proposed remedy (give readPendingObservations the same observation_id tie-break the claims leg has) stays proposed, not prescribed, and the extra untied-boundary fixture QA asked for stays required: one total order over a population the claims are a subset of is what makes the identity unconditional, and the second fixture is what proves the criterion is about the shapes and not about the tie. A stored check for the pin flip was added separately (see the checks amendment). [was: The pin `PIN admin-window/BUG-0167: at the cap, a tie on the boundary instant costs claims the id list kept` in tests/of… (1557 chars; full text in git history)]
- 2026-09-11T05:27:25Z [builder-260] claimed (attempt 1)
- 2026-09-11T05:40:37Z [builder-260] claimed → built: FIX (1 line + its doc): readPendingObservations (src/lib/db/gauges.ts) now orders observed_at asc, observation_id asc — the claims leg's own total order — so at the shared cap both legs cut ONE order and the intersection is the id-list join's set in every state. Measured cost of the extra ORDER BY on staging (877 rows, 8 paired reads, warm-up pair dropped): 132.2 ms median without it, 134.5 ms with — noise.
TESTS (tests/offline/gauges/pending-claims.test.ts): QA's pin is a plain it and passes, name verbatim; watched RED first on this tree with the exact reported failure (2 ids vs 3, missing …c04) before the src line went in. Two cap fixtures now, both at limit 3: the TIED one, and an UNTIED one where the populations differ (6 pending observations, claims for 4 of them — asserted: scan stops at …003, claims leg reaches …005, both shapes return {…001,…003}). New local helper capped() gives the SCAN a fixture database that answers its own .order()/.limit() (claimView's engine, reused not re-rolled), and hands tied rows in REVERSED id order — a legal answer for an incompletely ordered limit, which is what makes the pin strict: drop the tie-break and it reddens. idListJoin now takes bounds. The scan's chain assertion at line ~170 asserts both order clauses.
QA TRAPS: (1) tests/live/claims.live.test.ts — I added ONE line, .order("observation_id") on shape B's scan, so the comparand stays the scan the gauge issues. Both cap guards and both .lt(asOf) edges untouched; live suite 13/13 green on the rebased tree. (2) Nothing rendered changed: full offline suite 3688 passed, lint + build + tsc clean, all 7 stored checks green on the rebased tree (rebased onto 565cd51).
WARM /claims, production build over HTTP against staging (STACK §5 block, prefix on both commands, port 8785; 8770/8771/8772/8790 barred), 3 warm-ups discarded + 7 timed samples, every sample 200: MEDIAN 1.335 s (1.197-1.577) pass 1, MEDIAN 1.218 s (1.089-1.663) pass 2. BAR 1.4 s MET on both passes; ranges overlap QA's 1.134 s (1.119-1.243) on the pre-fix tree, and the paired query timing above says the tie-break is not where the spread comes from (this box had a concurrent QA lane running).
- 2026-09-11T05:40:58Z [dispatcher] landed ticket/BUG-0167 onto run/admin-window as 672854b
