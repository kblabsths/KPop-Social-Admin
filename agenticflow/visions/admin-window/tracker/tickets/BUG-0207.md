---
id: BUG-0207
type: bug
status: built
milestone: M3
priority: P0
opened_by: verifier
parent: null
children: []
assignee: builder-307
claimed_at: 2026-09-11T20:38:06Z
depends_on: []
touch_scope: [tests/offline/handoff/settle-review-item.test.ts]
force: false
attempts: 2
---
# The offline suite is RED at HEAD: the KS-code guard cannot tell our own installed handoff from a collision

## Description
M3 endgame verification, 2026-09-11, HEAD bed26500 on run/admin-window.

FROM A CLEAN START:
  env -u STAGING_SUPABASE_URL -u STAGING_SUPABASE_SERVICE_ROLE_KEY -u SUPABASE_URL -u SUPABASE_SERVICE_ROLE_KEY ./node_modules/.bin/vitest run --project=offline
EXPECTED (M3 EC1): exit 0, the suite passes offline by default.
OBSERVED: exit 1. 'Test Files 1 failed | 79 passed (80); Tests 1 failed | 3968 passed (3969)'.
  FAIL tests/offline/handoff/settle-review-item.test.ts > the settle_review_item migration > allocates no code the sibling's tree holds today
  AssertionError: expected [ 'KS029', 'KS030', 'KS031', 'KS032' ] to deeply equal []   (page.test line 854)
Same single failure with the staging names SET (vitest --project=offline, sourced .env): 1 failed | 3968 passed. The isolated project fails too, on the same root cause: tests/isolated/probe-race.isolated.test.ts > 'reddens no OTHER offline file that walks the tree either' (exit 1).

WHY IT FIRED, established read-only next door:
  ls -lT '../kspace Scraper/supabase/migrations/20260908000001_the_verdict_becomes_a_row.sql' -> Sep 11 11:59:31 2026
  ls -lT '../kspace Scraper/supabase/migrations/20260908000002_the_verdict_settles_the_item.sql' -> Sep 11 11:59:31 2026
  ls -lT '../kspace Scraper/tests/helpers/ks_codes.py' -> Sep 11 11:59:48 2026 (git-modified, adds UNREADABLE_VERDICT=KS029, WONT_FIX_WITHOUT_NOTE=KS030, ITEM_NOT_OPEN=KS031, SETTLEMENT_NOT_APPLIED=KS032)
Ben installed THIS campaign's own two handoff artifacts into the sibling working tree at 11:59 today, minutes before this walk and after QA declared M3 dry. The codes the sibling 'holds today' are the codes our own artifact allocates: same four codes, same four meanings. ksCodesUnder(SIBLING_ROOT) reads every KSnnn as text and cannot tell our installed artifact from a stranger's claim.

WHY IT MATTERS: 'npm test' is red for every builder from now on, and it goes red precisely BECAUSE the campaign's satisfaction condition (the handoffs land next door) is being met. The guard's purpose - do not allocate a code the sibling already means something else by - is still worth holding; what it must stop doing is counting our own file's codes against us.

## Acceptance criteria
Against the sibling tree AS IT STANDS TODAY (both handoff migrations present under ../kspace Scraper/supabase/migrations/ and ks_codes.py carrying KS029-KS032), 'vitest run --project=offline' and '--project=isolated' each exit 0 with the staging names unset and again with them set. The guard still fails if a code this artifact allocates is spelled next door by a file that is NOT one of this campaign's own handoff artifacts (pin that arm on a fixture, so the protection is not simply deleted). The dated snapshot TAKEN_NEXT_DOOR and the live scan keep disagreeing loudly rather than silently.

## Checks
```sh
env -u STAGING_SUPABASE_URL -u STAGING_SUPABASE_SERVICE_ROLE_KEY ./node_modules/.bin/vitest run --project=offline
env -u STAGING_SUPABASE_URL -u STAGING_SUPABASE_SERVICE_ROLE_KEY ./node_modules/.bin/vitest run --project=isolated
npm run lint
```

## History
- 2026-09-11T19:24:29Z [verifier] opened: The offline suite is RED at HEAD: the KS-code guard cannot tell our own installed handoff from a collision
- 2026-09-11T19:34:21Z [builder-307] claimed (attempt 1)
- 2026-09-11T19:45:50Z [builder-307] claimed → built: - The sibling KS-code scan now returns WHICH files spell each code, and a spelling next door is attributed to us when its line appears verbatim in a for-human note whose `target file` row names a sibling migration (derived from the notes, not a list). Our installed migration is that note's one sql block (file = block + trailing newline, measured); the four ks_codes.py / test_codes_named_once.py entries are §1a's python block. Line-level, not path-level: a later rewrite at the target path that re-points a code still fails.
- Collision arm intact and pinned on in-memory fixtures (new test "tells this handoff's own spellings from a stranger's claim on the same codes"): our two pasted halves must NOT flag, a stranger's `LEASE_NOT_RENEWED = "KS031"` MUST flag line by line, and a code we do not allocate (KS027) is not our business. Second new test pins where the corpus comes from. TAKEN_NEXT_DOOR stays a literal KS001-KS028, never re-derived from the scan; the live check's failure now names file: line, deduped.
- Verified on the rebased tree (5fe98fd6): offline 3971 passed / isolated 8 passed, each with staging names unset and again with them set (all exit 0); tsc --noEmit 0; npm run lint 0. Watched red first, twice: emptying the attribution corpus reddens the live check (34 lines, naming the pasted migration) plus both new tests; making unattributedSpellings return [] reddens the fixture test, so the protection cannot be deleted silently.
- QA notes: the worktree has no node_modules (I ran via a symlink to the primary's, removed before handoff). Nothing under ../kspace Scraper was touched — reads only; its git status is unchanged (2 untracked migrations + 2 modified test files, all Ben's 2026-09-11 install). Before Ben's paste the live check still passes (no spellings found), so this is not keyed on today's tree.
- 2026-09-11T19:46:13Z [dispatcher] landed ticket/BUG-0207 onto run/admin-window as c2cfe8af
- 2026-09-11T19:46:54Z [qa] built → qa: under attack
- 2026-09-11T19:57:33Z [qa] qa → reopened: BUG-0212 filed. Criteria 1 and 3 hold, criterion 2 does not.

GREEN, re-run on the landed tree (c2cfe8af, primary checkout): offline exit 0 (3971 passed) with STAGING_*/SUPABASE_* unset and again with .env sourced; isolated exit 0 (8 passed) both ways; npm run lint exit 0; tsc --noEmit exit 0. TAKEN_NEXT_DOOR is still a literal KS001-KS028 (line 144) and the live scan is still a real read — neither was edited into agreement (criterion 3).

ATTACK (scratch replica of the sibling, built from read-only copies of its two installed migrations, ks_codes.py, test_codes_named_once.py and tools/staging/harness_objects.sql; nothing next door was written, moved or read beyond those copies; probe copy of the test file with SIBLING_ROOT repointed, deleted after):
 - baseline replica -> exit 0 (harness faithful);
 - stranger python claim 'LEASE_NOT_RENEWED = "KS031"' -> exit 1, naming both lines. Arm alive here;
 - scan non-vacuity: replica stripped of every KS027/KS028 speller -> exit 1 on the sentinel. Not vacuous;
 - STRANGER SQL MIGRATION claiming KS031 for its own meaning, in the sibling's own idiom, comments spelling no code -> exit 0. SILENT.

Expected (criterion 2): a file next door that is NOT one of this campaign's handoff artifacts spelling an allocated code fails the guard. Found: it passes, because attribution is per line and the only line that spells the code is 'using errcode = 'KS031',' — a line our own note carries verbatim. That idiom is not an edge case next door: 'using errcode = 'KS024',' x22, ''KS029'' x20, KS003 x12, KS006 x11, and every KS001-KS032 raise is written that way (counted read-only over supabase/migrations + tools/staging). The existing fixture pins the arm in the ONE grammar the note does not carry per code (python), so the arm passes for the shape that does not occur next door and misses the shape that does. The handoff's claim that 'a later rewrite at the target path that re-points a code still fails' is false for the same reason: a re-point reuses that same errcode line.

Pinned and pushed at e9a8221f: tests/offline/handoff/settle-review-item.test.ts > 'flags a stranger's raise of an allocated code in the sibling's own SQL idiom', it.fails (strict xfail, BUG-0212), watched red first as a plain it(): expected [] to deeply equal [ "using errcode = 'KS029'," ]. Suite stays green; the fix flips it.fails to it.

Not asking for path-based attribution — rejecting it was right. The corpus being every line of the notes rather than the notes' code-bearing spellings is where the leak is.
- 2026-09-11T20:38:06Z [builder-307] claimed (attempt 2)
- 2026-09-11T20:44:28Z [builder-307] claimed → built: - BUG-0212's defect fixed at the root QA named: the corpus is no longer the notes' LINES. A spelling next door is ours only when it sits inside a verbatim, CONTIGUOUS copy of one of this campaign's paste blocks (a fenced block of a note whose `target file` row names a sibling migration). A one-line idiom is not an identity; 776 lines of it in order are. The three real installed halves still attribute — the migration IS the sql block, the registry entries ARE §1a's two python blocks — and the live scan over the sibling as it stands passes.
- Coverage is positional, so carrying our block buys no amnesty: a verbatim copy plus one extra `using errcode = 'KS029';` flags exactly that line (new fixture). QA's `it.fails` pin flipped to a plain `it`; two more pins added — the artifact's own idiom line standing ALONE is not attributed, and every allocated code must be spelled inside a block. The re-point case QA called false is now true for the same reason: a rewrite no longer matches the block it came from.
- Watched red first: with HANDOFF_BLOCKS degraded back to one-line blocks (the pre-fix line corpus), QA's SQL-idiom test and the new block-identity test both fail; restored, 44 pass in that file.
- Verified on the rebased tree (024f3766, run branch head a1c3d6b8): offline 3972 passed / isolated 8 passed, each with staging names unset and again with .env sourced — all exit 0; tsc --noEmit 0; npm run lint 0. Sibling read-only throughout, nothing written there (its git status is unchanged: 2 untracked migrations + 2 modified test files, Ben's install). Worktree carries no node_modules; I ran via a symlink to the primary's, removed before handoff.
- 2026-09-11T20:44:50Z [dispatcher] landed ticket/BUG-0207 onto run/admin-window as d5d36383
