---
id: BUG-0211
type: bug
status: built
milestone: M3
priority: P3
opened_by: verifier
parent: null
children: []
assignee: builder-311
claimed_at: 2026-09-15T04:41:55Z
depends_on: []
touch_scope: [src/lib/paging/machine.ts, src/components/ui/paging.tsx, tests/offline/paging/machine.test.ts, tests/offline/ui/paging.test.ts]
force: false
attempts: 1
---
# The paging control stays drawn and enabled after a page answers not_provisioned — the one arm whose text offers no press

## Description
M3 endgame verification, 2026-09-11, HEAD bed26500, production build on port 8775 against staging, Chromium 1440x900, walk cookie.

STEPS: load /claims (50 rows, control reads 'Show the next 50 claims'); intercept the next request to /api/admin/claims/rows and fulfil it 200 with the handler's own shape {"kind":"not_provisioned","missing":"pending_claims"}; press the control once.

OBSERVED: rows 50 -> 50 (correct), window line byte-unchanged (correct), and the refusal renders exactly the gray clause it should: 'pending_claims isn't in this database yet — it arrives with the scraper repo's migrations.' — with no 'Press it again', correctly, because Refusal's not-provisioned arm deliberately offers 'no fix the operator cannot take' (src/components/ui/paging.tsx, the comment above that arm). BUT the control itself is still there, directly beneath that sentence, enabled: [data-paging="more"] present, disabled=false, label unchanged. Screenshot at agenticflow/tracker/evidence/M3/verifier/claims-arm-not_provisioned.png (gitignored; the measurement above is the durable record).

The other two arms keep the control on purpose and say so ('Press it again to ask for the same rows.') — that is right for a refused bound and for a read error. This arm is the one where the app has just learned the table is not there, and M3 EC5's own heading is 'a paging affordance is never drawn where it cannot be honoured'; F14 says 'a control that cannot be honoured is never drawn (F10's rule, applied here)'.

NOT A FAILURE OF EC5's GRADED CLAUSES, and I say so plainly: all three pass. On the server-rendered PGRST205 surface the affordance is genuinely absent (zero 'data-paging' occurrences, zero 'Show the next' on /claims and /browse, measured on an instance pointed at a PGRST205 stub); on an exhausted set it is absent; a refusal extends no list. This is the mid-session arm only — the table went away between the server's screen and the press — and it is filed low for that reason.

WHY FILE IT AT ALL: it is the one arm where the sentence and the widget disagree about whether there is anything left to ask for.

## Acceptance criteria
After a page answers not_provisioned, the surface offers no press: [data-paging="more"] is absent from the rendered markup (not merely disabled), the not-provisioned clause still names its table exactly once, the rows already on screen are untouched, and the window line is byte-identical to before the press. The refused-bound and error arms are unchanged — they keep their control and their 'Press it again' sentence, pinned so this fix cannot spread to them.

## Checks
```sh
./node_modules/.bin/vitest run --project=offline tests/offline/paging/machine.test.ts tests/offline/ui/paging.test.ts tests/offline/claims/page.test.ts tests/offline/browse/page.test.ts
npm run lint
npm run build
```

## History
- 2026-09-11T19:26:01Z [verifier] opened: The paging control stays drawn and enabled after a page answers not_provisioned — the one arm whose text offers no press
- 2026-09-15T04:41:55Z [builder-311] claimed (attempt 1)
- 2026-09-15T04:57:18Z [builder-311] WALK (real surface, this fix in place): production build of ticket/BUG-0211 on port 8781 against staging ubfjjqlvnpnoborczbdb, Chromium 1440x900, minted walk cookie, /claims (689 claims, window 50). Playwright intercepted **/api/admin/claims/rows* and fulfilled 200 application/json with the handler's own shapes; each arm pressed once from a fresh load. not_provisioned {missing:pending_claims}: rows [data-claim] 50 -> 50; [data-paging="more"] 1 -> 0; [data-paging] arms after the press = [] (no exhausted and no limit sentence in the control's place); refusal line reads 'pending_claims isn't in this database yet — it arrives with the scraper repo's migrations.' and names pending_claims exactly once; both [data-window] paragraphs byte-identical before vs after (compared as outerHTML strings). MUST-NOT-FLAG, same page, same script: error {reading:pending_claims,message:'canceling statement due to statement timeout'} -> rows 50 -> 50, more 1 -> 1, arms ['more'], line byte-identical, 'Press it again to ask for the same rows.' still there; refused {reason:'a bound of 50 is not one this view serves'} -> identical numbers. Server was killed and every walk file deleted; these numbers are the durable record.
- 2026-09-15T04:57:31Z [builder-311] claimed → built: - src/components/ui/paging.tsx only: PageMore reads the state's own published condition ('not provisioned') and withdraws the control for that ONE arm — drawing no sentence in its place, because the refusal clause above it is already the whole account. Read ahead of the exhausted and ceiling arms so neither can claim the set ended or that this app stopped at its own bound. The machine is untouched: the refusal still leaves the state idle, so every machine pin stands.
- Proof: ./node_modules/.bin/vitest run --project=offline tests/offline/paging/machine.test.ts tests/offline/ui/paging.test.ts tests/offline/claims/page.test.ts tests/offline/browse/page.test.ts (466 passed on the REBASED tree), npx tsc --noEmit, npm run lint, npm run build all green; full offline+isolated npm test green once. New tests watched RED against the pre-fix component first (5 failures), then green. Real-surface walk numbers are in the History comment above.
- QA, the traps: (1) four existing pins asserted the OPPOSITE behaviour for this arm and are REWRITTEN, not deleted — tests/offline/ui/paging.test.ts 'changes nothing else about the line', and three in tests/offline/{claims,browse}/page.test.ts (outside my touch_scope; the fix's blast radius is those two page files plus the two in scope). Every one of them now carries a must-NOT-flag twin pressing the same surface with an error or a refused-bound answer, which keep their control and their 'Press it again' sentence: the withdrawal must not spread to the retryable arms. (2) The exhausted+not-provisioned combination is unreachable from the driver (a refusal always lands idle); I ordered the absence arm FIRST anyway so the rule is total — if you can reach that pair, the absence wins by design, not by accident. (3) The worktree keeps a node_modules symlink to the primary checkout so vitest runs there; nothing else was left behind (server killed, .env symlink, .next and all walk files removed).
- 2026-09-15T04:57:54Z [dispatcher] landed ticket/BUG-0211 onto run/admin-window as 18937f03
