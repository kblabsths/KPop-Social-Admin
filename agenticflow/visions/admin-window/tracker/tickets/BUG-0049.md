---
id: BUG-0049
type: bug
status: qa
milestone: M1
priority: P3
opened_by: designer
parent: null
discovered_from: M1-endgame-designer-walk
children: []
assignee: builder-100
claimed_at: 2026-09-04T04:12:58Z
depends_on: []
touch_scope: [src/app/queues/page.tsx, src/app/sources/page.tsx, tests/offline/queues/page.test.ts, tests/offline/sources/page.test.ts]
force: false
siblings: [BUG-0045, BUG-0046]
attempts: 1
---
# Micro labels uppercase machine identifiers: DATA_CONFLICT OPEN, ENTITY_LINK FOLDED, TICKETMASTER STUCK RECORDS

## Description
Found on the M1 endgame designer walk (2026-09-03), walk instance :8771 against staging.

Voice copy bar 5 names this exact string as its example: "Machine identifiers (**`data_conflict`**, `admin_locked`, `event_performers`, `wont_fix`) render verbatim in mono and are never prettified into Title Case prose."

The queue-health cards on `/queues` build their `micro` labels by concatenating a queue name with English, and the `micro` step is uppercase sans, so the identifier's case is rewritten on screen:

- `DATA_CONFLICT OPEN`, `DATA_CONFLICT FOLDED`, `DATA_CONFLICT OPEN AGE`
- `ENTITY_LINK OPEN`, `ENTITY_LINK FOLDED`
- `/sources`: `TICKETMASTER STUCK RECORDS` (a source name, same treatment)

The page already does it right one line above: the subsection heading renders `data_conflict` verbatim, lowercase, in mono `data` — so the same screen shows the same identifier two ways, three pixels apart.

This is the collision of two of the file's own rules (the `micro` step is uppercase; identifiers are verbatim mono), and the page has already demonstrated the way out: keep the identifier out of the sans label, the way the subsection heading does.

Evidence: `agenticflow/tracker/evidence/designer-walk-m1/queues-light.png` (the queue-health cards under the `data_conflict` heading), `sources-light.png`.

Smallest finding of the walk; filed because copy bar 5 names the string, not because it hurts.

**What this adds that its siblings do not.** BUG-0045 removes two prose sentences' ticket id; BUG-0046 fixes the count sub-lines under the figures. This one is the `micro` eyebrow label ABOVE the figure — a third string on the same card, cited to a different bar (copy bar 5, not 6), and it also reaches `/queues`, which neither sibling touches.

## Acceptance criteria
No `micro` label on any page contains an uppercased machine identifier. Where a card belongs to `data_conflict` or `entity_link`, the identifier appears verbatim in mono — as the subsection heading already renders it — and the sans label carries only the app's own words ("Open", "Folded", "Open age").

Source names get the same treatment: `ticketmaster` is not uppercased into a sans label.

Bucket, shape and status BADGES are unaffected — they are already mono `data` and already verbatim.

`tests/offline/queues/page.test.ts` and `tests/offline/sources/page.test.ts` assert no rendered `micro`-class element contains an underscore.

Human-check: on `/queues`, the string `data_conflict` looks the same everywhere it appears on the screen. (human-checkable)

## Checks
```sh
npx vitest run --project=offline tests/offline/queues/page.test.ts tests/offline/sources/page.test.ts tests/offline/gauges-ui/cards.test.ts
./node_modules/.bin/tsc --noEmit
npm run lint
```

## History
- 2026-09-03T22:05:31Z [designer] opened: Micro labels uppercase machine identifiers: DATA_CONFLICT OPEN, ENTITY_LINK FOLDED, TICKETMASTER STUCK RECORDS
- 2026-09-04T04:12:58Z [builder-100] claimed (attempt 1)
- 2026-09-04T04:32:10Z [builder-100] Real-render measurement, staging, dev instance on :8779 (my own; killed after), 1440x900, Chromium, both color schemes, 2026-09-03.

Computed styles of every visible leaf whose text carries an underscore, /queues and /sources, light AND dark: text-transform=none, font-family=Geist Mono, font-size=11px, letter-spacing=normal — for data_conflict, entity_link, data_conflict_fact, entity_link_fact, entity_link_source_pattern, source_id, test_harness, test_harness_control, stuck_pattern. Two inks only: #4a5565/#99a1af (secondary) and #1e2939/#e5e7eb (primary) — both existing palette jobs.

Queue-health eyebrow, both schemes: identifier span 'data_conflict' = Geist Mono 11px, transform none, tracking normal; words span 'open' = Geist 10px, transform uppercase, tracking 0.5px; parent textContent is exactly "data_conflict open" (the space is a text node, not a flex gap).

Review-item detail /queues/01a06287-5b67-7d1f-b4a5-d90aa93cb01a: dial eyebrow textContent "ticketmaster stuck records"; 'ticketmaster' = Geist Mono 11px transform none, 'stuck records' = Geist 10px uppercase. Every remaining uppercased element on that page is the app's own English (Review item, What happened, Evidence, record, value, source, tier now, observed, payload, stuck records, day (UTC), records). Browser console clean in both schemes.

Nothing was written to staging: no walk_sandbox reset, no catalog edit — this was a read-only render check.
- 2026-09-04T04:32:27Z [builder-100] claimed → built: One primitive, not per-site edits: new `src/components/ui/micro-label.tsx` (`MicroLabel = string | {identifier, words?}`, `Eyebrow`, `microLabelText`). The identifier renders as a SIBLING of the `micro` span in `type-data` mono with `normal-case tracking-normal`, so nothing carrying an underscore is ever inside the element that uppercases; only our words get `micro`. The separator is a real text node, not a flex gap. Prop widened (string still works) through StatCard/Empty/NotProvisioned/GaugeCard/GaugeStateCard/Distribution/TrendTable.

Verified: `npx vitest run --project=offline tests/offline/queues/page.test.ts tests/offline/sources/page.test.ts tests/offline/gauges-ui/cards.test.ts tests/offline/ui/tokens.test.ts tests/offline/ui/contrast.test.ts tests/offline/ui/primitives.test.ts`, `npm test` (1891+6), `npm run test:live` (72, whole suite — shared primitives), `tsc --noEmit`, `npm run lint`, `npm run build` — all on the rebased tree. Red-then-green: reverting only the two page call sites reddens exactly the two new page guards ('data_conflict open' x4, 'source_id'). Render measured in both schemes on my own :8779 instance; numbers are in the History comment above.

QA, three things to attack. (1) Scope beyond touch_scope, deliberately: the fix reaches `src/components/{ui,gauges,queues,claims,review}` and `src/app/queues/[reviewItemId]/page.tsx` — that last one is where `TICKETMASTER STUCK RECORDS` actually lived, not `/sources`. (2) A judgement call worth challenging: a filter group's label IS the URL parameter it sets, so I treated every facet as an identifier — `KIND QUEUE SHAPE STATUS` on /queues and `BUCKET SOURCE_ID DOMAIN` on /claims now render lowercase mono like `source_id` does. One rule, no per-facet lookup table; if the designer wants `kind` to stay a sans eyebrow, that is a copy decision, not a code one. (3) Same bug class, NOT fixed and not mine to guess: `/records/[table]/[id]` builds its h1 as `${config.table} record` in `type-title` (also uppercase sans), so `walk_sandbox` reaches the screen as `WALK_SANDBOX RECORD`. Fixing it means deciding whether a page h1 may mix mono and sans — worth its own ticket. The guard `uppercasedIdentifiers` in tests/offline/ui/markup.ts only looks at `type-micro` and so does not see it.
- 2026-09-04T04:32:49Z [dispatcher] landed ticket/BUG-0049 onto run/admin-window as 4d2deed
- 2026-09-04T04:33:34Z [qa] built → qa: under attack
