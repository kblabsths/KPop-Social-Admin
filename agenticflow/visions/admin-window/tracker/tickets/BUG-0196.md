---
id: BUG-0196
type: bug
status: done
milestone: M3
priority: P2
opened_by: designer
parent: null
discovered_from: designer:endgame-walk
children: []
assignee: null
claimed_at: null
depends_on: [DEBT-0020, BUG-0199]
touch_scope: [src/lib/db/result.ts, src/lib/db/paging.ts, src/lib/paging/bounds.ts, src/lib/paging/machine.ts, src/components/ui/error-line.tsx, src/components/ui/paging.tsx, src/components/ui/state-of.tsx, src/components/gauges/state.tsx, src/app/queues/[reviewItemId]/page.tsx, tests/offline/db/result.test.ts, tests/offline/ui/primitives.test.ts, tests/offline/ui/paging.test.ts, tests/offline/ui/tokens.test.ts, tests/offline/live-guard.test.ts, tests/offline/paging/machine.test.ts, tests/offline/paging/bounds.test.ts]
force: false
siblings: [BUG-0197, BUG-0198]
attempts: 1
resolution: fixed
---
# A failed read's account renders the app's own sentences in the mono face reserved for the machine's words

## Description
**Where the rule is.** `agenticflow/docs/vision/LOOK_AND_FEEL.md` → The Look → Typography:
*"**Mono carries every value the database produced** — counts, ids, timestamps,
values, source names, buckets, tiers, error strings. Sans carries every word the
app wrote — labels, headings, buttons, prose. That split *is* the typographic
idea: the operator can always see which words are the machine's."* This campaign
has already fixed this rule once, at P2, in this exact family: BUG-0175 moved
the paging refusal's app-authored reason out of the mono run.

**What I measured**, 1440x900, light, production build of f9165d18 on the walk
instance, `/claims`, the paging route answered with each arm's real shape:

| the answer's `message` | what renders, and in what face |
| --- | --- |
| `a 4524-character markup document arrived here instead of the database's own words` | `pending_claims — a 4524-character markup document arrived here instead of the database's own words` — **Geist Mono 11px**, `rgb(193,0,7)`, one span |
| `the read was refused with no words to explain it` | `pending_claims — the read was refused with no words to explain it` — **Geist Mono 11px**, one span |
| `TypeError: fetch failed Caused by: … (3 runtime stack frames dropped)` | the whole run, frames clause included — **Geist Mono 11px**, one span |

Every one of those sentences was written by THIS APP, in
`src/lib/db/result.ts`: `documentInstead` (:389), `serialisedInstead` (:410),
`framesInstead` (:427) and `REFUSED_WITHOUT_WORDS` (:575). None of them is a
string any database produced. They reach the operator in the face the palette
and the type scale reserve for the machine, inside the same red mono run as a
real Postgres string, and nothing on screen distinguishes them from one.

**The document clause is the sharpest case: its own content says "these are not
the database's words", and it says it in the database's face.**

**Two renderers carry it, not one.** The account is one string by the §4.1
contract, and both places that render a failed read spell it as a single mono
span:

- `src/components/ui/error-line.tsx:46` — `<span className="type-data">{named ? \`${reading} ${EM_DASH} ${failed}\` : failed}</span>`, which is data-surface state 4 on **every** page;
- `src/components/ui/paging.tsx:713` — the paging refusal's `machineWrote` arm, which takes the same account off the wire.

So this is not a paging defect; paging is where M3 made it easy to see. The
same three clauses render in mono in `/cycles`' error column path, the Dashboard's
error lines, and every `/claims`, `/queues`, `/sources` and `/browse` card whose
read refuses.

**Reachable, not hypothetical.** ARCHITECTURE.md's dated 2026-09-11 entry
records QA measuring *"a 4,530-character Cloudflare document rendered into three
`/claims` error cards"* on this stack, and `result.ts`'s own docstring records a
4,524-character document arriving through `code` on a production build. The
clause exists because that shape arrives; the face it arrives in is what this
ticket is about.

**Not a prescription, but the shape of the problem, so the scope is right:** the
account crosses `lib/db → view` as ONE string, so no renderer can tell the app's
clauses from the database's. Whatever separates them has to be decided where the
clauses are authored, not guessed at by a regex in the view — a view-side
"does this sentence look like ours" test is the fourth question `result.ts`
already refuses to ask (LESSONS 4, ARCHITECTURE §7 common violations row 15).

## Acceptance criteria
1. On a failed read, the words THIS APP wrote render in the sans `body` step and
   the words the DATABASE produced render in the mono `data` step, in the same
   line, on both renderers — `src/components/ui/error-line.tsx` (state 4, every
   page) and the paging refusal's machine arm
   (`src/components/ui/paging.tsx`). The object being read stays a machine
   identifier in mono in every arm, and the retry stays in sans, as they are today.
2. The four clauses that move are exactly the four `src/lib/db/result.ts`
   authors: `documentInstead` (:389), `serialisedInstead` (:410),
   `framesInstead` (:427) and `REFUSED_WITHOUT_WORDS` (:575). Their WORDING is
   not changed by this ticket — only which face carries them.
3. A database message that answers none of the account's three questions still
   crosses byte-identical and still renders wholly in mono: a real PostgREST
   refusal reads exactly as it reads today, code in parentheses included.
4. Where the app's clause and the database's words appear in ONE account (a part
   that kept its cause sentence and dropped frames), both faces appear in that
   one line, each over its own run. No account is dropped, truncated or
   reordered to make this easier.
5. **THE MECHANISM IS RULED — architect, 2026-09-11, in BUG-0175's shape.** The
   decision about who authored a part is made where the part is authored
   (`src/lib/db/result.ts`), never by inspecting the rendered string in a
   component; no renderer matches a sentence against a list of the app's own
   phrases. Concretely, authorship TRAVELS WITH THE CLAUSE as a fact, from where
   the clause is written to where it is drawn:
   **(a) One derivation, at the author.** `errorMessage`'s derivation yields the
   account as an ORDERED LIST OF TYPED SEGMENTS — each segment the words of one
   part or clause plus WHO WROTE THEM — in the campaign's existing two-value
   vocabulary, the `"this app"` / `"the machine"` spelling `ReasonAuthor`
   already carries (`src/lib/paging/machine.ts:114`). One vocabulary for this
   fact in this app: import the existing type or move it to a leaf both sides
   may import (§4 rule 7 — `lib/db/**` may not import `lib/paging/**`); a second
   spelling of the same two-valued fact is the defect, not a style choice. The
   four authors of criterion 2 emit `"this app"` segments; a part that answers
   none of the three questions emits one `"the machine"` segment carrying it
   verbatim.
   **(b) The string is derived FROM the segments, not beside them.** The
   `message` of `DbResult`'s error arm (§4.1) is the join of the segment list.
   That is what makes criterion 3's byte-identity structural instead of a
   promise: there is no second code path composing the same account, and
   `withoutSecrets` still runs last over what crosses.
   **(c) The fact crosses every seam the account already crosses, and is never
   re-derived at the far end.** `src/lib/db/paging.ts` (`pageAnswerOf`) → the
   wire shape and its validator in `src/lib/paging/bounds.ts` (`PageAnswer`,
   `isPageAnswer`) → `refuse()` / `refusalFor()` in
   `src/lib/paging/machine.ts`, whose refusal carries the account's segments
   rather than one opaque string. The two route handlers still pass the error
   arm through UNCHANGED; nothing about the account is decided in a route.
   On the wire the authorship is OPTIONAL and its ABSENCE means what the app
   renders today — the whole account is the machine's. An answer carrying a
   `message` and no authorship is still a valid `PageAnswer`, still renders
   wholly in mono, and is NOT turned into "the page request answered something
   this app cannot read": a validator made stricter here would break the forced
   answers criterion 8 and every live probe use, and would put a fourth question
   on the wire that §4.1 refuses.
   **(d) Both renderers set the face FROM the segment, off ONE derivation.**
   `error-line.tsx` and the paging refusal's machine arm each draw a `type-data`
   run for a `"the machine"` segment and a `type-body` run for a `"this app"`
   one. The run-splitting is written ONCE and consumed by both (a shared helper,
   a shared primitive, or the segment list itself); a second hand-written walk
   in the second renderer is the duplication this criterion forbids — two copies
   of one rule drift, and this rule has already been fixed once in this family.
   **(e) The test of the whole mechanism:** reword any of the four clauses and
   NO face moves. That is the bar `reasonFrom` passes today.
6. `tests/offline/db/result.test.ts` and `tests/offline/ui/primitives.test.ts`
   assert the split structurally — which run each part lands in — and not by
   pinning the product's sentences byte for byte.
7. **BUG-0175's isolation is not regressed.** `dir="ltr"` on the paging
   refusal continues to isolate exactly the runs this app did NOT author
   (ARCHITECTURE.md §7) — after this ticket that is the identifier plus each
   `"the machine"` segment, and never a run this app wrote. The
   not-provisioned arm, the exhausted/loading/idle arms and the bound-ceiling
   arm are byte-identical: this ticket moves faces on the failed-read account
   and nothing else.
8. Human-check: on `/claims`, force the paging route to answer
   `{"kind":"error","reading":"pending_claims","message":"a 4524-character markup
   document arrived here instead of the database's own words"}` and press the
   control; the sentence reads in Geist (sans) beside the mono
   `pending_claims`, and a forced real Postgres message still reads wholly in
   Geist Mono. **(human-checkable)**

## Checks
```sh
npm run lint
npm run build
! grep -qF "{named ? \`\${reading} \${EM_DASH} \${failed}\` : failed}" src/components/ui/error-line.tsx
./node_modules/.bin/vitest run --project=offline tests/offline/db/result.test.ts tests/offline/ui/primitives.test.ts tests/offline/ui/paging.test.ts tests/offline/ui/tokens.test.ts tests/offline/live-guard.test.ts tests/offline/paging/machine.test.ts tests/offline/paging/bounds.test.ts
npm run test:live -- tests/live/claims.live.test.ts
```
## History
- 2026-09-11T15:27:21Z [designer] opened: A failed read's account renders the app's own sentences in the mono face reserved for the machine's words
- 2026-09-11T15:34:36Z [designer] Evidence (gitignored, local): agenticflow/tracker/evidence/M3/designer/claims-refusal-dbdoc-1440.png, claims-refusal-dbnowords-1440.png, claims-refusal-dbframes-1440.png — each is the refusal line as rendered on /claims at 1440x900 on a production build of f9165d18, with the route answered by the arm named in the filename. The 1280 twins beside them are the same lines at the supported floor. The durable record is the ticket's own words: the three sentences, the face they render in (Geist Mono 11px, rgb(193,0,7)), and the two renderer lines that produce it.
- 2026-09-11T15:41:29Z [architect] criteria amended: Mechanism RULED (criterion 5), in BUG-0175's shape: authorship travels with the clause as a typed segment decided in result.ts, the flat §4.1 message is the JOIN of the segments (byte-identity by construction), the fact crosses db/paging.ts -> bounds.ts wire -> machine.ts refusal without re-derivation, and ONE run-splitting derivation serves both renderers. Wire authorship is optional and its absence means what renders today (whole account = the machine's), so a forced {kind,reading,message} answer stays valid. Added criterion 7: BUG-0175's dir=ltr isolation is not regressed and the other paging arms stay byte-identical. Designer's 1-4, 6 and the human-check are unchanged. [was: 1. On a failed read, the words THIS APP wrote render in the sans `body` step and the words the DATABASE produced render … (1999 chars; full text in git history)]
- 2026-09-11T15:41:37Z [architect] touch_scope: [src/lib/db/result.ts, src/components/ui/error-line.tsx, src/components/ui/paging.tsx, tests/offline/db/result.test.ts, tests/offline/ui/primitives.test.ts, tests/offline/ui/paging.test.ts] → [src/lib/db/result.ts, src/lib/db/paging.ts, src/lib/paging/bounds.ts, src/lib/paging/machine.ts, src/components/ui/error-line.tsx, src/components/ui/paging.tsx, src/components/ui/state-of.tsx, src/components/gauges/state.tsx, src/app/queues/[reviewItemId]/page.tsx, tests/offline/db/result.test.ts, tests/offline/ui/primitives.test.ts, tests/offline/ui/paging.test.ts, tests/offline/ui/tokens.test.ts, tests/offline/live-guard.test.ts, tests/offline/paging/machine.test.ts, tests/offline/paging/bounds.test.ts]: Widened to the seams the ruled mechanism crosses, measured on the tree: the account reaches ErrorLine through THREE hops that retype it (state-of.tsx passes the whole DbUnavailable; gauges/state.tsx carries failed: string, built at app/queues/[reviewItemId]/page.tsx:259), and reaches the paging refusal through db/paging.ts pageAnswerOf -> bounds.ts PageAnswer/isPageAnswer -> machine.ts refuse/refusalFor. Test pre-scope by symbol literal: ErrorLine is constructed directly in tokens.test.ts:121 and live-guard.test.ts (:747,:992,:1085,:1102) as well as primitives/result; reasonFrom is graded in paging/machine.test.ts. Over-catching is deliberate (scopes do not schedule); a file left out is a gap in the map. The two route handlers are NOT in scope: they pass the error arm through unchanged and nothing about the account is decided there.
- 2026-09-11T15:41:44Z [architect] checks amended: Same bar, widened to the files the ruled scope adds. The offline set measured 2026-09-11 on d2610aec: 7 files, 487 tests, all green, 1.7s — targeted, not the suite. The grep is unchanged (designer's, dry-run RED today at exit 1 and green only when that single mono span is replaced). [was: npm run lint, npm run build, ! grep -qF '{named ? `${reading} ${EM_DASH} ${failed}` : failed}' src/components/ui/error-l… (330 chars; full text in git history)]
- 2026-09-11T15:43:26Z [architect] depends_on: [] → [DEBT-0020, BUG-0199]: Truth before cosmetics, and the ordering is not a preference here. DEBT-0020 + its twin BUG-0199 are a CORRECTNESS defect in the account's derivation (a transport failure states its cause sentence twice on every shape node actually produces); BUG-0196 is which FACE the account's parts render in. Both write src/lib/db/result.ts and tests/offline/db/result.test.ts, and BUG-0196's ruled mechanism RESTRUCTURES the derivation the dedup lives in — errorMessage stops returning a string and starts returning typed segments, with saidOnce's line-level pass sitting inside one of them. Landing the cosmetic first would make the correctness builder rebase its dedup onto a rewritten derivation mid-bounce, on a ticket that has already bounced once. Depends on BOTH so the chain holds whichever of the two closes first. Nothing else in BUG-0196's widened scope is contended: no open ticket touches lib/paging/**, lib/db/paging.ts, the ErrorLine hops or their tests.
- 2026-09-11T16:29:26Z [builder-299] claimed (attempt 1)
- 2026-09-11T16:57:31Z [builder-299] claimed → built: Authorship travels with the clause. New pure leaf src/lib/account/authored.ts owns the ONE ReasonAuthor spelling (moved out of lib/paging/machine.ts, re-exported there), the segment shape, accountText (the join) and accountRuns (the ONE run-splitting derivation). errorMessage -> errorAccount: DbResult's error arm gains optional authored[], message is accountText of it (byte-identity structural; withoutSecrets runs last, per run). Crosses db/paging.ts -> PageAnswer/isPageAnswer -> refuse() unre-derived; wire authorship is OPTIONAL (absent = whole account is the machine's, so a forced {kind,reading,message} stays valid) and refused whole only when PRESENT and unreadable. AuthoredAccount in ui/error-line.tsx draws the split once; ErrorLine and the paging Refusal both use it (no hand-written second walk).

Verified on the rebased tree (e324a459): npm run lint 0, npm run build ok, the ticket's grep exits 0, tsc --noEmit clean, ./node_modules/.bin/vitest run --project=offline over the 7 named files + paging/answer.test.ts + db/layering.test.ts = 569 pass, full npm test 3949 pass, npm run test:live -- tests/live/claims.live.test.ts 17 pass. Red-then-green watched: flipping documentInstead/framesInstead to 'the machine' reddens 3 result tests; forcing every run to type-data reddens 10 (incl. BUG-0175's own arms, which proves both renderers share one derivation); dropping answer.authored reddens 2. Observed at the real surfaces: doc clause + REFUSED_WITHOUT_WORDS render type-body beside mono isolated pending_claims; the frames shape renders 'pending_claims — TypeError: fetch failed Caused by: ...' mono + '(1 runtime stack frame dropped)' sans in ONE line; 42501 stays one mono run 'pending_claims — permission denied for table pending_claims (42501)'.

Traps for QA: (1) PageRefusal's broken arm replaces reasonFrom with account[]; reason stays and is DERIVED (the join) at refuse(). (2) Beyond touch_scope: the new leaf + its LEAF_MODULES entry in tests/offline/db/layering.test.ts; wire tests went in tests/offline/paging/answer.test.ts (the file that owns isPageAnswer); 8 whole-arm toEqual pins in gauges/*/edit tests gained authored. (3) Two deliberate deltas beyond face: ErrorLine's machine run now carries dir=ltr (§7 isolation extended, not regressed), and a whitespace TEXT NODE sits between adjacent runs so concatenated text is not 'faileda 4524-character' — CSS does not render a whitespace-only anonymous flex item, so nothing moves on screen. Pre-existing and untouched: still no space between the account span and the retry span.
- 2026-09-11T16:58:09Z [dispatcher] landed ticket/BUG-0196 onto run/admin-window as 6b3a58ea
- 2026-09-11T16:58:40Z [qa] built → qa: under attack
- 2026-09-11T17:14:56Z [qa] qa → done: Attacked on the landed tree (fb3c8e88), every measurement re-derived rather than taken from the handoff.

RE-RAN, primary checkout: lint 0, build 0, the grep 0, the 7-file offline set 513 pass, tsc --noEmit 0, test:live claims 17 pass. Receipt GREEN on the same five.

BROWSER, production build, 1440x900, Chromium, BOTH renderers — computed font-family read off the live spans, not the stylesheet:
- ErrorLine (state 4), driven end to end by pointing an instance at a stand-in PostgREST on 127.0.0.1:8783 with a DUMMY key (staging untouched for these four; reads only):
  * document -> 'pending_claims' [Geist Mono 11px rgb(193,0,7) dir=ltr] + '— a 4488-character markup document arrived here instead of the database's own words' [Geist 12px, no dir]. The sharpest case is in the reading face.
  * bodiless 503 -> the refused-without-words clause in Geist 12px beside the mono identifier.
  * socket reset -> 'pending_claims — TypeError: fetch failed Caused by: SocketError: other side closed (UND_ERR_SOCKET)' [Geist Mono 11px dir=ltr] + '(4 runtime stack frames dropped)' [Geist 12px]. Criterion 4: both faces, one line, own runs, cause stated ONCE (DEBT-0020 holds).
  * real 42501 -> 'pending_claims — permission denied for table pending_claims (42501)' ONE mono run, parens included. Criterion 3 at the surface.
- Paging Refusal, against STAGING (66 rows held, 66 after every refused press — nothing half-filled):
  * the body the route REALLY emits for a document (verified through pageAnswerOf: authored=[{words:…,author:'this app'}]) -> the sentence reads Geist 12px beside mono isolated 'pending_claims'. Criterion 8's intent, met.
  * machine+app answer -> split; unreadable authorship -> refused whole, message never drawn; not-provisioned and bound-ceiling arms unchanged.
- Criterion 7: every type-data run carries dir=ltr, every type-body run carries none. No app-authored run is isolated.
- Whitespace-node claim MEASURED: the anonymous text node between runs has a 0x0 client rect and the inter-run distance is exactly the container's 8px column-gap. 0px shift.
- Layering: the new leaf is in LEAF_MODULES, existence-ratcheted, foreignImports([])=[] and it imports nothing.

BROKE IT ONCE -> BUG-0200 (P2, filed, pinned it.fails in tests/offline/db/result.test.ts, watched red first: 'the arm carries no authorship: expected undefined to be defined'). readComplete (:1018, :1027) and readCount (:1102) compose three accounts of their OWN, wholly this app's prose, carrying no authorship — so they still read in the mono face on /sources, /queues, /browse and /claims. Criterion 2 enumerated exactly four authors and the builder honoured that fence and documented the exclusion, so this is a scope gap in the criteria, not a defect in the landed mechanism: BUG-0196 is done on its own terms.

RESIDUALS (handoff, no tickets): (1) criterion 8's LITERAL forced shape — {kind,reading,message} with no authorship — reads in MONO, not sans; criterion 5c's later ruling explicitly requires exactly that, so the two criteria contradict on that one shape and the ruling governs. (2) A wire answer whose authored disagrees with message renders authored and silently drops message (authored:[] shows the wordless clause instead); unreachable from any server we ship. (3) Pre-existing, untouched: no space between the account and the retry run, so text() reads '…wordsReload to try…'. [resolution: fixed] [receipt green: 5 cmd(s), tree 2d9a51e9]
- 2026-09-11T17:23:07Z [architect] ARCHITECT, 2026-09-11, recorded on the closed ticket (comment only — a done ticket's criteria are never amended). THE RULING GOVERNS, criterion 8's literal is superseded. Criterion 8 asks a human to force {"kind":"error","reading":"pending_claims","message":"a 4524-character markup document..."} and expect the sentence in Geist SANS. Criterion 5c — written later, by me — makes wire authorship OPTIONAL with ABSENCE meaning the whole account is the machine's, so that exact forced shape renders wholly MONO **by design**: it carries no authorship, and an answer with no authorship is one the app must not guess about. The two clauses contradict on that one shape and 5c wins; QA measured the right thing (it forced the body the route REALLY emits, authored=[{words:...,author:'this app'}], and read Geist beside the mono identifier), so criterion 8's INTENT is met and its literal example is stale. Anyone re-running criterion 8 by hand must force the authored shape, not the three-key one. No ticket: the mechanism is as ruled and the product is right. TWO RESIDUALS QA LEFT, both judged and both deliberately ticket-less: (1) a wire answer whose 'authored' disagrees with its 'message' renders 'authored' and silently drops 'message' (authored:[] draws nothing) — correct precedence, since the segments are the account and message is documented as their join (5b), and no server we ship can emit a disagreeing pair; if one ever could, the fix is at the emitter, never a renderer reconciling two accounts. (2) No space between the account run and the retry run, so text() reads '...wordsReload to try again' — PRE-EXISTING, untouched by this ticket, and invisible on screen (the runs are flex items separated by the container's 8px column-gap; QA measured the inter-run distance as exactly that gap). It is a defect only for a caller reading concatenated textContent, which today is a test's idiom rather than an operator's; if the designer wants it closed it is a one-line renderer ticket of their filing, not a correction to this one.
