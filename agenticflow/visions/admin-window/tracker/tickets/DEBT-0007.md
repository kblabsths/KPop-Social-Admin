---
id: DEBT-0007
type: debt
status: done
milestone: M2
priority: P3
opened_by: qa
parent: FEAT-0010
discovered_from: qa:BUG-0087
children: []
assignee: null
claimed_at: null
depends_on: []
touch_scope: [src/lib/verdict/decision.ts, src/components/review/close/conflict-actions.tsx, src/components/claims/claim-list.tsx, src/app/queues/[reviewItemId]/page.tsx]
force: false
siblings: [BUG-0091]
attempts: 1
resolution: fixed
---
# a fact identifier is hand-spelled ${domain}.${field} in four places, one of them now load-bearing for the reference rule

## Description
**Expected** (ARCHITECTURE.md §13.7, quoted by BUG-0087's own builder note: "a helper several tickets need is seeded before them, or it becomes N copies that drift"; the same doctrine `settlePath` and `recordFieldApiPath` exist for — "two spellings of a path is two chances for one of them to be wrong in a way nothing catches"): the app's spelling of a fact identifier, `domain.field`, is produced in ONE place.

**Found**: four independent spellings on the landed tree (154503f), grepped:

    src/app/queues/[reviewItemId]/page.tsx:236          `${observation.domain}.${observation.field}`  -> EvidenceRow.fact
    src/components/claims/claim-list.tsx:144            `${row.domain}.${row.field}`                  -> the rendered identifier
    src/components/review/close/conflict-actions.tsx:80 `${fact.domain}.${fact.field}`                -> factName(), for `supplies` and the notice
    src/lib/verdict/decision.ts:240                     `${domain}.${field}`                          -> isReferenceField's lookup key

BUG-0087's builder flagged three of these in the handoff note and did not fix them; the fourth is the one that ticket added, and it is the one that changed the stakes. `isReferenceField` (`decision.ts:240`) builds the key it looks up in `REFERENCE_FIELDS`, and `factName` (`conflict-actions.tsx:80`) builds the identifier the withheld-control notice renders and the supply control's `supplies` carries. Those two must agree for the reference rule to hold at all: the predicate that decides whether a `venue_id` fact may be settled with typed text is a string built by hand, four lines of code away from another hand-built copy of the same string. Today they agree because both are the obvious two-part template; nothing tests that they will.

**Why it is DEBT and not a BUG**: no divergence is observable today. All four spellings produce the same string for every row the resolver can file — `tests/offline/review-item/conflict-actions.test.ts` drives `REFERENCE_FIELDS` through both `isReferenceField` and `factName`, and the notice's `data-close-notice` attribute matches the constant's own spelling at every card count. This is the seam that makes a future divergence silent, not a present defect.

**The shape of the fix** (architect's call on where it lands): one exported helper — `factName(domain, field)` or the like — in the pure domain leaf the components already import (`src/lib/verdict/decision.ts`), with the four call sites above using it. `REFERENCE_FIELDS`'s entries then read as the same helper's output rather than as a second literal spelling. The one thing to keep: `decision.ts` is a leaf (ARCHITECTURE.md §4 rule 7, pinned by `tests/offline/db/layering.test.ts`) — the helper goes IN it, and nothing goes into it from outside.

## Acceptance criteria
(to be written)

## Checks
```sh
./node_modules/.bin/vitest run --project=offline tests/offline/review-item/ tests/offline/verdict/ tests/offline/claims/
```

## History
- 2026-09-09T04:22:29Z [qa] opened: a fact identifier is hand-spelled ${domain}.${field} in four places, one of them now load-bearing for the reference rule
- 2026-09-09T04:22:40Z [qa] Sibling BUG-0091, filed by the same QA lane on the same tree: BUG-0091 is a DEFECT — the pre-database guard accepts a text-carrying supply_value for a reference fact and the route answers 200 — and its fix adds one invariant to `decisionRefusals`. DEBT-0007 adds no behaviour and fixes no defect: it is the four-way hand-spelling of `domain.field` that both the guard and the notice depend on agreeing about. They can land in either order and neither blocks the other; if BUG-0091's builder is already inside `decision.ts` adding the invariant, folding this in there is cheaper than a second visit.
- 2026-09-09T04:28:12Z [builder-153] claimed (attempt 1)
- 2026-09-09T04:40:35Z [builder-153] claimed → built: Fold: `factKey(domain, field)` added to the leaf src/lib/verdict/decision.ts (still imports nothing). REFERENCE_FIELDS entries and isReferenceField's lookup are both its output, so the list and the key can no longer be two joins; the three surfaces call it — close slot's `supplies` + withheld notice (local `factName` deleted), claim table's fact cell, review-item evidence rows. No behaviour change: every produced string is byte-identical.
Proof (on the rebased tree, HEAD 2530083 over run/admin-window 44ab63c): `vitest run --project=offline tests/offline/review-item/ tests/offline/verdict/ tests/offline/claims/ tests/offline/edit/` 14 files / 604 pass; `tsc --noEmit` 0; `npm run lint` 0; full `npm test` with STAGING_*/SUPABASE_* unset from a detached _receipt- worktree on that commit: 67 files / 2502 offline + 6 isolated, exit 0.
For QA: the new guard lives in tests/offline/verdict/decision.test.ts ("the hand-spelling guard itself") and scans src/** via sourceFiles()+codeText — it asserts the join is spelled NOWHERE but factKey's body, and exactly once there. It is deliberately narrow: it flags a dot-join whose halves name a `domain` and a `field` (template or `+`), not any `${a}.${b}`, because src/lib/db/result.ts legitimately joins table.column that way — must-flag and must-not-flag fixtures for both spellings are in the same block. Watched red once: restoring claim-list's old template made `sites` come back with 2 entries. It scans src/ only — tests/offline/review-item/page.test.ts keeps its hand-built expectation on purpose (independent oracle). Beyond touch_scope: tests/offline/verdict/decision.test.ts (tests for the new helper); no other file outside scope touched.
- 2026-09-09T04:40:53Z [dispatcher] landed ticket/DEBT-0007 onto run/admin-window as 793ad4e
- 2026-09-09T04:47:05Z [qa] built → qa: under attack
- 2026-09-09T04:57:10Z [qa] qa → done: Attacked with BUG-0091 as one batch (landed 793ad4e, re-verified at dc1507a); receipt GREEN (11 files / 468 pass).

factKey is the single producer, verified by reading the tree, not the note: 4 call sites folded (queues/[reviewItemId]/page.tsx:252, claims/claim-list.tsx:145, close/conflict-actions.tsx:130+171) and REFERENCE_FIELDS' two entries AND isReferenceField's lookup key are both its output — the list and the key can no longer be two joins, which was the whole stake. decision.ts still imports nothing (grep: zero import lines; layering guard green).

Sabotaged the src/** scan three ways in a throwaway detached worktree, each watched RED on 'finds the join in src/ nowhere but factKey's own body':
- restored claim-list's template join in a .tsx -> 2 sites instead of 1;
- a SECOND join inside factKey's own file -> 'to have a length of 1 but got 2' (the exactly-once clause is live);
- a `domain + "." + field` concatenation added to src/lib/db/result.ts -> flagged by name.
And the narrowness is real, not luck: result.ts's genuine `${missing}.${column}` table.column join sits in the must-not fixture set and the clean tree scans to exactly [src/lib/verdict/decision.ts].

P3 proportionality: one pass, no new tests required of it. Rendered output is unchanged — claims page, review-item evidence rows and the close notice all still green, including page.test.ts's deliberately hand-built independent oracle.

Full tree green from a detached _receipt- worktree with staging/supabase env unset: tsc 0, lint 0, npm test 67 files / 2520 + 6 isolated, test:http 16. No staging write; .env never opened.

Residual (no ticket, the builder documented it): the scan matches on variables literally named domain/field, so `${row.domain}.${row.fieldName}` would evade it. That is the declared line — it catches the copy-paste this debt was made of and does not pretend to catch a rename. [resolution: fixed] [receipt green: 1 cmd(s), tree 6df4e147]
