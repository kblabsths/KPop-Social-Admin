---
id: TASK-0061
type: task
status: built
milestone: patch
priority: P3
opened_by: strategist
parent: null
discovered_from: verifier
children: []
assignee: builder-245
claimed_at: 2026-09-10T22:06:46Z
depends_on: []
touch_scope: [README.md, agenticflow/docs/STACK.md]
force: false
attempts: 1
---
# README.md still describes the retired dashboard

## Description
Recorded by the verifier at the M2 close as doc drift, not filed then. `README.md` describes the dashboard this campaign deleted: scraper operations, reconciliation review, `.env.local`, port 3000. Nothing it names is reachable — `/scrapers` and `/review` are in the retired-route 404 set. The accurate instructions already exist in `agenticflow/docs/STACK.md` §5.

The repo's front door telling a new reader to run a page that 404s is the same defect class as every navigation-honesty bug this campaign filed, one layer out. Rewrite it against what the app IS: the data ecosystem's window, six pages, staging by name, the live/offline test tiers. Do not duplicate STACK.md §5 — point at it.

## Acceptance criteria
README.md names no retired surface: the strings /scrapers, /review, .env.local and port 3000 occur zero times in it, and every path and command it names exists in this repo. It describes the six-page window and points at agenticflow/docs/STACK.md §5 for the run instructions rather than restating them. No secret name carries a value.

## Checks
```sh
! grep -nE '/scrapers|/review|\.env\.local|:3000' README.md
npm run lint
```

## History
- 2026-09-10T21:23:25Z [strategist] opened: README.md still describes the retired dashboard
- 2026-09-10T22:06:46Z [builder-245] claimed (attempt 1)
- 2026-09-10T22:15:12Z [builder-245] claimed → built: README.md rewritten against the app that exists: the six pages and what each answers, the record/edit surface, env NAMES only (no values), the offline/live/http test tiers, and a pointer to agenticflow/docs/STACK.md §5 for run instructions instead of a second copy. STACK.md itself unchanged — §5 was already correct.
- New guard in tests/offline/shell/shell.test.ts ("the README's front door", 7 cases): README names every NAV_ITEMS route, no RETIRED_PATHS entry, no .env.local/:3000, and only paths, app routes, npm scripts and .env.example names that exist here. Two of them carry a positive probe so they cannot pass vacuously. Also added /scrapers and /review to RETIRED_PATHS, which widens the existing not-found coverage.
- Verified on the rebased tree (onto a9cca85): grep -nE '/scrapers|/review|\\.env\\.local|:3000' README.md finds nothing (exit 1); npm run lint exit 0; tsc --noEmit exit 0; full npm test exit 0 (3380 offline + 6 isolated). Red-then-green: the 7 new cases were run against git show HEAD:README.md (the old front door) and 5 of them failed there.
- Trap for QA: the worktree has no node_modules of its own — I symlinked the primary checkout's to run vitest and removed it before handoff. The path-existence assertion allowlists '.env' (gitignored by design; '.env.example' is the tracked proof of the names) and skips leading-slash tokens, which the route assertion checks against src/app instead.
- 2026-09-10T22:15:31Z [dispatcher] landed ticket/TASK-0061 onto run/admin-window as 0f06d3b
