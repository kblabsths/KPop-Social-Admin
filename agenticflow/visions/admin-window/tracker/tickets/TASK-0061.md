---
id: TASK-0061
type: task
status: done
milestone: patch
priority: P3
opened_by: strategist
parent: null
discovered_from: verifier
children: []
assignee: null
claimed_at: null
depends_on: []
touch_scope: [README.md, agenticflow/docs/STACK.md]
force: false
attempts: 1
resolution: already_done
---
# README.md still describes the retired dashboard

## Description
Recorded by the verifier at the M2 close as doc drift, not filed then. `README.md` describes the dashboard this campaign deleted: scraper operations, reconciliation review, `.env.local`, port 3000. Nothing it names is reachable — `/scrapers` and `/review` are in the retired-route 404 set. The accurate instructions already exist in `agenticflow/docs/STACK.md` §5.

The repo's front door telling a new reader to run a page that 404s is the same defect class as every navigation-honesty bug this campaign filed, one layer out. Rewrite it against what the app IS: the data ecosystem's window, six pages, staging by name, the live/offline test tiers. Do not duplicate STACK.md §5 — point at it.

## Acceptance criteria
README.md names no retired surface: the strings /scrapers, /review, .env.local and port 3000 occur zero times in it, and every path and command it names exists in this repo. It describes the six-page window and points at agenticflow/docs/STACK.md §5 for the run instructions rather than restating them. No secret name carries a value.

## Checks
```sh
! grep -nE '/scrapers|/review|\.env\.local|:3000' README.md
npm run lint
```

## History
- 2026-09-10T21:23:25Z [strategist] opened: README.md still describes the retired dashboard
- 2026-09-10T22:06:46Z [builder-245] claimed (attempt 1)
- 2026-09-10T22:15:12Z [builder-245] claimed → built: README.md rewritten against the app that exists: the six pages and what each answers, the record/edit surface, env NAMES only (no values), the offline/live/http test tiers, and a pointer to agenticflow/docs/STACK.md §5 for run instructions instead of a second copy. STACK.md itself unchanged — §5 was already correct.
- New guard in tests/offline/shell/shell.test.ts ("the README's front door", 7 cases): README names every NAV_ITEMS route, no RETIRED_PATHS entry, no .env.local/:3000, and only paths, app routes, npm scripts and .env.example names that exist here. Two of them carry a positive probe so they cannot pass vacuously. Also added /scrapers and /review to RETIRED_PATHS, which widens the existing not-found coverage.
- Verified on the rebased tree (onto a9cca85): grep -nE '/scrapers|/review|\\.env\\.local|:3000' README.md finds nothing (exit 1); npm run lint exit 0; tsc --noEmit exit 0; full npm test exit 0 (3380 offline + 6 isolated). Red-then-green: the 7 new cases were run against git show HEAD:README.md (the old front door) and 5 of them failed there.
- Trap for QA: the worktree has no node_modules of its own — I symlinked the primary checkout's to run vitest and removed it before handoff. The path-existence assertion allowlists '.env' (gitignored by design; '.env.example' is the tracked proof of the names) and skips leading-slash tokens, which the route assertion checks against src/app instead.
- 2026-09-10T22:15:31Z [dispatcher] landed ticket/TASK-0061 onto run/admin-window as 0f06d3b
- 2026-09-10T22:15:58Z [qa] built → qa: under attack
- 2026-09-10T22:19:57Z [qa] qa → reopened: BUG-0165 filed. Both stored checks pass (grep exit 1, npm run lint exit 0) and every address the new README offers resolves — 21 backticked paths, all 6 NAV_ITEMS routes, all 9 npm scripts, STACK.md's §5 heading text. What bounces it is the guard the builder shipped alongside: tests/offline/shell/shell.test.ts:184 reads .env.example OFF DISK, and the primary checkout's copy has not declared SUPABASE_URL / SUPABASE_SERVICE_ROLE_KEY since 2026-09-03 (uncommitted, mtime 2026-09-03T13:51:53Z). ./node_modules/.bin/vitest run --project=offline exits 1 here: 1 failed | 3388 passed, the one failure being that case. Green in a worktree (HEAD's .env.example still has the pair — which is where 'full npm test exit 0' was measured), red where Ben and ci_check run; verdict depends on uncommitted state. Same root reaches the prose: README.md:49-55 still says those two values live in a local gitignored .env, which STACK.md §5 and Ben's 2026-09-03 ruling retired — the doc-drift class this ticket exists to kill.
- 2026-09-10T22:38:36Z [qa] reopened → done: attacked: re-checked fix-scoped, zero-diff re-prove. Stored checks green on the landed tree, receipt-observed: the retired-surface grep exits 1 (check exit 0) and npm run lint exits 0. Criteria verified independently of the guard the builder shipped - I re-extracted every backticked token from README.md with my own extractor and resolved each one: 33 address-shaped tokens, ALL present, and every one of the 18 named files is git-TRACKED rather than merely on disk (src/lib/supabase.ts, src/lib/db/client.ts, src/lib/auth.ts, src/lib/admin.ts, src/lib/edit/config.ts, src/middleware.ts, src/app/not-found.tsx, src/app/api/health/route.ts, src/components/shell/nav-items.ts, tests/suite-globs.ts, tests/walk/session-cookie.mts, package.json, eslint.config.mjs, railway.toml, AGENTS.md, agenticflow/docs/STACK.md, agenticflow/docs/DECISIONS.md, .env.example); the guard regex skipped none of them. All 6 nav routes resolve under src/app and /records/ resolves as its parameterised directory. All 9 npm commands the README names exist in package.json (dev, build, start, lint, typecheck, test, test:isolated, test:live, test:http) - typecheck included, which STACK section 5 does not list. Six-page window: the README table is exactly NAV_ITEMS, in order, and Running it points at agenticflow/docs/STACK.md section 5 (The exact incantations) instead of restating a launch line. No secret name carries a value: no NAME=value form appears, and I watched the same predicate flag one when appended in a throwaway worktree. The README security claim was checked against code too - no "use client" file imports the db seam or reads process.env (the one grep hit in src/lib/db/client.ts is a doc comment at line 16). REOPEN REASON DEAD: the diff since the bounce is BUG-0165 alone (README.md + tests/offline/shell/shell.test.ts, nothing else), and npm test now exits 0 in the primary checkout with .env.example in its uncommitted state - 72 files / 3386 offline + 1 / 6 isolated - which is the state that reddened it. No further change was needed on this ticket, hence already_done; its own work landed at 0f06d3b. [resolution: already_done] [receipt green: 2 cmd(s), tree 8609db0c]
