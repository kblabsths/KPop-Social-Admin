---
id: FEAT-0007
type: feat
status: open
milestone: M1
priority: P1
opened_by: visionary
parent: null
children: []
assignee: null
claimed_at: null
depends_on: [FEAT-0001, FEAT-0002]
touch_scope: []
force: false
---
# Browse — the recent-events curated view

## Description
VISION: "Six pages — ... Browse (v1 view: recent events)". SPEC F7. Contracts read 2026-09-01: admin-observability.md §4 (Browse — curated data views, each defined in code with its query, the columns it may show and its default sort, plus a runtime column selector over the configured set); data-model.md (per-field provenance). v1 ships exactly one view: recent events — everything that came through the pipeline, newest first, with the spot-verification columns (title, description, poster image, date, venue) and the sources behind the row read from the field_provenance join. An event row links to the record edit surface (read-only for events in M1). No whole-table browsing, no free-SQL runner, no second curated view — the recurring query belongs in code where it is reviewed once, and one-off queries keep their existing read-only paths.

## Acceptance criteria
M1 EC9, EC3. Acceptance test 10: rows are newest first (the rendered order matches the SQL order by), the spot-verification columns render including the sources behind the row from the provenance join, and the column selector offers exactly the configured set — every configured column present, nothing outside it. Acceptance test 2: rendered rows match direct SQL on staging. An event row reaches its record page. No table browser and no SQL-executing route exists in the repo. Acceptance test 9: with field_provenance absent the page returns 200 with its not-provisioned marker. `npm run lint` and `npm run build` exit 0.

## Checks
```sh
npm run lint
npm run build
```

## History
- 2026-09-02T05:17:31Z [visionary] opened: Browse — the recent-events curated view
