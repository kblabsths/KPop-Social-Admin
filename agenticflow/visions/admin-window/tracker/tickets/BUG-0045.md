---
id: BUG-0045
type: bug
status: qa
milestone: M1
priority: P2
opened_by: designer
parent: null
discovered_from: M1-endgame-designer-walk
children: []
assignee: builder-82
claimed_at: 2026-09-04T00:07:15Z
depends_on: []
touch_scope: [src/app/sources/page.tsx, src/app/claims/page.tsx, src/app/cycles/page.tsx, tests/offline/sources/page.test.ts, tests/offline/claims/page.test.ts, tests/offline/cycles/page.test.ts]
force: false
siblings: [BUG-0041, BUG-0043, BUG-0046, BUG-0049]
attempts: 1
---
# Sources and Claims render an internal factory ticket id in product copy, and 'stuck_patterndial' has no space

## Description
Found on the M1 endgame designer walk (2026-09-03), walk instance :8771 against staging, 1440x900. Two defects in one sentence, plus its twin on a second page.

**On `/sources`, below the awaiting-row trend, the app renders:**

    No threshold line is drawn. The per-source stuck_patterndial lives only in the
    scraper repo's source registry, and where Admin may read it is an open question
    (admin-window/TASK-0024) — so no default is substituted here.

**On `/claims` (and the standing-disagreements tab), the app renders:**

    ...stuck-pattern dial is a source-registry value only the scraper repo holds, and
    where Admin may read it is an open question (admin-window/TASK-0024). No default
    is substituted here.

1. **`admin-window/TASK-0024` is a build-tracker ticket id, and it is on screen in the product**, on two of the six pages. The Voice register is "the pipeline's engineer reading you its own logbook" — the pipeline's logbook, not the factory's. The operator has no `/agenticflow` to open, the id will outlive the ticket, and it tells him nothing he can act on. The rest of that sentence already says the true and useful thing ("the dial lives only in the scraper repo's source registry, and no default is substituted here"); the id adds only provenance for a decision made off-screen. Confirmed rendered, not merely in a comment: 2 occurrences in the delivered HTML of `/sources`, 2 of `/claims`, 2 of `/claims?tab=standing`.

2. **`stuck_patterndial`** — on `/sources` the mono `<span>stuck_pattern</span>` is followed immediately by `dial lives only...` with no separating space, so the screen reads `stuck_patterndial` as one word. `/claims` writes the same concept as `stuck-pattern dial`, correctly spaced and not in mono — so the two pages also spell the same dial two ways.

Evidence: `agenticflow/tracker/evidence/designer-walk-m1/sources-light.png` (the sentence, mid-page), `claims-light.png`, `claims-standing-light.png`.

**What this adds that its siblings do not.** BUG-0041 changes how many claim ROWS render; BUG-0043 changes what a SOURCE is called. This one changes two prose sentences and nothing structural — no row count, no label, no link. All three touch `src/app/claims/page.tsx` in different hunks.

## Acceptance criteria
No page of the running app renders a factory ticket id. A scan of the delivered HTML of all six pages plus both claims tabs for `admin-window/` and for `TASK-` finds zero occurrences. What replaces it says the same operational fact in the app's voice, without pointing at a tracker.

`stuck_pattern` renders as its own word with a space before `dial`, and the two pages spell the dial the same way as each other — Voice bar 5 (machine identifiers verbatim in mono) decides which spelling.

**Third site, added 2026-09-03 from the user-sim walk.** On `/cycles`, below the runs table, the delivered HTML reads `<span>source</span>is the run’s own text` — the screen says **`sourceis`**. The source line already contains the space (`src/app/cycles/page.tsx:1003`: `</span> is the run&rsquo;s`), so the eye test on the file says there is no bug; JSX drops that leading space at build time and only the RENDERED string shows it. Fix it the way the same file already fixes its neighbour two lines up — an explicit `{" "}` — and prove it against rendered markup, never against the source line.

The delivered HTML of `/cycles`, `/sources` and `/claims` contains no run-together word: no `sourceis`, no `stuck_patterndial`, and no other `</span>` immediately followed by a letter where a space was intended.

Comments in `src/` may keep citing the ticket; this is about rendered strings only.

`tests/offline/sources/page.test.ts`, `tests/offline/claims/page.test.ts` and `tests/offline/cycles/page.test.ts` each assert the rendered markup contains no `admin-window/`, no `stuck_patterndial` and no `sourceis`.

Human-check: read the sentence under the awaiting-row trend, and the sentence under the runs table, aloud — each is a sentence an engineer would say, with no ticket number and no run-together words. (human-checkable)

## Checks
```sh
npx vitest run --project=offline tests/offline/sources/page.test.ts tests/offline/claims/page.test.ts tests/offline/cycles/page.test.ts
./node_modules/.bin/tsc --noEmit
npm run lint
```
## History
- 2026-09-03T22:03:57Z [designer] opened: Sources and Claims render an internal factory ticket id in product copy, and 'stuck_patterndial' has no space
- 2026-09-03T22:27:45Z [designer] touch_scope: [src/app/sources/page.tsx, src/app/claims/page.tsx, tests/offline/sources/page.test.ts, tests/offline/claims/page.test.ts] → [src/app/sources/page.tsx, src/app/claims/page.tsx, src/app/cycles/page.tsx, tests/offline/sources/page.test.ts, tests/offline/claims/page.test.ts, tests/offline/cycles/page.test.ts]: Third site of the same missing-space defect, found by a document-blind user-sim (Tomas, 2026-09-03) on Cycles & runs. Adding src/app/cycles/page.tsx and its test rather than opening a duplicate: one defect class, one fix, one pass.
- 2026-09-03T22:28:00Z [designer] criteria amended: Criteria extended for the third site (Cycles & runs, 'sourceis'), found by a document-blind user-sim (Tomas, 2026-09-03, evidence/user-sim-m1/tomas.md, 'Two small things that cost the tool nothing to fix'). Measured in the delivered HTML of :8771 the same day: '...a failure is, and sourceis the run’s own text'. The trap for the builder is recorded in the criteria — the SOURCE line has the space, JSX eats it, so this can only be verified against rendered markup. [was: No page of the running app renders a factory ticket id. A scan of the delivered HTML of all six pages plus both claims t… (891 chars; full text in git history)]
- 2026-09-03T22:28:05Z [designer] checks amended: Third page's targeted test added to the checks alongside the two already there (Cycles & runs, the 'sourceis' site). [was: npx vitest run --project=offline tests/offline/sources/page.test.ts tests/offline/claims/page.test.ts, ./node_modules/.bin/tsc --noEmit, npm run lint]
- 2026-09-04T00:07:15Z [builder-82] claimed (attempt 1)
- 2026-09-04T00:27:29Z [builder-82] claimed → built: Dropped '(admin-window/TASK-0024)' from the two prose sentences on /sources and /claims (the rest of each sentence already carries the fact); Claims now spells the dial <span>stuck_pattern</span> in mono like Sources does, per Voice bar 5. Every inter-element space in the three pages plus src/components/ui/not-provisioned.tsx is now an explicit {" "}.

TRAP, MEASURED — the offline suite is structurally BLIND to the missing-space half, so do not trust a green npm test on it. vitest's JSX transform KEEPS the space that 'next build''s transform DROPS. Measured on delivered HTML of a production build served on :8781 against staging, 2026-09-03: pre-fix /sources gave 'stuck_pattern</span>dial', pre-fix /cycles gave 'source</span>is', while renderToStaticMarkup of the very same files kept both spaces. The rule: the space is dropped only when that JSXText node is its parent's LAST child (same paragraph, '</span> says whose' survived, '</span> is the run's' did not). That is why tests/offline/source-tree.ts gained implicitInterElementSpaces() and each of the three page tests asserts its own page SOURCE is clean, beside the rendered-markup assertions.

Verified. Post-fix production build on :8781 (staging rows, minted cookie), 10 URLs — /, /sources, /claims, /claims?tab=standing, /cycles, /cycles?cycle=no-such-cycle-id, /browse, /queues, /records/events/<id>, /queues/<id>: 0 matches for 'admin-window/<ID>' or '(TASK|BUG|DEBT|DEP)-NNNN', 0 matches for '</(span|a|b|em|strong|code)>[A-Za-z0-9]'. Pre-fix the same scan gave 2 ids on /sources, 2 on /claims, '</span>dial' and '</span>is'. All 5 new guards observed RED on the pre-fix tree, green after. On the rebased tree (onto 193907e): the 3 targeted offline files 181 passed; npm test 1726 + 6 passed; tsc --noEmit clean; npm run lint exit 0; npm run test:live sources+claims+cycles 21 passed (all three are read-only — no insert/update/upsert — so no residue).

SCOPE, declared. Beyond touch_scope I touched three files: src/components/ui/not-provisioned.tsx (one site, same latent defect, and it renders on every page's not-provisioned state), tests/offline/ui/markup.ts (two new exported scanners, additive), tests/offline/source-tree.ts (the source rule, in the module documented as the one home for structural rules over src/) — one definition each rather than three copies. Not guarded anywhere: not-provisioned.tsx and any FUTURE page; a repo-wide assertion over sourceFiles() would close the class, but that is a new shared policy and I left it as a call for the designer rather than reddening other lanes mid-run. src/ has zero implicit inter-element spaces left today.
- 2026-09-04T00:27:53Z [dispatcher] landed ticket/BUG-0045 onto run/admin-window as 2712a9f
- 2026-09-04T00:30:09Z [qa] built → qa: under attack
