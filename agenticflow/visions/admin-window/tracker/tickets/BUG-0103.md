---
id: BUG-0103
type: bug
status: built
milestone: M2
priority: P3
opened_by: qa
parent: null
discovered_from: BUG-0098
children: []
assignee: builder-172
claimed_at: 2026-09-09T07:33:35Z
depends_on: []
touch_scope: [src/components/edit-refusal.ts, tests/offline/ui/editable-cell.test.ts]
force: false
attempts: 2
---
# The operator's own typed value steers the refusal's app-voice half, so the fix sentence can contradict what happened

## Description
Measured live 2026-09-09 on a production build of the landed tree (run/admin-window 70b37c9), 127.0.0.1:8846 against staging ubfjjqlvnpnoborczbdb, bundled Chromium, 1440x900, both themes, walk_sandbox row 00000000-0000-4000-8000-000000000001, reset-sandbox before AND after.

A coercion refusal QUOTES the operator's typed value back into its own text, and refusalFix (src/components/edit-refusal.ts:156-191) matches its arms against the WHOLE refusal string — earlier arms first (NOT_PROVISIONED :161, then 23502 :165, then 22P02 :171). So a value that happens to contain an earlier arm's prose selects that arm. The mono half stays truthful throughout; the app's voice does not.

REPRO (exact, from the cell, no module stubbing):
1. node tests/walk/reset-sandbox.mts, open /records/walk_sandbox/00000000-0000-4000-8000-000000000001
2. Click the 'tally' cell (integer NOT NULL), select-all, type: is not present in this database  — press Enter.
   Expected (criterion 2 of BUG-0098, 'one place decides it' from the refusal's own words): the coercion arm, since the database refused a TYPE — a sentence about the form tally stores.
   Found: mono half 'invalid input syntax for type integer: "is not present in this database" (22P02)' (Geist Mono 11px, rgb(193,0,7) light / rgb(255,100,103) dark) beside the app-voice half 'This edit needs a database object that has not arrived yet - nothing you retype will land until it does.' (Geist 12px). The app tells the operator that NO retype will land, when a correct retype lands immediately — the one sentence in the app's own voice is false and tells them to stop.
3. Same cell, type: violates not-null constraint — press Enter.
   Expected: the coercion arm.
   Found: mono 'invalid input syntax for type integer: "violates not-null constraint" (22P02)' + app-voice 'This column cannot be cleared - type a value into it.' — nothing was cleared; the operator typed a value.
   ('does not match' and '(23502)' steer it the same way — see the pin.)

Identical in light and dark; geometry unaffected (alert 320px wide at y271, inside the fields container y143-363).

Why it is P3 and not higher: today the only foreign text a refusal can carry is text the operator typed themselves at that same cell one second earlier, so the operator is unlikely to be misled by their own string. It stops being contrived the day a refusal quotes a value the operator did not type at that cell — a gate/registry refusal echoing a rejected value (FEAT-0011), or a settlement refusal quoting a payload field.

PINNED RED: tests/offline/ui/editable-cell.test.ts, 'PROBE: picks the arm from the database's words, not from what the operator typed' — currently it.fails(strict) so the suite stays green; the fix REMOVES .fails and the test must pass as a normal it(). It asserts relationally, never against copy: two refusals of the same class differing only in the quoted value must derive the same fix.

## Acceptance criteria
1. The arm is chosen from what the DATABASE said, not from the value quoted inside it: for a refusal of the shape 'invalid input syntax for type <t>: "<value>" (22P02)', the derived sentence is the same whatever <value> contains — including 'violates not-null constraint', 'is not present in this database', 'does not match' and '(23502)'.
2. Every arm BUG-0098 shipped still fires on the refusals it was built for: the real 23502 (label cleared), the real 22P02 (tally 'seven'), 22007 (observed_on 'nope'), the KS003 registry shape, the not-provisioned sentence, and the general fallback. No arm is lost to the narrowing.
3. tests/offline/ui/editable-cell.test.ts's PROBE test passes as a plain it() with .fails removed, and the rest of that file's refusal block is unchanged and green.
4. The mono half stays byte-identical to the database's message, both halves stay inside one role='alert', and the box's geometry is untouched.

## Checks
```sh
./node_modules/.bin/vitest run --project=offline tests/offline/ui/editable-cell.test.ts tests/offline/records/page.test.ts
bash -c 'rm -rf .next/types && npm run lint && ./node_modules/.bin/tsc --noEmit'
```

## History
- 2026-09-09T06:31:41Z [qa] opened: The operator's own typed value steers the refusal's app-voice half, so the fix sentence can contradict what happened
- 2026-09-09T06:56:42Z [builder-172] claimed (attempt 1)
- 2026-09-09T07:09:47Z [builder-172] claimed → built: refusalFix now picks its arm from the refusal's STRUCTURE (src/components/edit-refusal.ts): the SQLSTATE stated at the END of the string decides whenever it is one an arm claims (23502 / 22P02+22007 / KS003), otherwise the arms match prose with the value struck out (a coercion's value is everything after 'for type <t>:', other shapes quote theirs). Sentences, arms, mono half and geometry unchanged; column/field NAMES still read from the original refusal, so the live 'fix names the cleared column' assertion holds.
- Red-then-green observed: with HEAD's edit-refusal.ts restored, the un-pinned PROBE and the new both-directions test fail ('This column cannot be cleared…' / 'This edit needs a database object…' where the coercion / not-null sentence is due); with the fix, green.
- Tests (tests/offline/ui/editable-cell.test.ts): QA's pin is now a plain it(); added 'takes the arm from the code the refusal STATES, both directions' — a 22P02 quoting '(23502)' derives the same fix as the benign 22P02, and a real 23502 whose DETAIL carries 'is not present in this database' still derives the not-null fix and still names 'label'. Both relational, no copy asserted.
- Verified on the rebased tree (onto run/admin-window): stored checks green (222), npm run lint clean, tsc --noEmit clean, npm test 2698 offline + 6 isolated, npm run test:live -- tests/live/edit.live.test.ts 25 passed with reset-sandbox before AND after. Also measured against real staging (walk_sandbox …0001, tally): typing 'seven', 'is not present in this database', 'violates not-null constraint' and '(23502)' each returns 'invalid input syntax for type integer: "<typed>" (22P02)' and now derives the identical sentence 'Type a whole number, like 7.'; tally read back 7, nothing written.
- Trap for QA: the decision is code-FIRST, so a refusal stating a claimed code can no longer reach any other arm — a codeless refusal still falls back to prose, which is how the app's own '<obj> is not present in this database' (no SQLSTATE) and the bare 'violates not-null constraint' fixture keep their arms.
- 2026-09-09T07:10:12Z [dispatcher] landed ticket/BUG-0103 onto run/admin-window as 1381a7c
- 2026-09-09T07:10:39Z [qa] built → qa: under attack
- 2026-09-09T07:20:19Z [qa] qa → reopened: REOPENED — the defect this ticket names still reproduces live, through the cell, on the landed tree.

Stored checks re-run on the landed tree first (detached worktree at 06137ea, before reading the builder's story): vitest --project=offline editable-cell + records/page = 222 passed; lint clean; tsc --noEmit clean. Criteria 1-4 are GREEN as written — the coercion shape is genuinely fixed (COERCION_HEAD strikes the whole quoted tail) and every BUG-0098 arm still fires. The GOAL is not: the title's defect has a second, live vector the code-first guard fails open on.

Expected (this ticket's own title and criterion-1 intent): the arm is chosen from what the DATABASE said, never from a value the refusal carries.
Found: 'This edit needs a database object that has not arrived yet - nothing you retype will land until it does.' beside a real 23502 about 'label', where 'label cannot be cleared - type a value into it.' is due. src/components/edit-refusal.ts:198 (the NOT_PROVISIONED arm) reached via states() falling through to prose at :137, because shape.code is null.

WHY THE GUARD FAILS OPEN: src/lib/db/result.ts:282 appends the SQLSTATE 'only when the account does not already spell it' (its own docstring, :265). A refusal whose own text contains '23502' therefore arrives WITHOUT the trailing '(23502)', STATED_CODE matches nothing, and states() falls back to prose. A not-null refusal's prose is the whole failing-row dump, and Postgres writes those values UNQUOTED - so QUOTED_RUN (which only blanks double-quoted runs) cannot strike them out, and a value in ANY OTHER column of the row chooses the app's sentence again. That is exactly the escalation the ticket's own P3 rationale named: 'the day a refusal quotes a value the operator did not type at that cell'.

LIVE REPRO (exact; production build of 06137ea on 127.0.0.1:8840, AUTH_URL set, staging ubfjjqlvnpnoborczbdb, bundled Chromium 1440x900, reset-sandbox before AND after, both resets 'deleted 3, seeded 3, read back and verified'):
1. node tests/walk/reset-sandbox.mts; open /records/walk_sandbox/00000000-0000-4000-8000-000000000001
2. CONTROL - click 'label', select-all, Delete, Enter. role=alert carries, in two spans:
   mono: null value in column "label" of relation "walk_sandbox" violates not-null constraint Failing row contains (00000000-0000-4000-8000-000000000001, null, A note a walker may rewrite., 7, f, 2026-01-15, 2026-09-09 07:14:27.7384+00). (23502)
   app:  label cannot be cleared - type a value into it.   [CORRECT]
3. Click 'note', type: is not present in this database 23502  - Enter. Saves 200.
4. Click 'label', select-all, Delete, Enter. Same alert, two spans:
   mono: ...Failing row contains (..., null, is not present in this database 23502, 7, f, 2026-01-15, ...).   <- note the code is GONE from the end
   app:  This edit needs a database object that has not arrived yet - nothing you retype will land until it does.
   The operator just emptied a NOT NULL column; retyping a label lands immediately. The one sentence in the app's voice is false and tells them to stop - the same contradiction BUG-0103 was filed for, one cell over.
Also confirmed the packet's named probes on the same instance: tally <- 'seven (23502)' -> mono 'invalid input syntax for type integer: "seven (23502)" (22P02)' + 'Type a whole number, like 7.' (the appended 22P02 wins - that half of the fix holds). Offline probes: a value containing a newline, an unbalanced quote, the coercion head itself, and a coercion whose value spells 22P02 all derive the coercion arm correctly; '' -> GENERAL_FIX; '(23502)' -> the not-null arm. The ONLY steerable arm is NOT_PROVISIONED, which is first, and its sentence is the worst one to say wrongly.

REACH BEYOND THE SANDBOX: the same shape on a catalog table dumps every column of the failing row, i.e. scraper-written text nobody at that cell typed. It needs the row to spell a claimed SQLSTATE, which is why this stays P3 rather than climbing.

PINNED RED, then flipped: tests/offline/ui/editable-cell.test.ts, 'picks the arm from the database's words when the refusal states no code (admin-window/BUG-0103)'. Watched fail as a plain it() on the landed tree ('expected This edit needs a database object... to deeply equal label cannot be cleared...'), then committed as it.fails (a4e0b35) so the branch stays green. Relational, no copy asserted: three 23502s about 'label' differing only in another column's dumped value, all three codeless, must derive the same fix. THE FIX REMOVES .fails and it must pass as a plain it().

After the pin: lint clean, tsc --noEmit clean, full offline 2699 passed (2698 + the pin). Sandbox reset after; server killed (my PID only); worktree removed.

Suggested direction, not a prescription: strike the value out of the not-null prose the way COERCION_HEAD strikes the coercion's - everything from 'Failing row contains' on is dumped VALUES, none of it prose any arm should read.
- 2026-09-09T07:33:35Z [builder-172] claimed (attempt 2)
- 2026-09-09T07:42:10Z [builder-172] claimed → built: Second cut, QA's vector closed. refusalFix now decides in this order (src/components/edit-refusal.ts): (1) the shape the MESSAGE OPENS with — NOT_NULL_HEAD / COERCION_HEAD anchored at position 0, where errorMessage always puts the message, so no dumped or quoted value can reach it, and each head carries the column/type its sentence needs; (2) the stated SQLSTATE; (3) prose with every value struck — the dumped row (from 'Failing row contains' | 'Key (' | 'DETAIL:' on) as well as quoted runs. NOT_PROVISIONED moves LAST and now matches only a message that IS this app's own sentence ('<obj> is not present in this database', whole-message anchor) — it is the one arm that tells the operator to stop, and both live misfires were it; a wrapped variant degrades to GENERAL_FIX, never to a false sentence. Names (column, field) are read from the account with the dump already struck. result.ts untouched.
- Red-then-green: QA's pin flipped to it() fails on the landed source ('This edit needs a database object…' vs 'label cannot be cleared…'), passes after. Added 'reads the arm off the message's own opening, whatever the dump holds' — varies what the dump SPELLS (another arm's code, a whole coercion message, an unbalanced quote, a newline) and the coercion's own value likewise; relational, plus a not-vacuous clause that the two arms stay distinguishable.
- LIVE REPRO RE-MEASURED through the cell, production build of this tree on 127.0.0.1:8853 (bind-probed; my PID only, killed after), staging ubfjjqlvnpnoborczbdb, bundled Chromium 1440x900, reset before AND after. note <- 'is not present in this database 23502' (saved), then label cleared: one role=alert, two spans — mono 'null value in column "label" of relation "walk_sandbox" violates not-null constraint Failing row contains (00000000-0000-4000-8000-000000000001, null, is not present in this database 23502, 7, f, 2026-01-15, 2026-09-09 07:39:23.946327+00).' (no trailing code, byte-identical) beside 'label cannot be cleared — type a value into it.'; label still reads 'First sandbox row'. First cut re-checked on the same instance: tally <- 'is not present in this database' -> 'invalid input syntax for type integer: "is not present in this database" (22P02)' + 'Type a whole number, like 7.'; tally still 7.
- Green on the rebased tree: stored checks 227, lint clean, tsc --noEmit clean, npm test 2703 offline + 6 isolated, test:live edit.live.test.ts 25 passed.
- For QA: the remaining prose path is reachable only by a message opening with neither Postgres head AND stating no claimed code; the arms it can reach are not-null / coercion / registry / general — not the stop sentence.
- 2026-09-09T07:42:33Z [dispatcher] landed ticket/BUG-0103 onto run/admin-window as 18395db
