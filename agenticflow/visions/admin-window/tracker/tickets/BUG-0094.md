---
id: BUG-0094
type: bug
status: claimed
milestone: M2
priority: P2
opened_by: qa
parent: FEAT-0011
discovered_from: BUG-0086
children: []
assignee: builder-166
claimed_at: 2026-09-09T05:36:47Z
depends_on: []
touch_scope: [tests/offline/shell/client-boundary.test.ts, tests/http/records.http.test.ts]
force: false
attempts: 2
claim_tree_hash: 6384362e9ea5d13866d78334d81b17164c5b2039bf08b84d8b1aa59d9b2dfa7b
---
# A server component importing a value from a "use client" module 500s the record page, and every automated tier stays green

## Description
Measured by QA on the BUG-0086 attack (2026-09-08), landed tree run/admin-window @ d2e4623, production build on 127.0.0.1:8805, staging.

**Expected** — a defect that makes the record page answer 500 is caught by a check the factory runs, not by a human loading the page. This class already happened once in this campaign: BUG-0086's first cut had `RecordFields` (a synchronous SERVER component) calling `hintSide` out of `EditableCell.tsx` (`"use client"`), the record page answered 500 ("Attempted to call hintSide() from the server"), and the builder found it only because it walked. `src/components/edit-cell-layout.ts` exists solely to dodge that boundary — the campaign now depends on a rule no test states.

**Found** — nothing catches it. QA restored the exact violation in a private worktree (a `hintSideFromClientModule` export added to `src/components/EditableCell.tsx`, imported by `src/components/records/record-fields.tsx`) and ran the tiers on that build:

- `vitest run --project=offline tests/offline/records/page.test.ts` — 93 passed. The offline tier is plain node with `renderToStaticMarkup` (STACK.md §4): there is no client boundary in it to violate.
- `npm run test:http` — 16 passed, 3 files. The http tier builds and starts the real app, and it DOES request `/records/...` — but with no database, so the page renders its named refusal and `RecordFields` never executes. The tier that could see this never reaches the code.
- `npm run lint` + `tsc --noEmit` — clean. TypeScript sees an ordinary function export.
- The same build, served with the staging env and driven with a minted cookie: `GET /records/walk_sandbox/00000000-0000-4000-8000-000000000001` answers **500** (server log: `digest: 3603077637`, thrown out of the ssr chunk). Restored, rebuilt, same URL answers **200**.

So a one-line import that ships a 500 on the app's primary edit surface passes lint, tsc, the offline suite, the http suite and CI. Only a walk sees it, and walks are not run per ticket.

**Where it bites**: every server component under `src/app/**` and `src/components/**` that imports from a `"use client"` module. `record-fields.tsx` is one import line away from it today, and the reason it is safe is a comment.

## Acceptance criteria
1. A check in an automated tier FAILS when a synchronous server component imports a value export from a `"use client"` module, and passes on the current tree. Graded by re-running the sabotage in the description (add an export to `src/components/EditableCell.tsx`, import it in `src/components/records/record-fields.tsx`): the check must go red, then green again when reverted.
2. The check names the offending file and the client module in its failure message — a `500 digest` in a server log is what this ticket exists to replace.
3. It covers `src/**` as a rule, not the two files of the repro: a new server component importing from any `"use client"` module is caught the same way.
4. `npm run lint`, `tsc --noEmit` and the full offline suite stay green.

## Checks
```sh
./node_modules/.bin/vitest run --project=offline tests/offline/shell/client-boundary.test.ts
bash -c 'rm -rf .next/types && npm run lint && ./node_modules/.bin/tsc --noEmit'
```

## History
- 2026-09-09T04:43:50Z [qa] opened: A server component importing a value from a "use client" module 500s the record page, and every automated tier stays green
- 2026-09-09T04:51:52Z [builder-157] claimed (attempt 1)
- 2026-09-09T05:09:31Z [builder-157] claimed → built: Two guards, no src change (branch ticket/BUG-0094 @ 45b7c48, rebased onto run/admin-window @ 22f099e after BUG-0086 landed).

- tests/offline/shell/client-boundary.test.ts: walks src/** via the shared source-tree walker, classifies each module by a LEADING "use client" directive (not a grep — 4 files merely mention it), and reddens when a server module imports a non-component export from a client module. Names file, client module and symbol. Covers named/renamed/default/namespace imports, re-exports (a barrel crosses on its importers' behalf), `.js` specifiers and the `@/` alias; type imports are exempt (submit.ts takes SaveOutcome that way today); a PascalCase rename does NOT launder a helper — the export's own declaration is read. Fixtures are TEXT modules through an injected reader, so nothing is written under src/ (BUG-0029's hazard).
- tests/http/records.http.test.ts: drives /records/<table>/<uuid> for every EDIT_CONFIG table on the built server with a WELL-FORMED uuid — auth.http.test.ts uses `2f0bc11e`, which stops at isRecordId before any read, so this is the first http coverage that runs the four read legs. Asserts 200, our frame, no Next error shell, no digest in the server log.

Proof (all on this tree, 2026-09-08): QA's exact violation restored (hintSideFromClientModule on EditableCell.tsx, imported by record-fields.tsx) -> offline guard RED naming both files and the symbol; reverted -> GREEN. The same crossing moved into RecordFrame (always rendered, no DB needed) -> /records/events/<uuid> answered 500 and the http test went RED; that is its non-vacuity input.

TRAP QA SHOULD KNOW: the http test does NOT catch QA's original placement. Measured — with the DB sentinels readRecord is refused, so RecordFields never executes and every assertion still passed on the sabotaged build. The offline guard is the net for the class; the http file is the net for whatever DOES fail without a database. Both facts are in the http file's header. Also measured: the build's page_client-reference-manifest.js cannot distinguish them (Turbopack records "name":"*" per client module, and EditableCell.tsx is listed in a CLEAN build via field-editor.tsx) — there is no build-artifact signal, do not re-dig.

Verified: vitest offline tests/offline/shell/client-boundary.test.ts 13 passed; npm run test:http 17 passed (4 files); npm run lint exit 0; tsc --noEmit clean; full offline from a detached _receipt- worktree 68 files / 2538 passed + isolated 6 passed. No staging write, no .env opened, 8771 untouched.

NOT DONE, outside touch_scope: tests/isolated/probe-race.isolated.test.ts keeps a hand-kept WALKERS list of tree-walking offline files; mine is a 5th and is not in it (safe by construction — sourceFiles() skips the dot-directory probe and sourceText tolerates ENOENT; isolated suite green). Adding it would also want a line in LOUD_PROBE that trips this rule.
- 2026-09-09T05:09:44Z [dispatcher] landed ticket/BUG-0094 onto run/admin-window as f89711a
- 2026-09-09T05:10:23Z [qa] built → qa: under attack (batch qa-0086-0094)
- 2026-09-09T05:29:44Z [qa] qa → reopened: REOPENED (attempt 1): criteria 1 and 2 hold for the repro's exact spelling; criterion 3 ("covers src/** as a RULE, not the two files of the repro") is false one rename further on, and the file's own claim "a PascalCase rename does NOT launder a helper" is false. Landed tree run/admin-window d3d217e, attacked in a detached _receipt- worktree; production build on 127.0.0.1:8823 against staging.

WHAT HOLDS, driven before reading the handoff:
- Stored checks: vitest offline tests/offline/shell/client-boundary.test.ts 13 passed; rm -rf .next/types && npm run lint && tsc --noEmit exit 0.
- C1/C2 re-driven: the ticket's own sabotage (an export added to src/components/EditableCell.tsx, imported by src/components/records/record-fields.tsx) — guard RED naming both files and the symbol; reverted, green.
- The http file reads honestly and its stated limit is the right one: the source guard is the net for the class, the http file is the net for a crossing a DB-less render reaches. Judged acceptable AS THE SECOND NET; the first net is where the hole is.

WHY IT COMES BACK. `declaresComponent` accepts any right-hand side that opens with `(`, so an ordinary helper written as an arrow function and given a component's name is read as a component and waved through. Expected: flagged, like `export const HintSide = "left"` in the test above it. Found: 0 violations. tests/offline/shell/client-boundary.test.ts, `declaresComponent` -> `asBinding` branch (the `/^(?:\(|<|function\b|async\b|…)/` test).

Repro, run end to end on this tree:
1. Append to src/components/EditableCell.tsx:
   export const HintSideFor = (row: number, rows: number): HintSide =>
     rows > 1 && row === rows - 1 ? "above" : "below";
2. In src/components/records/record-fields.tsx (a synchronous SERVER component) import { HintSideFor } from "@/components/EditableCell" and call it where hintSide is called.
3. Measured on that build: client-boundary.test.ts 13 passed; FULL offline suite 2589 passed; npm run lint exit 0; tsc --noEmit clean.
4. Same build served with the staging env and a minted cookie: GET /records/walk_sandbox/00000000-0000-4000-8000-000000000001 -> **500**, server log `Attempted to call HintSideFor() from the server but HintSideFor is on the client`, digest 2955157816. Reverted the two files, rebuilt, same URL -> 200, zero digests. That is BUG-0094's own defect, unchanged, on a spelling the guard cannot see.
Also waved through, same branch: `export const FormatDate = (d: string) => d.slice(0, 10)`. Correctly flagged and NOT part of this: object/array constants with component names (`StatusWords`, `Limits`), the string-const rename, and the `export { hintSide as HintSide }` hop.

PINNED, red before it was pinned: tests/offline/shell/client-boundary.test.ts, "a capitalised ARROW helper does not launder past the guard either" — watched fail ("expected [] to have a length of 1"), then committed as `it.fails` (run branch d554b7b) so the suite stays green and goes RED the day the guard closes the hole. Delete the `.fails`, do not re-file.

Two residuals, NOT this ticket's bar and not filed: a server component doing `await import("@/components/EditableCell")` is not seen (no statement to match), and a specifier whose case differs from the file's (`./Cell` for `cell.tsx`) resolves to null on this case-insensitive filesystem, so it is skipped rather than flagged. Both are narrower than the shape above; mention them only if the fix is cheap in the same pass.

Full offline suite 2590 + isolated 6, lint 0, tsc 0 on the reverted tree with the pin.
- 2026-09-09T05:36:47Z [builder-166] claimed (attempt 2)
