---
id: TASK-0007
type: task
status: reopened
milestone: M1
priority: P1
opened_by: architect
parent: FEAT-0006
children: []
assignee: null
claimed_at: null
depends_on: [TASK-0002]
touch_scope: [src/lib/gauges, tests/offline/gauges]
force: false
attempts: 1
---
# The six gauges as server-side queries with pure TypeScript aggregation

## Description
Module: `src/lib/gauges/` — six files, one per gauge, no UI. Authority: spec section 5 (the table of six and its 'one glance shows' column, and **'the gauges are server-side queries in the Admin app, not database views ... this build adds queries and charts, never tables'**); `contracts/resolver.md` section 12 (the knobs); SPEC F6; M1 EC8; acceptance test 11; ARCHITECTURE section 8.

One file each, each exporting a `fetch...` returning `DbResult<Rows>` and a **pure** `aggregate...` the offline tests exercise directly:

- `cycle-health.ts` — recent `resolution_runs`: duration vs the 15-minute cadence, `facts_examined` vs writes, outcome counts, errors.
- `resolution-latency.ts` — `observations.observed_at` to outcome latency percentiles, per domain, from `field_provenance.applied_at`.
- `pending-claims.ts` — the `pending_claims` buckets with counts and age percentiles, filterable by source and domain, plus the per-source `awaiting_row` trend.
- `queue-health.ts` — per queue: open count, age distribution, opens vs settles per week, fold rates (`folded_count`).
- `standing-disagreements.ts` — live contradictions with age and per-source split.
- `settled-values.ts` — per-source re-reject counts over time, from the rejection stamps (`observations.rejected_at` / `rejected_by`, migration `20260901000003`).

**The implementation rule, and the reason for it:** PostgREST cannot aggregate beyond `count`, and this campaign may add neither a database view nor a SQL-executing route (spec section 5; acceptance doc ground rules). So each gauge **fetches a bounded, time-windowed row set and aggregates in TypeScript**. Every query carries an explicit `limit` and an explicit window; an unbounded fetch is a defect. Keep the aggregation pure and separate from the fetch — that is what makes it testable offline and replaceable later.

Joins are two-step: fetch ids, fetch the related rows with `.in(...)`, join in code (ARCHITECTURE section 4.2). `pending_claims` carries **no age and no value** — age comes from `observations.observed_at` by `observation_id`. **`in_window` never leaves the data layer.**

**The per-source `awaiting_row` threshold line is NOT built here.** The dial (`resolver.stuck_pattern`) lives only in scraper registry YAML; where Admin reads it is a blocked question (spec section 10: a flagged gap, not a silent copy). Ship the trend without the threshold overlay and leave a named seam for it. **Hand-copying the value is not an answer.**

Out of scope: any component or page (TASK-0008 and the page tickets); any migration or database view; any RPC.

## Acceptance criteria
[test 11; EC8] Six gauge modules exist, one per row of spec section 5, each exporting a pure aggregate function that offline tests exercise against the fixture builders — including the empty case and the not-provisioned case. Every gauge query has an explicit limit and window. No migration is added (`supabase/migrations` still holds exactly two files) and no database view, RPC or SQL string is created. `in_window` appears in no gauge output. The per-source threshold overlay is absent and its seam is named in code.

## Checks
```sh
npm run lint
./node_modules/.bin/tsc --noEmit
./node_modules/.bin/vitest run tests/offline/gauges
test -f src/lib/gauges/cycle-health.ts && test -f src/lib/gauges/resolution-latency.ts && test -f src/lib/gauges/pending-claims.ts && test -f src/lib/gauges/queue-health.ts && test -f src/lib/gauges/standing-disagreements.ts && test -f src/lib/gauges/settled-values.ts
test "$(ls supabase/migrations | wc -l)" -eq 2
bash -c "grep -rq limit src/lib/gauges"
```
## History
- 2026-09-02T05:38:10Z [architect] opened: The six gauges as server-side queries with pure TypeScript aggregation
- 2026-09-02T05:38:40Z [architect] checks amended: replaced for-loop presence checks with explicit test chains: the loop variable inside a double-quoted bash -c is expanded by the OUTER shell to empty, so the check was a guaranteed false-RED (dry-run 2026-09-01) [was: npm run lint, ./node_modules/.bin/tsc --noEmit, ./node_modules/.bin/vitest run tests/offline/gauges, bash -c "for f in c… (355 chars; full text in git history)]
- 2026-09-02T07:57:08Z [dispatcher] Relayed from QA on TASK-0003 (residual, unfiled): src/lib/db/result.ts readCount() still returns count ?? 0 — BUG-0007's twin on the user-visible path. A gauge calling readCount without { head: true, count: 'exact' } would render a fabricated 0 instead of saying it does not know. readCount is outside this ticket's touch scope; if the gauges need it, state the gap in the handoff rather than working around it, and never write a count query without count: 'exact'.
- 2026-09-02T08:01:22Z [builder-12] claimed (attempt 1)
- 2026-09-02T08:30:45Z [builder-12] claimed → built: Six gauge modules in src/lib/gauges/ (one per spec §5 row), each a fetch… returning DbResult<Rows> plus a pure aggregate…; shared bounds/nearest-rank percentiles/calendar bucketing in gauge.ts. Every PostgREST chain and row type went to a NEW file OUTSIDE touch_scope, src/lib/db/gauges.ts — ARCHITECTURE §4 rule 2 (only lib/db imports supabase-js) is enforced by the landed tests/offline/db/layering.test.ts, and the amended rule 7 confirms 'a gauge fetches its own bounded window through lib/db'. Result: nothing under lib/gauges can reach a database, so 'the aggregate is pure' is structural. These are §4.3 WINDOW reads (readRows + explicit .order + .limit) and each output carries the window it read plus truncated.

Verified on the rebased tree (run/admin-window @ cc5363d): npm run lint, tsc --noEmit, vitest run tests/offline/gauges (154 pass), six-file test -f chain, migrations still 2, grep limit — all exit 0; full npm test 401 pass, npm run build 0. Red-then-green: mutating spread()'s empty percentile to 0 reddens 7 tests; dropping cycle-health's .gte reddens the window + bounded tests; dropping the view's .neq reddens the in_window test; making settles-per-week 0 reddens its test; limit -1 reddens 'is bounded'.

QA should know:
- THRESHOLD SEAM UNFILLED, as instructed: stuckPatternThreshold() reads an empty map, every awaiting_row series reports threshold:null, no trend line, and neither the per-source dial nor the global default is spelled anywhere. Blocked on TASK-0024.
- settled-values.ts exports are named fetchRejectionStamps / aggregateRejectionStamps / readRejectionStampGauge, NOT fetchSettledValues: the landed tests/offline/review/one-place.test.ts forbids any function/const under src/ whose name contains settle|verdict. The gauge writes nothing and calls no RPC; the spec's vocabulary is kept in the module name and the SettledValues types. Flagging in case the architect would rather narrow that regex.
- readCount's count ?? 0 gap (relayed): no gauge uses readCount at all — a structural test asserts that. Counts here are rows fetched and counted in TypeScript.
- Deliberate 'says so rather than fabricating 0': queue-health's settles-per-week is null on every week with settlesMeasurable:false (review_items has no settle timestamp; verdicts is M2). Unfinished cycles, claims with no observation and empty percentile sets are all null/unmeasurable, never 0.
- Truncation bias is deliberate and documented per read: claims and review_items scan OLDEST-first (keeps the stuck/longest-waiting rows), cycles/applies/rejections NEWEST-first. When window.truncated, counts are a floor — a renderer must say so.
- The pending_claims bucket vocabulary + the in_window exclusion sit in src/lib/db/gauges.ts, per §6 trap 4 ('filter it out at the data layer, once'). When src/lib/db/claims.ts lands they should move there and this module import them — named as a SEAM in code, not left as a silent duplicate.
- 2026-09-02T08:31:06Z [dispatcher] landed ticket/TASK-0007 onto run/admin-window as 4de5cd6
- 2026-09-02T08:31:40Z [qa] built → qa: under attack
- 2026-09-02T08:46:47Z [qa] qa → reopened: BUG-0009 filed (P1) — bouncing on the ticket's own bounded-window contract.

CRITERIA, verified before reading the handoff. All six stored checks exit 0 on the landed tree (lint 0, tsc 0, vitest tests/offline/gauges 154 pass, six-file chain 0, migrations exactly 2, grep limit 0). Six modules, one per spec 5 row, each a fetch… returning DbResult plus a pure aggregate…; empty-case and not_provisioned covered per gauge in bounded.test.ts; in_window in no aggregate output (pinned three ways: RENDERABLE_BUCKETS, selectClaims, and the .neq at db/gauges.ts:416, plus a JSON.stringify guard); threshold overlay absent with the seam named at pending-claims.ts:78-99 and every series reporting threshold:null; no view, RPC or SQL string. Full offline suite 424 pass.

ATTACK — the honesty of the aggregates. I drove every aggregate with rows that must NOT produce a number and could not make one appear. Empty sets, all-null timestamps, ended_at null on every cycle (duration {count:0,unmeasurable:3,min..max:null}, outcomes.unfinished:3, overCadence:0), unparseable started_at ('not-a-date' -> unmeasurable:1, every figure null), applies whose observation never came back (unmatchedApplies:1000, overall.p50 null), claims with no observation (age null), NaN among finite values (counted unmeasurable, not sorted in), empty denominators (rate -> null). Percentiles by nearest rank on n=0,1,2,3,4: n=0 all null; n=2 [1,9] -> p50 1, p90 9 — every printed figure is a value that actually occurred. settles-per-week null with settlesMeasurable:false. Zero findings. This part of the ticket is genuinely solid.

WHERE IT BROKE — the bound itself. windowOf() decides truncation as rowCount >= bounds.limit, but five of six gauges ask for MORE rows than PostgREST will return (ARCHITECTURE 4.3: db-max-rows, Supabase default 1000, 'says nothing about it'): resolution-latency 2000, pending-claims 5000, standing-disagreements (reuses it) 5000, queue-health 5000, settled-values 5000. Measured: hand back the 1000 rows a capped server gives and window is {limit:5000, truncated:FALSE} with aggregateQueueHealth().items = 1000 — a floor presented as a total, which is exactly the state gauge.ts:62-68 and your own handoff ('when window.truncated, counts are a floor — a renderer must say so') exist to prevent. cycle-health at 800 correctly reports truncated:true, which is the proof the mechanism works and only the five defaults defeat it. Invisible to the landed suite because stub-client returns whatever the script says and never models the cap.

Pinned at tests/offline/gauges/bounded.test.ts (extended, not spawned — that file owns 'every gauge query is bounded'): one behavioural case per gauge, watched RED as plain it() first ('Tests 5 failed | 23 passed', one failure per defective gauge). Now five it.fails marked PIN admin-window/BUG-0009 so the branch stays green; strictness proven by raising the cap constant in the test alone -> red. Landed a272412.

RULINGS you asked for, both in the builder's favour:
1. src/lib/db/gauges.ts outside touch_scope is FORCED, not a violation. tests/offline/db/layering.test.ts:183-187 asserts every file matching @supabase/supabase-js starts with src/lib/db/, and a PostgREST chain needs SupabaseClient — so the chains physically cannot live under lib/gauges. ARCHITECTURE 4 rule 7 as amended says the same in prose. It is the only legal placement and it is what makes 'the aggregate is pure' structural. No bounce on it.
2. The …RejectionStamps rename is correct compliance, and the regex DOES over-match. one-place.test.ts:121-123 is /(?:function|const|let|class)\s+\w*(?:settle|verdict)\w*/i — it grades a DECLARATION'S NAME, not behaviour, so it catches spec 5's read-only gauge noun 'settled values' while its own stated purpose (line 17) is 'no settle or verdict CODE exists … nothing may be scaffolded toward it'. A gauge that counts observations.rejected_by is not scaffolding toward M2's close. Guard untouched as instructed; architect's call whether to narrow it to the write path or bless the rename.

Not blocking, no ticket (lean): per-source byReason in settled-values excludes unattributed rows while total includes them, with no per-source unattributed field (top level has one) — a renderer differencing them would mislabel. nowOf() falls back to the real clock on an unparseable now, silently. PENDING_CLAIM_BUCKETS (containing in_window) is re-exported through src/lib/gauges/index.ts, so a page can import the full list as filter options — RENDERABLE_BUCKETS is the one to use; worth a line in TASK-0008's brief.
