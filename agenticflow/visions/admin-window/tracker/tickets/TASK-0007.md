---
id: TASK-0007
type: task
status: qa
milestone: M1
priority: P1
opened_by: architect
parent: FEAT-0006
children: []
assignee: builder-17
claimed_at: 2026-09-02T09:02:04Z
depends_on: [TASK-0002]
touch_scope: [src/lib/gauges, tests/offline/gauges]
force: false
attempts: 2
compact_threshold: 12000
empty_diffs: 1
---
# The six gauges as server-side queries with pure TypeScript aggregation

## Description
Module: `src/lib/gauges/` — six files, one per gauge, no UI. Authority: spec section 5 (the table of six and its 'one glance shows' column, and **'the gauges are server-side queries in the Admin app, not database views ... this build adds queries and charts, never tables'**); `contracts/resolver.md` section 12 (the knobs); SPEC F6; M1 EC8; acceptance test 11; ARCHITECTURE section 8.

One file each, each exporting a `fetch...` returning `DbResult<Rows>` and a **pure** `aggregate...` the offline tests exercise directly:

- `cycle-health.ts` — recent `resolution_runs`: duration vs the 15-minute cadence, `facts_examined` vs writes, outcome counts, errors.
- `resolution-latency.ts` — `observations.observed_at` to outcome latency percentiles, per domain, from `field_provenance.applied_at`.
- `pending-claims.ts` — the `pending_claims` buckets with counts and age percentiles, filterable by source and domain, plus the per-source `awaiting_row` trend.
- `queue-health.ts` — per queue: open count, age distribution, opens vs settles per week, fold rates (`folded_count`).
- `standing-disagreements.ts` — live contradictions with age and per-source split.
- `settled-values.ts` — per-source re-reject counts over time, from the rejection stamps (`observations.rejected_at` / `rejected_by`, migration `20260901000003`).

**The implementation rule, and the reason for it:** PostgREST cannot aggregate beyond `count`, and this campaign may add neither a database view nor a SQL-executing route (spec section 5; acceptance doc ground rules). So each gauge **fetches a bounded, time-windowed row set and aggregates in TypeScript**. Every query carries an explicit `limit` and an explicit window; an unbounded fetch is a defect. Keep the aggregation pure and separate from the fetch — that is what makes it testable offline and replaceable later.

Joins are two-step: fetch ids, fetch the related rows with `.in(...)`, join in code (ARCHITECTURE section 4.2). `pending_claims` carries **no age and no value** — age comes from `observations.observed_at` by `observation_id`. **`in_window` never leaves the data layer.**

**The per-source `awaiting_row` threshold line is NOT built here.** The dial (`resolver.stuck_pattern`) lives only in scraper registry YAML; where Admin reads it is a blocked question (spec section 10: a flagged gap, not a silent copy). Ship the trend without the threshold overlay and leave a named seam for it. **Hand-copying the value is not an answer.**

Out of scope: any component or page (TASK-0008 and the page tickets); any migration or database view; any RPC.

## Acceptance criteria
[test 11; EC8] Six gauge modules exist, one per row of spec section 5, each exporting a pure aggregate function that offline tests exercise against the fixture builders — including the empty case and the not-provisioned case. Every gauge query has an explicit limit and window. No migration is added (`supabase/migrations` still holds exactly two files) and no database view, RPC or SQL string is created. `in_window` appears in no gauge output. The per-source threshold overlay is absent and its seam is named in code.

## Checks
```sh
npm run lint
./node_modules/.bin/tsc --noEmit
./node_modules/.bin/vitest run tests/offline/gauges
test -f src/lib/gauges/cycle-health.ts && test -f src/lib/gauges/resolution-latency.ts && test -f src/lib/gauges/pending-claims.ts && test -f src/lib/gauges/queue-health.ts && test -f src/lib/gauges/standing-disagreements.ts && test -f src/lib/gauges/settled-values.ts
test "$(ls supabase/migrations | wc -l)" -eq 2
bash -c "grep -rq limit src/lib/gauges"
```
## History
- 2026-09-02T08:49:59Z [compacted → agenticflow/tracker/notes/TASK-0007.md]
  Summary (state; attempts; DEAD ENDS preserved):
  **State** (reopened after attempt 1; needs a builder to clear BUG-0009).
  Six gauge modules built by builder-12 in `src/lib/gauges/` (one per spec §5 row), each a `fetch…` returning `DbResult<Rows>` plus a pure `aggregate…`; shared bounds / nearest-rank percentiles / calendar bucketing in `gauge.ts`; every PostgREST chain and row type in `src/lib/db/gauges.ts`. Landed on run/admin-window as 4de5cd6; QA's pin as a272412.
  All six stored checks exit 0 on the landed tree: `npm run lint` 0, `./node_modules/.bin/tsc --noEmit` 0, `./node_modules/.bin/vitest run tests/offline/gauges` 154 pass, six-file `test -f` chain 0, migrations exactly 2, `grep limit src/lib/gauges` 0. Full offline suite 424 pass. At build time (rebased tree run/admin-window @ cc5363d) `npm test` 401 pass, `npm run build` 0.
  QA VERDICT 2026-09-02T08:46:47Z — criteria met: six modules one per spec §5 row; empty-case and not_provisioned covered per gauge in `bounded.test.ts`; `in_window` in no aggregate output (pinned three ways: RENDERABLE_BUCKETS, selectClaims, the `.neq` at `db/gauges.ts:416`, plus a JSON.stringify guard); threshold overlay absent, seam named at `pending-claims.ts:78-99`, every series `threshold:null`; no view, RPC or SQL string. The attack on aggregate honesty (empty sets, all-null timestamps, unfinished cycles, unparseable `started_at`, 1000 unmatched applies, claims with no observation, NaN among finite values, empty denominators, percentiles at n=0..4) produced ZERO findings — "this part of the ticket is genuinely solid".
  OPEN, BLOCKING — BUG-0009 (P1): `windowOf()` decides truncation as `rowCount >= bounds.limit`, but five of six gauges ask for MORE rows than PostgREST will return (ARCHITECTURE §4.3 db-max-rows, Supabase default 1000, "says nothing about it"): resolution-latency 2000, pending-claims 5000, standing-disagreements (reuses it) 5000, queue-health 5000, settled-values 5000. Measured: hand back the 1000 rows a capped server gives and window is `{limit:5000, truncated:FALSE}` with `aggregateQueueHealth().items = 1000` — a floor presented as a total, the exact state `gauge.ts:62-68` and the builder's handoff ("when window.truncated, counts are a floor — a renderer must say so") exist to prevent. cycle-health at 800 correctly reports `truncated:true`, proof the mechanism works and only the five defaults defeat it. Invisible to the landed suite because stub-client returns whatever the script says and never models the cap.
  FAILING TESTS (re-runnable): pinned at `tests/offline/gauges/bounded.test.ts` (extended, not spawned — that file owns "every gauge query is bounded"), one behavioural case per gauge, watched RED as plain `it()` first ("Tests 5 failed | 23 passed", one failure per defective gauge). Now five `it.fails` marked `PIN admin-window/BUG-0009` so the branch stays green; strictness proven by raising the cap constant in the test alone -> red. Command: `./node_modules/.bin/vitest run tests/offline/gauges`.
  QA RULINGS, both in the builder's favour, no bounce on either:
  1. `src/lib/db/gauges.ts` outside touch_scope is FORCED, not a violation. `tests/offline/db/layering.test.ts:183-187` asserts every file matching `@supabase/supabase-js` starts with `src/lib/db/`, and a PostgREST chain needs SupabaseClient — so the chains physically cannot live under `lib/gauges`. ARCHITECTURE §4 rule 7 as amended says the same in prose. Only legal placement, and what makes "the aggregate is pure" structural.
  2. The `…RejectionStamps` rename is correct compliance, and the regex DOES over-match. `one-place.test.ts:121-123` is `/(?:function|const|let|class)\s+\w*(?:settle|verdict)\w*/i` — it grades a DECLARATION'S NAME, not behaviour, so it catches spec §5's read-only gauge noun "settled values" while its own stated purpose (line 17) is "no settle or verdict CODE exists … nothing may be scaffolded toward it". A gauge counting `observations.rejected_by` is not scaffolding toward M2's close. Guard untouched; ARCHITECT'S CALL whether to narrow it to the write path or bless the rename.
  AMENDED CHECKS (architect, 2026-09-02T05:38:40Z): for-loop presence checks replaced with explicit `test -f` chains — the loop variable inside a double-quoted `bash -c` is expanded by the OUTER shell to empty, so the check was a guaranteed false-RED (dry-run 2026-09-01).
  RELAYED GAP (dispatcher, from QA on TASK-0003, residual/unfiled): `src/lib/db/result.ts` `readCount()` still returns `count ?? 0` — BUG-0007's twin on the user-visible path; a gauge calling it without `{ head: true, count: 'exact' }` would render a fabricated 0. Out of this ticket's touch scope; never write a count query without `count: 'exact'`.
  NOT BLOCKING, no ticket (QA, lean): per-source `byReason` in settled-values excludes unattributed rows while `total` includes them, with no per-source unattributed field (top level has one) — a renderer differencing them would mislabel. `nowOf()` falls back to the real clock on an unparseable `now`, silently. `PENDING_CLAIM_BUCKETS` (containing `in_window`) is re-exported through `src/lib/gauges/index.ts`, so a page can import the full list as filter options — `RENDERABLE_BUCKETS` is the one to use; worth a line in TASK-0008's brief.
  
  **Tried**
  - Six modules, fetch + pure aggregate each, shared helpers in `gauge.ts`; all reads are §4.3 WINDOW reads (`readRows` + explicit `.order` + `.limit`), each output carrying its window plus `truncated`.
  - Chains and row types put in `src/lib/db/gauges.ts` so nothing under `lib/gauges` can reach a database — purity structural; later ruled forced.
  - Red-then-green proofs: mutating `spread()`'s empty percentile to 0 reddens 7 tests; dropping cycle-health's `.gte` reddens the window + bounded tests; dropping the view's `.neq` reddens the `in_window` test; making settles-per-week 0 reddens its test; `limit -1` reddens "is bounded".
  - Null-not-zero policy: unfinished cycles, claims with no observation, empty percentile sets, unmeasurable weeks report null/unmeasurable, never 0.
  - Documented truncation bias per read: claims and review_items scan OLDEST-first (keeps the stuck/longest-waiting rows), cycles/applies/rejections NEWEST-first.
  - QA extended `bounded.test.ts` with one capped-server case per gauge to pin BUG-0009.
  
  **DEAD ENDS**
  - **Filling the per-source `awaiting_row` threshold line — deliberately NOT built.** `stuckPatternThreshold()` reads an empty map, every awaiting_row series reports `threshold:null`, no trend line, and neither the per-source dial nor the global default is spelled anywhere. The dial (`resolver.stuck_pattern`) lives only in scraper registry YAML and where Admin reads it is a blocked question (spec §10). BLOCKED ON TASK-0024. Hand-copying the value is not an answer.
  - **Naming the settled-values exports `fetchSettledValues`.** Forbidden by the landed `tests/offline/review/one-place.test.ts`, which forbids any function/const under `src/` whose name contains `settle|verdict`. Exports are `fetchRejectionStamps` / `aggregateRejectionStamps` / `readRejectionStampGauge`; spec vocabulary kept in the module name and the `SettledValues` types.
  - **Putting the PostgREST chains under `src/lib/gauges/`.** Physically impossible: the layering guard confines `@supabase/supabase-js` to `src/lib/db/**` and a chain needs `SupabaseClient`. Hence `src/lib/db/gauges.ts` — ruled forced, not a scope violation.
  - **Using `readCount()` in a gauge.** Returns `count ?? 0` (fabricated 0). No gauge uses it and a structural test asserts that; counts here are rows fetched and counted in TypeScript.
  - **Measuring opens-vs-settles per week in queue-health.** Unmeasurable: `review_items` has no settle timestamp, verdicts are M2. Null on every week with `settlesMeasurable:false` rather than a fabricated 0.
  - **Trusting `rowCount >= limit` as the truncation signal with limits above the server cap.** The cause of BUG-0009; any fix must survive the capped-server cases in `tests/offline/gauges/bounded.test.ts`.
  - **Leaving the `pending_claims` bucket vocabulary + `in_window` exclusion duplicated.** They sit in `src/lib/db/gauges.ts` per §6 trap 4 ("filter it out at the data layer, once"); when `src/lib/db/claims.ts` lands they should MOVE there and this module import them — a SEAM named in code, not a silent duplicate.
- 2026-09-02T08:52:20Z [compactor] compact threshold 8000 -> 12000 (file is 12480 bytes): first compaction shrank History ~5%; dense evidence log, irreducible
- 2026-09-02T09:02:04Z [builder-17] claimed (attempt 2)
- 2026-09-02T09:04:23Z [builder-17] claimed → built: Verification-only reopen: BUG-0009's fix already landed on run/admin-window as 1b036f5, so ticket/TASK-0007 rebases to 23ffa35 with an EMPTY diff vs the run branch — nothing needed changing and nothing was invented.
- All stored checks re-run green on the rebased tree (worktree TASK-0007, npm ci fresh): npm run lint 0; rm -rf .next/types && ./node_modules/.bin/tsc --noEmit 0; ./node_modules/.bin/vitest run tests/offline/gauges = 167 passed / 0 failed / 0 skipped, exit 0; six-file test -f chain 0; ls supabase/migrations | wc -l = 2 (20260406_admin_allowed_emails.sql, 20260407_add_kblabs_admin_email.sql); grep -rq limit src/lib/gauges 0. Full offline suite npm test = 438 passed / 23 files, exit 0.
- The five ex-pins assert PLAINLY: no it.fails/it.skip/it.todo anywhere under tests/offline/gauges, and 'a gauge read the server truncated at its own cap' is a plain it() over all six SCANS. Diff a272412..1b036f5 on bounded.test.ts shows QA's pinned assertions unchanged — only the PINNED_BUG_0009 it.fails list removed, plus two STRICTER additions (every query's cap <= PLATFORM_ROW_CAP; an oversized caller limit is clamped, read off the exported defaults so a test-local number cannot fake it).
- Strictness observed red-then-green, not assumed: setting GAUGE_ROW_CAP = 5000 in gauge.ts reddens exactly the five ex-pinned gauges plus 8 sibling guards (13 failed / 154 passed); restored to HEAD -> 167 passed. QA trap: that probe was a temp edit in my worktree only, reverted; git status clean, HEAD == run/admin-window.
- Remaining criteria confirmed by inspection on the rebased tree: no .rpc(/CREATE VIEW/raw-SQL anywhere in src/; in_window filtered once at the data layer (src/lib/db/gauges.ts:108,124,399) and in no gauge output; threshold overlay absent with the seam named at src/lib/gauges/pending-claims.ts:83-91 and every awaiting_row series carrying threshold: null (still blocked on TASK-0024 — hand-copying the dial remains a dead end). [EMPTY DIFF: no file changed under scope — 1 so far]
- 2026-09-02T09:04:41Z [dispatcher] nothing to land: ticket/TASK-0007 has no commits and a clean worktree — on to QA as-is
- 2026-09-02T09:05:08Z [qa] built → qa: under attack (batched with BUG-0009: same fix, fix-scoped re-check)
