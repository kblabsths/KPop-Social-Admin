---
id: BUG-0131
type: bug
status: done
milestone: M2
priority: P2
opened_by: qa
parent: null
discovered_from: BUG-0129
children: []
assignee: null
claimed_at: null
depends_on: []
touch_scope: [src/app/queues/page.tsx, src/lib/review/queue-filters.ts, tests/offline/queues/page.test.ts]
force: false
attempts: 1
resolution: fixed
---
# A queue block still blames a facet in another name that removed none of its rows

## Description
Found attacking BUG-0129 (QA, 2026-09-09), on the surface that fix owns. BUG-0129 is correctly fixed for the `kind` facet; this is the same rule, on the two other facet VALUES that select exactly one block’s own set. Pre-existing, not caused by BUG-0129 — but BUG-0129 is what established the rule and the mechanism (`isNarrowed(filter, within)`), so it belongs on that seam.

**Expected** — LOOK_AND_FEEL, the four states ("a table with no rows and a filter that matched nothing are different states and never share a rendering") and its scoped-zero rule, as BUG-0129 restated it: a block whose rendered set is identical with and without a URL facet may not tell the reader that facet is why it is empty, nor scope its open figure by it.

**Found** — `src/app/queues/page.tsx:288-298`. `ownNarrowing` is `{ kind }` and `isNarrowed` excludes a URL facet only when `filter[facet] === within[facet]` (`src/lib/review/queue-filters.ts:196-203`), i.e. only ever a `kind` facet. But the SIGNAL block’s own set is also selected exactly by two other facet values, structurally and not by fixture accident:
- `shape=entity_link_source_pattern` — `shapesOfKind("signal")` is that one shape and no other (`src/lib/review/shapes.ts:136`), and `kindOf` maps it to `signal`;
- `queue=entity_link` — every row of that shape has `queue: "entity_link"` by `shapeOf` (`src/lib/review/shapes.ts:121-128`), and no other queue can produce a signal row.

Both are rendered chips on this very page: `/queues?shape=entity_link_source_pattern` and `/queues?queue=entity_link` are hrefs in the FilterBar (measured — `[data-facet="shape"] a` and `[data-facet="queue"] a`), so each is one click from `/queues`.

REPRO (offline, no database, from the repo root):
1. `./node_modules/.bin/vitest run --project=offline tests/offline/queues/page.test.ts -t "ANOTHER NAME"`
2. It is landed as `it.fails` and therefore PASSES while the defect is live. Drop `.fails` to see it red — that is the signal the fix landed, exactly as BUG-0129’s own pin worked.

MEASURED on run/admin-window @ 7d1c333, `renderQueues(healthyScript(), …)` in `tests/offline/queues/page.test.ts`, SIGNAL block, 3 of 3 rows rendered in every case (identical ids to `/queues`):

| URL | signal rows | open sub-line |
| --- | --- | --- |
| `/queues` | 3/3 | `1 high, 1 low  oldest 25d ago` |
| `/queues?kind=signal` | 3/3 | `1 high, 1 low  oldest 25d ago` (correct, BUG-0129) |
| `/queues?shape=entity_link_source_pattern` | 3/3 | `1 high, 1 low IN THIS FILTERED VIEW  oldest 25d ago` |
| `/queues?queue=entity_link` | 3/3 | `1 high, 1 low IN THIS FILTERED VIEW  oldest 25d ago` |

Same two URLs on an EMPTY `review_items` table, signal block rows region:
- `/queues` and `/queues?kind=signal`: `No signals / The resolver files one when a source crosses its stuck-record threshold.`
- `/queues?shape=entity_link_source_pattern` and `/queues?queue=entity_link`: `No items matching these filters / Widen a filter above; the "all" chip on any row shows everything again.` — Expected: the unfiltered card, since neither facet removed a row.

And with rows present but none open (every row settled), signal block open sub-line:
- `/queues`: `nothing open`
- either URL above: `nothing open in this filtered view`

So the reader is told to "widen a filter" on a queue already showing everything it has, and a whole-queue zero is dressed as a scoped one — the exact two states LOOK_AND_FEEL forbids sharing a rendering.

NOT A REGRESSION of BUG-0129: `/queues?kind=decision|signal`, `?kind=decision&status=settled`, `?kind=decision&kind=signal`, `?kind=bogus|Decision|""` were all re-driven and are correct; the block’s whole markup is byte-identical to unfiltered under its own kind facet, populated and empty (three new passing tests beside the pin).

NOTE FOR THE FIX (not a prescription): `within = { kind }` cannot express this — the narrowing a block applies to itself is a SET, and `shape`/`queue` values entail it rather than equal it. `shapesOfKind(kind)` already exists and is the app’s own statement of the shape half. Whatever shape the fix takes, it must not turn a block’s narrowing off wholesale: `?shape=data_conflict_fact`, `?shape=entity_link_fact` and `?queue=data_conflict` DO remove rows from the decision block (measured 5/7, 2/7, 5/7) and must keep naming their scope.

## Acceptance criteria
npm run lint and tsc --noEmit clean; the offline suite green with the BUG-0129-sibling pin in tests/offline/queues/page.test.ts flipped from it.fails to it.

## Checks
```sh
bash -c "rm -rf .next/types && npm run lint && ./node_modules/.bin/tsc --noEmit"
./node_modules/.bin/vitest run --project=offline tests/offline/queues/page.test.ts tests/offline/queues/filters.test.ts
```

## History
- 2026-09-09T14:59:49Z [qa] opened: A queue block still blames a facet in another name that removed none of its rows
- 2026-09-09T15:03:36Z [builder-198] claimed (attempt 1)
- 2026-09-09T15:04:30Z [qa] WALK CONFIRMS IT on a production build against staging, both themes, zero console errors/warnings.

Production build of run/admin-window @ 0cb3fa9 (`npm run build`, AUTH_URL=http://localhost:8801, SUPABASE_URL/KEY mapped from the STAGING_* pair), own bind-probed port 8801, minted `authjs.session-token`, 1440x900, read-only (no walk-sandbox reset — this walk wrote nothing). Server killed after; PID was mine (32726/32747), port 8801 free again. Text and hooks IDENTICAL in light (body bg rgb(243,244,246)) and dark (rgb(3,7,18)).

Per-URL SIGNAL block, sha of `[data-queue="signal"]` innerHTML (first 16 hex) and its open sub-line — staging holds exactly 1 open signal and 0 decisions:

| URL | signal items | signal block sha | signal open sub-line |
| --- | --- | --- | --- |
| `/queues` | 1 | `8161f3e9b728cb99` | `1 high, 0 low  oldest 7d ago` |
| `/queues?kind=signal` | 1 | `8161f3e9b728cb99` (identical) | `1 high, 0 low  oldest 7d ago` |
| `/queues?shape=entity_link_source_pattern` | 1 | `37535b1d8fc09a99` | `1 high, 0 low IN THIS FILTERED VIEW  oldest 7d ago` |
| `/queues?queue=entity_link` | 1 | `37535b1d8fc09a99` | `1 high, 0 low IN THIS FILTERED VIEW  oldest 7d ago` |

Expected: the last two rows read `8161f3e9b728cb99` and `1 high, 0 low  oldest 7d ago` — the same signal row is on screen in all four, so neither facet removed anything from that block. Found: a scope claim on a queue showing everything it holds.

The bound, re-measured on the same build so the fix cannot over-reach: `/queues?shape=data_conflict_fact` leaves the SIGNAL block `state="empty"`, 0 items, sha `3a9636a0e4725e7d`, `nothing open in this filtered view` + `No items matching these filters` — correct, that shape really does empty it, and it must stay that way.
- 2026-09-09T15:21:06Z [builder-198] claimed → built: - `narrowingOfKind(kind)` (src/lib/review/queue-filters.ts) replaces `ownNarrowing = { kind }`: a block's own narrowing is now every facet value its kind IMPLIES, derived from the registry — kind always; `shape` only when `shapesOfKind(kind)` holds exactly one; `queue` only when every shape of the kind lives in one queue (read OUT of `shapeOf` by a typed probe row, never a second `Record<Shape,ReviewQueue>` to drift); `status` never (a row's own state). `isNarrowed` is untouched — only `within` got richer, so exclusion is still by VALUE. Decision spans 2 shapes and both queues, so `?shape=data_conflict_fact` / `?queue=data_conflict` keep naming their scope. The page hands the SAME derived object to `selectItems` and `isNarrowed` (BUG-0129's anti-drift coupling kept).
- Tests: QA's `it.fails` flipped to `it`; 2 page sweeps over the WHOLE URL vocabulary x both blocks x populated/empty — block markup identical to unfiltered iff the value is implied, and the open sub-line gains its scope claim iff the facet removed rows (held against the same rows rendered with no filter, so no copy string is pinned); 6 unit pins in tests/offline/queues/filters.test.ts (OUTSIDE touch_scope — that file owns queue-filters; same precedent as BUG-0129). Expected values are spelled from spec §6, never read back from the function.
- Red->green, by file copy (never `git stash`): forcing `narrowingOfKind` back to `{ kind }` (pre-fix) reddens 5 — QA's pin, both new page sweeps, and 2 unit pins; weakening `only()` to "first" (over-claim, which DROPS rows) reddens 33. Restored: 127 green.

VERIFIED on the rebased tree (08f061c on run/admin-window @236a071): stored checks exit 0 and 0; `npm test` 0 (2934 offline + 6 isolated); `npm run test:live -- tests/live/queues.live.test.ts` 0 (9).

WALK, production build of the rebased tree (src/ byte-identical: `git diff dab3810 08f061c -- src/` empty), own bind-probed port 8797 (killed after; both PIDs mine), staging, minted cookie, 1440x900, BOTH themes — text identical in light (body rgb(243,244,246)) and dark (rgb(3,7,18)), zero console errors/warnings. Staging holds 1 open signal and 0 decisions. SIGNAL block figure + sub-line:
- `/queues` -> ok, 1 row, `1` `1 high, 0 low oldest 7d ago`
- `/queues?shape=entity_link_source_pattern` -> IDENTICAL (was `1 high, 0 low in this filtered view oldest 7d ago`)
- `/queues?queue=entity_link` -> IDENTICAL
- `/queues?kind=signal` -> IDENTICAL (BUG-0129, unregressed)
- `/queues?shape=data_conflict_fact`, `?queue=data_conflict`, `?shape=entity_link_fact` -> empty, `0` `nothing open in this filtered view` + rows region `No items matching these filters / Widen a filter above...` (correct: each really empties it)
DECISION block: `/queues` -> `0` `nothing open` + `No decisions waiting / The resolver files one when...`; all six facet URLs above -> `0` `nothing open in this filtered view` + `No items matching these filters` (correct: every one of them removes decision rows).

FOR QA: (1) staging holds zero decision items, so the POPULATED "a facet really narrowed the decision block" case is offline-only. (2) On screen the micro label is uppercased by CSS, so a walk reader using `innerText` sees `OPEN SIGNALS` and misses the card — read `textContent` (cost me a cycle). (3) `REVIEW_QUEUES` still lives in queue-filters.ts rather than shapes.ts; the derivation needs both, which is why it sits here — the long-term home note in that file still stands.
- 2026-09-09T15:21:30Z [dispatcher] landed ticket/BUG-0131 onto run/admin-window as b3fb571
- 2026-09-09T15:22:21Z [qa] built → qa: under attack
- 2026-09-09T15:35:43Z [qa] qa → done: Attacked on the landed tree (run/admin-window @ e071191), in a detached `_receipt-` worktree, criteria re-run BEFORE reading the builder's story.

STORED CHECKS, verbatim: `rm -rf .next/types && npm run lint && tsc --noEmit` exit 0; `vitest run --project=offline tests/offline/queues/page.test.ts tests/offline/queues/filters.test.ts` exit 0 (127 green). Criterion's place-clause checked: the sibling pin IS in tests/offline/queues/page.test.ts and IS flipped from `it.fails` to `it` (line ~1076, "…in ANOTHER NAME…(admin-window/BUG-0131)"); no `it.fails` remains in that file except the new BUG-0133 pin I landed. Whole suite from the worktree: `npm test` exit 0, 2935 offline + 6 isolated.

WHAT I TRIED TO BREAK:
1. COMBINATIONS the builder never drove. Swept all 98 multi-facet URLs (every kind x queue x shape x status subset of size >= 2) x both blocks x populated and empty, comparing `[data-queue]` innerHTML against the unfiltered render, with the expectation derived structurally (identical iff EVERY facet value the URL names is implied by that block's kind) and cross-checked against the fixture row-by-row. Zero divergences. Includes the packet's `?kind=signal&shape=entity_link_source_pattern` (both implied -> byte-identical) and the three-facet spelling.
2. THE BOUND. `?shape=data_conflict_fact`, `?queue=data_conflict`, `?shape=entity_link_fact` still narrow the decision block; `?status=open`/`?status=settled` narrow BOTH blocks on every URL that carries them (status is never implied) — the fix did not turn a block's narrowing off wholesale.
3. SABOTAGED THE DERIVATION. Added a fourth shape (`schedule_gap_pattern`, kind signal) in a third queue `schedule` to shapes.ts + REVIEW_QUEUES by file copy (never `git stash`), restored after. `narrowingOfKind("signal")` correctly collapsed to `{"kind":"signal"}` (2 shapes, 2 queues -> nothing implied) and `decision` was unchanged — the probe-row derivation adapts. And the suite reddened HONESTLY, not vacuously: 7 named failures including both spec-spelled IMPLIED tables and the BUG-0131 pin. The `queues.every(list => list.length > 0)` guard means a shape the probe cannot reach implies no queue (conservative direction).
4. WALK, production build of this tree, own bind-probed port 8805 (killed after; PIDs 49275/49298 were mine, port free), staging, minted cookie, 1440x900, BOTH themes — all 12 URLs text- and state-identical light vs dark, zero console errors/warnings. SIGNAL block sha of innerHTML: `8161f3e9b728cb99` on `/queues`, `?kind=signal`, `?shape=entity_link_source_pattern`, `?queue=entity_link`, `?kind=signal&shape=entity_link_source_pattern` and the three-facet spelling — byte-identical, the defect is dead where BUG-0131 measured it (`37535b1d8fc09a99` before). `?shape=data_conflict_fact`/`?queue=data_conflict`/`?shape=entity_link_fact`/`?kind=decision` -> `3a9636a0e4725e7d`, empty and correctly scoped. `?status=open` -> `37535b1d8fc09a99`, 1 row and "in this filtered view": correct, status narrows.

JUDGMENT: the ticket's goal is met — a block's own narrowing is now every facet value its kind implies, derived from the registry rather than written down, and the two chips one click from the page's own chip row no longer blame a facet that removed nothing.

FILED, not a bounce: BUG-0133 (P2, discovered_from BUG-0131). Same rule, one trigger further out and outside this fix's reach: when a block's own queue is EMPTY, no facet can have removed a row from it, yet 10 of the 12 URLs I walked put the DECISION block (staging holds 0 decisions) in `nothing open in this filtered view` + `No items matching these filters / Widen a filter above` — while widening shows the same zero. Pinned strict as `it.fails` in tests/offline/queues/page.test.ts, watched red first (`expected 'nothing open in this filtered view' to be 'nothing open'`), committed on the run branch as 45aa1ed. BUG-0131's own rule is correct and unbroken by it.

RESIDUAL (no ticket): `isNarrowed`'s default `within = {}` (src/lib/review/queue-filters.ts) is now dead in product code — queues/page.tsx:305 is its only caller and passes two args; claims/page.tsx uses a different module's one-arg `isNarrowed`. Its doc's "what the page-level callers want" is stale. Trigger for a ticket: someone adds a one-arg caller and inherits the whole-URL answer by accident. [resolution: fixed] [receipt green: 2 cmd(s), tree d0675b16]
