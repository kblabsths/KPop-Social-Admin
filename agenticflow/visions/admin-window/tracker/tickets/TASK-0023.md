---
id: TASK-0023
type: task
status: blocked
milestone: M1
priority: P1
opened_by: architect
parent: FEAT-0005
children: []
assignee: null
claimed_at: null
depends_on: []
touch_scope: [src/lib/db/runs.ts, src/app/cycles/page.tsx]
force: false
last_failure_sig: fa7764405279
same_failure_count: 1
seen_failure_sigs: [fa7764405279]
---
# ASK BEN: which of the adapter runs table's 22 columns does Cycles & runs show?

## Description
**Contract silence.** ARCHITECTURE marker `OPEN-RUNS`. SPEC named gap 1.

Spec section 4 puts *'`resolution_runs` and the adapter framework's `runs`, newest first, with the counts as columns and `error_summary` inline'* on the Cycles & runs page. For `resolution_runs`, `contracts/resolver.md` section 6 names every column, so that half is fully specified and is being built (TASK-0014). For `runs`, `adapters.md` is **not** among the contract snapshots, so 'the counts' is undefined — and the table has 22 columns.

What the table actually holds (scraper repo, migration `20260829000001`, read 2026-09-01): `run_id, source (text, no FK), started_at, ended_at, outcome (succeeded | partial | failed | skipped), failure_class (transient | structural | config), checkpoint_before, checkpoint_after, error_summary, payloads_fetched, payloads_archived, records_parsed, records_rejected, claims_emitted, claims_dropped_empty, claims_collapsed, claims_ai, records_linked, records_unlinked, records_escalated, batches_written, observations_returned`.

**Recommend** a nine-column row that reads like the GitHub-Actions-run-list taste reference — diagnosable before you click anything — and nothing else: `source`, `started_at`, duration (derived from `ended_at`), `outcome`, `failure_class`, `records_parsed`, `claims_emitted`, `records_unlinked`, `error_summary` inline. Rationale: source/when/how-it-ended answers 'did anything happen last night'; `failure_class` is the one column that says whose problem it is; parsed vs emitted is the yield; `records_unlinked` is the number that feeds the entity-link queue this app exists to render. The other thirteen are adapter-internal and belong on a run detail view that has no consumer yet.

TASK-0016 is blocked on this answer and holds the whole adapter-runs half; the resolver-cycles half ships without it.

## Acceptance criteria
(human-checkable) Ben names the column set (or approves the recommendation). The architect then removes the `OPEN-RUNS` marker from ARCHITECTURE and amends TASK-0016's criteria to that exact set, so 'no more, no fewer' is checkable.

## Checks
```sh
bash -c "! grep -q OPEN-RUNS agenticflow/docs/vision/ARCHITECTURE.md"
```

## History
- 2026-09-02T05:45:08Z [architect] opened: ASK BEN: which of the adapter runs table's 22 columns does Cycles & runs show?
- 2026-09-02T05:45:09Z [architect] open → blocked: ASK BEN: adapters.md is not a contract snapshot, so which of the runs table's 22 columns Cycles & runs shows is unspecified. Nine-column recommendation on the ticket. Holds TASK-0016 only.
- 2026-09-02T16:31:38Z [dispatcher] Ben 2026-09-02 (ruling): the adapter-runs half of Cycles shows: source, started_at, ended_at / running state, outcome, the error line (error_summary), records_parsed, claims_emitted, records_unlinked, failure_class. Nothing else of the 22 columns in M1. Architect: fold into TASK-0016's criteria and close this ASK; TASK-0016 also honours ?source=<name> on this half (Sources seam).
- 2026-09-02T16:31:38Z [dispatcher] blocked → open: unblocked: Ben ruled the column set; architect records and closes
- 2026-09-02T16:52:16Z [receipt] RED receipt: failure signature fa7764405279 (same failure 1x)
- 2026-09-02T16:52:43Z [qa] QA refused the close: the ASK is answered but the architect's follow-through has not landed, and the ticket's own criterion names that follow-through.

Expected (TASK-0023 acceptance criteria): 'Ben names the column set (or approves the recommendation). The architect then removes the OPEN-RUNS marker from ARCHITECTURE and amends TASK-0016's criteria to that exact set, so no more, no fewer is checkable.'

Found, half 1 (Ben's ruling) — PRESENT. TASK-0023 History 2026-09-02T16:31:38Z [dispatcher]: source, started_at, ended_at/running state, outcome, error_summary, records_parsed, claims_emitted, records_unlinked, failure_class; nothing else of the 22 in M1; TASK-0016 also honours ?source=<name>.

Found, half 2 (architect records) — ABSENT, three ways:
1. Stored check RED. Ran verbatim: bash -c "! grep -q OPEN-RUNS agenticflow/docs/vision/ARCHITECTURE.md" -> exit 1. receipt.py TASK-0023 -> 'receipt RED: tracker/receipts/TASK-0023.json'. The marker is still live at agenticflow/docs/vision/ARCHITECTURE.md:701 (= visions/admin-window/docs/ARCHITECTURE.md, same inode 77410326), still listed as open question 1 under section 12, whose own text is the bar: 'A question is closed only when its marker leaves this list.'
2. TASK-0016's packet does NOT carry the column set. Its criteria still read 'exactly the columns Ben's answer names — no more, no fewer', which is unresolvable without the set written on the ticket; the only place those column names appear in TASK-0016.md is line 21, the 22-column enumeration inside the paragraph that still says 'This ticket waits on a blocked question and must not be started before it is answered.' A builder opening TASK-0016 today is told to stop, not what to build.
3. agenticflow/docs/DECISIONS.md holds no paragraph for this ruling (grep -niE 'OPEN-RUNS|TASK-0023|records_unlinked|claims_emitted' -> zero hits), while ARCHITECTURE section 12 asserts every settled question has 'a dated paragraph in DECISIONS.md'.

Also unrecorded anywhere in TASK-0016: the ruling's second sentence, '?source=<name> on this half (Sources seam)'. TASK-0014's QA residual (TASK-0016 History 2026-09-02T15:31:46Z) covers the same seam, so it is not lost — but it is a relayed note, not a criterion.

Not closing and not transitioning: the ticket is correctly 'open' and the remaining work is the architect's, not a builder's. Sequence to unblock: architect strikes item 1 from ARCHITECTURE section 12, writes the dated DECISIONS.md paragraph, then amend-criteria on TASK-0016 to the nine named columns plus ?source=; TASK-0023's stored check goes green on step one alone and QA can then close it already_done.
- 2026-09-02T16:53:22Z [dispatcher] open → blocked: Parked pending the architect's record: strike OPEN-RUNS item 1 from ARCHITECTURE §12, DECISIONS paragraph, amend TASK-0016 criteria to the nine ruled columns plus ?source=, drop its 'must not be started' hold. Then QA closes already_done.
- 2026-09-02T16:57:26Z [architect] Architect's record has landed; QA's three gaps are closed. (1) ARCHITECTURE.md section 12 open question 1 is struck — the marker string is gone from the file, the list renumbered (OPEN-CLAIMS-COST is now 1), the settled paragraph reads 'Five questions were settled 2026-09-02' and carries the nine columns plus the ?source= facet, and a fourth-ruling-pass entry in the file's History says why. Stored check dry-run verbatim: bash -c '! grep -q OPEN-RUNS agenticflow/docs/vision/ARCHITECTURE.md' -> exit 0. (2) DECISIONS.md has the dated paragraph '2026-09-02 — the adapter-runs half of Cycles shows nine of the runs table's 22 columns, and honours ?source=', with the set, why those nine, and the three doors it closes: the other thirteen are out of M1 scope (a ticket wanting one re-opens the decision, it does not just add a column); the runs read stays a WINDOW read and no total may be computed from rows.length; runs.source is matched by name, never resolved by key. (3) TASK-0016 amended: the nine columns are enumerated in its criteria and description, ?source= is a requirement rather than a relayed note, the 'waits on a blocked question and must not be started' hold is struck, its read shape is pinned to reuse of DashboardRunRow in src/lib/db/dashboard.ts plus the four counts and failure_class, its runs.ts check widened to those four columns (measured exit 1 on today's unbuilt tree), and depends_on is now [TASK-0014] alone — it no longer waits on this ticket. Nothing further is owed here: QA may close TASK-0023 already_done.
