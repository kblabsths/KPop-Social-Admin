---
id: BUG-0183
type: bug
status: open
milestone: M3
priority: P2
opened_by: qa
parent: FEAT-0015
discovered_from: BUG-0180
children: []
assignee: null
claimed_at: null
depends_on: [BUG-0176]
touch_scope: [src/components/ui/window-line.tsx, src/app/claims/page.tsx, src/app/browse/page.tsx, tests/offline/claims/page.test.ts, tests/offline/ui/primitives.test.ts, tests/offline/browse/page.test.ts]
force: false
---
# The window line says 50 claims are below it while 37 are on screen — the unpaged arm falls back to the cap

## Description
**Module:** `src/components/ui/window-line.tsx:541` — `onScreen(info) { return info.drawn ?? info.limit; }`, read by the `matched` arm's `holdsBack` clause ("the {N} longest-waiting are below") on the UNPAGED arm of /claims, where `drawn` is absent.

**Expected** (admin-window/BUG-0174's own rule, LESSONS 2, ARCHITECTURE §4.3): the clause that names the rows BELOW the line states the rows that are on screen. BUG-0174 closed exactly this on the PAGED arm — its `onScreen` docstring records the defect as 'the matched arm fell back to `limit` whenever no press had grown the window past it'.

**Found:** the same fallback still fires wherever the window carries no `drawn` — which is every UNPAGED state of /claims — and the clause is NOT gated on the read having filled its cap (`onScreen`'s docstring claims it is: 'The cap keeps its ONE true use, the unpaged window, where the clause is rendered only over a read that filled'). The gate is `info.truncated`, and on /claims `truncated` comes from the COUNT read (`src/app/claims/page.tsx:1163`, `listCount.data > listed.length`), never from the rows filling 50.

Measured on run/admin-window at c35cc33, offline through the page's real render from the real deps, fixture `offGridScript(rows, count)` (the suite's own 'a database this app must survive' fixture, tests/offline/claims/page.test.ts):

- 37 rows drawn, count 900 -> `data-window-held="900"`, `data-window-truncated="true"`, no paging element, line: '… A window of at most 50 rows, not the whole view. 900 claims in all; the 50 longest-waiting are below — the rest are not shown.'
- 10 rows drawn, count 877 -> same clause: 'the 50 longest-waiting are below'.
- 49 rows drawn, count 51 -> same clause: 'the 50 longest-waiting are below'.
- 37 rows drawn, count 37 (the reads AGREE) -> 'The window did not fill — 37 of at most 50 …' (correct; this is the state the fallback is safe in).

So three screens holding 37, 10 and 49 claims all tell the operator that 50 are below the line. The number is the CAP, published as a description of the screen.

**Reachability:** /claims is drawn unpaged whenever `pageBound(String(listed.length), 50)` is off-grid — i.e. whenever the row read comes back with a number of rows that is not a multiple of the window while the count says more exist. That is the same two-reads divergence this campaign has already recorded on live data twice (staging's count moved 877 -> 878 mid-campaign; admin-window/BUG-0174, admin-window/BUG-0180) and the state the suite's own `offGridScript` comment calls 'a database this app must survive'. It was NOT reproduced on staging: staging's count and rows agree today, and the paged arm (BUG-0180's tree) is correct in every state QA drove in a browser.

**Repro (exact), offline:**
1. `./node_modules/.bin/vitest run --project=offline tests/offline/claims/page.test.ts -t "states the rows that are BELOW it"`
2. The case (QA, in `describe("the claim list's window")`) renders two off-grid screens under ONE count — 37 rows and 10 rows, both count 900 — and compares the two LINES against each other. It pins no word of the sentence: a line that states what is below it cannot say the same thing about two screens holding different numbers of claims. Watched RED on c35cc33 with every non-vacuity assertion green (37 and 10 rows drawn, held=900 on both, `pagingOccurrences` 0 on both).
3. Landed as a strict `it.fails` pin (this repo's QA convention), so the suite is green today and turns RED the moment the divergence disappears. **The fix also flips the pin** to a plain `it(...)`, which is what check 2 below measures.

**NOT IN SCOPE:** the PAGED arm and every one of admin-window/BUG-0180's criteria (graded green on this tree, and the paged control's two terminal sentences are correct — do not move them); the `didNotFill` clause, which is the honest reading where the two reads AGREE and must keep rendering exactly what it renders; the hooks (`data-window-*` are the live oracle's; none is added or renamed); whether /claims should offer a control in an off-grid state at all (admin-window/BUG-0172 ruled it may not, and that ruling stands). The WORDS are the designer's; what is graded here is that the number the clause names is the number of rows on screen.

## Acceptance criteria
THE FIX SHAPE IS RULED HERE (architect, 2026-09-11) because it cannot be chosen inside the ticket. `drawn`'s PRESENCE carries TWO facts today — how many rows are on screen, and whether a press may continue this window (`pageable(info)` is `drawn !== undefined && drawn !== null`) — so simply stating `drawn` on the unpaged arm would hand the unpaged line the PAGED arms and turn the agreeing state's `didNotFill` into `holdsBack`. The two facts are separated, in `src/components/ui/window-line.tsx` and the two pages that compose a window, and nowhere else.

1. `drawn` IS THE ROWS THIS WINDOW PUT ON SCREEN — ON THE FIRST SCREEN TOO. `/claims` states it on BOTH arms, from the rows it actually rendered (`listed.length`), so the unpaged `matched` line names a number a read established. `onScreen` answers `info.drawn`, and the CAP leaves that function: `info.limit` is the size of a window and is never a description of a screen, in any arm or any state. Its docstring stops claiming a true use for the cap it no longer has.

2. CONTINUABILITY IS STATED, NOT INFERRED FROM `drawn`. `DrawnWindow` gains one optional fact — `continues?: boolean`, absent meaning "no press can continue this window", which is the nine unpaged `WindowLine` call sites unchanged — and `pageable(info)` reads THAT field and nothing else. The two pages that already choose between the paged and the unpaged line state it from the same expression that makes the choice (`src/app/claims/page.tsx`'s `pageable`, `src/app/browse/page.tsx`'s `drawn !== null`), so ONE page-level fact reaches both components. `src/components/ui/paging.tsx` IS NOT EDITED: `continuedWindow` already spreads the page's window (so `continues` rides through) and already overwrites `drawn` with the driver's own `held`, which is exactly what this ticket wants it to keep doing. It is admin-window/BUG-0176's destination this week; this ticket is chained behind that one and stays out of the file.

3. A WINDOW THAT STATES NO ROWS ON SCREEN NAMES NONE. Where `drawn` is absent, the two arms that name the rows below (`matched`, `catalog`) render no clause naming them: the line keeps the words it already renders — its lede, its "a window of at most N" cap clause, and `didNotFill` where the window did not fill — and invents no number. NO NEW SENTENCE AND NO NEW WORD is added to this file by this ticket; the state is unreachable from either page once criterion 1 lands, and it is covered by a named test that builds such a window directly. `readsAgree` keeps its exported signature (`paging.tsx` calls it, unedited) and answers the DISAGREE verdict for a window that states no rows on screen: a comparison no read supports is not an agreement.

4. THE DEFECT ITSELF (QA's, as measured). On the UNPAGED /claims arm the below-clause states the rows the page DREW: two screens rendered from the same count and different row counts (37 and 10 under a count of 900) do not render the same line, asserted on rendered markup from the page's real render and compared against EACH OTHER, so no word of the sentence is pinned. The QA pin lands as a plain `it(...)` rather than `it.fails(...)`, and its own non-vacuity assertions stand: 37 rows and 10 rows drawn, `data-window-held` 900 on both, and no paging element in either.

5. NOTHING ELSE MOVES. The state where the two reads AGREE is byte-identical — a window that did not fill under a count equal to its rows still renders the `didNotFill` clause exactly as the pre-ticket tree does. The whole PAGED arm renders byte-identical markup in every state of admin-window/BUG-0180's tree, both pages, first screen included. A truncated UNPAGED window whose rows equal the cap is byte-identical too (50 rows under a cap of 50 named 50 before and names 50 after) — which is the only unpaged state that was ever right, and SPEC F14's first server render of /claims and /browse is unchanged. No `data-window-*` attribute is added, removed or renamed. The `newest` and `alphabetical` arms, `didNotFill`, `continuedTo`, `ended` and the nine unpaged call sites are untouched.

6. `npm run lint` and `npm run typecheck` are clean and a PRODUCTION build is green.

## Checks
```sh
./node_modules/.bin/vitest run --project=offline tests/offline/claims/page.test.ts tests/offline/browse/page.test.ts tests/offline/ui/paging.test.ts tests/offline/ui/primitives.test.ts
grep -q "states the rows that are BELOW it" tests/offline/claims/page.test.ts
! grep -q "it.fails(\"states the rows that are BELOW it" tests/offline/claims/page.test.ts
! grep -qF "info.drawn ?? info.limit" src/components/ui/window-line.tsx
grep -qF "continues?: boolean" src/components/ui/window-line.tsx
grep -qF "continues:" src/app/claims/page.tsx
grep -qF "continues:" src/app/browse/page.tsx
grep -qF "it(\"names no rows below a window that states none" tests/offline/ui/primitives.test.ts
npm run lint
npm run typecheck
npm run build
```
## History
- 2026-09-11T09:08:44Z [qa] opened: The window line says 50 claims are below it while 37 are on screen — the unpaged arm falls back to the cap
- 2026-09-11T09:20:09Z [architect] touch_scope: [src/components/ui/window-line.tsx, tests/offline/claims/page.test.ts] → [src/components/ui/window-line.tsx, src/app/claims/page.tsx, src/app/browse/page.tsx, tests/offline/claims/page.test.ts, tests/offline/ui/primitives.test.ts, tests/offline/browse/page.test.ts]: Architect ruling 2026-09-11: the fix splits drawn's two facts, so the two pages that compose a window state continues (claims, browse) and claims states drawn on its unpaged arm; window-line.tsx's unit home is tests/offline/ui/primitives.test.ts (24 sites spread a PAGED fixture and will state continues), and browse's page suite grades the catalog arm's byte-identity. src/components/ui/paging.tsx is deliberately NOT in scope - BUG-0176 holds it this week and continuedWindow already does what this ticket needs.
- 2026-09-11T09:20:15Z [architect] criteria amended: Architect 2026-09-11: QA's six criteria kept in substance (4 and 5 are theirs, near-verbatim) with the FIX SHAPE ruled ahead of them, because the obvious fix - state drawn on the first screen - breaks the agreeing state on its own: pageable() reads drawn's presence, so an unpaged window carrying drawn would render the PAGED arms and the didNotFill state QA's criterion 3 pins byte-identical would become holdsBack. Measured on this tree: claims/page.tsx composes listWindow with no drawn and chooses the component with its own pageable const; browse/page.tsx does the same with drawn !== null; paging.tsx's continuedWindow spreads the page's window and overwrites drawn, so a page-stated continues rides through untouched and that file needs no edit at all. [was: 1. On the UNPAGED /claims arm, the line's below-clause states the rows the page DREW, not the window's cap: two screens … (1430 chars; full text in git history)]
- 2026-09-11T09:20:24Z [architect] checks amended: Dry-run on run/admin-window at fadea8f, 2026-09-11 (architect). Line 1: 4 files / 434 tests green in 5.9s. Lines 2 and 3 are QA's pin, measured 0 and 1 today. Lines 4-8 are the ruling's presence checks and ALL FIVE exit 1 today, measured one by one: the cap fallback is still spelled 'info.drawn ?? info.limit' in window-line.tsx, neither page states continues, and the unreachable-state test does not exist. typecheck added because criterion 2 changes an exported interface eleven call sites build against. [was: ./node_modules/.bin/vitest run --project=offline tests/offline/claims/page.test.ts tests/offline/browse/page.test.ts tes… (384 chars; full text in git history)]
- 2026-09-11T09:20:29Z [architect] depends_on: [] → [BUG-0176]: Serial at the destination (architect ruling 2026-09-11, ARCHITECTURE 13.9). This ticket's unit home is tests/offline/ui/primitives.test.ts, which BUG-0176 holds right now (its touch scope names it, and it is claimed). BUG-0176 is also the ticket in src/components/ui/paging.tsx; this one is ruled to stay out of that file entirely, so the only collision left is the test file and the edge resolves it. No other edge: this ticket needs nothing BUG-0176 produces.
- 2026-09-11T09:22:59Z [architect] Contract amended with this ruling (architect, 2026-09-11): ARCHITECTURE §4.3's paged-surface bullet gains rule 4 - a fact a sentence depends on is STATED by whoever knows it, never inferred from another field's size or from whether it is present - and Common violations row 22 records the class at count 2 (BUG-0174's heldFrom size heuristic, and this ticket's drawn-presence). Read rule 4 before writing criterion 2: the field you are adding is the owner that was missing, and the reason this is an interface split rather than a one-line fix is that the second surface could not state a true fact without silently changing the first one's rendering.
