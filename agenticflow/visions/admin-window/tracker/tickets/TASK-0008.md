---
id: TASK-0008
type: task
status: reopened
milestone: M1
priority: P2
opened_by: architect
parent: FEAT-0006
children: []
assignee: null
claimed_at: null
depends_on: [TASK-0004, TASK-0007]
touch_scope: [src/components/gauges, tests/offline/gauges-ui]
force: false
attempts: 1
---
# Gauge cards: the figure card, the trend table and the distribution view

## Description
Module: `src/components/gauges/` — the presentation half of FEAT-0006. Authority: LOOK_AND_FEEL (Stat / gauge card, the data-table rule, the four mandatory states); spec section 5; ARCHITECTURE sections 4 and 7.

Three pure synchronous components, each taking the output shape of a `lib/gauges` aggregate function as plain props:

- `GaugeCard` — square, 1px border, surface fill, 12px padding: `micro` label, the `figure` number thousand-separated, at most one `data` line of sub-detail. Colour on the figure only when it carries a palette state.
- `TrendTable` — the data-table rule applied to a per-period or per-source series; no bespoke layout, no chart library.
- `Distribution` — an age/percentile distribution rendered as rows of label + `data` value; 'chart' here means the data-table rule and CSS bars built from tokens, **not a charting dependency** (none is allowed).

Each renders all four states from the `ui` primitives: loading, empty, **not provisioned** (gray, naming the missing table, never a zero that reads like data), error (one red `data` line naming what failed).

Out of scope: any data access (a component importing `lib/db` is a defect), any page, any new dependency.

## Acceptance criteria
Three components exist, are pure and synchronous, import nothing from `src/lib/db`, and render the four states through the `ui` primitives. Offline tests render each with a fixture aggregate output and with each of the four states and assert the emitted token classes and the presence of the named missing table in the not-provisioned case. No charting or UI dependency is added: no chart library name appears in `package.json`, and any dependency at all must first pass the supply-chain gate as a toolsmith DEP ticket against ALLOWED_DEPS.md — this ticket files none. (human-checkable) The cards match LOOK_AND_FEEL's gauge-card anatomy at the close-review screenshots.

## Checks
```sh
npm run lint
./node_modules/.bin/tsc --noEmit
npm run build
./node_modules/.bin/vitest run tests/offline/gauges-ui
bash -c "! grep -rq @/lib/db src/components"
bash -c "! grep -qiE 'recharts|chartjs|chart[.]js|apexcharts|victory|nivo|plotly|visx|d3-' package.json"
```
## History
- 2026-09-02T11:45:28Z [compacted → agenticflow/tracker/notes/TASK-0008.md]
  Summary (state; attempts; DEAD ENDS preserved):
  **State** — attempt 1 (builder-31) built and landed on run/admin-window as cbafd26; QA attacked it and bounced the ticket to `reopened` with BUG-0018, now being fixed by a builder in a separate worktree.
  
  Built: `src/components/gauges/{gauge-card,trend-table,distribution,state}.tsx` + `index.ts` — three pure sync components, plain props, no `lib/db` import (only reach: `import type { Spread }` via lib/gauges, type-only and erased), all four states through the `ui` primitives. `state.tsx` holds the one rule: Empty/NotProvisioned are cards and REPLACE the surface; Loading/ErrorLine are lines rendered inside, header intact. `GaugeState` kinds are spelled as `lib/db/result.ts` spells them, so a page maps a `DbResult` onto a gauge with no translation table. `GaugeCard`'s props are a 3-arm union — a value that can be null will not COMPILE without `absent`, which is how "em dash with its reason, never 0" is enforced; a floor/tone on an absent figure is suppressed on purpose. TrendTable/Distribution force `rows=[]` whenever `state` is set (stale rows under a loading line would be a lie). Distribution's "chart" is DataTable + a CSS bar of two token fills; the only inline style is the bar's data-driven width %, pinned by a test. Two additive edits BEYOND touch_scope, deliberate, both ruled keep: (1) `StatCard` gained `floor` — renders "at least" in sans-secondary beside the mono figure so `window.truncated` is visible without a second card anatomy in a second file; (2) `lib/format` gained `duration(seconds)` on relativeAge's unit ladder — every gauge distribution is seconds and the app has one formatting module. Tests for both went into the files owning those surfaces (primitives.test.ts, format.test.ts).
  
  QA re-ran all six stored checks verbatim on cbafd26, all green: `npm run lint` 0, `./node_modules/.bin/tsc --noEmit` 0, `npm run build` 0 (after `rm -rf .next/types`), `./node_modules/.bin/vitest run tests/offline/gauges-ui` 21 passed, no `@/lib/db` under src/components, no chart-library name in package.json. Full `npm test` 779 passed at build; full offline suite 781 passed with QA's pins. No dependency added; no DEP filed; package.json untouched by cecaab4. QA's attack drove real aggregate output, not hand shapes.
  
  Architect rulings, binding: checks amended to align the layering check with TASK-0004's spelling so it catches every import form of `@/lib/db`; checks and criteria amended 2026-09-02, replacing a false-RED package.json diff (see DEAD ENDS) with the named-chart-library absence pin — the bar is now "no chart library, and no DEP ticket filed". Test-idiom ruling (DECISIONS.md 2026-09-02): the offline include glob stays `tests/offline/**/*.test.ts` — no `.tsx`, no JSX, no jsdom. Component tests build elements with `createElement` and assert emitted markup. Import the landed shared helper `tests/offline/ui/markup.ts` (`h` = createElement, `render` = renderToStaticMarkup, `classesOf`, `tagsOf`, `textOf`) instead of writing your own render helper; see tests/offline/ui/primitives.test.ts for the pattern.
  
  Relayed residuals (dispatcher): (a) from QA on TASK-0007 — `src/lib/gauges/index.ts` re-exports `PENDING_CLAIM_BUCKETS`, which contains `in_window`. Never surface that constant as filter options or labels — `in_window` renders nowhere in the UI (spec §4; FEAT-0005 check pins it under src/app and src/components). Honoured: labels come only from props, QA-confirmed. (b) From QA on BUG-0009 — `resolveBounds` floors a fractional limit: `limit: 0.5` passes the `> 0` guard, floors to 0, and the query goes out as `.limit(0)` (empty window, blank card). Any page mapping a query-string limit through `Number()` needs an integer guard before it reaches a gauge. A page concern; no query-string parsing exists in these components.
  
  QA verdict, what broke (BUG-0018): GaugeCard decides absence with its own `value === null` (`gauge-card.tsx:93-105`) rather than `isAbsent` (`lib/format.ts`, the single definition BUG-0004 exists to enforce). StatCard dashes anything `isAbsent` accepts, so the two paths disagree. Expected (the component's own docblock and its sibling test 'never qualifies or colours a figure it does not have'): no floor qualifier and no palette colour on a dash. Found, with `value = duration(aggregateCycleHealth(unfinished).duration.p50)` i.e. the em-dash STRING this ticket's own new helper returns for a null percentile: `<span class=type-body>at least</span>` + `<span class="type-figure text-broken">—</span>`, and no reason line, because the third union arm is unreachable through a non-null string. Same markup for NaN and Infinity. The three-arm union is therefore structural only against a literal null, not against the app's formatters. Latent today (`grep GaugeCard src/app` -> no match), user-visible at the first gauge page. Pins committed 2b724d5 in `tests/offline/gauges-ui/cards.test.ts`, both watched red as plain `it()` first, now `it.fails` (strict xfail, BUG-0018); re-run with `./node_modules/.bin/vitest run tests/offline/gauges-ui`.
  
  QA residuals, not tickets: (1) a surface state on GaugeCard replaces the whole card, so the gauge's own `micro` label disappears in the empty/not_provisioned case — an operator sees which table is missing but not which knob; trigger for a ticket would be the six-card dashboard grid landing with two unprovisioned gauges. (2) TrendTable/Distribution with `rows:[]` and no state render a headers-only table with an empty tbody and no empty state — reachable when a page hands `AwaitingRowTrend.series:[]` without mapping it to a state; the four-states duty currently sits entirely on the caller.
  
  **Tried**
  - Three components plus `state.tsx` as the single owner of the four-state rule.
  - Distribution's "chart" as `ui/DataTable` + a CSS bar of two token fills.
  - `GaugeState` kinds mirroring `DbResult`'s spelling.
  - A three-arm props union on GaugeCard against a nullable figure.
  - Additive `StatCard.floor` and `lib/format.duration` instead of duplicating anatomy/ladder.
  - Tests via `createElement` + `renderToStaticMarkup` on real `aggregateCycleHealth` / `aggregateQueueHealth` output, seen red first (wrong fixture arithmetic + an HTML entity).
  
  **DEAD ENDS**
  - `git diff --quiet $(git merge-base HEAD main)..HEAD -- package.json` as the no-new-dependency check: false-RED, measured — exits 1 on the run branch because TASK-0001 legitimately added vitest and vite-tsconfig-paths, and the merge-base is main, so the diff reports the whole branch's work, not this ticket's. No builder could turn it green. Replaced by the named-chart-library absence grep (dry-run exit 0); real enforcement is the supply-chain hook plus a toolsmith DEP ticket.
  - The criterion "package.json dependencies are unchanged": same cause — it asserted a branch-wide diff this ticket does not control.
  - `.tsx` / JSX / jsdom component tests, and a bespoke render helper: ruled out 2026-09-02 (see the test-idiom ruling above).
  - Any charting dependency (recharts, chart.js, apexcharts, victory, nivo, plotly, visx, d3-): none allowed; the check greps package.json, and any dependency at all needs a toolsmith DEP ticket against ALLOWED_DEPS.md first. This ticket files none.
  - A zero standing in for a missing table: never — the not-provisioned card names the missing object verbatim.
  - Keeping stale rows under a loading line: rejected; rows forced to `[]` whenever `state` is set.
  - A second card anatomy for a truncated window, and a second copy of the unit ladder outside `lib/format`: rejected as drift — hence the two out-of-scope additive edits.
  - **GaugeCard's local `value === null` absence test — BUG-0018, detailed above.**
