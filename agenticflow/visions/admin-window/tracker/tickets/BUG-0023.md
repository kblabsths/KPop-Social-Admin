---
id: BUG-0023
type: bug
status: done
milestone: M1
priority: P1
opened_by: qa
parent: FEAT-0005
discovered_from: TASK-0014
children: []
assignee: null
claimed_at: null
depends_on: []
touch_scope: [src/app/cycles/page.tsx, tests/offline/cycles/page.test.ts]
force: false
attempts: 1
resolution: fixed
---
# Cycles: a ?cycle= link asserts the cycle is absent when resolution_runs was never read

## Description
Expected (ARCHITECTURE.md §7 / TASK-0030's four-state contract, and TASK-0014's own rule that a failed read never renders as a figure or a verdict): when the read of `resolution_runs` refuses or the object is absent, the page holds NO window, so it must not publish a verdict about whether the asked-for cycle is in one.

Found: `/cycles?cycle=<run_id>` renders the definite negative in both failure states — 'Cycle <id> is not among the 200 newest cycles, so it is not in this window - it ran earlier, or no cycle carries that id.' On the not-provisioned path that sentence sits directly ABOVE a NotProvisioned card saying `resolution_runs` is missing entirely, so the page contradicts itself on one screen.

Why it matters: the Dashboard links every cycle line to `/cycles?cycle=<run_id>` (`lineHref`, src/app/page.tsx) and an operator follows that link exactly when something is wrong. During an outage the page tells them the cycle does not exist, sending them after a phantom data problem instead of the missing/refused table the same screen already names.

Location: src/app/cycles/page.tsx:669-670 — `const rows = cycles.kind === \"ok\" ? cycles.data.rows : []` then `const found = rows.some(...)`, so a non-ok read collapses to `found === false`; :690 renders `<AskedCycle askedFor={askedFor} found={found} />` on the sole condition `askedFor !== undefined`, and AskedCycle's else branch (:366-370) states the absence as fact. The three states are not distinguished: 'read the window, it is not there' and 'never read a window' render identically.

Repro (offline, no database):
1. cd '/Users/ben-m4/Desktop/Coding/KPOP/kspace Admin'
2. Render the page with the cycles read failing and a cycle facet set — the two pins in tests/offline/cycles/page.test.ts, describe 'the ?cycle= link against a window the page could not read', do exactly this: they script `resolution_runs` to `tableNotInSchemaCache` (case 1) and to `permissionDenied` (case 2) and pass `{ cycle: SUCCEEDED.run_id }`.
3. Observed: `[data-cycle-found=\"false\"]` is present (length 1) in both cases, i.e. the page published the negative verdict. Measured 2026-09-02 on run/admin-window @ c102991: both assertions failed with 'expected 1 to be +0'.

Fix shape (builder's call): render nothing, or a line that says the window could not be read, when `cycles.kind !== \"ok\"`. The affirmative branch (`found === true`) is only reachable from an ok read and is correct as it stands.

The two pins are committed as `it.fails(...)` (vitest's strict-xfail: it goes RED the moment the page stops publishing the verdict). Flip both back to `it(...)` as part of the fix — leaving them as `it.fails` will redden the suite once the page is correct.

## Acceptance criteria
(to be written)

## Checks
```sh
./node_modules/.bin/vitest run tests/offline/cycles
./node_modules/.bin/tsc --noEmit
```

## History
- 2026-09-02T15:29:54Z [qa] opened: Cycles: a ?cycle= link asserts the cycle is absent when resolution_runs was never read
- 2026-09-02T15:36:31Z [builder-46] claimed (attempt 1)
- 2026-09-02T15:41:39Z [builder-46] claimed → built: - Fix: `AskedCycle` now takes a three-state prop (`found` / `absent` / `unchecked`) instead of a boolean, and the page computes it ONLY off `cycles.kind`: any non-ok read yields `unchecked`, so the negative verdict is unreachable from a read that returned no window. The unchecked arm renders no `data-cycle-found` at all; it carries `data-cycle-unchecked=<object>` (`missing` for not_provisioned, `reading` for error, via a new `readingOf(DbUnavailable)`) and says, observed verbatim: 'Whether cycle <id> is in this window is not something this page can say: the read of resolution_runs returned no window to look in. What is below says why.' The affirmative and absent arms are untouched and still reachable from an ok read.
- Sources seam (relayed on TASK-0016): `?source=<name>` now earns one sentence — 'The source facet <name> narrows the adapter framework's runs, which this page does not render yet. The resolver's cycles below carry no source, so they are the same cycles with or without it.' No filter over `resolution_runs` was invented; rendered cycle ids are byte-identical with and without the facet (asserted). `?run=` is left silent and unremarked (same adapter half, not in this ticket's brief) — say the word and it gets the same sentence.
- Criteria met on the REBASED tree (ee3ce81 onto run/admin-window): `vitest run tests/offline/cycles` 47/47 (was 41 + the 2 pins); `tsc --noEmit` 0; `npm run lint` 0; `npm run build` 0 with /cycles still ƒ; full `npm test` 46 files / 1156 pass. Both QA pins flipped `it.fails` -> `it(...)` with their assertions unchanged.
- Red-then-green, watched: the 2 pins failed as strict-xfail once the page was fixed, then passed as `it(...)`; my 4 new tests were watched RED against the pre-fix behaviour (guard forced false: 1 fail; AskedSource line removed: 2 fails) and green after.
- For QA: the not-provisioned path renders TWO not-provisioned cards (resolution_runs for the table and for the cycle-health gauge) — the contradiction is gone because the asked-cycle line above them no longer publishes a verdict, not because a card moved. `data-cycle-unchecked` is the new structural hook. Not walked in a browser: .env has no service-role key (STACK §5); the surface was observed through the page's own server render.
- 2026-09-02T15:42:02Z [dispatcher] landed ticket/BUG-0023 onto run/admin-window as 857ede3
- 2026-09-02T15:42:50Z [qa] built → qa: under attack
- 2026-09-02T15:49:01Z [qa] qa → done: attacked on the landed tree (run/admin-window @ 7391da6, fix ee3ce81). Both stored checks re-run VERBATIM and green before reading the builder's story: vitest tests/offline/cycles 47/47, tsc 0.

THE BOUNCE'S CAUSE IS DEAD, measured on all three shapes of a read that returns no window — the two filed, plus one the builder did not use:
- not_provisioned (tableNotInSchemaCache resolution_runs) + ?cycle=<id>: data-cycle-found nodes = 0; the line carries data-cycle-unchecked="resolution_runs" and names the id.
- error / permissionDenied + ?cycle=<id>: same, found nodes = 0, unchecked="resolution_runs".
- error / transportFailure (a fetch that never reached PostgREST — MY case, the builder pinned only the two PostgREST ones): also unchecked="resolution_runs", found nodes = 0, no row aria-current. The object's own spelling reaches the line; the transport's ~900-char account stays in the error card below, which is where it belongs. Now pinned.
The verdict is structurally unreachable from a non-ok read: AskedCycleState is computed off cycles.kind alone (src/app/cycles/page.tsx:753-758), not off rows.some over an emptied array, so the collapse at the old :669-670 cannot recur without a compile-visible change.

THE VERDICT STILL WORKS WHERE IT IS EARNED (the fix removes it from two states and no others):
- ok read holding the id: found=true, exactly one row aria-current, the line's href is the row's own anchor.
- ok read not holding it: found=false, no row marked.
- ok read of an EMPTY table + ?cycle=<id>: found=false — correct, an empty read IS a window (the page looked); no unchecked line, no not-provisioned card, the empty state renders. Pinned.
- TRUNCATED window (200 rows, cap filled) + an id beyond it: found=false renders, and data-window-truncated="true" stays on the same screen; the sentence scopes itself to the window and names both readings ('not among the 200 newest ... it ran earlier, or no cycle carries that id') rather than claiming the cycle does not exist. Judged honest: the claim never exceeds the evidence. Pinned that the truncation statement co-renders — a full window is the one case where 'not here' and 'does not exist' come apart, and dropping it because a facet was asked for would recreate BUG-0023 in the ok arm.

SEAMS: ?source=<name> earns one sentence, no filter invented over resolution_runs (rendered cycle ids byte-identical with and without it — the builder's assertion re-run); ?source= + a refused read co-render without interfering. ?cycle=<a>&cycle=<b> takes the first (platform semantics). ?run=<id> is silent: no crash, markup identical to no facet — acceptable per this ticket's brief; the follow-up is the dispatcher's call.
TRUST BOUNDARY (public-surface): both facets are URL-controlled strings rendered verbatim in text; ?cycle=<script>alert(1)</script> and ?source="><img src=x onerror=alert(1)> emit no <script> and no <img>, cheerio finds zero of each element, and both are still legible as text. Pinned.

+4 tests into tests/offline/cycles/page.test.ts (the file that owns this surface; no new file, no ticket id in a filename), 47 -> 51, committed at 7391da6. Each was watched RED first via a flipped assertion (truncation flag, asked id, escaped text, unchecked object) — 4 failed, 4 reverted, 51 green. Receipt green on this exact tree.

RESIDUAL, not filed (lean cap, and it is TASK-0014's facet handling rather than this fix): an EMPTY facet value is treated as a present facet. /cycles?cycle= renders the definite negative about a nameless cycle (data-cycle-found="false", data-cycle-asked=""), and /cycles?source= renders the source sentence with a blank name. firstValue (:151) returns "" for an empty parameter, while sources/page.tsx and lib/claims/filters.ts both apply the app's stated rule that a value the page cannot offer narrows NOTHING. No link the app emits can produce it — lineHref always encodes a non-empty run_id — so it is hand-URL-only and P3. Trigger that would make it a ticket: any generated link whose id can be empty, or a decision that '' should be no facet. TASK-0014 is in a lane now; a one-line firstValue change would land it at zero extra spawn cost. [resolution: fixed] [receipt green: 2 cmd(s), tree 30e4deb6]
