---
id: TASK-0047
type: task
status: done
milestone: M2
priority: P1
opened_by: architect
parent: FEAT-0010
children: []
assignee: null
claimed_at: null
depends_on: []
touch_scope: [src/lib/db/result.ts, tests/offline/db/result.test.ts, tests/offline/absence/pages.test.ts, tests/offline/absence/surfaces.ts, tests/live/harness.live.test.ts]
force: false
attempts: 1
resolution: fixed
---
# not_provisioned learns the absent FUNCTION: PGRST202 and 42883 join the classifier

## Description
**Module**: `src/lib/db/result.ts` — the ONE helper that decides `not_provisioned` from an error code (ARCHITECTURE §4.1). Today it knows four codes, all of them about a table, a view or a column: `PGRST205`, `PGRST204`, `42P01`, `42703`.

**What changes.** M2 reads and calls an object of a fifth kind — a FUNCTION — and the absence of a function has its own codes: PostgREST answers a call to a function that is not in its schema cache with **`PGRST202`**, and Postgres raises **`42883` undefined_function**. Both join the same one helper, and `missing` carries the function's name exactly as `lib/db/tables.ts` would spell it, so the rendered card says which object is absent and nothing throws.

**Why it is its own ticket, first**: it is a shared surface every M2 write path depends on (ARCHITECTURE §13.2's stated exception), and three tickets would otherwise each add a code to the same file. One owner (§10, "one owner per structural guard").

**Scope discipline.** This ticket adds two codes and their tests. It does NOT add the call seam, a page, or a control — those are the next tickets. It does not widen `kind: "error"`'s meaning: every code that is not one of the six is still an error carrying the database's own words, and a null count is still a refusal (§4.3).

**The absence is the normal case and is graded FIRST.** `verdicts` and `settle_review_item` do not exist on staging and will not until Ben installs the handoffs — that is the database `main` deploys against for the whole of M2. Build and grade the not-provisioned path before the happy path.

**No Admin-side workaround for the absent function may be written** — spec §10's one forbidden move, this campaign's brightest line: no queued write, no "pending overrides" table, no retry buffer, no flag-guarded direct write, no second path "until Ben installs it". The surface degrades to what M1 already ships.

**The live probe is free and permanent**: `settle_review_item` genuinely does not exist on staging, so `tests/live/harness.live.test.ts` can assert the classification against a real absent function without inventing one — the same trick §4.1 already records for the absent `verdicts` table.

## Acceptance criteria
1. The classifier in `src/lib/db/result.ts` returns `kind: "not_provisioned"` for `PGRST202` and for `42883`, alongside the four codes it already knows, and `missing` carries the object name the caller passed.
2. Offline: one fixture per code — six not-provisioned codes each classify as not-provisioned, and at least two neighbouring codes it must NOT flag (e.g. `57014` statement timeout, `23502` not-null violation) still classify as `error` carrying the database's own message. The guard proves itself on both kinds of input (LESSONS 3).
3. A null count is still a refusal and not a zero; nothing in this change makes an absent object render a confident number (§4.3, common violation 2).
4. `tests/live/harness.live.test.ts` asserts the classification of a real absent function on staging by calling `settle_review_item` — a call that writes nothing because the function does not exist — and asserts the result is `not_provisioned` naming it, never `error`.
5. `tests/offline/absence/pages.test.ts` still grades every existing surface exactly as it did; no page's behaviour changes in this ticket.
6. `npm run lint`, `tsc --noEmit`, the full offline suite (shared-surface exception, ARCHITECTURE §13.2) and the harness live suite are green.

## Checks
```sh
bash -c 'rm -rf .next/types && npm run lint && ./node_modules/.bin/tsc --noEmit'
npm test
npm run test:live -- tests/live/harness.live.test.ts
```

## History
- 2026-09-09T01:17:09Z [architect] opened: not_provisioned learns the absent FUNCTION: PGRST202 and 42883 join the classifier
- 2026-09-09T01:22:28Z [builder-122] claimed (attempt 1)
- 2026-09-09T01:37:32Z [builder-122] claimed → built: - classify() in src/lib/db/result.ts now reads PGRST202 and 42883 as not_provisioned carrying the caller's name; the four table/column codes are untouched. ONE deliberate exception, evidence-based: Postgres raises 42883 for a missing OPERATOR too, so a 42883 whose message says 'operator does not exist' stays kind:error (measured read-only on staging 2026-09-08: ilike on groups.created_at -> 42883 'operator does not exist: timestamp with time zone ~~* unknown' — the shape that bit BUG-0058). A 42883 with no message at all is still an absence. If the architect wants 42883 flat, it is one line.
- Measured on ubfjjqlvnpnoborczbdb.supabase.co, read-only, nothing written: rpc('settle_review_item',{p_decision}) -> PGRST202 'Could not find the function public.settle_review_item(p_decision) in the schema cache'; verdicts -> PGRST205; PostgREST's OpenAPI doc lists 54 /rpc paths, apply_resolution present, settle_review_item ABSENT. The new live case in tests/live/harness.live.test.ts establishes that absence from the schema description BEFORE it calls anything, so the day Ben installs the artifact it fails naming the ticket instead of calling a settlement function. Red-then-green seen on both tiers: with HEAD's classifier the live case failed 'expected error to equal not_provisioned' and 8 offline cases failed.
- Verified on the REBASED tree (onto run/admin-window @7031bed): npm run lint, tsc --noEmit, npm test (59 files, 2091 + 6 isolated), npm run test:live -- tests/live/harness.live.test.ts (5 passed). absence/pages.test.ts unchanged at 266 passing (criterion 5).
TRAPS FOR QA: (1) two files outside touch_scope changed, both additive — tests/fixtures/stub-client.ts gains .rpc(name,args) on the shared stub (script keyed by function name; tablesRead() excludes rpc calls, new functionsCalled() lists them) and four error shapes (functionNotInSchemaCache/undefinedFunction/missingOperator/notNullViolation); they belong there by ARCH §10 ('shared fixture builders live in tests/fixtures/') and the M2 call-seam ticket needs them. (2) One existing case in tests/offline/db/result.test.ts ('carries message, details, hint and code, in that order') used a 42883 fixture; its premise is superseded by this ticket, so its code is now 42501 — the ordering assertion is unchanged, nothing was weakened. (3) PostgREST answers PGRST202 for an EXISTING function called with wrong argument NAMES, indistinguishable by code — whoever builds the call seam must spell p_decision exactly or a provisioned function renders as absent. Documented on the fixture. No product code spells settle_review_item (the write-surface guard still passes).
- 2026-09-09T01:38:01Z [dispatcher] landed ticket/TASK-0047 onto run/admin-window as a4900e6
- 2026-09-09T01:39:01Z [qa] built → qa: under attack
- 2026-09-09T01:56:31Z [qa] qa → done: ATTACKED, all 6 criteria re-run on the landed tree (merge a4900e6) BEFORE reading the builder's handoff. Criteria: (1) classify() returns not_provisioned naming the caller's object for PGRST202 and 42883 — verified; (2) six absence codes + three neighbours (57014, 23502, 42883-operator) — verified, and I swept ALL 15 exported error shapes in tests/fixtures/stub-client.ts through classify(): no pre-existing shape flipped verdict; (3) null count still a refusal, never a 0 — readCount/readComplete unchanged, asserted over an rpc; (4) live probe green from a DETACHED worktree under the gitignored evidence dir (.env and node_modules symlinked, never opened; nothing written); (5) tests/offline/absence/pages.test.ts byte-identical (git diff empty) and 266/266 — unchanged; (6) lint+tsc, npm test (2120+6), test:live all green.

SABOTAGE (criterion 4's real question — does the OpenAPI absence case fail loudly once settle_review_item is installed?): retargeted SETTLE_FUNCTION to apply_resolution (which IS installed) and replaced both call sites with a throwing sentinel. Both cases went RED naming the ticket — 'apply_resolution is now installed on ubfjjqlvnpnoborczbdb.supabase.co ... must be retargeted rather than left to call an installed settlement function' — and the sentinel NEVER fired, proving the absence assertion aborts before any invocation. apply_resolution was not called. Read-only throughout.

HOSTILE SHAPES against the new arm: 42883 with no message (not_provisioned, correct — code classifies); numeric code 42883 (not_provisioned); null/absent code (error, correct); operator sentence uppercase, as an Error instance, in details-only (all error); 42P01 unchanged; empty missing name; non-string message. No real-database-reachable defect in the function arm.

BUILDER'S JUDGEMENT (keep the operator sentence red): ENDORSED. not_provisioned means 'the object you asked for is absent'; a 42883 from ilike-on-timestamptz is a query this app got wrong against a table holding rows, and BUG-0058 is the precedent. It cannot produce a false ERROR either — Postgres spells a missing function 'function X(...) does not exist', never 'operator does not exist'.

BUT THE EXCEPTION IS ONE SENTENCE SHORT → BUG-0080 (P2, child of FEAT-0010, pinned strict it.fails, committed 11dc5aa). Measured read-only on staging: from('groups').filter('created_at','fts','x') -> 42883 'function to_tsvector(timestamp with time zone) does not exist' -> classify(...,'groups') = not_provisioned. A provisioned table rendered as absent — the exact harm the exception exists to prevent, reached by the sentence it does not match. Before a4900e6 that shape was kind:error. Watched red as a plain it() first; strictness proved by XPASS ('Expect test to fail').

WHY DONE, NOT REOPENED: every criterion is met and the goal (M2's absent FUNCTION renders, nothing throws) is achieved; the builder's exception is BETTER than criterion 1's flat reading, not worse. The residue is unreachable from product code at this commit — no .ilike/.filter-fts/.textSearch/.rpc exists under src/ (config.test.ts bans .rpc( outright) — so zero user impact today, and the fix is an architect-level discriminator (a table read can never be a function absence) touching classify()'s signature, which is outside this ticket's stated scope. Bouncing a P1 shared-surface ticket onto a P2 redesign would block FEAT-0010's dependents for no user gain.

RESIDUALS (no tickets): (a) the operator exception is applied to PGRST202 as well as 42883 — unreachable, PostgREST's template can't carry that sentence; (b) PGRST202 for an EXISTING function called with wrong ARGUMENT NAMES is an indistinguishable false absence (builder documented it on the fixture) — the call seam must spell p_decision exactly; (c) ARCHITECTURE.md §4.1 still enumerates four codes and records no message-shaped exception, so the classifier's contract is now doc-drifted — architect's; (d) stub.tablesRead() now EXCLUDES rpc calls, so tests/offline/absence/surfaces.ts will not enumerate a function when the call seam lands; (e) the handoff says 'two files outside touch_scope' but only one is (tests/fixtures/stub-client.ts). NOTE: npm test exited 1 twice in the primary checkout at load average 261 with all 2120+6 tests passing — a vitest worker 'Timeout calling onTaskUpdate' in the isolated project, purely machine load; GREEN in the receipt's own worktree. [resolution: fixed] [receipt green: 3 cmd(s), tree ec0ba35d]
