---
id: TASK-0084
type: task
status: blocked
milestone: patch
priority: P2
opened_by: strategist
parent: null
children: []
assignee: null
claimed_at: null
depends_on: []
touch_scope: [tests/live/settle.live.test.ts, tests/live/sweep.ts, tests/live/staging-target.ts]
force: false
---
# HOLD until Ben answers — acceptance test 6: every §7 action proven end to end on staging

## Description
VISION defers acceptance tests 6, 8 and test 7's override half to 'a patch run after Ben installs them — it is the one acceptance item deliberately deferred'. Ben installed both §9 objects on staging on 2026-09-11 (verdicts 23:22Z, settle_review_item 23:26Z, measured 200-vs-404-PGRST205 and present in the PostgREST OpenAPI description), so the precondition named in VISION and in ROADMAP has cleared and this is the campaign's last ungraded acceptance item.

Test 6, verbatim from contracts/admin-build.md: 'Every §7 action, end to end on staging: choose / supply / keep-current / link / settle-only on decision items; fixed and wont_fix on a signal (a wont_fix without a note is refused). Each settlement is one transaction — its apply and rejection stamps share a timestamp, and a killed call leaves no partial write. Grant introspection (recorded as evidence) proves verdicts is written and review_items.status set by settle_review_item alone.'

BLOCKED, and only Ben can unblock it. A live §7 proof is NOT sweepable by this app: a settlement consumes a real review_items row (status set, irreversibly from Admin) and appends a verdicts row the service role cannot delete. That is the operation working correctly, not residue — but it spends staging data, and 71 of staging's 72 review items belong to the sibling entity-linking campaign's live work (commit 43505768). BUG-0215 is what happens when a live case writes through this path without that answer. Do not start this ticket until the human answer recorded in tracker/for-human/M3-retro-for-ben.md says which rows are spendable and how the after-state is restored.

The world this must read rather than assume: BUG-0215 made the live tier READ which settlement world staging is in (readSettlementReadiness, src/lib/db/verdict.ts, now the conjunction of table + readFunctionInstalled after BUG-0223). This test follows that rule — it reads the world, and skips with a named reason in the absent or half-installed world rather than failing or writing.

## Acceptance criteria
1. A new tests/live/settle.live.test.ts drives every §7 action listed in test 6 against staging through the app's own route, never through a hand-written SQL call: choose a claimed value, supply a different value, keep current & settle on a data_conflict decision item; link to an existing entity and settle-only on an entity_link fact item; fixed and wont_fix on a signal item.
2. A wont_fix without its required note is REFUSED, and the refusal names the object — asserted on the response, and asserted to have written no verdicts row and left review_items.status unchanged (LESSONS 8: the shape that must refuse and the shape that must not, both driven).
3. One transaction per settlement: the apply stamp and the rejection stamp of a single settlement share one timestamp, read back from the database; and a call killed mid-flight leaves no partial write — neither a verdicts row without its status change nor the reverse.
4. Grant introspection is recorded as evidence (the acceptance doc requires the evidence, not a claim): the service role's privileges on verdicts and review_items are read from the live catalog and show that verdicts is written and review_items.status is set by settle_review_item alone, with no direct write path from Admin.
5. The test READS the settlement world before it acts (readSettlementReadiness) and, in the absent or half-installed world, skips with the reason named — never fails, never writes. Graded on a fixture offline for both of those worlds, so the skip arm is not itself unproven.
6. Every row this test touches is named in its own evidence note with its before-state, so the after-state is restorable by the human who owns those rows. Nothing outside the rows named in the human answer is touched.

## Checks
```sh
./node_modules/.bin/tsc --noEmit
npm run lint
npm run test:live -- tests/live/settle.live.test.ts
npm run test:live -- tests/live/residue.live.test.ts
```

## History
- 2026-09-15T07:54:22Z [strategist] opened: Acceptance test 6: every §7 action proven end to end on staging, now that settle_review_item is installed
- 2026-09-15T07:55:45Z [strategist] title amended: Strategist cannot set 'blocked' (role table); the hold is carried in the title until the dispatcher or architect transitions it. Nothing here may start before Ben's answer in tracker/for-human/M3-retro-for-ben.md. [was: Acceptance test 6: every §7 action proven end to end on staging, now that settle_review_item is installed]
- 2026-09-15T08:00:25Z [dispatcher] open → blocked: held for Ben: a live §7 acceptance proof consumes a real review_items row and appends a verdicts row this app cannot delete, on a staging project the entity-linking campaign is writing; starts only on Ben's word (strategist retro, 2026-09-15)
