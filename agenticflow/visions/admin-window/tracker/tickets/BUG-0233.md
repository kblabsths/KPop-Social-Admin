---
id: BUG-0233
type: bug
status: open
milestone: M3
priority: P3
opened_by: qa
parent: null
discovered_from: BUG-0232
children: []
assignee: null
claimed_at: null
depends_on: []
touch_scope: [src/app/queues/page.tsx, tests/offline/review/review-items.test.ts, tests/offline/queues/page.test.ts, tests/offline/claims/read.test.ts, tests/offline/live-guard.test.ts, tests/offline/claims/population.ts, tests/offline/claims/page.test.ts]
force: false
---
# Ten more comments in src/ and tests/ describe the app's count legs as HEAD counts; every one of those reads is countRead

## Description
BUG-0232 closed three `head: true` comments in src/ and two test-stub lines. Its sweep grepped the LITERAL `head: true`, so it could not see the same claim spelled in prose ("HEAD count"). Sweeping both spellings over src/ and tests/ at 13e188f8 finds ten more sentences that describe reads THIS APP (or `lib/db`) ISSUES TODAY as HEAD counts. Every one of those reads is `countRead` — `db.from(o).select("*", { count: "exact" }).limit(0)`, src/lib/db/result.ts:1430 — through `readCount` (:1457). No `head: true` query exists in src/ at all.

This is the FOURTH ticket of one class (DEBT-0022, BUG-0231, BUG-0232). File it once, sweep it once, end it — do not fix a subset.

STALE — Expected: the read named as the code spells it (`countRead`, `{ count: "exact" }` over `limit 0`, the vocabulary src/app/claims/page.tsx:681-682 already uses). Found:

1. src/app/queues/page.tsx:704-705 — "when the URL carries a facet, one HEAD count per shape OF EACH KIND THE URL NARROWS". The page's per-shape counts are `readReviewQueues` -> `countQuery` -> `countRead(client, T.reviewItems)` (src/lib/db/review-items.ts:215, consumed at :273). Present tense, src/, names the retired mechanism as the shipped one — the same defect BUG-0232 fixed three files over.
2. tests/offline/review/review-items.test.ts:87 — "then one HEAD count per counted shape, in `SHAPES` order", 24 lines above the line BUG-0232 rewrote (:111-112, now "A `countRead` — `{ count: "exact" }` over `limit 0`"). One doc block now describes the same legs both ways.
3. tests/offline/queues/page.test.ts:174 — "then one HEAD count per COUNTED shape", 35 lines above BUG-0232's rewrite at :209-210. Same contradiction.
4. tests/offline/queues/page.test.ts:1524 — "every per-shape HEAD count refuses".
5. tests/offline/queues/page.test.ts:2186 — "URL adds one HEAD count per shape".
6. tests/offline/claims/read.test.ts:383 — "Exactly what a select written without `{ head: true, count: "exact" }` comes back with", on the test that pins the no-count refusal arm. Stale twice: it is the FALSE PREMISE admin-window/BUG-0224 removed (a HOST answering 404 with zero bytes reaches that arm too — src/lib/db/result.ts:1447-1456), and it spells the option `head: true`.
7. tests/offline/claims/read.test.ts:35 — "a set of head COUNTS (ARCHITECTURE.md §4.3)" describing the module under test.
8. tests/offline/live-guard.test.ts:1194 — "The app's own read path is not covered: `lib/db` counts with `head: true`". Flatly false today (result.ts:1399 "never `head: true`"), and it states the SCOPE of the guard asserted at :1190, so a reader mis-scopes the guard.
9. tests/offline/claims/population.ts:262 — "one window, six `head: true` counts and five `limit 1` seeks" describing what the Claims page issues.
10. tests/offline/claims/page.test.ts:2974 ("Every head:true count of the view a render issued") and :3736 ("every `head:true` count the render issued must carry").

MUST SURVIVE UNTOUCHED — these are CODE, or prose explaining why the app does NOT use head: true, and are true: src/lib/db/result.ts:1399/1401/1405 and :1004 (past tense, BUG-0210); src/lib/db/verdict.ts:251/256; src/lib/db/claims.ts:354; src/lib/db/review-items.ts:207; tests/live/parity.ts:28/605/614; tests/live/harness.live.test.ts:50-54; tests/live/claims.live.test.ts:374/378/1459; tests/live/edit.live.test.ts:834; tests/http/absence.http.test.ts:25/29/58; tests/fixtures/stub-client.ts:48/507/521; tests/offline/verdict/seam.test.ts:166-169; tests/offline/db/result.test.ts (its `head: true` lines are query CODE, deliberately exercising the shape); tests/offline/live-guard.test.ts:504/523/530 (CODE); tests/offline/claims/population.ts:280 (documents the FIXTURE's own emulation of PostgREST, implemented at :315 and :370 — it is about the stub, not about what the app issues); ARCHITECTURE.md §6 trap 12.

A blanket ban on the literal is the WRONG fix and is refused here (LESSONS 4): three of the files above hold `head: true` in CODE. No lint rule, no prose-grading test — grading prose with machinery is banned.

REPRO (static, no server), from the repo root:
  grep -rni 'head count\|head: true\|head:true' src tests
  sed -n '703,707p' src/app/queues/page.tsx
  sed -n '213,216p;271,275p' src/lib/db/review-items.ts
  sed -n '1428,1432p' src/lib/db/result.ts
  sed -n '85,89p' tests/offline/review/review-items.test.ts
  sed -n '172,176p;1522,1526p;2184,2188p' tests/offline/queues/page.test.ts
  sed -n '382,385p' tests/offline/claims/read.test.ts
  sed -n '1190,1196p' tests/offline/live-guard.test.ts
  sed -n '260,264p' tests/offline/claims/population.ts

IMPACT: prose only, no behaviour change, no user-visible defect, P3. Filed because the class has cost three lanes by being fixed a file at a time, and two of the ten sites now CONTRADICT, in the same doc block, the lines BUG-0232 rewrote three and thirty-five lines away. Ten sentences, one sweep. No new test.

## Acceptance criteria
1. THE BAR, positively: every comment in src/ and tests/ that describes a count read THIS APP or `lib/db` ISSUES names it as the code spells it — `countRead`, `{ count: "exact" }` over `limit 0` — in the vocabulary src/app/claims/page.tsx:681-682 uses. Concretely, all ten sites listed in the description say that, and after this ticket `grep -rni 'head count|head: true|head:true' src tests` returns only lines that are (a) query CODE, or (b) prose explaining why the app does NOT use head: true.
2. tests/offline/claims/read.test.ts:383 states the no-count arm as src/lib/db/result.ts:1447-1456 does — an answer this app cannot grade, whatever produced it (`unreadableAnswer`) — not "what a select written without the option comes back with". The test's assertions are unchanged.
3. tests/offline/live-guard.test.ts:1194 states the guard's real scope: it covers the count queries LIVE TESTS write, and the app's own count path is `countRead`, never head-shaped. The guard's assertions and its `tests/live` scan are unchanged.
4. Every site in the description's MUST SURVIVE list is untouched — proved by `git diff --name-only <merge-base>..HEAD`. No repo-wide ban on the literal, no lint rule, no test that greps prose.
5. Prose only: zero non-comment lines changed. Mechanical proof, per touched file: `./node_modules/.bin/esbuild --loader=ts|tsx --minify` output is byte-identical between the merge-base version and HEAD's. `./node_modules/.bin/tsc --noEmit` exits 0, `npm run lint` exits 0, and `./node_modules/.bin/vitest run --project=offline tests/offline/queues/page.test.ts tests/offline/review/review-items.test.ts tests/offline/claims/read.test.ts tests/offline/claims/page.test.ts tests/offline/live-guard.test.ts` is green (one project at a time — this machine is low on memory).

## Checks
```sh
grep -q 'countRead' src/app/queues/page.tsx
grep -q 'countRead' tests/offline/claims/population.ts
grep -q 'countRead' tests/offline/claims/read.test.ts
grep -q 'countRead' tests/offline/live-guard.test.ts
./node_modules/.bin/tsc --noEmit
npm run lint
```

## History
- 2026-09-15T07:09:07Z [qa] opened: Ten more comments in src/ and tests/ describe the app's count legs as HEAD counts; every one of those reads is countRead
