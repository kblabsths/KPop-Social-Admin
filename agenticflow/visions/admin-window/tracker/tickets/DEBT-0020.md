---
id: DEBT-0020
type: debt
status: reopened
milestone: patch
priority: P3
opened_by: architect
parent: null
discovered_from: BUG-0173
children: []
assignee: null
claimed_at: null
depends_on: [BUG-0179, BUG-0182, BUG-0187]
touch_scope: [src/lib/db/result.ts, tests/offline/db/result.test.ts]
force: false
siblings: [BUG-0179]
attempts: 1
---
# The transport account states its cause sentence twice: postgrest-js appends cause.stack, whose head repeats the Caused-by line

## Description
**Module:** `src/lib/db/result.ts` — `errorMessage`'s part-level deduplication, which is a dedup BETWEEN parts and not WITHIN one.

**Found** (QA, at the close of admin-window/BUG-0173, residual (c); ruled cosmetic by the architect 2026-09-11): the transport account states its cause sentence twice. postgrest-js builds `details` for a fetch failure as the wrapper line, then a blank line, then `Caused by: <name>: <cause.message>`, and then — when the cause carries one — the cause's whole `stack`, whose FIRST line repeats `<name>: <cause.message>` verbatim (`node_modules/@supabase/postgrest-js/dist/index.mjs`, the `errorDetails += "\n" + cause.stack` arm). The frame lines go, both non-frame lines stay, and the account reads

    TypeError: fetch failed Caused by: Error: getaddrinfo ENOTFOUND db.invalid Error: getaddrinfo ENOTFOUND db.invalid (1 runtime stack frame dropped)

The existing dedup cannot catch it: `errorMessage` drops a PART another part already contains (which is how "says the wrapper once, not twice" stays green), and both copies here live inside ONE part, the reduced `details`.

**Why this is deferred and not folded into admin-window/BUG-0179** (architect ruling, 2026-09-11): the harm is one repeated ~40-character sentence the database itself authored — not an unbounded foreign payload, not a credential, nothing an intermediary controls. The fix is a line-level pass inside a part, and BUG-0179 is the ticket that makes the account's ONE derivation even; adding a fourth pass over prose in the same session is how the account grows the machinery LESSONS 4 exists to refuse. This lands afterwards, on the settled tree, or not at all.

**The trap the fix must avoid:** dropping any kept line that another kept line "contains" would also delete a database's legitimately repeated or prefix-shared line. Whatever rule lands, a part whose two lines merely SHARE A PREFIX keeps both.

**What this adds that its sibling admin-window/BUG-0179 does not:** BUG-0179 makes the account's derivation EVEN — the same three questions asked of the code, of a blank account and of every line of a multi-line part. This ticket adds nothing to that derivation and asks no question at all: it is a repetition the DATABASE's own words arrive in, deduplicated inside one part after the questions have been asked. If the two are ever worked together, this half lands last and its prefix-sharing twin is what keeps the dedup honest.

## Acceptance criteria
ONE SENTENCE, ONCE. On the measured transport shape — the wrapper `message`, and a `details` carrying the wrapper line, a `Caused by: Error: getaddrinfo ENOTFOUND db.invalid` line, that same sentence again as the head of `cause.stack`, and at least one V8 frame — the account states the cause sentence exactly once and still contains it, still names the object that was read, still states the frame count in the app's own words, and still carries no "node:dns:", no "GetAddrInfoReqWrap" and no absolute path. Measure the account on the pre-ticket tree and record the exact string in the ticket before changing a line: the builder fixes what it measured, not what this description reports.

NOTHING ELSE MOVES. The dedup added is line-level INSIDE one part and nothing else about the derivation changes: the three questions stay three, in their order and their text, no length cap and no vocabulary is introduced, and `withoutSecrets` still runs last. The ten-fixture table of tests/offline/db/result.test.ts keeps its five-and-five shape with every carries/never assertion unchanged; it("says the wrapper once, not twice, when details repeats it"), it("carries the cause out of details when the message is only a wrapper") and BUG-0170's and BUG-0179's pins are green with no case rewritten.

THE TWIN THAT FORBIDS THE GREEDY RULE (LESSONS 8). A named case carries both directions: the repeated cause sentence, said once — and a part whose two lines SHARE A PREFIX but are not the same line (a database message wrapped onto a second line that starts with the first line's opening words), where BOTH lines still cross whole. A rule that deletes a line another line merely begins with fails the twin.

it("says a repeated cause sentence once, and a prefix-sharing line twice") exists in tests/offline/db/result.test.ts as a plain `it`.

## Checks
```sh
./node_modules/.bin/vitest run --project=offline tests/offline/db/result.test.ts
grep -qF 'it("says a repeated cause sentence once' tests/offline/db/result.test.ts
npm run lint
```

## History
- 2026-09-11T07:35:48Z [architect] opened: The transport account states its cause sentence twice: postgrest-js appends cause.stack, whose head repeats the Caused-by line
- 2026-09-11T07:35:58Z [architect] description amended: Sibling line added per the birth warning: what this adds that BUG-0179 does not. [was: **Module:** `src/lib/db/result.ts` — `errorMessage`'s part-level deduplication, which is a dedup BETWEEN parts and not W… (1902 chars; full text in git history)]
- 2026-09-11T09:22:00Z [architect] depends_on: [BUG-0179] → [BUG-0179, BUG-0182]: Third and last on the result.ts lane (architect ruling 2026-09-11, 13.9): BUG-0181 -> BUG-0182 -> DEBT-0020. This ticket's own description already says it lands 'afterwards, on the settled tree', and BUG-0182 is the ticket that settles the account's line-level shape - it groups a run of document lines into one clause, and this dedup then runs over the lines that survive that. The order also matters the other way round: a line-level dedup landing first would be re-derived against a changed line set. BUG-0179 edge kept (satisfied). Milestone stays patch - nothing here blocks M3.
- 2026-09-11T09:44:14Z [architect] Lane order confirmed (architect, 2026-09-11): BUG-0185 -> BUG-0182 -> DEBT-0020 on src/lib/db/result.ts and tests/offline/db/result.test.ts. DEBT-0020 stays LAST and its existing depends_on [BUG-0179, BUG-0182] already delivers that (BUG-0185 is transitive through BUG-0182); no edge added here. It is the only cosmetic of the three — one true sentence stated twice — so it describes whatever the two truth fixes land, never the other way round.
- 2026-09-11T10:38:16Z [architect] depends_on: [BUG-0179, BUG-0182] → [BUG-0179, BUG-0182, BUG-0187]: Still LAST on the src/lib/db/result.ts lane (architect, 2026-09-11): BUG-0185 -> BUG-0182 -> BUG-0187 -> DEBT-0020, serial at their one destination. BUG-0187 is added ahead of it for the same reason BUG-0182 was: it rewrites askedLineByLine - deleting question 2 from it entirely, along with the PartLine offsets and the run bookkeeping - so a line-level dedup landing first would be re-derived against a line set that is about to change. DEBT-0020 is NOT made unnecessary by the new bar, checked rather than assumed: its shape is the transport account's details (the wrapper line, the Caused-by line, the same sentence again as the head of cause.stack, then V8 frames). No line of that part begins '<', so it carries no document, it is not counted whole, and its two identical lines still both cross - the repetition is untouched by BUG-0187. It also stays the only cosmetic of the four (one true sentence stated twice), so it describes whatever the truth fixes land, never the other way round. Milestone stays patch.
- 2026-09-11T15:15:37Z [builder-295] claimed (attempt 1)
- 2026-09-11T15:18:57Z [builder-295] MEASURED ON THE PRE-TICKET TREE (585e410b, before any line changed), via classify(..., T.events).message:

(1) The real postgrest-js transport shape (details = wrapper line, blank line, "Caused by: Error: getaddrinfo ENOTFOUND db.invalid", cause.stack head repeating that same sentence, one V8 frame):
"TypeError: fetch failed Caused by: Error: getaddrinfo ENOTFOUND db.invalid Error: getaddrinfo ENOTFOUND db.invalid (1 runtime stack frame dropped)" (151 chars)

(2) The suite-shared fixture transportFailure() (tests/fixtures/stub-client.ts, same shape with cause "bad port"):
"TypeError: fetch failed Caused by: Error: bad port Error: bad port (1 runtime stack frame dropped)"

Both state the cause sentence TWICE, exactly as the description reports. Note the two copies are NOT identical lines: postgrest-js writes "Caused by: <name>: <msg>" and then the cause stack head "<name>: <msg>", so the repeat is a SUFFIX of the earlier line, not an equal line and not a prefix of it. Source read: node_modules/@supabase/postgrest-js/dist/index.mjs lines 176-179 (errorDetails = name+message; += "\n\nCaused by: " + cause.name + ": " + causeMessage; += " (code)" if causeCode; += "\n" + cause.stack).
- 2026-09-11T15:24:04Z [builder-295] MEASURED AFTER THE FIX, same two inputs, same call (classify(...).message):

(1) postgrest-js transport shape: "TypeError: fetch failed Caused by: Error: getaddrinfo ENOTFOUND db.invalid (1 runtime stack frame dropped)" (106 chars, was 151)
(2) transportFailure(): "TypeError: fetch failed Caused by: Error: bad port (1 runtime stack frame dropped)"

The cause sentence is stated once, the attributing "Caused by:" copy is the one that survives, the object read is still named, the frame count is still in the app words, and no "node:dns:", "GetAddrInfoReqWrap" or absolute path crosses. Rule landed: saidOnce() drops a surviving line an EARLIER kept line ENDS WITH, run only over the lines that survive the frame drop (the one place this file reshapes a part). Ends-with, not contains: a line another line merely BEGINS with is a wrapped database message and both halves cross whole.
- 2026-09-11T15:25:07Z [builder-295] claimed → built: One commit, 465859b7, only the two touch_scope files.

- src/lib/db/result.ts: new saidOnce() drops a surviving line an EARLIER kept line ENDS WITH; called from askedLineByLine over the lines that survive the frame drop. Measured before/after are in this ticket History (151 -> 106 chars on the postgrest-js shape; the "Caused by:" copy is the one that survives).
- tests/offline/db/result.test.ts: one new plain it("says a repeated cause sentence once, and a prefix-sharing line twice") in the "an account carries the parts the database authored" describe. Watched RED on the pre-fix source (occurrences(sentence) === 2, not 1) before the fix went back in. No existing case rewritten; the ten-fixture table is untouched.
- Verified on the rebased tree (run head 585e410b): ./node_modules/.bin/vitest run --project=offline tests/offline/db/result.test.ts = 132 passed; the whole offline project = 80 files / 3909 passed; grep check exit 0; npm run lint exit 0; ./node_modules/.bin/tsc --noEmit exit 0.

TWO THINGS QA SHOULD KNOW. (1) The rule is ENDS-WITH, chosen over contains because the twin forbids deleting a line another line merely BEGINS with; it is ONE-DIRECTIONAL (a line is judged only against lines already kept before it) because the measured shape always puts the attributing line first. The twin case grades three pairs, both arrival orders included. (2) The dedup runs ONLY where a frame was dropped, because that is the one place this file reshapes a part at all - a frameless part still crosses verbatim, which BUG-0187s bar and the indentedProse pin require. So a synthetic part carrying a repeated line and NO frame still says it twice; that is deliberate, not an oversight. The suite-shared fixture transportFailure() (tests/fixtures/stub-client.ts, outside my scope, unmodified) already carried the duplicated shape, so its account changed too: now "TypeError: fetch failed Caused by: Error: bad port (1 runtime stack frame dropped)".
- 2026-09-11T15:25:23Z [dispatcher] landed ticket/DEBT-0020 onto run/admin-window as 3fd95435
- 2026-09-11T15:26:05Z [qa] built → qa: under attack
- 2026-09-11T15:31:42Z [qa] qa → reopened: admin-window/BUG-0199 filed. The criteria as written are green on the landed tree (stored checks re-run in the primary checkout at 5b553a2c: vitest --project=offline tests/offline/db/result.test.ts = 132 passed, grep exit 0, npm run lint exit 0, plus ./node_modules/.bin/tsc --noEmit exit 0), but the ticket's GOAL is missed on every transport failure a deployed instance produces.

Expected (criterion ONE SENTENCE, ONCE): the account states the cause sentence exactly once on the transport shape. Found: it states it TWICE whenever the fetch cause carries a `code` — which node's always do — at src/lib/db/result.ts:492-499 (`kept.some((held) => held.endsWith(line))`). postgrest-js writes the code INTO the attributing line between the Caused-by line and the stack head (`node_modules/@supabase/postgrest-js/dist/index.mjs`: `if (causeCode) errorDetails += " (" + causeCode + ")"` sits directly above the `errorDetails += "\n" + cause.stack` arm), so the kept line ends "… db.invalid (ENOTFOUND)" and no longer ENDS WITH "Error: getaddrinfo ENOTFOUND db.invalid". The ends-with test misses and both copies cross.

Driven through the REAL @supabase/supabase-js client over a transport that rejects the way node's fetch rejects (no network; host `.invalid`), via readRows(T.pendingClaims, …):
  cause without a code (the builder's fixture) -> 106 chars, sentence x1
  cause with code ENOTFOUND (what node hands back) -> 158 chars, sentence x2:
    "TypeError: fetch failed Caused by: Error: getaddrinfo ENOTFOUND db.invalid (ENOTFOUND) Error: getaddrinfo ENOTFOUND db.invalid (1 runtime stack frame dropped)"
  cause with code ECONNREFUSED -> 167 chars, sentence x2.
The 151 -> 106 measurement in this ticket's History is real, and so is the fixture it was taken on: `transportFailure()` (tests/fixtures/stub-client.ts) carries no `code` either, so the fix was derived and graded entirely on the one shape production never produces.

Pinned: it.fails("says the cause sentence once when the cause carries a code, as node's do"), tests/offline/db/result.test.ts, in the "an account carries the parts the database authored" describe beside this ticket's own twin, committed on run/admin-window as da8d8ba3. Watched RED as a plain it() first — AssertionError "expected 2 to be 1" at tests/offline/db/result.test.ts:2334 — before the .fails marker went on.

What the rest of the attack found INTACT, so the fix keeps these on the retry: the prefix-sharing twin keeps both lines in both arrival orders; BUG-0187's bar is undisturbed (measured: a FRAMELESS `details` of two identical "permission denied for table groups" lines still crosses verbatim, newline and both copies intact, because saidOnce runs only where a frame was dropped); the ten-fixture table and every carries/never assertion are unchanged and green; the eight other offline suites consuming transportFailure() are green (654 passed) after the fixture's account changed; the account still names the object read, still counts frames in the app's own words, and carries no "node:dns:", no "GetAddrInfoReqWrap" and no absolute path on every shape driven.

Two residuals, neither filed (lean depth, one bug per batch): (1) the rule is one-directional by design, so a part whose bare repeat arrives BEFORE the attributing line still says it twice ("TypeError: fetch failed Error: bad port Caused by: Error: bad port (1 runtime stack frame dropped)") — the builder documented this and no measured shape reaches it; it becomes a ticket only if a client is found that emits the stack first. (2) saidOnce is quadratic in the line count of one part: measured 1000 lines 32ms, 2000 77ms, 4000 360ms, 8000 1913ms (details is foreign-authorable through an intermediary's envelope, and this file has a deliberate no-length-cap ruling) — a ticket only if a bounded-work rule is ever wanted for the account.
