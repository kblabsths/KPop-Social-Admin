---
id: BUG-0164
type: bug
status: done
milestone: patch
priority: P3
opened_by: strategist
parent: null
discovered_from: designer:endgame-walk
children: []
assignee: null
claimed_at: null
depends_on: [TASK-0072]
touch_scope: [src/app/queues/page.tsx, tests/offline/queues/page.test.ts, tests/offline/ui/copy.test.ts]
force: false
attempts: 1
resolution: fixed
---
# /queues still carries the exit copy BUG-0161 replaced on /claims

## Description
`src/app/queues/page.tsx:220` still reads:

    filledBy: "Widen a filter above; the 'all' chip on any row shows everything again."

BUG-0161 replaced exactly that sentence on /claims, and `src/app/claims/page.tsx:204` carries the comment recording why ("It used to be two sentences and neither exited"). /queues kept the old copy, so the same empty state now reads two different ways on two pages of one app.

Take the replacement /claims landed. If the sentence is worth sharing, share it by IMPORT rather than by retyping it a third time — LESSONS 5, and this ticket exists because a sentence was retyped once already.

## Acceptance criteria
The /queues empty-state exit copy matches what BUG-0161 landed on /claims, asserted on rendered HTML for the empty case. The literal string "Widen a filter above" occurs zero times under src/. If the copy is shared, it is one exported thing with one test and grep finds no second hand-written copy.

## Checks
```sh
./node_modules/.bin/vitest run --project=offline tests/offline/queues/page.test.ts tests/offline/ui/copy.test.ts
npm run lint
npm run test:live -- tests/live/queues.live.test.ts
```

## History
- 2026-09-10T21:22:51Z [strategist] opened: /queues still carries the exit copy BUG-0161 replaced on /claims
- 2026-09-10T21:46:08Z [architect] depends_on: [] → [TASK-0072]: destination collision: TASK-0072 touches src/app/queues/page.tsx and tests/offline/queues/** for the scope-list change, which BUG-0164 also writes. Chained behind it; priority and milestone unchanged.
- 2026-09-11T14:51:39Z [builder-294] claimed (attempt 1)
- 2026-09-11T15:14:45Z [builder-294] claimed → built: The retired advice ("Widen a filter above…") is gone from src/ entirely; /queues now renders the sentence BUG-0161 landed, imported not retyped: CLEAR_LABEL/CLEAR_EYEBROW/CLEARED_BY + clearNarrowing moved to src/lib/url/narrowing.ts, ui/ClearedBy assembles the sentence and ui/ClearRow draws the 'clear filters' chip it quotes. /queues gained that exit row (it had none; every 'all' chip carries source_id forward). /claims renders byte-identical.

OUT OF touch_scope, and why: (1) tests/offline/url/narrowing.test.ts refused a SECOND declaration of clearNarrowing — the right call — so it is declared once, signature now (narrowed, unnarrowedHref); its OWNER entry moved and tests/offline/claims/filters.test.ts composes as the page does (its tab assertion now spells the unnarrowed URL itself instead of asking claimsHref, i.e. stronger). (2) src/lib/claims/filters.ts, src/components/{claims,queues}/filter-bar.tsx, src/components/ui/{index.ts,cleared-by.tsx}, src/lib/review/queue-filters.ts.

Verified: ./node_modules/.bin/vitest run --project=offline tests/offline/queues/page.test.ts tests/offline/ui/copy.test.ts (141), npm run lint 0, npm run build 0, tsc --noEmit 0, npm run test:live -- tests/live/queues.live.test.ts (14) and claims.live (17), full npm test 3908 — all on the rebased tree. New tests seen RED on the pre-fix page: 7/9 of the queues 'way out of a narrowing' describe fail, and the copy guard names src/app/queues/page.tsx:220.

For QA: observed on a production build against staging (port 8781, killed) — /queues?source_id=<uuid-with-no-rows>&status=settled renders "The 'clear filters' chip above clears every filter in this URL, including source_id, which has no chip row of its own." with exit href /queues, which renders 72 item rows and no exit row. The filter bar renders on the queues tab only, so no exit chip on ?tab=verdict_log (that tab applies no facet; the dropped-params line says so). The two FilterBar components are still near-twins over two structurally identical FilterFacet types — a third page wanting chips is when one bar earns its keep.
- 2026-09-11T15:15:15Z [dispatcher] landed ticket/BUG-0164 onto run/admin-window as 00523e41
- 2026-09-11T15:15:51Z [qa] built → qa: under attack
- 2026-09-11T15:24:44Z [qa] qa → done: Attacked on the landed tree (585e410b), then walked both pages in Chromium against staging on an isolated worktree instance (port 8783, dev server + worktree mine, both removed; minted cookie deleted).

CRITERIA. (1) Retired advice gone: grep -rn 'Widen a filter above|shows everything again' src/ = 0 hits; no page body I rendered carried either fragment. (2) One declaration: 'chip above clears every filter in this URL' is spelled once under src/ (src/lib/url/narrowing.ts:260, interpolating CLEAR_LABEL); src/lib/claims/filters.ts:630 is a re-export, not a second hand-written copy; both pages render it through ui/ClearedBy. (3) Rendered sameness measured, not read: /queues?source_id=aaaaaaaa-1111-4111-8111-aaaaaaaaaaaa&status=settled -> card 'No items matching these filters The 'clear filters' chip above clears every filter in this URL, including source_id, which has no chip row of its own.'; /claims?domain=zzz -> same sentence with 'domain'. One spelling, two pages.

ATTACK (states driven, Expected/Found all matching). Exit chip present iff a narrowing facet was applied, and clicking it (real click, not an href comparison) landed unnarrowed with no exit row: source_id-only; source_id+status; all five facets at once (source_id+kind+queue+shape+status) -> /queues, 0 exit rows; kind=signal (narrowed WITH rows) -> /queues; status=settled (chip facet only) -> sentence is the promise alone, no 'including' clause, correct per LESSONS 2. No exit and no promise where nothing was applied: bare /queues, /claims, source_id=not-a-uuid and source_id=&status= (dropped-params line owns those instead), /claims?bucket=unmatched_venue. Seams: ?tab=verdict_log&source_id=..&status=settled renders no bar, no chip and no sentence, with 'The URL carries source_id and status, which this page did not apply' - promise and control cannot come apart there; /claims?domain=zzz&tab=standing keeps the tab in the exit href (/claims?tab=standing) exactly as /claims did before. Duplicated params (source_id twice, status twice), an UPPERCASE uuid, and domain=<script>alert(1)</script> (escaped, exit still /claims) all behave. Structural check that the card can never name a chip the page did not draw: the card arm needs isNarrowedBeyond(filter, ownNarrowing), the chip needs isNarrowedBeyond(filter, {}) - the weakest of the family, so card-narrowed is a subset of chip-shown.

OUT-OF-SCOPE EDITS, graded. (a) /queues gaining the exit row is not creep: the imported sentence names a chip, so importing it onto a page without one would have shipped a new lie. (b) clearNarrowing collapsing to one declaration was forced by tests/offline/url/narrowing.test.ts' OWNER map; the claims call site now passes hasNarrowingFacet(filter) and claimsHref(CLAIMS_PATH,{},tab) - literally what the old three-arg body computed - and claims stayed byte-identical on BUG-0161's states (220 offline page tests, 348 offline claims+narrowing, 17 live, plus the browser states above). The filters.test.ts change replaced an assertion against claimsHref's own output with the URL spelled out: strictly stronger.

CHECKS re-run in the primary checkout: offline vitest 141 passed, npm run lint 0, npm run test:live queues 14 passed, plus ./node_modules/.bin/tsc --noEmit 0 and claims live 17. Receipt GREEN on this tree.

RESIDUALS (not tickets): (1) tests/offline/ui/copy.test.ts locates the one declaration by the literal fragment 'chip above clears every filter in this URL', so a designer rewording that sentence reddens the offline suite though the code is correct - trigger: any copy edit to CLEARED_BY.chip. (2) QUEUES_UNCHIPPED_FACETS' before:'of source '/after:'' are rendered by nothing today (only unchippedPhrase reads them and /queues does not call it) - dead words that can drift from SourceScope's wording; no user path reaches them. [resolution: fixed] [receipt green: 3 cmd(s), tree c6cc083d]
