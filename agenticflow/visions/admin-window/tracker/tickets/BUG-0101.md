---
id: BUG-0101
type: bug
status: done
milestone: M2
priority: P2
opened_by: qa
parent: null
discovered_from: BUG-0098
children: []
assignee: null
claimed_at: null
depends_on: []
touch_scope: [src/components/EditableCell.tsx, src/components/edit-cell-layout.ts, src/components/records/record-fields.tsx, tests/offline/ui/editable-cell.test.ts, tests/offline/records/page.test.ts]
force: false
attempts: 1
resolution: fixed
---
# A refusal on a record's LAST field draws its app-voice half outside the table's clipping container - the half BUG-0098 shipped is the half that disappears

## Description
Measured 2026-09-08 by QA attacking the BUG-0098 + BUG-0099 batch: production build of `run/admin-window` at e73fa20, served on 127.0.0.1:8792 against staging (`ubfjjqlvnpnoborczbdb`), bundled Chromium, 1440x900, BOTH themes, `walk_sandbox` row `...0001` (`reset-sandbox` before AND after; "deleted 3, seeded 3, read back and verified" both times).

**Expected** — BUG-0098 criterion 1: "A refused write renders BOTH halves at the field: the database's own refusal verbatim in mono, and one sentence in the app's voice naming what to do." And the rule this repo already wrote down for this exact box, `hintSide` (`src/components/edit-cell-layout.ts:135-151`): "Below the last row there is no neighbour, only the table's own container: `DataTable` wraps its table in `overflow-x-auto`, and a box whose other axis is `visible` computes to `auto` - it clips. So the last line's hint hangs above."

**Found** — `EditStatus`'s failed box is `STATUS_BOX` (`src/components/EditableCell.tsx:451-452`), anchored `absolute top-0 left-full`. It grows DOWNWARD from the row's top and obeys no side, so on a record's LAST field it grows past the fields table's bottom edge - and the half it loses is the second one, the app-voice sentence BUG-0098 shipped.

Exact repro:
1. `node tests/walk/reset-sandbox.mts` with the STACK.md §5 staging mapping.
2. `npm run build` + `npm run start -- --port <free>`; mint the walk cookie; open `/records/walk_sandbox/00000000-0000-4000-8000-000000000001` at 1440x900.
3. Click the `observed_on` value - the LAST row of the fields table - type `nope`, press Enter. (Any refusal on the last row does it; `observed_on` is simply the last editable field of this record.)

Measured at rest (pointer parked at 0,0), identical in light and dark:
- fields table scroll container (`div.overflow-x-auto`): top 143, height 220 -> bottom edge y=363; `scrollHeight` 250 vs `clientHeight` 220.
- the refusal box (`role="alert"`): y 337 -> 393.
- mono half `invalid input syntax for type date: "nope" (22007)`: 339 -> 371, so its second line is cut 8px mid-word.
- app-voice half `Type a date, like 2026-01-15.`: 373 -> 391, **entirely below the container's bottom edge - 28px outside, `fullyOutside: true`**. None of it is painted. Scrolling that inner container to `scrollTop: 30` brings it back (`insideNow: true`), which is the only way to read it.
Walk screenshot (gitignored, local): `agenticflow/tracker/evidence/BUG-0098/lastrow-light.png` - the refusal ends mid-word and no sentence follows it.

**This is not BUG-0086's class and does not reopen it.** Nothing reflows: in the same walk, row geometry before and after the refusal is identical (rows at y=169.5 / 198.5 / 231.5 / 264.5 / 297.5 / 330.5; cells x=209 / 459.3 / 1175.7, w=250.3 / 716.41 / 247.3 in both snapshots), so the status is out of the flow exactly as BUG-0086 requires. This is the OTHER consequence of being out of the flow - the one `hintSide` already handles for the hint and nobody handles for the status.

Rows other than the last are correct and stay correct: `tally` / 22P02 box 271->327 and `label` / 23502 box 205->325 both sit inside y=363, both halves painted, both fix sentences right ("Type a whole number, like 7." / "label cannot be cleared - type a value into it.").

It gets worse with message length, not better: the box is `max-w-xs`, so its height is a function of the refusal's own words. `label`'s 23502 DETAIL already measures 120px tall; that same refusal on a last row would put ~94px outside the container. FEAT-0011's gate refusals arrive in this same box.

**Expected: both halves render at the field (BUG-0098 criterion 1). Found: on a record's last field the app-voice half is drawn 28px below the fields table's clipping container and is not painted at all** - `src/components/EditableCell.tsx:451` (`STATUS_BOX`, anchored `top-0`, no side), while `src/components/edit-cell-layout.ts:149` already computes that side for the hint.

Note for the fixer, and QA's own honesty: there is NO offline pin for this today and QA did not invent one. `tests/offline` is plain node with `renderToStaticMarkup` and no jsdom (STACK.md §4), so a bounding box is unobservable there - `edit-cell-layout.ts`'s own docstring says it: "The decision is pinned offline; the boxes themselves are measured in a walk." The decision this needs does not exist yet, which is why criterion 4 asks for it.

## Acceptance criteria
1. On the LAST field of a record, a refused write renders BOTH halves inside the fields table's own container: with the cell resting and nothing scrolled, the app-voice sentence is painted in full and the mono refusal is not cut mid-line. Measured on a production build at 1440x900 in both themes, `/records/walk_sandbox/00000000-0000-4000-8000-000000000001`, `observed_on` typed `nope`.
2. Every other row keeps exactly what it has today: `tally` / 22P02 and `label` / 23502 still render both halves at the field, unchanged.
3. The status stays OUT of the row's flow (BUG-0086, unchanged): committing a cell moves no row's y and no cell's x or width. Nothing here re-introduces the reflow, and the box stays inert to the pointer.
4. The placement decision is a PURE rule in `src/components/edit-cell-layout.ts`, beside `hintSide`, and is pinned in the offline suite the way `hintSide` is (`tests/offline/ui/editable-cell.test.ts`) - geometry stays a walk fact, the decision does not.
5. The refusal is still the database's words verbatim, both halves are still inside the one `role="alert"`, and the success path is untouched: `saving...` in secondary, `saved` in healthy for ~1.5s.
6. `npm run lint` and `tsc --noEmit` clean; the targeted offline files and the record page's live suite green.

## Checks
```sh
bash -c 'rm -rf .next/types && npm run lint && ./node_modules/.bin/tsc --noEmit'
./node_modules/.bin/vitest run --project=offline tests/offline/ui/editable-cell.test.ts tests/offline/records/page.test.ts
npm run test:live -- tests/live/edit.live.test.ts
```

## History
- 2026-09-09T05:55:36Z [qa] opened: A refusal on a record's LAST field draws its app-voice half outside the table's clipping container - the half BUG-0098 shipped is the half that disappears
- 2026-09-09T05:56:47Z [builder-168] claimed (attempt 1)
- 2026-09-09T06:11:36Z [builder-168] WALK, 2026-09-08 (builder-168). Production build of the rebased ticket/BUG-0101 tree (this fix on run/admin-window at f10f237), served 127.0.0.1:8780 (bind-probed; not 8770/8771) against staging ubfjjqlvnpnoborczbdb, bundled Chromium, 1440x900, BOTH themes, walk_sandbox row ...0001. reset-sandbox before AND after: both "deleted 3, seeded 3, read back and verified".

Fields table scroll container (div.overflow-x-auto) in every measurement: top 143, bottom 363, height 220. Pointer parked at 0,0 for every reading. Light and dark are byte-identical in geometry.

CRITERION 1 - observed_on (LAST row) typed `nope`:
- refusal box role="alert": 301 -> 357, h 56, fully inside 143..363 (was 337 -> 393, 30px past the edge).
- mono half `invalid input syntax for type date: "nope" (22007)`: 303 -> 335, 2 whole line boxes, inside (was 339 -> 371, cut 8px mid-word).
- app-voice half `Type a date, like 2026-01-15.`: 337 -> 355, inside, painted in full (was 373 -> 391, fullyOutside: true).
- container scrollTop 0, scrollHeight 220 == clientHeight 220: the box adds ZERO scrollable overflow now (QA measured 250 vs 220).

CRITERION 2 - other rows unchanged, same numbers QA recorded pre-fix:
- tally / 22P02 box 271 -> 327; halves 273 -> 305 and `Type a whole number, like 7.` 307 -> 325. All inside.
- label / 23502 box 205 -> 325 (h 120); halves 207 -> 303 (6 lines) and `label cannot be cleared - type a value into it.` 305 -> 323. All inside.

CRITERION 3 - nothing reflows, box still inert:
- rows before and after the refusal: 169.5 / 198.5 / 231.5 / 264.5 / 297.5 / 330.5 (identical, and identical to QA's pre-fix snapshot).
- cells x 209 / 459.3 / 1175.7, w 250.3 / 716.4 / 247.3 (identical both snapshots).
- computed position absolute, pointer-events none; document.elementFromPoint at the alert's centre returns a `td`, never the alert.

CRITERION 5 - success path on the SAME last row (observed_on written to a fresh date, PATCH 200):
- `saving...` 0-300ms then `saved` 400-1700ms, gone by 1800ms (~1.5s clock).
- `saved` ink rgb(1,102,48) light / rgb(5,223,114) dark (healthy).
- `saved` box 337 -> 357 and the resting value's box 337 -> 357: bottom-anchoring puts a one-line status at exactly the y top-anchoring did, so nothing an operator sees moved.
- 2026-09-09T06:14:18Z [builder-168] claimed → built: - `statusGrowth(row, rows)` added beside `hintSide` in `edit-cell-layout.ts`, both now reading one `lastOfSeveral` predicate; `RecordFields` -> `FieldEditor` -> `EditableCell` -> `EditStatus` carry it, and `STATUS_BOX` lost its hard-coded `top-0` so the box hangs from `top-0` (down) or `bottom-0` (up). Last of several lines grows up; everything else, and any caller that says nothing (the close form), grows down.
- Verified: red-then-green offline (4 assertions failed before the consumers were wired, listed in the run) + `vitest run --project=offline tests/offline/ui/editable-cell.test.ts tests/offline/records/page.test.ts` 214 pass, `npm test` 2689 pass, lint + `tsc --noEmit` clean, `npm run test:live -- tests/live/edit.live.test.ts` 25 pass - all re-run on the rebased tree (run tip 21e2d54). Walk on a production build, port 8780, staging, both themes, 1440x900: numbers are in the History comment above - last-row refusal box now 301->357 inside a container of 143->363, both halves painted, container scrollHeight == clientHeight (was 250 vs 220); tally 271->327 and label 205->325 unchanged; rows and cell widths byte-identical before/after; `elementFromPoint` at the box's centre still returns a `td`.
- TRAPS for QA: (1) `src/components/records/field-editor.tsx` is OUTSIDE touch_scope and I edited it - it is the only path from `RecordFields` to `EditableCell`, and it gains one pass-through prop, nothing else. (2) The last hop, `EditableCell` -> `EditStatus`, is NOT offline-observable (a status needs an interaction `renderToStaticMarkup` cannot produce), so it rests on the walk; the three hops that are observable are pinned. (3) The rule is ordinal, not geometric, exactly like `hintSide`: a refusal tall enough to exceed the whole container (>216px at this viewport) would still clip, at the top instead of the bottom - the tallest this surface produces today is label's 23502 at 120px.
- 2026-09-09T06:14:37Z [dispatcher] landed ticket/BUG-0101 onto run/admin-window as b75039b
- 2026-09-09T06:21:25Z [qa] built → qa: under attack
- 2026-09-09T06:35:57Z [qa] qa → done: Attacked on the LANDED tree (run/admin-window 88da50e) from a detached _receipt- worktree; criteria re-run before reading the builder walk. Production build on 127.0.0.1:8793 (bind-probed, not 8770/8771), staging ubfjjqlvnpnoborczbdb, bundled Chromium, 1440x900, BOTH themes, walk_sandbox ...0001, reset-sandbox before AND after (both "deleted 3, seeded 3, read back and verified"). Container div.overflow-x-auto in every reading: y 143, h 220, bottom 363. Pointer parked at 0,0.

C1 GREEN (measured, not read): observed_on typed `nope` -> alert 301 -> 357, fullyInside; mono `invalid input syntax for type date: "nope" (22007)` 303 -> 335 (two whole lines); app-voice `Type a date, like 2026-01-15.` 337 -> 355, painted in full; container scrollHeight 220 == clientHeight 220 (QA measured 250 vs 220 pre-fix). Light and dark byte-identical.
C2 GREEN: tally / 22P02 271 -> 327 (halves 273 -> 305, 307 -> 325); label / 23502 205 -> 325 (halves 207 -> 303, 305 -> 323) - the pre-fix numbers exactly.
C3 GREEN: 25 geometry frames across a real commit - rows 169.5 / 198.5 / 231.5 / 264.5 / 297.5 / 330.5 and cells x 209 / 459.3 / 1175.7 (w 250.3 / 716.41 / 247.3) identical in every frame and after; alert computed position absolute, pointer-events none; elementFromPoint at its centre returns a td.
C4 GREEN: statusGrowth is pure, beside hintSide, sharing lastOfSeveral; pinned both directions, the omitted-prop default, the one-line record and the RecordFields -> FieldEditor hops in tests/offline/ui/editable-cell.test.ts.
C5 GREEN: database words verbatim, both halves children of the one role="alert"; dark commit on the last row saving... rgb(153,161,175) 0-130ms -> saved rgb(5,223,114) 254-1626ms -> gone by 1750ms; status box 337 -> 357, the resting value own line.
C6 GREEN: receipt re-ran all three stored checks (exit 0 each); full npm test 2690 pass + 6 isolated, lint and tsc clean from the worktree.

Attacks that found nothing: double Enter on a refusing last-row cell -> exactly ONE role="alert", same box 301 -> 357, no reflow; a 180-char value in observed_on -> a 3-line refusal 285 -> 357, still inside (the builder trap 3 needs >216px, which this surface cannot produce); four consecutive last-row commits all 200 with the value round-tripping.

Attack that landed -> BUG-0104 filed, NOT a bounce: clearing is_flagged (row 5 of 6) is told "down" by the ordinal rule and its 120px 23502 box draws 304 -> 424 against the container 363 - app-voice half 404 -> 422, fullyOutside, painted nowhere, identical in both themes. Pre-existing and untouched by this fix (row 5 was told "down" before it too), and this ticket criteria name only the last field. Same class, one row up.

One transient discarded, not a bug: a last-row commit once answered 404 "no walk_sandbox record with that id" while a concurrent lane reset-sandbox had the row deleted (that lane probe value "qa r2 probe" was in the row I read back). Four re-runs answered 200.

No test added, deliberately: the class is geometric and tests/offline has no jsdom (STACK section 4); the only offline-observable fact - RecordFields telling is_flagged "down" - is CORRECT for an ordinal rule, so a pin there would fabricate a "last two lines" hack. BUG-0104 carries that pinning obligation as its criterion 4. [resolution: fixed] [receipt green: 3 cmd(s), tree 32b8c2b3]
