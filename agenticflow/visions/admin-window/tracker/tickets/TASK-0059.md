---
id: TASK-0059
type: task
status: qa
milestone: M2
priority: P2
opened_by: architect
parent: FEAT-0013
children: []
assignee: builder-155
claimed_at: 2026-09-09T04:41:34Z
depends_on: [TASK-0058, TASK-0049]
touch_scope: [src/app/queues/[reviewItemId]/page.tsx, src/lib/db/verdict.ts, src/lib/db/review-item.ts, src/components/review/close/slot.tsx, tests/offline/review-item/verdict-inline.test.ts, tests/live/review-item.live.test.ts]
force: false
attempts: 2
---
# A settled review item's detail carries its own verdict inline

## Description
**SPEC F13, second half.** The investigation path ends where the decision was made: a **settled** item's detail renders its own verdict inline, rather than sending the operator to a different tab to find out what they themselves decided.

**What it renders**, in the close slot's place: the action verbatim in mono, the actor, the note (the dash with no qualifier when there is none), when it was decided, and a link to the observation it wrote (the dash when the verdict carried no value). An **unsettled** item's detail renders no verdict block at all — an empty verdict block on an open item is the "renders X but not the absence of X" defect LESSONS 1 is about.

**The read** joins `verdicts` by `review_item_id` and belongs beside the other verdict reads in `src/lib/db/verdict.ts` (one module owns this object; four pages hand-copying a helper is common violation 9). It is a by-id read, not a list read.

**Absence, in the two ways it arrives:**
1. `verdicts` absent (staging today) — the detail renders the same `not_provisioned` card the close slot already renders, naming `verdicts`, and the rest of the page is exactly what M1 shipped. Nothing throws.
2. `verdicts` present but carrying no row for a settled item — an honest gap in the data, not an absence of the table: the block says the item is settled and no verdict row was found, and does NOT render a blank slot or claim the table is missing. These two are different states and the test asserts both.

**Landing order**: this ticket and the close slot both write `src/app/queues/[reviewItemId]/page.tsx`, so it is chained behind them (ARCHITECTURE §13.9 — consolidation-shaped work is chained at its destination, because worktrees isolate builds and not landings).

## Acceptance criteria
1. A settled item's detail renders its verdict inline — action verbatim in mono, actor, note or the unqualified dash, the time, and a link to the observation or the unqualified dash — in the close slot's place.
2. An unsettled item's detail renders no verdict block at all, and the close slot behaves exactly as its own ticket shipped it.
3. With `verdicts` absent: the detail renders the `not_provisioned` card naming `verdicts`, the page answers 200, nothing throws, and the rest of the page renders as M1 shipped it.
4. With `verdicts` present but no row for a settled item: the block says so in its own words and is distinguishable in the markup from the not-provisioned state — both states asserted in the same test file.
5. The read lives in `src/lib/db/verdict.ts` and is a by-id read; no page issues its own query and no helper is hand-copied into the page.
6. The review-item live suite names the state kind before comparing anything and passes against staging, where the one real item is open and `verdicts` is absent.
7. `npm run lint`, `tsc --noEmit`, the targeted offline files and the review-item live suite are green.

## Checks
```sh
bash -c 'rm -rf .next/types && npm run lint && ./node_modules/.bin/tsc --noEmit'
./node_modules/.bin/vitest run --project=offline tests/offline/review-item/verdict-inline.test.ts tests/offline/review-item/page.test.ts
npm run test:live -- tests/live/review-item.live.test.ts
```

## History
- 2026-09-09T01:21:36Z [architect] opened: A settled review item's detail carries its own verdict inline
- 2026-09-09T03:58:08Z [builder-147] claimed (attempt 1)
- 2026-09-09T04:20:50Z [builder-147] claimed → built: - `readItemVerdict` (src/lib/db/verdict.ts) — by-id COMPLETE read (count exact, total order created_at/verdict_id desc, range 0..cap-1) filtered `.eq("review_item_id", id)`, re-sorted through the log's own `newestFirst`, with the log's two-step observation leg beside it (`factUnavailable`, never instead of the verdict). Rendered by `CloseSlot` in the place a control would stand, as sub-surface `data-surface="item_verdict"`.
- Four states, all offline-graded in tests/offline/review-item/verdict-inline.test.ts: log absent -> the ONE not-provisioned card the close already draws, no second card, no block; log present + no row -> `data-state="empty"` in the block (never `not_provisioned`, never blank); log present + row -> action in mono verbatim, actor, note-or-dash, when, observation link-or-dash; unsettled -> no block AND no second query.
- Verified on the rebased tree (191fd9b, rebased twice onto run/admin-window; conflicts with BUG-0087's `notice` prop resolved additively) from a detached `_receipt-` worktree: tsc 0, lint 0, full offline 2471/2471, `npm run test:live -- tests/live/review-item.live.test.ts` 12/12 against staging. Red-then-green: 7 sabotages each reddened only the intended case (status guard dropped, Empty->NotProvisioned, `{" "}` dropped, `=== null` for the note, dashed-unresolvable-observation, `.eq` dropped, complete->limit(1) window).

QA traps worth knowing:
- The close's live grading now uses `stateOf(markup, CLOSE, ITEM_VERDICT)` in the two installed branches — the block's emptiness is the BLOCK's, not the close's. `assertState` takes no `excluding`, which is why those two lines changed shape.
- Staging measured 2026-09-08 read-only: `review_items` holds 1 row, status `open`, 0 settled items, `verdicts` absent. So the live "renders no verdict block on an item this database has not settled" case asserts for real today; the settled-item case returns early with no settled row to compare, by design.
- Judgment call: the dash-meaning line renders only when a dash is actually on screen (the block is one verdict, not the log's column of them). The verdict log renders its line unconditionally; if that inconsistency is unwanted, it is one `if` in `ItemVerdictBlock`.
- 2026-09-09T04:21:07Z [dispatcher] landed ticket/TASK-0059 onto run/admin-window as ab858a9
- 2026-09-09T04:22:32Z [qa] built → qa: under attack
- 2026-09-09T04:36:48Z [qa] qa → reopened: BUG-0092 filed (P3). All seven acceptance criteria verified green on the landed tree BEFORE reading the builder's handoff; the bounce is one corner of the block this ticket added, not the ticket's goal.

WHAT I RAN (detached worktree .worktrees/_receipt-qa0059 at efecfd8, node_modules and .env symlinked, never the primary checkout; no port bound anywhere — every check is vitest, in-process or over HTTPS; no pids started or killed; .env never opened or grepped; read-only on staging):
- stored check 1 `rm -rf .next/types && npm run lint && tsc --noEmit` -> exit 0
- stored check 2 targeted offline (verdict-inline + page) -> 99/99
- absence + queues + review-item + verdict suites -> 739/739 across 14 files
- full offline `npm test` -> 67 files / 2492 tests, zero failures (+ isolated project 6/6)
- stored check 3 `npm run test:live -- tests/live/review-item.live.test.ts` -> 12/12 against staging

CRITERIA, one by one: (1) action verbatim in mono, actor, note-or-dash, when, observation link-or-dash, in the close slot's place - green; (2) unsettled item renders no block and the close is untouched, and the by-id read is never even issued (1 verdicts call, the readiness probe) - green; (3) verdicts absent -> exactly ONE not-provisioned card naming verdicts, no second, no block, M1 surfaces untouched - green; (4) present-but-no-row draws data-state=empty, never not_provisioned, both asserted in one file - green; (5) the read is by-id in lib/db/verdict.ts, complete (count exact, range 0..cap-1, no limit), re-sorted through newestFirst, and the page spells no query - green; (6) the live oracle names the state kind from its own reads before comparing - green; (7) lint/tsc/targeted/live - green.

ATTACK (beyond the criteria, all on the landed markup through the stub):
- data-surface uniqueness in every state, on the conflict item AND the signal item with its dial: {what_happened:1, evidence:1, close:1, item_verdict:1} every time.
- verdicts dropped BETWEEN the readiness probe and the by-id read: still exactly one not-provisioned card, named `verdicts`, inside item_verdict; stateOf(close excluding item_verdict) stays ok. No double card.
- observations refusing (57014) and observations absent (PGRST205) on the leg: the verdict stays on screen and the leg names its OWN object beside it. Correct.
- newest-of-several where the NEWEST carries no observation and the older one does: the leg reads nothing and the block dashes it. Correct - no cross-row bleed.
- truncated complete read (count 4, rows 1) refuses instead of picking a verdict. Correct.
- 400-char note: clamped to 200 on screen, full text on title. Correct.

WHAT BROKE (BUG-0092): the block draws dashes it neither owns nor explains, on the two lines no criterion names.
Expected: every dash it puts on screen is the app's one dash and is accounted for by its own dash-meaning line ("Rendered only when a dash is actually on screen", slot.tsx; LESSONS 1).
Found, at src/components/review/close/slot.tsx ItemVerdictBlock: (a) actor "   " with a note and a resolvable observation -> [aria-label="no value"]=1, [data-absence-note]=0, an unexplained dash; (b) created_at "settled last tuesday" -> relativeAge answers {text: EM_DASH, title: ""} and the line renders that text directly -> a BARE em-dash node, 0 [aria-label="no value"], while src/components/queues/verdict-log.tsx guards the SAME column (`age.title === "" ? null : ...`) so the two screens render verdicts.created_at two different ways.
Pinned: 4 tests in tests/offline/review-item/verdict-inline.test.ts (commit efecfd8). Both pins were watched RED as plain it() first (2 failed | 25 passed), then wrapped as it.fails - strictness proved on a throwaway (a passing body reports FAILED), so they turn red the day the block is fixed and send the reader to BUG-0092. Two ordinary tests pin the states the block gets right, so a fix cannot buy the invariant by dropping a line.
Priced P3, not P2, honestly: neither trigger is reachable from today's data path (verdicts.actor is text not null, created_at is a timestamptz PostgREST serialises as ISO). It is a ticket rather than a residual because TASK-0059's own stored check already RENDERS the blank-actor state and grades it incompletely.

NOT a finding: the per-need dash-meaning line vs the log tab's unconditional one. The Look's "the anatomy never changes between screens" is scoped to the evidence pair, and LESSONS 1 asks for the line on "a column of dashes"; one verdict is not a column. The builder's judgment call stands - no designer bug filed.

RESIDUALS (handoff, not tickets): (1) the live test "carries the row the log holds for a settled item" passes VACUOUSLY - staging has no settled item, so it bare-returns and counts toward 12/12; a trigger would be making the skip announce itself. Pre-existing house pattern in that file, not this ticket's. (2) An open item that already carries a verdicts row renders the close form and never reads it - correct per criterion 2, invisible if a settlement ever half-lands. (3) The block shows the newest of several verdicts and says nothing about the rest, though readItemVerdict holds the exact count.
- 2026-09-09T04:41:34Z [builder-155] claimed (attempt 2)
- 2026-09-09T04:50:43Z [builder-155] claimed → built: BUG-0092 (the bounce reason) is fixed in this branch at 83dff88 — BUG-0092 should close already_done behind it. The block now owns every dash it draws, in src/components/review/close/slot.tsx ItemVerdictBlock:
- Leg B: an instant `relativeAge` cannot parse (`title === ""`) is handed over as null so `orDash` draws it — the same guard the sibling rendering of that column already spells (components/queues/verdict-log.tsx `created`), so verdicts.created_at renders one way on both screens. Measured: {stray, drawn} was {1, 0} pre-fix, is {0, 1} now.
- Leg A: the dash-meaning line is keyed to ALL four absent-able lines (`dashOnScreen`), not note/observation alone, so a blank actor's dash carries exactly one [data-absence-note] (was 0). Its sentence dropped 'here is not missing data' — that claim is false of an actor a row left blank or an instant that will not parse, and one line has to be true of all four dashes; it still names the two structural reasons.
- QA's two it.fails pins are ordinary it() again and pass; the two ordinary tests beside them (settle-only = {2, 0, 1}; everything present = {0, 0, 0}) are untouched and green.
Verified on the REBASED tree (83dff88, rebased twice; run branch moved to f5dbf2b mid-run, no conflicts) from a detached _receipt- worktree, node_modules/.bin and .env symlinked, .env never opened, read-only on staging, no port bound, no pid started or killed: lint 0, tsc 0, review-item+absence+queues offline 647/647 across 12 files, full offline 2518/2518 (67 files) + isolated 6/6, npm run test:live -- tests/live/review-item.live.test.ts 12/12 against staging.
Red-then-green watched: with HEAD's pre-fix slot.tsx restored, the two flipped tests were 2 failed | 6 passed; restored, 8 passed.
For QA: neither trigger is reachable from staging data (actor is text not null, created_at is a timestamptz), so this is graded offline only — the live suite's settled-item case still returns early, staging having no settled item. The dash-meaning wording change is deliberate and is the one judgment call here; if the log tab's 'not missing data' phrasing is wanted verbatim on both surfaces, that is a designer call, not a code one.
- 2026-09-09T04:51:04Z [dispatcher] landed ticket/TASK-0059 onto run/admin-window as 50f9463
- 2026-09-09T04:55:55Z [qa] built → qa: under attack (fix-scoped re-check r2)
