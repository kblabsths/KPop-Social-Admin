---
id: BUG-0093
type: bug
status: claimed
milestone: M2
priority: P2
opened_by: qa
parent: FEAT-0009
discovered_from: BUG-0088
children: []
assignee: builder-154
claimed_at: 2026-09-09T04:37:49Z
depends_on: []
touch_scope: [agenticflow/tracker/for-human/M2-handoff-settle-review-item.md, tests/offline/handoff/settle-review-item.test.ts]
force: false
attempts: 1
claim_tree_hash: e37a741ba1d22eb5bf5734d3b65bd2a6ddf291d97949e170893192c8e61f2a9f
---
# the artifact allocates KS027/KS028, which the sibling's staging harness doors already raise

## Description
**Expected** — the artifact's own citation table (`M2-handoff-settle-review-item.md:846`) tells Ben: "`KS027`, `KS028`, `KS029`, `KS030` | the four SQLSTATEs this file allocates, in the sibling's own grammar. A grep of that directory on 2026-09-08 shows `KS001`-`KS026` in use and nothing above it, **so these collide with none**". The sibling's rule is one code, one meaning: `kspace Scraper/tests/helpers/ks_codes.py:1-5` — "The campaign's own refusal codes, each named once for what it refuses. Every `KSnnn` the migrations raise is named here - that is the admission rule, and `test_codes_named_once.py` enforces it."

**Found** — `KS027` and `KS028` are already taken next door, and the grep that cleared them read only one of the sibling's two SQL worlds. Measured read-only in `/Users/ben-m4/Desktop/Coding/KPOP/kspace Scraper` on 2026-09-08:

- `tools/staging/harness_objects.sql:2026` raises `errcode = 'KS027'` ("harness lease refused: the % lease over the reserved identities is live, held by %"); `:2121` raises `KS028`.
- `tests/helpers/ks_codes.py:122` `LEASE_HELD_BY_ANOTHER = "KS027"`; `:128` `LEASE_NOT_LIVE = "KS028"`.
- `tests/live_safety/test_codes_named_once.py:343` pins that registry at exactly `KS001`-`KS028`.

The sibling keeps a witness for precisely this mistake. `test_codes_named_once.py:29-38` (`HARNESS_DOORS`): "`tools/staging/` holds the staging-only harness doors, which are not migrations and never become ones ... a door added now raises its code HERE and nowhere a walk of `supabase/migrations/` can see it -- so a comparison that read the migrations alone would call a live code an unraised one (resolver/BUG-0044's two lease codes were the first)." Those two lease codes are `KS027`/`KS028`.

**What it costs Ben, on the path the note tells him to walk** (§1: paste into `supabase/migrations/20260908000002_the_verdict_settles_the_item.sql`, then `supabase db push` from the scraper repo):

1. The sibling's own suite goes RED on the paste, before any push. `tests/live_safety/test_codes_named_once.py:424 test_no_code_the_migrations_raise_is_left_unnamed_by_the_harness` asserts `codes_not_named(migration_codes(MIGRATIONS), _harness_ks_codes()) == []`; the pasted migration raises `KS029` and `KS030`, which `ks_codes.py` names nowhere. That is the debugging session in a repo this campaign may not enter that FEAT-0009 exists to prevent, and the note's §1 never tells him the paste has a companion edit in `tests/helpers/ks_codes.py`.
2. `KS027` (20 raise sites in the artifact) and `KS028` (the `wont_fix` note refusal) would carry two meanings each on staging, where the harness doors are installed by `tools/staging/bootstrap_staging.sh` alongside the migrations. "Refused KS027" stops being readable as one thing, which is the whole point of the sibling's registry.

**Repro (offline, no database, no sibling checkout required):**

    ./node_modules/.bin/vitest run --project=offline tests/offline/handoff/settle-review-item.test.ts

The pin `allocates codes the sibling has not already taken` is landed as `it.fails` (strict xfail, house convention — cf. BUG-0087/BUG-0088). Watched RED as a plain `it` on HEAD 9043b72 of run/admin-window first:

    AssertionError: expected [ 'KS027', 'KS028' ] to deeply equal []

It reads the allocated codes off §3's citation row rather than hardcoding them, so a renumber moves the test with the file. `KS001` is raised by the artifact too and is NOT a collision: it reuses the gate's own code with the gate's own meaning, which is what the sibling's grammar wants.

**The fix is a renumber plus one sentence.** `KS029`-`KS032` are free by the same two-world grep (`grep -rho "KS[0-9]\{3\}" --exclude-dir=node_modules --exclude-dir=.git --exclude-dir=agenticflow .` in the sibling shows `KS000`-`KS028` and nothing above). Renumber the four allocated codes, correct §3's row to say the grep covered `supabase/migrations/` **and** `tools/staging/` and name the date, and add to §1 the companion edit the sibling's admission rule requires: each new code gets its comment and constant in `kspace Scraper/tests/helpers/ks_codes.py`, and `test_codes_named_once.py:354`'s pinned list grows by them. Both of those are sibling edits — a HANDOFF line in this note for Ben, never an edit from here (design §10, AGENTS.md). Then flip the pin from `it.fails` back to `it`.

## Acceptance criteria
(to be written)

## Checks
```sh
./node_modules/.bin/vitest run --project=offline tests/offline/handoff/settle-review-item.test.ts
bash -c 'grep -q "^  it(\"allocates codes the sibling has not already taken\"" tests/offline/handoff/settle-review-item.test.ts'
```

## History
- 2026-09-09T04:35:46Z [qa] opened: the artifact allocates KS027/KS028, which the sibling's staging harness doors already raise
- 2026-09-09T04:37:49Z [builder-154] claimed (attempt 1)
