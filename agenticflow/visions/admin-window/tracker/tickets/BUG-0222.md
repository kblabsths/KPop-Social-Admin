---
id: BUG-0222
type: bug
status: reopened
milestone: patch
priority: P2
opened_by: architect
parent: null
discovered_from: BUG-0221
children: []
assignee: null
claimed_at: null
depends_on: []
touch_scope: [src/lib/paging/machine.ts, src/components/ui/paging.tsx, src/components/claims/paged-claim-list.tsx, src/components/browse/paged-browse-table.tsx, tests/offline/paging/machine.test.ts, tests/offline/ui/paging.test.ts, tests/offline/claims/page.test.ts, tests/offline/browse/page.test.ts]
force: false
attempts: 1
---
# A paged press draws a row the screen above it already holds: the same claim twice, and React's duplicate key

## Description
SPLIT FROM BUG-0221 by the architect's ruling of 2026-09-12 (that ticket's History carries it in full). BUG-0221 found two defects at one seam and the contract answers them differently: the SKIP is held for Ben's paging-shape decision (ARCHITECTURE §4.3 kind 3 already declines the snapshot promise, and its honest fix changes what the bound IS — the same layer Ben is deciding). THIS ticket is the other half, and it is buildable today under the contract exactly as it stands.

THE DEFECT. The wire bound is a POSITION (?offset=<rows held>, src/lib/paging/machine.ts:386 pageUrl; src/app/api/admin/claims/rows/route.ts:136 re-reads at that offset). A row INSERTED ahead of the bound between the screen read and the press moves every later row down one, so the page at offset N begins with a row the client already holds — and the surface concatenates it, drawing one claim twice. React's duplicate-key error is the operator-visible symptom in Ben's dev log (BUG-0221 repro B, measured 2026-09-11 on a dev instance: 100 rows drawn, 99 distinct, 01a058f1-02c7-7e11-9191-adfa6207a0f0 twice). In production the resolver and the scraper both write pending_claims while an operator reads it, so the trigger is ordinary traffic, not a dev-server artifact.

WHY THIS HALF NEEDS NO RULING. §4.3 kind 3's 'A concatenation is still not a total' permits the population to move under a bounded read; it does not permit one drawn list to hold one id twice. That is not a snapshot promise, it is the list disagreeing with its own count — and BUG-0216's own property ('what these surfaces draw is one contiguous run of the order their last read held') is the bar. The fix lives entirely in the CLIENT DRIVER: it does not change what the bound is, so it survives whichever paging shape Ben picks.

INTERFACE CONTRACT — what the machine must do (this is the design; a builder implements it, it is not re-decided in the lane):
1. THE STATE HOLDS THE IDS IT HAS DRAWN and appends only rows whose id it does not already hold. The drop happens on the APPEND, never on the wire.
2. THE WIRE BOUND STAYS THE OFFSET and advances by the number of rows THE ROUTE RETURNED — never by the number appended. If it advanced by the appended count, the next press would re-ask positions already answered and the overlap would repeat forever. So the wire bound and the drawn row count become TWO facts: each is stated by whoever owns it and neither is inferred from the other's size (§4.3 kind 3, rule 4 — the rule promoted from BUG-0174/BUG-0183, which is exactly this shape).
3. EXHAUSTION IS STILL THE ROUTE'S (exhausted === rows.length < size, §4.3 kind 3 'Full-or-exhausted, on BOTH sides of the wire'). A page deduped to zero appended rows is NOT exhaustion, and the control stays.
4. THE OVERLAP IS STATED, ONCE, IN CLIENT-LAND. The operator learns the list moved under them; a silent dedupe would hide the one fact that matters. It renders only after a press, so the first server screen stays byte-identical (M3 EC4) and NO data-window-* attribute is added (§4.3 kind 3, rule 3 of the window-line amendment). The WORDS are the designer's — grade the fact and the hook, never the prose (the Voice bars grade wording at the walk).
5. /browse carries the property through the same driver and the same primitive.

NOT IN THIS TICKET, and the ticket may not claim it: the SKIP (BUG-0221, blocked on Ben). Deduping does not fix a skipped row, and no sentence this ticket adds may imply the drawn run is contiguous or complete.

## Acceptance criteria
1. NO ID IS DRAWN TWICE on either paged surface, whatever the route answers. Driven offline through the machine on a fixture where a row is inserted ahead of the bound between the screen read and the press: every id in the drawn run is distinct, and the run's length equals its distinct count. The failing fixture and the passing one are both shipped (LESSONS 8): an overlapping page AND an ordinary page that must append every row it carried.

2. PROGRESS IS PRESERVED. The wire bound advances by the ROUTE's row count, so the press after an overlapping page asks the next unanswered positions and a walk still reaches exhaustion. Proved offline on a walk to the end of a population that moves once mid-walk — the walk terminates, appends no duplicate, and ends in the exhausted state.

3. THE OVERLAP IS STATED, NOT SWALLOWED. When a press returns rows the state already holds, the surface says so once, in client-land, through the paging area's own copy home (src/components/ui/paging.tsx), carrying a machine-readable hook an offline test asserts. No sentence added by this ticket claims the drawn run is complete or contiguous; no new data-window-* attribute is introduced; the words are the designer's to grade at the walk.

4. THE FIRST SERVER-RENDERED SCREEN IS UNCHANGED: same window line, same figures, same row set on the same fixtures (M3 EC4). The hook of criterion 3 is absent from the first render.

5. EXHAUSTION, REFUSAL AND NOTES BEHAVE AS THEY DO TODAY: a page deduped to zero appended rows is not exhaustion and keeps the control; a refused page still appends nothing, leaves the bound unmoved and retains the control; per-leg notes still merge as §4.3 kind 3 requires.

6. THE SKIP IS UNTOUCHED AND UNCLAIMED. tests/offline/paging/machine.test.ts keeps BUG-0221's it.fails pin exactly as QA wrote it — this ticket neither removes the marker nor makes the pin XPASS, and if the marker's case flips, that is a finding to report, not a criterion met.

7. The live walks stay green: tests/live/claims.live.test.ts and tests/live/browse.live.test.ts (the live population is frozen during a walk, so they grade the path this ticket must not change).

## Checks
```sh
./node_modules/.bin/vitest run --project=offline tests/offline/paging/machine.test.ts tests/offline/ui/paging.test.ts tests/offline/claims/page.test.ts tests/offline/browse/page.test.ts
grep -q 'admin-window/BUG-0222' src/lib/paging/machine.ts
npm run test:live -- tests/live/claims.live.test.ts
./node_modules/.bin/tsc --noEmit
npm run lint
npm run build
```
## History
- 2026-09-12T01:26:28Z [architect] opened: A paged press draws a row the screen above it already holds: the same claim twice, and React's duplicate key
- 2026-09-12T01:26:38Z [architect] checks amended: Adds the presence-of-the-fix check the ticket was filed one minute too early to carry (the id did not exist yet): the dedupe rule lands in the driver, cited by ticket id the way every other rule in that file is (machine.ts:190 cites admin-window/BUG-0216). Dry-run on this tree: red now, and it fails LOUDLY if the fix lands anywhere but the driver, which is the interface contract's point. [was: ./node_modules/.bin/vitest run --project=offline tests/offline/paging/machine.test.ts tests/offline/ui/paging.test.ts te… (301 chars; full text in git history)]
- 2026-09-15T02:28:46Z [builder-322] claimed (attempt 1)
- 2026-09-15T02:52:02Z [builder-322] claimed → built: - DRIVER (src/lib/paging/machine.ts, rule 8): PageState gains `drawnIds` (ids drawn — the first screen's, seeded by the surface, plus every appended row) and `overlapped`; the append drops rows whose id the state holds. `held` still advances by the ROUTE's row count, exhaustion is still `answer.exhausted`/zero rows, refusals unchanged. PageDeps is now `PageDeps<Row>` with a required `id: (row) => string`; `initialPage` takes an optional 4th arg (the screen's ids) — optional so BUG-0221's it.fails pin and ~45 existing call sites stay byte-identical; both pages pass real ids. Blank ids are appended, never collapsed.
- SURFACE (src/components/ui/paging.tsx, PageMore): one `data-paging-overlap` line, role=status, secondary ink, rendered only when `overlapped` — so the first server render is unchanged. Both paged surfaces get it through the same primitive; no data-window-* added.
- OUT OF touch_scope, and deliberate: src/app/claims/page.tsx and src/app/browse/page.tsx (each spells its id fn once for boundOf + initialPage + deps.id), and tests/live/{claims,browse}.live.test.ts (deps gained `id`, walks seed the screen's ids) — the first screen's ids live only on the pages.
- VERIFIED on the rebased tree: the 4 targeted offline files 457/457; red-then-green observed — neutering the dedupe loop reddens 9 of the new cases incl. both surface-level ones; `npm test` 4003 green; tsc, lint, build clean; `npm run test:live` claims 19/19 and browse 7/7 (both walks "none twice, none skipped").
- FOR QA: BUG-0221's pin is untouched and still fails as expected (the dedupe cannot XPASS it — the skipped row is never in `drawnIds`). Known boundary, not fixed here and in BUG-0221's lane: `continuing()` keeps a state when `after` matches, so a first screen that churned ABOVE an unchanged last row leaves stale ids in the set; a row that left that screen and returns in a later page would be dropped. Requires a simultaneous insert+delete above the bound.
- 2026-09-15T02:52:25Z [dispatcher] landed ticket/BUG-0222 onto run/admin-window as feb21a66
- 2026-09-15T02:53:12Z [qa] built → qa: under attack
- 2026-09-15T03:09:41Z [qa] qa → reopened: BUG-0226 filed (P1). Expected (criterion 4): the first server-rendered screen of /claims and /browse unchanged. Found: both answer HTTP 500 on a production build of the landed tree (feb21a66, measured 2026-09-14 against staging, port 8781 + minted cookie) — "Functions cannot be passed directly to Client Components … {route: ..., params: "", size: 50, id: function Z}", digest 3992294335. In a browser /claims is Next's error shell with zero rows and no control. Cause is this diff: src/app/claims/page.tsx:1854 and src/app/browse/page.tsx:1380 hand the "use client" PagingProvider a FUNCTION prop (deps.id). Not markup-visible: renderToStaticMarkup has no client boundary, the http tier has no rows so PagingProvider never renders, the live tier never requests the page — 457/457 offline, tsc, lint, build and both live walks are green over a dead app (LESSONS 10, twin of BUG-0094). Pinned at d7c73420: it.fails("hands the client provider DATA only…") in tests/offline/{claims,browse}/page.test.ts over the value actually handed across (tests/fixtures/client-props.ts); watched red with the marker flipped — `expected [ 'deps.id' ] to deeply equal []` on both.

Attacked otherwise, and the contract HOLDS offline: fully-deduped page stays idle with held +served and keeps the control; a page carrying one id twice is caught by the same rule; blank ids are appended not collapsed; a walk over a population that moves once mid-walk asks 2,4,6, draws a..f distinct and ends exhausted; BUG-0221's pin is untouched and still fails (drawnIds is empty in it, so the dedupe cannot XPASS it). The builder's flagged boundary is NOT worse: with churn above an unchanged last row the drawn run is ["z","b","c","d","e","f"] — never a duplicate; the returning row is a SKIP (BUG-0221's class), and it needs a REORDER, which the list's total order (observed_at asc, observation_id asc) does not produce from an insert or a delete alone.

SECOND FINDING, not filed (lean cap) — requestPage now REJECTS on foreign rows, breaking its own rule 5 and the "Nothing here throws" header (machine.ts:18-21): deps.id(row) runs before any guard, so rows:[null], [{}], [{id:null}] and [42] all raise TypeError instead of refusing. usePageRows does `void requestPage(...).then(publish)` with no catch (paging.tsx:258), so the surface stays `loading` forever — control permanently disabled, no refusal line, unhandled rejection. Extension point is the existing hostile list at tests/offline/paging/machine.test.ts:1591. Evidence: agenticflow/tracker/evidence/BUG-0222/http-500.md.
