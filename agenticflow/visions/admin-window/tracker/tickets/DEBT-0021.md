---
id: DEBT-0021
type: debt
status: done
milestone: M3
priority: P3
opened_by: architect
parent: null
discovered_from: BUG-0200
children: []
assignee: null
claimed_at: null
depends_on: []
touch_scope: [src/lib/db/result.ts, src/lib/db, tests/offline/db/result.test.ts]
force: false
attempts: 1
resolution: fixed
---
# The error arm's `authored` field is optional, so the only guard on it is a regex inside a closed ticket's checks

## Description
M3 endgame ruling (architect, 2026-09-11), from QA's residual on BUG-0200. Recorded in ARCHITECTURE section 10 and LESSONS 12.

WHAT IS TRUE TODAY. Every `kind: "error"` arm this repo builds under `src/lib/db/**` carries an `authored` array (the segments that say which words the DATABASE wrote and which the app did), and BUG-0200 proved it with a perl sweep over `find src/lib/db -name "*.ts"` that walks each `kind: "error"` object and fails any without `authored`.

WHY THAT IS NOT DURABLE, QA's finding, not a guess: the sweep lives ONLY in BUG-0200's stored `## Checks` — a closed ticket — so nothing runs it again, ever. And it is a regex over one spelling: an arm built by spreading a base object, by a helper that returns the union member, or with the key written on another line is invisible to it. The next error arm anyone adds is unguarded and the class comes back.

THE DURABLE FORM. `authored` is declared OPTIONAL on the error member of the result union (`src/lib/db/result.ts:43`, `authored?: readonly AccountSegment[]`). Make it REQUIRED and the type checker becomes the standing runner: every construction site, in every spelling, is checked by `tsc --noEmit`, which already runs on every ticket and in ci. No regex, no sweep, no ticket-local guard.

MEASURED BEFORE FILING (2026-09-11, HEAD aa4051aa): `./node_modules/.bin/tsc --noEmit` exits 0 today, and BUG-0200's own sweep passes over at least 9 sites, so every existing arm already supplies the field — this is a type tightening over code that already complies, not a migration.

TRAPS FOR WHOEVER TAKES IT. (1) The field may be optional because a caller outside `lib/db` constructs the union too; if `tsc` names such a site, the answer is that arm supplying its own segments, never widening the type back. (2) `accountText(authored)` and `message` must stay in step - `message` is derived from the segments, and the two must not become two spellings of one fact (LESSONS 11). (3) Do not delete or weaken any assertion in `tests/offline/db/result.test.ts`; the type change must leave the account's rendered text byte-identical.

## Acceptance criteria
`authored` is REQUIRED on the error member of the `DbResult` union in `src/lib/db/result.ts` - no `?` - so every construction site in every spelling is checked by the compiler rather than by a regex living in a closed ticket's checks. `./node_modules/.bin/tsc --noEmit` exits 0 with the field required, which is the proof that every existing arm already supplies it. The account's rendered text does not change: the offline suites that read it are green unchanged, and no assertion in them is deleted or loosened to make the type fit. `npm run lint` exits 0.

(human-checkable) The point of the change is that the guard now runs on every ticket forever without anyone remembering it; if the required field forces any call site to invent segments it does not have, that is a finding to report rather than a cast to write - `as` and `!` are not the fix here.

## Checks
```sh
./node_modules/.bin/tsc --noEmit
test "$(grep -c 'authored?: readonly AccountSegment' src/lib/db/result.ts)" -eq 0
grep -q 'authored: readonly AccountSegment' src/lib/db/result.ts
./node_modules/.bin/vitest run --project=offline tests/offline/db/result.test.ts tests/offline/db/layering.test.ts tests/offline/edit/records.test.ts tests/offline/verdict/seam.test.ts
npm run lint
```

## History
- 2026-09-11T21:48:39Z [architect] opened: The error arm's `authored` field is optional, so the only guard on it is a regex inside a closed ticket's checks
- 2026-09-15T06:03:32Z [builder-321] claimed (attempt 1)
- 2026-09-15T06:10:52Z [builder-321] claimed → built: Dropped the `?`: `authored: readonly AccountSegment[]` is now REQUIRED on `DbResult`'s error member (src/lib/db/result.ts:54), with the doc comment restating the property positively (LESSONS 13) — the compiler now refuses an unauthored arm at every construction site, in every spelling. NO src call site changed, which is the ticket's own claim proved: all 9+ existing arms already complied. No cast, no `!`, nothing invented.
- Pinned in both directions, repo idiom (tests/offline/edit/config.test.ts:525): new `it("cannot spell an error arm whose account does not say who wrote it")` in tests/offline/db/result.test.ts uses `@ts-expect-error` — an UNUSED directive is TS2578, so restoring the `?` reddens `tsc`. Observed red-then-green: with the `?` put back, tsc exits 2 with 5 errors (3x TS2578 on the pins, 2x TS18048 'possibly undefined'); restored, exits 0.
- OUTSIDE touch_scope, unavoidable and deliberate: tests/offline/paging/answer.test.ts had the only two authorless `DbResult` literals in the repo (lines 62, 157). Each got a `@ts-expect-error` with the reason — NO assertion was deleted, loosened or reworded, so 'an arm that carried no authorship crosses carrying none' is still graded at runtime. src/lib/paging/bounds.ts keeps `authored?` on its wire types ON PURPOSE (BUG-0196 criterion 5c: a pre-BUG-0196 `{kind,reading,message}` body must stay a readable PageAnswer) — this type governs what the app CONSTRUCTS, not what it ACCEPTS. Widening the wire validator would be the wrong fix.
Verified on the REBASED tree (af108ac5 on 1f8c7866): tsc --noEmit 0; both grep checks 0; vitest --project=offline over the 4 files in Checks + paging/answer.test.ts = 303 passed; npm run lint 0; npm run build 0. Full `npm test` once pre-rebase: 81 files / 4059 tests passed. No live test run — the diff has zero runtime effect in src (one `?` and a comment; nothing emitted changes).
Trap for QA: the worktree has a `node_modules` symlink to the primary checkout (gitignored) — without it no binary resolves there.
- 2026-09-15T06:11:15Z [dispatcher] landed ticket/DEBT-0021 onto run/admin-window as 1c4d306a
- 2026-09-15T06:12:09Z [qa] built → qa: under attack
- 2026-09-15T06:16:47Z [qa] qa → done: Attacked on the landed tree (47257c39, DEBT-0021 merged at 1c4d306a); criteria re-run before reading the builder's story. Stored checks all exit 0 (receipt GREEN): tsc --noEmit 0, both greps 0, the 4-file offline run 293 passed, lint 0; tests/offline/paging/answer.test.ts also 0.

TSC IS REALLY THE STANDING RUNNER (measured on a detached scratch worktree under evidence/, removed after; primary tree never touched). (1) Put the `?` back: tsc exits 2 with exactly 5 errors — TS2578 unused-directive at result.test.ts:2551, answer.test.ts:62 and :157, plus 2x TS18048 at result.test.ts:2569/2570. The pin is bidirectional and I watched it red. (2) Wrote a probe under src/lib/db in SIX spellings and every one is refused: bare literal returned from a DbResult-annotated fn (TS2322), spread of a base object (TS2322), intermediate `const` with `kind: "error" as const` (TS2322), helper returning Extract<DbResult,{kind:"error"}> (TS2741), contextually-typed array element (TS2322), property of an object typed with the union (TS2322); `authored: undefined` also refused (TS2322). So the class is closed in spellings the BUG-0200 regex could never see. Scope of the runner checked, not assumed: tsconfig include is repo-wide `**/*.ts` minus node_modules/scripts/agenticflow/tests/.probes — tests/live and tests/http are type-checked too. No `@ts-expect-error`, `@ts-ignore` or `as DbResult` anywhere in src/, so no site silences the requirement.

WIRE TOLERANCE INTACT. src/lib/paging/bounds.ts:181 and :242 still spell `authored?: readonly AccountSegment[]` on PageWithoutRows/PageNote (BUG-0196 criterion 5c), and a narrowed required-authored DbResult error arm compiles as an assignment to BOTH — measured in the same probe, zero errors on those two lines. The strict construct-type did not narrow what the app accepts.

OUT-OF-SCOPE DIRECTIVES LOOSENED NOTHING: git numstat for the ticket commit is tests/offline/db/result.test.ts 37/0 and tests/offline/paging/answer.test.ts 8/0 — zero deletions, so no assertion was dropped or reworded; a directive is a comment with no runtime effect and both answer.test.ts cases still execute green. The new pin is not skipped: -t 'cannot spell an error arm whose account does not say who wrote it' → 1 passed | 151 skipped.

BUG-0200's SWEEP IS REDUNDANT, NOT CONTRADICTED: re-ran that closed ticket's perl sweep verbatim on this tree — exit 0, 9 sites, 0 UNAUTHORED. It agrees with the type; it is simply no longer the only guard.

Rendered text unchanged: the src diff is one `?` and a doc comment, both erased at emit.

RESIDUAL (not filed, lean depth, no criterion-driven path reaches it): a required field constrains presence, not length. `authored: []` compiles, and isAccountSegments([]) returns true, so an empty run list passes the wire validator; ErrorLine falls back only on `?? []` for ABSENT, so with `authored: []` the database's words vanish from the surface — measured markup 'pending_claims — connection refused' (absent) vs 'pending_claims' (empty), retry sentence only. Pre-existing and equally open under BUG-0200's regex (which also only checked key presence); no src site constructs an empty list and the wire's only producer is this app. Becomes a ticket if any arm ever composes segments conditionally. [resolution: fixed] [receipt green: 5 cmd(s), tree 48125698]
