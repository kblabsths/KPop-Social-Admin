---
id: DEP-0002
type: dep
status: blocked
milestone: M1
priority: P2
opened_by: toolsmith
parent: null
children: []
assignee: null
claimed_at: null
depends_on: []
touch_scope: [agenticflow/docs/SERVICES.md]
force: false
---
# Service: Railway (existing deploy target of this app)

## Description
Ben asked for the Railway service vet to start ahead of any ticket needing it. This is an EXISTING-SERVICE DECLARATION vet, not a new dependency: Railway is already this app's deploy target (railway.toml at the repo root: nixpacks builder, buildCommand 'npm run build', startCommand 'npm run start', healthcheckPath '/api/health'), deploying from main via Railway's GitHub integration. Nothing is being provisioned and nothing is being installed. The question on the table is narrow and mechanical: should agenticflow/docs/SERVICES.md carry a '## railway' section, given that .claude/hooks/remote_gate.py refuses the 'railway' CLI while it is absent and passes every railway command freely once it is present? Scope of the vet: (1) what agents in the admin-window campaign would use the railway CLI for at all, against contracts/admin-build.md's ground rule that the deployed service is never repointed and the campaign never deploys; (2) the CLI's identity and health if it were ever installed (@railway/cli on npm - registry identity, maintainers, provenance, advisories, install-script behavior; NOT installed by this vet, and note the CLI is already present on this machine at /opt/homebrew/bin/railway, so allowlisting is not the operative control - the SERVICES.md declaration is); (3) the staging-vs-production boundary - this repo's Railway service targets the PRODUCTION Supabase project via its own service-side env (SUPABASE_URL / SUPABASE_SERVICE_ROLE_KEY), while all campaign work targets kspace-staging via STAGING_SUPABASE_*, so a declaration must not leave any agent believing the deployed service is a staging target; (4) the cost ceiling. The toolsmith cannot edit SERVICES.md (human-owned) - this ticket ends by handing Ben the section text to paste, or the recommendation not to.

## Acceptance criteria
The ticket's History carries: a necessity finding for the railway CLI in this campaign; the @railway/cli identity/health/advisory/install-script evidence with sources marked [web-sourced: <url>]; an explicit staging-vs-production statement naming what the deployed service actually points at and what the remote gate can and cannot enforce about it; a cost-ceiling line checked against agenticflow/docs/SERVICES.md; and a DRAFT section text for Ben to paste verbatim (or a reasoned recommendation not to paste it). No secret value appears anywhere in the ticket. agenticflow/docs/SERVICES.md is NOT edited by the toolsmith - the ticket ends in blocked, waiting on Ben's declaration decision, and only Ben's own commit may add a '## railway' heading.

## Checks
```sh
bash -c "grep -q 'web-sourced' agenticflow/tracker/tickets/DEP-0002.md"
bash -c "grep -q 'DRAFT SECTION' agenticflow/tracker/tickets/DEP-0002.md"
bash -c "! git log --format=%an -- agenticflow/docs/SERVICES.md | head -1 | grep -qi toolsmith"
```

## History
- 2026-09-02T16:24:44Z [compacted → agenticflow/tracker/notes/DEP-0002.md]
  Summary (state; attempts; DEAD ENDS preserved):
  COMPACTED 2026-09-02 by compactor. Raw History preserved verbatim at agenticflow/tracker/notes/DEP-0002.md.
  
  ## State
  Vet COMPLETE (toolsmith, 2026-09-02, sections 1/5-5/5). Nothing installed, nothing provisioned, no `railway` command ever executed (the gate refuses it today). agenticflow/docs/SERVICES.md was NOT edited by the toolsmith - human-owned; only Ben's own commit may add a '## railway' heading. Blocked on BEN and nothing else: his declaration decision (paste OPTION A, OPTION B, or neither). Nothing in M1 waits on this ticket; no work role should be dispatched to it.
  
  Dispatcher state note (2026-09-02, open -> blocked), verbatim: "Blocked on Ben's declaration decision (toolsmith vet 2026-09-02): recommend Option A - record Railway as a '### railway (deliberately NOT declared)' block; nothing in M1 needs the CLI, and a '## railway' declaration would let the installed CLI reach the production deploy unfenced. No work role should be dispatched to this ticket."
  
  Close-out rule agreed by the toolsmith: resolution `no_change` if Ben takes OPTION A (no '##' declaration made); if a human edit lands OPTION B it closes `fixed`. The toolsmith could not set status itself - ticket.py's TRANSITIONS gives open->blocked to builder/architect/qa/dispatcher only, and toolsmith edges are open->done / open->wont_fix.
  
  VERDICT (verbatim): "legitimate service, real and already ours, correctly identified, no advisories, no new cost - and NOT recommended for declaration in M1. Necessity is zero (1/5), and declaration is an unfenced grant over a production surface (3/5). Recommendation: paste OPTION A now, and paste OPTION B only on the day a ticket states a railway command it needs and Ben decides that command is worth the grant."
  
  ## Tried
  - 1/5 NECESSITY - audited every campaign use of Railway against contracts/admin-build.md; finding: nothing in M1 needs the railway CLI.
  - 2/5 IDENTITY/HEALTH - read-only npm registry + GitHub API checks on @railway/cli; clean, and not the operative control anyway (the CLI is already on PATH).
  - 3/5 STAGING-VS-PRODUCTION BOUNDARY - what the deployed service points at and what remote_gate.py can and cannot enforce; the decisive section.
  - 4/5 COST CEILING - $0 incremental; the service already exists and already bills.
  - 5/5 DRAFT SECTION - verdict plus the two paste-ready texts below.
  
  NECESSITY FACTS (1/5): deploys happen by `git push` to `main` via Railway's GitHub integration reading railway.toml (nixpacks; build `npm run build`; start `npm run start`; healthcheck `/api/health`) - the CLI buys nothing the push already does. contracts/admin-build.md ground rule: "The deployed Railway service is never repointed by this campaign, and every push to `main` must leave the app deployable against whatever project the service targets". M1 EC13 grades "the deployed Railway service was never repointed"; SPEC.md 54 and 98 and STACK.md section 6 ("Deploy (untouched by this campaign)") repeat it. EC1-EC14 are graded locally or against staging - none names a deploy, log pull, env-var read or restart. The only check command in the corpus containing the word is TASK-0005's `bash -c "grep -q healthcheckPath railway.toml"`, a file read; .claude/hooks/remote_gate.py parses the head token of each pipeline segment (`_cmd_word` / `remote_segments`), so grep/cat on railway.toml is not a CLI invocation and does not trip the gate.
  
  PACKAGE FACTS (2/5): the CLI is ALREADY on this machine at /opt/homebrew/bin/railway -> ../Cellar/railway/5.9.1/bin/railway (Homebrew, pre-factory), and is NOT an npm dependency of this repo (no node_modules/@railway; package.json never mentions it) - so ALLOWED_DEPS.md and the supply gate are NOT the control; the absence of a '## railway' section in SERVICES.md is the only thing between an agent and the live deploy.
  - `@railway/cli` [web-sourced: https://registry.npmjs.org/@railway/cli] - repository git+https://github.com/railwayapp/cli.git; maintainers jakerunzer, jakecooper (Railway's founders/early team); ISC on the npm manifest, MIT on the repo. Registry -> repo link verified [web-sourced: https://api.github.com/repos/railwayapp/cli]: full_name railwayapp/cli, homepage https://railway.com, Rust, 600 stars / 189 forks, archived false, created 2020-10-01, pushed 2026-09-02. Low star count for a vendor CLI; adoption evidenced by the vendor, not stars.
  - Latest dist-tag 5.47.2 [web-sourced: https://registry.npmjs.org/@railway/cli/latest]; a search index reported 5.45.10, same 5.x line, registry authoritative. The local Homebrew build 5.9.1 is the same major, many minors behind.
  - ADVISORIES: none. GitHub Advisory Database query for npm/@railway/cli returned no advisories [web-sourced: https://api.github.com/advisories?ecosystem=npm&affects=%40railway%2Fcli]. No CVE found.
  - INSTALL SCRIPT: 5.47.2 scripts.postinstall = "node ./npm-install/postinstall.js"; deps tar ^6.1.11, node-fetch ^3.1.0; bin railway -> bin/railway.js; unpackedSize 10,633 bytes across 6 files - a downloader shim for the Rust binary. The shim [web-sourced: https://raw.githubusercontent.com/railwayapp/cli/master/npm-install/postinstall.js] fetches a per-platform tar.gz from "https://github.com/railwayapp/cli/releases/download/v{{version}}/railway_{{version}}_{{platform}}_{{arch}}.tar.gz", unpacks it, checks the binary exists. NO checksum and NO signature verification, and it does not execute what it downloaded - industry-normal esbuild-shaped pattern, stated plainly: installing executes a network fetch at install time and trusts GitHub Releases as the integrity boundary. Positive signal: 5.47.2 published by GitHub Actions via npm trusted publishing / OIDC (npm-oidc-no-reply@github.com, npm 11.17.0). Tripwire: this repo already carries install scripts on sharp and unrs-resolver, plus esbuild once vitest resolves - @railway/cli would be a FOURTH.
  - TYPOSQUAT NEIGHBOURHOOD, clean: unscoped `railway` on npm [web-sourced: https://registry.npmjs.org/railway] was RailwayJS, a 2012-era Express MVC framework by `anatoliy`; the same two Railway maintainers now co-own it, latest 3.11.0, recent versions carry a deprecation notice pointing at @railway/cli. The most plausible fat-finger name is vendor-controlled, not squattable - do NOT block it. Adjacent names seen in search and NOT vetted, NOT wanted: `@techtrain/cli-railway`, `akasia-dev/railway-cli`.
  - RUN-TIME: an authenticated remote-control tool reading a token from the user's home config; surface includes `up` (deploy), `redeploy`, `down`, `link` (repoint), `variables` (PRINTS SECRET VALUES), `run` (execute locally with the service's env injected), `add` (provision billable services). Nothing about it is read-only by default.
  
  BOUNDARY FACTS (3/5, decisive): the deployed service reads SUPABASE_URL / SUPABASE_SERVICE_ROLE_KEY (src/lib/db/client.ts; TASK-0002 pins those names precisely because the service is never repointed), which on Railway resolve to the PRODUCTION Supabase project - the shared live catalog, recorded in SERVICES.md as nexuvegwukhyhrktxhha and stated there to be NO LONGER a test target. Campaign work uses STAGING_SUPABASE_URL / STAGING_SUPABASE_SERVICE_ROLE_KEY against kspace-staging. Per the dispatcher note on TASK-0021 (2026-09-02), Ben removed the old SUPABASE_* names from the LOCAL .env while the deployed service keeps reading them. Net: the running Railway service is a PRODUCTION surface holding a service-role key that bypasses RLS on the live catalog. No staging Railway environment exists and none is proposed, so SERVICES.md's "staging target agents may touch:" line has exactly one truthful value for railway: NONE.
  - WHAT THE GATE ENFORCES (.claude/hooks/remote_gate.py, read 2026-09-02): `declared_services()` collects the first word of every line starting with "## "; `railway` is in REMOTE_CLIS. Undeclared -> every railway invocation refused. Declared -> every railway invocation PASSES SILENTLY. No per-command, per-environment or read-only granularity: declaration is a binary switch opening `up`, `down`, `redeploy`, `link`, `variables`, `run` and `add` at once.
  - WHAT IT CANNOT: the production fence is TEXTUAL ONLY - PROD_RE searches the command's own tokens for prod/production. Railway creates every project with an environment named `production` by default [web-sourced: https://docs.railway.com/environments - "All projects in Railway are created with a production environment by default"], and the CLI acts on the LINKED environment when none is named. So `railway up`, `railway redeploy`, `railway logs` and `railway variables` carry no prod/production token, sail past PROD_RE, and hit production anyway. The fence catches the honest command (`railway up -e production`) and misses the dangerous default (`railway up`). Not a bug anyone is asked to fix - it is why declaration cannot be made safe by prose.
  - CROSS-REPO CHECK: the sibling `kspace Scraper` repo's agenticflow/docs/SERVICES.md (read read-only, 2026-09-02) declares `## supabase`, `## cloudflare-r2` and `## ticketmaster` and has NO railway section, though that repo also deploys to Railway; its cloudflare-r2 entry records "no CLI (wrangler is not installed and not needed)" and "The production bucket ... is reachable only from Railway and is never a target". The sibling's shape for a Railway-hosted production surface: record the boundary, do not hand agents the CLI.
  
  COST LINE (4/5, checked against agenticflow/docs/SERVICES.md): no new spend, and no agent action in M1 can create any while the CLI stays out of reach; the service already exists and already bills. Railway's paid entry tier is Hobby at $5/mo including $5 of usage credit, metered per second at $20/vCPU-mo, $10/GB RAM-mo, $0.05/GB egress, $0.15/GB-mo volume [web-sourced: https://docs.railway.com/pricing/plans and https://railway.com/pricing via search snippets, 2026-09-02]. Exact plan and current spend are Ben's facts to fill in - the toolsmith has no billing access. Ceiling vetted against: the existing plan, $0 incremental; any DEP adding a Railway-billed resource (new service, Railway Postgres/Redis plugin, extra environment, bumped replica count) is refused - none requested. Spend paths the CLI opens: `railway add` provisions billable services, `railway up`/`redeploy` burn build minutes and can loop, a duplicated environment doubles the running footprint. Non-declaration is also the cheapest cost control. Railway's free/trial credit is one-time and this project is past it; nothing here is a "free tier".
  
  DRAFT SECTION - the two paste-ready texts, verbatim for Ben. The toolsmith cannot edit agenticflow/docs/SERVICES.md; both are for Ben to paste, or not.
  
  --- OPTION A (RECOMMENDED): record the facts WITHOUT opening the gate ---
  Paste at the end of agenticflow/docs/SERVICES.md, verbatim. The heading is deliberately "###", not "##": remote_gate.py's declared_services() only reads lines beginning with "## ", so this records the service for humans and leaves the railway CLI refused. That is the intent, stated out loud rather than hidden.
  
  ### railway (recorded, deliberately NOT declared - the CLI stays refused)
  - what it is for: the deploy target of this app. Railway builds from `railway.toml`
    (nixpacks; build `npm run build`; start `npm run start`; healthcheck `/api/health`)
    and deploys from `main` through the GitHub integration. Pushing to `main` IS the
    deploy; no CLI is involved.
  - console: https://railway.com/dashboard  <-- Ben: replace with the project URL
  - tier / cost ceiling: <Ben: plan and $/mo>. Ceiling for DEP purposes: the existing
    plan, $0 incremental. Anything that adds a billable Railway resource (new service,
    Postgres/Redis plugin, extra environment) is a new DEP and is refused by default.
  - staging target agents may touch: NONE. There is no Railway staging environment.
    The deployed service is a PRODUCTION surface: it reads SUPABASE_URL /
    SUPABASE_SERVICE_ROLE_KEY (service-side names, never the STAGING_* ones) and
    therefore holds a service-role key on the live catalog. Campaign work targets
    kspace-staging only.
  - CLI status: NOT declared, on purpose (DEP-0002, 2026-09-02). `railway` is on this
    machine via Homebrew (5.9.1), so the remote gate's refusal is the only control.
    Agents run no railway command; deploys happen by `git push` to `main`.
  - provisioned by: Ben, pre-factory
  
  --- OPTION B: the full declaration, ONLY if Ben decides agents need the CLI ---
  Pasting this makes EVERY railway command pass silently, including `railway up`,
  `railway link`, `railway redeploy` and `railway variables`. Read the two warnings
  in the section before pasting; they are the only fence that exists.
  
  ## railway
  - what it is for: the deploy target of this app - Railway builds from `railway.toml`
    (nixpacks; build `npm run build`; start `npm run start`; healthcheck `/api/health`)
    and deploys from `main`. Pushing to `main` is the deploy; the CLI is for reading
    deploy state only.
  - console: <Ben: https://railway.com/project/...>
  - tier / cost ceiling: <Ben: plan, $N/mo> - the toolsmith refuses any DEP that adds a
    billable Railway resource (new service, database plugin, extra environment).
  - staging target agents may touch: NONE - there is no Railway staging environment.
    The only environment is the default, which Railway names `production`, and it runs
    the live dashboard against the PRODUCTION Supabase project via its own
    SUPABASE_URL / SUPABASE_SERVICE_ROLE_KEY. Nothing about this service is a test
    target. <Ben: if you later create a staging environment, name it here and it
    becomes the one an agent may touch.>
  - WARNING 1 - the prod fence does not cover the default: the gate only refuses
    commands whose text contains prod/production. `railway up`, `railway redeploy` and
    `railway logs` act on the LINKED environment - the one Railway named `production` -
    without the word appearing. Read-only inspection (`railway status`, `railway logs`)
    is the ONLY sanctioned use; `up`, `down`, `redeploy`, `link`, `add` and `run` are
    out of bounds for every agent in this campaign (contracts/admin-build.md: the
    deployed service is never repointed; M1 EC13 grades it).
  - WARNING 2 - never `railway variables`: it prints secret VALUES to stdout, and
    stdout here is a persisted transcript. Running it burns the production service-role
    key and forces a rotation. Read variable NAMES from `.env.example`.
  - provisioned by: Ben, pre-factory (declared <date> after DEP-0002)
  
  --- notes attached to both options ---
  - ALLOWED_DEPS.md gets NO entry from this ticket; BLOCKED_DEPS.md gets none either (see DEAD ENDS for both, incl. why unscoped `railway` must NOT be blocked and that `railway-cli` / `@railway-cli/cli` are the squattable shapes worth blocking if anything is ever installed in this space).
  - No near-name / [near-ok] marker is proposed: no allowlist entry is within typo distance of `railway`, because no railway package is being allowlisted.
  ## DEAD ENDS
  - Declaring `## railway` now - REJECTED: unfenced CLI access to a production surface for capability nobody asked for. Today `railway link`/`up` (repoint or redeploy the live service, exactly what EC13 grades as never having happened) and `railway variables` are impossible by construction; after declaration both are one plausible agent command away.
  - Filing the declaration "so it is there when we need it" - REJECTED: buys zero velocity and spends a real fence.
  - Relying on PROD_RE to fence a declaration - REJECTED: the default environment is literally named `production` and the CLI acts on the linked environment without the word appearing, so the textual fence misses the dangerous default.
  - Relying on prose warnings inside a declared section - REJECTED: declaration is binary with no per-command granularity; once declared the only defense left is that the agent read the prose.
  - Making a declaration safe by naming a staging Railway environment - DEAD: none exists. The staging story that makes `## supabase` safe has no Railway equivalent; provisioning one is Ben's to do, not the factory's, and is not needed for M1.
  - Filling "staging target agents may touch:" with any project or environment name - REJECTED as false: the only truthful value is NONE, and a false line in the one document an agent reads to learn what it may touch is worse than no line.
  - Adding `@railway/cli` to ALLOWED_DEPS.md / treating allowlisting as the control - DEAD END: the binary is already on PATH, so nobody has to install anything to run `railway`; the SERVICES.md declaration is the operative control. It would also be this repo's FOURTH install-script package.
  - Adding unscoped `railway` to BLOCKED_DEPS.md - REJECTED: vendor-owned name (see above).
  - Using `railway variables` to read service env - FORBIDDEN, declared or not: it prints secret VALUES to stdout, and stdout here is a persisted transcript, so running it burns the production service-role key and forces a rotation. Read variable NAMES from `.env.example`. Reading or setting service env vars is separately forbidden by the never-repointed rule.
  - Reading deploy logs after a bad push - a real future want, but M2/close-out at the earliest, and `railway logs` is exactly the command whose environment defaults to the live one.
  - Upgrading the local Homebrew 5.9.1 binary toward 5.47.2 - not the toolsmith's call; recorded only so nobody assumes the local binary matches the current release.
  - The toolsmith setting this ticket's status itself - no legal transition existed (see State); the dispatcher flipped open->blocked instead.
