---
id: TASK-0014
type: task
status: built
milestone: M1
priority: P1
opened_by: architect
parent: FEAT-0005
children: []
assignee: builder-47
claimed_at: 2026-09-02T15:42:53Z
depends_on: [TASK-0005, TASK-0007, TASK-0008, TASK-0026, TASK-0030]
touch_scope: [src/app/cycles/page.tsx, src/lib/db/cycles.ts, tests/offline/cycles, tests/live/cycles.live.test.ts]
force: false
attempts: 2
---
# Cycles & runs — the resolver's cycles, newest first, with their counts and errors

## Description
Module: `src/app/cycles/page.tsx` + `src/lib/db/cycles.ts` — **the `resolution_runs` half only**. Authority: spec section 4 (Cycles & runs) and section 5; `contracts/resolver.md` section 6 (the `resolution_runs` row and every column's meaning); SPEC F5; acceptance tests 2, 9, 11; LOOK_AND_FEEL taste reference 'GitHub Actions run list — a run diagnosed from its row'.

- `resolution_runs` **newest first**, with the counts as columns: `facts_examined`, `applied`, `held`, `escalated`, `entities_created`, `claims_linked`, `claims_rerejected`, `errors`, and `error_summary` **inline**, verbatim, in mono.
- **A cycle still running (`ended_at` null) and a `skipped` cycle are each legible as such.** `outcome` is null until completion by design, and an old row with a null `ended_at` is a cycle that died — nothing repairs it, so render it as what it is rather than as 'running' forever (migration `20260901000001`'s own words).
- The **cycle-health** and **resolution-latency** gauges (TASK-0007) render on this page.
- **The adapter framework's `runs` are NOT rendered by this ticket.** That half is TASK-0015, which waits on a blocked question. Leave a clearly named seam — a section that renders nothing — and do not guess at its columns.

Standing rules for every page ticket (ARCHITECTURE sections 4, 5, 7): the page function is the only async component on the route and every child is a pure sync component; read only through `src/lib/db/**`; table names live only in `tables.ts`; render all four states from the `ui` primitives (a missing table never throws and never renders as 0); tokens and primitives, never raw values; every filter, sort and tab is a `searchParams` value; ages relative with the absolute in the title attribute, scheduled times absolute UTC with the zone in the column header, machine identifiers verbatim in mono; **nothing settles anything in M1**.

- **Every latency figure on this page is the AGGREGATE's, never `rows.length`.** `ResolutionLatencyRows.applies` is the raw window — applies AND verdict unsets, exactly as the read returned them — while `ResolutionLatency.applies` is a COUNT of the applies among them, with the unsets excluded and reported separately as `verdictUnsets` (`src/lib/gauges/resolution-latency.ts:63-72` and `:90-124`; BUG-0012). The two spellings are deliberately not the same population, so `rows.applies.length` over-counts by exactly the unsets. Render `readResolutionLatency`'s output — `applies`, `verdictUnsets`, `unmatchedApplies`, `overall`, `byDomain` — and never re-derive a figure from a row array. A domain whose window held only unsets is listed with `applies: 0` and a latency of nulls; that zero is a measured count and `verdictUnsets` is what explains it, so show both or the zero reads as a defect in the resolver.
- A count from a truncated window is a floor and says so (`window.truncated` -> `GaugeCard`'s `floor`); it is never presented as a total.

## Acceptance criteria
[tests 2, 9, 11] Offline tests render the page against fixtures and assert: cycles are newest first by `started_at`; all eight counts and `error_summary` render; a running cycle, a `skipped` cycle and a completed cycle are each distinguishable in the markup; the counts render as thousand-separated figures. A live-marked parity test asserts the rendered cycle rows and their newest-first order against a query the test issues itself. The cycle-health and resolution-latency gauges render. The adapter-runs section renders nothing and names no column. The not-provisioned state names `resolution_runs`.

## Checks
```sh
npm run lint
./node_modules/.bin/tsc --noEmit
npm run build
./node_modules/.bin/vitest run tests/offline/cycles
bash -c "grep -q claims_rerejected src/lib/db/cycles.ts"
bash -c "grep -q readCycleHealth src/app/cycles/page.tsx && grep -q readResolutionLatency src/app/cycles/page.tsx"
```
## History
- 2026-09-02T12:45:33Z [compacted → agenticflow/tracker/notes/TASK-0014.md]
  Summary (state; attempts; DEAD ENDS preserved):
  State: open, never claimed, never built; no test has ever run for it, so there is no failing-test record. Only the TASK-0005 route shell exists (`src/app/cycles/page.tsx`, 17 lines, one `Empty`); `src/lib/db/cycles.ts` and `tests/offline/cycles` do not exist. depends_on grew twice to [TASK-0005, TASK-0007, TASK-0008, TASK-0026, TASK-0030] — TASK-0026 (complete reads) and TASK-0030 (the typed four-state contract), each decided once because parallel builders cannot see each other's code. Checks were amended twice; the Checks block in the body is the current, authoritative set. The two gauge exports this page must mount were measured present in lib and absent from the page: `readCycleHealth` (`src/lib/gauges/cycle-health.ts:213`) and `readResolutionLatency` (`src/lib/gauges/resolution-latency.ts:216`), each documented in its own module as "what a page calls". QA's BUG-0012 residual was promoted out of History into the Description on 2026-09-02, with the file:lines for both shapes of `applies` — obey it there.
  
  Binding text carried verbatim:
  
  [dispatcher 2026-09-02, relayed from QA on TASK-0009] this route is still o static in the build route table. Add export const dynamic = 'force-dynamic' (or a request-time API) when it gains a read, or it prerenders at build with no credential and ships a frozen error page; the Dashboard verified f.
  
  [Architect ruling, 2026-09-02 — read kinds, now ARCHITECTURE.md section 4.3. Cited on every ticket that owns a `src/lib/db/*.ts` module.]
  QA found `src/lib/db/review-items.ts` (TASK-0006) building its list query with no `.range()`, no `.limit()` and no `.order()`. Against PostgREST's `db-max-rows` (Supabase default 1000) that truncates silently, in unspecified order — so a count or an "exactly the matching items" claim built on it would be wrong rather than refused. You are one of eight builders about to write a `lib/db` module without seeing each other's code; the bound is decided once, in TASK-0026 (now a dependency), not eight times. Two read kinds. Pick deliberately; do not blur them. 1. **Complete read — `readComplete(missing, run, db?)`, new in `src/lib/db/result.ts`.** Use it whenever the surface presents the result as the whole set: a list the page shows in full, a count, an oldest age, an "exactly the matching items" claim. The `run` callback passes `{ count: "exact" }`, a total `.order()` ending in the primary key, and `.range(0, cap - 1)` with the cap handed in. If the exact count exceeds the rows returned, it returns `kind: "error"` naming the object, the count and the cap. **An `ok` array from a complete read is the whole matching set** — that property is what every figure in this app rests on, and it is why no caller needs a "was this all of it?" flag. 2. **Window read — `readRows`, unchanged.** The caller's own `.order()` + `.limit()` define a named window, and the surface says which window it is showing. This is section 8's gauge contract and TASK-0007's landed gauges use it correctly. A window read's rows must never become a figure presented as a total. Also: no read helper may substitute a number the database did not give. `readCount` returned `count ?? 0` — BUG-0007's user-visible twin — and TASK-0026 makes it refuse a null count instead. Do not reintroduce a `?? 0` on any read path.
  
  [Architect ruling, 2026-09-02 — the four data-surface states are now a TYPED contract (TASK-0030, ARCHITECTURE section 7, DECISIONS same date). Code against these signatures; do not work around them.]
  1. **`reading` is REQUIRED on `ErrorLine` and on `GaugeState`'s error arm.** QA's residual on BUG-0016 was right: an optional prop is a rule TypeScript cannot enforce, and the omission it permits is a red line that names none of the page's reads. `DbResult`'s error arm carries `reading: string` already (`src/lib/db/result.ts:14`), so pass `result.reading` straight through on every error arm you render. Do NOT invent a string, do not pass `""` to satisfy the compiler, and do not trim the message: a transport failure's account runs ~900 chars including stack frames, and whether that is too long is the designer's call at the walk, not yours. 2. **A state card that replaces a labelled surface keeps its eyebrow.** `Empty` and `NotProvisioned` take an optional `micro` eyebrow; the three gauge components always pass their own `label`, so an unprovisioned gauge still says which knob it is and not just which table is missing. On your page's own `Empty`/`NotProvisioned` the eyebrow is optional — the `Section` heading above it already names the surface — so only pass it where there is no heading. 3. **A rows surface has no headers-only rendering.** `TrendTable` and `Distribution` take a REQUIRED `empty: { holds, filledBy }` and render it themselves when there are no rows and no other state. The ruling on who owns the empty state: **the component owns WHEN, you own THE WORDS.** Write them in the app's voice, naming what the series holds and the one thing that fills it (LOOK_AND_FEEL Voice bar 4) — never "No data". An explicit `state` still wins, so pass your own empty state where the emptiness has a REASON (a filter that matched nothing) rather than being simply no rows yet. For you specifically: the resolution-latency gauge's rows are the trap BUG-0012 found — render the AGGREGATE's counts, never `rows.applies.length` (now in your description, with the file:lines). Both gauges here are empty against a database whose resolver has not run, which is the state your reviewer will see first.
  
  Tried: (no build attempt exists; the History is briefs, edges and amendments)
  - 2026-09-02 checks amendment 1: swapped an absence check for a presence check — the absent string is one a LATER, correct ticket legitimately adds, so the original would have reddened this ticket's receipt re-check on unrelated correct work (criteria rot).
  - 2026-09-02 checks amendment 2: replaced the module-path greps with greps for the landed page-facing exports named in State. Checks 1-5 unchanged.
  - 2026-09-02 description amendment: the BUG-0012 residual promoted from History into the Description as two bullets with the file:lines. No scope or criteria change.
  
  DEAD ENDS:
  - `grep -q cycle-health src/app/cycles/page.tsx` / `grep -q resolution-latency ...` as checks — DEAD, same defect as TASK-0013's (Common violations 5): they pin module-path spellings that a page importing from the `@/lib/gauges` barrel never contains. Use the export-name greps in the Checks block; do not reinstate the paths.
  - The original absence-string check — DEAD: criteria rot; a later correct ticket adds that string and would redden this receipt.
  - `rows.applies.length`, or any figure re-derived from a row array — DEAD (BUG-0012): `ResolutionLatencyRows.applies` is the raw window INCLUDING verdict unsets while the aggregate's `applies` counts only applies, so the row length over-counts by exactly the unsets.
  - `readCount`'s `count ?? 0` — DEAD: BUG-0007's user-visible twin; TASK-0026 makes the helper refuse a null count. No `?? 0` on any read path.
  - An unbounded list query (no `.range()`, no `.limit()`, no `.order()`) — DEAD: found by QA in `src/lib/db/review-items.ts` (TASK-0006); PostgREST's `db-max-rows` truncates it silently, in unspecified order.
  - An optional/invented/empty/trimmed `reading` on an error state — DEAD (BUG-0016 residual), see ruling 1.
  - A headers-only `TrendTable`/`Distribution` ("rows: [] with no state"), and the words "No data" — DEAD, see ruling 3.
  - Leaving this route static — DEAD: it prerenders at build with no credential and ships a frozen error page (TASK-0009 relay above).
- 2026-09-02T15:05:11Z [dispatcher] Relayed from builder on TASK-0013 (seam): the Sources page links each source to /cycles?source=<name> (runs.source is text with no FK). Spell the Cycles narrowing facet exactly source.
- 2026-09-02T15:06:10Z [builder-44] claimed (attempt 1)
- 2026-09-02T15:24:26Z [builder-44] claimed → built: - `src/lib/db/cycles.ts` (new): WINDOW read of resolution_runs (order started_at desc + run_id desc, limit 200 clamped to ROW_CAP, `truncated` when it fills); `CYCLE_COUNTERS` = the eight counts in resolver.md §6 order (the page's columns and the tests iterate that one list); `cycleState` = the four readings migration 20260901000001 demands — outcome verbatim / running / died (null ended_at older than one 15m cadence) / unrecorded. Window read, not complete: the table grows forever at 96 rows/day and no figure is computed from rows.length (dashboard.ts's precedent, §4.3 kind 2); the page states the window.
- `src/app/cycles/page.tsx`: all 8 counts as columns (thousand-separated) + error_summary inline verbatim, `dynamic = "force-dynamic"` (route is ƒ in the build table), `?cycle=<run_id>` marks the row and says so when the id is not in the window. Gauges via readCycleHealth / readResolutionLatency; every latency figure is the AGGREGATE's (`applies` 3, `verdictUnsets` 2 over a 5-row fixture window — BUG-0012 pinned in tests/offline/cycles/page.test.ts). Adapter-runs seam renders NOTHING (returns null, no heading, no column, `runs` never read) — OPEN-RUNS unanswered; `?run=` and `?source=` are carried but unconsumed for that reason, documented at CYCLE_FACET.
- Verified on the rebased tree (9ab6893 onto run/admin-window): lint 0, tsc 0, build 0, `vitest run tests/offline/cycles` 41 pass, full `npm test` 46 files / 1145 pass. `npx vitest run --project live tests/live/cycles.live.test.ts` refuses by name (STAGING_SUPABASE_URL) — today's correct state; not walked (no service-role key in .env, STACK §5).
- Traps for QA: one real defect found and fixed mid-build — an outcome cell returning a `<Component/>` element renders BLANK, since DataTable's orDash sees a body; `stateCell` is a plain function returning `null` so the dash renders (pinned). Fixtures are clock-relative on purpose (running must sit inside the cadence, died outside). Nothing outside touch_scope was edited.
- 2026-09-02T15:24:56Z [dispatcher] landed ticket/TASK-0014 onto run/admin-window as 0c818df
- 2026-09-02T15:25:49Z [qa] built → qa: under attack
- 2026-09-02T15:31:02Z [qa] qa → reopened: BUG-0023 filed. Attacked on the landed tree (run/admin-window @ c102991), all six stored checks re-run VERBATIM and green: lint 0, tsc 0, build 0 (/cycles is f in the route table, not o), vitest tests/offline/cycles 41/41, both greps 0.

CRITERIA — all met, verified myself before reading the handoff. Eight counters checked against BOTH authorities character by character: migration 20260901000001's create-table body and resolver.md S6's table are the same eight in the same order, and CYCLE_COUNTERS (src/lib/db/cycles.ts:53-62) matches with none missing, none extra, none misordered; the 'satisfies keyof ResolutionRunRow' makes a rename fail to compile. Newest-first, thousand-separated counts, error_summary inline, the four states each distinct in markup, both gauges mounted, adapter seam renders nothing and never reads T.runs, not-provisioned names resolution_runs. Live parity refuses by name (LiveGuardError, STAGING_SUPABASE_URL) — correct state today.

ATTACK, on the landed tree:
- ended_at null -> no duration fabricated: duration cell is the dash for both running and died; unrecorded (ended, no outcome) IS measurable. Correct.
- error_summary 5041 chars with '<script>alert("x")</script> & "quoted"': rendered verbatim (5041 == 5041), escaped (markup carries &lt;script&gt;, never <script>alert), inline in the cycle's own tr. Correct.
- window at exactly CYCLE_WINDOW=200 -> truncated true and the line says older cycles ran; 199 -> false. Ties on started_at break run_id descending (c,b,a); an unparseable started_at sorts LAST and still renders, with the dash in its started cell. Correct.
- latency on a window of unsets only (2 raw rows, 0 applies): renders applies=0, unsets=2 — the aggregate, never rows.length, which would have said 2. BUG-0012 stays dead.
- cadence boundary, all four readings: cadence-1s running / exactly cadence running / cadence+1s died(901) / future started_at running / ended-no-outcome unrecorded / outcome-with-null-ended_at carries the outcome / unknown word 'aborted' carried verbatim / unparseable started_at running not died. Correct at every edge.

BROKE IT: ?cycle=<run_id> against a window the page NEVER READ. Expected (four-state contract, TASK-0030): a refused or absent read yields no verdict. Found: both failure states render data-cycle-found="false" and the sentence 'Cycle <id> is not among the 200 newest cycles ... it ran earlier, or no cycle carries that id' — on the not-provisioned path directly above the card saying resolution_runs is missing, so the page contradicts itself on one screen. src/app/cycles/page.tsx:669-670 collapses every non-ok read to found=false and :690 renders AskedCycle on askedFor alone. Reachable by a real path: the Dashboard's lineHref sends the operator here precisely during an outage, and the page sends them after a phantom data problem. Two it.fails pins committed at ed3036b, watched RED as plain it() first ('expected 1 to be +0', twice).

RESIDUAL (not filed, lean cap): ?source=<name> from the Sources page is silently ignored — markup byte-identical to no facet, the name never appears. Defensible today (resolution_runs has no source column; the facet narrows the adapter half TASK-0015 owns), but if TASK-0015 does not consume it, this page owes the arrival a sentence.
- 2026-09-02T15:42:53Z [builder-47] claimed (attempt 2)
- 2026-09-02T15:46:19Z [builder-47] claimed → built: - NO CODE CHANGE, deliberately: BUG-0023's fix landed on run/admin-window as ee3ce81 before I claimed. ticket/TASK-0014 is rebased to e12892d and carries zero commits of its own — the branch to land is an empty diff; verification is the deliverable.
- All six stored Checks re-run on that exact tree (HEAD == run/admin-window == e12892d): npm run lint 0; rm -rf .next/types && tsc --noEmit 0; npm run build 0 with /cycles rendered dynamic in the route table (not static); vitest run tests/offline/cycles 47/47 across read.test.ts (18) + page.test.ts (29) — was 41 pre-fix, BUG-0023 added 6; grep claims_rerejected src/lib/db/cycles.ts 0; greps readCycleHealth + readResolutionLatency on src/app/cycles/page.tsx 0. Live parity (tests/live/cycles.live.test.ts) refuses by name with LiveGuardError naming STAGING_SUPABASE_URL — the correct state today; not walked against staging.
- The two ex-pins are plain it(...) and load-bearing, not vacuous: I copied the pre-fix page in (git show 0c818df:src/app/cycles/page.tsx), ran -t BUG-0023 and watched exactly those two go RED with 'expected 1 to be +0' (QA's original signature), then restored with git checkout -- src/app/cycles/page.tsx; tree clean. Every acceptance criterion is carried by a named passing test: newest-first by started_at; eight counts thousand-separated as columns; error_summary inline verbatim; running / skipped / completed / died / outcome-unrecorded each distinct; both gauges render and the latency figures come off the aggregate (a domain with applies 0 lists verdictUnsets beside it); adapter-runs section renders nothing and never reads T.runs; not-provisioned names resolution_runs.
- For QA: the ?cycle= verdict is now three-state — data-cycle-unchecked=<table> on a refused/absent read, data-cycle-found only off an ok read. QA's earlier ?source= residual is answered by one sentence (the facet narrows the adapter half); nothing on this page narrows until TASK-0015 renders that half.
- 2026-09-02T15:46:39Z [dispatcher] nothing to land: ticket/TASK-0014 has no commits and a clean worktree — on to QA as-is
