---
id: TASK-0017
type: task
status: open
milestone: M1
priority: P1
opened_by: architect
parent: FEAT-0008
children: []
assignee: null
claimed_at: null
depends_on: [TASK-0005, TASK-0002, TASK-0026, TASK-0027]
touch_scope: [src/lib/edit, src/app/api/admin/records/[table]/[id]/route.ts, src/lib/db/records.ts, tests/offline/edit, tests/http/edit.http.test.ts]
force: false
---
# The edit config map and the one PATCH route that obeys it

## Description
Module: `src/lib/edit/config.ts` (**the one hand-written config**), `src/app/api/admin/records/[table]/[id]/route.ts` (the one write path) and `src/lib/db/records.ts` (the read + the direct update). Authority: spec section 8 and section 3; acceptance doc ground rule 'One hand-written config drives the edit surface: the {table -> editable columns} map. Write path and widget derive'; `AGENTS.md` data-ownership rule (BINDING); SPEC F8; M1 EC10; acceptance test 7 (pre-cutover half).

Interface contract:

```ts
// src/lib/edit/config.ts
export type Regime = "pre_cutover" | "resolver_owned";
export interface TableEditConfig {
  readonly table: string;                // the canonical table
  readonly pk: string;                   // groups.id, idols.id, events.event_id, venues.venue_id
  readonly regime: Regime;               // decides the WRITE PATH; never configured per column
  readonly editable: readonly string[];  // user-facing scalars only
}
export const EDIT_CONFIG: Readonly<Record<string, TableEditConfig>>;
```

- **`groups` and `idols` are `pre_cutover`**: they edit **directly**, within their allowlist — legal and unprovenanced, as the ownership rules allow. Seed `editable` from the columns the retired routes allowed (they are the vetted set): groups — `name, korean_name, short_name, company, status, type, member_count, debut_date, image_url, bio`; idols — `stage_name, real_name, korean_name, position, nationality, gender, bio, birth_date, image_url, status, height_cm, weight_kg, blood_type, mbti, agency, birth_place`. Primary key is `id` for both.
- **`events` and `venues` are `resolver_owned`** and appear in the map with an **empty** `editable` list in M1. **No write path to them exists** — no PATCH branch, no helper, no scaffold, no commented-out future code. Their override path is M2's, through `settle_review_item`.
- **User-facing fields only: never ids, keys or timestamps.** Never widen a list to a link or a non-scalar — performers and venues are `event_performers` / `venues` rows, not fields of `events`.
- **The route reads the same `EDIT_CONFIG`.** A column absent from the map is refused **server-side**, with the row unchanged and a refusal naming the field — hiding the widget is not the refusal (acceptance test 7). A table absent from the map is refused. A `resolver_owned` table is refused. There is no second allowlist anywhere in the repo.
- The route sits behind `requireAdmin()` like every route before it, and it neither inserts nor deletes a row. `scraped_events` and any `*_legacy` table are never written. No DDL, no migration, no SQL-executing route.

Out of scope: the record page and its widgets (TASK-0018); any observation, any `admin_locked` stamp, any `verdicts` row, any entity picker — all M2.

## Acceptance criteria
[test 7 pre-cutover half; EC10] Offline tests prove the config map is the single source: a column in `editable` is accepted; a column absent from it is refused with the row unchanged; a table absent from the map is refused; a `resolver_owned` table is refused; an id, key or timestamp column is refused even when spelled correctly. An http test proves the refusal is **server-side** by sending a forged PATCH naming an unmapped column directly to the route and asserting a non-2xx response and an unchanged row. The route requires an admin session. No insert or delete of a catalog row exists in the repo, nothing writes `scraped_events`, and no write path to `events`, `venues` or `event_performers` exists.

## Checks
```sh
npm run lint
./node_modules/.bin/tsc --noEmit
npm run build
./node_modules/.bin/vitest run tests/offline/edit
bash -c "test -f src/lib/edit/config.ts && grep -q pre_cutover src/lib/edit/config.ts"
bash -c "grep -q resolver_owned src/lib/edit/config.ts"
bash -c "! grep -rq scraped_events src"
bash -c "! grep -rqE '\.insert\(|\.upsert\(' src/app/api src/lib/db"
```
## History
- 2026-09-02T05:42:19Z [architect] opened: The edit config map and the one PATCH route that obeys it
- 2026-09-02T05:42:37Z [architect] checks amended: narrowed the no-insert/delete scan to the data and route layers: a bare '.delete(' also matches Set/Map calls anywhere in src, which would be a false-RED on correct code [was: npm run lint, ./node_modules/.bin/tsc --noEmit, npm run build, ./node_modules/.bin/vitest run tests/offline/edit, bash -… (304 chars; full text in git history)]
- 2026-09-02T08:23:11Z [architect] depends_on: [TASK-0005, TASK-0002] → [TASK-0005, TASK-0002, TASK-0026, TASK-0027]: Adds TASK-0026 (complete reads). Every lib/db list read in this milestone goes through the one readComplete helper — an ok row set is the whole matching set or the read refuses. Without the edge, eight builders who cannot see each other's code each invent a bound; see ARCHITECTURE.md section 4.3. Also adds TASK-0027: this ticket lands tests/http/edit.http.test.ts, an http test that PATCHes the record route. The harness's Supabase strip is undone by Next's own .env reload (measured), so on a machine whose .env carries SUPABASE_SERVICE_ROLE_KEY that test would exercise a write route against the real catalog with a key that bypasses RLS. TASK-0027 makes 'no database' enforced before this ticket can run.
- 2026-09-02T08:23:54Z [architect] Architect ruling, 2026-09-02 — read kinds, now ARCHITECTURE.md section 4.3. Cited on every ticket that owns a `src/lib/db/*.ts` module.

QA found `src/lib/db/review-items.ts` (TASK-0006) building its list query with no `.range()`, no `.limit()` and no `.order()`. Against PostgREST's `db-max-rows` (Supabase default 1000) that truncates silently, in unspecified order — so a count or an "exactly the matching items" claim built on it would be wrong rather than refused. You are one of eight builders about to write a `lib/db` module without seeing each other's code; the bound is decided once, in TASK-0026 (now a dependency), not eight times.

Two read kinds. Pick deliberately; do not blur them.

1. **Complete read — `readComplete(missing, run, db?)`, new in `src/lib/db/result.ts`.** Use it whenever the surface presents the result as the whole set: a list the page shows in full, a count, an oldest age, an "exactly the matching items" claim. The `run` callback passes `{ count: "exact" }`, a total `.order()` ending in the primary key, and `.range(0, cap - 1)` with the cap handed in. If the exact count exceeds the rows returned, it returns `kind: "error"` naming the object, the count and the cap. **An `ok` array from a complete read is the whole matching set** — that property is what every figure in this app rests on, and it is why no caller needs a "was this all of it?" flag.
2. **Window read — `readRows`, unchanged.** The caller's own `.order()` + `.limit()` define a named window, and the surface says which window it is showing. This is section 8's gauge contract and TASK-0007's landed gauges use it correctly. A window read's rows must never become a figure presented as a total.

Also: no read helper may substitute a number the database did not give. `readCount` returned `count ?? 0` — BUG-0007's user-visible twin — and TASK-0026 makes it refuse a null count instead. Do not reintroduce a `?? 0` on any read path.
- 2026-09-02T08:24:14Z [architect] Architect ruling, 2026-09-02 — pure domain leaves sit BELOW `lib/db`, not above it (ARCHITECTURE.md section 4, arrow diagram + new rule 7).

QA flagged on TASK-0006 that `src/lib/db/review-items.ts` imports `src/lib/review/shapes.ts` — the reverse of the arrow the contract drew. The code was right and the diagram was wrong: `shapes.ts` has zero imports, no cycle is constructible, and TASK-0006's own two-module contract required the split. The diagram is amended rather than the code.

The rule you are working under, because your ticket owns both a domain module and a `lib/db` module:

- A **pure leaf** — today `src/lib/review/**`, `src/lib/format.ts`, `src/lib/edit/config.ts`, and `src/lib/browse/**` when it lands — imports NOTHING that can reach a database: not `lib/db/**`, not `@supabase/supabase-js`, not `process.env`. It holds the vocabulary, the row interfaces it reasons about, and pure functions over rows.
- `lib/db/**` imports the leaf. **The leaf never imports `lib/db/**` back — not a value, and not a type.** A type-only import would erase at runtime, but it still writes a directory-level cycle into the contract, and the day someone changes it to a value import the cycle is real with nothing to catch it. If both sides need a row type, **declare it in the leaf** — that is what `ReviewItemRow` in `shapes.ts` already does, correctly.
- `lib/gauges/**` is NOT a leaf and is unaffected: a gauge fetches its own bounded window through `lib/db`, which is the arrow as drawn (section 8, TASK-0007 as landed).
