---
id: BUG-0212
type: bug
status: done
milestone: null
priority: P1
opened_by: qa
parent: null
discovered_from: BUG-0207
children: []
assignee: null
claimed_at: null
depends_on: []
touch_scope: [tests/offline/handoff/settle-review-item.test.ts]
force: false
siblings: [BUG-0213]
resolution: already_done
---
# The KS-code collision arm is blind to the sibling's own raise idiom: a stranger's 'using errcode = KS029' is attributed to us

## Description
QA on admin-window/BUG-0207, 2026-09-11, run/admin-window at c2cfe8af (landed).

BUG-0207's criterion: 'The guard still fails if a code this artifact allocates is spelled next door by a file that is NOT one of this campaign's own handoff artifacts.'
Expected: a later sibling migration that raises KS029-KS032 for a meaning of its own makes tests/offline/handoff/settle-review-item.test.ts > 'allocates no code the sibling's tree holds today' FAIL, naming the line.
Found: it passes, exit 0, silently. Attribution is per LINE against this campaign's handoff notes (unattributedSpellings, tests/offline/handoff/settle-review-item.test.ts:~300), and next door a SQLSTATE is raised on a line of its own — 'using errcode = 'KSnnn','. Our note carries that exact line for each code it allocates, so a stranger writing the sibling's ordinary idiom writes a line the corpus already holds.

MEASURED, sibling's own grammar (read-only count over ../kspace Scraper/supabase/migrations + tools/staging, 2026-09-11): 'using errcode = 'KS024',' x22, 'using errcode = 'KS029',' x20, KS003 x12, KS006 x11 — the code-on-its-own-line raise is how every one of KS001-KS032 is raised there. The python spelling the existing fixture uses (LEASE_NOT_RENEWED = "KS031") is the one grammar the note does NOT carry, so the pinned arm passes for the shape that does not occur and misses the shape that does.

REPRO (no write next door — a scratch replica of the sibling, copied read-only):
 1. cp ../kspace Scraper/{supabase/migrations/20260908000001*.sql,supabase/migrations/20260908000002*.sql,tests/helpers/ks_codes.py,tests/live_safety/test_codes_named_once.py,tools/staging/harness_objects.sql} into <scratch>/replica/ at the same relative paths.
 2. Copy tests/offline/handoff/settle-review-item.test.ts to tests/offline/handoff/__probe.test.ts with SIBLING_ROOT rewritten to <scratch>/replica.
 3. Baseline: ./node_modules/.bin/vitest run --project=offline tests/offline/handoff/__probe.test.ts -t 'allocates no code the sibling' -> exit 0 (replica is faithful).
 4. Add <scratch>/replica/supabase/migrations/20260915000001_a_strangers_lease.sql containing a plpgsql function that raises: "raise exception 'lease % could not be renewed', p_lease_id / using errcode = 'KS031', / detail = ... / hint = ..." and whose comments spell no KS code.
 5. Re-run step 3 -> exit 0. EXPECTED exit 1 naming that file. (Control: make the file's comment also spell KS031 and it does fail, naming only the comment line — the errcode line itself is never reported.)

Consequence: the guard is unchanged for python/registry collisions and defeated for SQL ones — including the case BUG-0207's handoff claims it covers ('a later rewrite at the target path that re-points a code still fails': a re-point reuses the same idiom line, so it does not fail).

PIN (committed, currently it.fails / strict-xfail so the branch stays green): tests/offline/handoff/settle-review-item.test.ts > 'flags a stranger's raise of an allocated code in the sibling's own SQL idiom'. Watched red as a plain it(): 'expected [] to deeply equal [ "using errcode = 'KS029'," ]'. The fixer flips it.fails back to it().

Not in scope for the fixer: widening attribution by path (BUG-0207 rejected that, correctly) or re-deriving TAKEN_NEXT_DOOR from the scan.

## Acceptance criteria
(to be written)

## Checks
```sh
env -u STAGING_SUPABASE_URL -u STAGING_SUPABASE_SERVICE_ROLE_KEY ./node_modules/.bin/vitest run --project=offline tests/offline/handoff/settle-review-item.test.ts
./node_modules/.bin/tsc --noEmit
npm run lint
```

## History
- 2026-09-11T19:55:09Z [qa] opened: The KS-code collision arm is blind to the sibling's own raise idiom: a stranger's 'using errcode = KS029' is attributed to us
- 2026-09-11T20:57:50Z [qa] open → done: Fixed in BUG-0207's lane (024f3766), per the campaign rule; verified here as part of the BUG-0207 re-check on the landed tree d5d36383.

REPRO from this ticket, re-run: scratch replica of the sibling (read-only copies, nothing written next door), stranger migration raising KS031 in the sibling's own idiom, comments spelling no code -> now exit 1, naming 'supabase/migrations/20260915000001_a_strangers_lease.sql: using errcode = 'KS031','. Before the fix it exited 0 silently.

The consequence this ticket named is also closed: a re-point at the target path (installed file rewritten so KS031 raises 'staging lease % could not be renewed') -> exit 1, naming all five KS-bearing lines of that file. My it.fails pin ('flags a stranger's raise of an allocated code in the sibling's own SQL idiom') is a plain it() again and passes, and it is not vacuous — degrading HANDOFF_BLOCKS back to the pre-fix LINE corpus reddens it plus two others.

Receipt green on this ticket's own checks (offline file, tsc, lint, all exit 0). New, distinct finding filed as BUG-0213: the block corpus's two-line registry paste makes the sibling's NEXT allocation a loud false positive — the opposite direction of this ticket's silent miss. [resolution: already_done]
