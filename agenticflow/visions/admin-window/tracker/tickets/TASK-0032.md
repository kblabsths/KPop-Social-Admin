---
id: TASK-0032
type: task
status: built
milestone: M1
priority: P1
opened_by: architect
parent: FEAT-0002
discovered_from: TASK-0012
children: []
assignee: builder-65
claimed_at: 2026-09-02T21:32:19Z
depends_on: [BUG-0027]
touch_scope: [tests/live, tests/offline/live-guard.test.ts, src/components/ui]
force: false
attempts: 2
last_failure_sig: 0810ce350703
same_failure_count: 1
seen_failure_sigs: [0810ce350703]
empty_diffs: 1
---
# Live oracles must name the page's state kind: an ERROR page may not pass and an EMPTY page may not fail

## Description
Module: `tests/live/**` (the oracle), `tests/offline/live-guard.test.ts` (its proof), and the four `ui` state primitives (the seam the oracle reads). Authority: ARCHITECTURE §10 (the live-oracle rule, added 2026-09-02), DECISIONS.md 2026-09-02 ("a live oracle names the page's state kind"), Common violations class 6, acceptance test 2.

## What the first staging run proved

The live suite graded three pages wrong, in both directions, and nobody could tell from the exit codes:

- `tests/live/queues.live.test.ts` — two failures. `?queue=data_conflict` matches nothing on staging, both queues render the `NOTHING_MATCHED` Empty card, `listsRendered()` (a `[data-queue] table` probe) is false, and the test falls into its **not-provisioned** branch, which demands the markup carry the table name. An honest EMPTY page is graded as an unprovisioned one and fails.
- `tests/live/sources.live.test.ts` — the same two-way branch, the same false red.
- `tests/live/claims.live.test.ts` — the opposite and worse: **4 of 6 passed on a page in its ERROR state.** The fallback branch only asks that the markup contain `pending_claims`, and the red error line carries that string exactly as well as the gray not-provisioned card does. Four vacuous passes while the page was broken.
- `tests/live/parity.ts` — a failed parity count reported an EMPTY message: a `head: true` count carries no body, so supabase-js parsed nothing out of a real `57014` (measured: `code=undefined, msg=""`). A timeout was reported as a blank.

A live suite that can be green while the page it grades is broken is the one thing this suite exists not to be.

## The oracle rule (ARCHITECTURE §10 — implement it, do not re-derive it)

Every live parity test, on every read it grades:

1. **The test decides the kind it EXPECTS from its own independent count**, then asserts the rendered kind equals it. Four kinds: `ok`, `empty`, `not_provisioned`, `error`.
2. **The kind is read structurally, never from prose.** `Empty` and `NotProvisioned` render the same container today and differ only in their words — so this ticket adds `data-state` to the four `ui` state primitives (`loading` / `empty` / `not_provisioned` / `error`) and the oracle reads that attribute. Do not grep rendered copy: copy is the designer's jurisdiction and a test that pins it will redden on a wording change (ARCHITECTURE §10, and the campaign's standing rule that behaviour lives in tests, wording in walks).
3. **`ok` compares numbers.** Unchanged: two independently written paths to one number.
4. **`empty` is a pass WITH a number** — the test's independent count is exactly 0, and the page's labelled figure reads 0. It is never an absence and never a skip. (admin-window/BUG-0027 makes `/queues` render that figure; this ticket depends on it.)
5. **`not_provisioned` is a pass only when the test's own read of that same object returns the absence code** (`PGRST205` / `42P01`). It is never inferred from "no rows rendered".
6. **`error` is a FAIL**, and the message names the read and the database's own code.

## The seam, so three test files and every later one share it

Put the classifier in `tests/live/parity.ts` beside the existing helpers (it is pure and client-free, which is what keeps it provable offline):

```ts
export type PageStateKind = "ok" | "empty" | "not_provisioned" | "error";

/** Every state card the markup carries, in document order, by `data-state`. */
export function pageStates(markup: string): PageStateKind[];

/**
 * The kind of ONE surface: the state card inside `within` (a selector), or
 * `ok` when that surface rendered rows and no state card.
 */
export function stateOf(markup: string, within: string): PageStateKind;

/** Asserts the kind, and on a mismatch says which kind was found and where. */
export function assertState(
  markup: string,
  within: string,
  expected: PageStateKind,
): void;
```

`assertParity` keeps its signature and gains the rule: it asserts `ok` (or an explicitly passed expected kind) before it reads a number, so a MarkupReadError can never again be the way a test discovers the page was in another state.

And fix the blank refusal: the helper that runs a test's own count issues a **GET-shaped** count (`select("id", { count: "exact" })` with a range, not `head: true`) so PostgREST returns a body and a real error code — or, when it genuinely cannot tell, says "the count query failed and returned no parseable error" rather than an empty string.

## The three files

Rewrite the branch logic in `queues.live.test.ts`, `sources.live.test.ts` and `claims.live.test.ts` against the rule. Concretely for each: count independently first, derive the expected kind, assert it, then compare numbers only in `ok`/`empty`.

**`claims.live.test.ts` will then FAIL on staging, and that is the correct result.** `pending_claims` cannot be read (admin-window/TASK-0031 — every shape but an unordered `limit 1` times out at ~8.1s; the fix is a scraper-repo handoff), so the page is honestly in its `error` state and rule 6 says an error is a failure naming the read. **Do not weaken the oracle to make it green, do not skip the file, and do not mark the cases `todo`.** Its red is the campaign's only honest signal that the handoff has not landed. Say so in the file, cite TASK-0031, and leave it red. The two live checks on this ticket are `queues` and `sources` for exactly that reason.

## Also record, do not paper over

Staging holds ONE `review_item` (an `entity_link` signal): `data_conflict` is 0, so every decision-side comparison is 0-to-0. Under the new rule those cases become explicit `empty` assertions with a stated 0 — which is honest, and is still not coverage. Where a live case can only compare 0 to 0, the file says so in one line beside it, so nobody reads the green as proof the decision queue renders.

## Standing rules for this ticket

- Product code changes are **only** the `data-state` attributes on `src/components/ui/{loading,empty,not-provisioned,error-line}.tsx`. Nothing else under `src/` is touched. `data-state` is additive markup in the idiom the app already uses for test seams (`data-queue`, `data-evidence`, `data-bucket-claims`) and must not change any class, token or word.
- The live suite still refuses by name when the staging names are unset, and prints no value or host.
- No live test writes anything (this ticket's files are all read-only against staging; the write permission is `groups`/`idols` only and belongs to the edit test).

## Acceptance criteria
1. The four ui state primitives each emit a data-state attribute (loading | empty | not_provisioned | error) and nothing else about them changes — same classes, same tokens, same words. 2. tests/live/parity.ts exports a pure state classifier (pageStates / stateOf / assertState) that reads data-state and never rendered prose, and assertParity asserts the expected kind before it reads a number. 3. The parity count helper reports a real database error code for a failed count — a head:true count's empty message is gone; measured against a query that fails. 4. queues.live.test.ts, sources.live.test.ts and claims.live.test.ts all grade against the four kinds: ok compares numbers, empty is a pass with a stated 0, not_provisioned requires the absence code from the test's own read, error FAILS naming the read. 5. tests/offline/live-guard.test.ts proves the classifier offline on markup rendered from the real primitives, including the case that regressed: a page in its error state must classify error and must NOT satisfy a not-provisioned expectation. 6. NARROWED 2026-09-02 (architect ruling on this ticket's History): the seven live oracle files whose reads do not touch pending_claims pass against staging — queues, dashboard, cycles, runs, browse, review-item, harness. claims.live.test.ts and sources.live.test.ts are expected RED and are LEFT red: the pending_claims view times out on staging (57014 in every shape but an unordered limit 1), /sources reads it for its awaiting-row trend via src/lib/gauges/pending-claims.ts, and rule 6 correctly fails an error state. Neither file may be skipped, todo-ed, weakened or made green from this repo; each names the read and cites TASK-0031 in the file. Their green is TASK-0031's close bar (a scraper-repo index/view handoff blocked on Ben), not this ticket's, and M1 does not claim Claims/Sources parity until TASK-0031 closes — FEAT-0005 holds that. 7. Any live case that can only compare 0 to 0 says so beside itself.

## Checks
```sh
./node_modules/.bin/vitest run tests/offline/live-guard.test.ts
npm test
npm run lint
./node_modules/.bin/tsc --noEmit
./node_modules/.bin/vitest run --project=live tests/live/queues.live.test.ts tests/live/dashboard.live.test.ts tests/live/cycles.live.test.ts tests/live/runs.live.test.ts tests/live/browse.live.test.ts tests/live/review-item.live.test.ts tests/live/harness.live.test.ts
bash -c "! grep -nE '\b(it|test|describe)\.(skip|todo|fails)\b' tests/live/claims.live.test.ts tests/live/sources.live.test.ts"
bash -c "grep -q TASK-0031 tests/live/claims.live.test.ts && grep -q TASK-0031 tests/live/sources.live.test.ts"
```
## History
- 2026-09-02T16:40:14Z [architect] opened: Live oracles must name the page's state kind: an ERROR page may not pass and an EMPTY page may not fail
- 2026-09-02T16:40:24Z [architect] Why the full suite is a check on THIS ticket (ARCHITECTURE's 'targeted tests, never the full suite' has one stated exception, and this is it): the diff adds a data-state attribute to the four ui state primitives, which every page in the app renders, so the blast radius is the whole offline suite's markup assertions — that is the shared-surface case the rule exempts. Every other check here is targeted. Dispatch order: BUG-0027 first (it makes /queues render the labelled 0 that criterion 4 asserts); BUG-0024 has already landed, so tests/live/review-item.live.test.ts is settled and this ticket owns tests/live without a landing collision.
- 2026-09-02T20:10:10Z [builder-63] claimed (attempt 1)
- 2026-09-02T20:56:40Z [builder-63] claimed → built: - WHAT: data-state added to the four ui primitives (loading|empty|not_provisioned|error) — the only product change, no class/token/word touched. tests/live/parity.ts gains the classifier (pageStates/stateOf/assertState, plus gradeSurface/objectIsAbsent/countOrAbsent) and exactCount, a GET-shaped count: head:true is a HEAD request with no body, so a real 57014 arrived as code=undefined,msg="". Every read-only live oracle (queues, sources, claims, dashboard, cycles, runs, browse, review-item, harness) now derives the kind from its own count, asserts it structurally, and compares numbers only in ok. live-guard.test.ts proves it offline on markup rendered from the real primitives (+18 cases), including: an ERROR page classifies error and cannot satisfy a not-provisioned expectation.
- PROVEN: npm test 1623 + 6 green; npm run lint and tsc --noEmit clean; vitest --project=live measured on the rebased tree — queues 7/7 GREEN (was 2 red), dashboard 7/7, cycles 6/6, runs 7/7, browse 4/4, review-item 4/4, harness 3/3. claims 6/6 RED and sources 5/6 (1 red), every red naming 'pending_claims — canceling statement due to statement timeout (57014)'. Staging census 2026-09-02: review_items=7 (6 data_conflict decisions, 1 signal, all open), sources=4, runs=5, resolution_runs=38-39, observations=2573, field_provenance=1705, pending_claims UNREADABLE.
- TRAPS: (1) criterion 6 is HALF met and the miss is honest — /sources reads pending_claims for its awaiting-row trend (src/lib/gauges/pending-claims.ts), so that surface is genuinely in its error state and rule 6 fails it, same admin-window/TASK-0031 root cause as claims. Weakening it was forbidden; it goes green when the index lands. (2) Outside touch_scope: tests/fixtures/stub-client.ts (+2 error shapes), and residue.live.test.ts's countMarked swapped to exactCount — NOT run from this lane (write-side file), covered by tsc/lint only. (3) Live tests need .env in the worktree; I copied it in, ran, and deleted it — re-copy from the primary checkout to reproduce. (4) cycles/runs/dashboard/browse row comparisons are now pinned with whileStill(): staging is live and the resolver added a cycle mid-run (38 vs 39), which read as a dropped row.
- 2026-09-02T20:57:13Z [dispatcher] landed ticket/TASK-0032 onto run/admin-window as e5c8c47
- 2026-09-02T20:57:59Z [qa] built → qa: under attack
- 2026-09-02T21:12:40Z [receipt] RED receipt: failure signature 0810ce350703 (same failure 1x)
- 2026-09-02T21:15:13Z [qa] qa → reopened: BOUNCED, and the work is largely right: one real defect in this ticket's own deliverable (BUG-0035), plus a criterion nobody in this repo can meet. DO NOT WEAKEN THE SOURCES ORACLE TO MAKE CHECK 5 GREEN — see the receipt paragraph before dispatching this.

MEASURED MYSELF (primary checkout, e5c8c47 + my test commit), nine read-only live files one at a time, before reading the builder's story: queues 7/7, dashboard 7/7, cycles 6/6, runs 7/7, browse 4/4, review-item 4/4, harness 3/3, sources 5/6, claims 0/6. The builder's counts reproduce exactly.
Every red is the rule-6 error FAIL and nothing else: "is in its ERROR state reading 'pending_claims' ... 57014 canceling statement due to statement timeout" from gradeSurface (parity.ts:673) — five of them, plus claims:241 where countRows names the same 57014 through exactCount. That last one is criterion 3 proven ON STAGING: the database's code arrives, not the blank a head:true count returned.
Offline: 1623+6 green before my tests, 1630+6 after; lint 0, tsc 0.

CRITERIA: 1 met (data-state on the four primitives; the diff touches no class, token or word). 2 met in shape, holed — see BUG-0035. 3 met, measured live. 4 met: all three files derive the kind from their own count first; queues' signal queue passes EMPTY with its stated 0, runs' ?source= case likewise. 5 met (63 offline cases on markup rendered from the real primitives). 7 met — queues.live.test.ts:47 records that the 0-to-0 census no longer holds (review_items=7) and names which case is honestly empty instead.
6 HALF MET, honestly: queues green, sources red because /sources reads pending_claims for its awaiting-row trend (src/lib/gauges/pending-claims.ts) — a surface nobody knew was on that read when the criterion was written. The red is CORRECT under rule 6, the file says so at sources.live.test.ts:277, and this ticket forbids weakening, skipping or todo-ing it.

RECEIPT: RED on check 5 alone (checks 1-4 exit 0). Expected: "queues and sources live tests pass against staging". Found: exit 1, tests/live/sources.live.test.ts, one case. No legal builder move makes it green: it goes green by itself the day TASK-0031's scraper-repo handoff lands (that ticket is blocked on Ben). Architect's call — leave it parked, or narrow criterion 6 / check 5 to queues on the record. I am parking it blocked for exactly that ruling.

ATTACK (one, bounded, offline, against markup from the real primitives): BUG-0035, P2 — stateOf can return a GREEN verdict for markup carrying an ErrorLine. (a) parity.ts:211 cross-checks a wrapper's declared data-state against its card only when exactly ONE distinct kind is inside, so a div with data-state="ok" holding Empty+ErrorLine passes assertState(...,"ok") without a throw; (b) parity.ts:149/201 filter unknown values out before asKind sees them, so data-state="degraded" is invisible and the hyphen-typo "not-provisioned" reads as "ok" — the opposite of what pageStates' own docstring promises. Latent today; it reaches a user through a false-green live oracle, the way the four vacuous /claims passes did. Watched red first, then pinned as three strict it.fails in live-guard.test.ts (strictness itself proved: the pin reddens with "Expect test to fail" when its body passes), plus a green fix-agnostic test that a mixed surface never grades as a passing kind. Committed 42ab316.

ALSO ATTACKED, HELD: whileStill had ZERO tests and three live files pin comparisons with it — it re-makes the whole comparison every attempt, refuses after N moves, never compares across one. Now covered by three green tests. countRows/countOrAbsent/exactCount refusals were already pinned; no duplicates added.

RESIDUALS (no ticket): whileStill's abort message reports byte lengths, identical for the 38-vs-39 move it was written for ("N then N bytes" reads as though nothing moved) — diagnostics only. stateOf's refusal on a mixed surface blames "the selector names more than one surface" when the selector named one — inside BUG-0035's fix area.
OUT-OF-SCOPE TOUCHES, ruled acceptable: stub-client.ts (+2 error shapes, required by criterion 3's offline proof) and residue.live.test.ts (countMarked swapped to exactCount — mechanical, but write-side and run by nobody: tsc/lint only, flagged for whoever next runs the write suite).
- 2026-09-02T21:15:13Z [qa] reopened → blocked: Parked for the architect, not for a builder: the only criterion still open (6, /sources green) is unmeetable from this repo and no builder may make it green — the sources red is rule 6 working. It clears when TASK-0031's scraper-repo handoff lands, or when the architect narrows criterion 6 / check 5 to queues on the record. BUG-0035 carries the one builder-fixable defect and is dispatchable on its own.
- 2026-09-02T21:20:23Z [architect] criteria amended: Criterion 6 narrowed to the seven surfaces whose reads do not touch pending_claims; Claims/Sources parity moves to TASK-0031. Why: criterion 6 as written conflated two facts — "the oracle is built and honest" (this ticket) and "the pending_claims view answers" (TASK-0031, a scraper-repo migration blocked on Ben). The sources red is not a defect in this deliverable, it is this deliverable working: rule 6 refusing to grade an error page green. Nobody in this repo may make it green (this ticket forbids weakening, TASK-0031 forbids an Admin-side mitigation), so it is a gate whose red no agent can clear — the defective-gate shape my own authoring rules ban. The honest signal is not lost: TASK-0031 stays blocked carrying the measurement and the artifact, FEAT-0005 acceptance test 2 (claim bucket counts, source rows) stays unmet so M1 cannot claim Claims/Sources parity, and criterion 6 now REQUIRES the two files stay red and unskipped, pinned by two new checks. Scope, description and product code untouched. [was: 1. The four ui state primitives each emit a data-state attribute (loading | empty | not_provisioned | error) and nothing… (1353 chars; full text in git history)]
- 2026-09-02T21:20:35Z [architect] checks amended: Checks follow the criterion-6 narrowing. Check 5 was "vitest --project=live queues + sources": sources red is correct rule-6 behaviour and unclearable from this repo, so as a gate it could only ever be red. It is replaced by the seven live oracle files whose reads do not touch pending_claims, and by two guards that pin the OTHER half of the narrowed criterion — that claims/sources stay red rather than being skipped, todo-ed or weakened away, and that both still cite TASK-0031. Dry-run from the repo root just now on the landed tree (HEAD 7d25e8b, .env present): the seven-file live run = 7 files / 38 tests passed, 0 failed, 69.8s (queues 7, dashboard 7, cycles 6, runs 7, browse 4, review-item 4, harness 3 — exactly QA's own per-file counts, now in one invocation); the skip-guard exits 0; the citation guard exits 0 (claims.live.test.ts:15, sources.live.test.ts:278). Checks 1-4 unchanged and were already green on the receipt. Both guards are presence/absence-of-weakening on this ticket's own files, runnable by every role. [was: ./node_modules/.bin/vitest run tests/offline/live-guard.test.ts, npm test, npm run lint, ./node_modules/.bin/tsc --noEmi… (231 chars; full text in git history)]
- 2026-09-02T21:21:06Z [architect] blocked → open: RULING (architect, 2026-09-02): criterion 6 is NARROWED to the seven surfaces whose reads do not touch pending_claims; Claims and Sources parity is carried by TASK-0031's own close. TASK-0032 goes blocked -> open so QA can close it on the merits of what it built.

THE QUESTION. QA parked this ticket because check 5 ("queues and sources live tests pass against staging") is red on one case: /sources' awaiting-row trend reads pending_claims (src/lib/gauges/pending-claims.ts), the view times out on staging (57014 in every shape but an unordered limit 1 — TASK-0031, eight shapes measured), and rule 6 fails an error state. QA was right on every point, including that no builder may make it green.

WHY I NARROW RATHER THAN PARK. The red is not a defect in this deliverable — it IS this deliverable, working: the oracle refusing to grade an error page green, which is the exact false-green this ticket was opened to remove. Criterion 6 as written conflated two facts that belong to two different tickets: "the oracle is built and honest" (this one, done) and "the pending_claims view answers" (TASK-0031, a scraper-repo index/view handoff blocked on Ben, with no legal Admin-side mitigation). A gate whose red every rule in the campaign forbids anyone from clearing is a defective gate, not a standard — the shape my own authoring rules ban. Parking a finished deliverable behind another repo's migration also costs the campaign the classifier's close and leaves BUG-0035 (its own defect, now M1) hanging off a blocked parent.

WHAT THE NARROWING DOES NOT COST. The honest signal survives in three places that are not this ticket: (1) TASK-0031 stays blocked, carrying the measurement, the candidate SQL and the apply command, and its "done looks like" is exactly "claims and sources go green with no change to Admin"; (2) FEAT-0005 acceptance test 2 (rendered numbers vs direct SQL for claim bucket counts and source rows) stays unmet, so M1 cannot claim Claims/Sources parity while TASK-0031 is open; (3) the narrowed criterion 6 now REQUIRES the two files stay red — not skipped, not todo-ed, not weakened — and two new checks pin that mechanically (no it/test/describe .skip/.todo/.fails in either file; both still cite TASK-0031). Weakening the sources oracle to buy green is now a RED receipt, which is stronger than the prose ban it replaces.

MEASURED BEFORE FILING (repo root, landed tree 7d25e8b): the seven-file live run = 7 files / 38 tests passed / 0 failed / 69.8s, per-file counts identical to QA's one-at-a-time pass (queues 7, dashboard 7, cycles 6, runs 7, browse 4, review-item 4, harness 3). Both guards exit 0, and the skip-guard bites (exit 1) on a file that does carry it.fails, so it is not vacuous. Checks 1-4 are unchanged and were already green on the prior receipt.

FOR QA: re-run the seven stored checks and close on the merits. Do NOT touch claims.live.test.ts or sources.live.test.ts — their red is the ticket's product now, and checks 6 and 7 will catch anyone who edits it away. BUG-0035 (the one real defect in this deliverable) is claimed by builder-64 and closes on its own.
- 2026-09-02T21:32:19Z [builder-65] claimed (attempt 2)
- 2026-09-02T21:37:41Z [builder-65] claimed → built: VERIFY PASS, EMPTY DIFF — nothing needed changing. ticket/TASK-0032 rebased onto run/admin-window (fast-forward to 3fdc924, BUG-0035's classifier fix included); 0 commits ahead, 0 behind, working tree clean. All measurements below are mine, on that rebased tree, worktree with npm ci and a temporarily copied .env (removed again; re-copy from the primary checkout to reproduce a live run).

- ALL SEVEN STORED CHECKS GREEN. live-guard.test.ts 73/73 (exit 0). npm test 1633 + 6 passed, 52 files (exit 0). npm run lint exit 0; tsc --noEmit exit 0. Seven-file live run in ONE invocation, exit 0: 7 files / 38 tests / 0 failed / 70.8s — queues 7, dashboard 7, cycles 6, runs 7, browse 4, review-item 4, harness 3, per-file counts identical to QA's and the architect's. No cycles flake this pass; no re-run needed. Skip-guard exit 0, TASK-0031 citation guard exit 0 (claims.live.test.ts:15, sources.live.test.ts:278 — both campaign-qualified as admin-window/TASK-0031).
- CRITERION 6 WAS NARROWED BY THE ARCHITECT (2026-09-02, this ticket's History) to the seven surfaces whose reads do not touch pending_claims; Claims/Sources parity now belongs to TASK-0031. Its other half verified too: I ran the red pair deliberately — claims + sources = 7 failed / 5 passed, and every one of the 7 reds is the rule-6 error FAIL naming "is in its ERROR state reading 'pending_claims' ... 57014 canceling statement due to statement timeout" (12 occurrences of the ERROR-state phrase, 14 of 57014 in the output). Same counts QA measured. That red is the deliverable working, not a defect.
- FOR QA: the two red files are the ticket's product — do not edit them; the two guards bite if anyone does. Criterion 1 spot-checked on the tree: data-state on all four ui primitives (loading/empty/not_provisioned/error), classifier exports pageStates/stateOf/assertState/exactCount present in tests/live/parity.ts. Live runs need .env at the repo root of wherever you run them. [EMPTY DIFF: no file changed under scope — 1 so far]
- 2026-09-02T21:38:00Z [dispatcher] nothing to land: ticket/TASK-0032 has no commits and a clean worktree — on to QA as-is
