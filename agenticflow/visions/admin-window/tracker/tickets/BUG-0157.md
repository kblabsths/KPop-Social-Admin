---
id: BUG-0157
type: bug
status: done
milestone: patch
priority: P3
opened_by: qa
parent: null
discovered_from: BUG-0156
children: []
assignee: null
claimed_at: null
depends_on: []
touch_scope: [src/components/review/evidence-cells.tsx, tests/offline/review-item/page.test.ts]
force: false
attempts: 1
resolution: fixed
---
# The folded-records table prints the dash-meaning line with no dash on screen: a record's own em-dash reference is explained as "a value this row does not carry"

## Description
Found attacking BUG-0156 on the landed tree (run/admin-window @ 886dd1a, offline vitest + cheerio, no device, no network, 2026-09-09). BUG-0156 gave `sourceColumn` a condition that is the one its cell branches on; `recordColumn` kept the old shape and still diverges from its own cell.

Expected (`src/components/review/shape-views.tsx:225-233`, and the landed rule at `src/components/review/evidence-cells.tsx:100-125`: "Each column now states its own condition, and each states exactly the one its cell uses"): the dash-meaning line is printed "only while one is on screen", and what it says of that dash is true — "A - is a value this row does not carry".

Found, on the `entity_link_source_pattern` item's folded-records table (the one view carrying `recordColumn`, `shape-views.tsx:526`), rendered through the real page:

 1. observation with `entity_id: null`, `external_ref: "—"` -> the record cell is `<span data-record="—">—</span>` (the SOURCE's own reference, verbatim ink); `[aria-label="no value"]` inside `[data-surface="evidence"]` = 0; `[data-absence-note="dash"]` = 1. The page explains a dash that is the producer's value - BUG-0156's own untruth, one column over.
 2. same with `external_ref: "​"` -> the cell is `<span data-record="​">​</span>`: an evidence cell with nothing in it and no absence element at all (the rendering BUG-0152/BUG-0154 were filed to remove), and the note is printed for a dash that is not on screen.

Where it comes from: `src/components/review/evidence-cells.tsx:344-347` - `absent: (row) => isAbsent(entityId-when-linked else externalRef)` - while the cell (`evidence-cells.tsx:348-357`) draws the app's dash only by RETURNING NULL, which it does only when neither identity is there (`entityId`/`recordHref` absent AND `externalRef === null`). The two expressions coincide in that one case only; for every ink-less or em-dash `external_ref` they disagree. Same class as BUG-0156 (a predicate answering a rendering's question, LESSONS 4/11), and it PREDATES the BUG-0156 fix: the pre-fix `value` accessor run through `isAbsent` gives the same two verdicts, so this is not a regression of that diff.

Not filed, for the record: a third arm (`entityId` present but ink-less, with a `recordHref`) renders `<a href="/records/events/" data-record="">` - an anchor with nothing to read or click - but `recordHref` (`src/lib/records/routes.ts:49`) refuses an empty id, so reaching it needs a whitespace-only uuid. Unreachable, so no claim is made on it.

Reach: `observations.external_ref` is plain `text` with NO check constraint (`kspace Scraper/supabase/migrations/20260818000000_the_schema_arrives_as_one_snapshot.sql:3827`), i.e. LESS constrained than the `sources.source` name BUG-0156 was priced on (`CHECK (source ~ '^[a-z0-9_]+$')`). Staging holds no such row. P3 for that reach; inside it, a false sentence about a value the row does carry, plus an unreadable cell.

Pins (landed on run/admin-window by qa-0156, `it.fails` while the divergence stands - THE FIX FLIPS BOTH TO PLAIN `it`), both watched RED first as plain `it` on this exact tree, in `tests/offline/review-item/page.test.ts`, describe "an evidence cell with nothing in it":
 1. "says a dash is on screen only when one is, in the folded-records table" - red with `AssertionError: the dash-meaning line, with 0 dash(es) on screen: expected 1 to be +0`;
 2. "draws SOMETHING for a claim whose only identity has no ink" - red with `a cell a person can read: the app's dash, or words: expected false to be true`.
Non-vacuous: the neighbouring "says what a dash means once, and only while one is on screen" pins the other polarity, and a claim with NEITHER identity draws the dash and does get the line.

Repro:
 1. cd "kspace Admin"
 2. ./node_modules/.bin/vitest run --project=offline tests/offline/review-item/page.test.ts -t "folded-records table"
 3. It passes as an EXPECTED FAILURE. Flip `it.fails` to `it` and it goes red on the note that explains a dash nobody drew.

Fix shape (not binding): give `recordColumn` the condition its cell really branches on (neither identity: no linked `entityId`/`recordHref` AND no `externalRef`), or make the cell branch on `absent` itself so the two cannot part again; then decide the ink-less reference the way the source cell now decides an ink-less label - the app's dash, and the sentence that explains it true.

## Acceptance criteria
(to be written)

## Checks
```sh
./node_modules/.bin/vitest run --project=offline tests/offline/review-item/page.test.ts -t "folded-records table"
./node_modules/.bin/vitest run --project=offline tests/offline/review-item/page.test.ts -t "only identity has no ink"
./node_modules/.bin/vitest run --project=offline tests/offline/review-item/page.test.ts
```

## History
- 2026-09-10T00:58:35Z [qa] opened: The folded-records table prints the dash-meaning line with no dash on screen: a record's own em-dash reference is explained as "a value this row does not carry"
- 2026-09-10T01:26:53Z [builder-234] claimed (attempt 1)
- 2026-09-10T01:34:37Z [builder-234] claimed → built: recordColumn now reads ONE accessor, recordIdentity(row) (entityId when there is a row to link to, else externalRef); the cell draws the app's labelled dash exactly when !hasVisibleContent(identity) and 'absent' IS that predicate, so dash-drawn <=> dash-meaning line. A producer's em-dash external_ref renders verbatim in its span with no note; an ink-less one ("", U+200B) keeps its data-record hook and draws the dash inside it; no identity at all still returns null (no hook), which is what BUG-0122's 'name it no way at all' pin requires.
- QA's two it.fails in tests/offline/review-item/page.test.ts are plain it now; both watched RED first as plain it on the pre-fix tree (0 dash(es): expected 1 to be +0; readable expected false to be true). Added one test in the same describe pinning BOTH directions over null, "", U+200B and em dash, also watched red on the pre-fix source (git checkout HEAD -- of the file, restored after).
- Verified on the rebased tree (branch ticket/BUG-0157 onto run/admin-window): ./node_modules/.bin/vitest run --project=offline tests/offline/review-item/page.test.ts = 121 passed; npm test = 72 files / 3272 + isolated 6; tsc --noEmit clean; npm run lint clean; npm run test:live -- tests/live/review-item.live.test.ts = 12 passed (staging, read-only).
- For QA: the ink-less arm deliberately KEEPS data-record (hook carries the unreadable ref) while the no-identity arm has none — that asymmetry is what makes BUG-0122's toHaveLength(0) pin and BUG-0157's toHaveLength(1) pin both true. The linked-but-ink-less entityId arm now dashes too (no anchor around nothing) though recordHref makes it unreachable.
- 2026-09-10T01:34:53Z [dispatcher] landed ticket/BUG-0157 onto run/admin-window as 55f79f9
- 2026-09-10T01:35:39Z [qa] built → qa: under attack
- 2026-09-10T01:49:19Z [qa] qa → done: Attacked on the landed tree (run/admin-window, fix 011e852 / land 55f79f9), in a detached _receipt- worktree; criteria re-run BEFORE reading the builder's story.

STORED CHECKS, verbatim, all exit 0: the two -t filters and the whole file (121 passed). Receipt GREEN on the branch tip (8f90247, which also carries the BUG-0155 lane's landings). Suite from my worktree: npm test = 72 files / 3273 offline + 6 isolated, tsc --noEmit clean, npm run lint clean.

THE PREDICATE, INDEPENDENTLY GRADED. recordColumn.absent and the cell now read ONE accessor (recordIdentity, evidence-cells.tsx:358) and branch on ONE predicate (hasVisibleContent). I re-derived the case table by rendering the real page, not by reading it: entity_id+recordHref -> anchor; ink-less identity -> the app's dash INSIDE data-record; identity null -> null body, no hook, DataTable's own orDash draws the dash (BUG-0122's toHaveLength(0) pin holds); producer's em dash -> verbatim, no dash, no note. Note <=> dash on every one.

BUILDER'S CLAIMS VERIFIED, NOT TAKEN. Restored 011e852^:evidence-cells.tsx into my worktree and re-ran the file: 4 failed / 118 passed - the two flipped it.fails AND the builder's own both-directions test are all genuinely red pre-fix. Restored with git checkout; no product source committed by me.

MY ATTACK (one integrated pass over the batch's surface):
1. Nine folded claims in nine ink states in ONE table, across all five columns that can dash here (record/value/tier/source/payload): value null, payload null, ref "" and U+200B, ref = the producer's em dash, no ref, ref carrying U+202E mid-string, a source the registry has no row for, a source the registry NAMES with no ink. Measured: 5 dashes drawn, each in the cell that holds nothing and in no other cell of its row; EXACTLY 1 dash-meaning line; the U+202E ref and the em-dash ref read verbatim (ink -> no absence); the ink-less registry name renders the source id verbatim (sourceLabel), so that column does not dash - BUG-0156's polarity, held. Landed as a plain  in the same describe, watched RED on the pre-fix source ("a reference with no ink: expected +0 to be 1", 4 dashes where the rule wants 5).
2. Linked-but-ink-less entity_id: renders the dash inside data-record with NO anchor (0 a[data-record]) - and it is UNREACHABLE, proved not argued: observations.entity_id is  (schema snapshot :3821), whose grammar admits no ink-less string, and recordHref refuses "" (routes.ts:49).
3. Bidi isolation: not needed here. A lone U+202E ref is ink-less, so the RLO reaches only the data-record ATTRIBUTE and the dash is the app's own element; a ref WITH ink renders inside a <td>, a block box, so a mid-string override reorders that cell alone (BUG-0150's own measured ruling).
4. Note placement unchanged: one <p data-absence-note="dash"> before the table it explains, asserted by document order, not by class.
5. STAGING, on a PRODUCTION BUILD of this tree (npm run build + start on my own bind-probed port 8791 - never 8770/8771 - minted cookie, read-only, server killed by pid, port confirmed free): item 01a06287-5b67-7d1f-b4a5-d90aa93cb01a = 200, 91 evidence rows, 91 record hooks, 0 anchors, 0 dashes, 0 dash-meaning lines. Exactly the ticket's own claim that staging holds no such row, and the fix moves nothing an operator sees today.

Could not break it. Closing on the goal, not just the floor: the sentence the page prints about a dash is now true of every dash it prints in that table.

RESIDUALS (no ticket, lean depth - both pre-date this fix and neither is on a path a criterion drives): (a) /claims renders 56 app dashes and /queues 54, both with 0 dash-meaning lines - the LESSONS-1 sentence exists only on the review item's claims table, the close slot and the verdict log; it becomes a ticket if the rule is meant to be app-wide. (b) The dial's TrendTable hands null cells to DataTable's orDash inside the same data-surface="evidence" as the claims table, so a null measure would put an unexplained dash beside an explained one; trigger: a gauge series with a null figure on a source-pattern item. [resolution: fixed] [receipt green: 3 cmd(s), tree 535dbf3a]
- 2026-09-10T01:49:33Z [qa] Two words were eaten by shell backtick expansion in the close note above: (1) "Landed as a plain [it] in the same describe" - the new pin is a plain it(), not it.fails; (2) "observations.entity_id is [uuid] (schema snapshot :3821)" - the column type is uuid, which is what makes the ink-less-entity_id arm unreachable.
