---
id: TASK-0022
type: task
status: open
milestone: M1
priority: P0
opened_by: architect
parent: FEAT-0002
children: []
assignee: null
claimed_at: null
depends_on: []
touch_scope: [tests/live]
force: false
---
# ASK BEN: what is installed on staging, and may a live test write fixture rows into resolver-owned tables?

## Description
**Contract silence.** ARCHITECTURE marker `OPEN-FIXTURES`. Folds SPEC named gaps 2 and 5 — they share one answer.

Acceptance tests 2, 3, 5, 10 and 11 assert rendered numbers against real staging rows. Two things could make that vacuous:

1. **The tables may not be there.** Schema truth is the scraper repo's `supabase/migrations/`, and the resolver campaign is still building it — a campaign is running in that repo right now. Every one of the resolver migrations (`20260901000001` `resolution_runs`, `...02` `review_items`, `...03` the rejection stamps, `...04` the `pending_claims` view, `...05` the canonical write path) says in its own header that **'the staging apply is separate and human-gated.'** So: **which of these are actually applied to the staging project today?**
2. **The tables may be empty.** If `review_items` exists with zero rows, 'renders real staging data' is satisfied trivially and every parity test compares 0 to 0.

The question that decides the harness: **may a live test write its own fixture rows into resolver-owned tables** (`observations`, `review_items`, `pending_claims`'s inputs) and sweep them? Acceptance test 13 sanctions a live suite that writes and sweeps; spec section 8 forbids an *app* write path to those domains. Those are not in conflict, but the difference matters and is not written down.

**Recommend:** the app keeps no write path to resolver-owned domains (spec section 8, unchanged), and the **live test suite** may insert a small, tagged fixture population into `observations` / `review_items` **through the service role, only when a table is present and only under the live marker**, sweeping every row in a `finally` (test 13). Tag every fixture row so the residue sweep can find it — a recognisable `external_ref` prefix such as `admincampaign-fixture-`. Without that permission, M1's parity tests can only assert 'the rendered number equals the number the database has', which is still a real bar but proves nothing about rendering a populated queue.

Second half of the answer: **if a column this campaign's SPEC names is missing on staging, that is reported, never invented.** Everything SPEC names was verified present in the scraper repo's migrations on 2026-09-01 — but authored is not applied. Read-only introspection of staging was not possible from here: the `STAGING_SUPABASE_*` names do not exist in `.env` yet, so this ticket's facts come from migrations, not from the live database.

## Acceptance criteria
(human-checkable) Ben states which resolver migrations are applied to staging today, whether the tables hold rows, and whether the live suite may write and sweep tagged fixture rows. The architect then removes the `OPEN-FIXTURES` marker from ARCHITECTURE and amends TASK-0003's harness scope and the affected pages' live criteria accordingly.

## Checks
```sh
bash -c "! grep -q OPEN-FIXTURES agenticflow/docs/vision/ARCHITECTURE.md"
```

## History
- 2026-09-02T05:45:08Z [architect] opened: ASK BEN: what is installed on staging, and may a live test write fixture rows into resolver-owned tables?
- 2026-09-02T05:45:09Z [architect] open → blocked: ASK BEN: which resolver migrations are applied to staging, and may live tests write and sweep tagged fixture rows in resolver-owned tables? Without an answer the parity tests may be comparing 0 to 0.
- 2026-09-02T16:13:51Z [dispatcher] Ben 2026-09-02 (ruling): live tests MAY write-and-sweep on staging groups/idols (the pre-cutover tables Admin already edits directly; one field on an existing row, prior value restored in a finally, residue check after) — approved as the interim practice with the scraper's live suites as precedent. Resolver-owned tables (events, venues, review_items, observations, field_provenance) are NEVER written by Admin tests. groups/idols are slated for deletion later; a dedicated fixture table is requested (panel note migrate-later-fixture-tables.md for ../MIGRATE_LATER.md). Architect: record and close this ASK.
- 2026-09-02T16:21:49Z [qa] The staging census this ASK asked for, MEASURED 2026-09-02 (first live pass; read-only, my own client through the same staging guard the live suite uses). Counts only, no values.

review_items 1 | observations 2567 | field_provenance 1705 | sources 4 | resolution_runs 28 | runs 5 | events 118 | event_listings 118 | venues 128 | event_performers 134 | groups 1759 | idols 7234 | pending_claims UNREADABLE | verdicts ABSENT (PGRST205 "Could not find the table 'public.verdicts'" — expected, it arrives with M2).

So the resolver tables ARE applied to staging and are not empty; the parity tests are not comparing 0 to 0 — with two exceptions that matter more than the totals:

1. `review_items` holds exactly ONE row: queue=entity_link, source_id NOT NULL, status=open, severity=low, evidence 2064 stored / 86 distinct. Broken down: data_conflict 0, entity_link 1, entity_link+source_id NULL 0, entity_link+source_id NOT NULL 1, open 1, settled 0. **The decision queue has no live coverage at all** — every decision-side parity assertion is comparing 0 to 0 today, and three of the queues suite's failures are that population meeting oracles written before staging was reachable (see TASK-0010).

2. `pending_claims` cannot be read. Any select of more than one row times out: `57014 canceling statement due to statement timeout` at ~8.1-8.5s, reproduced 8 times across 5 read shapes (the Claims page's own query; range(0,49) with orders; limit 50; limit 10 with no order or filter; a GET-shaped exact count). Only `limit 1` returns, in 737ms. So the count above is unobtainable, /claims is in its error state on staging, and the sources awaiting-row gauge with it (TASK-0012, TASK-0013). The view is the scraper repo's (migration 20260901000004) — a fix there is a handoff for Ben, not an edit from here.

Second half of the ASK — "if a column this campaign's SPEC names is missing on staging, that is reported, never invented": one is. `observations.entity_type` does not exist; the schema owner dropped it in migration 20260819000002 and the app still selects it. Reported as BUG-0024, not worked around.

Live-suite writes: run under the ruling recorded here on 2026-09-02. `tests/live/edit.live.test.ts` -> exit 0, 5 passed; residue scan after (29 text columns of `groups`, 34 of `idols`, ilike on the probe marker) returned 0 rows. Nothing was written to a resolver-owned table.
- 2026-09-02T16:42:20Z [architect] blocked → open: UNBLOCKED and ANSWERED — for QA to close as already_done; no code change is required by the answer, and the harness Ben approved is the one the edit test already runs.

Ben's ruling, 2026-09-02: live tests MAY write-and-sweep on staging GROUPS/IDOLS — the pre-cutover tables Admin already edits directly — one field of an existing row, prior value restored in a finally, residue scanned after. Resolver-owned tables (events, venues, review_items, observations, field_provenance) are NEVER written by an Admin test. A fixture population that would need one is reported as a gap, never inserted. Recorded in ARCHITECTURE §10 and DECISIONS.md; the OPEN-FIXTURES marker is out of §12 and this ticket's check dry-runs green.

The other half of the ASK is answered by QA's census (History above): the resolver migrations ARE applied to staging and the tables are populated, so parity is not comparing 0 to 0 across the board — with two exceptions the campaign now carries as facts rather than as hopes. (1) review_items holds exactly ONE row, an entity_link signal, so data_conflict is 0 and EVERY DECISION-SIDE LIVE ASSERTION IN M1 COMPARES 0 TO 0. That is a fact of the run, not an Admin defect: a decision item appears when the resolver escalates a real conflict, which is the scraper campaign's work. It is recorded in DECISIONS.md and noted on FEAT-0004, TASK-0010 and TASK-0011, and it means no ticket may close a gap by pointing at a 0-to-0 comparison. TASK-0032 makes those cases explicit empty assertions with a stated 0 and a line saying they are not coverage. (2) pending_claims is unreadable — TASK-0031, a scraper-repo handoff. The 'report, never invent' half worked exactly as designed: observations.entity_type was missing on staging and came back as BUG-0024 rather than as a workaround. Nothing was written to a resolver-owned table in that run.
