---
id: BUG-0030
type: bug
status: claimed
milestone: M1
priority: P2
opened_by: qa
parent: FEAT-0008
discovered_from: BUG-0028
children: []
assignee: builder-57
claimed_at: 2026-09-02T18:42:15Z
depends_on: []
touch_scope: [tests/offline/edit, eslint.config.mjs, tsconfig.json]
force: false
attempts: 3
claim_tree_hash: aa41a5008f6da65ffdbab3e8e2ced03bc53c27469dc687a73f44bffb2e71593b
---
# The admin_locked write guard's balanced-argument scan is not string-aware: it reports a READ as a write, and misses a real WRITE

## Description
RULING (architect, 2026-09-02) on QA's churn flag — bounce 3 of 3 on one class.
The inputs are changed; the approach below is NOT the fourth tokenizer round.

## Why the last three rounds failed

`codeOnly` is a hand-rolled JavaScript lexer living in a test file. Each fix
closed the case in front of it and opened the next, because "is this `/`
division or a regex, is this backtick a template or text" is not a corner —
it is the whole of lexing TypeScript, and the three bounces were three
different facts about the grammar (parens in a payload string; an unclosable
template blanking to EOF; a backtick inside a regex literal after `=>`, where
the `/` reads as division). A fourth patch is the same bet at longer odds.

## The disposition: stop lexing, use the compiler's parser

TypeScript's own parser already ships here — `typescript` ^5 (lock 5.9.3), a
devDependency, grandfathered in `agenticflow/docs/ALLOWED_DEPS.md:72`. No DEP
ticket, no new dependency. Verified 2026-09-02 that it resolves from this repo
(`require.resolve("typescript")` → `node_modules/typescript/lib/typescript.js`,
version 5.9.3). `ts.createSourceFile` + a small AST walk answers the question
the scan actually asks — "does a write verb receive this column" — and
strings, comments, template literals, interpolations and regex literals are
handled by construction rather than by heuristic.

Prototyped before ruling, not prescribed on faith:
`agenticflow/tracker/evidence/BUG-0030/ast/proto.mjs` runs the predicate below
over the real `src/` tree and 20 mirror-tree cases — every landed case in
`describe("the argument scan and string literals")`, all four mirror probes,
all three QA pins, plus five shapes nobody has tried (regex after `return`, a
nested payload object, a computed key spelling the column, a payload built by
a helper call, a `.tsx` write). 20/20 green on the first run, no per-case
tuning. Real tree, identical to today: `filesWritingColumn("admin_locked")`
=== `[]`, `writeArguments("src/lib/db/records.ts")` === `["{ [field]: value }"]`,
zero `src/` files with a parse error. The strict-typed form
(`evidence/BUG-0030/ast/typed.ts`) exits 0 under `tsc --strict` and under the
repo tsconfig's exact flags.

## Why not the alternative (drop the argument scan)

It was on the table and it is genuinely cheap, and the residual is narrower
than this ticket implies: `.insert(` / `.upsert(` / `.rpc(` are already banned
repo-wide and `.update(` is already pinned to `records.ts` alone by "writes the
database from exactly one module", so the only shape that slips a line-only pin
is a Prettier-wrapped `admin_locked` payload inside `records.ts` itself. I keep
the guard anyway: that wrapped shape is exactly what Prettier produces, after
this ticket the pin is ~45 lines of parser calls with no bespoke logic left in
it, and the campaign has already paid for this guard three times.

**Pre-decided escalation:** if a FOURTH corner is found in the AST route, do
not patch it. Take the drop-the-scan disposition — delete `writeArguments`,
`filesWritingColumn`, the `filesWritingColumn(ADMIN_LOCKED)` line of the rule
and this whole `describe`, keeping only the ruled line pin and the
`EDIT_CONFIG` value assertion, and record the wrapped-payload gap as a
residual. This paragraph is the authorisation; nobody needs to re-litigate it.

## What to build — test-only, one file, `tests/offline/edit/config.test.ts`

1. **Delete the tokenizer whole.** `codeOnly` and every part of it: `quoted`,
   `template`, `regexLiteral`, `previousSignificant`, `REGEX_MAY_FOLLOW`,
   `scan`, `blank`. No tokenizer and no regex over code text to decide a
   literal boundary survives this ticket. `sourceText`, `codeLines`,
   `filesWhereCodeMatches`, `sourceFiles` and `WRITE_VERB` all STAY — the
   ruled line pin and the other repo-wide rules use them.

2. **Parse instead.**

       const WRITE_VERBS = new Set(["update", "upsert", "insert"]);

       function parsed(file: string, base: string): ts.SourceFile {
         return ts.createSourceFile(
           file,
           sourceText(file, base),
           ts.ScriptTarget.Latest,
           /* setParentNodes */ true,
           file.endsWith(".tsx") ? ts.ScriptKind.TSX : ts.ScriptKind.TS,
         );
       }

   `setParentNodes` must be `true` — `node.getText(source)` needs it. `.tsx`
   must use `ScriptKind.TSX`: `sourceFiles()` walks `.tsx`, and a `.tsx` parsed
   as TS produces parse errors that would over-report the whole file.

3. **A write call is structural.** A `ts.CallExpression` whose `expression` is
   a `ts.PropertyAccessExpression` whose `name.text` is in `WRITE_VERBS`;
   walked with `ts.forEachChild`. This is what makes
   `.update(values).select("… admin_locked …")` safe by construction —
   `.select(` is a different call, not an argument of the write. BUG-0028's
   read can no longer be false-positived by accident.

4. **`namesColumn(node, column, source)` — does this ARGUMENT name the column?**
   - `ObjectLiteralExpression`: each property's NAME (an identifier's or a
     string-literal key's `.text`; a `ComputedPropertyName`'s
     `expression.getText(source)`), then recurse into a `PropertyAssignment`'s
     `initializer` and a `SpreadAssignment`'s `expression`.
   - `ArrayLiteralExpression`: recurse into elements.
     `ParenthesizedExpression`: recurse into `.expression`.
   - `ts.isStringLiteralLike(node) || ts.isTemplateExpression(node)`: **false**.
     A column named in PROSE inside a payload string is not a write of it —
     the one place text is deliberately not matched, pinned by the landed case
     "does not report the column when only a payload string names it".
   - anything else (identifier, call, conditional, `as`, …):
     `node.getText(source).includes(column)`. That default is the over-report
     direction on purpose: `update(buildPayload({ admin_locked: true }))` is
     reported; `update(values)` is not, because nothing spells it.

5. **A parse error is REPORTED, never skipped.**
   `(source as unknown as { parseDiagnostics?: readonly ts.Diagnostic[] }).parseDiagnostics ?? []`
   — `parseDiagnostics` exists at runtime and is `@internal` in typescript's
   d.ts, so it needs that cast; the cast is `unknown`-based, not `any`, and the
   helper typechecks strict-clean (measured). A file with any parse diagnostic
   is reported by `filesWritingColumn` without further inspection: a file the
   scan cannot see into is a file it may not stay silent about. This is what
   keeps QA's round-2 pin (`src/unterminated.ts`) green.

6. **Re-read once before reporting a parse error.**
   `tests/offline/db/layering.test.ts` writes and deletes a probe under `src/`
   while this suite runs in a parallel worker (BUG-0020, BUG-0029). Today a
   torn read is harmless — the tokenizer gives up and reports nothing — but
   under rule 5 it would be a flaky red on the real-tree assertion. On a parse
   error, read and parse the file a second time and use the second result. No
   fail-open either way: a genuinely unparseable file fails both reads; a
   half-written one either comes back whole (correct silence) or torn again
   (correct over-report).

7. **`writeArguments` keeps its name and shape** — `string[]`, the source text
   of every argument of every write call — because "extracts the argument of
   the one real write in this repo" asserts on it and that is what keeps the
   pin non-vacuous. `filesWritingColumn` no longer decides from that text; it
   decides from `namesColumn` over the argument NODES.
   **The one assertion in this file that changes:** in "does not report the
   column when only a payload string names it", drop
   `expect(argument).not.toContain(ADMIN_LOCKED)` — its premise was that
   `codeOnly` blanked the string's text out of the extracted argument, and
   there is no blanking any more. Replace it with the assertions that carry the
   same weight without the dead premise: the write call WAS found
   (`writeArguments("src/note-about-locking.ts", base)` has length 1 and its
   one entry contains `note:`), and `filesWritingColumn` is still `[]`.
   Nothing else in the file's assertions may change.

8. **The three QA pins become plain `it()`s and pass**; no `it.fails` survives
   in this file. The four mirror probes in
   `describe("the admin_locked write guard itself")` and every other case in
   `describe("the argument scan and string literals")` pass unchanged. Two case
   names are now inaccurate — correct their PROSE without touching their
   assertions: the QUOTED and TEMPLATE "over-reports rather than blinding
   itself" cases, because a line-continued string now parses cleanly and its
   write is a TRUE report, while the unterminated backtick is reported through
   rule 5 rather than through a give-up path.

9. **`filesWithWriteLineNaming` stays byte-identical** — sha1
   `e11757bbe2fea7d639397382f7514bc9b45302b2` over
   `awk '/^function filesWithWriteLineNaming/,/^}/'`, the value QA recorded and
   I re-measured today — and so does the `EDIT_CONFIG` value assertion. Both
   are checks.

10. **Scope.** `tests/offline/edit/config.test.ts`, plus the two already-landed
    ignore lines in `eslint.config.mjs` and `tsconfig.json`, which STAY: QA
    ruled them acceptable and `src/unterminated.ts` is still an
    unparseable-by-design fixture. No product code. No new dependency. No
    migration.

## Acceptance criteria
1. **The parser decides, not a lexer.** `tests/offline/edit/config.test.ts`
   decides "does a write verb receive `admin_locked`" through
   `ts.createSourceFile` and a `ts.forEachChild` walk that finds
   `CallExpression`s whose callee is a `PropertyAccessExpression` named
   `update` / `upsert` / `insert`. The hand-rolled tokenizer is gone:
   `codeOnly`, `quoted`, `template`, `regexLiteral`, `previousSignificant` and
   `REGEX_MAY_FOLLOW` do not appear in the file, and no regular expression is
   matched against code text to decide where a literal or an argument ends.
2. **The standing bar, unchanged: a MISS is never allowed.** No input, valid or
   malformed, may make `filesWritingColumn` fail to report a write of the
   column; over-reporting is the only permitted failure direction. A file the
   parser reports any parse diagnostic for is REPORTED, after one re-read and
   re-parse so another suite's half-written probe under `src/` is not a flaky
   red (`tests/offline/db/layering.test.ts` plants one while this suite runs).
3. **All three QA pins are plain `it()`s and pass**, no `it.fails` left in the
   file: parens inside a payload string (READ below not reported, WRITE below
   reported), an unclosable template above a write (reported), and a regex
   literal holding a backtick after `=>` with a later template (reported). The
   `return`-position form of the third is covered too.
4. **Every landed case still passes, unchanged.** The four mirror-tree probes
   in `describe("the admin_locked write guard itself")` keep their exact
   expectations — `byLine` === `[WRITE_LINE_PROBE]`, `byArgument` ===
   `[WRITE_LINE_PROBE, WRITE_SPREAD_PROBE].sort()`, and neither pin contains
   the READ or the CHAINED_READ probe — and so does every other case in
   `describe("the argument scan and string literals")`.
5. **Exactly one assertion changes**, as ruled in the description §7:
   `expect(argument).not.toContain(ADMIN_LOCKED)` in "does not report the
   column when only a payload string names it" is replaced by "the write call
   was found and its one argument contains `note:`", with
   `filesWritingColumn` still `[]`. Two case names whose prose is now
   inaccurate may have their COMMENTS corrected; no other assertion in the file
   may be added, removed or weakened.
6. **The ruled pin and the value assertion are untouched.**
   `filesWithWriteLineNaming` is byte-identical (sha1
   `e11757bbe2fea7d639397382f7514bc9b45302b2`) and the `EDIT_CONFIG` editable
   assertion is present verbatim.
7. **The real tree behaves exactly as it does today**:
   `filesWritingColumn("admin_locked")` is `[]`,
   `writeArguments("src/lib/db/records.ts")` is non-empty, and no file under
   `src/` is reported for a parse error.
8. **Scope and hygiene.** Test-only: `tests/offline/edit/config.test.ts` plus
   the already-landed `tests/.probes/**` ignore lines in `eslint.config.mjs`
   and `tsconfig.json`, which stay. No product code, no new dependency
   (`typescript` is an existing devDependency, ALLOWED_DEPS.md:72), no
   migration. `tests/.probes` is empty after the run; the suite, `tsc --noEmit`
   and `npm run lint` are all green.

## Checks
```sh
./node_modules/.bin/vitest run tests/offline/edit
./node_modules/.bin/tsc --noEmit
npm run lint
grep -q 'ts\.createSourceFile(' tests/offline/edit/config.test.ts
grep -q 'isPropertyAccessExpression' tests/offline/edit/config.test.ts
grep -q 'parseDiagnostics' tests/offline/edit/config.test.ts
! grep -qE 'codeOnly|REGEX_MAY_FOLLOW|regexLiteral|previousSignificant' tests/offline/edit/config.test.ts
! grep -q 'it\.fails' tests/offline/edit/config.test.ts
test "$(awk '/^function filesWithWriteLineNaming/,/^}/' tests/offline/edit/config.test.ts | shasum | cut -d' ' -f1)" = "e11757bbe2fea7d639397382f7514bc9b45302b2"
grep -qF 'expect(config.editable, table).not.toContain(ADMIN_LOCKED);' tests/offline/edit/config.test.ts
grep -qF 'does not report a READ that follows a payload string opening a paren' tests/offline/edit/config.test.ts
grep -qF 'over-reports rather than blinding itself when a TEMPLATE cannot be closed' tests/offline/edit/config.test.ts
grep -qF 'reports a WRITE between a regex literal holding a backtick and a later template' tests/offline/edit/config.test.ts
grep -q 'tests/.probes' eslint.config.mjs
grep -q 'tests/.probes' tsconfig.json
```
## History
- 2026-09-02T18:40:35Z [compacted → agenticflow/tracker/notes/BUG-0030.md]
  Summary (state; attempts; DEAD ENDS preserved):
  STATE (2026-09-02; reopened after 2 builder attempts / 2 QA bounces; awaiting a builder for
  the architect's AST rewrite. Test-only work in tests/offline/edit/config.test.ts.)
  - Landed on run/admin-window: round-1 fix ee8e570 (landed 87c87d7), round-2 fix c8c06e7
    (landed 1808e3c); QA pins 04456d1 and dc64acf. Byte-identity diffs were read from
    3a175df..ee8e570.
  - Current tree: ./node_modules/.bin/vitest run tests/offline/edit -> 72 passed / 3 files
    (count includes the one open it.fails pin); ./node_modules/.bin/tsc --noEmit exit 0;
    npm run lint exit 0 (measured 7.8s); npm test -> 1510 passed / 51 files (round 2);
    tests/.probes empty; git status clean.
  - OPEN FAILURE, must become a plain it():
    it.fails("reports a WRITE between a regex literal holding a backtick and a later template")
    in describe("the argument scan and string literals"), tests/offline/edit/config.test.ts.
    Watched RED as a plain it() first: "AssertionError: expected [] to deeply equal
    [ 'src/tick-regex.ts' ]" at :1088. Re-run: ./node_modules/.bin/vitest run tests/offline/edit
  - Architect dry-ran every stored check. RED now and they ARE the bar: ts.createSourceFile( ,
    isPropertyAccessExpression, parseDiagnostics present; codeOnly|REGEX_MAY_FOLLOW|
    regexLiteral|previousSignificant absent; no it.fails. GREEN now and must stay green: the
    sha1 pin on filesWithWriteLineNaming, the EDIT_CONFIG value assertion, the three QA pin
    titles, and the two tests/.probes ignore lines (eslint.config.mjs:23, tsconfig.json:33).
  - Architect also amended criteria (they read "(to be written)" through three rounds), checks,
    touch_scope ([tests/offline/edit] -> [tests/offline/edit, eslint.config.mjs, tsconfig.json])
    and milestone (none -> M1, matching parent FEAT-0008 and the BUG-0028 it descends from; it
    was milestone-null and invisible to the scheduler's milestone view, DEBT-0005 shape).
    NO breaker reset: the breaker never tripped — prior-attempts reads "empty-diff exits: 0;
    same failure signature seen 0x", no BUG-0030 entry in tracker/events.jsonl, no breaker
    state file. QA's churn flag is a split-or-escalate ruling, not a reset.
  
  ARCHITECT RULING 2026-09-02, operative parts verbatim (full text also in the Description) —
  bounce 3 of 3 on one class; the inputs are changed and this is NOT the fourth tokenizer round:
  - "stop lexing, use the compiler's parser": decide "does a write verb receive admin_locked"
    through ts.createSourceFile and a ts.forEachChild walk finding CallExpressions whose callee
    is a PropertyAccessExpression named update / upsert / insert, reporting a file if any
    ARGUMENT names the column (namesColumn over argument NODES, never over text). typescript is
    an existing devDependency (ALLOWED_DEPS.md:72) — no DEP ticket. .update(values).select("...
    admin_locked ...") is then safe by construction, so BUG-0028's read cannot be
    false-positived by accident.
  - "A parse error is REPORTED, never skipped ... a file the scan cannot see into is a file it
    may not stay silent about" (via the unknown-based parseDiagnostics cast). "Re-read once
    before reporting a parse error": tests/offline/db/layering.test.ts plants and deletes a probe
    under src/ while this suite runs in a parallel worker (BUG-0020, BUG-0029), so on a parse
    error read and parse a second time and use the second result. No fail-open either way.
  - filesWithWriteLineNaming "stays byte-identical" — sha1
    e11757bbe2fea7d639397382f7514bc9b45302b2 over
    awk '/^function filesWithWriteLineNaming/,/^}/' , the value QA recorded and the architect
    re-measured — "and so does the EDIT_CONFIG value assertion. Both are checks."
  - THE ONE LEGITIMATELY CHANGED ASSERTION: in "does not report the column when only a payload
    string names it", drop expect(argument).not.toContain(ADMIN_LOCKED) — "its premise was that
    codeOnly blanked the string's text out of the extracted argument, and there is no blanking
    any more" — replaced by: the write call WAS found (writeArguments("src/note-about-locking.ts",
    base) has length 1, its one entry contains note:) and filesWritingColumn is still [].
    "Nothing else in the file's assertions may change." Only the PROSE/COMMENTS of the QUOTED and
    TEMPLATE "over-reports rather than blinding itself" cases may be corrected.
  - Prototyped before ruling, not prescribed on faith:
    agenticflow/tracker/evidence/BUG-0030/ast/proto.mjs, run with `node <path>` from the repo
    root. Last run 2026-09-02: REAL filesWritingColumn: [], REAL records.ts args:
    ["{ [field]: value }"], REAL unparseable files: [], then ALL CASES PASS — 20/20 first run,
    no per-case tuning, covering all three QA pins, all four mirror probes, every landed
    string-literal case and five new shapes. A prototype, not the deliverable.
    evidence/BUG-0030/ast/typed.ts is the strict-typed form incl. the parseDiagnostics cast,
    exit 0 under tsc --strict and under the repo tsconfig's exact flags.
  - PRE-AUTHORISED ESCALATION, verbatim: "if a FOURTH corner is found in the AST route, do not
    patch it. Take the drop-the-scan disposition — delete writeArguments, filesWritingColumn,
    the filesWritingColumn(ADMIN_LOCKED) line of the rule and this whole describe, keeping only
    the ruled line pin and the EDIT_CONFIG value assertion, and record the wrapped-payload gap
    as a residual. This paragraph is the authorisation; nobody needs to re-litigate it."
  - FOR QA: the bar did not move — no input, valid or malformed, may make filesWritingColumn
    MISS a write; over-report is the only permitted direction. Newest targets: namesColumn's
    final branch (node.getText(source).includes(column)) is the single over-report default, and
    string/template literals are its one deliberate blind spot; parse-error reporting with one
    re-read is the newest moving part.
  
  TRIED
  - QA opened it (found while attacking BUG-0028, landed fd2ad40, which implemented its own
    ruled predicate correctly): the balanced-argument scan is not string-aware.
  - Round 1 (builder-54, ee8e570): tokenizer codeOnly() blanking the CONTENT of strings,
    templates (nested ${} rescanned as code), comments and regex literals, keeping delimiters
    and length so indices align; QA's two pins dropped .fails; four cases added; the QA
    plant/stringBase pair became a withProbes() helper giving each case its own mirror tree
    under tests/.probes/strings-<pid>-<n>/, removed in finally, plus a hygiene test. 66 passed;
    red-then-green watched twice (blinding codeOnly back to codeLines(...).join -> 6 failed /
    60 passed, so no new case is decorative).
  - QA round-1 attack: 14 cases through helpers extracted verbatim from config.test.ts against
    mirror bases under tests/.probes (never the shared src/ — another QA lane holds this tree);
    13 pass, 1 break (bounce cause 2).
  - Round 2 (builder-54, c8c06e7), three fixes: (i) GIVE-UP CONTRACT — template() gets
    quoted()'s: no closing backtick (or an interpolation that never closes) returns null, the
    caller undoes every blank it made and reads the backtick as ordinary code, instead of
    blanking to EOF; (ii) RAW-FILE TOKENIZING — writeArguments tokenizes the raw file via a new
    sourceText helper (extracted from codeLines' reader) instead of the codeLines join, so no
    line filter can hide a closer, and filesWritingColumn now asks EVERY source file instead of
    pre-filtering through filesWhereCodeMatches; (iii) BLOCK-COMMENT PAYLOAD — that filter drops
    any line whose trimmed form starts with "/*", so `/* legacy */ admin_locked: true,` in a
    wrapped payload was invisible to both the scan and the file choice, a second silent
    fail-open the give-up alone does not fix. Ablation, each half reverted alone: raw read
    reverted -> "reports a WRITE on a payload line that opens with a block comment" red, 70/71;
    give-up reverted -> "over-reports rather than blinding itself when a TEMPLATE cannot be
    closed" red, 70/71; QA's own pin XPASSed the moment the fix landed. 71 passed.
  - Round 2 also landed, deliberately OUT OF TOUCH SCOPE, 1 line each: eslint.config.mjs ignores
    tests/.probes/** and tsconfig.json excludes it. src/unterminated.ts is unparseable BY DESIGN
    and a run killed mid-test leaks one — measured: a leaked fixture failed npm run lint
    ("Parsing error: Unterminated template literal"); with the two ignores, lint and tsc --noEmit
    stay green with the same fixture planted. QA RULED THEM ACCEPTABLE and they STAY:
    tests/.probes/** is gitignored (.gitignore:74), holds no product source, no check on src/ is
    weakened. Now in touch_scope and pinned as checks so a "cleanup" cannot drop them.
  - QA round-2 attack: 17 cases, 15 pass (closer inside ${} of another template; backtick in a
    string in an interpolation; a whole write inside an interpolation; backtick in a regex in
    ASSIGNMENT position; /* in a payload string on the write's line; "/*" above and "*/" below
    the write; file ending mid-${ with writes above and below; unterminated quote and unclosed
    block comment both over-reporting correctly; BUG-0028's read still not false-positived). All
    three bounced causes verified dead at that point against mirror trees. 1 break (cause 3).
  - Architect prototyped the AST route over the real tree and 20 mirror cases BEFORE ruling, then
    ruled the tokenizer out entirely.
  
  DEAD ENDS
  - THE CLASS (architect, verbatim): "codeOnly is a hand-rolled JavaScript lexer living in a test
    file. Each fix closed the case in front of it and opened the next, because 'is this /
    division or a regex, is this backtick a template or text' is not a corner — it is the whole
    of lexing TypeScript, and the three bounces were three different facts about the grammar
    (parens in a payload string; an unclosable template blanking to EOF; a backtick inside a
    regex literal after =>, where the / reads as division). A fourth patch is the same bet at
    longer odds." A FOURTH TOKENIZER ROUND IS RULED OUT.
  - BOUNCE CAUSE 1 — parens in a payload string: the scan counted parens inside string literals,
    so it reported a READ as a write and missed a real WRITE. Fixed round 1. Pin title, keep:
    "does not report a READ that follows a payload string opening a paren".
  - BOUNCE CAUSE 2 — unclosable template blanking to EOF; round 1's own fix created a NEW silent
    fail-open in codeOnly's template() path. Expected, per this ticket's contract restated in the
    landed doc comment at config.test.ts:363-368: "where a literal cannot be closed with
    certainty ... nothing is blanked", so the failure direction is a visible over-report, never a
    silent fail-open. Found: template() had no give-up path; with its closing backtick never met
    it did blank(chunk, at) to END OF FILE, so every write below vanished. Repro — VALID
    COMPILING TypeScript (tsc --noEmit --strict --target es2022 exits 0, node imports it;
    BULLETS = "first line\n  *") — mirror file src/bullets.ts: a template assigned to BULLETS
    whose closing backtick sits on a line whose trimmed form starts with "*", above a
    Prettier-wrapped db.from("field_provenance").update({ admin_locked: true }).eq("id", id).
    codeLines() drops any "*"-led line as commentary — here the one carrying the CLOSING backtick
    — and codeOnly then blanked from the opening backtick to EOF.
    filesWritingColumn("admin_locked", base) -> Expected ["src/bullets.ts"], Found [];
    writeArguments -> []. The ruled line pin missed it too (wrapped payload) and EDIT_CONFIG's
    value assertion does not cover a hand-built payload: a forbidden write of a resolver-owned
    column slipped EVERY pin, silently. Control proving defect, not limit: the same predicament
    for a double-quoted literal IS reported — quoted() returns null and blanks nothing; the
    asymmetry the fix documents was real for quotes and absent for backticks. Second trigger,
    same root: any unterminated backtick blanked to EOF. Pinned 04456d1, watched RED first; fixed
    round 2. Pin title, keep: "over-reports rather than blinding itself when a TEMPLATE cannot be
    closed".
  - BOUNCE CAUSE 3 — a backtick inside a regex literal after => or return, read as division.
    STILL OPEN; this is what the AST rewrite must kill. Found: filesWritingColumn(ADMIN_LOCKED,
    base) === [] on a file that writes it. Repro — mirror file src/tick-regex.ts, three ordinary
    declarations: an arrow `hasTick = (s: string) => /<backtick>/.test(s)`, then the same
    Prettier-wrapped .update({ admin_locked: true }) arrow, then `label = (n: number) =>
    <backtick>${n} rows<backtick>`. tsc --noEmit --strict --target es2022 exits 0 on that exact
    source and node imports it; same result with `return /<backtick>/.test(s)` in a function
    body. ROOT, config.test.ts:388 and :505 — REGEX_MAY_FOLLOW decides a / from ONE previous
    significant character, and neither => (prev char >) nor return (prev char n) is in that set,
    so the regex is read as division and its backtick as opening a template; template() (:416)
    then DOES find a partner — the next ordinary template literal in the file — so the round-2
    give-up contract never fires, and blank(chunk, at) erases everything between, the write with
    it. It slips EVERY pin: filesWritingColumn === [] AND filesWithWriteLineNaming === []
    (Prettier-wrapped payload), and EDIT_CONFIG's value assertion does not cover a hand-built
    payload. Round 1's end state, reached by a closer that is wrong rather than absent. Control
    proving defect, not limit: the same regex in ASSIGNMENT position (export const TICK =
    /<backtick>/;) IS handled — = is in the set — and the write below is reported. Pinned
    dc64acf.
  - QA RESIDUAL — not a ticket, DO NOT CHASE SEPARATELY: malformed input only — an unterminated
    double quote whose line holds /*, with a "*/" string below the write; quoted() gives up, the
    /* then opens a block comment that swallows the write. Fold in only if the same change covers
    it.
  - ROUND-1 DECLARED RESIDUAL (superseded by round 2's raw read, recorded so it is not
    rediscovered): a mid-line-opened block comment whose "*/" line codeLines strips is read as
    code (over-reports, red not silent). And the regex heuristic that became bounce cause 3: a /
    is a literal only where one may legally start (prev code char in ( , = : [ ! & | ? + - * % ^
    ~ ; { ) and only if it closes on the same line, else it stays division. src/lib/db/
    result.ts:257 is the only regex literal in src/ today.
  - PRE-FILTERING through filesWhereCodeMatches — abandoned round 2; it drops a whole line whose
    trimmed form starts with "/*", hiding both the write text and the file choice. Do not
    reintroduce a line-based pre-filter.
  - SKIPPING a file the parser cannot read — rejected by the ruling; reporting it is what keeps
    QA's round-2 pin (src/unterminated.ts) green.
  - DROPPING THE ARGUMENT SCAN — considered and rejected as round 3's route (but pre-authorised
    as the escalation above): "genuinely cheap, and the residual is narrower than this ticket
    implies: .insert( / .upsert( / .rpc( are already banned repo-wide and .update( is already
    pinned to records.ts alone by 'writes the database from exactly one module', so the only
    shape that slips a line-only pin is a Prettier-wrapped admin_locked payload inside
    records.ts itself. I keep the guard anyway: that wrapped shape is exactly what Prettier
    produces, after this ticket the pin is ~45 lines of parser calls with no bespoke logic left
    in it, and the campaign has already paid for this guard three times."
  - DEAD PREMISE: expect(argument).not.toContain(ADMIN_LOCKED) held only because codeOnly blanked
    the string's text; with no blanking it must be replaced as ruled above.
  - Real-tree behaviour that must not change: filesWritingColumn("admin_locked") === [],
    filesWithWriteLineNaming("admin_locked") === [], writeArguments("src/lib/db/records.ts")
    === ["{ [field]: value }"], no src/ file reported for a parse error.
- 2026-09-02T18:41:11Z [compactor] compactor addendum (cited-prose survival; no History content was lost in the compaction above): tests/offline/edit/config.test.ts cites this ticket at :325, :380, :499, :529, :743, :912, :1037. The :325 doc comment — which the ruled AST rewrite deletes — is where bounce cause 1 lives with its two concrete shapes; carried verbatim: "a parenthesis inside a payload STRING moved the argument boundary in both directions — .update({ note: "opens ( here" }) ran past the real ) and swallowed a later READ of the column, and .update({ note: "set by admin :)", admin_locked: true }) ended early and hid a real WRITE."
- 2026-09-02T18:42:15Z [builder-57] claimed (attempt 3)
