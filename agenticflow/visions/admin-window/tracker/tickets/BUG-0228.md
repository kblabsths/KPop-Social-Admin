---
id: BUG-0228
type: bug
status: done
milestone: null
priority: P2
opened_by: qa
parent: null
discovered_from: BUG-0227
children: []
assignee: null
claimed_at: null
depends_on: []
touch_scope: [src/lib/db/result.ts, src/lib/db, src/app, src/components, tests/http/absence.http.test.ts, tests/http/postgrest-stub.ts, tests/offline/db/result.test.ts]
force: false
attempts: 1
resolution: fixed
---
# A host answering 200 with a JSON ARRAY that is not this table's rows still crashes four surfaces with a 500

## Description
EXPECTED (admin-window/BUG-0224's bar, carried unchanged through BUG-0227): "Whatever a host answers ... the operator reads the app's refusal naming pending_claims (or review_items, or sources)." Positively, and this is the property that ends the class rather than one more shape of it: NO body a host can put on the wire may make a surface throw — every route ANSWERS (200, no error shell), with the app's refusal naming the object it read, or with the rows if rows arrived.

FOUND on the landed BUG-0227 tree (run/admin-window @ 2102d150), measured 2026-09-15 by QA over real HTTP against a production build (own 'next start', port 8801/8802, SUPABASE_URL pointed at a loopback stub):

    MODE alien  (200, body [{"message":"no upstream"}])
      /        -> 500 errorShell=true
      /claims  -> 500 errorShell=true
      /browse  -> 500 errorShell=true
      /cycles  -> 500 errorShell=true
      /queues  -> 200 (states=empty,error)
      /sources -> 200 (states=empty,error)

Same five-hundreds with [[{"claim_id":"c1"}]], ["a","b"], [{}] and [{"foo":1}]; with [null,null] ALL SIX routes 500. Server log: 'TypeError: Cannot read properties of undefined (reading \'trim\')', '... (reading \'length\')', and for the null elements '... of null (reading \'started_at\')' / '(reading \'bucket\')' / '(reading \'queue\')' / '(reading \'observation_id\')' / '(reading \'event_id\')'. The same server answers 200 with a refusal in BUG-0227's 'foreign' mode and 200 with the empty card in 'empty' mode, so this is the payload shape and nothing else.

MECHANISM: BUG-0227 widened the row-set question to isRowSet = Array.isArray (src/lib/db/result.ts:1044). An array of things that are not rows PASSES that guard, so readRows/readComplete return {kind:'ok', data} typed Row[], and the first column the render reads throws on an element that has no such property. Measured with the real client (@supabase/supabase-js 2.x) against the same loopback host: error=null, data=array(1) [{"message":"no upstream"}], count=null — nothing in the answer says it is not a row set.

NOT A REGRESSION and NOT A RETYPE OF BUG-0227: that ticket's bar was 'did an ARRAY arrive', and on every non-array shape I could reach (string body, number, JSON object, object beside a Content-Range, null body) the landed fix refuses correctly on all six routes. This is the next shape in the same trigger family (a host that is not this database answering for it: a wrong SUPABASE_URL, a proxy/gateway in front of the service).

TWO CANDIDATE ANSWERS, and the builder should not enumerate payload shapes a third time (LESSONS 13 — this derivation has already cost BUG-0210, BUG-0224, BUG-0227):
  (a) the read seam asks a stronger question — every element is a non-null, non-array object, and/or each db module's read validates the columns its own callers read (it knows them: the reads name explicit column lists, e.g. PENDING_CLAIM_COLUMNS); or
  (b) a surface simply cannot 500 — the route owns an error boundary so a render throw becomes the same refusal card, whatever caused it.
(a) alone does NOT close the class: [{}] and [{"foo":1}] are arrays of plain objects and still crash. If the builder judges the answer is (b) or a design change, BOUNCE TO THE ARCHITECT rather than shipping a third predicate.

REPRO (exact, no database needed):
1. tests/http/postgrest-stub.ts carries the mode, committed by QA with this ticket: 'alien' — status 200, body [{"message":"no upstream"}].
2. tests/http/absence.http.test.ts carries the pin, currently marked an EXPECTED FAILURE: "refuses every panel against a host answering 200 with an ARRAY that is not this table's rows". Flip it.fails to a plain it and run 'npm run test:http': RED at '/?probe=alien status: expected 500 to be 200', watched 2026-09-15 (1 failed | 34 passed).
3. THE FIX FLIPS THAT MARKER back to 'it' (check 2 refuses the close while it stands) and adds "alien" to EVERY_MODE in that file, so the call-site-prose sweep covers this mode like every other.

## Acceptance criteria
1. Against a host answering 200 with a JSON array that is not this table's rows, every route answers (200, no Next error shell): /, /claims, /queues, /sources, /browse, /cycles — and each graded surface renders only refusal states naming the object it read (pending_claims, review_items, sources), never an empty card and never a figure. 2. The same holds for the element shapes measured above: [[{...}]], ["a","b"], [null,null], [{}], [{"foo":1}]. 3. Answers PostgREST really gives are untouched: a real row set still renders its rows, and the 'empty' mode (200 [] with content-range */0) still draws the empty card on every graded surface. 4. One derivation, not a third spelling: whatever refuses here refuses in the SAME words unreadableAnswer already uses (BUG-0224 criterion 4), or — if the answer is a render boundary rather than a read predicate — the ticket is bounced to the architect instead. 5. The offline suite stays green with no assertion deleted or loosened (npm test), and npm run test:http is green including the new mode.

## Checks
```sh
npm run test:http
! grep -F -n 'it.fails("refuses every panel against a host answering 200 with an ARRAY' tests/http/absence.http.test.ts
./node_modules/.bin/vitest run --project=offline tests/offline/db/result.test.ts
./node_modules/.bin/tsc --noEmit
npm run lint
```

## History
- 2026-09-15T04:13:42Z [qa] opened: A host answering 200 with a JSON ARRAY that is not this table's rows still crashes four surfaces with a 500
- 2026-09-15T04:18:19Z [builder-328] claimed (attempt 1)
- 2026-09-15T04:40:52Z [builder-328] claimed → built: FIX (answer (a), the column half — NOT a third payload-shape predicate, and not bounced): src/lib/db/result.ts now asks one question derived from the read's own declaration — a row set is an array whose every element is a plain object carrying EVERY column the query NAMED. Each lib/db read declares its columns once as an array; selectList() builds the .select() string from it and the same array goes to the seam, so query and guard cannot drift. readOne asks the column half alone (the home page's last-applied leg and the record/detail reads 500'd the same way). Refuses through the SAME unreadableAnswer (criterion 4). ANY_COLUMNS is the spelled-out opt-out with the reason at each call site: verdicts readiness probe (select *), and the four GENERIC RECORD reads — records.ts pins 'a mapped column the read returned nothing for draws as the absence' (tests/offline/records/page.test.ts), so a short row is a row that page renders, and those reads guard every value with typeof before reading it. FOLDED IN (same derivation, in scope): a count is Number.isInteger or it did not arrive — closes the count:NaN from an unparseable Content-Range.

PROOF: npm run test:http 35/35 green including the flipped pin, now looped over ALL SIX bodies x all six routes (stub gained setAlienBody; alien still sends NO content-range, exactly as QA measured — giving it one makes counts real answers and figures legitimately publish). Watched red first: with the marker still it.fails the suite reported 'Expect test to fail' (1 failed | 34 passed). 5 of 6 new offline tests watched RED against the pre-fix predicates. npm test 4024 green (0 assertions deleted/loosened), tsc, lint, build green, all re-run after rebase onto fc95b7a1. LIVE, because this touches every lib/db read: 94 staging tests green (browse/claims/cycles/dashboard/queues/review-item/runs/sources/harness/edit) — real PostgREST rows all pass the guard. Sandbox reset before and after edit.live.

QA SHOULD KNOW: (1) OUT OF touch_scope, 3 files, all signature-follow or fixture-honesty: tests/live/harness.live.test.ts, tests/offline/gauges/gauge.test.ts (+columns arg), tests/offline/dashboard/read.test.ts — its HEALTHY script fed ONE array to both a window read and a .maybeSingle() seek on resolution_runs; it now answers each the shape PostgREST really gives. (2) RESIDUAL, named honestly: a host answering objects that carry every declared column with values of the WRONG JS TYPE can still throw. Closing that structurally is a route error boundary — a design change, the architect's, not mine.
- 2026-09-15T04:41:17Z [dispatcher] landed ticket/BUG-0228 onto run/admin-window as fa41daaf
- 2026-09-15T04:42:08Z [qa] built → qa: under attack
- 2026-09-15T04:55:29Z [qa] qa → done: ATTACKED, criteria verified on the landed tree (run/admin-window @ 0d765273, fa41daaf's merge), receipt GREEN (5/5 checks, exit 0).

CRITERIA. 1+2 re-run myself before reading the handoff: npm run test:http 35/35, the flipped pin looping all six alien bodies x six routes. 3: the empty control is green AND — the real risk of a column guard — 94 targeted live tests against real PostgREST rows on staging (browse/claims/cycles/dashboard/queues/review-item/runs/sources/harness) all pass; 'renders every row the table holds' and 'renders every count and every error line the row carries' are real rows through the new predicate, not vacuous greens. 4: one derivation — unreadableAnswer is the only refusal added; no third predicate, no payload-shape list. 5: npm test 4025 (4024 + my own new pin), lint and tsc clean.

DERIVATION AUDIT (point 1 of my brief): every '.select(' under src/lib/db/** is selectList(<declared array>) and the SAME array reaches the seam — 31 sites checked pairwise (readRows/readComplete/readOne/readRowsByIds all take columns; readRowsByIds forwards to readRows). The only select strings not built from a declaration are the two spelled-out opt-outs: verdict.ts:265 readiness probe (select *) and records.ts recordColumns(config). No drift found.

POINT 2 — the records.ts ANY_COLUMNS exclusion is CORRECT, not a dodge: the record page's pinned contract (tests/offline/records/page.test.ts:2625-2633) renders a fixture row of {venue_id} alone — a row MISSING its other mapped columns — and asserts every mapped line is still drawn. Passing mappedColumns(config) would have reddened that pin. Measured consequence, over HTTP against a production build with an alien host: /records/{events,venues,walk_sandbox}/<uuid> and /queues/<uuid> all answer 200 with no error shell on all six alien bodies, so the exclusion costs no 500.

POINT 3 — the three out-of-scope test edits are honest: harness.live and gauge.test are signature-follow (+columns arg, and gauge.test now builds its select with selectList so the fixture cannot drift either); dashboard/read.test.ts's HEALTHY fixture fed ONE array to both a window read and a .maybeSingle() seek — a shape PostgREST cannot give — and now answers each its real shape. No assertion deleted or loosened; I re-ran the full offline suite to confirm.

POINT 4 — the named residual REPRODUCED and I agree it is the architect's: rows carrying every declared column with wrong-typed values (all-object) 500 /, /claims, /sources, /browse, /cycles; the all-number/array/bool variants 500 / alone. It cannot be derived from a column list (no type declaration exists), so closing it is a route boundary or per-column types — a design change, and LESSONS 13 forbids a fifth payload predicate. Residual, not a ticket from me.

BROKE SOMETHING ELSE, filed BUG-0229 (P2, discovered_from this): the folded-in count guard is Number.isInteger, which is TRUE for a negative and for 1e20, so a host answering 200 [] with 'content-range: */-5' publishes -5 in EVERY bucket of /claims' total-counts table (and */1e20 publishes 100,000,000,000,000,000,000) — measured over real HTTP on a production build. Pre-existing (the old null/undefined test let it through too), one line, no enumeration. Pin watched RED, committed as it.fails at 0d765273.

Two residuals for the digest, neither filed: (a) /records/walk_sandbox/<uuid> against an alien host draws a full record page with every field as the absence dash — the ANY_COLUMNS contract working as pinned, but a nonexistent row presented as an editable record; (b) src/lib/admin.ts:14-22 requireAdmin() is NOT a lib/db read — against an alien host answering [{...}] it grants access (the /api/admin/*/rows routes answered 200), while ['a','b'] 403s. Out of this ticket's derivation, reachable only from a host that is not the database, and a valid session cookie is still required. [resolution: fixed] [receipt green: 5 cmd(s), tree 0e3580bd]
