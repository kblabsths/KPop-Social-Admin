---
id: TASK-0015
type: task
status: qa
milestone: M1
priority: P1
opened_by: architect
parent: FEAT-0007
children: []
assignee: builder-18
claimed_at: 2026-09-02T09:05:24Z
depends_on: [TASK-0005, TASK-0002, TASK-0004, TASK-0026]
touch_scope: [src/app/browse/page.tsx, src/lib/browse, src/lib/db/browse.ts, src/components/browse, tests/offline/browse, tests/live/browse.live.test.ts]
force: false
attempts: 1
---
# Browse — the recent-events curated view and its column selector

## Description
Module: `src/app/browse/page.tsx` + `src/lib/browse/views.ts` (the view definitions) + `src/lib/db/browse.ts`. Authority: spec section 4 (Browse — 'each view is defined in code — the query, the columns it may show, the default sort — with a runtime column selector over the configured set'); `contracts/data-model.md` (per-field provenance); SPEC F7; M1 EC9; acceptance tests 10, 2, 9.

- **Exactly one curated view ships: recent events.** Everything that came through the pipeline, **newest first** — that is arrival order, `events.created_at desc`, not the calendar; `starts_at` is a column the view shows, never its sort (ARCHITECTURE section 11).
- The spot-verification columns: **title, description, poster image, date, venue** — and **the source(s) behind the row, read from the `field_provenance` join** (`entity_type = 'events'`, `entity_id = event_id`), resolved to source names through `field_provenance.source_id`. Read events through `event_listings` where the venue name is wanted; the keys are `event_id` (AGENTS.md).
- **The view is defined in code**: a single exported definition carrying its query, the columns it *may* show, and its default sort. The **runtime column selector shows and hides exactly the configured set** — every configured column offered, nothing outside it — and its state lives in `searchParams`.
- An event row links to `/records/events/<event_id>` — which is read-only for events in M1.
- **No whole-table browsing, no free-SQL runner, no second curated view** (spec section 1 and section 4 rationale). Adding a second view is a later decision, not a builder's.

Standing rules for every page ticket (ARCHITECTURE sections 4, 5, 7): the page function is the only async component on the route and every child is a pure sync component; read only through `src/lib/db/**`; table names live only in `tables.ts`; render all four states from the `ui` primitives (a missing table never throws and never renders as 0); tokens and primitives, never raw values; every filter, sort and tab is a `searchParams` value; ages relative with the absolute in the title attribute, scheduled times absolute UTC with the zone in the column header, machine identifiers verbatim in mono; **nothing settles anything in M1**.

## Acceptance criteria
[tests 10, 2, 9; EC9] Offline tests assert: rows render newest-first by `created_at`; every spot-verification column renders, including the source names from the provenance join; the column selector's option set **equals** the view definition's configured column set — every configured column offered and nothing outside it — and toggling a column changes exactly that column; the selector state round-trips through the URL. A live-marked test asserts the rendered order and the first page of rows against a query the test issues itself. Exactly one view definition exists. No table browser, no SQL-executing route and no second view exists in the repo. With `field_provenance` absent the page renders its not-provisioned state and the event rows still render or the page says which table is missing — either way nothing throws.

## Checks
```sh
npm run lint
./node_modules/.bin/tsc --noEmit
npm run build
./node_modules/.bin/vitest run tests/offline/browse
bash -c "test -f src/lib/browse/views.ts && grep -qE 'fieldProvenance|field_provenance' src/lib/db/browse.ts"
bash -c "! grep -rqiE 'select \* from|rpc\(|execute_sql' src"
```
## History
- 2026-09-02T05:41:06Z [architect] opened: Browse — the recent-events curated view and its column selector
- 2026-09-02T07:55:09Z [architect] checks amended: false-RED against a landed contract: the check demanded the literal 'field_provenance' inside src/lib/db/browse.ts, but ARCHITECTURE rule 4.4 (landed as TASK-0002's src/lib/db/tables.ts) says only tables.ts spells a table or view name — a conforming builder writes T.fieldProvenance and goes red. The pin now accepts either spelling, so it proves browse.ts reads provenance without forcing a rule-4 violation. Other checks verbatim. [was: npm run lint, ./node_modules/.bin/tsc --noEmit, npm run build, ./node_modules/.bin/vitest run tests/offline/browse, bash… (269 chars; full text in git history)]
- 2026-09-02T07:56:07Z [architect] Test-idiom ruling (DECISIONS.md 2026-09-02): the offline include glob stays `tests/offline/**/*.test.ts` — no `.tsx`, no JSX, no jsdom. Component tests build elements with `createElement` and assert emitted markup. Import the landed shared helper `tests/offline/ui/markup.ts` (`h` = createElement, `render` = renderToStaticMarkup, `classesOf`, `tagsOf`, `textOf`) instead of writing your own render helper; see tests/offline/ui/primitives.test.ts for the pattern. This adds no work and no scope — it names the helper you reuse.
- 2026-09-02T08:23:10Z [architect] depends_on: [TASK-0005, TASK-0002, TASK-0004] → [TASK-0005, TASK-0002, TASK-0004, TASK-0026]: Adds TASK-0026 (complete reads). Every lib/db list read in this milestone goes through the one readComplete helper — an ok row set is the whole matching set or the read refuses. Without the edge, eight builders who cannot see each other's code each invent a bound; see ARCHITECTURE.md section 4.3.
- 2026-09-02T08:23:54Z [architect] Architect ruling, 2026-09-02 — read kinds, now ARCHITECTURE.md section 4.3. Cited on every ticket that owns a `src/lib/db/*.ts` module.

QA found `src/lib/db/review-items.ts` (TASK-0006) building its list query with no `.range()`, no `.limit()` and no `.order()`. Against PostgREST's `db-max-rows` (Supabase default 1000) that truncates silently, in unspecified order — so a count or an "exactly the matching items" claim built on it would be wrong rather than refused. You are one of eight builders about to write a `lib/db` module without seeing each other's code; the bound is decided once, in TASK-0026 (now a dependency), not eight times.

Two read kinds. Pick deliberately; do not blur them.

1. **Complete read — `readComplete(missing, run, db?)`, new in `src/lib/db/result.ts`.** Use it whenever the surface presents the result as the whole set: a list the page shows in full, a count, an oldest age, an "exactly the matching items" claim. The `run` callback passes `{ count: "exact" }`, a total `.order()` ending in the primary key, and `.range(0, cap - 1)` with the cap handed in. If the exact count exceeds the rows returned, it returns `kind: "error"` naming the object, the count and the cap. **An `ok` array from a complete read is the whole matching set** — that property is what every figure in this app rests on, and it is why no caller needs a "was this all of it?" flag.
2. **Window read — `readRows`, unchanged.** The caller's own `.order()` + `.limit()` define a named window, and the surface says which window it is showing. This is section 8's gauge contract and TASK-0007's landed gauges use it correctly. A window read's rows must never become a figure presented as a total.

Also: no read helper may substitute a number the database did not give. `readCount` returned `count ?? 0` — BUG-0007's user-visible twin — and TASK-0026 makes it refuse a null count instead. Do not reintroduce a `?? 0` on any read path.
- 2026-09-02T08:24:14Z [architect] Architect ruling, 2026-09-02 — pure domain leaves sit BELOW `lib/db`, not above it (ARCHITECTURE.md section 4, arrow diagram + new rule 7).

QA flagged on TASK-0006 that `src/lib/db/review-items.ts` imports `src/lib/review/shapes.ts` — the reverse of the arrow the contract drew. The code was right and the diagram was wrong: `shapes.ts` has zero imports, no cycle is constructible, and TASK-0006's own two-module contract required the split. The diagram is amended rather than the code.

The rule you are working under, because your ticket owns both a domain module and a `lib/db` module:

- A **pure leaf** — today `src/lib/review/**`, `src/lib/format.ts`, `src/lib/edit/config.ts`, and `src/lib/browse/**` when it lands — imports NOTHING that can reach a database: not `lib/db/**`, not `@supabase/supabase-js`, not `process.env`. It holds the vocabulary, the row interfaces it reasons about, and pure functions over rows.
- `lib/db/**` imports the leaf. **The leaf never imports `lib/db/**` back — not a value, and not a type.** A type-only import would erase at runtime, but it still writes a directory-level cycle into the contract, and the day someone changes it to a value import the cycle is real with nothing to catch it. If both sides need a row type, **declare it in the leaf** — that is what `ReviewItemRow` in `shapes.ts` already does, correctly.
- `lib/gauges/**` is NOT a leaf and is unaffected: a gauge fetches its own bounded window through `lib/db`, which is the arrow as drawn (section 8, TASK-0007 as landed).
- 2026-09-02T09:05:24Z [builder-18] claimed (attempt 1)
- 2026-09-02T09:24:09Z [builder-18] claimed → built: - One view, defined in code: src/lib/browse/views.ts (query shape, columns it may show, default sort) + rows.ts (row types, pure join). src/lib/db/browse.ts binds them to T: a WINDOW read of events ordered created_at desc + event_id desc, limit 50 (arrival, never starts_at), then three COMPLETE reads over that window's ids — eventListings for venue_name, fieldProvenance (.eq entity_type, T.events) for the decisions, sources for their names. Each leg fails independently: field_provenance absent => rows still render, Sources cells dash, a NotProvisioned card names the table. Selector state is ?cols=; it offers exactly the configured set and a toggle changes exactly one column.
- Verified on the rebased tree (onto e601f1b): npm run lint / tsc --noEmit / npm run build / vitest run tests/offline/browse (83) / both grep checks — all 0; full npm test 531 pass. Red-then-green observed: mutating arrivalOrder to sort on starts_at reddens 10 tests incl. the page's row-order test; dropping the .eq("entity_type") reddens the provenance test.
- Traps + chosen forks (say if any is wrong): (1) events window is deliberately a WINDOW read, not readComplete — events is a growing catalog and a complete read would refuse the page past ROW_CAP; the three id-set legs ARE complete, so a truncated provenance set refuses instead of understating a row's sources. (2) Out of touch_scope: tests/fixtures/rows.ts gains eventRow/eventListingRow — the event_listings view carries NO created_at (migration 20260825000004), which is why the window comes off events and only the venue name off the view. (3) window=50 and "toggling off the last column is a no-op" were under-specified; both chosen, documented in views.ts. (4) The configured set is the six spec-named spot-verification columns PLUS "arrived" — the column the sort is on. (5) Poster renders as a 24px img with a targeted eslint-disable: next/image would need every scraped poster host allowlisted in next.config.ts. (6) tests/live/browse.live.test.ts asserts rendered order and the first page against its own independent queries; npm run test:live correctly refuses today (STAGING names + SERVICES.md absent) — that refusal is the passing state, not this file failing.
- 2026-09-02T09:24:31Z [dispatcher] landed ticket/TASK-0015 onto run/admin-window as 280985b
- 2026-09-02T09:25:06Z [qa] built → qa: under attack
