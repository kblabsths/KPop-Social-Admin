---
id: TASK-0015
type: task
status: built
milestone: M1
priority: P1
opened_by: architect
parent: FEAT-0007
children: []
assignee: builder-21
claimed_at: 2026-09-02T09:47:35Z
depends_on: [TASK-0005, TASK-0002, TASK-0004, TASK-0026]
touch_scope: [src/app/browse/page.tsx, src/lib/browse, src/lib/db/browse.ts, src/components/browse, tests/offline/browse, tests/live/browse.live.test.ts]
force: false
attempts: 2
compact_threshold: 22000
empty_diffs: 1
---
# Browse — the recent-events curated view and its column selector

## Description
Module: `src/app/browse/page.tsx` + `src/lib/browse/views.ts` (the view definitions) + `src/lib/db/browse.ts`. Authority: spec section 4 (Browse — 'each view is defined in code — the query, the columns it may show, the default sort — with a runtime column selector over the configured set'); `contracts/data-model.md` (per-field provenance); SPEC F7; M1 EC9; acceptance tests 10, 2, 9.

- **Exactly one curated view ships: recent events.** Everything that came through the pipeline, **newest first** — that is arrival order, `events.created_at desc`, not the calendar; `starts_at` is a column the view shows, never its sort (ARCHITECTURE section 11).
- The spot-verification columns: **title, description, poster image, date, venue** — and **the source(s) behind the row, read from the `field_provenance` join** (`entity_type = 'events'`, `entity_id = event_id`), resolved to source names through `field_provenance.source_id`. Read events through `event_listings` where the venue name is wanted; the keys are `event_id` (AGENTS.md).
- **The view is defined in code**: a single exported definition carrying its query, the columns it *may* show, and its default sort. The **runtime column selector shows and hides exactly the configured set** — every configured column offered, nothing outside it — and its state lives in `searchParams`.
- An event row links to `/records/events/<event_id>` — which is read-only for events in M1.
- **No whole-table browsing, no free-SQL runner, no second curated view** (spec section 1 and section 4 rationale). Adding a second view is a later decision, not a builder's.

Standing rules for every page ticket (ARCHITECTURE sections 4, 5, 7): the page function is the only async component on the route and every child is a pure sync component; read only through `src/lib/db/**`; table names live only in `tables.ts`; render all four states from the `ui` primitives (a missing table never throws and never renders as 0); tokens and primitives, never raw values; every filter, sort and tab is a `searchParams` value; ages relative with the absolute in the title attribute, scheduled times absolute UTC with the zone in the column header, machine identifiers verbatim in mono; **nothing settles anything in M1**.

## Acceptance criteria
[tests 10, 2, 9; EC9] Offline tests assert: rows render newest-first by `created_at`; every spot-verification column renders, including the source names from the provenance join; the column selector's option set **equals** the view definition's configured column set — every configured column offered and nothing outside it — and toggling a column changes exactly that column; the selector state round-trips through the URL. A live-marked test asserts the rendered order and the first page of rows against a query the test issues itself. Exactly one view definition exists. No table browser, no SQL-executing route and no second view exists in the repo. With `field_provenance` absent the page renders its not-provisioned state and the event rows still render or the page says which table is missing — either way nothing throws.

## Checks
```sh
npm run lint
./node_modules/.bin/tsc --noEmit
npm run build
./node_modules/.bin/vitest run tests/offline/browse
bash -c "test -f src/lib/browse/views.ts && grep -qE 'fieldProvenance|field_provenance' src/lib/db/browse.ts"
bash -c "! grep -rqiE 'select \* from|rpc\(|execute_sql' src"
```
## History
- 2026-09-02T09:39:49Z [compacted → agenticflow/tracker/notes/TASK-0015.md]
  Summary (state; attempts; DEAD ENDS preserved):
  **State** — opened 2026-09-02 by architect; attempt 1 by builder-18 landed onto run/admin-window as 280985b; QA bounced it; status reopened.
  
  Built: one curated view defined in code — `src/lib/browse/views.ts` (query shape, columns it may show, default sort) + `src/lib/browse/rows.ts` (row types, pure join), both PURE LEAVES; `src/lib/db/browse.ts` binds them to `T`; `src/app/browse/page.tsx` + `src/components/browse/{browse-table,column-selector}.tsx`; `tests/live/browse.live.test.ts` issues its own independent queries. Selector state is `?cols=`.
  
  Read shape: a WINDOW read (`readRows`) of `events` ordered `created_at desc` then `event_id desc`, `.limit(50)` — arrival order, never `starts_at` — then three COMPLETE reads (`readComplete`) over that window's ids: `eventListings` for `venue_name`, `fieldProvenance` (`.eq` entity_type, `T.events`) for the decisions, `sources` for their names. Legs fail independently: `field_provenance` absent => rows still render, Sources cells dash, a NotProvisioned card names the table.
  
  All six stored Checks re-run verbatim by QA at 280985b and GREEN: `npm run lint` 0; `./node_modules/.bin/tsc --noEmit` 0; `npm run build` 0; `./node_modules/.bin/vitest run tests/offline/browse` 83/83; `bash -c "test -f src/lib/browse/views.ts && grep -qE 'fieldProvenance|field_provenance' src/lib/db/browse.ts"` 0; `bash -c "! grep -rqiE 'select \* from|rpc\(|execute_sql' src"` 0. Full offline suite 531 pass before QA's pins, 534 after.
  
  Pending the builder: BUG-0010 only. Three strict xfails at commit d68ba55, currently expected-failing; flip `it.fails` back to `it(` with the fix; re-run with `./node_modules/.bin/vitest run tests/offline/browse`:
  - `tests/offline/browse/page.test.ts:202` — "does not name a superseded source, or a decision with no source"
  - `tests/offline/browse/views.test.ts:398` — "names only the source behind the current value, not a superseded one"
  - `tests/offline/browse/views.test.ts:424` — "contributes no source name for a decision that names no source"
  
  **Tried**
  - Window read on `events`, not `event_listings`, for arrival order.
  - The three id-set legs as `readComplete` so a truncated leg refuses.
  - Under-specified points chosen and documented in `views.ts`: window = 50; toggling off the last remaining column is a no-op.
  - Configured columns = the six spec-named spot-verification columns PLUS `arrived` (the sort column); default shows all seven.
  - Poster as a 24px `<img>` with a targeted eslint-disable.
  - Out of touch_scope, declared: `tests/fixtures/rows.ts` gained `eventRow` / `eventListingRow`.
  - Red-then-green observed: mutating `arrivalOrder` to sort on `starts_at` reddens 10 tests incl. the page's row-order test; dropping `.eq("entity_type")` reddens the provenance test.
  
  **Rulings binding this ticket.** Two architect rulings of 2026-09-02 were landed verbatim into `agenticflow/visions/admin-window/docs/ARCHITECTURE.md` and are cited on every ticket owning a `lib/db/*.ts` module — read them THERE, that doc is the authority, not this summary. §4.3 line 260, read kinds: **complete read** `readComplete(missing, run, db?)` in `src/lib/db/result.ts` whenever the surface presents the result as the whole set — an `ok` array from it IS the whole matching set, and it errors naming object/count/cap when the exact count exceeds the rows returned; **window read** `readRows` where the caller's own `.order()` + `.limit()` name a window the surface states — a window read's rows must never become a figure presented as a total; and no read helper may substitute a number the database did not give (`readCount`'s `count ?? 0` was BUG-0007's user-visible twin). That ruling added TASK-0026 to depends_on (now TASK-0005, TASK-0002, TASK-0004, TASK-0026); its origin was QA finding `src/lib/db/review-items.ts` (TASK-0006) with no `.range()`, `.limit()` or `.order()`, silently truncated by PostgREST `db-max-rows` (Supabase default 1000) in unspecified order. §4 rule 7 + amended arrow diagram, line 585: a pure leaf (`src/lib/review/**`, `src/lib/format.ts`, `src/lib/edit/config.ts`, `src/lib/browse/**`) imports nothing that can reach a database — not `lib/db/**`, not `@supabase/supabase-js`, not `process.env`; `lib/db/**` imports the leaf and **the leaf never imports `lib/db/**` back, not a value and not a type**; shared row types are declared in the leaf; `lib/gauges/**` is not a leaf and is unaffected.
  
  **Checks amendment, architect 2026-09-02** (the Checks above are the amended set): the original pin was a false-RED against a landed contract — it demanded the literal `field_provenance` inside `src/lib/db/browse.ts`, but ARCHITECTURE rule 4.4 (landed as TASK-0002's `src/lib/db/tables.ts`) says only `tables.ts` spells a table or view name, so a conforming builder writes `T.fieldProvenance` and goes red. The pin now accepts either spelling; other checks verbatim.
  
  **Test-idiom ruling, DECISIONS.md 2026-09-02**: the offline include glob stays `tests/offline/**/*.test.ts` — no `.tsx`, no JSX, no jsdom. Component tests build elements with `createElement` and assert emitted markup; import the landed shared helper `tests/offline/ui/markup.ts` (`h`, `render`, `classesOf`, `tagsOf`, `textOf`) instead of writing your own. Pattern: `tests/offline/ui/primitives.test.ts`.
  
  **QA verdict 2026-09-02 — BOUNCE, BUG-0010 filed.** One contradiction with a cited authority. `contracts/data-model.md` "Per-field provenance", named in this ticket's own Description: "The latest row per fact identity is the current provenance; the rows before it are that fact's decision history ... On an unset row ... observation_id and source_id are null". Browse reads `field_provenance` as if it were current-state, one row per field. Expected: Sources cell = the sources behind the row's current values. Found, measured: with (event,title) decided by `retired_feed` then re-decided by `ticketmaster`, the cell renders "retired_feed, ticketmaster"; with one verdict-unset row (`source_id` null) alongside a real decision, `joinBrowseRows` returns `[null, "ticketmaster"]` and the cell renders a trailing empty segment ("bandsintown, ticketmaster,"). Anchors: `src/lib/db/browse.ts:139-154` projects only `entity_id,source_id` — no `field`, no `applied_at`/`provenance_id`, so it structurally cannot pick the latest decision; `src/lib/browse/rows.ts:132-142` set-unions every `source_id`; `src/lib/browse/rows.ts:44` types `source_id` as `string` against a column that dropped NOT NULL in scraper migration 20260901000005 (2026-09-01).
  
  Everything else survived QA's attack at 280985b — do not re-attack blind. Covered and clean: `?cols=` against 20 hostile values (unknown names, case variants, duplicates, empty/whitespace/commas-only, an injected in_window value, SQL-injection and PostgREST-embed strings, path traversal, newline, NUL, unicode, 10k-char values, a JSON blob, `__proto__`/`constructor`/`toString`) each through `shownColumns` and a full page render, plus a mixed hostile array param — no throw, always a non-empty non-duplicated subset of the configured set, `columnOptions` always exactly the seven configured keys; full toggle algebra; every refusing leg (`field_provenance` absent → rows still render and the card names the table; provenance/sources truncated → rows render and the real number reaches the page; empty provenance leg fabricates nothing); `created_at` tie-break by `event_id desc`; the window at exactly 50 with the page stating its size. `npm run test:live` refuses non-zero today (STAGING names + SERVICES.md absent) — that refusal is the passing state, not the live file failing.
  
  All four documented forks JUDGED, none a bounce: window=50 (contract silent, consistent with the §4.3 window contract, page states the window); default = all seven columns incl. `arrived` (§4 says "e.g." on its column list and names arrival as the sort); 24px img + eslint-disable (not a contract question); events window as `readRows` not `readComplete` (correct — `events` is a growing catalog). The last was verified against schema truth in the sibling scraper repo: `events.created_at` is NOT NULL (no NULLS-FIRST window-slot theft), `field_provenance.provenance_id` is the PK (the total order resolves), `entity_type` is the domain and `domain_target` maps events→events (the `.eq` filter is right), `event_listings` LEFT-JOINs so it holds one row per event id.
  
  **DEAD ENDS**
  - **Reading `field_provenance` as current state — the live bug, BUG-0010.** It is an APPEND-ONLY DECISION LOG, not one row per field: the latest row per fact identity is current, earlier rows are that fact's decision history, and a verdict-unset row has `source_id` null. Set-unioning every `source_id` for an entity is wrong twice — it names superseded sources, and it emits an empty segment for an unset verdict. A fix must project enough to order the log (`field` plus `applied_at`/`provenance_id`) and pick the latest row per fact identity, and must type `source_id` as nullable at `rows.ts:44`.
  - **Ordering the list on `starts_at`** — rejected by ARCHITECTURE §11 and the ticket: the view is arrival order, `events.created_at desc`; `starts_at` is a column it shows, never its sort. Mutating `arrivalOrder` to sort on it reddens 10 tests.
  - **Taking the window off `event_listings`** — impossible; the view carries no `created_at` (scraper migration 20260825000004). Only `venue_name` comes off it.
  - **Making the events window a `readComplete`** — deliberately not done, QA agreed: `events` is a growing catalog, so a complete read would refuse the page past ROW_CAP. Only the three id-set legs are complete, so a truncated provenance set refuses instead of understating a row's sources.
  - **Spelling the literal `field_provenance` in `src/lib/db/browse.ts`** to satisfy the original check — violates rule 4.4; the check was amended instead. Write `T.fieldProvenance`.
  - **`next/image` for the poster** — would need every scraped poster host allowlisted in `next.config.ts`.
  - **A leaf importing `lib/db`, even type-only** — forbidden by §4 rule 7; a type-only import erases at runtime but still writes a directory-level cycle into the contract.
  - **`?? 0` on any read path** — forbidden; it was BUG-0007's twin.
  
  **RESIDUAL, no ticket:** from a one-column state the remaining chip renders active but its `href` is the current URL (toggling off the last column is a no-op) — a dead control, cosmetic, contract-silent. Would earn a P3 only if a walk shows an operator clicking it expecting something.
  
  Ticket IDs cited: TASK-0002, TASK-0004, TASK-0005, TASK-0006, TASK-0007, TASK-0026, FEAT-0007, BUG-0007, BUG-0010.
- 2026-09-02T09:40:08Z [compactor] compact threshold 8000 -> 22000 (file is 14816 bytes): irreducible: remainder is the BUG-0010 reproduction measurement, three named xfail tests, the amended checks and the dead ends; the two architect rulings are already cut to ARCHITECTURE.md pointers
- 2026-09-02T09:47:35Z [builder-21] claimed (attempt 2)
- 2026-09-02T09:50:17Z [builder-21] claimed → built: Verification-only handoff, EMPTY DIFF: BUG-0010's fix already landed on run/admin-window as 5f415eb (rows.ts currentDecisions() latest-per-(entity_id,field), nullable source_id; browse.ts provenance leg selects field+applied_at+provenance_id and orders applied_at,provenance_id). Nothing in TASK-0015's scope needed changing, so nothing was changed. ticket/TASK-0015 rebased onto run/admin-window; it is now identical to bfb2f65 (0 unique commits, clean tree).
- All six stored Checks re-run verbatim in the worktree at bfb2f65, all exit 0: npm run lint (no output); rm -rf .next/types && ./node_modules/.bin/tsc --noEmit (0 lines); npm run build (11 routes, /browse dynamic); ./node_modules/.bin/vitest run tests/offline/browse = 102/102 in 3 files (was 83 pre-BUG-0010, +19 from the fix's browse-read pins); test -f src/lib/browse/views.ts && grep -qE 'fieldProvenance|field_provenance' src/lib/db/browse.ts; ! grep -rqiE 'select \* from|rpc\(|execute_sql' src.
- The three ex-xfails assert plainly (no it.fails / skip / todo anywhere under tests/offline/browse) and pass by name: page.test.ts 'does not name a superseded source, or a decision with no source'; views.test.ts 'names only the source behind the current value, not a superseded one'; views.test.ts 'contributes no source name for a decision that names no source'.
- Criteria re-confirmed on the rebased tree by named coverage: newest-first arrival order + starts_at-is-not-the-sort + event_id tie-break; every spot-verification column incl. resolved source names; selector option set EQUALS the configured seven and toggling changes exactly one; URL round-trip incl. every single-column set and every rendered href; field_provenance absent => rows still render and the card names the table; exactly one view definition (BROWSE_VIEWS = [RECENT_EVENTS], views.ts:116); no table browser, no SQL-executing route (only health, nextauth, records PATCH); tests/live/browse.live.test.ts issues its own events/listings/provenance/sources queries and computes latest-per-fact itself.
- QA notes: full offline suite green as a regression guard, 601/601 in 29 files. npm run test:live exits 1 with LiveGuardError 'STAGING_SUPABASE_URL and STAGING_SUPABASE_SERVICE_ROLE_KEY are not set' — that refusal-by-name is the passing state, not a failing live file. Residual from attempt 1 still stands (toggling off the last remaining column is a no-op chip, cosmetic, contract-silent). [EMPTY DIFF: no file changed under scope — 1 so far]
- 2026-09-02T09:50:42Z [dispatcher] nothing to land: ticket/TASK-0015 has no commits and a clean worktree — on to QA as-is
