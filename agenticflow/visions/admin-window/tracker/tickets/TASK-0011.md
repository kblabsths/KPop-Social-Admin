---
id: TASK-0011
type: task
status: done
milestone: M1
priority: P1
opened_by: architect
parent: FEAT-0004
children: []
assignee: null
claimed_at: null
depends_on: [TASK-0010]
touch_scope: [src/app/queues/[reviewItemId]/page.tsx, src/lib/db/review-item.ts, src/components/review, tests/offline/review-item, tests/live/review-item.live.test.ts]
force: false
compact_threshold: 16000
attempts: 1
resolution: fixed
---
# Review-item detail — three typed views over one anatomy, evidence resolved

## Description
Module: `src/app/queues/[reviewItemId]/page.tsx` + `src/lib/db/review-item.ts`. Authority: spec section 6 **in full**; `contracts/resolver.md` section 7 and section 11; `contracts/data-model.md` (observations, per-field provenance); SPEC F4; M1 EC7; acceptance test 5; LOOK_AND_FEEL 'The evidence pair — this app's signature' and 'Key screens — Review item detail'.

The shared anatomy (spec section 6), rendered for all three shapes:

1. **What happened** — the `summary` sentence, `severity`, age from `opened_at`, and `folded_count` rendered as 'asked again xN'.
2. **Evidence, side by side** — every observation id in `review_items.evidence` (a `uuid[]`, rendered in its stored fold order) resolved to its row: **value, source, tier, `observed_at`, payload link** — with **the fact's current canonical value and its provenance beside them**, in the `EvidencePair` primitive (contenders left, canonical rightmost and labelled current).
3. **The close slot** — present in the layout and **empty in M1**. The verdict actions and the fixed / won't-fix dispositions arrive with the verdict slice. Do not build them, do not scaffold them, do not render a disabled button.
4. **The recommendation slot** — exists in the layout and **renders nothing**; its producer is parked.

Two facts from the schema you must not guess at (ARCHITECTURE section 6):

- **A claim has no tier of its own.** The evidence row's tier is `sources.tier` (the source's *current* tier); the canonical card's tier is `field_provenance.tier_at_apply` (frozen at the apply). Label each as what it is.
- **The current canonical value** is the observation named by the **latest** `field_provenance` row for `(entity_type, entity_id, field)` ordered `applied_at desc, provenance_id desc`, and only if that observation is still live. `field_provenance` spells the domain column `entity_type`; `review_items` spells it `domain`.

**Each of the three shapes gets its own detail view** over that anatomy — the evidence block renders what that shape's evidence *is*, not one generic layout:

- `data_conflict` fact item: the contending claims.
- `entity_link` fact item: the stuck claims and the unmet requirement (`pending_claims.unmet_requirement` names it: a missing NOT NULL column by name, 'at least one linked performer', or 'curated domain').
- `entity_link` source-pattern item: the folded records **as a list**, with the per-source dial beside it — and the dial's *threshold* is the blocked question (see the stuck-pattern ASK ticket); render the trend without a threshold line until it is answered, never a hand-copied number.

**Detail links out**: to the item's claims (`/claims` filtered), to its source (`/sources`), and to the affected record (`/records/<table>/<id>`).

Every page ticket in this milestone carries the same standing rules (ARCHITECTURE sections 4, 5, 7):
- The page function is the **only** async component on the route; every child is a pure synchronous component taking plain props. That is what lets the tests render `await Page(props)` with `react-dom/server`.
- Read only through `src/lib/db/**`; never build a Supabase client in a page; never spell a table name outside `src/lib/db/tables.ts`.
- Render all four states from the `ui` primitives: loading, empty, **not provisioned** (naming the missing table, gray, never a zero), error (the database's own message). A missing backing table must not throw and must not take the rest of the app down.
- Use tokens and primitives, never raw hex, arbitrary type sizes or ad-hoc radii.
- **State lives in the URL**: every filter, sort and tab is a `searchParams` value and survives the back button.
- Voice: buttons are a verb plus their object; ages are relative with the absolute in the title attribute; scheduled times are absolute UTC with the zone stated once in the column header; machine identifiers render verbatim in mono; empty and not-provisioned states name what the surface holds and what fills it.
- **Nothing settles anything in M1.** No verdict action, no settle control, no fixed/won't-fix, no scaffolding toward them.

## Acceptance criteria
[test 5, test 2, test 9; EC7] For each of the three shapes, offline tests render the detail page against fixtures and assert: every evidence id appears as an observation row carrying value, source, tier and `observed_at`; the current canonical value and its provenance render beside them; the header carries summary, severity, age and `folded_count`; the recommendation slot renders nothing; **no settle or verdict control is present**. The three shapes render three distinct views. An evidence id that resolves to no row renders honestly rather than throwing. The links to claims, source and record are real URLs. A live-marked test opens a real staging item and asserts its evidence rows against rows the test fetches itself. (human-checkable) One close-review screenshot per shape.

## Checks
```sh
npm run lint
./node_modules/.bin/tsc --noEmit
npm run build
./node_modules/.bin/vitest run tests/offline/review-item
bash -c "test -f src/lib/db/review-item.ts"
bash -c "grep -q tier_at_apply src/lib/db/review-item.ts"
```

## History
- 2026-09-02T12:40:39Z [compacted → agenticflow/tracker/notes/TASK-0011.md]
  Summary (state; attempts; DEAD ENDS preserved):
  **State** — `open`, never built: no page, no `src/lib/db/review-item.ts`, no tests, no QA verdict, no failing test to re-run. Blocked on TASK-0010 (unmet dep); TASK-0026 (the read bound, decided once there and not by eight builders) is also a dependency. TASK-0019 waits on this page landing. History to date is six architect rulings only, all landed and binding before the first line is written.
  
  **Tried** — no build attempt. The six rulings, each cited here because this ticket owns both a page and a `lib/db` module:
  
  1. **Test idiom (DECISIONS.md 2026-09-02).** The offline include glob stays `tests/offline/**/*.test.ts` — no `.tsx`, no JSX, no jsdom. Component tests build elements with `createElement` and assert emitted markup. Import the landed shared helper `tests/offline/ui/markup.ts` (`h` = createElement, `render` = renderToStaticMarkup, `classesOf`, `tagsOf`, `textOf`) instead of writing your own render helper; see `tests/offline/ui/primitives.test.ts` for the pattern. Adds no work and no scope.
  
  2. **Two read kinds (ARCHITECTURE.md 4.3). Pick deliberately; do not blur them.** *Complete read — `readComplete(missing, run, db?)` in `src/lib/db/result.ts`*: use it whenever the surface presents the result as the whole set (a list shown in full, a count, an oldest age, an "exactly the matching items" claim). The `run` callback passes `{ count: "exact" }`, a total `.order()` ending in the primary key, and `.range(0, cap - 1)` with the cap handed in; if the exact count exceeds the rows returned it returns `kind: "error"` naming the object, the count and the cap. **An `ok` array from a complete read is the whole matching set** — that property is what every figure in this app rests on, and it is why no caller needs a "was this all of it?" flag. *Window read — `readRows`, unchanged*: the caller's own `.order()` + `.limit()` define a named window and the surface says which window it is showing (section 8's gauge contract; TASK-0007's landed gauges use it correctly). **A window read's rows must never become a figure presented as a total.**
  
  3. **Pure leaves sit BELOW `lib/db` (ARCHITECTURE.md section 4 diagram + new rule 7).** A **pure leaf** — today `src/lib/review/**`, `src/lib/format.ts`, `src/lib/edit/config.ts`, and `src/lib/browse/**` when it lands — imports NOTHING that can reach a database: not `lib/db/**`, not `@supabase/supabase-js`, not `process.env`. `lib/db/**` imports the leaf. **The leaf never imports `lib/db/**` back — not a value, and not a type.** If both sides need a row type, **declare it in the leaf** (as `ReviewItemRow` in `shapes.ts` already does). `lib/gauges/**` is NOT a leaf and is unaffected: a gauge fetches its own bounded window through `lib/db`.
  
  4. **`shapeOf`'s default (ARCHITECTURE.md section 6 trap 11 + DECISIONS.md).** `shapeOf` in `src/lib/review/shapes.ts` branches on `queue === "entity_link"` and falls through to `data_conflict_fact` for everything else, so an unrecognised future queue (a `freshness` queue, say) would render as a **decision**, silently and plausibly. Ruled: leave it — unreachable today (migration `20260901000002` constrains `review_items.queue` to exactly `data_conflict` and `entity_link`), and a total function keeps an exception path out of every caller. **Trigger named: the migration that widens that CHECK constraint** — that is when `Shape` / `SHAPES` / `KIND_BY_SHAPE` / `shapeOf` get extended. For this ticket: **do not add a defensive throw, a null return or an `unknown` shape** to view selection — write the switch so a fourth `Shape` would fail to compile here; and **do not re-derive shape or kind** from `queue`, `source_id` or a summary string — `shapeOf`/`kindOfItem` are the only spellings (section 11).
  
  5. **The four data-surface states are a TYPED contract (TASK-0030, ARCHITECTURE section 7, DECISIONS 2026-09-02). Code against these signatures; do not work around them.** (a) **`reading` is REQUIRED on `ErrorLine` and on `GaugeState`'s error arm.** `DbResult`'s error arm carries `reading: string` already (`src/lib/db/result.ts:14`), so pass `result.reading` straight through on every error arm you render. **Do NOT invent a string, do not pass `""` to satisfy the compiler, and do not trim the message**: a transport failure's account runs ~900 chars including stack frames, and whether that is too long is the designer's call at the walk, not yours. (b) A state card that replaces a labelled surface keeps its eyebrow: `Empty` and `NotProvisioned` take an optional `micro` eyebrow; on this page it is optional because the `Section` heading above already names the surface — only pass it where there is no heading. (c) A rows surface has no headers-only rendering: `TrendTable` and `Distribution` take a REQUIRED `empty: { holds, filledBy }` and render it themselves. **The component owns WHEN, you own THE WORDS** — the app's voice, naming what the series holds and the one thing that fills it (LOOK_AND_FEEL Voice bar 4), never "No data"; an explicit `state` still wins, so pass your own where the emptiness has a REASON. **For this ticket: the page mounts no gauge, so only (a) touches the code — but it renders several reads (the item, its evidence, the canonical value and its provenance) and each error arm must name which one refused.**
  
  6. **No `notFound()` on `/queues/[reviewItemId]` (BUG-0017's measurement, ARCHITECTURE section 5 new rule, DECISIONS 2026-09-02).** **Resolve every URL your segment matches — do not call `notFound()` and do not add a second rewrite.** A record's table segment is an enum this app owns, which is why routing its miss was right; a review-item id is DATA, and an id that resolves to no row is one of your four states, not a routing outcome. So a read that comes back with no row renders the page's own surface, **at 200**, naming the id verbatim in mono and saying it is not in `review_items` — the same voice as the evidence-id-that-resolves-to-no-row case the criteria already require. Everything else (the links out, the three shape views) is unaffected. **If you find yourself wanting `notFound()`, stop and say so on this ticket instead — it is an architecture question, not a page decision.**
  
  **DEAD ENDS**
  
  - **`notFound()` thrown from a dynamic segment, Next 16.2.2** — cannot both set 404 and server-render the framed page: the status is set in the same `catch` that emits `<html id="__next_error__">` (`node_modules/next/dist/esm/server/app-render/app-render.js:1894-1918`).
  - **A `not-found.tsx` beside the page** — changes nothing; measured: still the error shell, len 8820.
  - **Rendering the not-found component inline** — yields the whole document at status 200.
  - **Copying the `beforeFiles` rewrite in `next.config.ts`** that solves `/records/<unmapped-table>/<id>` — it is `/records`-specific and **this route does not inherit it**.
  - **A list read with no `.range()`, no `.limit()` and no `.order()`** — QA found exactly this in `src/lib/db/review-items.ts` (TASK-0006). Against PostgREST's `db-max-rows` (Supabase default 1000) it truncates silently, in unspecified order, so a count or an "exactly the matching items" claim built on it would be wrong rather than refused.
  - **`?? 0` on any read path** — `readCount` returned `count ?? 0`, BUG-0007's user-visible twin: a read helper substituting a number the database did not give. TASK-0026 makes it refuse a null count instead. Do not reintroduce it.
  - **The old ARCHITECTURE arrow diagram putting pure leaves above `lib/db`** — wrong, and amended rather than the code: QA flagged `review-items.ts` importing `shapes.ts`, but `shapes.ts` has zero imports, no cycle is constructible, and TASK-0006's own two-module contract required the split.
  - **A type-only `leaf → lib/db` import** — erases at runtime but still writes a directory-level cycle into the contract, and the day someone changes it to a value import the cycle is real with nothing to catch it.
  - **A defensive throw / null return / `unknown` shape for `shapeOf`'s fall-through** — rejected; the compiler's exhaustiveness check over `Shape` is the guard.
  - **A ticket or a test for the unreachable `queue` value** — rejected: inventing a `queue` the database cannot hold, to exercise a branch it cannot reach, is work with no user behind it.
  - **`.tsx` / JSX / jsdom offline component tests** — outside the include glob; not an option.
  - **An optional `reading` prop** — QA's residual on BUG-0016 was right: an optional prop is a rule TypeScript cannot enforce, and the omission it permits is a red line naming none of the page's reads.
- 2026-09-02T12:43:09Z [compactor] compact threshold 8000 -> 16000 (file is 14411 bytes): History is verbatim architect rulings a builder must obey (test idiom, read kinds, shapeOf note, four-state contract, notFound ruling); compaction cannot shrink it honestly
- 2026-09-02T14:11:44Z [builder-42] claimed (attempt 1)
- 2026-09-02T14:39:28Z [builder-42] claimed → built: Built /queues/<review_item_id> + src/lib/db/review-item.ts + src/components/review/*: the §6 anatomy (summary/severity/age/'asked again ×N'), evidence resolved per shape (value, source→/sources, tier NOW, observed_at, payload pointer) with the canonical value+provenance as the rightmost pair card (tier_at_apply labelled 'at apply'). Three views via Record<Shape,…> (a 4th shape fails to compile); conflict=contenders, entity_link fact=stuck claims + pending_claims unmet_requirement, source-pattern=folded-record list + per-source dial, trend drawn with NO threshold line (seam empty). Recommendation slot and the close render nothing; markup contains zero button/form/input/select/textarea. No notFound(): an unknown id renders this page's own surface at 200 naming the id in mono and review_items.
Verified on the rebased tree (onto 6682202): npm run lint, tsc --noEmit, npm run build (route ƒ dynamic), vitest run tests/offline/review-item = 40 pass, full npm test = 1053 pass. Red-then-green observed: replacing currentDecisions(log)[0] with log[0] and dropping the live-status check reddened exactly the two canonical tests. Three shapes rendered and read at the real surface (markup transcribed in this run): pair cards read contender/contender/current with 'trusted at apply' vs the contender's 'official'; dial = 15 daily rows + the no-threshold sentence.
QA traps: (1) beyond touch scope, both to avoid duplicating a landed rule — currentDecisions in src/lib/browse/rows.ts is now generic in its row, and src/lib/db/review-items.ts exports its select list; (2) field_provenance.admin_locked is deliberately NOT read: tests/offline/edit/config.test.ts bans that column name on any code line under src/, and LOOK_AND_FEEL wants it on the EDIT surface — that ticket must settle the collision, I did not weaken the guard; (3) payload_ref renders verbatim, not an href — no object-storage base URL name exists in .env.example and I would not invent one; (4) the dial's display window is 14 days, my choice, stated on screen — it is not the resolver's pattern window, which stays uncopied; (5) the two fact shapes render each claim twice on purpose (pair = the 2-second signature, table = machine detail); (6) NOT walked in a browser: .env carries no service-role key (STACK §5) and the route is behind the sign-in gate, so the per-shape screenshots are still owed by the walk agent.
- 2026-09-02T14:40:00Z [dispatcher] landed ticket/TASK-0011 onto run/admin-window as f930912
- 2026-09-02T14:40:51Z [qa] built → qa: under attack
- 2026-09-02T14:50:40Z [qa] qa → done: Attacked the landed tree (run/admin-window @ 552a243), criteria before the builder's story. All 6 stored checks re-run verbatim: green (receipt). Full offline suite 1056 pass.

BROKE IT -> BUG-0021 (P2). Not a bounce: every acceptance criterion of THIS ticket holds, and the campaign's own precedent is TASK-0017 done + BUG-0013 spun out. Expected (spec 6 + review-item.ts:334 "a silently shorter list would read as an item with less evidence than it has"): resolved + unresolved account for every evidence id. Found at page.tsx:441-445 - the DEDUPLICATED claim count divided by the RAW row.evidence.length, so [A,A,B] with both ids resolving renders 2 rows, 0 unresolved, and "2 of 3 evidence ids resolved". Reachable without a malformed row: evidence is uuid[] with no uniqueness (migration 20260901000002) and resolver.md 11:487 folds by APPENDING. Pinned it.fails(strict)/BUG-0021 in page.test.ts, watched red as a plain it() first ("expected 1 to be +0"). Commit 248d786.

ONE INTEGRATED ATTACK, three shapes, edge fixtures + built rows: 200 folded records across readRowsByIds' 100-id chunk boundary -> all 200 render (now pinned); 0 evidence on a source-pattern item -> honest empty, dial still drawn, no threshold line; winning observation rejected/superseded/quarantined -> value withheld all three, provenance line names the status; every evidence id unresolved while a live decision exists -> canonical from provenance, ids named, nothing fabricated; truncated provenance (count 4321 > cap 1000) -> refuses naming field_provenance; summary carrying <img onerror>/<script> -> escaped, 0 script and 0 img nodes; payload_ref "javascript:alert(...)" -> text, 0 anchors with a javascript href (now pinned); source_id "javascript:alert(1)" -> percent-encoded in both hrefs; folded_count 0/1/41 -> x0/x1/x41; unknown queue "freshness" -> data_conflict_fact, as ruled.

RULINGS ASKED FOR. (1) Unknown id at 200 is CORRECT and the /records rewrite must NOT apply - History ruling 6 + DECISIONS 2026-09-02 bind it (a table segment is an app-owned enum, a review-item id is data). Contract silence to record: admin-observability.md 6 says nothing about an id naming no row; the answer lives only in ARCHITECTURE 5 / DECISIONS. (2) Both out-of-scope edits KEEP: currentDecisions generic in Row is type-only (body byte-identical), and exporting REVIEW_ITEM_COLUMNS removes a second hand-kept select list on one table; browse and queues suites green. (3) admin_locked: guard OVER-REACH, for the architect, not this ticket - config.test.ts:358 bans the NAME on any src/ line under a test titled "builds no write path", but admin_locked is a COLUMN and reading it is not a write path (settle_review_item/apply_resolution are actions; this is not). Builder was right not to weaken it; I did not narrow it either - loosening a catalog-ownership guard is not QA's call. (4) payload_ref verbatim not a link: correct, no storage base URL exists as a name; the new pin stops "verbatim" ever silently becoming "linked".

RESIDUAL (2nd finding, no ticket under lean): any ONE refusing leg inside readItemEvidence blanks the claims that DID arrive. Measured - sources 42501 on a data_conflict item -> 0 evidence rows, block replaced by "sources - permission denied"; same for field_provenance; and a source-pattern item that also names a fact (edge row spOpenLowBothSubjects) loses its folded-record list AND its dial to a field_provenance refusal its view never renders. page.tsx:435-440 already has the right pattern for pending_claims. Ticket trigger: a real both-subjects row, or a decision to report a leg refusal beside the evidence rather than instead of it.

NOT WALKED: no service-role key in .env (STACK 5) and the route is behind the sign-in gate, so the human-checkable per-shape screenshots are still owed by the walk agent. Live suite verified refusing by name, non-zero, no value printed. [resolution: fixed] [receipt green: 6 cmd(s), tree 11a9df01]
