---
id: BUG-0106
type: bug
status: done
milestone: M2
priority: P2
opened_by: designer
parent: null
discovered_from: designer:endgame-walk
children: []
assignee: null
claimed_at: null
depends_on: []
touch_scope: [src/components/cycles/outcome.tsx, src/app/page.tsx, tests/offline/cycles/page.test.ts, tests/offline/dashboard/page.test.ts]
force: false
siblings: [BUG-0108, BUG-0109, BUG-0110]
attempts: 1
resolution: fixed
---
# Seventeen cycles report errors and still say succeeded in healthy green — the palette bar TASK-0038 wrote and nothing built

## Description
Measured on the M2 endgame walk, 2026-09-09, walk instance on 127.0.0.1:8771 against staging, bundled Chromium, 1440x900, light and dark, computed styles at rest.

**Expected** — LOOK_AND_FEEL's palette, in the bullet TASK-0038 wrote: "**Healthy is green only when nothing needs a human.** An outcome whose own row or card carries a non-zero error count renders in **attention amber**, whatever word its producer wrote: the word stays verbatim in mono (copy bar 5) and only the colour is the app's reading of it. Green is for an outcome with nothing left to answer for. **Walkable on `/cycles`: no row that shows errors carries its outcome word in healthy green.**" The bullet names its own origin: an M1 user-sim found fifteen cycles reporting 108 errors each, labelled `succeeded` in green — "'succeeded' is doing work it hasn't earned."

**Found** — those same fifteen cycles are still green. Measured on `/cycles`, light theme, over the 69 rows of the Cycles table:

| rows | outcome | ERRORS column | rendered colour |
| --- | --- | --- | --- |
| 15 | `succeeded` | **108** | `rgb(1, 102, 48)` = `#016630` = `--color-healthy` |
| 2 | `succeeded` | **1** | `rgb(1, 102, 48)` = `--color-healthy` |
| 47 | `succeeded` | 0 | `rgb(1, 102, 48)` — correct |
| 4 | `died` | 0 | broken red — correct |
| 1 | `skipped` | 0 | neutral ink — correct |

Seventeen of the 69 rows on that page carry an error count and a green word. The fifteen 108-error rows all read `column "venue" of relation "events" does not exist` in the ERROR column of the same row — the page states the failure verbatim two cells to the right of a word coloured to say nothing failed. Rendered markup of an errored row's outcome cell:

    <span class="type-data inline-block rounded-control bg-chrome px-2 py-0.5 text-healthy">succeeded</span>

identical, class for class, to a clean row's.

TASK-0038 wrote the bar into the doc and changed no `src/` file ("No src/ or tests/ change" — its own History), and it named this walk location for the endgame to check: "on /cycles, every row with error count > 0 has its outcome word NOT in --color-healthy". Nothing built it. `src/components/cycles/outcome.tsx` maps the word alone — `OUTCOME_TONE = { succeeded: "healthy", failed: "broken" }` — and the row's error count never reaches the badge.

**Second copy, same defect.** `src/app/page.tsx:181` declares its own `OUTCOME_TONE` with identical content, for the Dashboard's cycles table. That table's six rows all carry `errors = 0` on staging today, so the Dashboard is not currently *showing* a green word it should not — but it is the same table drawn from the same producer, and it will show one the next morning a cycle errors. `outcome.tsx`'s own docstring says the map lives there so both halves of `/cycles` share it, "because two copies is how the same word comes to be green on one table and grey on the one below it"; the Dashboard's copy is exactly that.

Provenance checked before filing: `git log` on `src/components/cycles/outcome.tsx` (0543d42, DEBT-0004) and `src/app/page.tsx` (c8ee060 TASK-0039, 98e21be DEBT-0006, 7f12642 BUG-0074, …) returns ticket-bearing commits only. No human-lane divergence, so this is a violation and not a reconciliation.

Evidence: `agenticflow/tracker/evidence/M2/designer/cycles-dark-1440.png`, `cycles-light-1440.png`.

## Acceptance criteria
1. On `/cycles`, no row whose ERRORS cell is non-zero renders its outcome word in `--color-healthy`. The seventeen rows measured above (fifteen at 108 errors, two at 1) render `succeeded` in `--color-attention`. Asserted structurally in the offline suite over a fixture carrying an errored `succeeded` row and a clean one, and re-measured by computed style on the next walk.
2. **The word is untouched.** The badge still reads `succeeded` verbatim, in mono, at the `data` step (copy bar 5). Only the colour changes. A row with zero errors still renders healthy green; `failed` still renders broken red; `died` and `skipped` are unchanged.
3. The Dashboard's cycles table obeys the same rule from the **same** map: `src/app/page.tsx` stops declaring its own `OUTCOME_TONE` and takes the one in `src/components/cycles/outcome.tsx`, where that module's docstring already says it lives. Two copies of the tone decision do not survive this ticket.
4. The decision is made in one place: one function that takes the producer's word AND the row's error count and returns the tone, called by both tables — not a tone re-derived at each call site.
5. Adapter runs are unaffected: a run row carries no error-count column of its own (`failed` already renders broken and its `error` cell carries the text), and its rendering does not change.
6. No new colour enters the palette and no contrast regression: `--color-attention` on chrome measures >= 4.5:1 in light and dark (bar 12), the ratio the palette already records for the `high` severity badge.
7. Human-check: on `/cycles` at 1440x900, light theme, scrolling the Cycles table, no green word sits on a row whose ERRORS cell is not `0`. (human-checkable)
8. `npm run lint` and `tsc --noEmit` clean; the targeted offline files green; the two live page suites green.

## Checks
```sh
bash -c 'rm -rf .next/types && npm run lint && ./node_modules/.bin/tsc --noEmit'
bash -c '! grep -q "OUTCOME_TONE" src/app/page.tsx'
./node_modules/.bin/vitest run --project=offline tests/offline/cycles/page.test.ts tests/offline/dashboard/page.test.ts
npm run test:live -- tests/live/cycles.live.test.ts tests/live/dashboard.live.test.ts
```

## History
- 2026-09-09T08:18:15Z [designer] opened: Seventeen cycles report errors and still say succeeded in healthy green — the palette bar TASK-0038 wrote and nothing built
- 2026-09-09T08:24:56Z [builder-174] claimed (attempt 1)
- 2026-09-09T08:38:58Z [builder-174] claimed → built: - One decision, one module: `outcomeTone(word, errors)` in src/components/cycles/outcome.tsx returns neutral|healthy|attention|broken; `OUTCOME_BADGE_TONE` maps it onto Badge's existing `high` = --color-attention (no new colour). errors>0 => attention, errors 0 => the word's own tone, `failed` stays broken (red already says a human is needed), errors=null (adapter runs, which have no such column) => word alone, rendering byte-identical. The Dashboard's copy is gone: src/app/page.tsx imports the function. Beyond touch_scope, one line each: cycle-table.tsx passes row.errors, run-columns.tsx and components/cycles/index.ts follow the renamed export — the /cycles table could not reach the count otherwise.
- Each outcome badge now publishes the reading it earned on `data-outcome-tone` (both surfaces), so tests and the walk read the app's own tone word, never a class or a colour value.
- Verified: red-then-green — the 6 new cases fail on the pre-fix src (`expected undefined to be 'attention'`) and pass after. Rebased on run/admin-window, then: lint clean; tsc --noEmit clean; `! grep -q OUTCOME_TONE src/app/page.tsx` passes; vitest --project=offline on the two page files 122/122; `npm test` 2711+6; `npm run test:live -- tests/live/cycles.live.test.ts tests/live/dashboard.live.test.ts` 17/17 against staging. Real-surface measurement, /cycles rendered against staging (scratch live file, since deleted): 69 rows — 17 `succeeded` with errors>0 now tone `attention` (was healthy: the ticket's 15+2), 47 clean `succeeded` healthy, 1 skipped neutral, 4 died unchanged; rows rendering green while showing errors = 0.
- For QA: a `skipped` (neutral-word) row with errors>0 would also read attention — the palette bar says "whatever word its producer wrote", and no such row exists on staging; a `failed` row with errors deliberately stays broken, per criterion 2. Untested by me: the computed-style/contrast re-measurement (criteria 6/7) is the walk's.
- 2026-09-09T08:39:16Z [dispatcher] landed ticket/BUG-0106 onto run/admin-window as 5acdc52
- 2026-09-09T08:39:59Z [qa] built → qa: under attack
- 2026-09-09T08:54:05Z [qa] qa → done: attacked: criteria re-run on the landed tree before reading the builder's story. All four stored checks green (lint+tsc; `! grep OUTCOME_TONE src/app/page.tsx`; the two offline page files 122/122; the two live files 17/17 against staging).

INDEPENDENT READ of staging (resolution_runs, newest 200, read-only, the palette bullet reimplemented rather than the app's own function): 69 rows — 17 `succeeded` with errors>0, 47 clean `succeeded`, 1 `skipped`, 4 no-outcome. `errors` arrives as a JS integer, never null/negative/non-numeric; zero rows carry errors=0 beside a non-empty error_summary. The ticket's census reproduces exactly.

REAL-SURFACE WALK (production build of fae1332, my own bind-probed port 8793; 8770/8771 untouched; read-only, no staging write, so the sandbox was not reset), Chromium 1440x900, computed styles, light AND dark:
- /cycles, 69 rows: the 17 errored rows render rgb(187,77,0) light / rgb(255,185,0) dark, data-outcome-tone=attention. Rows showing a non-zero ERRORS cell in healthy green: 0 (was 17). The 47 clean rows keep rgb(1,102,48) — the exact ink the ticket measured.
- Whole-page sweep of EVERY leaf element painted in the healthy ink, not just table rows: 51 per theme = 47 clean cycle badges + 4 adapter-run badges, each of those four with an em dash in its error cell. No green word on the page sits beside an error line.
- Criterion 6 by measurement, never by reading CSS: attention on chrome 4.81:1 light, 8.52:1 dark (>= 4.5 both), unchanged when the row is hovered and when it is the marked ?cycle= target. Word verbatim `succeeded`, rendered in Geist Mono.
- Dashboard: 6 newest cycles all errors=0 -> healthy; run tones unchanged. Cross-surface agreement is pinned offline row by row.
- Criterion 5 checked at the source, not the story: `runs` has no errors column (scraper migration 20260829000001), and neither RunTableRow nor the select list carries one.

ATTACKS THAT FOUND NOTHING: errors as null/undefined/NaN/string/negative — unreachable (`errors integer default 0 not null`); an unknown outcome word — both outcome columns are CHECK-constrained to {succeeded,failed,skipped(,partial)}, so the word space is closed.

BUILDER'S FLAGGED JUDGMENT, GRADED: `skipped`+errors reading attention is CORRECT — the bar says 'whatever word its producer wrote'; `failed` staying broken is criterion 2's own instruction. Both are now pinned rather than left to the next reader.

TESTS ADDED (fae1332) at the gaps the ticket's own assertions leave: errors = 1 (every existing assertion uses 108, so an `errors > 1` implementation passes all of them and leaves green exactly the two rows the ticket's table lists at one error); a neutral word with errors; and 'no new colour' as an equality between the errored badge's rendering and the app's existing high-severity badge rendering — no class or colour literal, so a palette restyle moves both sides and cannot redden it. Full offline suite 70 files / 2714 tests green with them; lint and tsc --noEmit clean.

RESIDUALS for the digest, none ticket-worthy: (a) a negative errors count would show a non-zero ERRORS cell with a green word — the column has no CHECK >= 0, but only the resolver writes it and no such row exists; (b) /cycles' runs table is the one outcome badge with no data-outcome-tone hook (criterion 5 keeps runs unchanged); (c) the four green `succeeded` runs each report records_unlinked = 134 — not an error count, so outside this bar, but a designer question about which counters mean 'needs a human'; (d) LOOK_AND_FEEL records 4.57:1 for attention-on-chrome where the rendered badge measures 4.81:1. [resolution: fixed] [receipt green: 4 cmd(s), tree 63e4383c]
