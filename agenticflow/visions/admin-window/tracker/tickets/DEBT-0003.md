---
id: DEBT-0003
type: debt
status: qa
milestone: M1
priority: P3
opened_by: architect
parent: null
discovered_from: M1-endgame-structure-walk
children: []
assignee: builder-115
claimed_at: 2026-09-04T07:39:39Z
depends_on: [BUG-0043, BUG-0044, BUG-0045, BUG-0046, BUG-0049, BUG-0054, BUG-0055, BUG-0057]
touch_scope: [src/components/ui/state-of.tsx, src/components/ui/window-line.tsx, src/components/ui/index.ts, src/app/cycles/page.tsx, src/app/sources/page.tsx, src/app/claims/page.tsx, src/app/queues/[reviewItemId]/page.tsx, tests/offline/ui/primitives.test.ts, tests/offline/cycles/page.test.ts, tests/offline/sources/page.test.ts, tests/offline/claims/page.test.ts, tests/offline/review-item/page.test.ts]
force: false
siblings: [DEBT-0004]
attempts: 2
empty_diffs: 1
---
# Four byte-identical StateOf copies (and three WindowLines, one already drifted) stand in the pages instead of one ui primitive

## Description
M1 structure walk, 2026-09-03. Common violation 9.

`StateOf` — the component that turns a `not_provisioned`/`error` DbResult into the card the operator sees — is defined FOUR times, byte for byte including its comment, in `src/app/cycles/page.tsx`, `src/app/sources/page.tsx`, `src/app/claims/page.tsx` and `src/app/queues/[reviewItemId]/page.tsx`. It is not incidental duplication: it renders the `data-not-provisioned` and `data-read-failed` hooks that ARCHITECTURE §10's live-state rule requires every live oracle to read structurally. Four copies is four chances for that contract to drift, in the exact place a drift grades a broken page as a pass (common violation 6).

The drift has already happened one helper over. `WindowLine` stands three times: `cycles` and `sources` are identical but for the window's TYPE, and the `claims` copy has ALREADY diverged — it carries none of the `data-window` / `data-window-since` / `data-window-until` / `data-window-truncated` attributes the other two do, and says 'not the whole view' where they say 'not the whole table'. So the Claims window line cannot be read structurally at all, and the two sentences a reader would assume are one are two.

Also hand-copied, same cause: `RETRY` (4 pages) and `ARRIVES_WITH` (all 8 pages) — one sentence each, repeated verbatim, that a copy edit would have to find eight times.

Why it happened, so the next one does not: builders work in isolated worktrees and cannot see each other's code, so a helper nobody seeded as its own ticket becomes N hand-copies. That is now ARCHITECTURE §13.7.

Scope note: this ticket rewrites four PAGES. It is serial at its destination with every open bug on those pages, which is why it depends on all of them. If M1 closes before those clear, this is M2's — say so rather than co-dispatching it.

Not in scope: extracting the per-page sections into component modules (that is the sibling DEBT), and any change to what the four states RENDER — this is a fold, and the offline and live suites for all four pages must be untouched and green.

## Acceptance criteria
One `StateOf` and one `WindowLine` exist, exported from `src/components/ui` and imported by all four pages; no page file defines either. The shared `StateOf` renders exactly what the four copies rendered — the `data-not-provisioned={missing}` wrapper around `NotProvisioned` and the `data-read-failed={reading}` wrapper around `ErrorLine`, with the optional `eyebrow` — and the shared `WindowLine` carries the four `data-window*` attributes on every page that uses it, INCLUDING `/claims`, whose copy had dropped them; its sentence is one sentence, and the 'not the whole view' / 'not the whole table' split is resolved deliberately (a window over a view says view) rather than by whichever copy won. `RETRY` and `ARRIVES_WITH` are single exported constants, spelled once. The four pages' offline suites and their four live parity oracles are green, unchanged in what they assert: this fold changes no rendered output, and any diff a live oracle can see is a defect in the fold.

## Checks
```sh
test -f src/components/ui/state-of.tsx
grep -q "data-not-provisioned" src/components/ui/state-of.tsx
grep -q "data-read-failed" src/components/ui/state-of.tsx
! grep -rqE "^function (StateOf|WindowLine)\(" src/app
test "$(grep -rlE "^const (RETRY|ARRIVES_WITH) =" src/app | wc -l)" -eq 0
npx vitest run --project=offline tests/offline/cycles/page.test.ts tests/offline/sources/page.test.ts tests/offline/claims/page.test.ts tests/offline/review-item/page.test.ts tests/offline/ui/primitives.test.ts
npm run test:live -- tests/live/cycles.live.test.ts tests/live/sources.live.test.ts tests/live/claims.live.test.ts tests/live/review-item.live.test.ts
./node_modules/.bin/tsc --noEmit
npm run lint
```

## History
- 2026-09-03T23:11:14Z [architect] opened: Four byte-identical StateOf copies (and three WindowLines, one already drifted) stand in the pages instead of one ui primitive
- 2026-09-04T06:33:32Z [builder-112] claimed (attempt 1)
- 2026-09-04T06:58:20Z [builder-112] claimed → built: FOLD: src/components/ui/state-of.tsx + window-line.tsx (exported from the ui barrel) replace the 4 StateOf and 3 WindowLine page copies; ARRIVES_WITH/RETRY are single constants. The retry sentence had NINE spellings, not four — 5 more stood as literal retry="…" props on /, /queues, /records/[table]/[id], /browse, so scope grew to those 4 pages (the ticket's own check demands zero in src/app) and to tests/offline/absence/pages.test.ts, whose WINDOWED floor now records /claims publishing 2 hooks.

READ THIS BEFORE GRADING 'no rendered change': two of the four StateOf copies were NOT byte-identical — /claims and /queues/[reviewItemId] never had the data-not-provisioned / data-read-failed wrappers (git log: they never did; only cycles/sources ever carried them). Measured the fold by rendering all 8 routes x {populated, empty, not-provisioned, error} x param sets = 60 offline renders, before and after: rendered TEXT identical in 60/60, and the ONLY markup deltas are those two pages gaining the wrapper divs, plus /claims's gauge window line gaining data-window/-since/-until/-truncated. Both are the drift the ticket names, reconciled per ARCHITECTURE §4.3/§10 rather than silently; nothing else on any page moved (the /queues and root diffs that remain are wall-clock values in age bars).

Two choices worth a second's attention: 'over' is a required prop and names what the window was read over — /claims says view, /cycles and /sources say table (acceptance's rule, and it keeps all three sentences byte-identical); the claims gauge hooks are 'pending' and 'standing', NOT 'pending_claims', because tests/offline/db/layering.test.ts forbids a page to spell a view name and the list already publishes data-window="claims" over that same view.

VERIFIED on the rebased tree (worktree has node_modules and .env symlinked from the primary, so live re-runs work there): npm run lint clean; tsc --noEmit clean; npm test 58 files / 2005 tests; isolated 6; npm run test:live 11 files / 72 tests green; npm run build green. New tests — 5 StateOf + 5 WindowLine in tests/offline/ui/primitives.test.ts, 3 in tests/offline/claims/page.test.ts — each watched RED under sabotage (wrappers removed, data-window removed, truncated omitted when false, 'over' pinned to 'table') and green restored.

LEFT DELIBERATELY (same class, outside the four pages): /queues carries a FOURTH inline window sentence with no data-window hooks at all, and /, /queues, /records, /browse still hand-roll their refusal rendering (LegNote and inline ErrorLine) instead of StateOf — folding either changes markup on pages this ticket does not own. Worth a follow-up ticket. The fold harness is kept at agenticflow/tracker/evidence/DEBT-0003/fold-snapshot.test.ts with its run recipe in the docstring.
- 2026-09-04T06:58:44Z [dispatcher] landed ticket/DEBT-0003 onto run/admin-window as 5406885
- 2026-09-04T06:59:21Z [qa] built → qa: under attack
- 2026-09-04T07:20:49Z [qa] qa → reopened: admin-window/BUG-0077 filed — one clause of the acceptance criterion is unmet; everything else in the fold is verified and must not be re-litigated.

ATTACKED (landed tree, 0309ff3 -> 5406885, re-checked at 2115162):
- Rendered all 8 routes through tests/offline/absence/surfaces.ts against 4 databases {populated, empty, not-provisioned, error} x my own wider param matrix (absurd inputs included: empty string, array-valued param, ../../etc/passwd, 600-char id, '  <script>', '%%%', wrong-case tab) = 244 renders per side, in two detached worktrees, timestamps normalised (fixtures build their instants at import; every raw delta was exactly the 10s between the two runs). Rendered TEXT identical 244/244. Markup: 9 distinct added lines and 1 removed, and they are exactly the three deltas the builder declared — data-not-provisioned/data-read-failed wrappers on /claims (pending_claims, observations) and /queues/[reviewItemId] (review_items), and /claims's gauge line gaining the four data-window* hooks. /, /queues, /browse, /cycles, /sources and /records/[table]/[id] are byte-identical on every render. Both deltas are correct under ARCHITECTURE 4.3 and 10: on the not-provisioned and error databases NO surface publishes a data-window hook, and on the empty database every windowed surface keeps every hook — including /claims's new gauge window on BOTH tabs, a path pages.test.ts's default render never drives.
- Sabotage, 6 suites / 600 tests green at baseline: StateOf wrappers stripped -> 14+ failures across 5 files; data-window-truncated omitted when false -> 2; data-window removed -> 14. Rewording ARRIVES_WITH and RETRY leaves 600 green, which is CORRECT (no test pins copy) and is why I am not asking for a copy oracle.
- Four files beyond touch_scope (/, /queues, /browse, /records/[table]/[id]) are pure ARRIVES_WITH/RETRY substitutions, demanded by the ticket's own zero-copies check, and render byte-identically. The fifth, tests/offline/absence/pages.test.ts, raises the WINDOWED floor to ['claims','pending'] correctly — leg 1 and leg 2 both still grade every hook they measure.
- Full suites on the landed tree: npm test 58 files / 2005 green; npm run test:live 11 files / 72 green (full, staging); npm run test:http 3 files / 14 green (8772 probed free by real bind on 127.0.0.1, ::1 and 0.0.0.0 first); tsc --noEmit exit 0; npm run lint exit 0.

BROKE IT (admin-window/BUG-0077, P2). Expected (this ticket's criterion, and window-line.tsx:35-39's own docstring): 'the view/table split is resolved deliberately - a window over a view says view - rather than by whichever copy won'. Found: it was parameterised, not resolved. /claims's gauge (src/app/claims/page.tsx:303, :382) and /sources's awaiting-row gauge (src/app/sources/page.tsx:469) render THE SAME WindowInfo - both fetchPendingClaims (src/lib/gauges/pending-claims.ts:179-203), window built at line 200 by windowOf over readPendingObservations -> .from(T.observations) (src/lib/db/gauges.ts:335-352), i.e. over a TABLE - and the two sentences are byte-identical for 100 characters and then end 'not the whole view' against 'not the whole table'. On /claims alone, two window lines now stand on one page under two different caps (50 and 1,000) both ending 'not the whole view', and the 1,000 was never a cap on the view at all. Nothing grades the value: flipping /claims to 'table', and separately /cycles to 'view', each left all 600 tests green.

PINNED: tests/offline/absence/pages.test.ts, 'two surfaces over one read describe it the same way', committed 9e8d77b. Watched RED first (it named both sentences, differing in one word), then made a STRICT xfail so the branch stays green; proved strict by flipping /claims to 'table' in a scratch worktree and watching the pin turn RED. Copy-independent by construction - both lines come from the one primitive, so a copy edit moves both together and only a disagreement between call sites can redden it.

RE-CHECK SCOPE when this comes back: BUG-0077's repro only, plus this ticket's stored checks. The 244-render equivalence, the sabotage rounds and the live suites are done and recorded; do not repeat them. Either /claims's over follows its object, or the architect rules the other way and /sources moves with it and the primitive's docstring is restated - what is not closeable is the two pages disagreeing. Retire the strict pin either way.

ALSO FILED: admin-window/DEBT-0006 (P3, M1) - the residual class, swept rather than remembered. The builder named two items; there are ten. Six window sentences still stand outside WindowLine (/queues:402 with NO data-window hook at all - the one with a real contract consequence, since it keeps /queues out of pages.test.ts's WINDOWED floor entirely; /claims's own list line at :135 and :570; /cycles:1095 and :1361; /browse:151; shape-views:340), leaving the app with two window-line dialects an oracle cannot ask one question of, and four pages still hand-roll their refusal rendering. Sequenced after BUG-0077, which settles what 'over' means. Evidence: agenticflow/tracker/evidence/DEBT-0003/qa/fold-diff.txt.
- 2026-09-04T07:39:39Z [builder-115] claimed (attempt 2)
- 2026-09-04T07:45:02Z [builder-115] claimed → built: ZERO-DIFF HANDOFF — the bounce reason was fixed by admin-window/BUG-0077 (85eb487), not by new code here. Rebased ticket/DEBT-0003 onto run/admin-window; the branch is now byte-identical to it (git diff --stat run/admin-window: empty, HEAD 66dc915). No product or test file changed; the strict xfail QA left in tests/offline/absence/pages.test.ts had already been retired to a plain 'it' by BUG-0077's builder, and it PASSES for real.

VERIFIED on the rebased tree (worktree with node_modules and .env symlinked from the primary; read-only against staging, nothing written, no port touched): all 5 static stored checks exit 0 (state-of.tsx present with both data-hooks, zero StateOf/WindowLine definitions and zero RETRY/ARRIVES_WITH constants under src/app); targeted vitest 5 files / 338 tests green (cycles, sources, claims, review-item, ui/primitives); the retired pin 'two surfaces over one read describe it the same way' green on its own; FULL offline suite 'npm test' 58 files / 2025 tests + isolated 1 file / 6 tests, exit 0; FULL live suite 'npm run test:live' 11 files / 72 tests green against staging (ubfjjqlvnpnoborczbdb), exit 0, including all four named parity oracles; tsc --noEmit exit 0; npm run lint exit 0.

OBSERVED at the real surface (rendered /claims, /sources, /cycles on a populated stub, clock pinned 2026-09-04T12:00Z, every [data-window] node read back): the two sentences BUG-0077 was filed over are now byte-identical — /claims [pending] and /sources [awaiting_row] both read 'Claims observed since 2026-06-06 12:00 UTC, read to 2026-09-04 12:00 UTC - a window of at most 1,000 rows, not the whole table.' (one WindowInfo over the observations TABLE), while /claims [claims], the list window over the pending_claims VIEW, reads '... a window of at most 50 rows, not the whole view.' So the criterion clause 'a window over a view says view' now holds because windowOf stamps over=objectKindOf(scanned) from the table registry; grep shows zero 'over=' props left in src/app. Probe file was scratch and is deleted; tree clean.

FOR QA: re-check scope is BUG-0077's repro plus this ticket's stored checks — the 244-render equivalence and sabotage rounds are already recorded and were not repeated. The residual class stays open as admin-window/DEBT-0006. [EMPTY DIFF: no file changed under scope — 1 so far]
- 2026-09-04T07:45:24Z [dispatcher] nothing to land: ticket/DEBT-0003 has no commits and a clean worktree — on to QA as-is
- 2026-09-04T07:46:40Z [qa] built → qa: under attack (r2, fix-scoped: BUG-0077 repro + stored checks)
