---
id: DEBT-0006
type: debt
status: done
milestone: M1
priority: P3
opened_by: qa
parent: null
discovered_from: qa:DEBT-0003
children: []
assignee: null
claimed_at: null
depends_on: []
touch_scope: [src/app/page.tsx, src/app/queues/page.tsx, src/app/browse/page.tsx, src/app/records/[table]/[id]/page.tsx, src/app/claims/page.tsx, src/app/cycles/page.tsx, src/components/review/shape-views.tsx, src/components/ui/window-line.tsx, src/components/ui/state-of.tsx, tests/offline/absence/pages.test.ts]
force: false
attempts: 1
resolution: fixed
---
# The other half of the fold: six window sentences and four refusal renderings still stand hand-rolled outside the two ui primitives

## Description
DEBT-0003 folded the four StateOf copies and the three WindowLine copies it named. Measured on the landed tree at 2115162 (2026-09-04, qa-DEBT-0003), the same class stands in ten more places — the builder's handoff named two of them; the sweep below is the whole set, so the next fold is not scoped from a memory again.

**Window sentences outside `WindowLine` (6).** `grep -rn 'not the whole' src/` plus `grep -rn 'data-window=' src/app/`:

  - src/app/queues/page.tsx:402 — the queue-health gauge states a window in prose and publishes NO `data-window*` hook at all. This is the one with a contract consequence: ARCHITECTURE.md 4.3 says the presence or absence of the line is what tells an oracle 'this read happened' from 'this read did not', and it is graded 'once for every surface at once' in tests/offline/absence/pages.test.ts. /queues cannot enter that file's WINDOWED floor, so the rule is unenforced on exactly one surface — which is the state /claims was in before DEBT-0003, and the state BUG-0063/0067/0070 were all filed out of.
  - src/app/claims/page.tsx:135 + :568-577 — the LIST window line: its own `<p>`, its own sentence (`windowSentence`), and a different hook set (`data-window-limit`, `data-window-held`, no `-since`/`-until`). It stands two elements away from the folded gauge line on the same page.
  - src/app/cycles/page.tsx:1095 (`data-window="runs"`) and :1361 (`data-window="cycles"`) — two more hand-rolled `<p>`s with the `-limit`/`-held` hook set, on a page this ticket's predecessor rewrote.
  - src/app/browse/page.tsx:151 ('not the whole catalog') and src/components/review/shape-views.tsx:340 — same sentence shape, no hooks.

  So the app has TWO window-line dialects: six lines through the primitive (since/until/truncated) and six hand-rolled (limit/held, or nothing). An oracle cannot ask one question of all of them.

**Refusal rendering outside `StateOf` (4 pages).** /, /queues, /browse and /records/[table]/[id] hand-roll `NotProvisioned`/`ErrorLine` (`LegNote` in browse:83-90 and records:248-256, inline arms in page.tsx:397/407-411/433/436-440, queues:210/222-226/390/392-396), so none of them publishes the `data-not-provisioned` / `data-read-failed` wrapper the four folded pages now do — the hook ARCHITECTURE.md 10 has live oracles read state 3 from state 4 by. They consume ARRIVES_WITH and RETRY correctly; it is the wrapper that is missing.

**Why one ticket and not two.** One cause (a helper nobody seeded as its own ticket, ARCHITECTURE 13.7), one destination (src/components/ui), and an overlapping set of four page files — splitting it serialises two builders over the same files for no gain.

**Sequencing.** Serial with admin-window/BUG-0077, which settles what `WindowLine`'s `over` prop means; fold /queues onto a contract that is still being argued and it is two rewrites. Do BUG-0077 first.

## Acceptance criteria
Every window sentence in `src/` is rendered by `WindowLine` — `grep -rn 'not the whole' src/` names only src/components/ui/window-line.tsx — and every surface that states a window publishes `data-window` with it, so tests/offline/absence/pages.test.ts's WINDOWED floor lists /queues alongside /claims, /sources and /cycles. The two hook dialects are reconciled deliberately, in the primitive, rather than by whichever call site is folded last: whatever `limit`/`held` say, they say it on every window or on none. Every not-provisioned and error card in `src/app/` is rendered by `StateOf`, so `data-not-provisioned` / `data-read-failed` name the refused read on all eight routes and not on four. Rendered TEXT is unchanged on all eight routes in all four database states (the fold's own bar, and the 244-render harness recipe is in agenticflow/tracker/evidence/DEBT-0003/fold-snapshot.test.ts); the offline and live suites for every page stay green and unchanged in what they assert.

## Acceptance criteria
(to be written)

## Checks
```sh
test "$(grep -rn 'not the whole' src | grep -cv '^src/components/ui/window-line.tsx:')" -eq 0
test "$(grep -rn 'data-window=' src/app | grep -cvE '^[^:]*:[0-9]+:[[:space:]]*(\*|//|/\*)')" -eq 0
npx vitest run --project=offline tests/offline/absence/pages.test.ts tests/offline/ui/primitives.test.ts
npx vitest run --project=offline
./node_modules/.bin/tsc --noEmit
npm run lint
```

## History
- 2026-09-04T07:18:51Z [qa] opened: The other half of the fold: six window sentences and four refusal renderings still stand hand-rolled outside the two ui primitives
- 2026-09-04T07:46:02Z [builder-116] claimed (attempt 1)
- 2026-09-04T08:16:24Z [builder-116] claimed → built: Folded all six window sentences into WindowLine and all four refusal renderings into StateOf. WindowLine now has two call shapes (a SCAN takes `measured`; a DRAWN list takes `shows`, one arm per read shape: newest / matched / catalog) — four sentences that used to live in four page files now sit side by side in one file. Hook dialects reconciled IN the primitive: limit+held+truncated on EVERY window, since/until only on time-bounded scans (their absence is the statement). WindowInfo gained `held` (the row count windowOf already had), so 7 gauge fixtures gained one field. Object-kind is derived where the query is issued (CYCLES_OBJECT/RUNS_OBJECT/EVENTS_OBJECT/CLAIMS_OBJECT in lib/db) — no page spells it.

RENDERED TEXT — exactly ONE line changed, and I list it: /browse loses "The 50 newest events by arrival, newest first — a window, not the whole catalog." in the not-provisioned and error states, because the line now follows the READ (ARCHITECTURE 4.3). It stood over a table the page could not read — the BUG-0063/0067/0070 class. Wording in the populated and empty states is byte-identical. Measured: DEBT-0003 harness, 60 renders (8 routes x param sets x 4 db states) at parent 63e5e84 vs after, text extracted and clock instants masked -> 58 of 60 identical, the 2 above the only diffs. The dial on /queues/[reviewItemId] is not in that harness (its fixture builds a fact item, not a source-pattern one), so I rendered it separately in both trees, both truncation directions: text identical.

MARKUP GAINED HOOKS — say so and pinned: /queues publishes data-window="queue_health" and /browse data-window="events" for the first time; both join the WINDOWED floor in tests/offline/absence/pages.test.ts (now 5 routes, 10 hooks). Every pre-existing window line gained data-window-limit and data-window-held; the two drawn lines on /cycles gained held. StateOf wrappers (data-not-provisioned / data-read-failed) now appear on all 8 routes instead of 4.

ONE TEST ASSERTION RESCOPED, not weakened: tests/offline/claims/page.test.ts "claims no count it never took when the list is read fails" selected [data-window-held] anywhere on the page. Since every window now publishes held, the pending-claims gauge (whose observations read SUCCEEDED and found nothing) honestly publishes 0 there. Selector narrowed to [data-window="claims"][data-window-held] — the claim its own name makes, unchanged. Reason recorded in its docstring.

VERIFIED on the rebased tree (98e21be onto run/admin-window at eb759aa): both ticket greps = 0; vitest offline pages.test.ts + primitives.test.ts 341 passed; full offline 2032 passed / 58 files; tsc --noEmit clean; npm run lint exit 0; npm run build green; FULL live suite npm run test:live 72 passed / 11 files against staging, read-only, no writes and no residue. Red-then-green watched twice: (a) dropping data-window-held, hardcoding "table" in the matched arm and defaulting since/until reddened 4 of the new primitive tests; (b) making /browse unconditional reddened absence leg 1 with "/browse still stated [events] over tables it could not read", and replacing /queues WindowLine with a prose <p> reddened both legs with "/queues published no window line". All reverted.

SCOPE beyond touch_scope, deliberate: src/lib/gauges/gauge.ts (WindowInfo.held), src/lib/db/{cycles,runs,browse,claims}.ts (one exported ObjectKind constant each), src/lib/db/tables.ts + 7 gauge test fixtures (comment reword + one field). The comment rewords are because the ticket check `grep -rn "not the whole" src` counts COMMENT lines too and already failed at the parent commit on tables.ts:52 and gauge.ts:112; I reworded those two and my four new ones so it now reads 0. No product copy changed by that.

FOR QA / residual: the primitive holds FOUR sentence variants, not one. That is deliberate — the ticket bar is unchanged rendered text, and the four ledes are different grammatical objects (a noun phrase for cycles/runs/browse, a full sentence for /claims), so unifying them is a COPY decision I will not make silently. They are now visible side by side in one file, which is the precondition for someone who owns the voice to unify them. Second residual: /browse computes its own truncated as `events.data.length >= view.window` in the page, because lib/db/browse.ts returns no window object; that is honest but it is the one truncation flag not produced by the read module.
- 2026-09-04T08:16:57Z [dispatcher] landed ticket/DEBT-0006 onto run/admin-window as 00dd3e6
- 2026-09-04T08:17:35Z [qa] built → qa: under attack
- 2026-09-04T08:30:09Z [qa] qa → done: ATTACKED on the landed tree (00dd3e6; product identical at 3a94fbb, DEBT-0004's landing touched tracker only). Criteria re-run BEFORE reading the handoff; receipt GREEN on all six checks.

CRITERIA. (1) grep 'not the whole' src -> 5 hits, all src/components/ui/window-line.tsx. (2) grep 'data-window=' src/app -> 2 hits, both comment lines. (3) WINDOWED floor = 5 routes / 10 hooks, /queues and /browse joined. (4) StateOf wrappers measured on all 8 routes, both refusal states: /, /queues, /browse, /records gained them (0->3, 0->3, 0->1, 0->1); the other four unchanged. (5) full offline 2033/58, tsc clean, lint 0, FULL live suite 72/11 green against staging, read-only, no writes.

RENDERED TEXT — the builder's 'exactly one change' claim, re-measured on a WIDER drive than they ran. Detached worktree at parent 63e5e84 under evidence/, same harness in both trees, text extracted, clock instants masked. 127 renders (8 routes x 15 param sets x FIVE db moods — I added sparse/all-nullables, and empty + junk + duplicate-column params on every param-reading route, which the DEBT-0003 harness never drove) + 8 cap-filled renders + the dial x2 = 137 comparisons. 127 identical, 10 differing: browse.{not-provisioned,error}.{0..4} losing exactly the one sentence, ARCHITECTURE 4.3. No other diff anywhere. The review-item dial: text identical in BOTH truncation directions (1000 observations -> held=1000 truncated=true; 12 -> false), and it publishes 6 hooks where the parent published none.

THE HOLE IN THAT CLAIM, CLOSED: neither the builder's 60 renders nor the DEBT-0003 harness ever FILLED a cap — the shared populations hold 6 cycles / 4 runs / 10 claims / 1 event against caps of 200/200/50/50, so the truncated arm of all four DrawnSentence variants was rendered by NEITHER tree. Drove 201 cycles, 201 runs, 273 matching claims, 50 events: all four truncated sentences fire ('older cycles ran than the ones below', '273 claims match these filters; the 50 longest-waiting are below', ...) and all 8 pages are byte-identical to parent.

FLOOR SABOTAGE, in a throwaway worktree (product source never touched here): drop data-window from the primitive -> 3 legs red; delete /queues' WindowLine -> '/queues published no window line on a healthy read'; make /browse unconditional -> 6 red. The floor bites.

GAP I FOUND AND CLOSED MYSELF (tests are mine): the fold gave the /queues/[reviewItemId] dial a data-window hook for the FIRST time and no test read it — pages.test.ts's own comment claimed 'its own suite drives it', and grep showed zero data-window assertions in tests/offline/review-item/. That is the exact state /queues was in when this ticket was filed, on the one surface the fold created. Added a 3-leg test to that file (4a90f04), each leg watched RED under its own sabotage: hook dropped, since/until dropped, page handing a window down on a refused trend read. Behaviour was already correct (window: null at page.tsx:395) — a coverage gap, not a defect, so no bounce.

RESIDUALS (no ticket; dispatcher's call). (a) The 'catalog' arm is the only DrawnSentence with no truncated clause: /browse at 50 events publishes data-window-truncated=true and the sentence says nothing — pre-existing, identical at parent, and a COPY decision (designer's). (b) Four ledes in one primitive is the right shape for THIS ticket (bar was unchanged text) and is now the precondition for a voice owner to unify them — a copy ticket, not a build one. (c) /browse computes truncated in the page: honest, one flag not produced by a read module; §4 permits it, worth one line if lib/db/browse ever grows a window object. (d) src/components/gauges/state.tsx is the THIRD refusal rendering, outside src/app/ and so outside this ticket's criterion — the remaining half of the DEBT-0003/0006 class. [resolution: fixed] [receipt green: 6 cmd(s), tree 8603a3fe]
