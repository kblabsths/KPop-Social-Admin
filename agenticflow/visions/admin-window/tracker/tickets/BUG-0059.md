---
id: BUG-0059
type: bug
status: done
milestone: M1
priority: P2
opened_by: verifier
parent: null
discovered_from: M1-endgame-verifier
children: []
assignee: null
claimed_at: null
depends_on: []
touch_scope: [src/components/ui/not-provisioned.tsx, tests/offline/absence/pages.test.ts]
force: false
attempts: 1
resolution: already_done
---
# Not-provisioned card jams the missing object's name into the sentence: 'eventsisn't in this database yet'

## Description
EC11 is the criterion this defaces: it requires every ecosystem page to render a not-provisioned state NAMING what is missing. It does name it — and then runs the name into the next word, so a human reads 'eventsisn't'.

REPRO from a clean start (no staging needed; measured 2026-09-03, verifier M1 walk).
1. Start a stub PostgREST of your own that answers every read 404 with a PostgREST absence body:
     {"code":"PGRST205","message":"Could not find the table 'public.<t>' in the schema cache"}
   (I ran a 20-line python http.server on 127.0.0.1:8785, my own pid, killed after.)
2. Launch a second app instance pointed at it, on a free port:
     ( set -a && source .env && set +a && SUPABASE_URL="http://127.0.0.1:8785" SUPABASE_SERVICE_ROLE_KEY="stub-not-a-real-key" AUTH_URL="http://localhost:8782" npm run start -- --port 8782 )
3. Mint a cookie (node tests/walk/session-cookie.mts) and open /browse, /, /queues, /claims, /sources, /cycles, /records/groups/<id>.

EXPECTED: 'events isn't in this database yet - it arrives with the scraper repo's migrations.'
OBSERVED: 'eventsisn't in this database yet - it arrives with the scraper repo's migrations.'
Served HTML, verbatim:
  <div data-state="not_provisioned" class="border border-hairline bg-surface p-3"><p class="type-body text-ink-secondary"><span class="type-data text-ink">events</span>isn&rsquo;t in this database yet - it arrives with <!-- -->the scraper repo&#x27;s migrations<!-- -->.</p></div>
Playwright inner_text agrees: 'eventsisn't in this database yet'.

Every page shows it in absence mode, with its own object name: review_items (Dashboard ATTENTION, both Queues panels, QUEUE HEALTH), resolution_runs and runs (Dashboard CYCLES/RUNS, Cycles and runs), pending_claims and observations (Claims, Sources gauges), sources (Sources REGISTRY), events (Browse), groups (a groups record page). All seven surfaces answered 200 with zero page errors — the state itself works, only its copy is broken.

WHERE IT COMES FROM. src/components/ui/not-provisioned.tsx line 36 writes the space in the source —
    <span className="type-data text-ink">{missing}</span> isn&rsquo;t in this
    database yet - it arrives with {arrivesWith}.
— and that space does not survive into the rendered output. Same family as BUG-0045's 'stuck_patterndial', a different component; BUG-0045's touch scope does not include this file, so this is not a duplicate. Fix wants a RENDERED assertion, not a source-text one: tests/offline/absence/pages.test.ts already renders these pages, so assert the rendered string contains 'events isn' with the space.

Evidence (gitignored working files; every string transcribed here): agenticflow/tracker/evidence/M1/verifier/absence-browse.png, absence-dashboard.png, absence-queues.png. Run log: agenticflow/tracker/notes/verify-M1-2026-09-03.md section 6.

## Acceptance criteria
In absence mode every not-provisioned card renders one space between the object name and the sentence — rendered text reads 'events isn't in this database yet - it arrives with the scraper repo's migrations.' — on all six pages and the record page

## Checks
```sh
npx vitest run --project=offline tests/offline/absence/pages.test.ts
```

## History
- 2026-09-03T22:53:36Z [verifier] opened: Not-provisioned card jams the missing object's name into the sentence: 'eventsisn't in this database yet'
- 2026-09-04T01:04:17Z [builder-86] claimed (attempt 1)
- 2026-09-04T01:11:03Z [builder-86] MEASUREMENT — delivered HTML, production build, absence mode (2026-09-03, builder-86).

Setup: `npm run build` then `next start --hostname 127.0.0.1 --port 8787` in the BUG-0059 worktree, SUPABASE_URL pointed at my own 20-line stub PostgREST on 127.0.0.1:8785 answering every read 404 {"code":"PGRST205", "message":"Could not find the table 'public.<t>' in the schema cache"}; throwaway AUTH_SECRET literal (no .env read, no staging touched); minted cookie via tests/walk/session-cookie.mts --domain 127.0.0.1. Both pids mine, both killed.

All 7 surfaces answered 200. 13 distinct not-provisioned cards, EVERY one spaced. Verbatim, /browse:
  <div data-state="not_provisioned" class="border border-hairline bg-surface p-3"><p class="type-body text-ink-secondary"><span class="type-data text-ink">events</span> <!-- -->isn’t in this database yet — it arrives with <!-- -->the scraper repo&#x27;s migrations<!-- -->.</p></div>
Rendered text per surface: / review_items, resolution_runs, runs; /queues review_items; /claims pending_claims, observations; /sources sources, observations; /cycles resolution_runs, runs, field_provenance; /browse events; /records/groups/<id> groups — each reading '<name> isn’t in this database yet — it arrives with the scraper repo's migrations.'
Repo-wide scan of those 7 delivered pages for '</span|a|code|strong|em|b>[optional <!-- -->][letter]': 0 hits.

So the defect is FIXED ON THE RUN BRANCH by commit a295ced (admin-window/BUG-0045, 'no factory ticket id, and no run-together words, in rendered copy'), which replaced the implicit space with {" "} in src/components/ui/not-provisioned.tsx. The verifier's 2026-09-03 walk ran against a build predating that landing.

Pin strength, measured on this tree (mutate component, run tests/offline/absence/pages.test.ts, restore):
- name glued directly to the next word -> 8/8 per-surface tests RED.
- the PRE-FIX implicit-space spelling BUG-0045 replaced -> 247/247 still pass. renderToStaticMarkup keeps the whitespace run next build drops, so the offline rendered pin cannot see THAT spelling; tests/offline/ui/copy.test.ts (source rule, repo-wide) is the guard that can. The two are complements and the test docblock says so.
- 2026-09-04T01:11:57Z [builder-86] claimed → built: ALREADY FIXED IN THE PRODUCT — by a295ced (admin-window/BUG-0045), which put an explicit {" "} in src/components/ui/not-provisioned.tsx. The verifier's walk ran against a build predating that landing; no product code changed here.

- Proved on DELIVERED HTML from a production build (worktree, next start on my own port 8787, SUPABASE_URL pointed at my own stub PostgREST on 8785 answering every read PGRST205; throwaway AUTH_SECRET, no .env read, staging untouched; both pids mine, both killed). All 7 surfaces 200; 13 not-provisioned cards, every one reading '<name> isn’t in this database yet — it arrives with the scraper repo's migrations.' Full transcription incl. verbatim /browse markup is in the comment above.
- Landed: the guard the ticket asked for, in tests/offline/absence/pages.test.ts — per surface, under a fully unprovisioned database, every card's naming element must be followed by a space. Structural (name matched against TABLE_NAMES on a leaf, 'after' read off the following text node); asserts a separator, not the card's words, so copy edits stay free.
- TRAP FOR QA, measured not assumed: this offline pin CANNOT see the pre-fix spelling. Mutating the component to glue the name directly reddens 8/8 surfaces; restoring BUG-0045's pre-fix implicit-space spelling still passes 247/247, because renderToStaticMarkup keeps the whitespace run 'next build' drops. Only tests/offline/ui/copy.test.ts (source rule, repo-wide) catches that spelling — the two are complements, and the docblock says so. Don't 'strengthen' the offline pin into a delivered-HTML claim it can't make.

Verified on the rebased tree: npx vitest run --project=offline tests/offline/absence/pages.test.ts (247 pass), same for tests/offline/ui/copy.test.ts (3 pass), npm test (1755 + 6 pass), tsc --noEmit clean, npm run lint clean.
- 2026-09-04T01:12:15Z [dispatcher] landed ticket/BUG-0059 onto run/admin-window as 9b434c3
- 2026-09-04T01:12:47Z [qa] built → qa: under attack
- 2026-09-04T01:21:01Z [qa] qa → done: ATTACKED ON THE LANDED TREE (9b434c3), criteria driven before the builder's story was used, in a detached worktree of the merge commit so the shared checkout was never mutated.

1. CRITERION, ON DELIVERED HTML (not renderToStaticMarkup). Own production build in that worktree; own stub PostgREST on 127.0.0.1:8795 answering every read PGRST205 "Could not find the table 'public.<t>' in the schema cache"; own `next start` on 127.0.0.1:8796; throwaway AUTH_SECRET literal, no .env read, staging never touched; both pids mine, both killed (ports free after, verified with lsof).
Expected (criterion): "<name> isn't in this database yet - it arrives with the scraper repo's migrations." on the six pages and the record page.
Found: 10 routes (/, /queues, /queues/<id>, /claims, /sources, /cycles, /browse, /records/{groups,idols,events}/<id>) all 200; 22 not-provisioned cards; structural read of the char after every naming span (`<span class="type-data text-ink">NAME</span>(<!-- -->)?X`) = 0 glued, 22/22 X == " ". Verbatim, /sources:
  <span class="type-data text-ink">sources</span> <!-- -->isn’t in this database yet — it arrives with <!-- -->the scraper repo&#x27;s migrations<!-- -->.
Eyebrow cards are not a seam: the eyebrow is its own block <p class="type-micro"> before the body <p> (/claims "Pending claims"), so it cannot glue onto the name.

2. WIDENED THE ATTACK past the criterion — same 10 routes re-driven under four database moods (table-absent PGRST205, empty 200 [], server-error 500, and column-absent PGRST204 which routes through classify()'s `${missing}.${column}` arm and renders names like `review_items.severity`). 38 pages delivered, every one 200; whole-page glue scan `</span|a|b|i|em|strong|code|small|abbr|label|button|time|sup|sub>(<!-- -->)?[A-Za-z0-9]` over all of them: 0 hits, 19 further naming spans in the column mood all spaced.

3. THE PIN IS NON-VACUOUS, and its stated limit is real (mutate component, run, restore; all in the throwaway worktree):
 - name glued directly to the next word -> absence/pages.test.ts 8/8 surface tests RED, copy.test.ts green (correctly: no implicit space in source);
 - BUG-0045's pre-fix implicit-space spelling (`</span> isn&rsquo;t in this` + newline) -> absence pin GREEN 247/247 (the documented blindness), tests/offline/ui/copy.test.ts RED naming `src/components/ui/not-provisioned.tsx 36:` — the complement holds, checked rather than trusted;
 - the third spelling nobody measured, `</span>` + newline + word -> absence pin RED 8/8. So all three glue-capable spellings are caught by one pin or the other; no gap between them.

4. SUITES on the landed tree: `npm test` exit 0 (1755 offline + 6 isolated), `npm run test:http` exit 0 (13), ticket check exit 0 (receipt GREEN).

RESOLUTION already_done: the defect the verifier saw was fixed by a295ced (BUG-0045) before this ticket was claimed; no product code changed here and none was needed. What lands is the guard EC11 lacked. RESIDUAL (no ticket): the pin recognises a name only if it is in TABLE_NAMES, so a column-absent card (`events.severity`) is invisible to it — same JSX renders both, and the column mood above shows no divergence on delivered HTML. [resolution: already_done] [receipt green: 1 cmd(s), tree 6aed9a6d]
