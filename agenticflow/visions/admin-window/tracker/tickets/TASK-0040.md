---
id: TASK-0040
type: task
status: done
milestone: patch
priority: P1
opened_by: strategist
parent: null
discovered_from: inbox:2026-09-09-vision-amendment.md
children: []
assignee: null
claimed_at: null
depends_on: []
touch_scope: [src/lib/edit, src/app/records, src/app/api/admin, src/components, src/lib/db, tests/offline, tests/live, tests/http]
force: false
attempts: 2
empty_diffs: 1
human_note: Groups and idols can no longer be edited from the dashboard at all: their record pages now answer with the ordinary not-found page, and any attempt to change one of their fields is refused without touching the data.
resolution: fixed
---
# Strike the direct-edit path: groups/idols leave EDIT_CONFIG, and every surface that assumed it is reconciled

## Description
**Ben struck the direct-edit scope from the frozen VISION (2026-09-08, routed 2026-09-09).** The struck sentence is "groups/idols edit directly within it", with his note: *"admin edits catalog tables only through the observation pipeline; do not re-implement direct edits. groups/idols stay as test tables until they are removed."* He ruled two adjacent things the same day: groups/idols get **no door** (no listing, no search, no Browse entry — ROADMAP question 1 answered "nothing"), and the group row's own `source_*` provenance columns **stay hidden** as he ruled on 2026-09-02.

**A strike licenses nothing. This ticket only removes and reconciles.** No new surface, no compensating feature, no new regime invented here, and above all no second write path built anywhere to keep the old behaviour reachable. If a step seems to need one, stop and report it.

**Sequencing — read this before you claim it.** What `Regime` means in a world with no catalog `pre_cutover` table (the walk sandbox is the only `pre_cutover` entry left) is the **architect's ruling of 2026-09-08**, being written today into `agenticflow/docs/DECISIONS.md` and as an ARCHITECTURE rule. Do not start until that ruling is in the repo; take the regime mechanics and the "do not re-implement direct edits" rule **from it, by reference**. Do not restate the rule in a third place and do not decide the sandbox's regime yourself.

**The change itself is two entries.** Delete the `groups` and `idols` objects from `ENTRIES` in `src/lib/edit/config.ts`. That is the whole mechanism: the map is the one allowlist, so the consequences follow by construction — `editConfigFor("groups")` returns null, the record page `notFound()`s, and `PATCH /api/admin/records/groups/<id>` refuses `unknown_table` with 404. After it, **no catalog table is writable from Admin**: `events`/`venues` stay resolver-owned with an empty `editable` list until FEAT-0011 lands their override path (that path is FEAT-0011's, not this ticket's), and nothing here touches their rendering.

**What must be reconciled, not deleted silently:**

- **`tests/live/edit.live.test.ts`** — its `describe.each(["groups","idols"])` write case is the direct path's proof. **Invert it**, do not drop it: the record URL 404s, a forged PATCH naming a column the old allowlist carried is refused, and the staging row re-reads identical. The suite must still prove the negative on both tables.
- **`tests/http/auth.http.test.ts`** hardcodes `/records/gro%75ps/2f0bc11e` expecting **200** (the percent-encoding / case-variant rewrite tripwire, BUG-0017). That expectation inverts — but the tripwire it guards must survive, re-aimed at a table that IS on the map, or a future unmapped table starts rendering silently.
- **`tests/live/residue.live.test.ts`** — its editable-table floor asserts that a `pre_cutover` table has a scanned text column and a non-zero row count, so a sweep over nothing cannot pass as clean. **Before the map narrows, run the sweep once against staging with `groups`/`idols` still on it and paste the result into this ticket's History.** That is the last chance anyone has to see a leftover from the interim walk-write exception; after this the sweep cannot look there at all. Then re-express the floor over the surfaces Admin can still write, keeping the property that an empty sweep fails loudly.
- The offline suites that assert the two entries (`tests/offline/edit/config.test.ts` and the record-page/route suites) assert their **absence** instead.

**Leave alone:** `src/lib/db/tables.ts`'s `groups`/`idols` names (the sweep and the schema description still spell those tables — the table registry is not the edit map); `events`/`venues` behaviour; `.env.example`; port 8771; `walk_sandbox`'s entry (the architect's call); VISION.md, which is byte-identical before and after. No migration and no DDL from this repo, ever.

**No door means no door.** Do not add a read-only groups/idols listing, a search box, a Browse entry, or a nav link "to make up for" the removal. If the architect's ruling keeps a read-only *record* rendering for either table, follow that ruling and the four-state contract; do not invent one from your own judgment.

## Acceptance criteria
1. `EDIT_CONFIG` carries no `groups` and no `idols` entry and `EDITABLE_TABLES` names neither; a grep over `src` finds no second allowlist, no per-table route, and no helper that reintroduces either table's write path.
2. `PATCH /api/admin/records/groups/<id>` and `/api/admin/records/idols/<id>`, naming any field including one the retired allowlist carried, are refused server-side (404, `unknown_table`, the refusal naming the table); the staging row re-reads identical, every column compared.
3. `/records/groups/<uuid>` and `/records/idols/<uuid>` answer the app's framed 404 — the ROUTED miss, never a `notFound()` thrown from the dynamic segment (ARCHITECTURE §5, BUG-0017), through the `next.config.ts` rewrite that is already derived from `EDITABLE_TABLES`. No read-only record rendering survives for either table: the architect's 2026-09-08 ruling was re-ruled the same day (ARCHITECTURE §9, DECISIONS 2026-09-08) and the map is where they leave.
4. Nothing was added: no new write path, no listing, no search box, no Browse entry, no nav link and no cross-link over `groups`/`idols` anywhere in `src`; the sidebar still holds exactly six links.
5. **The regimes become two, and the teeth are structural.** `Regime` is `"sandbox" | "resolver_owned"`; `pre_cutover` occurs nowhere under `src`; `walk_sandbox` carries `sandbox` and `events`/`venues` carry `resolver_owned`. `writePathFor(regime): WritePath` is total (`sandbox` -> "direct", `resolver_owned` -> "override") and the record page's regime note is honest for both — the sandbox's says a value written there goes to a staging fixture, not "to the catalog" (ARCHITECTURE §9.1 item 5). `tests/offline/edit/config.test.ts` pins **the only table whose write path is `direct` is `walk_sandbox`**, proved on TWO fixtures: a probe map entry adding a catalog table with a direct path must redden it, and the shipped map must not (LESSONS 3).
6. Every test that exercised the direct write is converted rather than deleted — `tests/live/edit.live.test.ts` proves the refusal on both tables, `tests/http/auth.http.test.ts` keeps its rewrite tripwire re-aimed at a table that IS on the map, and the offline config suite asserts the two entries are absent.
7. The residue sweep ran once against staging with the old map still in place and its result is pasted in this ticket's History (DONE, 2026-09-09: clean, 58 columns across 5 of 5 mapped tables); afterwards its editable-table floor is expressed over `walk_sandbox` — measured present on staging with rows the same day — and a sweep with nothing to scan still fails loudly instead of passing clean.
8. `events` and `venues` behaviour is unchanged (still read-only, still refusing — their override path is FEAT-0011's), `src/lib/db/tables.ts` still spells both catalog table names, `.env.example` is untouched, port 8771 is untouched, and `agenticflow/docs/vision/VISION.md` is byte-identical before and after.
9. `npm run lint`, `./node_modules/.bin/tsc --noEmit` and `npm test` green; `npm run test:live -- tests/live/edit.live.test.ts` and `npm run test:live -- tests/live/residue.live.test.ts` green.

## Checks
```sh
bash -c 'rm -rf .next/types && npm run lint && ./node_modules/.bin/tsc --noEmit'
bash -c 'npm test'
bash -c 'test "$(grep -nE "table: \"(groups|idols)\"" src/lib/edit/config.ts | wc -l)" -eq 0'
bash -c 'test "$(grep -rn "pre_cutover" src --include=*.ts --include=*.tsx | wc -l)" -eq 0'
npm run test:live -- tests/live/edit.live.test.ts
npm run test:live -- tests/live/residue.live.test.ts
```
## History
- 2026-09-09T01:06:21Z [strategist] opened: Strike the direct-edit path: groups/idols leave EDIT_CONFIG, and every surface that assumed it is reconciled
- 2026-09-09T01:14:01Z [builder-118] claimed (attempt 1)
- 2026-09-09T01:19:46Z [builder-118] PRE-REMOVAL RESIDUE SWEEP (criterion 6, the one-time read-only run over the OLD map, groups/idols still on it). Command, verbatim, on the rebased worktree tree with EDIT_CONFIG unchanged: `npm run test:live -- tests/live/residue.live.test.ts`. Result: exit 0, 5/5 tests, 8.4s wall, host ubfjjqlvnpnoborczbdb.supabase.co. CLEAN: 58 column(s) scanned across 5 of 5 mapped table(s), every count 0 for the marker "admin-window". Per table — groups: 19 text columns scanned, all 0 (name, short_name, korean_name, image_url, bio, company, status, type, fanclub_name, kpopping_slug, wikidata_qid, wikipedia_url, spotify_id, image_source_url, image_license, source, source_page_id, source_url, source_license); 12 not scannable by ilike (id uuid, debut_date date, member_count integer, created_at/updated_at timestamptz, profile_image_id uuid, spotify_followers/spotify_popularity integer, social_links jsonb, source_rev_id bigint, last_synced_at/last_verified_at timestamptz). idols: 23 text columns scanned, all 0 (stage_name, real_name, korean_name, image_url, position, nationality, gender, kpopping_slug, wikidata_qid, wikipedia_url, spotify_id, bio, blood_type, mbti, agency, birth_place, image_source_url, image_license, source, source_page_id, source_url, source_license, status); 15 not scannable (incl. height_cm/weight_kg smallint, birth_date/debut_date date). events: 7 text columns, all 0. venues: 7 text columns, all 0. walk_sandbox: 2 text columns (label, note), both 0. NO leftover from the interim walk-write exception exists on staging; nothing was written by this run (the sweep is read-only). SECOND MEASUREMENT, unexpected and load-bearing: public.walk_sandbox IS NOW PRESENT ON STAGING WITH ROWS. The sweep did not take the tolerated-absent branch for it — it appears in PostgREST's live OpenAPI description with its 7 columns (sandbox_id uuid, label, note, tally integer, is_flagged boolean, observed_on date, created_at timestamptz), it was counted in "5 of 5 mapped table(s)", and the pre_cutover floor `rowCount(walk_sandbox) > 0` passed. So Ben's DDL paste has landed; the dispatch premise "walk_sandbox does not exist on staging yet" is stale as of 2026-09-08 18:18 local.
- 2026-09-09T01:20:54Z [builder-118] claimed → blocked: QUESTION (one sentence, answerable): do `groups`/`idols` STAY in EDIT_CONFIG as regime `read_only` — keeping their read-only record pages, so a PATCH to them is refused as a known-but-unwritable table — per the architect's 2026-09-08 ruling this ticket tells me to follow, or are their two ENTRIES objects DELETED so `/records/groups/<uuid>` 404s and PATCH answers 404 `unknown_table`, per this ticket's criteria 1-2 and its stored check?

Both are written down, and they are mutually exclusive:
- STAY: ARCHITECTURE §9 (architect, 2026-09-08, uncommitted in the primary checkout) declares `Regime = "sandbox" | "resolver_owned" | "read_only"` plus `writePathFor`, and says of `read_only` — "`groups`, `idols`. writePathFor -> null ... They stay in the map because their record pages, their `display` columns and F12's reference links are reads". DECISIONS 2026-09-08 says it again ("stay in the map and keep their record pages ... stated so it is cheap for Ben to correct"). This ticket's description says to take the regime mechanics "from it, by reference", and criterion 3 admits "exactly the rendering the architect's 2026-09-08 ruling names".
- DELETE: this ticket's description ("Delete the `groups` and `idols` objects from `ENTRIES`"), criteria 1 and 2, and the stored check `grep -cE "table: \"(groups|idols)\"" src/lib/edit/config.ts -eq 0` — which goes RED under STAY, and only a strategist/architect may amend it. That, not the design uncertainty, is why I stopped: the ticket cannot pass its own receipt under the ruling it tells me to follow.

Nothing else is in doubt and none of it is started, so one word unblocks the whole build: three regimes, `writePathFor(read_only) -> null`, `walk_sandbox` -> `sandbox`, no `src/` write path for groups/idols, live edit tests inverted not deleted, http tripwire re-aimed, sweep floor re-expressed.

DONE MEANWHILE (criterion 6, the one-time pre-removal sweep over the OLD map, read-only, full per-column result in the previous History note): `npm run test:live -- tests/live/residue.live.test.ts` exit 0, 5/5, 58 columns scanned across 5 of 5 mapped tables, every count 0 for "admin-window" — no leftover of the interim walk-write exception exists on staging. That run also measured that `public.walk_sandbox` IS NOW PRESENT on staging with rows (it was swept, not skipped, and the row-count floor passed), so the dispatch premise "walk_sandbox does not exist on staging yet" is stale and the editable-table floor can be re-expressed over the sandbox and actually hold. [EMPTY DIFF: no file changed under scope — 1 so far]
- 2026-09-09T01:24:42Z [architect] criteria amended: Architect ruling, 2026-09-08 (re-ruled the same day on builder-118's question): groups/idols LEAVE the map — DELETE, not a read_only regime. The regimes become two, and the teeth move from the type to a structural pin (the only direct write path is walk_sandbox), proved on two fixtures. Criterion 3's 'or the rendering the architect names' ambiguity is removed; criteria 5 and 7 are new; the pre-removal sweep is recorded as done. [was: 1. `EDIT_CONFIG` carries no `groups` and no `idols` entry and `EDITABLE_TABLES` names neither; a grep over `src` finds n… (2065 chars; full text in git history)]
- 2026-09-09T01:24:43Z [architect] checks amended: The grep -c count form is replaced with the sanctioned test "$(... | wc -l)" -eq 0 form (the gate refuses grep -c), the pre_cutover pin is added, and the two live suites the criteria name are now stored checks rather than prose (ARCHITECTURE §13.1: a ticket touching a page carries that page's live suite). [was: bash -c 'npm run lint && ./node_modules/.bin/tsc --noEmit', bash -c 'npm test', bash -c 'test "$(grep -cE "table: \"(gro… (165 chars; full text in git history)]
- 2026-09-09T01:24:43Z [architect] blocked → open: UNBLOCKED. Answer to builder-118's question, in one word: DELETE. groups/idols leave EDIT_CONFIG; there is no read_only regime. I drafted one this morning and re-ruled against it this afternoon on your question — with no door (Ben, 2026-09-08) and no write, a record page reachable only by a pasted uuid is a surface nobody can reach; F12's reference-as-link stands on events.venue_id, which is what acceptance test 8 grades; and 'stay as test tables' is satisfied by the database, since lib/db/tables.ts keeps both names and your sweep reads all 42 of their columns. ARCHITECTURE §9 and DECISIONS 2026-09-08 now say exactly this, and the ticket's criteria and checks are amended to match (the check that blocked you was also in the refused grep -c form; it is now the sanctioned wc -l form). Two regimes: sandbox (walk_sandbox alone, direct) and resolver_owned (events/venues, override — FEAT-0011's, not yours). The teeth are no longer the type: pin, on two fixtures, that the only table whose write path is direct is walk_sandbox. Thank you for the sweep and for the walk_sandbox measurement — both are recorded in DECISIONS and in the M2 tickets.
- 2026-09-09T01:27:26Z [builder-118] claimed (attempt 2)
- 2026-09-09T01:58:57Z [builder-118] claimed → built: DONE — `groups`/`idols` are gone from `EDIT_CONFIG`; regimes are two (`sandbox` = walk_sandbox direct, `resolver_owned` = events/venues override), `writePathFor` is total, `pre_cutover` occurs nowhere under src.

- **Teeth, on two fixtures** (criterion 5): `tests/offline/edit/config.test.ts` pins `directWriteTables(map)` — shipped map is exactly `["walk_sandbox"]`, and a probe map re-adding a catalog table under `sandbox` MUST be flagged. Watched red for real: re-adding a `groups` entry with `regime: "sandbox"` reddened 5 tests in that file, including this pin. The struck allowlists are refused whole, not by sample — all 10 groups columns and 16 idols columns, in the offline config suite, the route suite and the live suite.
- **Converted, not deleted**: `tests/live/edit.live.test.ts`'s write case is inverted (record page rejects with Next's 404, every retired column PATCHes 404 `{"error":"<table> is not an editable table"}`, row re-read whole and identical); watched red by putting `groups` back on the map. `tests/http/auth.http.test.ts` keeps the BUG-0017 tripwire re-aimed at `events` (`/records/ev%65nts/<id>` → 200 framed; `/records/EVENTS/<id>` → 404), and `/records/groups|idols/<id>` moved into its unmapped list — both now serve the app's framed 404 through the `EDITABLE_TABLES`-derived rewrite, proved over http on a real build. Residue floor is re-expressed over what Admin can write, with a new `assertSweptSomethingWritable` guard (two fixtures); its call site is live — flipping the sandbox to `resolver_owned` fails the sweep loudly instead of passing clean.
- **Proof, on the rebased tree (onto 9923c2f)**: `npm run lint` 0, `tsc --noEmit` 0, `npm test` 2129 passed / 59 files, `npm run test:http` 14 passed, `npm run test:live` 78 passed / 11 files (sweep clean: 16 columns across 3 of 3 mapped tables, all 0).

TRAPS FOR QA. (1) I merged a rebase conflict in `tests/live/edit.live.test.ts` with TASK-0037: its walk-sandbox block is intact and I deliberately DROPPED my duplicate sandbox-forgery case, so "an unmapped column is refused 403 with the row unchanged" lives once, in `the walk sandbox > refuses a column the map does not carry`. (2) Several suites had `groups` as a stand-in subject where the assertion was about something else (the id gate, the body parser, the four states); re-aiming them at an UNMAPPED table would have made them pass vacuously, so each moved to `walk_sandbox` or `events` — check `tests/http/edit.http.test.ts`'s hostile-id block and step 4/5 especially. (3) Nothing writes `groups`/`idols` anywhere now; the live suite still READS both (the inverted case, and `countMarked("groups","created_at")` in the sweep's refusal test). (4) `next.config.ts`, `src/lib/db/tables.ts`, `.env.example`, port 8771 and VISION.md are untouched (VISION shasum still 4b934d15…).
- 2026-09-09T01:59:00Z [dispatcher] landed ticket/TASK-0040 onto run/admin-window as 58c776a
- 2026-09-09T02:09:51Z [qa] built → qa: under attack: qa-TASK-0040, detached worktree under evidence/, production build on 8779
- 2026-09-09T02:13:37Z [qa] qa → done: ATTACKED on a production build of the landed tree (run/admin-window @ fcc47c8), own `next build` + `next start --port 8779` from a detached worktree under evidence/, staging env, minted authjs.session-token, REAL staging uuids. I wrote nothing to staging: no INSERT, no UPDATE, no DELETE, on any table including walk_sandbox. Criteria re-run before reading the builder's handoff.

C1 grep: EDITABLE_TABLES = [events, venues, walk_sandbox]. The only `groups`/`idols` left under src are comments, `lib/db/tables.ts` (required), a `hexadecimal digits ... groups` copy string and a `gauge.ts` local. No second allowlist, no per-table route.

C2 26 FORGED PATCHes — the WHOLE retired allowlists (10 groups + 16 idols columns) with real uuids and a valid admin cookie — all 404 `{"error":"<table> is not an editable table"}`. Row re-read whole after: groups 31 columns, idols 38 columns, differing=[]. Also refused: `/GROUPS/`, `/gro%75ps/`, `/groups%20/`, `?columns=name`, field=`id`, field=`__proto__`; absent `value` -> 400. `walk_sandbox` with a groups id -> 404 "no walk_sandbox record with that id" (no write).

C3 `/records/groups/<uuid>` and `/records/idols/<uuid>` -> 404, len 9728, FRAMED: `<h1>` present, six nav links, no `id="__next_error__"` — byte-shape identical to `/analytics`, the routed-miss control. Case variants `/GROUPS/`, `/IDOLS/` likewise. DIVERGENCE on the percent-encoded spelling -> BUG-0081 (below), which is NOT a bounce: criterion 3 names the rewrite "already derived from EDITABLE_TABLES" and that rewrite is what shipped, and this ticket was told to leave next.config.ts alone.

C4 delivered HTML of `/`, `/browse`, `/queues`, `/claims`, `/sources`, `/cycles`: sidebar exactly six links (/, /queues, /claims, /sources, /cycles, /browse) on every one; zero `groups`/`idols` occurrences in any body; the only /records/ link anywhere is `/records/venues/<uuid>` from the events page.

C5 SABOTAGE, watched myself: re-added a `groups` entry with `regime:"sandbox"` to the map in my worktree -> 6 offline tests red, including the pin "gives a direct write path to the walk sandbox and to nothing else"; reverted, clean. Sandbox regime note as DELIVERED: "walk_sandbox is a staging fixture table, edited directly: a value changed here is written to it as it stands and reaches no catalog record" — honest; events' says resolver-owned. `pre_cutover` absent from src.

C6 the live struck-table block is `describe.each` over BOTH tables with their whole retired allowlists and a whole-row compare (not a sample). Tripwire re-aimed and measured: `/records/ev%65nts/<id>` -> 200 framed, `/records/EVENTS/<id>` -> 404.

C7 pre-removal sweep is in History; the floor guard `assertSweptSomethingWritable` is proved on three fixtures and its call site is live.

C8 the landed commit 91a549f touches 16 files — none of them VISION.md, .env.example, next.config.ts or src/lib/db/tables.ts, which still spells both names. (`.env.example` is dirty in the shared checkout from another lane, names only, not this ticket's.)

C9 receipt green, all six stored checks exit 0.

GATE, security-adversarial per the declared public-surface boundary: anonymous GET on every spelling (plain, case-variant, percent-encoded, /__no-record-surface__) -> 307 to /login?callbackUrl=..., so nothing discloses which surfaces exist; anonymous PATCH likewise.

FILED: BUG-0081 (P3) — `/records/gro%75ps/<uuid>` is the same URI as `/records/groups/<uuid>` (RFC 3986 §6.2.2.2, the reasoning next.config.ts itself carries) but serves the client-rendered error shell: 404, len 8006, no <h1>, no nav, `id="__next_error__"`. Pinned `it.fails` in tests/http/auth.http.test.ts, watched RED as a plain it() first, committed 9d92e15.

RESIDUAL, not a ticket: the sweep now scans 16 columns across 3 tables — groups/idols columns are no longer swept for the marker. That is what this ticket sanctioned ("after this the sweep cannot look there at all") and the clean pre-removal run retired the risk, but the architect's 2026-09-08 unblock gave "your sweep reads all 42 of their columns" as a REASON for the delete ruling, and that sentence is no longer true. Worth one line of correction in DECISIONS. [resolution: fixed] [receipt green: 6 cmd(s), tree 65536ff0]
