---
id: BUG-0218
type: bug
status: open
milestone: patch
priority: P2
opened_by: qa
parent: null
discovered_from: BUG-0217
children: []
assignee: null
claimed_at: null
depends_on: []
touch_scope: [agenticflow/tracker/for-human/M3-paging-shape-for-ben.md, agenticflow/tracker/for-human/M3-endgame-walk.md]
force: false
last_failure_sig: 754df2da0da7
same_failure_count: 1
seen_failure_sigs: [754df2da0da7]
---
# Ben's open paging-shape decision names only /claims in its "no" branch, so the cheap branch under-states its scope by the surface BUG-0217 just un-hid

## Description
BUG-0217 corrected LOOK_AND_FEEL bar 14 so its record states that BOTH paged surfaces fail the bar. The two human-facing notes that carry the same verdict to Ben were not corrected, and one of them is the live decision document he is being asked to answer.

Expected (LOOK_AND_FEEL.md:357-362, as landed by BUG-0217 at 0eb45dfa): '**Both paged surfaces FAIL this bar** ... `/claims` and `/browse` render the same `PageMore` control as the last child, after the last row', and 'the fix is whichever paging shape Ben picks, **for both surfaces**'. Verified mechanically: PAGE_ROUTES has exactly two entries (src/lib/paging/bounds.ts:36-42), both render PageMore as the last child of their column (src/components/claims/paged-claim-list.tsx:107 list -> :115 control; src/components/browse/paged-browse-table.tsx:157 list -> :177 control), and both windows are 50 rows (src/components/claims/claim-list.tsx:81 CLAIM_WINDOW = 50; src/lib/browse/views.ts:109 window: 50).

Found A (agenticflow/tracker/for-human/M3-paging-shape-for-ben.md:69-70) — the decision branch Ben answers: '**If no**, say so and `/claims` keeps append-on-press; TASK-0079's new bar then produces one ticket to move the control above the list, which is cheap.' One surface, one ticket. Under the corrected bar 14 the 'no' branch is TWO surfaces and two moves. The same note's line 13 already says the ruling 'Applies to `/claims` and `/browse`', so the note contradicts itself: its 'yes' branch covers both surfaces (line 67, 'both surfaces'), its 'no' branch covers one. Line 61-62 repeats the singular: 'the FIX it implies for `/claims` is deliberately not filed'.

Found B (agenticflow/tracker/for-human/M3-endgame-walk.md:251-253) — the closure paragraph written on 2026-09-11 by the pass that answered the proposal (not struck relic text, present-tense standing statement): 'The `/claims` fix is deliberately **not** filed - it depends on Ben's still-open paging-shape decision.' Same singular, in the walk note that points at bar 14 as authoritative.

Consequence, and why it is not cosmetic: this is exactly the harm BUG-0217's description predicted ('if he picks a narrower fix than page windows, /browse keeps a control the operator must scroll a full window to find'). BUG-0217 assumed Ben's paging-shape note already covered both surfaces; it does on the 'yes' branch only. If Ben answers 'no' to page windows, the document he answered from tells him the cost is one cheap ticket for /claims, and /browse ships with the same control below the same 50-row list.

Repro (no device, no server, no build):
1. cat agenticflow/tracker/for-human/M3-paging-shape-for-ben.md and read the '## What I need from you, in one word each' section.
2. Note the 'no' branch names `/claims` only; `/browse` appears nowhere in that section:
   awk '/^## What I need from you/,0' agenticflow/tracker/for-human/M3-paging-shape-for-ben.md | grep -c '/browse'   ->  0
3. Read LOOK_AND_FEEL.md:357-362 for the standing verdict: both surfaces fail, and the fix is Ben's pick 'for both surfaces'. Two documents, two scopes, one decision.

Fix shape (designer's call, not prescribed): name /browse alongside /claims in the 'no' branch and in line 61-62, with the same 'two surfaces, two moves' arithmetic, and correct the endgame walk's closure paragraph to the same count. Do NOT restate or re-grade bar 14 - it is correct as landed; these notes just have to agree with it. No src/ or tests/ change: the /claims and /browse fixes themselves still wait on Ben's open decision, per BUG-0217 criterion 4.

WHY NO FAILING TEST PINS THIS: the defect is prose in a human-facing tracker note; a suite test that graded its wording would be the copy-oracle QA is banned from building. The stored check below is not a wording pin - it asserts the decision section NAMES the second surface at all, which any correct fix satisfies and no rewording breaks. Watched red on the current tree before filing: exit 1. Found B carries NO stored check on purpose: the walk note's proposal-1 section already contains the literal `/browse` in its measurement table (M3-endgame-walk.md:237), so every mechanical grep over that region is green today and would pin nothing; criterion 3 grades it by reading, the way BUG-0217's own criterion 1 was graded.

## Acceptance criteria
1. M3-paging-shape-for-ben.md's '## What I need from you' section states the 'no' branch's scope for BOTH paged surfaces - /browse named explicitly, with the ticket count the corrected bar 14 implies - so the branch Ben answers from agrees with LOOK_AND_FEEL.md:357-362.
2. The same note's earlier singular at line 61-62 ('the FIX it implies for /claims') names both surfaces too, so the note does not contradict its own line 13 ('Applies to /claims and /browse').
3. M3-endgame-walk.md:251-253's closure paragraph names both surfaces as waiting on Ben's decision.
4. Bar 14, bar 11 and M3.md are not re-edited: BUG-0217 landed them correctly and this ticket only makes the two for-human notes agree with them.
5. (proxy) No product source and no test file changes - the /claims and /browse control moves still wait on Ben's open paging-shape decision.

## Checks
```sh
awk '/^## What I need from you/,0' agenticflow/tracker/for-human/M3-paging-shape-for-ben.md | grep -q '/browse'
test -z "$(git status --porcelain -- src tests)"
```

## History
- 2026-09-12T00:03:00Z [qa] opened: Ben's open paging-shape decision names only /claims in its "no" branch, so the cheap branch under-states its scope by the surface BUG-0217 just un-hid
- 2026-09-12T00:03:42Z [receipt] RED receipt: failure signature 754df2da0da7 (same failure 1x)
