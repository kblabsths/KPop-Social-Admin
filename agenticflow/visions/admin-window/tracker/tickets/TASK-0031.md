---
id: TASK-0031
type: task
status: qa
milestone: M1
priority: P1
opened_by: architect
parent: FEAT-0005
children: []
assignee: architect
claimed_at: 2026-09-03T04:55:55Z
depends_on: []
touch_scope: [agenticflow/docs/vision/ARCHITECTURE.md]
force: false
attempts: 1
---
# ASK BEN: pending_claims view times out on staging (handoff: index or view rewrite in the scraper repo)

## Description
**A handoff, not Admin work.** `public.pending_claims` is defined in the sibling scraper repo (`supabase/migrations/20260901000004_every_pending_claim_lands_in_one_bucket.sql`), a campaign is running there, and ARCHITECTURE §1.1 makes every scraper-side change a handoff for this milestone. This ticket carries the measurement, the candidate artifacts, and the ruling that **no Admin-side mitigation exists**.

## What is measured, and by whom

QA first, on the live parity pass (TASK-0012's History, 2026-09-02): the Claims page's own read times out — `57014 canceling statement due to statement timeout` at 8515 / 8099 / 8127 ms, 3 of 3 — and narrowing it did not help.

Then the architect, read-only, my own client through the staging guard, eight shapes (evidence, redacted: `agenticflow/tracker/evidence/architect/claims-probe.tsv` and `claims-probe2.tsv`). Every shape but one hits the 8s statement timeout with `57014`:

| shape | result |
| --- | --- |
| `limit 1`, no order, no filter | **852–1083 ms, 1 row** — the only shape that returns |
| `limit 2` | 8098 ms, 57014 |
| `limit 3`, `limit 5`, `limit 10` (narrow select, two columns) | 8106–8178 ms, 57014 |
| `order(observation_id) limit 1` | 8107 ms, 57014 |
| `.in("observation_id", <10 known pending ids>)` | 8118 ms, 57014 |
| `.in("observation_id", <100 known pending ids>)` — the gauges' shape | 8146 ms, 57014 |
| `head:true count exact`, whole view | 8138 ms |
| `head:true count exact` per bucket, one per bucket (5 runs) | 8137–8172 ms each |
| `eq("bucket","awaiting_row") limit 10` | 8137 ms, 57014 |

Population, same session, from the base tables (all sub-150 ms): `observations` where `status='pending'` = **859**; `field_provenance` = 1705; `observations` total = 2567.

**Read the shape of that.** A `limit 1` returns in under a second, `limit 2` does not; a filter on the view's own key over ten ids is as slow as no filter at all. Whatever the plan is, no qualifier and no limit bounds the work — the population is classified before anything narrows it.

## Where the cost plausibly is (from the SQL; the plan is yours to confirm)

Two candidates, both readable in the migration:

1. **The `incumbent` lateral has no index to use.** Per claim it runs `select provenance.observation_id from public.field_provenance where entity_type = claim.domain and entity_id = claim.entity_id and field = claim.field order by applied_at desc, provenance_id desc limit 1`. `field_provenance` carries **only its primary key** — I read every `create index` in `supabase/migrations/`: three on `review_items` (20260901000002), one on `confirmed_matches` (20260829000003), three on `observations` (rebuilt on `domain` by 20260819000002), and nothing at all on `field_provenance` beyond `field_provenance_pkey (provenance_id)`. So that lookup is a seq scan of 1705 rows plus a top-N sort, once per live pending claim, 859 times.
2. **The multiply-referenced CTEs are materialized, so nothing pushes down.** `live_pending_claim` is referenced five times and `reference_entry` three; PostgreSQL 12+ materializes a CTE referenced more than once. A materialized CTE is an unindexed tuplestore: correlated lookups into it (`record_bar`'s `missing_column` NOT EXISTS, the `performers` lateral) are full scans of it, and an outer qualifier — `observation_id in (…)`, `bucket = …` — cannot be pushed inside it. That is exactly the measurement's signature: filters do not help.

## The artifact, as precisely as I can state it from here

**Step 0 — the one thing only you can run** (scraper repo, psql; the default timeout will cancel it, so raise it for the session):

```sql
set statement_timeout = '120s';
explain (analyze, buffers, verbose)
select observation_id, bucket from public.pending_claims limit 2;
```

**Candidate A — the index. Forward-only, reversible, no semantic change.** Target file, scraper repo: `supabase/migrations/2026090300000X_the_current_provenance_row_is_one_seek.sql`

```sql
-- pending_claims' incumbent lateral (20260901000004) asks the same question
-- resolver/weighing.py's _incumbent() asks: the latest provenance row for one
-- fact identity. field_provenance carries only its primary key, so that is a
-- seq scan and a top-N sort per live pending claim. This is the seek it wants.
create index if not exists field_provenance_current_per_fact
    on public.field_provenance (entity_type, entity_id, field, applied_at desc, provenance_id desc);

comment on index public.field_provenance_current_per_fact is
  'The current provenance row for one fact identity, in one seek: pending_claims'' incumbent lateral and every "what is canonical now" read';
```

Apply: `supabase db push` from `kspace Scraper/` (the only repo that pushes migrations). Reverse: `drop index public.field_provenance_current_per_fact;`.

**Candidate B — the planner hint, if A is not enough.** In the view, `with live_pending_claim as not materialized (…)` and `reference_entry as not materialized (…)`. One word each; a CTE materialization hint changes no semantics. It is what lets an outer `observation_id in (…)` or `bucket = …` reach `observations` and use the partial indexes 20260819000002 already built. If B alone fixes it, the view stays exactly as written otherwise.

**Candidate C — the one I recommend against reaching for first: a materialized view.** It would break the property the view's own header opens with — "derived on every read and storing nothing … it can never disagree with them" — and hand the resolver a refresh cadence to own. That is a design conversation in the scraper campaign, not a fix.

**Not a fix: raising the statement timeout.** An honest read that takes 30 seconds is still a broken page, and the timeout is the only thing currently telling anyone the view is super-linear.

## Two things worth knowing before you decide

- **This is not an Admin-only symptom.** The view is `security_invoker` and Admin reads it as `service_role` — the same role the resolver reads it as. The resolver's own reads of `pending_claims`, and its parity test, pay the same cost on the same data.
- **The population is small.** 859 live pending claims, 1705 provenance rows, 2567 observations. A classification view that cannot answer over 859 rows in 8 seconds will not survive the catalog growing.

## The Admin-side ruling: there is no mitigation to build, and none may be built

I looked for an honest faster read and measured five candidates for one: a narrowed select list, a per-bucket `count: "exact"` head query instead of a complete read, an id-restricted `.in()` read (the shape `readPendingClaims` already uses for the gauges), a bucket-filtered list, and a smaller range. **All five time out.** So:

- **No mitigation TASK is filed**, and none should be invented later. `src/lib/db/claims.ts` and `src/lib/db/gauges.ts` are correct as written; `listClaims`' complete read is the right kind for a page that claims exact bucket counts (§4.3), and narrowing it would trade a true refusal for a slow one.
- **`/claims` and the Sources awaiting-row gauge render their error state**, naming `pending_claims` and the database's own words. That is the correct rendering of a database that will not answer, and it stays until this lands.
- **No workaround code** (spec §10, ARCHITECTURE §6 trap 12): no cache, no swallowed timeout, no surface hidden because it is red.
- **Admin never re-computes the buckets** from `observations` + `field_provenance` + `review_items`. That would be a second copy of the resolver's precedence rules living in this repo; the bucket of a claim is the ledger's answer or it is nothing.

## Done looks like

On staging, `select observation_id, bucket from public.pending_claims limit 10` returns, and so does the Claims page's own shape — all six columns, `neq bucket in_window`, `order bucket, observation_id`, `range(0,999)`, `count: "exact"` — both inside the 8s timeout. Then `tests/live/claims.live.test.ts` and the Sources awaiting-row assertions go green **with no change to Admin**, which is the point: if an Admin change turns out to be needed, this diagnosis was wrong and the ticket comes back here.

## Acceptance criteria
(human-checkable) Ben (or the scraper campaign) runs the EXPLAIN, applies candidate A and/or B in the scraper repo, and pushes it to staging. Then: the view answers a limit-10 read and the Claims page's own query shape inside the 8s statement timeout, with no change to Admin. The architect confirms by re-measuring, removes the OPEN-CLAIMS-COST marker from ARCHITECTURE's open-questions list, and re-runs the claims and sources live tests. Until then /claims and the Sources awaiting-row gauge stay in their honest error state and no Admin-side workaround is written.

## Checks
```sh
bash -c "! grep -q OPEN-CLAIMS-COST agenticflow/docs/vision/ARCHITECTURE.md"
```

## History
- 2026-09-02T16:37:57Z [architect] opened: ASK BEN: pending_claims view times out on staging (handoff: index or view rewrite in the scraper repo)
- 2026-09-02T16:38:04Z [architect] open → blocked: HANDOFF for Ben: pending_claims cannot be read on staging in any shape but an unordered limit 1 (8 shapes measured, all 57014 at ~8.1s). The fix is a scraper-repo artifact — candidate A is one index on field_provenance (entity_type, entity_id, field, applied_at desc, provenance_id desc), which that table lacks entirely; candidate B is a not-materialized hint on two CTEs. Complete SQL, target path and apply command are on the ticket. Admin-side: I measured five mitigation candidates including per-bucket head counts and an id-restricted read; all five time out, so NO mitigation TASK is filed and none may be invented. /claims and the Sources awaiting-row gauge stay in their honest error state.
- 2026-09-03T04:55:28Z [architect] CONFIRMED on staging, read-only, 2026-09-03. The handoff landed and it works.

**1. What actually landed in the scraper repo** (read-only; that repo is never edited from here). One new migration since my probe:
`supabase/migrations/20260903000001_the_creation_bar_is_read_once_and_the_incumbent_is_one_seek.sql`, Ben-licensed in session as an ordered exception outside the resolver campaign's six-migration license, citing this ticket. It carries two things:
- **Candidate A verbatim** — `create index if not exists field_provenance_current_per_fact on public.field_provenance (entity_type, entity_id, field, applied_at desc, provenance_id desc)`, with the comment I proposed. That table previously held nothing but `field_provenance_pkey`.
- **A better candidate B than mine.** Not `not materialized` on `live_pending_claim`/`reference_entry`: the opposite shape, one word `materialized` on `required_column`. Their EXPLAIN (full read 11.5s) found that `required_column` is referenced ONCE, so the planner inlined it into `record_bar`'s per-record lateral and re-ran the `pg_catalog` join per uncreated record per probe — 6,586 `pg_attribute` scans, ~10s of the 11.5s. The view is otherwise verbatim from 20260901000004. **My inference from the SQL named the right table and the wrong dominant cost**; the step-0 EXPLAIN earned its place on this ticket and the next reader should trust a plan over my reading.

**2. Re-measurement**, my own client through the same staging guard, same shapes as claims-probe/probe2 plus the page's own. Evidence: `agenticflow/tracker/evidence/architect/claims-probe3.mjs` and `claims-probe3.tsv`, redacted the same way (shape name, ms, row count, error code — no values, no host, no credentials), sitting beside the earlier two so the before/after reads as one file set.

| shape | before (2026-09-02) | after (2026-09-03) |
| --- | --- | --- |
| `limit 1` (unordered) | 852–1083 ms, 1 row | **143 ms**, 1 row |
| `limit 2` / `3` / `5` | 8098–8178 ms, 57014 | **131 / 118 / 129 ms**, rows returned |
| **`limit 10`, narrow select** | 8172 ms, 57014 | **132 ms, 10 rows** |
| `order(observation_id) limit 1` | 8107 ms, 57014 | **158 ms**, 1 row |
| `.in(observation_id, 10 ids)` | 8118 ms, 57014 | **130 ms**, 10 rows |
| `.in(observation_id, 100 ids)` (gauge shape) | 8146 ms, 57014 | **254 ms**, 100 rows |
| `head:true count exact` per bucket (5) | 8137–8172 ms each | **205–225 ms each** |
| `head:true count exact`, `neq in_window` | 8138 ms | **209 ms, count 859** |
| `eq(bucket, awaiting_row) limit 10` | 8137 ms, 57014 | **126 ms**, 10 rows |
| **PAGE SHAPE** — six columns, `neq bucket in_window`, `order bucket, observation_id`, `range(0,999)`, `count: "exact"`, 3 runs | timed out | **312 / 281 / 289 ms, 859 rows, count 859** |

Thirteen shapes, thirteen returns, zero `57014`. Both bars this ticket set — the limit-10 read and the page's own shape, inside the 8s timeout — are met with two orders of magnitude of headroom. Population unchanged in kind: `observations` pending 859, `field_provenance` 1708, `observations` 2570. The page shape's `count` (859) equals the base-table pending count exactly, which is the parity signal the Claims page's counts rest on. Bucket census: awaiting_row 751, awaiting_link 108, escalated 0, standing_disagreement 0, agreeing 0, in_window 0.

**3. Live tests — and the one red, which is NOT the page.**
`npm run test:live -- tests/live/claims.live.test.ts tests/live/sources.live.test.ts`: **11 of 12 green, with zero changes to Admin — product code or otherwise.**
- `sources.live.test.ts` — **6/6 green**, including the awaiting-row assertion this ticket named ("counts each source's awaiting-row claims as the view holds them", 4560 ms).
- `claims.live.test.ts` — 5/6. Green: each bucket's count as the view holds it; the same per source filter; the complete read with nothing dropped; the standing tab as exactly that bucket's subset; the parked bucket holding no row in the view.
- Red: "the parked bucket … is a string the database still spells, and the page never does" — `StateMismatchError: 'section:nth-of-type(1)' rendered its EMPTY state, but this test counted 859 row(s) of 'pending_claims', which is OK.`

**This is a defect in the test's arithmetic, not in the page, and it needs no Admin product change.** The test loops four param sets and grades every one with `counted: () => countRows(() => claimCount())` — the whole view, 859 — while `within: listOf(params.tab)` selects, for the two `tab: "standing"` sets, the standing tab's list, whose set is `bucket = 'standing_disagreement'` = **0** on staging (my probe, same session). `gradeSurface` therefore expects `ok` and the page honestly renders EMPTY. Three facts pin the page as correct: its own sibling test four lines above grades the same surface with the right count and passes on `empty`; iteration 1 of the failing loop (buckets tab, `section:nth-of-type(2)`) grades `ok` against 859 and passes its `not.toContain("in_window")` assertion; and 11 of 12 assertions are green. The page's former error state was masking this — it is newly visible, not newly broken.

Filed as **BUG-0037** (scope `tests/live/claims.live.test.ts`, explicitly NOT `src/`), and recorded as ARCHITECTURE Common violations **row 7**: 'a live oracle counting a DIFFERENT set than the surface it grades', count 1 — a near miss of row 6's class. The real cost is not the red: three of the four param sets throw before reaching the `not.toContain("in_window")` assertion the test exists for, so the parked-bucket property is 25% verified. BUG-0037's criteria require all four reached.

**4. Doc changes** (this ticket's touch scope plus one queued residual). ARCHITECTURE.md: the claims-cost marker struck from §12's open-questions list — **the ticket's stored check passes** — leaving that list empty, which the section now says is a legal state; §6 trap 12 rewritten from 'cannot be read today' to what was measured, what fixed it and the re-measurement, with its **two rules kept and explicitly decoupled from the cost** (no workaround code; Admin never re-computes the classification) so neither reads as expired; §4's pure-leaf list corrected to `lib/review/**`, `lib/browse/**`, `lib/claims/**`, `lib/records/**`, `lib/format.ts`, `lib/edit/config.ts` — the queued DEBT-0001 residual (`src/lib/records/routes.ts`, verified importing nothing) plus two stale entries it still carried ('`lib/browse/**` when TASK-0015 lands' after TASK-0015 was done, and `lib/claims/**` never named at all). DECISIONS.md carries the dated paragraph, whose door is the decoupling above, not 'the view is fast now'.

**Nothing in `src/` was touched by anyone for any of this**, which was the entire point of the ruling that no Admin-side mitigation existed.
- 2026-09-03T04:55:54Z [architect] blocked → open: Unblocked: the scraper-repo handoff landed. Migration 20260903000001 applied to staging — candidate A's index on field_provenance verbatim, plus 'materialized' on the view's required_column CTE (a better candidate B than mine; their EXPLAIN found the once-referenced CTE inlined into a per-record lateral, ~10s of an 11.5s read). Reopening to carry the confirmation through the gate.
- 2026-09-03T04:55:55Z [architect] claimed (attempt 1)
- 2026-09-03T04:56:09Z [architect] claimed → built: Confirmed on staging, read-only, no Admin code changed by anyone. LANDED IN THE SCRAPER REPO: 20260903000001_the_creation_bar_is_read_once_and_the_incumbent_is_one_seek.sql — candidate A's index field_provenance_current_per_fact verbatim, plus one word 'materialized' on the view's required_column CTE (their EXPLAIN found it referenced once and inlined into record_bar's per-record lateral: 6,586 pg_attribute scans, ~10s of an 11.5s read — my candidate B named the wrong CTE and the wrong direction). RE-MEASURED, thirteen shapes through the same live guard, evidence claims-probe3.tsv beside claims-probe.tsv/claims-probe2.tsv: every shape returns, zero 57014. limit-10 narrow select 8172ms -> 132ms. The Claims page's own shape (six columns, neq bucket in_window, order bucket then observation_id, range(0,999), count exact) timed out before and now returns all 859 rows with count 859 in 312/281/289ms over three runs — count equals the base-table pending count exactly, which is the parity signal the page's bucket counts rest on. LIVE TESTS: sources.live.test.ts 6/6 green including the awaiting-row assertion this ticket named; claims.live.test.ts 5/6. The one red is not the page: the parked-bucket test grades the STANDING tab (0 standing disagreements on staging) against claimCount() = 859, so the page's honest EMPTY reads as a failure — a test-arithmetic defect, newly visible because the page's former error state was masking it. Filed BUG-0037, scope tests/live/claims.live.test.ts and explicitly not src/; ARCHITECTURE Common violations row 7, count 1. DOCS: the ticket's stored check passes (marker struck from ARCHITECTURE §12, list now empty and said to be legally so); §6 trap 12 rewritten from 'cannot be read today' to what was measured/what fixed it/the re-measurement, with its two rules kept and explicitly decoupled from the cost so neither reads as expired; §4 pure-leaf list corrected for DEBT-0001's lib/records/** plus two stale entries; DECISIONS.md paragraph appended, whose door is that decoupling rather than 'the view is fast now'. QA note: BUG-0037 is a separate ticket by design — this one's bar was the view answering with no Admin change, and it does.
- 2026-09-03T04:57:40Z [qa] built → qa: under attack
