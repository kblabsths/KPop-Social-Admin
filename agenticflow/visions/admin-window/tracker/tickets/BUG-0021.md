---
id: BUG-0021
type: bug
status: built
milestone: M1
priority: P2
opened_by: qa
parent: FEAT-0004
discovered_from: TASK-0011
children: []
assignee: builder-45
claimed_at: 2026-09-02T15:25:58Z
depends_on: []
touch_scope: [src/app/queues/[reviewItemId]/page.tsx, src/lib/db/review-item.ts, tests/offline/review-item]
force: false
attempts: 1
---
# Review-item detail miscounts resolved evidence when an id repeats

## Description
Expected (contracts/admin-observability.md §6 and this page's own contract — 'every observation id in review_items.evidence resolved to its row', and src/lib/db/review-item.ts:334 'the page names them rather than dropping them, because a silently shorter list would read as an item with less evidence than it has'): the accounting sentence under the evidence block accounts for every id the item carries — resolved plus unresolved equals the ids looked at.

Found: with a repeated evidence id the sentence understates and contradicts itself. src/app/queues/[reviewItemId]/page.tsx:441-445 divides the DEDUPLICATED claim count by the RAW array length:
    {count(evidence.data.claims.length)} of {count(row.evidence.length)} evidence ids resolved to a claim
while readItemEvidence (src/lib/db/review-item.ts:364) correctly does distinct(item.evidence) first, so claims and unresolved are both over DISTINCT ids. An item whose evidence is [A, A, B], with A and B both resolving, renders two claim rows, NO unresolved id, and the sentence 'json2 of 3 evidence ids resolved to a claim' — telling the operator one evidence id went unaccounted for while naming none.

Reachable from real data, no malformed row needed: review_items.evidence is 'uuid[] default {} not null' with no uniqueness (kspace Scraper migration 20260901000002), and contracts/resolver.md §11 (line 487) folds by APPENDING to it — 'new evidence folds in: folded_count increments, evidence appends' — with no dedup rule. The builder's own distinct() call is the acknowledgement that duplicates occur.

Repro (offline, no database):
1. cd '/Users/ben-m4/Desktop/Coding/KPOP/kspace Admin'
2. ./node_modules/.bin/vitest run tests/offline/review-item/page.test.ts -t 'accounts for every evidence id when one is repeated'
3. The test is PINNED it.fails (strict) for this ticket, so it is GREEN only while the defect is live. Watched failing as a plain it() against run/admin-window @ 552a243:
   AssertionError: 2 of 3 reported resolved, but no id is listed as unresolved: expected 1 to be +0

Fix: make the two figures come from one accounting. Either report over the distinct ids the read actually looked at, or have readItemEvidence carry the id count it deduplicated to; do not fix it by dropping distinct(), which would render the same claim twice and double-count the evidence. Then flip the pinned it.fails back to a plain it() — the assertion is behavioural (resolved + unresolved must cover the ids the page says it looked at) and pins no wording.

## Acceptance criteria
(to be written)

## Checks
```sh
npm run lint
./node_modules/.bin/tsc --noEmit
./node_modules/.bin/vitest run tests/offline/review-item
```

## History
- 2026-09-02T14:48:11Z [qa] opened: Review-item detail miscounts resolved evidence when an id repeats
- 2026-09-02T15:25:58Z [builder-45] claimed (attempt 1)
- 2026-09-02T15:35:21Z [builder-45] claimed → built: One accounting, both figures: readItemEvidence now carries ids:{stored,distinct} and the page's sentence reads BOTH from it, so claims+unresolved===ids.distinct by construction and every id it calls unaccounted-for is named below it. A repeat is stated, not hidden: '2 of 2 evidence ids resolved to a claim, in the order they folded in. It stores 3 ids in all: a claim that folded in again is appended, and counts once here.' (rendered markup, [A,A,B]); mixed case renders '1 of 2 ... It stores 4 ids in all' with the one missing id named once, not twice. QA's it.fails is a plain it() again (watched flip: 'Expect test to fail' the moment the divide was fixed).
QA's 2nd finding, PARTIALLY taken and deliberately so. sources now DEGRADES instead of blanking: it only LABELS a claim, so the read falls back to the source id verbatim with no tier (identical to an absent registry row, already its documented behaviour) and carries the refusal in ItemEvidence.sourcesUnavailable, which the page renders beside the evidence — the pending_claims pattern. observations and field_provenance still refuse the WHOLE read, on purpose: reporting them beside would need a 5th CanonicalSide arm ('the log refused') plus rewriting read.test.ts's landed 'refuses rather than naming the latest decision out of a truncated log' assertion — a contract change on the canonical side, which is the ruling QA asked for. Still open therefore: a source-pattern item that also names a fact loses its list+dial to a field_provenance refusal.
Verified on the rebased tree (onto run/admin-window @ 33c57d7): npm run lint clean, tsc --noEmit clean, vitest run tests/offline/review-item = 48 pass, npm run build green (/queues/[reviewItemId] still f), full npm test = 1152 pass. Traps for QA: (1) the accounting regex now reads N-of-DISTINCT, and a second sentence appears only when stored!==distinct; (2) the sources-refusal path renders claims AND a role=alert naming sources — the older 'database's own words' test still passes because it never asserted the blanking; (3) touch scope only, no component changed (shape views already accepted canonical:null); (4) not walked: no service-role key in .env, screenshots still owed by the walk agent.
- 2026-09-02T15:35:39Z [dispatcher] landed ticket/BUG-0021 onto run/admin-window as e09dff0
