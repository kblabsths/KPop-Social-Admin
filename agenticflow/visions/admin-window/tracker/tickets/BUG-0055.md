---
id: BUG-0055
type: bug
status: built
milestone: M1
priority: P3
opened_by: designer
parent: null
discovered_from: M1-endgame-user-sim
children: []
assignee: builder-103
claimed_at: 2026-09-04T04:46:09Z
depends_on: []
touch_scope: [src/app/cycles/page.tsx, src/lib/gauges/cycle-health.ts, tests/offline/cycles/page.test.ts, tests/offline/gauges/cycle-health.test.ts]
force: false
siblings: [BUG-0054]
attempts: 1
---
# Cycles & runs names one state two ways on one screen: the rows say 'died', the health panel says 'unfinished'

## Description
Found by the document-blind user-sim walk (Tomas, 2026-09-03), `agenticflow/tracker/evidence/user-sim-m1/tomas.md`, Claim 2. Confirmed live on :8771 and at source, 2026-09-03.

**Voice glossary:** "one name per concept, everywhere" — "two builders who never meet must write the same labels."

On `/cycles`, the same four cycles are named twice, two different ways, on one screen:

- the table rows call them **`died`** — `<Badge tone="broken">died</Badge>` (`src/app/cycles/page.tsx:261-264`), from `cycleState()`'s `{ kind: "died" }` (`src/lib/db/cycles.ts:223, 261`);
- the cycle-health panel below calls them **`unfinished`** — `outcomeRows()`'s fourth known key (`src/app/cycles/page.tsx:478`), from `cycle-health.ts:134,156` where a null outcome is bucketed as `unfinished`.

Confirmed in the delivered HTML of `/cycles`: `data-cycle-state="died"` on the rows, `data-outcome-count="unfinished"` reading `4` in the panel. Same four cycles, same page, two words.

Tomas: "The panel calls four cycles **unfinished**; the rows themselves call them **died**. Same four cycles, two different words on the same screen. **I had to satisfy myself they were the same set before I'd trust the count.**"

That cost is the whole point of the glossary rule. This page's argument is that its summaries are computed from its rows — Tomas re-added all 69 rows by hand and found not one cell off, and called it "the strongest evidence in the tool that the summaries are computed from the rows and not from somewhere else". Naming the same set two ways is the one thing that made him doubt an arithmetic he had just verified.

Neither word is a machine identifier the database wrote: `resolution_runs.outcome` is NULL for these rows (`cycles.ts:214` — "`died` — no `ended_at`, and the cycle started longer ago than that"). Both words are the app's own, invented at two different call sites. So Voice bar 5 (machine identifiers verbatim in mono) does not protect either spelling — the app is free to pick one, and must.

## Acceptance criteria
`/cycles` names the no-end-past-one-cadence state with ONE word, used by both the table rows and the cycle-health panel. Which word is the build's call (`died` is the more specific and is what the row badge already carries), but the two must agree, verbatim, on the same screen.

The other three outcome words are untouched: `succeeded`, `failed`, `skipped` are the database's own `outcome` values and keep rendering verbatim (Voice bar 5).

The still-running state (no end, younger than one cadence) keeps its own distinct word — three states, three words, no two sharing a spelling and no state losing one.

The counts do not change: the panel still reads 4 for that state and the table still shows the same four rows.

`tests/offline/cycles/page.test.ts` and `tests/offline/gauges/cycle-health.test.ts` assert the row badge text and the panel's outcome label are the same string.

Human-check: on `/cycles`, read the badge on a no-end row and the fourth line of the outcome panel — same word. (human-checkable)

## Checks
```sh
npx vitest run --project=offline tests/offline/cycles/page.test.ts tests/offline/gauges/cycle-health.test.ts tests/offline/cycles/read.test.ts
./node_modules/.bin/tsc --noEmit
npm run lint
```

## History
- 2026-09-03T22:27:28Z [designer] opened: Cycles & runs names one state two ways on one screen: the rows say 'died', the health panel says 'unfinished'
- 2026-09-03T22:27:34Z [designer] Sibling note vs BUG-0054 (same file, same page): 0054 changes ONE row's visual rendering and the sentence above the table (a selection affordance); this changes ONE word in the health panel's outcome list so it matches the badge the rows already carry. Different hunks — 0054 is in cycleColumns()/AskedCycle (lines ~290-440), this is outcomeRows() at line 478 and cycle-health.ts's bucket key. Neither reads the other's code. BUG-0040 (claimed) also lands here and moves section ORDER only.
- 2026-09-04T04:46:09Z [builder-103] claimed (attempt 1)
- 2026-09-04T04:57:43Z [builder-103] claimed → built: One word per cycle state on /cycles. `cycle-health.ts` no longer lumps every null-outcome row under the invented word `unfinished`: it buckets by `cycleState()` (the same function the rows use), so `running`, `died` and `unrecorded` are counted apart under the new exported `CYCLE_OUTCOME_KEYS`, and `page.tsx` reads every state's word from one `STATE_WORD` map used by BOTH the row's outcome cell and the panel's outcome list. NB: a straight rename of the panel's word would have been WRONG — the one bucket also held running and ended-no-outcome rows, so `died` would have counted a cycle still going. `unrecorded` keeps its absence dash on both surfaces via the shared `orDash`. `CyclesPage` now passes its one render clock to `readCycleHealth({ now })` so table and panel cannot land a row on opposite sides of the cadence boundary.

Verified on the rebased tree (onto run/admin-window, commit 9432c4b): `vitest run --project=offline tests/offline/cycles tests/offline/gauges tests/offline/gauges-ui tests/offline/dashboard` 354 passed; full `npm test` 1896+6 passed; `tsc --noEmit` clean; `npm run lint` exit 0; `npm run test:live -- tests/live/cycles.live.test.ts tests/live/runs.live.test.ts tests/live/dashboard.live.test.ts` 22 passed. Red-then-green observed: the two new page tests and three new gauge tests fail on HEAD's pre-fix source ("running row says \"still running\": expected false to be true").

Real render, staging, dev server on port 8781 from a detached worktree (killed; port free): /cycles delivers 69 cycle rows — 64 `succeeded`, 1 `skipped`, 4 `died` — and the panel reads succeeded 64, failed 0, skipped 1, still running 0, died 4, — 0. Sum 69 = rendered rows. The line reading 4 is labelled `died`, the same word as the four row badges; the string `unfinished` occurs 0 times in the delivered HTML. QA traps: (1) the new page test compares row text to panel text read from the SAME markup, never to a literal, so renaming `died` will not redden it — that is deliberate (property, not copy); (2) the `unrecorded` panel row's label is the em dash, matching the table cell, not a word; (3) an `outcome` value spelled `running`/`died`/`unrecorded` would merge with a state bucket — impossible under today's check constraint, flagged in a comment in cycle-health.ts. Touch scope respected exactly (4 files). Unrelated, not filed: /cycles also renders 'KSPACE_BOT_USER_AGENT is not set' from another panel.
- 2026-09-04T04:57:59Z [dispatcher] landed ticket/BUG-0055 onto run/admin-window as 59a3e9a
