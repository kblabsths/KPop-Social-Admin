---
id: BUG-0165
type: bug
status: qa
milestone: patch
priority: P1
opened_by: qa
parent: null
discovered_from: TASK-0061
children: []
assignee: builder-245
claimed_at: 2026-09-10T22:21:48Z
depends_on: []
touch_scope: [README.md, tests/offline/shell/shell.test.ts]
force: false
attempts: 1
---
# npm test is red in the primary checkout: the README env guard reads an untracked .env.example

## Description
Landed by TASK-0061 at 0f06d3b (tests/offline/shell/shell.test.ts, case "carries a name from .env.example for every variable it documents, and no value").

REPRO (primary checkout, no edits needed — this is the tree as it stands):
  cd '/Users/ben-m4/Desktop/Coding/KPOP/kspace Admin'
  ./node_modules/.bin/vitest run --project=offline

Expected: exit 0 (AGENTS.md: green before pushing; run.yaml ci_command ends in 'npm test').
Found:    exit 1 — 'Test Files 1 failed | 72 passed (73); Tests 1 failed | 3388 passed (3389)', the single failure being
          tests/offline/shell/shell.test.ts:192
          'AssertionError: SUPABASE_URL is not declared in .env.example: expected [ STAGING_SUPABASE_URL, …(6) ] to include SUPABASE_URL'.

CAUSE, measured. The new assertion builds its 'declared' set by reading .env.example OFF DISK at
tests/offline/shell/shell.test.ts:184, and forces every backticked NAME in README.md that starts with
SUPABASE or AUTH to appear in it. The primary checkout's .env.example has not declared SUPABASE_URL or
SUPABASE_SERVICE_ROLE_KEY since 2026-09-03 (file mtime 2026-09-03T13:51:53Z; the edit is uncommitted and
carries the comment 'Production Supabase variables should only exist in the production Railway environment
variables'). HEAD's committed copy still declares both. So the test's verdict depends on uncommitted state:
green in any git worktree (which checks out HEAD — which is where TASK-0061's builder measured 'full npm test
exit 0'), red in the primary checkout, which is where Ben and ci_check run.

Proof of the dependency, replicating the assertion against both versions
(agenticflow/tracker/evidence/TASK-0061/repro.mjs, reads only):
  working tree  .env.example: documented=8 missing=["SUPABASE_URL","SUPABASE_SERVICE_ROLE_KEY"] -> FAIL
  committed HEAD .env.example: documented=8 missing=[]                                            -> PASS

THE PROSE HALF, same root. README.md:49 says '.env.example carries the names, and only the names — values
live in a gitignored .env locally and in the Railway service in production', and README.md:54-55 lists
SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY under it. agenticflow/docs/STACK.md §5 says the opposite for that
pair: '.env no longer carries SUPABASE_URL / SUPABASE_SERVICE_ROLE_KEY at all; it carries the STAGING_* pair …
production values live only in Railway's production environment (Ben's ruling, 2026-09-03, recorded in
agenticflow/docs/DECISIONS.md)'. The app reads those two names from its process environment, mapped there from
the STAGING_* pair by the launch line. So the front door still tells a new reader a local-.env story the
campaign retired — the same doc-drift class TASK-0061 exists to kill, and the .env.example on disk already
reflects the ruling. Expected: README's env section matches STACK.md §5 / DECISIONS.md 2026-09-03. Found:
README.md:49-55 describes the pre-ruling arrangement.

FIX DIRECTION (builder's call, both halves are one edit): align README.md:47-61 with STACK.md §5 — name the
two the app reads and say they come from the launch-line mapping locally and from Railway in production,
while .env.example carries the STAGING_* pair — and make the guard's 'declared' set no longer decide the
suite's colour from an untracked file (assert against the committed .env.example, or drop the
declared-in-.env.example half and keep the 'no value is printed beside a name' half, which is the assertion
with teeth). Do not touch .env.example itself: the uncommitted edit is Ben's and it is the correct direction.

Everything else in TASK-0061 verified green and is not in scope here: both stored checks pass, all 21 paths
and 6 nav routes the README names exist on disk, and the other 6 new guard cases pass.

## Acceptance criteria
npm test exits 0 in the primary checkout /Users/ben-m4/Desktop/Coding/KPOP/kspace Admin with .env.example in its current on-disk state, and the README's environment section agrees with STACK.md §5 about where the SUPABASE_URL / SUPABASE_SERVICE_ROLE_KEY values live (not a local .env). The guard's verdict does not depend on whether .env.example has uncommitted edits: it passes against both the committed and the on-disk version, or it stops reading that file for this assertion.

## Checks
```sh
./node_modules/.bin/vitest run --project=offline tests/offline/shell/shell.test.ts
! grep -nE "/scrapers|/review|\.env\.local|:3000" README.md
```

## History
- 2026-09-10T22:19:44Z [qa] opened: npm test is red in the primary checkout: the README env guard reads an untracked .env.example
- 2026-09-10T22:21:48Z [builder-245] claimed (attempt 1)
- 2026-09-10T22:29:45Z [builder-245] claimed → built: Guard no longer reads any env file; README env section rewritten to match STACK.md §5 / DECISIONS.md 2026-09-03. .env.example and .env were not touched.
- tests/offline/shell/shell.test.ts: the '.env.example declares it' half is replaced by one derived from tracked source — every name src/ reads via process.env (walked with the shared ../source-tree helper, comment-stripped) must be documented in README. The teeth half (no value beside a name) stays; a third case, 'grades itself from tracked files alone', scans this file's own readFileSync calls and fails if any names an env file, with an in-test probe proving the scan finds the read that was removed.
- README.md env section split in two: the two names the app itself reads (src/lib/db/client.ts, src/lib/supabase.ts) come from Railway in production and from the launch-line mapping locally — in NO file — and the rest (STAGING_*, AUTH_*) are what a machine carries in a gitignored .env. That also clears TASK-0061's reopen reason; TASK-0061 needs nothing further, its re-prove is zero-diff (both its stored checks re-run green here).
- Measured on the rebased tree (onto c5ab8a4): npm test exit 0 (3386 offline + 6 isolated), lint 0, tsc 0, grep check exit 1. Content-independence measured rather than argued: a throwaway detached worktree of this branch (evidence/BUG-0165/probe, since removed) was given a FABRICATED names-only .env.example declaring none of the README's names — the state that reddened the old guard — and the whole offline project exited 0 there, 3386 passed. The primary checkout's only source divergence from HEAD is that same file (git status: .env.example + spawn_log.tsv), so it goes green when this lands.
- For QA: worktrees have no node_modules; I symlinked the primary's to run vitest and removed it before handoff (tree is clean). The path-existence case still requires .env.example to EXIST — that grades the README's claim, not the file's content, and it is a tracked file present in every checkout.
- 2026-09-10T22:30:08Z [dispatcher] landed ticket/BUG-0165 onto run/admin-window as 19e8a4a
- 2026-09-10T22:30:37Z [qa] built → qa: under attack
