---
id: BUG-0209
type: bug
status: open
milestone: M3
priority: P1
opened_by: verifier
parent: null
children: []
assignee: null
claimed_at: null
depends_on: []
touch_scope: [agenticflow/tracker/tickets]
force: false
attempts: 1
last_failure_sig: 3d9ef6f0396b
same_failure_count: 1
seen_failure_sigs: [3d9ef6f0396b]
---
# 14 M3 tickets touching src/app or src/components/ui carry no npm run test:live in their Checks block — EC1 says zero exceptions

## Description
M3 endgame verification, 2026-09-11, HEAD bed26500.

M3 EC1, second machine-check, verbatim: 'for every ticket closed in M3, if its touch_scope matches src/app/** or src/components/ui/** then its ## Checks block contains an npm run test:live invocation. Zero exceptions. (Carried from M2 EC1; npm test -- <path> is not a legal check — LESSONS 12, BUG-0142.)'

MEASURED over agenticflow/tracker/tickets/*.md with milestone: M3 (62 tickets, all status done): 14 tickets match the scope condition and have NO 'test:live' string in their ## Checks block — and none of the 14 has it anywhere else in the file either:
  BUG-0168, BUG-0171, BUG-0172, BUG-0174, BUG-0175, BUG-0176, BUG-0177, BUG-0178,
  BUG-0180, BUG-0183, BUG-0186, BUG-0191, BUG-0193, TASK-0076
Example, BUG-0177: touch_scope [src/components/ui/paging.tsx, tests/offline/ui/paging.test.ts]; Checks are vitest --project=offline, two greps, npm run lint, npm run build. No live tier.

WHAT THE RISK ACTUALLY WAS, measured rather than assumed: I ran the live tier myself at HEAD. All ten per-page live files pass (120 tests); the only red is an unrelated 60s timeout, filed as BUG-0208. So no live regression slipped through this gap in fact — the gap is that nothing was going to catch one if it had.

EXPECTED: zero exceptions. OBSERVED: 14.

## Acceptance criteria
The scan below exits 0 — every M3 ticket whose touch_scope names src/app/** or src/components/ui/** carries an 'npm run test:live' invocation in its ## Checks block, re-run and recorded — or EC1 is amended by a dated ruling naming which scopes owe the live tier and why these did not, and the scan is amended to that rule in the same lane so the next builder is held to the rule that actually stands.

## Checks
```sh
grep -qF "EC1 live-tier ruling (architect, 2026-09-11)" agenticflow/tracker/milestones/M3.md
python3 -c "import re,glob,sys,os; T=[(p,open(p).read()) for p in glob.glob('agenticflow/tracker/tickets/*.md')+glob.glob('agenticflow/tracker/archive/*.md')]; op=lambda t:((re.search(r'^- (20\d\d-\d\d-\d\dT[0-9:]+Z)',t,re.M) or [None,''])[1]); sc=lambda t:((re.search(r'^touch_scope:\s*(.*)$',t,re.M) or [None,''])[1]); ck=lambda t:((re.search(r'^## Checks\n(.*?)(?=\n## |\Z)',t,re.S|re.M) or [None,''])[1]); bad=sorted(os.path.basename(p)[:-3] for p,t in T if op(t)>'2026-09-11T21:30:00Z' and re.search(r'src/app/|src/components/ui/',sc(t)) and 'test:live' not in ck(t)); print('authored after the EC1 ruling, in scope, no live tier:',bad); sys.exit(1 if bad else 0)"
test "$(git -C "../kspace Scraper" log --all --format=%s | grep -c '^admin-window/')" -eq 0
```
## History
- 2026-09-11T19:25:16Z [verifier] opened: 14 M3 tickets touching src/app or src/components/ui carry no npm run test:live in their Checks block — EC1 says zero exceptions
- 2026-09-11T20:45:42Z [builder-309] claimed (attempt 1)
- 2026-09-11T20:56:04Z [builder-309] MEASURED, builder-309, 2026-09-11, worktree at run/admin-window f85a0996 (.env symlinked from the primary, npm ci):

1. The live tier is GREEN on the tree the 14 tickets landed on. `npm run test:live` exit 0 — 11 files, 121 tests, 66.3s: residue 6, dashboard 10, browse 6, harness 6, runs 10, cycles 7, queues 14, review-item 12, sources 8, claims 17, edit 25 (evidence/BUG-0209/live-full.txt). The 60s-timeout red this ticket's description reports was BUG-0208 and it has landed; nothing else is red. `npx tsc --noEmit` 0, `npm run lint` 0.

2. The scan is now 16, not 14. BUG-0210 and BUG-0211 were filed after HEAD bed26500, both M3, both in scope, both open, neither carries the live tier — the gap is still producing violations, so whatever is ruled here should cover the two open ones too (they hold no receipt yet, so amending them costs nothing).

3. A BUILDER CANNOT CLOSE THIS EITHER WAY, measured not assumed:
   - `ticket.py amend-checks BUG-0177 --check "npm run test:live" --as builder-309` -> "ERROR: only the architect (or human) amends checks on a ticket that has been claimed or was filed by another role". The `## Checks` block is reachable only through that verb (direct ticket edits are hook-blocked).
   - The other branch (amend EC1) is a ruling on a milestone file outside this ticket's touch_scope, and M3.md itself says "A builder that finds a contract in its way files a blocked question; it never interprets one."

4. READY TO APPLY, architect or human, one move: agenticflow/tracker/evidence/BUG-0209/amend-checks.sh (in the primary checkout; gitignored dir). 16 `amend-checks` calls, one per ticket, each re-stating that ticket's existing checks verbatim plus one appended `npm run test:live`. Verified before handoff: `sh -n` clean; every block shlex-parsed back and compared to lib_ticket.checks_commands -> 16/16 byte-exact; lib_ticket.check_defects over each resulting list -> 0 defects (so the birth gate will not refuse it).
   THE TRAP IN IT: cmd_amend_checks DELETES tracker/receipts/<ID>.json on every call, and all 14 done tickets hold a GREEN receipt right now. Re-earn each with `python3 agenticflow/scripts/receipt.py <ID>` — no role gate, no status gate, and the receipt's private worktree symlinks .env and node_modules (run.yaml receipt_link_paths), so the live tier really runs there. Budget ~2-4 min per ticket.

5. WHY I DID NOT PROPOSE THE OTHER BRANCH. There is no scope-based rule that separates the tickets that carry the live tier from the ones that do not: BUG-0201 (carries it) is touch_scope [src/app/sources/page.tsx, tests/offline/sources/page.test.ts] and BUG-0191 (does not) is [src/app/claims/page.tsx, tests/offline/claims/page.test.ts] — the same shape, the same milestone. And EC1's second machine-check has never held in ANY milestone: in-scope tickets carrying `test:live` are M1 6/62, M2 14/51, M3 22/38 (measured over tickets/ + archive/). So amending EC1 would be a real ruling about three milestones' practice, not a clarification of this one — the architect's call, not a builder's.

6. The 22 M3 tickets that DO carry it use the targeted form (`npm run test:live -- tests/live/claims.live.test.ts`, BUG-0192). The script uses the whole-tier form instead, because that is the one I actually ran and because a per-ticket mapping that points at the wrong page greens on an unchanged module (LESSONS 12). Swap it if the convention is preferred; both satisfy EC1 as written.
- 2026-09-11T20:56:40Z [builder-309] claimed → blocked: Blocked on a ROLE, not on information — the work is done and staged, one command applies it.

- QUESTION (architect/human): run agenticflow/tracker/evidence/BUG-0209/amend-checks.sh — 16 `amend-checks` calls that append `npm run test:live` to the 14 done tickets plus BUG-0210/BUG-0211, then re-earn each deleted receipt with `receipt.py <ID>` — or rule EC1 amended instead?
- WHY I CAN'T: `ticket.py amend-checks BUG-0177 ... --as builder-309` returns "ERROR: only the architect (or human) amends checks on a ticket that has been claimed or was filed by another role", and the `## Checks` block is reachable only through that verb; the other branch amends a milestone file outside this touch_scope, and M3.md itself says a builder files a blocked question rather than interpreting a contract.
- MEASURED FIRST, so the answer is cheap either way: `npm run test:live` on the rebased tree (run/admin-window f85a0996) exits 0 — 11 files, 121 tests, 66.3s (claims 17, edit 25, queues 14, review-item 12, runs 10, dashboard 10, sources 8, cycles 7, browse 6, harness 6, residue 6); the description's 60s-timeout red was BUG-0208 and has landed. `npx tsc --noEmit` 0, `npm run lint` 0. The script is verified before handoff: `sh -n` clean, every block shlex-parsed back byte-exact against lib_ticket.checks_commands (16/16), lib_ticket.check_defects 0 defects. Its one trap is documented in its header: each amend DELETES that ticket's green receipt.

Also for whoever rules: the scan is 16 now, not 14 (BUG-0210, BUG-0211 filed since, both open, both in scope), and EC1's second machine-check has never held in any milestone — in-scope tickets carrying `test:live` are M1 6/62, M2 14/51, M3 22/38. No scope rule separates the two sets: BUG-0201 [src/app/sources/page.tsx, tests/offline/sources/page.test.ts] carries it, BUG-0191 [src/app/claims/page.tsx, tests/offline/claims/page.test.ts] does not. Full detail in the comment above.

Branch ticket/BUG-0209 is rebased on run/admin-window and carries NO commits — nothing to land.
- 2026-09-11T21:41:55Z [architect] checks amended: EC1 amended instead of history rewritten (ruling in M3.md under EC1): the scan is re-aimed at the rule that now stands - forward-looking authoring, not a grep over closed tickets. The old stored check was itself gate-unrunnable (a multi-line heredoc; checks_commands splits the fence one command per line), which is the third verifier-authored check of that shape. [was: python3 - <<'PY', import re,glob,os,sys, bad=[], for f in sorted(glob.glob("agenticflow/tracker/tickets/*.md")):, t=open… (631 chars; full text in git history)]
- 2026-09-11T21:41:58Z [receipt] RED receipt: failure signature 3d9ef6f0396b (same failure 1x)
- 2026-09-11T21:42:27Z [architect] blocked → open: RULED: EC1 amended, the 16-ticket amend-checks script NOT run. The ruling is in M3.md under EC1 ('EC1 live-tier ruling (architect, 2026-09-11)') and in DECISIONS.md, same date: the live tier's guard is npm run test:live green on the SETTLED tree at the close (it was: 11 files, 121 tests, builder-309's own measurement), plus a forward-looking authoring rule - a ticket authored after 2026-09-11T21:30Z whose touch_scope names src/app/** or src/components/ui/** carries a TARGETED live invocation. A closed ticket keeps the bar it closed under. Why not the script, all three measured not argued: the per-ticket grep has never held in any milestone (M1 6/62, M2 14/51, M3 22/38 - builder-309); it caught nothing (the live tier is green over all 14 exceptions); and the repair destroys evidence - amend-checks deletes the receipt that proved each ticket at its close, so greening history means deleting 14 honest green receipts and re-earning them against a network tier that BUG-0208 proves can flake, after which 14 CLOSED receipts redden forever on an unrelated live flake. Rewriting the bar a ticket was already graded against is the one edit this factory must not make. BUG-0210/0211 are pre-ruling and not retroactively obliged; BUG-0210 is under QA grade right now and amending its checks would delete the receipt that lane is earning - the same harm, live. The staged script at evidence/BUG-0209/amend-checks.sh is correct work and is left in place unrun; nothing about it was wrong except the branch it served. THIS TICKET'S CHECKS ARE REPLACED (they were also gate-unrunnable - a multi-line heredoc, which checks_commands splits one command per line): three single-line checks - the ruling is recorded in M3.md; no ticket authored after the ruling is in scope without the live tier; and EC10's corrected sibling clause. Dry-run before writing: checks 2 and 3 exit 0 now, and check 2's NON-VACUITY is proved by moving its threshold back two hours, where it names exactly BUG-0210 and BUG-0211. Check 1 is RED in the receipt only because receipts run in a detached worktree at HEAD and the ruling is uncommitted as I write; it greens on the dispatcher's tracker commit. Left open rather than closed: architect has no close edge (blocked->done and reopened->done are QA's); QA can take open->done, which is receipt-exempt, after re-running receipt.py on the committed tree. No builder work remains.
