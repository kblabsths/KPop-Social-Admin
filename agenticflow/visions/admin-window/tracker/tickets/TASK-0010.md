---
id: TASK-0010
type: task
status: open
milestone: M1
priority: P1
opened_by: architect
parent: FEAT-0004
children: []
assignee: null
claimed_at: null
depends_on: [TASK-0005, TASK-0006, TASK-0007, TASK-0008, TASK-0026]
touch_scope: [src/app/queues/page.tsx, src/components/queues, tests/offline/queues, tests/live/queues.live.test.ts]
force: false
---
# Queues — two queues of equal standing, with exact filters

## Description
Module: `src/app/queues/page.tsx` (+ its presentational components). Authority: spec section 4 (Queues) and section 6; `contracts/resolver.md` section 11; SPEC F4; M1 EC6; acceptance tests 4 and 2; LOOK_AND_FEEL quality bar 2 and 'Key screens — Queues'.

- `review_items` renders as **two queues of equal standing**: the decision queue and the signal queue, **at the same width, the same type scale and the same level of the page**. Neither is nested inside, beside or beneath the other, and neither is styled as the primary inbox (spec section 4 rationale: 'a signal is the machine breaking, a decision is a question about one datum; tuned signals outrank the inbox they'd be filed under').
- **Open items first, ordered severity then age**, settled items browsable. The sort is stated on screen ('open first, severity then age') — LOOK_AND_FEEL quality bar 6.
- **Filters for queue and for shape**, as `searchParams`. Each filter returns **exactly** the matching items — no extras, none missing. Ordering, classification and filtering all come from `lib/review/shapes.ts` and `lib/db/review-items.ts` (TASK-0006); do not re-implement any of it here.
- A row reads as one sentence: what happened (`summary`), how old, how many times folded (`folded_count` as 'asked again xN').
- Each row opens `/queues/<review_item_id>`.
- The **queue-health gauge** (TASK-0007) renders on this page: per queue open count, age distribution, opens vs settles per week, fold rates.

Every page ticket in this milestone carries the same standing rules (ARCHITECTURE sections 4, 5, 7):
- The page function is the **only** async component on the route; every child is a pure synchronous component taking plain props. That is what lets the tests render `await Page(props)` with `react-dom/server`.
- Read only through `src/lib/db/**`; never build a Supabase client in a page; never spell a table name outside `src/lib/db/tables.ts`.
- Render all four states from the `ui` primitives: loading, empty, **not provisioned** (naming the missing table, gray, never a zero), error (the database's own message). A missing backing table must not throw and must not take the rest of the app down.
- Use tokens and primitives, never raw hex, arbitrary type sizes or ad-hoc radii.
- **State lives in the URL**: every filter, sort and tab is a `searchParams` value and survives the back button.
- Voice: buttons are a verb plus their object; ages are relative with the absolute in the title attribute; scheduled times are absolute UTC with the zone stated once in the column header; machine identifiers render verbatim in mono; empty and not-provisioned states name what the surface holds and what fills it.
- **Nothing settles anything in M1.** No verdict action, no settle control, no fixed/won't-fix, no scaffolding toward them.

## Acceptance criteria
[tests 4, 2, 9; EC6] The two queues render at the same width and type scale, neither nested in the other. Offline tests over a fixture population containing all three shapes in both statuses prove that for **every** queue-filter and shape-filter value the rendered id set equals the expected id set, and that the order is open-first, then severity, then age. The sort is stated on screen. A live-marked parity test asserts the rendered queue counts against counts the test issues itself. The not-provisioned and empty states render without throwing. The queue-health gauge renders on the page. No settle control exists anywhere in the markup.

## Checks
```sh
npm run lint
./node_modules/.bin/tsc --noEmit
npm run build
./node_modules/.bin/vitest run tests/offline/queues
bash -c "! grep -rq settle_review_item src"
bash -c "! grep -rl verdicts src | grep -qv '^src/lib/db/tables.ts$'"
```
## History
- 2026-09-02T05:40:05Z [architect] opened: Queues — two queues of equal standing, with exact filters
- 2026-09-02T07:54:52Z [architect] checks amended: same false-RED as TASK-0006, provable on the current tree: '! grep -rqE settle_review_item|verdicts src' exits 1 because TASK-0002's landed src/lib/db/tables.ts carries verdicts: "verdicts" by contract (ARCHITECTURE 4.1, the permanent not_provisioned probe). Split into settle_review_item nowhere in src/, and verdicts nowhere in src/ outside tables.ts — the M2-scaffolding pin is unchanged in force. Other checks verbatim; dry-run 0/0 today. [was: npm run lint, ./node_modules/.bin/tsc --noEmit, npm run build, ./node_modules/.bin/vitest run tests/offline/queues, bash… (171 chars; full text in git history)]
- 2026-09-02T07:56:06Z [architect] Test-idiom ruling (DECISIONS.md 2026-09-02): the offline include glob stays `tests/offline/**/*.test.ts` — no `.tsx`, no JSX, no jsdom. Component tests build elements with `createElement` and assert emitted markup. Import the landed shared helper `tests/offline/ui/markup.ts` (`h` = createElement, `render` = renderToStaticMarkup, `classesOf`, `tagsOf`, `textOf`) instead of writing your own render helper; see tests/offline/ui/primitives.test.ts for the pattern. This adds no work and no scope — it names the helper you reuse.
- 2026-09-02T08:23:10Z [architect] depends_on: [TASK-0005, TASK-0006, TASK-0007, TASK-0008] → [TASK-0005, TASK-0006, TASK-0007, TASK-0008, TASK-0026]: Adds TASK-0026 (complete reads). Every lib/db list read in this milestone goes through the one readComplete helper — an ok row set is the whole matching set or the read refuses. Without the edge, eight builders who cannot see each other's code each invent a bound; see ARCHITECTURE.md section 4.3.
- 2026-09-02T08:23:40Z [architect] Architect ruling, 2026-09-02 — the queue list's bound, so you do not decide it here.

QA found on TASK-0006 that `listReviewItems` had no `.range()`, no `.limit()` and no `.order()`: against PostgREST's `db-max-rows` (Supabase default 1000) the row set truncates silently in unspecified order. This page is its first caller, so the exactness this ticket promises — "each filter returns EXACTLY the matching items, no extras, none missing" — rested on an unbounded read.

The fix is NOT yours and is not on this page. TASK-0026 (now a dependency) puts it in one helper, `readComplete` in `src/lib/db/result.ts`, and migrates `src/lib/db/review-items.ts` to it: `{ count: "exact" }`, a total server-side order ending in `review_item_id`, `.range(0, ROW_CAP - 1)`, and — the point — `kind: "error"` naming the object, the exact count and the cap whenever the count exceeds the rows returned. **An `ok` array from `listReviewItems` is therefore the whole matching set, always.** A partial answer can never reach this page as an `ok`.

What that means for you, concretely:

- `listReviewItems`' signature is unchanged (`Promise<DbResult<ReviewItemRow[]>>`). Code against TASK-0006's contract exactly as the description says.
- **Render no paging UI and no "showing N of M" line.** There is nothing to page: an `ok` set is complete, and the over-cap case arrives as the error state you already render (the database-refusal line, in mono, per LOOK_AND_FEEL). Nothing in the spec asks for paging and adding it would be scope you were not given.
- **Do not add your own `.limit()`, `.range()`, `.order()` or slice anywhere on this page**, and do not re-sort: `queueOrder` in `src/lib/review/shapes.ts` stays the only display-order authority (the server-side order exists to make truncation deterministic, not to decide what you show).
- The queue-health gauge on this page is a **window read** and a different contract (ARCHITECTURE section 4.3 kind 2, section 8): it aggregates a bounded, ordered window and its card says so. Do not "fix" it to look like the list read, and do not present a window aggregate as a total.
