---
id: TASK-0010
type: task
status: open
milestone: M1
priority: P1
opened_by: architect
parent: FEAT-0004
children: []
assignee: null
claimed_at: null
depends_on: [TASK-0005, TASK-0006, TASK-0007, TASK-0008, TASK-0026, TASK-0030, BUG-0020]
touch_scope: [src/app/queues/page.tsx, src/components/queues, tests/offline/queues, tests/live/queues.live.test.ts]
force: false
---
# Queues — two queues of equal standing, with exact filters

## Description
Module: `src/app/queues/page.tsx` (+ its presentational components). Authority: spec section 4 (Queues) and section 6; `contracts/resolver.md` section 11; SPEC F4; M1 EC6; acceptance tests 4 and 2; LOOK_AND_FEEL quality bar 2 and 'Key screens — Queues'.

- `review_items` renders as **two queues of equal standing**: the decision queue and the signal queue, **at the same width, the same type scale and the same level of the page**. Neither is nested inside, beside or beneath the other, and neither is styled as the primary inbox (spec section 4 rationale: 'a signal is the machine breaking, a decision is a question about one datum; tuned signals outrank the inbox they'd be filed under').
- **Open items first, ordered severity then age**, settled items browsable. The sort is stated on screen ('open first, severity then age') — LOOK_AND_FEEL quality bar 6.
- **Filters for queue and for shape**, as `searchParams`. Each filter returns **exactly** the matching items — no extras, none missing. Ordering, classification and filtering all come from `lib/review/shapes.ts` and `lib/db/review-items.ts` (TASK-0006); do not re-implement any of it here.
- A row reads as one sentence: what happened (`summary`), how old, how many times folded (`folded_count` as 'asked again xN').
- Each row opens `/queues/<review_item_id>`.
- The **queue-health gauge** (TASK-0007) renders on this page: per queue open count, age distribution, opens vs settles per week, fold rates.

Every page ticket in this milestone carries the same standing rules (ARCHITECTURE sections 4, 5, 7):
- The page function is the **only** async component on the route; every child is a pure synchronous component taking plain props. That is what lets the tests render `await Page(props)` with `react-dom/server`.
- Read only through `src/lib/db/**`; never build a Supabase client in a page; never spell a table name outside `src/lib/db/tables.ts`.
- Render all four states from the `ui` primitives: loading, empty, **not provisioned** (naming the missing table, gray, never a zero), error (the database's own message). A missing backing table must not throw and must not take the rest of the app down.
- Use tokens and primitives, never raw hex, arbitrary type sizes or ad-hoc radii.
- **State lives in the URL**: every filter, sort and tab is a `searchParams` value and survives the back button.
- Voice: buttons are a verb plus their object; ages are relative with the absolute in the title attribute; scheduled times are absolute UTC with the zone stated once in the column header; machine identifiers render verbatim in mono; empty and not-provisioned states name what the surface holds and what fills it.
- **Nothing settles anything in M1.** No verdict action, no settle control, no fixed/won't-fix, no scaffolding toward them.

## Acceptance criteria
[tests 4, 2, 9; EC6] The two queues render at the same width and type scale, neither nested in the other. Offline tests over a fixture population containing all three shapes in both statuses prove that for **every** queue-filter and shape-filter value the rendered id set equals the expected id set, and that the order is open-first, then severity, then age. The sort is stated on screen. A live-marked parity test asserts the rendered queue counts against counts the test issues itself. The not-provisioned and empty states render without throwing. The queue-health gauge renders on the page. No settle control exists anywhere in the markup.

## Checks
```sh
npm run lint
./node_modules/.bin/tsc --noEmit
npm run build
./node_modules/.bin/vitest run tests/offline/queues
./node_modules/.bin/vitest run tests/offline/review/one-place.test.ts tests/offline/edit/config.test.ts
```
## History
- 2026-09-02T12:40:52Z [compacted → agenticflow/tracker/notes/TASK-0010.md]
  Summary (state; attempts; DEAD ENDS preserved):
  Compacted 2026-09-02 by compactor; raw History preserved at agenticflow/tracker/notes/TASK-0010.md.
  
  **State.** Open, unclaimed, never built — no page, component or test exists yet for src/app/queues/page.tsx. Description and acceptance criteria above are unchanged and binding. Blocked on TASK-0005, TASK-0006, TASK-0007, TASK-0008, TASK-0026 (complete reads), TASK-0030 (typed state contract) and BUG-0020. BUG-0020 gates because this ticket's checks now RUN tests/offline/review/one-place.test.ts, whose current predicate reddens on any declaration named settle*/verdict* — and this page renders "settled items browsable"; BUG-0020 narrows that over-matching predicate first, so legitimate `settled` naming cannot redden it (`settled` is a `review_items.status` value, not a banned word). Checks were amended twice (2026-09-02 07:54 and 12:34); the Checks block above is the current authoritative set, and its two guard suites were dry-run together that day: 27 tests, 0.4s, green.
  
  **Tried.** (a) M2-scaffolding absence pinned three ways in turn — one combined grep, then two split greps, then the two guard suites; both grep forms were measured failing on landed correct code (DEAD ENDS 1-2). (b) TASK-0026 added as a dependency after QA on TASK-0006 found the queue list read unbounded; the bound went into one shared helper, not onto this page. (c) Test idiom ruled on before any component test was written. (d) The four data-surface states made a typed contract, TASK-0030, so four parallel builders write against one shape instead of four merges fixing it. Rulings (b)-(d) verbatim below.
  
  BINDING RULINGS — code against these, do not work around them.
  
  *Read kind (architect, 2026-09-02; TASK-0026, ARCHITECTURE 4.3).* QA found on TASK-0006 that `listReviewItems` had no `.range()`, no `.limit()` and no `.order()`: against PostgREST's `db-max-rows` (Supabase default 1000) the row set truncates silently in unspecified order, and this page is its first caller, so the exactness this ticket promises — "each filter returns EXACTLY the matching items, no extras, none missing" — rested on an unbounded read. The fix is NOT yours and is not on this page. TASK-0026 puts it in one helper, `readComplete` in `src/lib/db/result.ts`, and migrates `src/lib/db/review-items.ts` to it: `{ count: "exact" }`, a total server-side order ending in `review_item_id`, `.range(0, ROW_CAP - 1)`, and — the point — `kind: "error"` naming the object, the exact count and the cap whenever the count exceeds the rows returned. **An `ok` array from `listReviewItems` is therefore the whole matching set, always.** A partial answer can never reach this page as an `ok`. Concretely: `listReviewItems`' signature is unchanged (`Promise<DbResult<ReviewItemRow[]>>`) — code against TASK-0006's contract exactly as the description says. **Render no paging UI and no "showing N of M" line** (an `ok` set is complete; the over-cap case arrives as the error state you already render, the database-refusal line in mono per LOOK_AND_FEEL). **Do not add your own `.limit()`, `.range()`, `.order()` or slice anywhere on this page**, and do not re-sort: `queueOrder` in `src/lib/review/shapes.ts` stays the only display-order authority (the server-side order exists to make truncation deterministic, not to decide what you show). The queue-health gauge on this page is a **window read** and a different contract (ARCHITECTURE 4.3 kind 2, section 8): it aggregates a bounded, ordered window and its card says so — do not "fix" it to look like the list read, and do not present a window aggregate as a total.
  
  *Test idiom (architect, DECISIONS.md 2026-09-02).* The offline include glob stays `tests/offline/**/*.test.ts` — no `.tsx`, no JSX, no jsdom. Component tests build elements with `createElement` and assert emitted markup. Import the landed shared helper `tests/offline/ui/markup.ts` (`h` = createElement, `render` = renderToStaticMarkup, `classesOf`, `tagsOf`, `textOf`) instead of writing your own render helper; see tests/offline/ui/primitives.test.ts for the pattern. Adds no work and no scope.
  
  *Four-state contract, TYPED (architect, 2026-09-02; TASK-0030, ARCHITECTURE 7, DECISIONS same date).*
  1. **`reading` is REQUIRED on `ErrorLine` and on `GaugeState`'s error arm.** QA's residual on BUG-0016 was right: an optional prop is a rule TypeScript cannot enforce, and the omission it permits is a red line that names none of the page's reads. `DbResult`'s error arm carries `reading: string` already (`src/lib/db/result.ts:14`), so pass `result.reading` straight through on every error arm you render. Do NOT invent a string, do not pass `""` to satisfy the compiler, and do not trim the message: a transport failure's account runs ~900 chars including stack frames, and whether that is too long is the designer's call at the walk, not yours.
  2. **A state card that replaces a labelled surface keeps its eyebrow.** `Empty` and `NotProvisioned` take an optional `micro` eyebrow; the three gauge components always pass their own `label`, so an unprovisioned gauge still says which knob it is and not just which table is missing. On your page's own `Empty`/`NotProvisioned` the eyebrow is optional — the `Section` heading above it already names the surface — so only pass it where there is no heading.
  3. **A rows surface has no headers-only rendering.** `TrendTable` and `Distribution` take a REQUIRED `empty: { holds, filledBy }` and render it themselves when there are no rows and no other state. **The component owns WHEN, you own THE WORDS.** Write them in the app's voice, naming what the series holds and the one thing that fills it (LOOK_AND_FEEL Voice bar 4) — never "No data". An explicit `state` still wins, so pass your own empty state where the emptiness has a REASON (a filter that matched nothing) rather than being simply no rows yet. For you specifically: the queue-health gauge is a trend surface, so the `empty` words are yours to write — what a week row of this queue holds and what fills it.
  
  **DEAD ENDS.**
  1. *One combined grep,* `! grep -rqE settle_review_item|verdicts src` (the check as opened) — false RED, provable on the tree, same failure as TASK-0006: exits 1 because TASK-0002's landed `src/lib/db/tables.ts` carries `verdicts: "verdicts"` by contract (ARCHITECTURE 4.1, the permanent not_provisioned probe).
  2. *The split pair that replaced it* (settle_review_item nowhere in src/; verdicts nowhere in src outside tables.ts) — also measured FAILING on the 2026-09-02 tree, on landed correct work (ARCHITECTURE Common violations class 4): `! grep -rq settle_review_item src` exits 1 because of the COMMENT at `src/lib/edit/config.ts:43`, which explains that M2's override path goes through that RPC — documentation, not a call; the verdicts pipeline exits 1 because of doc comments in `src/lib/gauges/queue-health.ts` and `src/components/gauges/trend-table.tsx` (both landed, both correct). Lesson: hand-rolled absence greps match prose — do not re-add one. Replaced by the two guard suites that already own these guarantees, on CODE LINES only and in one place each (ARCHITECTURE section 10): `tests/offline/review/one-place.test.ts` pins the M2 close (verdicts under src only as the tables.ts entry) and `tests/offline/edit/config.test.ts` pins the whole repo's write surface (no `.insert`/`.upsert`/`.rpc` anywhere, `.update` only in `lib/db/records.ts`, `settle_review_item` nowhere). The M2-scaffolding pin itself is unchanged in force — nothing settles anything in M1.
  3. Rejected on this page, full reasoning in the rulings above: an unbounded `listReviewItems` read; paging UI, a "showing N of M" line, or a local `.limit`/`.range`/`.order`/slice/re-sort; `.tsx` offline tests, JSX, jsdom or a home-grown render helper; optional `reading`, an invented string, `""` to satisfy the compiler, or a trimmed error message (BUG-0016's defect); headers-only `TrendTable`/`Distribution` rendering for `rows: []` with no state.
