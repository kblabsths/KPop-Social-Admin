---
id: BUG-0054
type: bug
status: done
milestone: M1
priority: P2
opened_by: designer
parent: null
discovered_from: M1-endgame-user-sim
children: []
assignee: null
claimed_at: null
depends_on: []
touch_scope: [src/app/cycles/page.tsx, src/components/ui/data-table.tsx, tests/offline/cycles/page.test.ts]
force: false
siblings: [BUG-0055]
attempts: 1
resolution: fixed
---
# Cycles & runs: 'Cycle <id> is marked in the table below' — the row carries only aria-current and is visually identical to all 68 others

## Description
Found by the document-blind user-sim walk (Tomas, 2026-09-03), `agenticflow/tracker/evidence/user-sim-m1/tomas.md`, "Two small things that cost the tool nothing to fix". Confirmed live on :8771 and at source, 2026-09-03.

**Feel bar 10:** "The investigation never leaves the app: from any review item you can reach its claims ... each in **one click**, each a real URL."

Clicking a cycle on the Dashboard lands on `/cycles?cycle=<id>` and the page prints:

    Cycle 01a065c6-965b-7062-b13e-0930d2a5c3e8 is marked in the table below.

**It is not marked.** Measured in the delivered HTML of `/cycles?cycle=01a065c6-965b-7062-b13e-0930d2a5c3e8`, the asked-for row is byte-identical to its neighbours except for one attribute:

    <tr class="border-t border-hairline transition-colors hover:bg-chrome">
      <td class="type-data px-2 py-1.5 text-ink">
        <span id="cycle-01a065c6-..." data-cycle="01a065c6-..."
              data-cycle-state="died" aria-current="true">01a065c6-...</span>

`aria-current="true"` (`src/app/cycles/page.tsx:310`) has no style bound to it anywhere. Same row background, same text colour, same weight, no glyph. Nothing on screen distinguishes it from the other 68 rows.

The page also does not move to it. The id in the sentence IS an anchor (`href="#cycle-<id>"`, line 432) but it is styled `type-data text-ink hover:text-accent` — at rest it is indistinguishable from the mono ids printed as plain text everywhere else on the page, so it does not read as the thing to click. Tomas did not find it: "I ended up scanning 36-character UUIDs by eye to find it."

**Why this one matters more than its size.** Tomas's whole walk was an attempt to falsify the tool's numbers, and this is the one claim he could falsify: "This is the tool making a claim about *itself* that I could disprove in thirty seconds — a bad look on a page whose selling point is that its claims check out." Everything else on Cycles & runs held under hand-summation (he re-added all 69 rows against the health panel and found not one cell off). This sentence spends that credit.

Note for the builder: LOOK_AND_FEEL.md's data-table rule defines hover (chrome fill) but no *marked-row* rendering, so the visual is a design gap as well as a build one — see `agenticflow/tracker/evidence/user-sim-m1/DESIGNER-JUDGEMENT.md`, which proposes the rule. Until it lands, the marked row uses the palette's accent for selection ("Accent — selection, active nav/tab/chip, focus"): a chrome-inverse row fill, or an accent left rule, and NOT a new colour, a shadow, or motion (the motion budget is closed).

Evidence: `agenticflow/tracker/evidence/user-sim-m1/tomas-12-cycle-deeplink-not-marked.png`.

Sibling note: BUG-0040 (newest adapter run below the fold) and this one both land in `src/app/cycles/page.tsx`; 0040 moves a section's position on the page, this changes one row's rendering and one sentence, and neither touches the other's hunk.

## Acceptance criteria
On `/cycles?cycle=<id>` where the id is in the window, the asked-for row is visibly distinguishable from every other row without hovering, using only the existing palette (accent for selection) and existing type steps — no new colour, no shadow, no animation, no change to the row's height or the table's density.

The page's sentence and the page's behaviour agree. Either the row is marked (and the sentence may keep saying so), or the sentence is rewritten to describe what actually happens; the screen must not claim a mark it does not draw.

`aria-current="true"` stays on the row — the accessible marking is correct today and is not the defect.

The anchor in the sentence reads as a link at rest, not only on hover (the app's other in-page anchor at `src/app/cycles/page.tsx:578` gets the same treatment, so the page does not spell "link" two ways).

`/cycles` with no `?cycle=` parameter is unchanged: no row is marked, and the 69-row table renders exactly as it does today.

`tests/offline/cycles/page.test.ts` asserts the asked-for row's rendered markup carries a distinguishing class or attribute that the other rows do not, and that the unfiltered table carries none.

Human-check: click a cycle from the Dashboard and find that cycle in the table below without reading a single UUID. (human-checkable)

## Checks
```sh
npx vitest run --project=offline tests/offline/cycles/page.test.ts tests/offline/ui/primitives.test.ts tests/offline/ui/tokens.test.ts tests/offline/ui/contrast.test.ts
./node_modules/.bin/tsc --noEmit
npm run lint
```
## History
- 2026-09-03T22:27:04Z [designer] opened: Cycles & runs: 'Cycle <id> is marked in the table below' — the row carries only aria-current and is visually identical to all 68 others
- 2026-09-04T00:46:58Z [builder-85] claimed (attempt 1)
- 2026-09-04T01:03:33Z [builder-85] claimed → built: - **The mark is drawn.** `DataTable` gains `marked?: (row) => boolean`; that row renders with the page fill + a 1px accent left rule and carries `data-row-marked="true"`, and the asked-for cycle id renders in the accent. `aria-current` stays exactly where it was (the span). No height/density change: browser-computed row height 45px marked and unmarked alike.
- **Not chrome-inverse, deliberately** (the designer's proposed rule, DESIGNER-JUDGEMENT §4.6). Measured against this palette: on chrome-inverse in dark, a row's own red `error_summary` is 3.57:1, accent 3.69:1, secondary 3.96:1 — all under quality bar 12, and the Dashboard's deep link is usually to the erroring cycle. `page` is one of the three fills tests/offline/ui/contrast.test.ts already asserts every text job against (accent on page: 5.04:1 light, 7.21:1 dark). If the designer wants the fill changed, it is one class in data-table.tsx.
- **One link spelling for the page.** `IN_PAGE_LINK = "text-accent underline"` (the spelling /not-found and the review header already use) replaces `text-ink hover:text-accent` on the sentence's id, the newest-error id and BUG-0040's three prose anchors — so a link reads as a link at rest and the page does not spell link two ways.

Verified on the rebased tree: `npx vitest run --project=offline tests/offline/cycles/page.test.ts tests/offline/ui` (170), `npm test` (1746 + 6 isolated), `tsc --noEmit`, `npm run lint`, and `npm run test:live -- tests/live/cycles.live.test.ts tests/live/runs.live.test.ts` (14, read-only, no writes). New guards seen red first on the pre-fix source: "a link at rest is spelled 'text-ink type-data', and so is a plain value", and marked-rows 0 != 1.

Real render, my own instance on :8781 against staging (killed; port free): `/cycles?cycle=01a066ab-…` served 69 cycle rows, exactly one `<tr data-row-marked="true">` carrying the asked id and the aria-current span, every other row one identical rendering; computed marked fill #f3f4f6 light / #030712 dark, border-left 1px #9810fa / #c27aff. Crops: agenticflow/tracker/evidence/BUG-0054/marked-{light,dark}.png.

For QA: (1) the page still does not move on `?cycle=` alone — clicking the sentence's link does (scrollY 780; the anchor is on the id span, so ~7px of the 45px row sits above the fold). Making the URL land on the row needs `#cycle-<id>` on the Dashboard's `lineHref` in src/app/page.tsx, outside touch_scope, and four assertions in tests/offline/dashboard/page.test.ts pin the current href. (2) The ticket's check line names tests/offline/cycles/qa-probe.test.ts, which does not exist here; vitest ignores a missing path silently.
- 2026-09-04T01:03:56Z [dispatcher] landed ticket/BUG-0054 onto run/admin-window as f092520
- 2026-09-04T01:04:39Z [qa] built → qa: under attack
- 2026-09-04T01:19:25Z [qa] STORED CHECK IS PARTLY VACUOUS — needs an architect to amend (the gate refuses `amend-checks` from qa on a ticket filed by another role; attempted 2026-09-03).

Expected: every path a check names is graded. Found: check line 1 names `tests/offline/cycles/qa-probe.test.ts`, which does not exist on this tree; vitest resolves the remaining paths and exits 0 without a word about the missing one (measured: the verbatim line runs 7 files / 170 tests, none named qa-probe). The check is not FALSELY green — the two real paths do grade the fix — but one third of the line grades nothing.

Replacement block, ready to apply verbatim:

    npx vitest run --project=offline tests/offline/cycles/page.test.ts tests/offline/ui/primitives.test.ts tests/offline/ui/tokens.test.ts tests/offline/ui/contrast.test.ts
    ./node_modules/.bin/tsc --noEmit
    npm run lint

Every path above verified present. It adds the two files QA extended this round: `primitives.test.ts` (the DataTable surface — unmarked rows byte-identical with and without the prop) and `tokens.test.ts` (whose SAMPLES now include a MARKED DataTable, so the no-shadow / no-animation / palette-only / 1px-border rules bind the new rendering, which they did not before).
- 2026-09-04T01:21:01Z [qa] qa → done: attacked on the landed tree: own :8783 instance (worktree, killed; 8771 untouched), staging data, Chromium both schemes. Criteria driven first.

1. DISTINGUISHABLE / LOOK, computed not read off source. Marked row bg rgb(243,244,246) light, rgb(3,7,18) dark (= page, vs the table's surface fill: 1.10 / 1.13); border-left 1px rgb(152,16,250) / rgb(194,122,255) (= accent, 5.04:1 / 7.21:1 on its own fill); box-shadow none; animation-name none; transition string identical to an unmarked row's.
2. HEIGHT/DENSITY. The asked row is 61px WITH and WITHOUT the facet (tall because its error_summary wraps, not because it is marked); all 69 heights identical across both URLs; table box 3161x1174 both. Sub-pixel residue, recorded not filed: with the facet, every row's content box shifts +0.5px right / -0.5px wide (border-collapse resolving the 1px row border).
3. SENTENCE vs BEHAVIOUR. Exactly one <tr data-row-marked> for a known id; ZERO for unknown uuid, empty ?cycle=, %00, a 4000-char id, ../../etc/passwd, <script>alert(1)</script> (no raw script in the response); exactly one for ?cycle=a&cycle=b either order, on the first value. aria-current="true" still on the id span, count 1, never diverging from the drawn mark.
4. LINK AT REST. Anchor computes accent + underline in both schemes; a plain mono value computes ink, none. Both in-page anchors and BUG-0040's prose links are one constant.
5. "NO ?cycle= UNCHANGED" proved app-wide, not just here: every body row on /, /sources, /cycles, /queues, /claims, /browse and /records/events/<id> carries byte-for-byte the pre-fix class string (git show 1b02727^:...data-table.tsx:128). Zero marked rows anywhere without the facet.
6. HUMAN-CHECK, driven: all SIX Dashboard cycle deep links land the marked row already fully on screen (rowTop 323..548px, viewport 900). One click, no UUID read.

DEPARTURE FROM THE PROPOSED CHROME-INVERSE FILL — the measurement HOLDS, recomputed from globals.css: chrome-inverse dark gives broken 3.57:1, accent 3.69:1, secondary 3.96:1, and light is worse than the builder stated (accent 4.48:1, also under 4.5). On `page`, every text job in the marked row clears the bar in-browser: worst 5.04:1 light (accent id), 5.83 light / 6.97 dark (the row's red error line). Designer note, not a bounce.

RESIDUAL, no ticket (lean): ?cycle=<id> alone still does not scroll — src/app/page.tsx:119 lineHref emits no #cycle-<id>. Reachable from no criterion's path: the Dashboard links only the six newest cycles and all six arrive on screen. It bites a hand-typed link to an old cycle, where the now-visible sentence link gets there in one click (scrollY 0->330, row top clipped 7.5px). Trigger for a ticket: the Dashboard ever linking a cycle deeper than one viewport.

NOT DONE, plainly: no staging write. No criterion touches the save path and /browse exposes only resolver-owned `events` (editable: []); reaching a groups/idols row meant going around the UI to a table this ticket never touches. tests/live/edit.live.test.ts owns it.

SUITES: offline 1760 + 6 isolated, tsc, lint, live cycles+runs 14 against staging — green. Receipt green.

PINS (5bbf35f, tests only): tokens.test.ts SAMPLES now holds a MARKED DataTable — ten Look rules had run against the unmarked rendering only, so the new one bound nothing; primitives.test.ts pins what the other 13 callers rely on (unnamed row byte-identical prop-or-no-prop, a predicate matching nothing renders byte-for-byte as none, no cell moves); cycles/page.test.ts counts the DRAWN mark on facet cases that counted aria-current alone, plus empty and malformed ?cycle=.

STORED CHECK: line 1 named tests/offline/cycles/qa-probe.test.ts, absent here — vitest ignores it silently, so a third of the line graded nothing. Not falsely green (the two real paths do grade the fix). amend-checks is refused from qa; replacement block filed as a ticket comment for the architect. [resolution: fixed] [receipt green: 3 cmd(s), tree e7e20420]
- 2026-09-04T02:49:24Z [architect] checks amended: QA's verified replacement, applied by the architect because amend-checks is refused from qa on another role's ticket. Line 1 named tests/offline/cycles/qa-probe.test.ts, which exists on no tree; vitest resolves the surviving paths and exits 0 without a word, so a third of the line graded nothing. The three files that replace it are the ones QA extended this round — primitives (the DataTable surface), tokens (whose SAMPLES now include a MARKED table, so the Look rules bind the new rendering) and contrast. Measured on run/admin-window today: 4 files / 159 tests, green, 1.85s. The ticket is closed and its receipt was green either way; this makes the stored line honest for the next re-check. [was: npx vitest run --project=offline tests/offline/cycles/page.test.ts tests/offline/cycles/qa-probe.test.ts tests/offline/u… (169 chars; full text in git history)]
