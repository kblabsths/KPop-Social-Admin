---
id: TASK-0073
type: task
status: done
milestone: M3
priority: P2
opened_by: architect
parent: FEAT-0016
children: []
assignee: null
claimed_at: null
depends_on: [TASK-0072]
touch_scope: [src/components/sources/trends.tsx, src/app/sources/page.tsx, src/lib/sources/routes.ts, tests/offline/sources/page.test.ts, tests/offline/sources/read.test.ts, tests/live/sources.live.test.ts]
force: false
attempts: 1
resolution: fixed
---
# /sources' two scan-window lines name the narrowing their read carried

## Description
**Modules:** `src/components/sources/trends.tsx` (the two scan window lines), `src/app/sources/page.tsx` (which resolves the narrowing and hands it down), `src/lib/sources/routes.ts` (where `/sources`' unchipped facet table lands), and their offline + live suites.

**The defect (SPEC F15, M3 EC8).** `/sources`' two scan-window lines — `measured="Claims observed"` and `measured="Claims adjudicated"` in `src/components/sources/trends.tsx` — name NO narrowing, while the read beneath them carries one. `/sources` and `/sources?<facet>=<value>` therefore render byte-identical window lines over two different reads. This is precisely the defect BUG-0163 fixed on `/claims` one page over.

**`/claims` is the model, and it is COPIED BY IMPORT** (LESSONS 5; the whole reason TASK-0072 lands first): the phrase shape, the composition and the subtraction all live in `src/lib/url/narrowing.ts` and `src/components/ui/window-line.tsx` after that ticket. What this ticket writes is `/sources`' own facet table — which facets of this URL narrow which read, in this surface's own words — and the wiring that hands the resulting `scope` to the two lines. **A hand-written narrowing sentence in `trends.tsx` fails this ticket even if it renders the right words.**

**The rule the lines must obey** (ARCHITECTURE §4.3): a window line states the read that HAPPENED. So the scope is unconditional on what came back — a narrowed scan that returned nothing still names its narrowing — and it names the narrowing the READ carried, never the parameters the URL happened to spell. A facet this page DROPPED (`lib/url/dropped-params.ts`) narrows nothing and appears in no scope; a facet the read applied appears in both lines that carried it, and in neither line that did not.

**Both halves of the two-fact rule stay where they are:** this ticket changes what the LINE says (the read's scope), not which empty card the surface draws (DEBT-0008 owns that, and `data-empty` on this page is not this ticket's business).

**The other half of the same read — bounds — landed in TASK-0070.** If a figure on this page still disagrees with the line above it after both, that is a new finding: file it, do not patch it here.

**Live (M3 EC8):** `tests/live/sources.live.test.ts` grades a narrowed URL and an unnarrowed one, decides the state kind from `data-state` before comparing anything, addresses its surface by `data-surface`, and asserts the two window lines are NOT byte-identical between the two URLs when the facet narrows the read.

## Acceptance criteria
Both /sources scan-window lines name the narrowing their read carried, asserted on rendered HTML for an unnarrowed URL and for a narrowed one, and /sources versus /sources?<facet>=<value> do not render byte-identical window lines when the facet narrows the read — while a facet the page dropped changes neither line. A narrowed scan that returned no rows still names its narrowing (the line follows the read, not the rows). The sentence is imported, not retyped: src/components/sources/trends.tsx declares no narrowing phrase of its own and grep shows unchippedPhrase and narrowedTo declared once each in src, both outside this page's files; /sources' facet table is the only new declaration and it lives in src/lib/sources/routes.ts. Which empty card the surface draws is unchanged, no figure moves, no surface or control is added, the sidebar still holds exactly six links, and /sources' live parity suite is green with a narrowed and an unnarrowed URL graded by state kind first.

## Checks
```sh
./node_modules/.bin/vitest run --project=offline tests/offline/sources tests/offline/url tests/offline/absence
./node_modules/.bin/vitest run --project=offline tests/offline/ui tests/offline/shell
test "$(grep -rn "export function unchippedPhrase" src | wc -l)" -eq 1
npm run lint
npm run build
npm run test:live -- tests/live/sources.live.test.ts
```

## History
- 2026-09-10T21:45:54Z [architect] opened: /sources' two scan-window lines name the narrowing their read carried
- 2026-09-11T13:57:26Z [builder-291] claimed (attempt 1)
- 2026-09-11T14:14:55Z [builder-291] claimed → built: DONE: /sources' two scan-window lines now state the narrowing THEIR OWN read carried, by import. New declaration is SOURCES_NARROWING_FACETS in src/lib/sources/routes.ts (this surface's facet table, one facet: source_id, phrase 'from source_id <id>'); shape/composition imported (UnchippedFacet, unchippedNarrowings, unchippedPhrase, narrowedTo) — trends.tsx declares no phrase and takes scope as a prop; page.tsx composes it via scopeOf(<the filter that read was GIVEN>).

TRAP / READ THIS FIRST — the two lines DIFFER, on purpose. readAwaitingRowTrend({filter}) narrows at the query, so 'Claims observed' names it. readRejectionStampGauge() takes NO filter: it scans every source's adjudications and RejectionSection narrows only the ROWS returned — so 'Claims adjudicated' names nothing, under both URLs. The ticket's description assumes both reads carry the narrowing; that premise is wrong for the rejection read (verified in lib/gauges/settled-values.ts). Naming it there would put 'a window of at most 1,000 rows' + the truncation verdict over a population the scan never took = BUG-0114 with halves swapped, and would break 'no figure moves'. Criterion wording covers this: 'in neither line that did not [carry it]'. If the architect wants that read narrowed, that is a new ticket (it moves figures).

Also: RejectionSection's local const scope -> reported (one word, one concept; the prop is now the window line's scope). No figure, empty card, surface or control changed.

VERIFIED on the rebased tree (61bda579 on 8f2f5424): vitest --project=offline tests/offline/{sources,url,absence} 476 pass; tests/offline/{ui,shell} 532 pass; full npm test 3884 pass; grep unchippedPhrase==1; lint 0; build 0; npm run test:live -- tests/live/sources.live.test.ts 8/8.
Red-then-green: with scope={scope} removed, 3 new offline cases AND the new live case fail on the id; restored, green.
Measured on staging (ubfjjqlvnpnoborczbdb) 2026-09-11: bare awaiting surface ok -> 'Claims observed since ... — a window of at most 1,000 rows'; narrowed to 01a01808-8c6f-78aa-b7a7-b07ddb3b057a the surface is EMPTY and the line still reads 'Claims observed from source_id 01a01808-... since ...' (narrowed scan, zero rows, still names its narrowing). Rejections empty under both, line byte-identical.
- 2026-09-11T14:15:15Z [dispatcher] landed ticket/TASK-0073 onto run/admin-window as a13df7d3
- 2026-09-11T14:15:57Z [qa] built → qa: under attack
- 2026-09-11T14:29:16Z [qa] qa → done: ATTACKED on the landed tree (a13df7d3 + receipt re-run in the primary checkout). All six stored checks green in the receipt AND by hand: offline sources/url/absence 476, ui/shell 532, unchippedPhrase==1, lint 0, build 0, live sources 8/8.

Criteria verified myself, before reading the builder's story, driven over HTTP against a PRODUCTION build on staging (ubfjjqlvnpnoborczbdb, port 8781, mine, killed) and then WALKED in a real browser (playwright, six URLs). Line text measured, instants masked:
- bare: "Claims observed since <i>, read to <i> — a window of at most 1,000 rows, not the whole table." (held=877)
- ?source_id=01a01808-8c6f-…057a: "Claims observed FROM SOURCE_ID 01a01808-8c6f-…057a since <i>…" with held=0 — a narrowed scan that returned NOTHING still names its narrowing. Same for 01a01808-8c70-…d0a2 (held=0) and 01a05782-…62f6 (held=877).
- dropped facets name nothing and are byte-identical to bare: ?source_id=nobody, ?source_id= (empty), ?foo=bar.
- ?source_id=00000000-0000-4000-8000-000000000999 (well-formed, no registry row): data-empty="narrowing", line still names the facet.
- spelling attack: ?source_id=01A01808-8C6F-… and the hyphen-less 01a018088c6f… both render the ONE canonical lowercase id in the phrase; ?source_id=a&source_id=b takes the first. No second uuid grammar, no forged segment (source_id cannot carry ", ").
- browser: both lines visible (1056px), sidebar exactly 6 nav links on all six URLs, no surface or control added (3 data-surface: registry, awaiting_row, rejections).
Imported, not retyped: grep — unchippedPhrase 1× (src/lib/url/narrowing.ts), narrowedTo 1× (src/components/ui/window-line.tsx), both outside /sources' files; trends.tsx contains no "from source_id", no narrowedTo, no unchippedPhrase; SOURCES_NARROWING_FACETS is the only new declaration.

THE BUILDER'S PREMISE-CORRECTION IS CORRECT — verified at the read, not taken on trust. readRejectionStampGauge(options: GaugeOptions) (src/lib/gauges/settled-values.ts:249-253) and GaugeOptions (src/lib/gauges/gauge.ts:66-75: since/days/limit/now only) carry no source facet, so that scan is the fleet's and its line rightly names none. The ticket's "in neither line that did not [carry it]" covers it exactly, and BUG-0192's effect-not-presence rule does NOT bite: that rule constrains when an ATTRIBUTION may render and explicitly exempts phrases that state what the read carried — silence is always true. Naming the facet there would restate the fleet's 1,000-row cap and truncation verdict over one source (BUG-0114, halves swapped) and would move figures. Line: correct as landed.

THE RESIDUAL IS REAL AND IS FILED, NOT BOUNCED — BUG-0194 (P2, discovered_from TASK-0073). RejectionSection narrows the ROWS it reports, so under ?source_id= both cards carry one source's figures while the line and both labels are byte-identical to the fleet's. Measured offline: 3→2 and "2 of 2"→"1 of 1" with the head unchanged. Pre-existing (this commit only renamed the local scope→reported), graded by no criterion here, and exactly the finding this ticket's own description ordered filed rather than patched. Pinned it.fails in tests/offline/sources/page.test.ts, watched RED as a plain it() on headOf(narrowed).words !== headOf(bare).words after asserting the figures differ (non-vacuous), committed 5d82adbc. Latent on staging today: the 90-day rejection window holds 0 rows under every URL.

Not re-filed: TASK-0060's source-name renderers. Not touched: BUG-0192's files. [resolution: fixed] [receipt green: 6 cmd(s), tree a298969e]
