---
id: BUG-0142
type: bug
status: done
milestone: M2
priority: P2
opened_by: verifier
parent: null
discovered_from: verifier
children: []
assignee: null
claimed_at: null
depends_on: []
touch_scope: [src/app/cycles/page.tsx, src/components/cycles/run-columns.tsx, tests/offline/cycles/page.test.ts, tests/live/runs.live.test.ts]
force: false
attempts: 1
resolution: fixed
---
# The Dashboard's run links land on /cycles?run=<id>, which marks nothing and says nothing: the run the operator clicked is not identified

## Description
Walked 2026-09-09 on the walk instance (http://localhost:8771, STAGING, branch run/admin-window), cookie-authed, bundled chromium 151.

Steps from a clean start:
1. GET /. In the RUNS panel, the failing row "ticketmaster / 9d ago / failed / refusing to run: KSPACE_BOT_USER_AGENT is not set" carries two anchors, both to /cycles?run=01a057a7-61ab-7148-8bfe-cd6c5ad635cf.
2. Click either. URL becomes /cycles?run=01a057a7-61ab-7148-8bfe-cd6c5ad635cf, status 200.
3. Observed on that page: the run id 01a057a7 appears 0 times in the rendered text; `data-row-marked="true"` occurs **0** times in the HTML; the ADAPTER RUNS table renders all 5 runs unmarked; no sentence names a parameter that was not applied.
4. Contrast, same page and same minute: /cycles?cycle=01a06701-9f7b-7330-b136-8ab5786addec renders the line "Cycle 01a06701-9f7b-7330-b136-8ab5786addec is marked in the table below." and `data-row-marked="true"` occurs exactly **1** time. /cycles?cycle=not-a-uuid renders "Cycle not-a-uuid is not among the 200 newest cycles, so it is not in this window — it ran earlier, or no cycle carries that id."

Expected: the run the operator clicked is identified on arrival — marked in the ADAPTER RUNS table the way ?cycle= marks its row, or named in a sentence the way the same page already names an unapplied narrowing (/cycles?source=<name> does exactly that: "Narrowed to the runs whose source is <name>. This facet narrows the runs below and nothing else...").

Observed: the run parameter is consumed silently. With 5 runs in the window the operator can guess; the window caps at 200, where they cannot. Spec F3's "Everything on it links into the pages below — the dashboard is the entry to the investigation path, never a dead end" is the clause at stake.

Documented as intentional in src/app/cycles/page.tsx (~116): "One more parameter reaches this route today and narrows NOTHING here: run=<run_id> (the Dashboard's run lines). It is not guessed at — an unrecognised parameter narrows nothing rather than erroring, so the link lands on this page rather than dead-ending." The page's own SOURCE_FACET docstring records the opposite standard for the same situation: it says so in one sentence "rather than either ignoring the parameter silently — which is what it did until admin-window/TASK-0016". Marking the row or drawing the sentence is the architect's call; silence is what fails.

Evidence: agenticflow/tracker/evidence/M2/verifier/31-cycles-run-unmarked.png and 10-cycles-run-facet.png; run log agenticflow/tracker/notes/verify-M2-2026-09-09.md.

## Acceptance criteria
Standing on the Dashboard and clicking a run row's link, /cycles?run=<id> identifies that run — the matching ADAPTER RUNS row carries data-row-marked="true", or a sentence on the page names the run id and says what was and was not narrowed — and a run id outside the window says so, as ?cycle= already does. Pinned in tests/offline/cycles/page.test.ts.

## Checks
```sh
./node_modules/.bin/vitest run --project=offline tests/offline/cycles
npm run test:live -- tests/live/runs.live.test.ts
npm run lint
```
## History
- 2026-09-09T18:33:44Z [verifier] opened: The Dashboard's run links land on /cycles?run=<id>, which marks nothing and says nothing: the run the operator clicked is not identified
- 2026-09-09T18:53:57Z [architect] checks amended: M2 structure walk (architect): the first check was defective and could never pass. `npm test` is `vitest run --project=offline && vitest run --project=isolated`, so npm appends the path filter to the SECOND command; the isolated project (include: tests/isolated/**/*.isolated.test.ts) matches nothing under tests/offline/cycles and exits 1 under any path filter. Measured on run/admin-window 2026-09-09: `npm test -- tests/offline/cycles` EXIT=1 ("No test files found"); the replacement `./node_modules/.bin/vitest run --project=offline tests/offline/cycles` EXIT=0, 2 files / 101 tests. Same coverage, runnable by every role. [was: npm test -- tests/offline/cycles, npm run test:live -- tests/live/runs.live.test.ts, npm run lint]
- 2026-09-09T19:11:06Z [builder-209] claimed (attempt 1)
- 2026-09-09T19:26:35Z [builder-209] claimed → built: - /cycles now consumes ?run=<run_id>: the runs half draws the named row as marked (DataTable `marked` + aria-current on that row's source cell, which also carries the new `run-<id>` anchor), and one sentence names the run — found ("Run <id> is marked in the table below.", the id an in-page link at rest), not-in-this-window (naming the ?source= scope its read had, since the run may be another source's), or no-window-read (naming the object, BUG-0023's third state). A value that is not a run id canonicalises to null via canonicalRecordId, marks nothing, and is named by BUG-0141's shared dropped-params owner, now rendered on /cycles for all three of its facets. The Dashboard's href (src/app/page.tsx `lineHref`) is untouched.
- Verified on the rebased tree: `./node_modules/.bin/vitest run --project=offline tests/offline/cycles` 112 pass; `npm test` 3037+6; `npm run lint`; `tsc --noEmit`; `npm run test:live -- tests/live/runs.live.test.ts` 10 pass and `tests/live/cycles.live.test.ts` 7 pass. Red-then-green: the 11 new offline cases all fail against HEAD~ sources.
- WALK, production build on :8791 (own bind-probed port, staging, cookie-authed, chromium): /cycles?run=01a05978-1875-74e8-a93d-3d6b7d6aaa89 -> 200, data-row-marked="true" x1, "Run 01a05978-1875-74e8-a93d-3d6b7d6aaa89 is marked in the table below."; the same id UPPERCASED and unhyphenated both mark the same one row and name it in the canonical spelling; ?run=00000000-0000-4000-8000-0000000000ff -> 200, 0 marked, "Run 00000000-... is not among the 200 newest runs, so it is not in this window - it ran earlier, or no run carries that id."; ?run=not-a-uuid -> 200, 0 marked, 0 run sentences, "The URL carries run, which this page did not apply: nothing below is narrowed by it." (the value itself never rendered). Both colour schemes, 1440x900: marked row border-left 1px accent (light rgb(152,16,250), dark rgb(194,122,255)) vs 0px on every other row; the sentence's link is accent+underline at rest in both. The marked row sits 3631px down, so the link matters: clicking it puts the row at top:0, in view. 5 run rows, 5 unique anchors, no duplicate id.
- QA notes: touch_scope did not name src/components/cycles/{adapter-runs,links}.tsx, which had to carry the sentence and the anchor spelling (the page may not define components; ARCHITECTURE §13.6). The LEAD copy of the newest run is deliberately never marked and carries no anchor - marking it would draw two marks and duplicate an id. The offline stub does not apply .eq(), so a ?source=-narrowed case must hand the script runsFrom(name) or the "absent" arm passes vacuously.
- 2026-09-09T19:26:57Z [dispatcher] landed ticket/BUG-0142 onto run/admin-window as 00d5f08
- 2026-09-09T19:27:43Z [qa] built → qa: under attack
- 2026-09-09T19:38:57Z [qa] qa → done: Criteria re-run first, on the landed tree, before reading the builder's story. Stored checks verbatim from a detached _receipt- worktree at 4650e96: offline/cycles exit 0 (114, was 112 — my two are added), runs.live exit 0 (10), lint exit 0; plus tsc --noEmit 0, full offline 3039, isolated 6. Receipt green.

Criterion clause by clause. 'Standing on the Dashboard and clicking a run row's link': walked, not typed — production build on :8793 (own bind-probed port, staging, cookie-authed, chromium, 1440x900, both colour schemes, read-only; server killed, port free). Clicked the first a[href*='/cycles?run='] on /: 200, exactly 1 tr[data-row-marked='true'], and it is the clicked run's row inside table[aria-label='Adapter runs'] — the criterion's PLACE clause, checked, not assumed. aria-current on exactly 1 [data-run]. Sentence: 'Run 01a05978-1875-74e8-a93d-3d6b7d6aaa89 is marked in the table below.' Its link href '#run-<id>' scrolls the row from y=3622 to y=-9.5 (33px tall, 23.5px visible, is_visible true). Mark measured as computed style, not read from source: border-left 1px rgb(152,16,250) light / rgb(194,122,255) dark vs 0px on all 4 other rows, and it HOLDS under hover (LOOK_AND_FEEL's asked-for-row rule). 0 duplicate ids. Dashboard href byte-unchanged: src/app/page.tsx is not in 00d5f08's diff at all.

Attacked, all over HTTP on staging, all 200: ?run=00000000-0000-4000-8000-0000000000ff (well-formed, no row) -> 0 marked, absent arm naming the 200-window. ?run=<real>&source=a-source-that-never-ran -> 0 marked, NOT a false mark, and the arm names the narrowed scope and the 'filed under another source' third possibility. ?run=<real>&cycle=<real> -> 2 marks, one per half, two sentences, anchors cycle-<id> vs run-<id>, 0 duplicate ids. ?run= uppercased and unhyphenated -> both mark the same 1 row, sentence prints the canonical spelling. ?run=not-a-uuid -> 0 marked, no run sentence, dropped-param line names 'run', value never rendered. ?run=<a>&run=<b> -> first wins, 1 mark. Offline I also drove the empty runs window, the 200-row truncated window, not_provisioned/refused/transport, and a run outside a genuinely narrowed read (the builder's stub caveat honoured — runsFrom(name), not vacuous).

Pinned two things the builder left unpinned, committed on the branch (4650e96): (1) the anchor collision links.ts exists to prevent — one id held by BOTH resolution_runs and runs, two marks, two distinct anchors, ids still unique; (2) a strict it.fails for BUG-0143, watched red as a plain it() first ('expected false to be true' on data-cycle-found).

Broke it, but not this ticket: BUG-0142 canonicalises ?run= and its twin ?cycle= two lines above is still compared raw, so /cycles?cycle=01A06701-9F7B-7330-B136-8AB5786ADDEC says 'is not among the 200 newest cycles' while [data-cycle='01a06701-...'] is on the page — filed BUG-0143 (P2), not a bounce: the run facet meets its criterion in every clause.

Residual for the dispatcher, no ticket: the absent arm's 'it ran earlier' is offered even when the window line directly above says the window did NOT fill ('so it holds all the runs the read found'), and even when the window is EMPTY beside 'No runs recorded'. AskedRun takes limit and scope but not RunsWindow.truncated; AskedCycle has the same shape. Cosmetic second: the sentence's link anchors the id SPAN, so the row's own top 9.5px sits above the fold. [resolution: fixed] [receipt green: 3 cmd(s), tree 138f03fa]
