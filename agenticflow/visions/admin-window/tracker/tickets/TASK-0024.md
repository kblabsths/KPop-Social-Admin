---
id: TASK-0024
type: task
status: done
milestone: M1
priority: P1
opened_by: architect
parent: FEAT-0006
children: []
assignee: null
claimed_at: null
depends_on: []
touch_scope: [src/lib/gauges/pending-claims.ts, src/app/sources/page.tsx]
force: false
resolution: already_done
---
# ASK BEN: where does Admin read the per-source stuck_pattern dial, which lives only in scraper YAML?

## Description
**Contract silence.** ARCHITECTURE marker `OPEN-DIAL`. SPEC named gap 4.

Spec section 5 says the pending-claims gauge judges each source's `awaiting_row` trend *'against its pattern threshold'*. `contracts/resolver.md` section 12 puts that threshold in the source registry: a global default of 20 records per source in 7 days, with a per-source override keyed `resolver: {stuck_pattern: {count, window}}` in `registry/sources/<source>.yaml` — **a file in the scraper repo**. Spec section 10 says reuse that would need scraper files at runtime is *'a flagged gap, not a silent copy'*, and this campaign does not read scraper files at runtime.

So the gauge can render the trend but cannot honestly draw the line. Options, worst to best in my reading:

- Hand-copy the values into Admin: **no.** The contract forbids it by name, and the copy drifts silently the first time a dial changes.
- Read the YAML from a sibling path at runtime: **no.** ARCHITECTURE section 1.2 — a relative parent path resolves inside the worktree during builds, and the deployed Railway service has no sibling repo at all.
- Show only the global default (20 in 7d) and label the line 'global default; per-source overrides not visible here': honest, cheap, and wrong for exactly the sources that have an override.
- **Recommend: ship the trend with no threshold line in M1**, and file the real fix as a handoff for later — the registry becomes readable to Admin the way the ecosystem already intends (spec section 10's 'the common home for cross-repo artifacts is infrastructure.md's — registry hosting, codegen'), i.e. the dial lands in a row Admin already reads, not in a file it has to reach for.

TASK-0007 and TASK-0013 ship the trend without the line; only the threshold overlay waits.

## Acceptance criteria
(human-checkable) Ben says where the dial comes from, or confirms shipping the trend without a threshold line in M1. The architect then removes the `OPEN-DIAL` marker from ARCHITECTURE and, if the answer needs new work, files it — including any scraper-side change as a **handoff** ticket, since a campaign is running in that repo.

## Checks
```sh
bash -c "! grep -q OPEN-DIAL agenticflow/docs/vision/ARCHITECTURE.md"
```

## History
- 2026-09-02T05:45:08Z [architect] opened: ASK BEN: where does Admin read the per-source stuck_pattern dial, which lives only in scraper YAML?
- 2026-09-02T05:45:09Z [architect] open → blocked: ASK BEN: the per-source stuck_pattern threshold lives only in scraper registry YAML, which Admin may not read at runtime and may not hand-copy. Recommend shipping the trend with no threshold line in M1.
- 2026-09-02T16:28:24Z [dispatcher] Ben 2026-09-02 (ruling): for M1 the per-source stuck-pattern threshold line stays ABSENT (as rendered today, with the reason stated). Design principle from Ben: dial-able values must not live in YAML files — a dial is easy to change, a YAML file is not; the ecosystem should move dials to rows (a design-queue item, paste-ready note on the panel). Architect: close this ASK on that ruling.
- 2026-09-02T16:41:36Z [architect] blocked → open: UNBLOCKED and ANSWERED — for QA to close as already_done; nothing is left to build. Ben's ruling, 2026-09-02: (1) the per-source stuck-pattern threshold line stays ABSENT for M1, exactly as rendered today, with its reason on screen. (2) The principle behind it, recorded in DECISIONS.md and explicitly NOT campaign work: a dial-able value must not live in a YAML file — dials belong in rows. So Admin does not read the scraper registry YAML now or later; when the dial becomes a row, the gauge reads the row and nothing about spec §10 is bent in the meantime. The architect's part is done: the OPEN-DIAL marker is out of ARCHITECTURE §12 (this ticket's stored check passes — dry-run 2026-09-02), and §12's settled paragraph deliberately does not repeat the marker string, which is what lets the check pass at all. No code change was required by this ruling; src/lib/gauges/pending-claims.ts and src/app/sources/page.tsx are correct as landed.
- 2026-09-02T16:48:52Z [qa] open → done: Closed on Ben's ruling; nothing was left to build and nothing was built. Receipt GREEN (OPEN-DIAL appears nowhere in ARCHITECTURE, re-run verbatim on 00a53c8).

VERIFIED THAT THE RULING MATCHES WHAT IS LANDED, rather than closing on the write-up alone: the threshold line is genuinely absent and the absence carries its reason on screen. src/components/review/shape-views.tsx:336-339 renders the no-threshold sentence whenever `series.threshold === null`, and the threshold branch exists but is unreachable because nothing supplies a value; src/app/sources/page.tsx:449 and :468 carry the same structure (`series.filter((one) => one.threshold !== null)`). No hand-copied 20/7d default appears anywhere: grep for the registry values in src/ returns nothing, so the "silent copy" spec §10 forbids was not made.

Also verified the second half of the ruling holds in code: Admin reads no scraper registry YAML at runtime — no fs read of a sibling path exists under src/, which is what would have broken the deployed Railway service (it has no sibling repo).

RESIDUAL, not a ticket: the `threshold` field and its rendering branch remain in the component as dead-but-typed structure. That is the right shape for the day the dial becomes a row (Ben's principle, recorded in DECISIONS.md: dial-able values belong in rows, not YAML), and it costs nothing today — I flag it only so a future reader does not mistake unreachable code for a bug. [resolution: already_done]
- 2026-09-02T18:50:56Z [dispatcher] Ben 2026-09-02 (standing rule): Admin builds NOTHING dial-shaped in this campaign — no threshold overlay, no dial display, no dial edit — until his dials-as-rows design lands in a follow-up; building it now would be replaced immediately. Architect: carry into DECISIONS.md at the next pass.
