---
id: BUG-0083
type: bug
status: done
milestone: M2
priority: P3
opened_by: qa
parent: null
discovered_from: qa:BUG-0081
children: []
assignee: null
claimed_at: null
depends_on: []
touch_scope: [next.config.ts, tests/http/auth.http.test.ts]
force: false
attempts: 1
resolution: fixed
---
# a case variant of a configured table serves the client-rendered error shell, not the app's framed 404

## Description
**Expected** (admin-window/BUG-0017's whole reason, and the invariant admin-window/BUG-0081 just restored for encoded spellings): a record URL whose table the edit map does not carry answers with the app's OWN framed 404 — the routed miss, server-rendered through the root layout — never a `notFound()` thrown from the dynamic segment.

**Found**: a CASE variant of a configured table gets the client-rendered error shell instead. `next.config.ts`'s `beforeFiles` source is compiled case-insensitively while `editConfigFor` is exact (`Object.prototype.hasOwnProperty` on the map, `src/lib/edit/config.ts`), so the matcher reads `EVENTS` as a configured table, does NOT claim the URL, and it reaches `src/app/records/[table]/[id]/page.tsx` — where `editConfigFor("EVENTS")` is null and `notFound()` throws inside the render shell. That is exactly the divergence BUG-0081 removed for `gro%75ps`, on the one spelling class left over.

**Repro, measured 2026-09-08 on a production build of run/admin-window @ 269bad0** (own `next build` + `next start --hostname 127.0.0.1 --port 8792` from a detached worktree, `SUPABASE_URL` pointed at a dead port so no database is involved, minted `authjs.session-token`, id `2f0bc11e-0000-4000-8000-000000000001`):

    GET /records/events%20/<id>     -> 404, len 9728, framed: <h1> present, href="/", token-layer class, no id="__next_error__"
    GET /records/groups/<id>        -> 404, len 9728, framed   (the routed-miss control)
    GET /records/EVENTS/<id>        -> 404, len 8004, id="__next_error__" present, NO <h1>, no navigation
    GET /records/Events/<id>        -> 404, len 8004, id="__next_error__"
    GET /records/WALK_SANDBOX/<id>  -> 404, len 8016, id="__next_error__"
    GET /records/EV%65NTS/<id>      -> 404, len 8006, id="__next_error__"   (case + encoding, same class)
    GET /records/EVENTS/<id>?x=1    -> 404, len 8034, id="__next_error__"

Every OTHER spelling in the same sweep is framed: plain and encoded struck tables, doubly-encoded (`gro%2575ps`), `%2F`-bearing, trailing `%20`, `..`, a 4000-character segment, a mapped name with one wrong byte (`ev%66nts`), a mapped name plus a suffix (`eventsx`), a bad escape (`gro%zzps`) — 32 of 32 unmapped spellings framed, 4 of 4 case variants shelled.

**Not a security finding, measured**: the gate is untouched on every one of these spellings — anonymous `GET /records/EVENTS/<id>` answers 307 to `/login?callbackUrl=...`, as do all 44 URLs in the sweep, so nothing discloses which record surfaces exist. `PATCH /api/admin/records/EVENTS/<id>` is refused by `requireAdmin()` exactly like every other spelling; no write path is involved.

**Case is NOT an encoding equivalence, and this ticket does not ask for one.** A URI path is case-sensitive (RFC 3986 §6.2.2.1), so `EVENTS` correctly has no record surface — the defect is only the DOCUMENT it is refused with. `EDIT_CONFIG`'s keys are the database's own spelling and must stay exact; the fix belongs in the routing layer, not in `editConfigFor`.

**Direction is the architect's, not prescribed here.** `next.config.ts` already records the constraint: the rewrite is what decides the miss, and its source is matched case-insensitively by Next's own compiler. Whether the answer is a case-sensitive matcher for this one rule, an exclusion written to match only the exact spelling, or deciding the miss somewhere else, is a design call — the last of the BUG-0017 class either way.

**P3 for the same reason BUG-0081 was**: no browser uppercases a path segment on its own, so the reach is a hand-typed or hand-edited URL and the impact is a degraded 404 document, not a wrong answer. Raise it if the architect judges the BUG-0017 class worth closing whole.

**The pin is `it.fails`.** `tests/http/auth.http.test.ts`, in the `a record URL for a table the edit map does not carry` describe: `it.fails("BUG-XXXX: serves the framed 404 for a case variant of a configured table")` — green while the divergence stands, RED the day it is closed. **The fix flips it to a plain `it(...)`**, and the check below then proves it. Watched red as a plain `it()` on this exact tree before filing: `AssertionError: /records/EVENTS/2f0bc11e: expected '<!DOCTYPE html><html id="__next_error…' to match /<h1[\s>]/`. Block 1b of the same file continues to pin the STATUS of that URL and pins no document shape, so the two do not collide.

## Acceptance criteria
(to be written)

## Checks
```sh
npm run test:http
```
## History
- 2026-09-09T02:31:45Z [qa] opened: a case variant of a configured table serves the client-rendered error shell, not the app's framed 404
- 2026-09-09T02:35:13Z [qa] checks amended: same reason: a bare vitest --project=http check cannot run in receipt.py's fresh worktree, which has no .next. npm run test:http builds first and includes tests/http/auth.http.test.ts. [was: ./node_modules/.bin/vitest run --project=http tests/http/auth.http.test.ts]
- 2026-09-09T02:37:26Z [builder-136] claimed (attempt 1)
- 2026-09-09T02:55:31Z [builder-136] claimed → built: - FIX (next.config.ts): the rewrite's matcher is now case-sensitive (`experimental.caseSensitiveRoutes`), so the ONE derived spelling list decides case as well as encoding — no second table list, `editConfigFor`'s exactness is the rewrite's. The rule moved `beforeFiles` -> `afterFiles` because Next does NOT pass that flag to a beforeFiles rewrite (`filesystem.js:293-300` builds it with neither basePath nor sensitive; measured: flag alone, rule left in beforeFiles, /records/EVENTS/<id> still served the shell). afterFiles runs after static files/non-dynamic pages but BEFORE dynamic routes, and nothing under /records/ is either, so the miss is still decided before the page. ES2025 `(?-i:)` was the local alternative and is rejected in the file: SyntaxError below V8 12.5 and engines.node is >=20.0.0.
- PROOF: watched the flipped pin RED on this tree first (/records/EVENTS/2f0bc11e: expected '<!DOCTYPE html><html id=\"__next_error…' to match /<h1[\s>]/), then green. Rebased on run/admin-window @757b83e: `npm run test:http` 16/16, `npm test` 2215+6, tsc 0, lint 0, `npm run build` 0. Own sweep, production build, own `next start` on 8793 (DB at a dead port, minted cookie): 8 case variants (EVENTS, Events, eventS, WALK_SANDBOX, Walk_Sandbox, EV%65NTS, VENUES, EVENTS?x=1) -> 404 len 9728 framed, byte-identical in length to the /analytics routed-miss control; 6 mapped spellings incl. %65%76%65%6E%74%73 and wal%6B_sandbox -> 200 framed; gro%75ps / idol%73 / events%20 / gro%2575ps / gro%zzps / eventsx / ev%66nts -> 404 framed; /RECORDS/events/<id> -> 404 framed; anonymous EVENTS, events, gro%75ps, /__no-record-surface__ -> 307 /login.
- FOR QA: the flag is `experimental` and invisible to beforeFiles — moving the rule back reopens this silently, and only the http block catches it. Uppercase hex digits (%6E) are now carried ONLY by the hand-written two-case classes in the pattern, so the BUG-0083 block gained its must-not-flag fixture: every EDITABLE_TABLE spelled with uppercase hex must render 200. Also added /records/EVENTS/<id> to the anonymous gate loop; comments in block 1b and the gate loop were rewritten to the new behaviour (assertions unchanged). No product code touched outside next.config.ts.
- 2026-09-09T02:56:02Z [dispatcher] landed ticket/BUG-0083 onto run/admin-window as ed11a92
- 2026-09-09T02:56:35Z [qa] built → qa: under attack
- 2026-09-09T03:07:53Z [qa] qa → done: ATTACKED on the landed tree, own detached worktree, own production build, own next start on 127.0.0.1:8801 (probed free by real bind on 127.0.0.1/::1/0.0.0.0; never 8770/8771/8772), DB at a dead port, minted cookie. .env never opened, no secret printed, staging untouched, only my own pids killed.

CRITERIA FIRST, before the builder note: npm run test:http 16/16 (the BUG-0083 pin green as a plain it), npm test 2219+6, tsc 0, lint 0, npm run build 0 — from a worktree with no .env, so the offline claim holds offline. Re-swept and receipt re-run on 4cbaa79 after TASK-0049/0058 landed under me.

SWEEP: 115 assertions, 0 failures (evidence/BUG-0083/sweep.mts).
- 24 case variants of mapped tables (EVENTS/Events/eventS/eVents/EVENTs/eveNTS, VENUES/Venues/venueS/vENUES, WALK_SANDBOX/Walk_Sandbox/walk_SANDBOX/WALK_sandbox/walk_Sandbox, EV%65NTS, %65VENTS, Ev%65nts, %45vents, %45VENTS, WAL%6B_SANDBOX, wal%6B_SANDBOX, VEN%75ES, +?x=1) -> 404 and every body BYTE-IDENTICAL (sha256, len 9732) to the /analytics routed-miss control and to /__no-record-surface__. Not merely framed: the same document.
- every EDITABLE_TABLE (events, venues, walk_sandbox) plain, all-lowercase-hex, all-UPPERCASE-hex, alternating-hex-case, partial-encoding -> 200 framed, no shell (16 spellings). Extras run also cleared %45%56%45%4E%54%53 and %57%41%4C%4B%5F%53%41%4E%44%42%4F%58 (a case variant spelled wholly in encoding) -> 404 == control, and %77%61%6C%6b%5F%73%61%6E%64%62%6F%78 -> 200.
- all 21 BUG-0081 unmapped spellings still framed 404 == control: gro%75ps, idol%73, gro%2575ps, gro%zzps, events%20, %20events, ev%66nts, eventsx, events%2Fx, %2e%2e, %00events, e%76ents%00, events;x, events.json, a*4000. The hostile segment is never reflected into the document — identical sha proves it.
- /RECORDS/, /Records/, /records/x/y/extra, /records/events, /records -> 404 == control; /records/ -> 308.
- FLAG BLAST RADIUS: 9 pages+handlers 200 on their real spelling; 11 path-case variants (/BROWSE /Browse /CLAIMS /Cycles /QUEUES /QUEUES/<id> /SOURCES /LOGIN /API/health /api/HEALTH /api/Health) -> 404 == control; /login and /api/health still public 200. TASK-0049 landed a new handler mid-attack, so it was swept too: POST-only settle -> 405 on GET, /…/SETTLE and /API/… -> 404 == control, both anonymous -> 307 /login.
- GATE: 16 anonymous URLs incl. EVENTS, encoded, /RECORDS/, PATCH /api/admin/records/EVENTS/<id> -> 307 /login?callbackUrl. No spelling discloses which surfaces exist.
- METHODS: HEAD/POST/OPTIONS/PUT/DELETE on /records/EVENTS/<id> -> 404, no shell.

ROUTING ORDER PROVED, not trusted (reach.mts + a counting stub as SUPABASE_URL): 9 misses (EVENTS, Events, WALK_SANDBOX, EV%65NTS, VENUES, gro%75ps, no-such-table, /analytics, backstop) -> db_requests=0, so afterFiles decides before the dynamic route and the page is never reached; 5 mapped spellings -> exactly 1 catalog read each (GET /rest/v1/events?select=…), so the counter is demonstrably not blind.

SABOTAGE, both risks the builder named, each rebuilt and re-run: (1) rule moved back to beforeFiles, flag kept -> the pin goes RED with the original symptom (expected <html id="__next_error… to match /<h1[\s>]/); (2) flag removed, rule left in afterFiles -> same RED. The silent-reopen risk IS caught by the suite. Worktree reverted, git status clean before removal.

DEV MODE too (next dev, the surface endgame walkers drive): 5 case variants -> 404 framed, 5 mapped spellings -> 200 framed. RSC flavour (RSC:1, i.e. client-side navigation) of /records/EVENTS/<id> -> 404 like no-such-table, no error shell.

NO TEST ADDED, deliberately: the one class I could have pinned (a case variant spelled entirely in %XX) decodes to the same string as EV%65NTS, already pinned in the block — a duplicate buys run time, not protection.

RESIDUAL, no ticket: afterFiles runs after static files, so a future public/records/** asset would out-rank this rule. Nothing exists there today.

Could not break it. The goal — a case variant of a mapped table gets the app OWN framed 404, not the client-rendered shell — is met at the document level, byte for byte, and the two ways to silently undo it both redden the suite. [resolution: fixed] [receipt green: 1 cmd(s), tree 9bfb46c9]
