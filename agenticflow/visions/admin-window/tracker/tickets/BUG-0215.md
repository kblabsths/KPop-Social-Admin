---
id: BUG-0215
type: bug
status: done
milestone: M3
priority: P1
opened_by: qa
parent: null
discovered_from: BUG-0209
children: []
assignee: null
claimed_at: null
depends_on: []
touch_scope: [tests/live]
force: false
attempts: 1
resolution: fixed
---
# Live tier red on the settled tree: two edit.live tests assume the §9 settlement path is absent from staging, and one now writes an unswept override into the catalog

## Description
MEASURED by QA on the landed tree, run/admin-window HEAD 1e5d3e92, while grading BUG-0209's amended EC1 (arm 1: 'On the settled tree, at the close: npm run test:live exits 0 — that is the guard').

EXPECTED (EC1 as amended, M3.md:98-100): npm run test:live exits 0 on the settled tree.
FOUND: exit 1.
  23:22Z, whole tier:  Test Files 1 failed | 10 passed (11); Tests 1 failed | 120 passed (121).
  23:27Z, targeted:    npm run test:live -- tests/live/edit.live.test.ts -> EXIT=1; Tests 2 failed | 23 passed (25).
  (evidence/BUG-0209/edit-live.txt)

WHY IT MOVED — the §9 handoff artifacts are now INSTALLED ON STAGING. Read-only probes against the declared staging target (project ref ubfjjqlvnpnoborczbdb, no credential printed):
  - GET /rest/v1/verdicts?select=*&limit=0 -> 200, content-range */0. Control: no_such_table_xyz -> 404 PGRST205. So the table is genuinely there.
  - GET /rest/v1/ (OpenAPI) lists /rpc/settle_review_item. So the function is there too.
  - It landed DURING this QA lane: builder-309 measured the whole tier green at 20:56Z; at 23:22Z the record-page test was already red (table present) while the forged-edit test still passed; by 23:26Z the forged-edit test failed too (function present).

FAILURE 1 — tests/live/edit.live.test.ts:362 'a resolver-owned record page > renders from staging with no editable widget on it, and names why'.
Expected (test:375): markup not to match /<(button|input|textarea|select)[\s>]/ and one [data-note="override-unavailable"].
Found: the events record page renders editable widgets (title, description, poster_url, starts_at buttons, plus a venues picker) and [data-note="regime"] instead. The PRODUCT IS RIGHT: src/lib/db/verdict.ts:253 readSettlementReadiness reads the verdicts table, which now answers ok, so src/app/records/[table]/[id]/page.tsx:602 overrideClosed is false and the override regime renders as designed. The TEST encodes the pre-handoff world as an unconditional premise (its own docblock: 'with nothing on staging to record an override').

FAILURE 2 — tests/live/edit.live.test.ts:317 'a forged edit > answers a mapped column's override with the absence, and changes nothing'.
Expected (test:329): 503 naming the object the seam called, and the row byte-identical afterwards.
Found: 200 with {"ok":true,"verdict":{"verdict_id":"01a092cb-d024-7f8a-9763-2c3e357e0b7f",...,"action":"override"}} — the override was APPLIED.
Its docblock (tests/live/edit.live.test.ts:314) says 'It writes nothing on any path, so it needs no sweep'. That premise is now false, so the test writes and never sweeps — acceptance test 13 ('every live test sweeps every row it wrote') is violated at runtime.

STAGING RESIDUE THIS LEFT, measured read-only, NOT cleaned (database writes need Ben's approval):
  - events row 01a03c9b-1d28-707d-9873-f73ab3add10c: title is now 'admin-window/TASK-0018 probe override'. Its value before the override was 'XG' (read off the record-page markup captured in the 23:22Z run).
  - verdicts holds 2 rows, both action=override, created_at 2026-09-11T23:26:15.097336Z and 23:27:05.668905Z — one per targeted run I made. Every further run of this file adds another.

REPRO (exact): from /Users/ben-m4/Desktop/Coding/KPOP/kspace Admin, npm run test:live -- tests/live/edit.live.test.ts. Exit 1, 2 failed / 23 passed. NOTE: each run writes another override verdict to staging until this is fixed.

NOT A REGRESSION IN ANY LANDED CODE. No src/ change since builder-309's green run touches either path (git diff f85a0996..HEAD -- src = claims.ts, result.ts, review-items.ts, all BUG-0210's count-read fix). The environment moved under the suite.

FOR WHOEVER TAKES IT: the fix is not just 'flip the assertions'. FEAT-0011 criterion 2 and ARCHITECTURE §9.2 name the ABSENT settlement path as M3's graded normal case; with the handoff installed that normal case no longer exists on staging. Whether M3's graded case moves is an architect ruling, not a builder's — file blocked if it blocks. What is a builder's either way: the suite must branch on the readiness it observes rather than assume absence, and any override it applies must be swept (acceptance test 13). docs/build_judgments.md's routed-questions section also still states 'verdicts and settle_review_item are physically in the sibling repo and not installed' — true when TASK-0078 wrote it at 21:06Z, false on staging now; the M3 close should re-check that sentence.

## Acceptance criteria
(to be written)

## Checks
```sh
npm run test:live -- tests/live/edit.live.test.ts
```

## History
- 2026-09-11T23:30:44Z [qa] opened: Live tier red on the settled tree: two edit.live tests assume the §9 settlement path is absent from staging, and one now writes an unswept override into the catalog
- 2026-09-11T23:34:07Z [builder-315] claimed (attempt 1)
- 2026-09-11T23:52:38Z [builder-315] claimed → built: WHAT: the live suite now READS which settlement world staging is in instead of assuming absence. parity.ts gains functionsOnStaging() (the harness's private reader, moved+exported — a READ of PostgREST's schema description, never a call); edit.live.test.ts gains settlementPath() (verdicts + settle_review_item, read once) and branches both broken cases on it; harness.live.test.ts's absence fixture is now a name no migration can install (admin_window_no_such_function), and the settlement path's state is read, logged, and graded against the app's own readiness seam.

NO CASE APPLIES AN OVERRIDE, in either world. Where the seam is installed the override ARM is probed with a CLEAR, which decisionRefusals invariant 5 refuses 400 value_payload_missing BEFORE anything is called — that refusal belongs to the override arm alone (the direct arm would 200 and null the column), and the 'before anything is called' half is pinned offline against a mocked seam (tests/offline/edit/route.test.ts, 'refuses a clear rather than sending an override that says nothing'). Guarded in-process first: decideEdit(...).edit.path must be 'override' before the request leaves. MEASURED: verdicts still holds exactly the 2 rows QA's runs left, after 4 live runs on the fixed tree; events.title unchanged by me.

VERIFIED on the rebased tree (741b4936 on run/admin-window): npm run test:live -- tests/live/edit.live.test.ts EXIT 0, 25/25; harness.live EXIT 0, 7/7; npm test 3973/3973; tsc --noEmit and lint clean. Red-then-green observed on both new branches by mutating only expectations (400->499, 1->2): both failed naming the real answer, then green reverted.

TRAP FOR QA — the WHOLE tier is still 1 failed | 121 passed (122), and it is not a test defect: residue.live.test.ts correctly names 'events.title: 1 row(s)' — the catalog row the OLD test rewrote (01a03c9b-1d28-707d-9873-f73ab3add10c, real value 'XG', confirmed by the pipeline's own observation 01a058f1-...). Restoring it is a catalog write only Ben may make; paste-ready SQL + the admin_locked field_provenance rows it also left are in agenticflow/tracker/for-human/BUG-0215-staging-residue.md. Also still stale and NOT mine (routed to the M3 close by this ticket): docs/build_judgments.md:296 states the two §9 artifacts are 'not installed' — false on staging since 2026-09-11.
- 2026-09-11T23:53:07Z [dispatcher] landed ticket/BUG-0215 onto run/admin-window as 35fd1e49
- 2026-09-11T23:54:04Z [qa] built → qa: under attack
- 2026-09-12T00:07:13Z [qa] qa → done: attacked: READ BEFORE RUN, then measured. Static: in the installed world both changed cases send {field, value:null} on events.title / venues.name; the route builds a VerdictValue with value/ref/observation_id all null, decisionRefusals finds 0 filled slots over ALL_SLOTS (decision.ts:606) and returns 400 value_payload_missing at route.ts:307-318 — BEFORE settleReviewItem. If the map ever moved the column to the direct arm (where that same clear would null a catalog column) the in-process guard `expect(decision.edit.path).toBe("override")` throws before patch() leaves. Offline pin exists and is green: tests/offline/edit/route.test.ts:325 asserts settleReviewItem not called (62/62). Both MIXED worlds read off the branch too: log present + seam absent -> value-carrying override, refused by the absent function, nothing written; log absent + seam installed -> clear, 400. No world in which this file applies an override.
MEASURED on ubfjjqlvnpnoborczbdb (read-only, no credential printed): verdicts held exactly 2 rows (23:26:15Z, 23:27:05Z, actor live-suite@example.invalid) before my first run and EXACTLY THE SAME 2 after four runs of edit.live (2 whole-file, 1 targeted, 1 receipt) plus harness.live and residue.live. Zero new verdicts, zero new field_provenance rows; events.title untouched by me. Pre-fix, each run added one.
Tier grep: no live test calls settle_review_item or apply_resolution; the only .rpc() left is harness's uninstallable name (admin_window_no_such_function), re-established absent before it is called. harness.live 7/7, its new case logging "verdicts is present, settle_review_item is INSTALLED (of 56 function(s) exposed)". residue.live exits 1 naming ONLY events.title: 1 row(s) — the pre-existing row awaiting Ben — with walk_sandbox scanned clean [label=0, note=0], so my runs swept everything they wrote. tsc --noEmit exit 0.
Checked the human-facing artifact against staging (LESSONS 9): observations.01a058f1-00d1-7978-8f6e-7399255d1884 does hold value "XG" for that event's title; the two admin_locked=true provenance rows are 01a092cb-0afc-… and 01a092cb-d019-…; both verdicts and timestamps match. The paste-ready SQL targets event_id + the probe title and the lock rows — accurate as written.
ONE FINDING, filed as BUG-0219 and NOT this ticket's defect: run 1 of the stored check was EXIT 1 on two UNCHANGED cases (edit.live:718, :854) naming entity 6527b112-3de7-459f-b081-17aee6d7a002, which exists in neither events nor field_provenance ten minutes later (both 0 rows; the 108-id population is all uuidv7). A concurrent writer on the shared project; both re-runs green, 25/25, and no failing line is in BUG-0215's diff.
Residuals for the M3 close, not bounces: (1) FEAT-0011 criterion 2's graded normal case — the ABSENT settlement path — no longer exists on staging; the builder routed that to the architect rather than deciding it, correctly. (2) docs/build_judgments.md:296 still says the two §9 artifacts are "not installed"; verified stale. (3) readSettlementReadiness reads only the verdicts TABLE, so a half-installed world (table, no function) would offer an override control whose save 503s — no case grades that state.
Judgment: the ticket's goal is met — the suite reads which settlement world it is in instead of assuming absence, and writes nothing it cannot sweep. Receipt green on the landed tree. [resolution: fixed] [receipt green: 1 cmd(s), tree 9368249f]
