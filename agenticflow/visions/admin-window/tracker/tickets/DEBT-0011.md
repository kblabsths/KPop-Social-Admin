---
id: DEBT-0011
type: debt
status: done
milestone: patch
priority: P2
opened_by: architect
parent: null
children: []
assignee: null
claimed_at: null
depends_on: [BUG-0141, DEBT-0008]
touch_scope: [src/components/ui/identifier.tsx, src/components/ui/index.ts, src/app, src/components, tests/offline/ui/primitives.test.ts, tests/offline/records/page.test.ts, tests/offline/review-item/page.test.ts]
force: false
attempts: 3
empty_diffs: 1
resolution: fixed
---
# The mono identifier face is hand-spelled 44 times across 24 files with no shared primitive — the structural root of BUG-0112/0120/0121

## Description
M2 structure walk finding (architect, 2026-09-09), measured on run/admin-window.

§11: 'Machine identifiers render verbatim in mono, never prettified.' The rule is real and the campaign fixed three separate breaches of it in M2 — BUG-0112 (the record page's regime note), BUG-0120 (the record page's empty-state cards), BUG-0121 (the queues detail page's empty-state cards, 'the face BUG-0120 fixed one route over'). Each fix was one hand-typed class pair.

**The measurement.** `grep -rno 'type-data text-ink' src` returns **44 occurrences across 24 files** (counted 2026-09-09). `src/components/ui/` holds fifteen primitives — Page, Section, DataTable, StatCard, Badge, Chip, Button, Loading, Empty, NotProvisioned, ErrorLine, StateOf, WindowLine, MicroLabel, cx — and **not one of them is the identifier**. Two files each grew their own private one instead:

- `src/app/records/[table]/[id]/page.tsx:277` — `function TableName({ config }) { return <span className="type-data text-ink">{config.table}</span>; }`
- `src/app/queues/[reviewItemId]/page.tsx:174` — `function ReviewItems() { return <span className="type-data text-ink">{T.reviewItems}</span>; }`

Byte-identical bodies, different names, two routes, neither reachable from the other. This is exactly Common violations row 9 ('a page helper hand-copied into every page that needs it', promoted 2026-09-03 with `StateOf` ×4 and `WindowLine` ×3) and the reason its promotion note gives: builders in isolated worktrees cannot see each other's code.

**Why it matters beyond tidiness.** A rule spelled as two Tailwind classes at 44 call sites has no place to hold anything else. Three things the campaign has already decided cannot be applied once: the ARCHITECTURE row 15 ruling that foreign text reaches prose through an allowlist or a bidi-isolated box (BUG-0137 — an identifier span is precisely where foreign text lands); `` `unicode-bidi: isolate` ``; and any future rule about wrapping a long unbroken identifier, which BUG-0105 had to solve inside the refusal layout alone.

Scoped `patch` rather than M2: the sweep touches 24 files, several of which BUG-0141 and DEBT-0008 are writing, and it is a mechanical change with no user-visible delta. It should land early in the next milestone, before the next surface adds a 45th hand-spelling.

## Acceptance criteria
1. `src/components/ui/` gains one identifier primitive, exported from the barrel alongside the other fifteen, that renders a machine identifier verbatim in the mono face. It is the only place in `src/` that spells the identifier's class pair.
2. Every call site that renders a machine identifier — a table or view name, a column name, a status/shape/bucket value, an id — imports the primitive. `TableName` and `ReviewItems` are deleted in favour of it.
3. The rendered output is unchanged: the same face, the same ink, verbatim text, no prettification, no uppercasing. Every case BUG-0073, BUG-0112, BUG-0120, BUG-0121 and BUG-0034/43/44/45/49 pinned still holds, graded by the page suites those tickets left behind.
4. The primitive isolates its content for bidi, so an identifier carrying a bidi control cannot reorder the sentence around it — the ARCHITECTURE row 15 rule (BUG-0137) applied once instead of per sentence. A test grades it on two fixtures: one identifier that must be isolated and one ordinary label that must not be touched.
5. Look and feel (LOOK_AND_FEEL, machine identifiers and the Data table rules): the primitive uses the design tokens and never a raw colour or font value; a page of identifiers reads the same in both themes at every supported width; the primitive introduces no new hover-only or pointer-only behaviour.
6. `npm run lint` and `tsc --noEmit` clean; the full offline suite green (this is a shared render primitive touching every page — the §13 targeted-tests exception applies and is claimed here deliberately).

## Checks
```sh
./node_modules/.bin/vitest run --project=offline
bash -c "rm -rf .next/types && npm run lint && ./node_modules/.bin/tsc --noEmit"
test "$(grep -rlo "type-data text-ink" src | wc -l)" -eq 1
! grep -rq "function TableName\|function ReviewItems" src
```

## History
- 2026-09-09T18:58:05Z [architect] opened: The mono identifier face is hand-spelled 44 times across 24 files with no shared primitive — the structural root of BUG-0112/0120/0121
- 2026-09-09T21:19:54Z [builder-217] claimed (attempt 1)
- 2026-09-09T21:39:04Z [builder-217] claimed → built: One primitive: src/components/ui/identifier.tsx (Identifier, barrel-exported). All 48 hand-spellings of the class pair in 26 files now import it; TableName and ReviewItems deleted. It renders <span dir="ltr" class="type-data text-ink[-secondary]"> — the row-15 bidi isolation hung off the one place — and className/dir/style are typed OUT of its props. The four app-word sites that wear the face without being identifiers (saving…, settling…, Loading, StatCard sub) take the exported DATA_MUTED class, which is why grep finds one file.
- Rendering proof (whole window, not per component): every route x {populated, empty, sparse} rendered through tests/offline/absence/surfaces.ts on the pre-sweep tree (detached worktree at run/admin-window) and on mine — 24 dumps, 130 class-pair renderings (55 ink + 75 muted) before and after, rendered TEXT byte-identical on all 24 (md5 with tags stripped). Markup delta is exactly two things: ' dir="ltr"' on the 62 identifier renderings, and three block identity lines that became <span> (the queues item id, the queues gauge slice's queue name, the record page's row id) — all three are direct children of a flex column, so blockified flex items either way, same box, same face, same hooks.
- Checks on the rebased tree (onto 45c772c): offline 72 files / 3140 tests green (baseline on the pre-sweep tree was 3132; +6 new, zero existing tests changed to pass), lint + tsc --noEmit clean, npm run test:http green (real next build), grep -rlo 'type-data text-ink' src = 1 file, no 'function TableName|ReviewItems'.
- Two things QA should know. (1) Beyond touch_scope by necessity: tests/offline/source-tree.ts — implicitInterElementSpaces now reads </Identifier> too, or the BUG-0045 guard goes blind on the 48 closing tags this sweep renamed (must-flag + must-not-flag fixtures added in tests/offline/ui/copy.test.ts); tests/offline/ui/tokens.test.ts samples both inks so the token rules bind the new primitive. (2) Deliberate boundary, not an oversight: Eyebrow (micro-label.tsx) and the page h1 render an identifier in the same mono step but take the LABEL's ink with normal-case tracking-normal (BUG-0049/0073) — they never spelled the class pair, and inking them from the primitive would change what they draw, so they keep their own owner. Same for identifiers rendered as anchors (type-data + IN_PAGE_LINK) and at text-broken. New guards were watched RED first: dropping dir="ltr" reddens the isolation fixture (and DroppedParamsLine's existing dir pin), re-hand-spelling one call site reddens the one-owner sweep. ARCHITECTURE §3's 'No identifier primitive' note and §11 are now stale — architect's to amend.
- 2026-09-09T21:39:30Z [dispatcher] landed ticket/DEBT-0011 onto run/admin-window as 70ec2d3
- 2026-09-09T21:40:06Z [qa] built → qa: under attack
- 2026-09-09T21:54:54Z [qa] qa → reopened: BUG-0148 filed. Criterion 2 is unmet at one call site; everything else I could break held.

Stored checks, re-run VERBATIM on the landed tree (99b8b1b) in a detached `_receipt-` worktree before reading the handoff: offline 72 files / 3140 tests green; `rm -rf .next/types && npm run lint && tsc --noEmit` both exit 0; `grep -rlo 'type-data text-ink' src | wc -l` = 1; no `function TableName|ReviewItems`. Plus `npm run test:http` 4 files / 17 tests green on a real build. After my pin: 3141.

WHAT BOUNCES IT. `src/components/review/close/slot.tsx:309-313` renders `verdict.observationId` — an id the database produced, labelled "observation id" — as a hand-faced `<span data-verdict-observation={observation} className="type-data">`, not through `Identifier`. Not an anchor, not `text-broken`, not Eyebrow/h1: none of the four stated boundaries. It is in a file the sweep edited, 30 lines below `ItemVerdictBlock`'s OWN conversion of `verdict.action` to `<Identifier>` in the same function (2cf6be1). And the "inking it from the primitive would change what it draws" reason does not apply here: MEASURED in Chromium (production build, my bind-probed port 8813, 1440x900, light), injecting the shipped markup and the primitive's side by side into a live page so the app's CSS applies — shipped: Geist Mono / 11px / rgb(30,41,57) / 52.81px / unicode-bidi normal; primitive: same font, same size, same colour, same 52.81px box, unicode-bidi ISOLATE. Identical but for the isolation criterion 4 exists to deliver. Pinned strict in the file that owns the surface (`tests/offline/review-item/verdict-inline.test.ts`, `it.fails`, watched red first at 1 failed | 32 passed, non-vacuous — the `[data-verdict-action]` assertion above it passes), landed as 5010e2f. No class or attribute literal in it: both read off `Identifier`'s own render.

WHAT HELD, and what I tried to break it with.
- The three `<p>`->`<span>` swaps, in Chromium, production build, BOTH schemes, 1440x900: for each site I reconstructed the pre-sweep element (same tag, same class, no `dir`) in place and re-measured. `/queues` gauge name, `/queues/not-an-id` identity, `/records/walk_sandbox/not-an-id` id — rect, display (block both ways), line-height 16px, margins 0, parent height, BODY height and every sibling top identical to the pixel in all six measurements. Zero layout shift; the flex-column parent blockifies either way, as the handoff claimed.
- Sabotage, three rounds, each watched red: re-hand-spelling `<Identifier>{stats.queue}</Identifier>` in `/queues` reddens the one-owner sweep and NAMES the file and line; dropping `dir="ltr"` from the primitive reddens both the isolation fixture and DroppedParamsLine's existing pin; introducing a real implicit inter-element space at a live `</Identifier>` site (asked-cycle.tsx:58) reddens copy.test.ts's tree assertion with `asked-cycle.tsx 58: …`. The BUG-0045 guard is not blind on the 48 renamed tags.
- Bidi containment in a real browser, not just markup: `/cycles?cycle=%E2%80%AEnot-a-uuid`, characters by Range rect, both schemes — the reversal is contained to the value's own 66.00px box (241.8->307.8) and the app's 118 characters of prose read in order. Measurement written into BUG-0147's History for its builder: its RENDERED harm is dead, its criterion (no control in the paragraph's text) is not met, its `it.fails` pin has not silently XPASSed.
- Eyebrow / the page h1: `micro-label.tsx` and `ui/page.tsx` are untouched by 2cf6be1, so byte-identical by construction.
- The one existing test file the sweep edited (`review-item/page.test.ts`) is a comment-only change — no assertion weakened to pass.

RESIDUALS, not tickets. (1) The one-owner guard matches the class pair only when ADJACENT, so `data-table.tsx:169` and `EditableCell.tsx:1476` spell both classes on one element and pass; both are pre-existing primitives with their own owner, but a future `<span className="type-data font-normal text-ink">` would evade the sweep. (2) `item-header.tsx:174` renders `ItemLink.value` ("the machine value verbatim") hand-faced inside an anchor — measured, no reordering harm ("Its source" reads in order), and the anchor boundary arguably covers it, so it is named in BUG-0148 as deliberately NOT filed. (3) `dir="ltr"` fixes LTR base direction as well as isolating; no identifier today carries RTL text, and row 15 asks for isolation, so this is intent, not defect.

Process note for the human: `node tests/walk/session-cookie.mts`'s one-line JSON reached my transcript through a mis-ordered redirection of mine. It is a minted 2-hour walker session cookie bound to `localhost:8813` (now dead), not a stored secret — `AUTH_SECRET` never appeared on either stream. Flagging it rather than hiding it.
- 2026-09-09T21:59:58Z [builder-220] claimed (attempt 2)
- 2026-09-09T22:04:33Z [builder-220] claimed → built: ZERO-DIFF re-prove: nothing to fix, nothing committed. `git diff run/admin-window` is empty at HEAD (rebased twice onto the moving tip, final fe49f6a); the one bounce (verdict block observation id, src/components/review/close/slot.tsx) was fixed by admin-window/BUG-0148, landed at 7a06074, and slot.tsx:308-311 now renders `<Identifier data-verdict-observation>` on the unlinked arm. BUG-0148 converted its own `it.fails` pin in tests/offline/review-item/verdict-inline.test.ts to a strict `it` that reads dir+class off the primitive's render; only its doc comment still says "landed as a STRICT pin (`it.fails`)" — stale prose in BUG-0148's file, left alone to keep this ticket zero-diff.

- Stored checks, verbatim, on the rebased tree: offline 72 files / 3154 tests green (was 3141 after QA's pin; +13 from other lanes, zero from me); `rm -rf .next/types && npm run lint && tsc --noEmit` exit 0; `grep -rlo "type-data text-ink" src | wc -l` = 1; no `function TableName|ReviewItems`. Plus `npm test` (offline+isolated) exit 0, isolated 1 file / 6 tests. Guard watched RED first: appending `<span className="type-data text-ink">{v}</span>` to ui/loading.tsx reddens "leaves no file under src/ but the primitive spelling the face" and NAMES `src/components/ui/loading.tsx: return <span ...>` — reverted, tree clean.

- SWEEP for a 49th hand-faced identifier: NONE FOUND. `grep -rn type-data src` = 34 hits; 2 are the primitive, 1 is the `@utility` definition in globals.css, 4 are comments. Every one of the remaining 27 falls inside a stated boundary, checked by reading each: 8 anchors (`IN_PAGE_LINK`/`LINK_DECORATION` — app/page.tsx:405, asked-cycle:68, cycle-health:185, adapter-runs:187, record-fields:88, entity-picker:567, item-header:174, slot:317), 6 at `text-broken` or inside a `text-broken` parent (run-columns:155, cycle-health:198, cycle-table:179, form:140, error-line:47, EditableCell:821 — the last two render the DB's refusal verbatim and would change ink if inked from here), 2 Eyebrow/page-h1 (micro-label:82, page.tsx:73), 4 form controls (EditableCell:890/893, entity-picker:394, form:407 — an input is not a span), 5 primitives that compose layout/interaction classes the primitive types OUT (data-table:169, EditableCell:803/1485, chip:24, badge:42), and item-header:144, a `<p>` container of app prose (opened/last evidence/asked again) at secondary ink. `grep -rn "font-mono|Geist Mono|fontFamily" src` outside globals.css = 0, so no third spelling of the face exists.

- Two `DATA_MUTED` sites the primitive's own doc does not enumerate (it says "four places"): evidence-pair.tsx:74 and :90. Examined, not defects and not filed — each renders a COMPOSITE line mixing machine values with app text (`source · tier · <relative age>`; `"ticketmaster, applied 3d ago"`), so no single value can be wrapped without either giving app words the identifier's ink or re-splitting the line. Worth one word in the primitive's doc some day; not worth a diff here. QA residual (1) still stands unchanged: the one-owner guard matches the class pair only when ADJACENT, so a future `type-data font-normal text-ink` would evade it (today only data-table:169 and EditableCell:1485/893, entity-picker:394, form:407 spell them non-adjacently, all controls/primitives). [EMPTY DIFF: no file changed under scope — 1 so far]
- 2026-09-09T22:04:56Z [dispatcher] nothing to land: ticket/DEBT-0011 has no commits and a clean worktree — on to QA as-is
- 2026-09-09T22:06:05Z [qa] built → qa: under attack (re-check r2, fix-scoped)
- 2026-09-09T22:14:30Z [qa] qa → reopened: BUG-0151 filed. Criterion 2 is unmet at one more call site; the bounced one is dead and everything else I re-attacked held. Bounce 2 of 3 — a third sends this to the architect.

BOUNCE CLAUSE DEAD. src/components/review/close/slot.tsx:308-311 now renders <Identifier data-verdict-observation={observation}> on the unlinked arm; the anchored arm at :317 is the stated anchor boundary. BUG-0148's own pin is a strict `it` again (verdict-inline.test.ts, 34 tests green), not `it.fails`, so it is not silently XPASSing.

STORED CHECKS, verbatim, re-run on the landed tree (04b9570) in a detached `_receipt-` worktree BEFORE reading either handoff: offline 72 files / 3157 tests green (3156 before my pin); `rm -rf .next/types && npm run lint` exit 0; `tsc --noEmit` exit 0; `grep -rlo "type-data text-ink" src | wc -l` = 1 (identifier.tsx); no `function TableName|ReviewItems`. Plus `npm run test:http` 4 files / 17 tests green on a real build.

MY OWN SWEEP, DIFFERENT METHOD FROM THE BUILDER'S (he read src; I read what RENDERS). Every route x {populated, empty, sparse} through tests/offline/absence/surfaces.ts, cheerio over the markup, every element carrying `type-data` bucketed: 62 isolated `<Identifier>` renderings, 1801 raw. Categories of the 1801: 1478 data-table `td` (the table's own body class, block box, no sentence around it), 124 anchors, 67 Badge/Chip (`inline-block` — an atomic inline box is its own bidi paragraph already), 62 DATA_MUTED, 40 Eyebrow/h1, 15 text-broken, 13 form controls, 2 the item-header `<p>`. UNEXPLAINED: **0 distinct**. So the builder's 27 source-level sites and my 8 rendered categories agree, with no residue — no 49th hand-faced identifier exists on any rendered surface.

WHAT BOUNCES IT — one of those 62 DATA_MUTED renderings is a machine identifier, not app prose. src/components/evidence/evidence-pair.tsx:73-77 hands the WHOLE claim line `DATA_MUTED` and leaves `claim.source` (the source's own name, shape-views.tsx:270) and `orDash(claim.tier)` (`sources.tier`, per the file's own comment) as bare text nodes inside it. Rendered: `<span class="type-data text-ink-secondary">ticket‮master · official · <span title=…>14d ago</span></span>` — one un-isolated inline box holding foreign data AND the app's own separators and age. The primitive's own doc calls this exactly wrong: ":35 an identifier span is precisely where foreign text lands: … a source's own name"; ":81 A machine identifier takes `<Identifier muted>` instead — never this."

The builder's stated reason for declining these two sites is measurably false. His note: "no single value can be wrapped without either giving app words the identifier's ink or re-splitting the line." Measured: `<Identifier muted>` renders `<span dir="ltr" class="type-data text-ink-secondary">` — the IDENTICAL class string to `DATA_MUTED`. Wrapping `claim.source` changes exactly one thing: `dir="ltr"`. (`:90`, `{canonical.provenance}`, IS a caller-composed sentence with no separable value — his reasoning holds there, and BUG-0151 excludes it.)

The harm is rendered, not theoretical. Chromium (playwright, agenticflow/.venv-tools, 1440x900, per-character Range rects sorted by x): as shipped, logical `ticketmaster · official · 14d ago` draws as `ticketoga d41 · laiciffo · retsam` — the app's own `·`, `official` and `14d ago` reversed by data the app did not author. With the source wrapped: `ticketretsam · official · 14d ago`, reversal contained to the value's box. Same file, same page (/queues/[reviewItemId]), same defect class as BUG-0148 one component over.

PINNED: tests/offline/ui/evidence-pair.test.ts, `it.fails` "…— admin-window/BUG-0151", landed 5a7a85c, in the file that already owns this surface (extended, not spawned). It reads the expected `dir` OFF `<Identifier muted>`'s own render — no class or attribute literal in it. Watched RED as a plain `it` first, twice (before and after a typing fix): 1 failed | 10 passed, so the failure is the assertion and not a broken fixture.

WHAT ELSE I TRIED, AND IT HELD.
- Every `<Identifier>` rendering that is not a bare token: exactly one, the em dash on /queues/[reviewItemId] (evidence-pair `CardValue` = `<Identifier>{orDash(value)}</Identifier>`). Pre-sweep that site hand-spelled the same pair, so the dash was already mono — criterion 3 holds, not a regression.
- Badge/Chip (67 renderings of `data_conflict`, `standing_disagreement`, `awaiting_link`, `official`…): un-isolated, but `inline-block` makes each an atomic inline box with its own bidi paragraph, so the isolation is already there structurally and `Identifier` types the layout classes out anyway. Boundary, not defect.
- The 1478 `td` renderings: the class is on the cell, a block container — a control cannot reach a sibling cell. No sentence to reorder.
- Criterion 1: one file spells the pair. Criterion 4's two fixtures still grade both arms (primitives.test.ts 100 tests green). Criterion 6: full offline suite, lint and tsc all green from my worktree, plus test:http.

RESIDUALS, not tickets. (1) Prior lane's residual stands: the one-owner grep matches the pair only when ADJACENT, so a future `type-data font-normal text-ink` would evade it. (2) The `DATA_MUTED` doc says "four places"; `grep -rn DATA_MUTED src` = six (loading:14, stat-card:76, EditableCell:797, close/form:134, evidence-pair:74, :90). Not mine to fix — it is product source and no criterion makes the count mine — so it rides along in BUG-0151 as a one-word correction for whoever is in the file.
- 2026-09-09T22:26:34Z [builder-223] claimed (attempt 3)
- 2026-09-09T22:40:33Z [builder-223] claimed → built: Both known residuals closed; rebase onto run/admin-window was the expected EMPTY diff, so this ticket is no longer zero-diff — one commit, 6 files.

- (a) `ui/identifier.tsx`'s `DATA_MUTED` doc said "the four places" while SIX sites render it (loading:14, stat-card:76, EditableCell:797, close/form:134, evidence-pair ×2). Replaced the count with the RULE, which cannot go stale: the text is the app's OWN transient words, or the element is a wrapper that inks only the app's own words with every machine value already in its own `<Identifier>` box. No roster to re-count.
- (b) CRITERION 2 DOES REACH the canonical card's provenance line, and I fixed it. Three of its words are machine values, not one: the winning source's own name, `tier_at_apply`, and — on the not-live arm — `observation.status`, which criterion 2 names outright ("a status … value"). `canonicalCard` (`src/app/queues/[reviewItemId]/page.tsx:320`, not shape-views — shape-views only passes the object through) now hands over PARTS as `ProvenanceSegment[]` (`string` = the app's words; `{before?, identifier, after?}` = one machine value the app may qualify either side of, the shape `MicroLabel` already uses), and `evidence-pair.tsx` renders each identifier through `<Identifier muted>` with the app's words as real text nodes beside it. The " · " separator moved INTO the component, shared with the claim line, so a caller can no longer join parts into a sentence.

PROOF. Rendered text byte-identical: md5 `a9f6784450bf992af781d37b79d80ec8` over a fixture exercising all four segment kinds, measured on the pre-fix concatenation (reconstructed in place) and on the fix — the only markup delta is `dir="ltr"` on the values. Both new tests watched RED first against that reconstruction (2 failed | 13 passed, non-vacuous), on the isolation assertions. Pinned in the file that already owns the surface (`tests/offline/ui/evidence-pair.test.ts`), U+202E source fixture included; no class or attribute literal — `dir` is read off `<Identifier muted>`'s own render, and the healthy line's text is compared against the caller's own parts joined by the separator READ OFF the claim line.

RENDERED SWEEP, QA's method (every route × {populated, empty, sparse} through `tests/offline/absence/surfaces.ts`, cheerio, every `type-data`-bearing element bucketed), 24 dumps: **UNEXPLAINED = 0**. 65 isolated (55 `<Identifier>` + 10 `<Identifier muted>`); 1801 raw = 1478 `td` + 124 anchor-or-inside-one + 79 Badge/Chip + 64 DATA_MUTED + 40 Eyebrow/h1 + 13 control + 3 text-broken. Bucket order is td → anchor → control → Badge/Chip → Eyebrow/h1 → text-broken → DATA_MUTED, so it differs from QA's split on overlapping elements; the totals and the 0 are the point. I also dumped the OWN text of all 64 DATA_MUTED renderings (excluding `[dir]` children): every one is app prose — the provenance line now reads ` ·  at apply · applied 10m ago · the claim it applied is now ` with the three values gone into their own boxes. The two elements QA's method counts as anchors and mine first flagged UNEXPLAINED are `item-header.tsx:174` (`link.value` inside an anchor), the site BUG-0148 deliberately did not file.

CHECKS on the rebased tree (onto d287ff4): offline 72 files / 3173 tests green (3171 before my +2; zero existing assertions weakened — three fixtures were migrated to the segment type, and "adds the provenance line" got STRONGER, from a substring literal to each part asserted inside the canonical card). `rm -rf .next/types && npm run lint` 0; `tsc --noEmit` 0; `grep -rlo 'type-data text-ink' src | wc -l` = 1; no `function TableName|ReviewItems`; `npm test` 0; `npm run test:http` 4 files / 17 tests green on a real build; `npm run test:live -- tests/live/review-item.live.test.ts` 12 tests green on staging (read-only), including "puts the fact's current canonical value beside them, from the latest decision" — the provenance path against real rows.

FOR QA. (1) `h(Component, props)` in the offline tests is NOT type-checked — `conflict-actions.test.ts:620` passed a `string` provenance and `tsc` was silent; only the run reddened. That is a live blind spot for every fixture built with `h`, not mine to fix here. (2) Prior residual stands unchanged: the one-owner grep matches the class pair only when ADJACENT.
- 2026-09-09T22:40:57Z [dispatcher] landed ticket/DEBT-0011 onto run/admin-window as 741cd24
- 2026-09-09T22:56:14Z [qa] built → qa: under attack (re-check r3, fix-scoped)
- 2026-09-09T22:56:47Z [qa] qa → done: BOTH BOUNCE CLAUSES DEAD, criterion 2's boundaries hold, closing on merit. BUG-0152 filed P3 (see below) — a NEW divergence, not a fourth missed site, so this is not a third bounce.

STORED CHECKS, verbatim, in a detached _receipt- worktree BEFORE reading either handoff (landed tree 4de1a8e): offline 72 files / 3173 tests green; `rm -rf .next/types && npm run lint && tsc --noEmit` exit 0; `grep -rlo "type-data text-ink" src | wc -l` = 1 (identifier.tsx); no `function TableName|ReviewItems`. After my +4: 3177, and `receipt.py` re-ran all four green on e9b31e1. Plus `npm test` (offline+isolated, 6 isolated) exit 0 and `npm run test:http` 4 files / 17 tests green on a real build.

MY OWN RENDERED SWEEP, independent of the builder's (every route x {populated, empty, sparse} through tests/offline/absence/surfaces.ts, cheerio, every `type-data`-bearing element bucketed, 24 dumps): 65 isolated, 1801 raw = 1478 td + 124 anchor + 79 Badge/Chip + 62 DATA_MUTED + 40 Eyebrow/h1 + 13 control + 3 text-broken + 2 UNEXPLAINED. RECONCILED with the builder's eight buckets exactly: my 2 UNEXPLAINED are `item-header.tsx:144`, the `<p class="type-data … text-ink-secondary">` whose OWN text is "opened 10d ago / last evidence 8d ago / asked again x2" — app prose, no machine value; 62 + 2 = his 64 DATA_MUTED. Real UNEXPLAINED = 0. I also dumped the own text (minus [dir] children) of all 64: every one is app prose, and the provenance line reads " ·  at apply · applied 10m ago · the claim it applied is now " — all three values gone into their own boxes on the real route, including the not-live arm.

THE ANCHOR AND text-broken BOUNDARIES, decided against rows 13/15 rather than accepted. 124 anchor renderings are three shapes: (a) filter chips (`inline-block rounded-control`) whose whole body IS the value (`data_conflict`, `entity_link_source_pattern`, `ticketmaster`, `open`…) — atomic inline box, own bidi paragraph already; (b) the cycle-id in-page anchor, whole body a uuid; (c) `item-header.tsx:174` `<span className=\"type-data\"> {link.value}</span>` inside an anchor whose label PRECEDES it. Putting `<Identifier>` inside any of them is exactly ARCHITECTURE row 13's promoted defect — an ink-bearing span as a link's body overrode the inherited accent and painted over the underline (BUG-0113/0115) — and `Identifier` types `className` out, so it cannot carry the link's ink. Row 15 asks for isolation of foreign text in an app sentence; (c) is the only one with an app word in the box, and the word is BEFORE the value while each `<a>` is a flex item of `p.flex` (its own paragraph), so an unterminated override reaches no app-authored character. Boundary upheld; BUG-0148's decline confirmed by a second method. text-broken (3 renderings: `died`, `failed`, the Postgres refusal verbatim) would change ink if inked from the primitive — structurally impossible anyway.

ATTACKED THE NEW SEAM, and it held. (1) Hostile U+202E TIER on the provenance line, the arm where the app's \"at apply\" sits one text node BEHIND the value: appWords byte-identical to healthy, control contained. (2) Hostile status on the NOT-LIVE arm (`{before}`), rendered by no test before mine. Both now pinned green in tests/offline/ui/evidence-pair.test.ts (extended, not spawned). (3) A separator smuggled in a `string` segment re-joins fine ("songkick · applied 3d ago · injected · official at apply") and a `before`/`after` that is itself a machine word goes un-isolated — both are caller-contract violations the doc reserves for the app's own words; today's only caller obeys. (4) `h()` type gap: a `string` passed for segments THROWS `segments.map is not a function` (pre-fix it rendered) — unreachable from src, which tsc guards. (5) Empty segment array renders an empty span, no caller produces it. (6) Criterion 1: one file spells the pair. Criterion 3: no existing assertion weakened. Criterion 5: `Identifier` types className/dir/style out, so no call site can add a raw value.

WHAT I FOUND — BUG-0152, P3, filed not bounced. `ProvenancePart` wraps `segment.identifier` in `<Identifier muted>` with NO absence guard, so a source with nothing visible in it draws an EMPTY isolated box and announces no absence (`[aria-label=\"no value\"]` count 0; line reads \" ·  at apply · applied 3d ago\"), on all three shapes `isAbsent` calls empty. The CLAIM LINE one card to its left, same file, same value class (`sources.source`), draws the app's dash — the guard BUG-0151 put there. Reachable via `canonicalSideOf`'s `sources.get(id)?.source` from a registry row with a blank name. NOT a regression: the pre-sweep concatenation drew the same nothing, so criterion 3 holds — DEBT-0011 gave the gap a box, it did not create it. Pinned `it.fails` x3, watched RED as a plain `it` first (3 failed | 20 passed, non-vacuous), expectation read off `orDash` and `<Identifier muted>`, no literal.

RESIDUALS, not tickets. (1) The one-owner grep matches the class pair only when ADJACENT — a future `type-data font-normal text-ink` evades it (third lane to record this). (2) `h(Component, props)` is not type-checked anywhere in tests/offline, so every fixture in the suite can drift from its component's props silently; the builder hit it at conflict-actions.test.ts:620. Suite-wide, not this ticket's. [resolution: fixed] [receipt green: 4 cmd(s), tree d219d791]
