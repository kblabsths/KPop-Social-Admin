---
id: TASK-0016
type: task
status: done
milestone: M1
priority: P2
opened_by: architect
parent: FEAT-0005
children: []
assignee: null
claimed_at: null
depends_on: [TASK-0014]
touch_scope: [src/app/cycles/page.tsx, src/lib/db/runs.ts, tests/offline/runs, tests/live/runs.live.test.ts]
force: false
attempts: 1
resolution: fixed
---
# Cycles & runs — the adapter framework's runs half

## Description
Module: `src/lib/db/runs.ts` (new) and the adapter-runs section of
`src/app/cycles/page.tsx` (the seam TASK-0014 left named and empty). Authority:
spec §4 ('`resolution_runs` and the adapter framework's `runs`, newest first,
with the counts as columns and `error_summary` inline'), SPEC F5, acceptance
tests 2 and 9, and **Ben's ruling of 2026-09-02** — recorded as a dated
paragraph in `agenticflow/docs/DECISIONS.md` and in ARCHITECTURE.md §12's
settled paragraph. **The question that held this ticket is answered; it is
startable.**

### The column set — exactly nine, no more, no fewer

`source`, `started_at`, `ended_at` (a row with none is still running and reads
as such), `outcome`, `error_summary` (inline, verbatim, mono), `records_parsed`,
`claims_emitted`, `records_unlinked`, `failure_class`
(`transient | structural | config`).

Migration `20260829000001` gives the table 22 columns. The other thirteen —
`checkpoint_before/after`, `payloads_fetched`, `payloads_archived`,
`records_rejected`, `claims_dropped_empty`, `claims_collapsed`, `claims_ai`,
`records_linked`, `records_escalated`, `batches_written`,
`observations_returned` — are out of M1 scope; adding one re-opens a settled
decision rather than extending a table.

### Reuse the Dashboard's read shape

`/` already reads and renders the first five of these: `DashboardRunRow` /
`readRecentRuns` in `src/lib/db/dashboard.ts` select `run_id, source,
started_at, ended_at, outcome, error_summary`, ordered `started_at desc` then
`run_id desc`, through `readRows` with a clamped limit. `src/lib/db/runs.ts`
takes that same shape and adds the four counts plus `failure_class`. Keep
`run_id` as the row key and the order tiebreak — it is the primary key, not a
tenth displayed column. Do not re-derive anything the Dashboard already owns,
and do not widen `dashboard.ts` (it is another ticket's landed surface and is
deliberately narrower).

**The read stays a WINDOW read** (ARCHITECTURE §4.3). Every number this section
renders is a column of the row it sits in, never a count over the set, so a
bounded newest-first window is honest — and no figure on this page may be
computed from `rows.length`. Do not use `readComplete` here. No `?? 0` on any
read path: a null the database gave is not a zero.

### `?source=<name>` — the Sources page's seam

`/sources` links `/cycles?source=<name>`; today the Cycles page ignores it
silently (QA residual, relayed on this ticket 2026-09-02). `resolution_runs`
carries no source column at all, so the facet narrows **this half only** and the
page says so when it is applied. Match by NAME — `runs.source` is text with no
foreign key, deliberately (§6 trap 6) — never by key, and a source name with no
matching `sources` row is still a run that renders. A facet that matches nothing
yields the section's `empty` state with a 0, never the error state.

### Settled already, do not re-decide

Newest first by `started_at` with a total order; `error_summary` inline and
verbatim; an in-flight run and a `skipped` run each legible as such; the
data-table rule and the four states — with `runs` absent the section renders its
not-provisioned state naming `runs` and the cycles half of the page still works;
an empty result renders `empty` with a real 0 (DECISIONS 2026-09-02, "a counted
zero is a real figure").

### Brief, from the Common-violations ledger

Design tokens only, never raw values. A live test reads the state kind from
`data-state` before it compares a number: `error` is always a FAIL, `empty` is a
pass with a 0 (ledger row 6). Assert behaviour, never rendered wording. Imports
stay inside `src/` (§1.2 as narrowed). This ticket owns
`src/lib/db/runs.ts` and the adapter-runs section of `src/app/cycles/page.tsx`
exclusively — correct the stale "open question" comments in those two files; the
same comments in `src/app/page.tsx`, `src/lib/db/dashboard.ts`,
`src/lib/db/sources.ts` and the Dashboard offline tests are outside this scope
and stay.

Do not read `adapters.md` from a sibling repo path — it is not in this repo's
`contracts/`, and the ruling above is the whole contract for these columns.

## Acceptance criteria
[tests 2, 9] The adapter-runs section of `/cycles` renders exactly the nine
columns Ben ruled on 2026-09-02 — `source`, `started_at`, `ended_at` (a null one
rendered as still running), `outcome`, `error_summary`, `records_parsed`,
`claims_emitted`, `records_unlinked`, `failure_class` — no more, no fewer,
newest first by `started_at`, with `error_summary` inline and verbatim.
`/cycles?source=<name>` narrows this half by source name (never by key) and the
page states that the facet applies to the runs half only; a name that matches
nothing renders the `empty` state with a 0, not the error state. Offline tests
under `tests/offline/runs` render the section against `runs` fixtures and assert:
the nine columns present, no tenth `runs` column, the newest-first order, an
in-flight run, a `skipped` run, the facet applied and the facet unmatched, and —
with `runs` absent — the not-provisioned state naming `runs` while the cycles
half of the page still renders. A live-marked parity test asserts the rendered
rows against a query the test issues itself, reading the state kind from
`data-state` before comparing anything (`error` is a FAIL, `empty` a pass with a
0). `src/lib/db/runs.ts` is a window read per §4.3 — ordered, clamped, limited,
no `readComplete`, no `?? 0` — and no figure on the page is computed from
`rows.length`.

## Checks
```sh
npm run lint
./node_modules/.bin/tsc --noEmit
npm run build
./node_modules/.bin/vitest run tests/offline/runs
bash -c "test -f src/lib/db/runs.ts && grep -q error_summary src/lib/db/runs.ts && grep -q records_parsed src/lib/db/runs.ts && grep -q claims_emitted src/lib/db/runs.ts && grep -q records_unlinked src/lib/db/runs.ts && grep -q failure_class src/lib/db/runs.ts"
```
## History
- 2026-09-02T05:42:18Z [architect] opened: Cycles & runs — the adapter framework's runs half
- 2026-09-02T05:45:32Z [architect] depends_on: [TASK-0014] → [TASK-0014, TASK-0023]: the adapter-runs half cannot be authored until Ben names the column set (TASK-0023); the resolver-cycles half of the page ships without it
- 2026-09-02T08:23:54Z [architect] Architect ruling, 2026-09-02 — read kinds, now ARCHITECTURE.md section 4.3. Cited on every ticket that owns a `src/lib/db/*.ts` module.

QA found `src/lib/db/review-items.ts` (TASK-0006) building its list query with no `.range()`, no `.limit()` and no `.order()`. Against PostgREST's `db-max-rows` (Supabase default 1000) that truncates silently, in unspecified order — so a count or an "exactly the matching items" claim built on it would be wrong rather than refused. You are one of eight builders about to write a `lib/db` module without seeing each other's code; the bound is decided once, in TASK-0026 (now a dependency), not eight times.

Two read kinds. Pick deliberately; do not blur them.

1. **Complete read — `readComplete(missing, run, db?)`, new in `src/lib/db/result.ts`.** Use it whenever the surface presents the result as the whole set: a list the page shows in full, a count, an oldest age, an "exactly the matching items" claim. The `run` callback passes `{ count: "exact" }`, a total `.order()` ending in the primary key, and `.range(0, cap - 1)` with the cap handed in. If the exact count exceeds the rows returned, it returns `kind: "error"` naming the object, the count and the cap. **An `ok` array from a complete read is the whole matching set** — that property is what every figure in this app rests on, and it is why no caller needs a "was this all of it?" flag.
2. **Window read — `readRows`, unchanged.** The caller's own `.order()` + `.limit()` define a named window, and the surface says which window it is showing. This is section 8's gauge contract and TASK-0007's landed gauges use it correctly. A window read's rows must never become a figure presented as a total.

Also: no read helper may substitute a number the database did not give. `readCount` returned `count ?? 0` — BUG-0007's user-visible twin — and TASK-0026 makes it refuse a null count instead. Do not reintroduce a `?? 0` on any read path.
- 2026-09-02T15:31:46Z [dispatcher] Relayed from QA on TASK-0014 (residual): /cycles?source=<name> (the Sources page's seam) is silently ignored by the Cycles page today because resolution_runs has no source column — the facet narrows the adapter-runs half this ticket owns. Honour ?source= on the runs half, or state on the page that the facet applies to runs only.
- 2026-09-02T16:56:43Z [architect] description amended: Ben's ruling 2026-09-02 folded in: the nine-column set is named on the ticket, the '?source=' facet is a requirement rather than a relayed note, and the 'must not be started before it is answered' hold is struck. Read shape reuses src/lib/db/dashboard.ts's DashboardRunRow (source/started_at/ended_at/outcome/error_summary) plus the four counts and failure_class; the read stays a window read (ARCHITECTURE 4.3). [was: Module: `src/lib/db/runs.ts` and the adapter-runs section of `src/app/cycles/page.tsx` (the seam TASK-0014 left named an… (1666 chars; full text in git history)]
- 2026-09-02T16:56:47Z [architect] criteria amended: Was 'exactly the columns Ben's answer names' — unresolvable on the ticket. Now the nine ruled columns are enumerated, plus the ?source= facet (match by name, unmatched = empty not error) and the state-kind rule for the live test. [was: [tests 2, 9] The adapter-runs section renders exactly the columns Ben's answer names — no more, no fewer — newest first,… (498 chars; full text in git history)]
- 2026-09-02T16:56:57Z [architect] depends_on: [TASK-0014, TASK-0023] → [TASK-0014]: TASK-0023 dropped: Ben answered the runs column question 2026-09-02 and the record has landed (ARCHITECTURE 12 marker struck, DECISIONS paragraph written, criteria amended here), so the edge no longer holds anything. TASK-0014 (done) stays — it owns the /cycles page shell and the named seam this ticket fills.
- 2026-09-02T16:57:16Z [architect] checks amended: Widened the runs.ts presence check from error_summary alone to the four counts Ben's ruling added (records_parsed, claims_emitted, records_unlinked, failure_class) — measured exit 1 on today's tree (runs.ts unbuilt), green when the ruled set lands. The 'no tenth column' half stays with the offline tests, where it belongs. Other four checks unchanged. [was: npm run lint, ./node_modules/.bin/tsc --noEmit, npm run build, ./node_modules/.bin/vitest run tests/offline/runs, bash -… (194 chars; full text in git history)]
- 2026-09-02T17:41:48Z [builder-55] claimed (attempt 1)
- 2026-09-02T18:02:14Z [builder-55] claimed → built: Built the runs half per Ben's 2026-09-02 ruling.

- NEW src/lib/db/runs.ts: window read of `runs` (readRows, total order started_at desc + run_id desc, clamped .limit, NO readComplete/range/count, no `?? 0`). `RUN_COLUMNS` is the nine ruled columns in the ruling's own order and `satisfies keyof RunRow`; `RunRow extends DashboardRunRow` (dashboard.ts NOT widened). `?source=` -> `.eq("source", name)`, by name only.
- src/app/cycles/page.tsx: new "Adapter runs" Section between Cycles and the gauges; table generated from RUN_COLUMNS via a Record<RunColumn,…> (a tenth column is a compile error). Four states on `data-state` at `[data-surface="runs"]`; empty renders a labelled literal 0 ("Runs in this window") — no figure anywhere comes from rows.length, and that figure is absent in the ok state (asserted). AskedSource rewritten: narrows the runs half, states so, `data-source-facet` kept.
- Tests: tests/offline/runs/{population,read,page}.test.ts (39) + tests/live/runs.live.test.ts (written, NOT run — it grades `data-state` first: error=FAIL naming the read, empty=pass with 0 + its own count, not_provisioned only if its own read gets PGRST205/42P01).

Verified on the rebased tree: `npm run lint` 0, `tsc --noEmit` 0, `npm run build` 0, `vitest run tests/offline/runs` 39/39, the presence grep 0, and full `npm test` 1505/1505. Red-then-green observed: dropping `.eq` + the sort reddened 7 tests; a smuggled tenth column and a mislabelled empty figure reddened 8.

QA should know:
- Column ORDER follows Ben's enumeration verbatim (error_summary 5th, before the counts), not the cycles table's error-last shape. The set is what "no more, no fewer" pins; order was not stated as a requirement, so I took the ruling's list literally.
- OUT OF SCOPE, unchanged: `?run=<run_id>` (the Dashboard's run lines) still narrows/marks nothing and is documented as such — not in this ticket's criteria.
- Beyond touch_scope, deliberately: `src/lib/db/cycles.ts` — `newestFirst` made generic over `{run_id, started_at}` so both halves share ONE comparator instead of a copied one (its stale OPEN-RUNS clause corrected too); `tests/offline/cycles/page.test.ts` — its two tests pinning the empty seam ("renders nothing at all" / "does not read the runs table") were the absence this ticket fills, so they now assert the boundary instead (4 sections, cycles unchanged), and it mocks `@/lib/db/runs` to stay offline-pure.
- The runs window line and the column-explainer paragraph render only for an `ok` read: a refused/absent read got no window to describe.
- 2026-09-02T18:02:36Z [dispatcher] landed ticket/TASK-0016 onto run/admin-window as 868e403
- 2026-09-02T18:03:33Z [qa] built → qa: under attack
- 2026-09-02T18:11:44Z [qa] qa → done: Attacked on the landed tree (868e403 + my pins). Stored checks re-run VERBATIM by receipt.py: lint 0, tsc 0, build 0, vitest tests/offline/runs 0, presence grep 0 -> receipt GREEN.

CRITERIA, each clause driven myself before reading the handoff:
- Nine columns, ruled order: headers == RUN_COLUMNS offline and against staging. Smuggle probe (edit + tsc + revert, evidence/TASK-0016/tsc-*.txt): a tenth non-row column (payloads_fetched) = TS2322 + TS2741, exit 2; the PRIMARY KEY as a tenth = TS2741, exit 2.
- ?source= by NAME, against real Postgres (live probe, deleted after): exact name -> ok/5 rows; UPPER-case variant -> data-state=empty, figure 0; name + trailing space -> empty, 0; ?source= empty -> no narrowing, no facet sentence, 5 rows; ?source=a&source=b -> first value only, all rendered rows carry it. No case-fold, no trim-into-a-match.
- Empty branch 0 is the literal NO_RUNS (runs.ts/page.tsx read): rows.length appears in the runs half only to CHOOSE the state kind, never as a figure. No ?? 0, no readComplete, no .range, no count:'exact'.
- Clamp boundary via readRuns against staging: limit == row count -> truncated true; limit+1 -> false; limit 0 -> clamped to 1 row/limit 1; limit NaN -> 200.
- ended_at null renders its own in-flight marker, not a dash and not a computed duration; skipped legible as itself; failure_class/error_summary verbatim; runs absent -> not_provisioned naming 'runs' with the cycles half and its figure intact.
- Live: vitest --project=live runs.live (7) + cycles.live (6) = 13 passed, read-only. NON-VACUOUS, checked independently: data-state=ok, 5 rendered == 5 held, 1 distinct source (counts only; no row values recorded).

NEW PINS (committed 0b39b09, tests/offline/runs/page.test.ts, 39 -> 41): the ROW's foreign text, which no test covered - only the URL's. An outcome ('abandoned') and failure_class ('quota') outside the migration's check constraints render verbatim, never narrowed and never the em dash; source/outcome/failure_class/error_summary carrying markup reach the document as text (0 script, 0 img elements) and stay legible. Both watched: they fail if the fixture's narrow union is trusted over the column's text type.

GUARD WATCHED RED: the one tenth-column shape tsc accepts - a DUPLICATE of one of the nine - reddens 6 offline tests (column set, no-tenth, order, truncation, facet, half-independence). The 'no tenth column' criterion is pinned by tests where the compiler stops.

OUT-OF-SCOPE CHANGES, ruled: (1) newestFirst made generic in src/lib/db/cycles.ts - body byte-identical, one comparator for both halves instead of a copy, cycles offline suite 33/33 green: ACCEPT. (2) tests/offline/cycles/page.test.ts's two seam tests re-pointed - their old assertions ('renders nothing at all', 'does not read the runs table') asserted this ticket's ABSENCE; the replacements assert the same boundary that still exists (four sections, runs surface in its own state, cycles unchanged) and the file mocks @/lib/db/runs to stay offline-pure: ACCEPT, not a weakened test.

Residuals in the handoff, none of them this ticket's defect. [resolution: fixed] [receipt green: 5 cmd(s), tree 33fee548]
