---
id: TASK-0016
type: task
status: open
milestone: M1
priority: P2
opened_by: architect
parent: FEAT-0005
children: []
assignee: null
claimed_at: null
depends_on: [TASK-0014, TASK-0023]
touch_scope: [src/app/cycles/page.tsx, src/lib/db/runs.ts, tests/offline/runs, tests/live/runs.live.test.ts]
force: false
---
# Cycles & runs — the adapter framework's runs half

## Description
Module: `src/lib/db/runs.ts` and the adapter-runs section of `src/app/cycles/page.tsx` (the seam TASK-0014 left named and empty). Authority: spec section 4 ('`resolution_runs` and the adapter framework's `runs`, newest first, with the counts as columns and `error_summary` inline'); SPEC F5 and SPEC named gap 1; acceptance tests 2, 9.

**This ticket waits on a blocked question and must not be started before it is answered.** `adapters.md` is not among the contract snapshots, so which of `runs`' columns this page shows is unspecified. The table itself is real and readable — migration `20260829000001` in the scraper repo gives: `run_id, source (text, no FK), started_at, ended_at, outcome (succeeded | partial | failed | skipped), failure_class (transient | structural | config), checkpoint_before, checkpoint_after, error_summary, payloads_fetched, payloads_archived, records_parsed, records_rejected, claims_emitted, claims_dropped_empty, claims_collapsed, claims_ai, records_linked, records_unlinked, records_escalated, batches_written, observations_returned` — twenty-two columns, and a table showing all of them answers nothing.

Build exactly the column set Ben's answer names. Everything else here is settled: newest first by `started_at`; `error_summary` inline and verbatim; a run still in flight (`ended_at` null) and a `skipped` run each legible as such; `runs.source` joins to `sources` **by name**, never by key (no FK exists, deliberately); the data-table rule and the four states; nothing settles anything.

Do not add a column the answer does not name, and do not read `adapters.md` from a sibling repo path — it is not in this repo's `contracts/`.

## Acceptance criteria
[tests 2, 9] The adapter-runs section renders exactly the columns Ben's answer names — no more, no fewer — newest first, with `error_summary` inline. Offline tests render the section against `runs` fixtures and assert the column set, the order, an in-flight run and a `skipped` run. A live-marked parity test asserts the rendered rows against a query the test issues itself. With `runs` absent the section renders its not-provisioned state naming `runs` and the cycles half of the page still works.

## Checks
```sh
npm run lint
./node_modules/.bin/tsc --noEmit
npm run build
./node_modules/.bin/vitest run tests/offline/runs
bash -c "test -f src/lib/db/runs.ts && grep -q error_summary src/lib/db/runs.ts"
```

## History
- 2026-09-02T05:42:18Z [architect] opened: Cycles & runs — the adapter framework's runs half
- 2026-09-02T05:45:32Z [architect] depends_on: [TASK-0014] → [TASK-0014, TASK-0023]: the adapter-runs half cannot be authored until Ben names the column set (TASK-0023); the resolver-cycles half of the page ships without it
