---
id: BUG-0231
type: bug
status: built
milestone: M3
priority: P3
opened_by: qa
parent: null
discovered_from: DEBT-0022
children: []
assignee: builder-331
claimed_at: 2026-09-15T06:45:48Z
depends_on: []
touch_scope: [src/app/claims/page.tsx]
force: false
attempts: 1
---
# Three comments on /claims still describe a mechanism the code no longer has: two 'head count' paraphrases and one definition of source: null

## Description
DEBT-0022 (landed be2bb27d) removed four truth residuals of one class. Its own pass left three more in one of its files, two of them created by the change it made.

EXPECTED (DEBT-0022 criterion 4): every comment in src/app/claims/page.tsx states the shape the read actually has — BUG-0210 made `countRead` (a GET with `{ count: "exact" }` over `limit 0`, src/lib/db/result.ts:1430) the one count spelling, and ARCHITECTURE §4.3 retired `head: true` for shipped reads.
FOUND: the criterion enumerated SIX literal `head: true` mentions and the builder fixed exactly those six (grep count is 0, criterion met). Two PARAPHRASES of the same retired mechanism survive, describing the same bucket-table reads:
  - src/app/claims/page.tsx:688 — "**The ruling is LABEL, not equalise.** Capping the head counts to the window would make the page's totals wrong…". Four lines above it, in the SAME JSDoc paragraph, the builder's new sentence correctly calls those reads "`countRead` counts — `{ count: "exact" }` over `limit 0`". One paragraph, two spellings of one read.
  - src/app/claims/page.tsx:1101 — "The standing tab draws no bucket table, so nothing here collides with a head count". Same reads (readClaimCount → countRead, src/lib/db/claims.ts:367-379); no HEAD request exists on this page.
This is LESSONS 4's shape: the criterion enumerated the spellings it thought of, so the fix landed on the literal string and not on the fact.

THIRD RESIDUAL, same class, same file, introduced by DEBT-0022's own semantic change:
EXPECTED: src/lib/gauges/standing-disagreements.ts:59-70 (the builder's new JSDoc) states that SourceSplit.source is `null` where the registry NAMED NOTHING — no row for the id, OR a row whose name has no ink — and that the field "answers neither on its own".
FOUND: src/app/claims/page.tsx:1064, five lines above the code that consumes it, still says "`source: null` there means that read returned no row for it" — the pre-DEBT-0022 meaning, and one of the two mechanisms only. The comment's conclusion ("a named split becomes an entry and an unnamed one contributes nothing, which is exactly the input `sourceLabel` answers with the id") is still true; its premise is not.

REPRO (static, no server), from the repo root:
  grep -n 'head count' src/app/claims/page.tsx                                  # → 2 hits, :688 and :1101
  grep -n 'means that read returned no row for it' src/app/claims/page.tsx      # → 1 hit, :1064
  sed -n '676,690p;1062,1070p;1095,1103p' src/app/claims/page.tsx               # read each in context
  sed -n '55,72p' src/lib/gauges/standing-disagreements.ts                      # what source: null means today

IMPACT: prose only — no behaviour changes, no user-visible defect. It is filed because this campaign has paid for this class repeatedly (BUG-0152/54/56/58/59 on 'unnamed source'; BUG-0210 on the count spelling) and because a comment that names one of two mechanisms is how the next lane re-learns the wrong rule. Fix is three sentences at their owners; no test accompanies it — grading prose with machinery is banned, and the greps below are the same check form DEBT-0022 itself shipped. The builder still runs the page's offline suite (./node_modules/.bin/vitest run --project=offline tests/offline/claims/page.test.ts) before pushing; it is not a stored check here because this ticket's scope touches no test file.

NOTE FOR THE FIXER: the surviving legitimate `head: true` mention in this repo is ARCHITECTURE.md §6 trap 12's; it is not this ticket's, and nothing here should reintroduce the phrase "head count" in src/app/claims/page.tsx — say what the read is (`countRead`), as the other six mentions now do.

## Acceptance criteria
1. No comment in src/app/claims/page.tsx describes a `countRead` read as a HEAD count, in any spelling — the literal `head: true` (already 0) and the paraphrase "head count" alike. Each of :688 and :1101 states what its reads are, in the vocabulary the same file already uses four lines above (`countRead`, `{ count: "exact" }` over `limit 0`).
2. The comment above StandingGauge's `names` map (:1062-1069) states what `SourceSplit.source === null` means TODAY — the registry named nothing for the id, by no row or by a row with no ink in its name — in one sentence, agreeing with src/lib/gauges/standing-disagreements.ts' own JSDoc. Its existing conclusion about `sourceLabel` is kept; nothing else in the block changes.
3. Prose only: no behaviour change, no new test, no sentence a user reads is touched. `tsc --noEmit` 0, `npm run lint` 0, and tests/offline/claims/page.test.ts is green with nothing rewritten.

## Checks
```sh
test "$(grep -c 'head count' src/app/claims/page.tsx)" -eq 0
! grep -q 'means that read returned no row for it' src/app/claims/page.tsx
./node_modules/.bin/tsc --noEmit
npm run lint
```

## History
- 2026-09-15T06:41:59Z [qa] opened: Three comments on /claims still describe a mechanism the code no longer has: two 'head count' paraphrases and one definition of source: null
- 2026-09-15T06:45:48Z [builder-331] claimed (attempt 1)
- 2026-09-15T06:48:37Z [builder-331] claimed → built: Comment-only, one file, one commit 662d79cb on ticket/BUG-0231 (rebased onto run/admin-window 66ae7933).
- :688 and :1101 no longer paraphrase the retired HEAD count: both now name the bucket table's `countRead` counts, `{ count: "exact" }` over `limit 0` of the whole narrowing — the same vocabulary the JSDoc four lines above already used. The 688 paragraph was reflowed (one sentence joined) so the edit does not leave a ragged line; no other sentence in it changed.
- The StandingGauge `names` comment now says `source: null` means the read NAMED NOTHING for the id — no row for it, or a row whose name has no ink — citing campaign admin-window/DEBT-0022 and pointing at lib/gauges/standing-disagreements.ts' JSDoc as the owner. Its `sourceLabel` conclusion is kept verbatim.
- Verified on the REBASED tree: grep -c 'head count' = 0; 'means that read returned no row for it' absent; ./node_modules/.bin/tsc --noEmit = 0; npm run lint = 0; ./node_modules/.bin/vitest run --project=offline tests/offline/claims/page.test.ts = 226 passed. No test written or rewritten (prose ticket) and no runtime path touched.
Traps for QA: (1) the worktree has no node_modules of its own — I symlinked the primary's to run tsc/lint/vitest and removed the symlink before handoff, so re-run from a checkout that has one. (2) criterion 2 says nothing else in that block changes, so I left the pre-existing BARE 'BUG-0158' citation on the next line uncampaign-qualified; it is out of this ticket's scope, flagging it rather than silently widening. (3) the new :1101 text sits inside a JSX {/* */} comment and contains braces and quotes — tsc/lint/build parse it fine, confirmed above.
- 2026-09-15T06:48:56Z [dispatcher] landed ticket/BUG-0231 onto run/admin-window as fb70e793
