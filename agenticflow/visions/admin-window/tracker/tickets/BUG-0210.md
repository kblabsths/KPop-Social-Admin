---
id: BUG-0210
type: bug
status: built
milestone: M3
priority: P2
opened_by: verifier
parent: null
children: []
assignee: builder-310
claimed_at: 2026-09-11T21:08:22Z
depends_on: []
touch_scope: [src/lib/db/result.ts, src/app/claims/page.tsx, tests/http]
force: false
attempts: 1
---
# Against a real PGRST205 database /claims leaks a developer sentence: 'a count read requires { head: true, count: "exact" }'

## Description
M3 endgame verification, 2026-09-11, HEAD bed26500. Found outside the test harness — the offline absence proof cannot see it, because it never speaks HTTP.

REPRO FROM A CLEAN START:
1. Stand up a stub that answers every PostgREST request with 404 and the PGRST205 body (46 lines of python; the walk used one on 127.0.0.1:8777 answering
   {"code":"PGRST205","details":null,"hint":null,"message":"Could not find the table 'public.<t>' in the schema cache"}).
2. ( set -a && source .env && set +a && SUPABASE_URL="http://127.0.0.1:8777" SUPABASE_SERVICE_ROLE_KEY="stub" AUTH_URL="http://localhost:8776" npm run start -- --port 8776 )
3. GET /claims with a walk cookie.

EXPECTED (F2/F10, VISION 'absence is honest'): every panel renders the app's one not-provisioned clause naming its table.
OBSERVED: 200, and the page carries BOTH. The not-provisioned cards are right —
  'pending_claims isn't in this database yet — it arrives with the scraper repo's migrations.' (x3, for pending_claims, sources, observations)
— and beside them one data-state="error" line reads, verbatim:
  'pending_claims — the query returned no count, so the number of rows is unknown; a count read requires { head: true, count: "exact" }.Reload to try the read again.'
That second half is a note to a developer about this app's own call site, rendered to the operator, in the one state the campaign most cares about getting right.

MECHANISM, established against REAL staging rather than guessed:
  curl -I -X HEAD ... '<staging>/rest/v1/verdicts?select=*' with Prefer: count=exact  ->  HTTP/2 404, content-type application/json, content-length 162, and NO body on the wire (HEAD).
  curl (GET) on the same absent table -> 404 with {"code":"PGRST205", ...}.
So the COUNT leg of a read against a missing table cannot carry PGRST205 to the client: a count read is a HEAD, and a HEAD's body never arrives. The row leg classifies correctly (not_provisioned); the count leg falls through to the 'no count' arm. The offline stub client returns structured results and never exercises this, which is why the suite is green and the product is not.

REACHABILITY: any deploy pointed at a project without the ecosystem tables — which is exactly the state 'honest absence beats a crash' exists for, and the state the deployed Railway service is in whenever the scraper's migrations are behind.

## Acceptance criteria
1. Against a database answering 404+PGRST205 to every request over real HTTP, /claims renders no sentence containing 'count read requires', 'head: true' or 'count: "exact"' — the count leg's absence reaches the page as the same not-provisioned clause the row leg does, naming the same table once. 2. The same holds for every surface whose read includes a count leg, asserted over HTTP against that stub, not against an in-process stub client. 3. A count leg that fails for a reason that is NOT absence still reaches the page as an error naming the read and the database's own words — the fix narrows what absence means, it does not swallow errors. 4. The regression lives in tests/http (the tier that speaks HTTP); the offline absence proof is left as it is.

## Checks
```sh
npm run test:http
npm run lint
npm run build
```

## History
- 2026-09-11T19:25:41Z [verifier] opened: Against a real PGRST205 database /claims leaks a developer sentence: 'a count read requires { head: true, count: "exact" }'
- 2026-09-11T21:08:22Z [builder-310] claimed (attempt 1)
- 2026-09-11T21:28:11Z [builder-310] claimed → built: - ROOT CAUSE at the wire, not in the page: a count leg was `{ head: true, count: "exact" }`. Measured on staging 2026-09-11 — HEAD on an absent table = 404, content-length 162, ZERO bytes on the wire; supabase-js parses its error out of the body, finds none, and rewrites to `status 204, error:null, count:null` (postgrest-js PostgrestBuilder, the `404 && body===''` arm), so the absence never reached `classify`. Fix: `countRead(db, object)` in src/lib/db/result.ts is now the ONE count-query spelling — `{ count: "exact" }` + `limit(0)`, GET-shaped. Same staging probe: GET+limit=0 on an absent table returns the full PGRST205 body; on a live table 206, content-length 2, whole count in Content-Range; empty match 200, total 0; never a 416.
- SCOPE: needed src/lib/db/claims.ts and src/lib/db/review-items.ts (the two readCount call sites) beyond touch_scope — the shape is spelled at the call site. src/app/claims/page.tsx needed no change; its prose still says "head: true" in four comments (left alone, as does ARCHITECTURE.md 4.3/7 — flagging for the architect, code and that doc now disagree in wording only).
- VERIFIED: new tests/http/absence.http.test.ts + tests/http/postgrest-stub.ts drive /claims and /queues over real HTTP against a PGRST205 stub that withholds a HEAD's body as the real server does. RED-then-GREEN observed: with only the two db modules reverted to HEAD~1 the same file fails, /claims carrying "pending_claims — the query returned no count, so the number of rows is unknown; a count read requires ..." and 12 HEAD requests at the stub; on the fix, /claims renders 4 not_provisioned clauses (Buckets, All claims, Source names, gauge) and ZERO error states, /queues renders 3, and the stub logs 0 HEAD requests.
- QA TRAPS: (1) the no-count arm in readCount still exists and still says "a count read requires { count: \"exact\" }" — deliberate, it is now unreachable from any shipped read and is a defect report about THIS repo, not about the database; reach it only by writing a count query without count:"exact". (2) The offline pins that keyed "is this a count leg?" on `head === true` were re-pointed at one shared predicate `isCountLeg` in tests/fixtures/stub-client.ts (count:"exact" AND limit(0) AND no head) — none weakened; each still asserts the leg's shape. (3) startServer() now takes `{ dbUrl }`; serverEnv still deletes every *SUPABASE* name first, so no real database is reachable from the http suite.
- CHECKS on the rebased tree: npm run test:http 30/30, npm test 3973+8, tsc --noEmit 0, npm run lint 0, npm run build 0, and live claims+queues 31/31 against staging (bucket counts still render exactly as the view holds them).
- 2026-09-11T21:28:35Z [dispatcher] landed ticket/BUG-0210 onto run/admin-window as 17abae74
