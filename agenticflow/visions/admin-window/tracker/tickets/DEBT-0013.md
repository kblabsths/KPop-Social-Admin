---
id: DEBT-0013
type: debt
status: built
milestone: patch
priority: P2
opened_by: architect
parent: null
children: []
assignee: builder-224
claimed_at: 2026-09-09T22:41:38Z
depends_on: []
touch_scope: [src/components/records/entity-picker.tsx, tests/offline/records/entity-picker.test.ts]
force: false
attempts: 2
empty_diffs: 1
---
# The entity picker manages no focus at all, and its hint promises an Escape that only works while focus is inside the panel

## Description
M2 structure walk finding (architect, 2026-09-09), measured on run/admin-window.

The picker and `EditableCell` are the app's two inline-edit widgets and BUG-0111/0119 deliberately made them share their status clock and the page's one refusal slot. They do not share the third thing an edit widget owns: where focus is.

**The measurement.** `grep -c '\.focus()'`: `src/components/EditableCell.tsx` = 2, `src/components/records/entity-picker.tsx` = 0. The cell's own docstring (:49) states the contract it keeps — 'an edit the operator ends with Enter or Escape puts focus back on that button'. The picker never moves focus into the panel it opens and never restores it to the Choose button when the panel closes or a choice is refused.

**The half-promise.** `PICKER_HINT` (:153) is shown where the operator is about to choose and says an Escape cancels, without qualification. The Escape handler (`:643-656`) is bound to the OPEN PANEL's `onKeyDown`, so it fires only while focus is inside the panel. BUG-0119's fix comment (:536) records the state this leaves: 'a refusal leaves the panel open with focus wherever the operator put it'. After a refusal, an Escape from outside the panel retires the red line (the page-wide rule BUG-0119 armed) and leaves the panel open — two different meanings for one key, from one sentence that promises one.

LOOK_AND_FEEL bar 9: 'Every action on every page is reachable by Tab with a visible focus ring, and no verdict, sort, or edit is available only on hover.' A panel that opens without receiving focus is reachable only by tabbing forward through however many rows the search returned, and a panel that closes without returning focus drops the operator at the top of the document.

**What is NOT in this ticket.** The wording of `PICKER_HINT` is the designer's jurisdiction, not a criterion here — the ticket fixes the behaviour so that whatever the hint says can be true, and the walk grades the sentence.

## Acceptance criteria
1. Opening the picker moves keyboard focus into the panel, at the place the operator will act (the search field). Closing it — by the Choose button, by a choice that lands, or by Escape — returns focus to the Choose button, the contract `EditableCell` already keeps.
2. Escape means one thing wherever focus is while the panel is open: it closes the panel. An Escape pressed after a refusal, from inside or outside the panel, produces the same outcome, so the hint beside the control cannot be half-true. A refusal still retires through the page's one refusal slot and `retiresRefusal`'s single answer — BUG-0119's ruling is not reopened, and a retirement still does not close the panel on its own.
3. Focus is never dropped to the document: after a refusal, after a save that lands, and after a save that is superseded by another widget, focus is on a real focusable element of this widget.
4. Look and feel (LOOK_AND_FEEL, 'Inputs and inline edit' and bar 9): every focusable element of the picker shows the 2px accent outline at 1px offset on keyboard focus, with no `outline: none` anywhere in the widget; the whole choose-search-pick-confirm path is completable by Tab and Enter alone, with no pointer. The panel's focus ring and both themes at every supported width are (human-checkable) at the walk.
5. Every case BUG-0097 (a second click during an in-flight save), BUG-0102 (the picker live during a settlement), BUG-0111 (the confirmation clock) and BUG-0119 (the refusal's lifetime and the page's one slot) pinned still holds — this ticket adds focus, it does not re-decide any of them.
6. `npm run lint` and `tsc --noEmit` clean.

## Checks
```sh
./node_modules/.bin/vitest run --project=offline tests/offline/records tests/offline/ui
bash -c "rm -rf .next/types && npm run lint && ./node_modules/.bin/tsc --noEmit"
grep -q "\.focus()" src/components/records/entity-picker.tsx
! grep -rq "outline: none\|outline-none" src/components/records/entity-picker.tsx
```

## History
- 2026-09-09T18:58:58Z [architect] opened: The entity picker manages no focus at all, and its hint promises an Escape that only works while focus is inside the panel
- 2026-09-09T21:40:29Z [builder-218] claimed (attempt 1)
- 2026-09-09T21:54:54Z [builder-218] claimed → built: Focus and Escape, as two exported pure rules the render suite grades; branch ticket/DEBT-0013 rebased on run/admin-window (d7fba8e).

- `pickerFocus({open, was, status, adrift})` — panel opens focused on its search field; the write that disables every option (BUG-0097) parks focus on the search field (the widget's one live control, and INSIDE its box, so retiresRefusal still reads it as 'not walking away'); closing by toggle, landed choice or Escape returns focus to the Choose button. `saving` waits for the button to be real and 'anything the operator has since focused is left alone' are focusVerdict's own orderings, not new ones. Acts on the open TRANSITION, so a shift-Tab back to the toggle is not a focus trap. `focusIsAdrift` is now EXPORTED from EditableCell.tsx (one `export` + widened param, outside touch_scope) so both widgets share one reading of 'focus is on nothing' — the same move BUG-0111/0119 made.
- `closesPanel(move)` — Escape closes the panel, every other move armRetire carries does not, so BUG-0119's 'a retirement does not close the panel' is code, not a comment. Armed over the SAME armRetire/domRetireHost the refusal rule uses; the panel's `onKeyDown` is deleted, so Escape has one path and one meaning from anywhere. One Escape after a refusal now retires the red line AND closes the panel, identically from inside and outside.
- Trap for QA: the one window where this widget holds no focus is Escape pressed WHILE a write is in flight — the panel unmounts and the toggle is disabled, so focus waits on the body until the answer arrives and then lands on the toggle. That is the cell's own BUG-0069 'saving waits' ordering, pinned explicitly ('waits for the Choose button to be real'). Also: Escape closes EVERY open panel on the page — moot today, EDIT_CONFIG allows at most one reference column per table, so a page draws at most one picker.
- Verified on the rebased tree: `vitest run --project=offline tests/offline/records tests/offline/ui` 619 pass; `npm test` 3153+6 pass; `npm run lint` + `tsc --noEmit` clean; `npm run build` green; both greps pass. Red-then-green: pre-fix source (git show HEAD^) reddens 12 of the new tests incl. the structural one on the real old `onKeyDown` line; four mutations of the landed rules each redden behaviourally (open→'leave' 2 fail, close→'leave' 3 fail, closesPanel→always-true 2 fail, busy-leg→'leave' 2 fail). No browser walk: the picker needs settle_review_item, absent on staging by design.
- 2026-09-09T21:55:18Z [dispatcher] landed ticket/DEBT-0013 onto run/admin-window as a0ba886
- 2026-09-09T22:03:05Z [qa] built → qa: under attack
- 2026-09-09T22:04:55Z [qa] qa → reopened: BUG-0149 filed. Criterion 3 is false on the landed tree.

Attacked: re-ran the four stored checks verbatim on the landed tree before
reading the builder's story (vitest offline records+ui 619 pass; lint + tsc
clean; both greps 0). Then drove the exported rules directly.

**Broke it — criterion 3.** Dumped `pickerFocus` over its whole transition table
(open x was x {idle,saving,saved,failed} x adrift = 32 rows). Two rows are
reachable and wrong:

    open=1 was=1 failed adrift=1 -> leave
    open=1 was=1 idle   adrift=1 -> leave   (the superseded leg)

Expected (criterion 3): "after a refusal, and after a save that is superseded by
another widget, focus is on a real focusable element of this widget."
Found: `document.activeElement === document.body`, at
src/components/records/entity-picker.tsx:387-390 — the no-transition branch
rescues focus for `saving` and for no other status, and a refusal keeps the panel
open (BUG-0119) so `open !== was` at :382 never fires.

Repro: open the picker, activate a row, and while the PATCH runs press any
non-control part of the panel (the hint line, the window line, the gap between
rows). The press is inside the box so `retiresRefusal` rightly ignores it, and
neither `open` nor `status` changed so the focus effect (:761, deps
`[open, status]`) does not run — focus sits on the body. The refusal then lands
and the rule says "leave". The operator's next Tab restarts at the top of the
document, which is the defect this ticket exists to kill, at the moment they most
need to act.

**Why 12 new tests missed it**: `pickerShell().settle()` ran the focus effect
after EVERY move while the shipped effect sleeps unless `open` or `status`
changed, so the model re-parked focus before the answer arrived. I corrected
`runFocus()` to honour the shipped deps — 67/67 pre-existing tests in the file
still pass under the correction, so the gap was the model's alone. Pinned as two
strict `it.fails` (296bdda); un-`fails` them when the rule is fixed. Do NOT fix by
deleting `&& status.kind === "saving"` from :389 — that reddens the existing
"steals no focus from a page that has merely drawn a picker" pin, which needs
open/was/idle/adrift to stay "leave". See BUG-0149 for the reasoning.

**Held under attack** (everything else I tried):
- Escape from the toggle, from inside the panel, from elsewhere on the page, and
  with a refusal showing: one meaning everywhere. `armRetire`'s keydown is a
  capture-phase document listener, so no target can swallow it; the panel's
  `onKeyDown` is gone (0 `onKeyDown` lines in the file).
- Escape with a refusal showing AND the panel open: one keypress does both —
  retires the red line and closes the panel — identically from either side. Both
  arms are independent, so their registration order (refusal effect at :703
  before the panel effect at :739 in React; reversed in the harness) cannot
  matter.
- shift-Tab search -> toggle, then Tab forward: no bounce. `pickerFocus` acts on
  the `open` transition, and the `focusin` it raises is `inside: true` so it
  retires nothing. No trap.
- Cross-widget seams: the picker's own `.focus()` calls land inside `root`, so
  they never retire its own refusal; and every path on which they reach outside a
  cell's box was already earned by an operator press or Tab into the picker.
- The "focus waits on the body" window (Escape mid-write): judged honest, not
  bounced. The toggle is `disabled` for the write and the search field has
  unmounted, so the widget owns no control able to hold focus; criterion 3 does
  not name that moment; and it is pinned explicitly in the suite rather than
  glossed.
- "Escape closes every open panel on the page" is moot at the type level, not by
  convention: `EDIT_CONFIG` types `reference: ReferenceColumn | null`
  (src/lib/edit/config.ts:185) — at most one per table, one record per page.
- BUG-0097/0102/0111/0119 suites byte-unchanged: `git show d7fba8e --numstat` on
  the test file is `464 0` — zero deletions.

Criteria 1, 2, 5, 6 hold. Criterion 4's ring/theme half is human-checkable at a
walk and the picker needs `settle_review_item`, absent on staging; its offline
half (nothing taken out of the tab order, every control natively focusable) is
green.
- 2026-09-09T22:41:38Z [builder-224] claimed (attempt 2)
- 2026-09-09T22:46:25Z [builder-224] claimed → built: Zero-diff re-prove: fixed by admin-window/BUG-0149. ticket/DEBT-0013 rebased onto run/admin-window (4bde0f8) and is now byte-identical to it — `git diff run/admin-window -- src tests` is empty, no commit of my own. No product code changed; no criterion still failed.

- All four stored checks green on that exact tree: `vitest run --project=offline tests/offline/records tests/offline/ui` 636 pass (was 619 at attempt 1 — BUG-0149's pins); `rm -rf .next/types && npm run lint && tsc --noEmit` exit 0; `grep -q '\.focus()'` exit 0 (4 call sites); the outline-none grep exit 0. `npm test` 3173 + 6 pass, exit 0.
- Criteria re-read against the tests that own them: 1 and 2 in 'Escape means one thing wherever focus is' and 'where the picker leaves focus' (open→search; toggle/landed-choice/Escape→toggle; `closesPanel` true for escape and false for all five other `RetireMove`s; 0 `onKeyDown` lines in the file). 3 now via BUG-0149's `wasStatus` edge — QA's two reachable bad rows are pinned as plain `it`s plus the whole-table 'puts adrift focus on a live control at every edge that ends a write' and its negative twin. 5: BUG-0097/0102/0111/0119 suites all present and green in the same 75-test file. 6 is stored check 2. 4's offline half holds structurally — every control is a native button/input, no tabIndex removal, and the 2px accent / 1px offset ring is globals.css's single `:focus-visible` rule that tokens.test.ts's outline-none sweep guards.
- Red-then-green, so the pins are not vacuous: I reverted `pickerFocus`'s post-transition block to the pre-BUG-0149 saving-only rescue in the worktree — 8 of 75 redden, including both named criterion-3 pins — then `git checkout --` restored it and the file is 75/75 green again. Tree left clean.

For QA: no walk. The picker renders only where `settle_review_item` is installed, absent on staging by design, so criterion 4's ring-and-theme half stays human-checkable at the walk, exactly as attempt 1 and BUG-0149 left it. The one honest gap both rounds recorded is unchanged: Escape pressed WHILE a write is in flight parks focus on the body until the answer arrives (panel unmounted, toggle disabled), pinned explicitly rather than glossed. [EMPTY DIFF: no file changed under scope — 1 so far]
- 2026-09-09T22:46:46Z [dispatcher] nothing to land: ticket/DEBT-0013 has no commits and a clean worktree — on to QA as-is
