---
id: TASK-0037
type: task
status: done
milestone: patch
priority: P2
opened_by: strategist
parent: null
discovered_from: inbox:2026-09-03-walker-sandbox-table.md
children: []
assignee: null
claimed_at: null
depends_on: []
touch_scope: [tests/walk, tests/live, agenticflow/docs/vision/STACK.md, agenticflow/docs/DECISIONS.md]
force: false
attempts: 1
last_failure_sig: 19912cf14e51
same_failure_count: 1
seen_failure_sigs: [19912cf14e51]
human_note: The practice table testers use is now live on the staging database, and it snaps back to the same three starting rows before every walkthrough, so nobody has to edit real catalog data to test the edit screens.
resolution: fixed
---
# Prove the walk sandbox on staging once Ben pastes it, and retire the interim walk-write exception

## Description
**Blocked on Ben, then a small proof.** TASK-0034/0035/0036 built the whole walk-sandbox chain and every one closed done: the `EDIT_CONFIG` entry (`walk_sandbox`, uuid-keyed, `regime: "pre_cutover"`), the record page that renders `not_provisioned` until the table exists, and `tests/walk/reset-sandbox.mts` which DELETEs and re-INSERTs the checked-in fixture through PostgREST before every walk. What has never happened is the table existing.

**What Ben does, once:** paste the SQL block from `agenticflow/tracker/for-human/TASK-0034.md` into the STAGING Supabase SQL editor. Staging only, never production. The ids are uuids — `00000000-0000-4000-8000-00000000000{1,2,3}` (DECISIONS 2026-09-04) — because `isRecordId` gates every record page before any read and a text-keyed table made both of §9.1's required states unreachable.

**What this ticket proves, after he pastes** — and only after; it stays blocked until then:
1. `/records/walk_sandbox/00000000-0000-4000-8000-000000000001` renders the record's OK state with its editable cells, not the `not_provisioned` card and not the "that is not an id" empty state.
2. `tests/walk/reset-sandbox.mts` runs green against staging and leaves the three fixture rows exactly as checked in — run it, change a value through the UI, run it again, confirm the value is back.
3. The guard still refuses out loud: with the staging names unset, or pointed at any host other than the declared target, the reset tool exits non-zero naming the missing name. This is the half that must never silently pass.
4. Production is untouched: no DDL from any code path here, and the paste is Ben's alone.

**Why it matters now:** the sandbox RETIRES the interim walk-write exception (DECISIONS 2026-09-03) that currently lets a walker edit one field of one real `groups`/`idols` row and restore it. Until it lands, every M2 walk that wants to exercise the edit cell is writing to the real catalog under a note-restore-sweep discipline. M2 changes the edit cell twice (FEAT-0011's resting affordance and open-with-value-selected), so the walks that grade those changes want a sandbox, not the catalog.

If Ben has not pasted it by the time M2's endgame walks run, that is not a blocker for M2 — it is a narrower walk, stated as such in the walk report. Do not work around it, do not create the table from code, and do not add a `pg` driver.

## Acceptance criteria
1. (blocked until Ben pastes) The walk-sandbox record page at the seeded uuid renders its OK state with editable cells — not `not_provisioned`, not the not-an-id empty state.
2. `tests/walk/reset-sandbox.mts` runs green against staging; after an edit through the UI, a second run restores the checked-in fixture exactly (verified by re-reading the row).
3. With the staging names unset, and with the target pointed at any undeclared host, the reset tool exits NON-ZERO naming the missing name — the failure mode is a loud red, never a silent green. Both cases run.
4. No DDL runs from any code path in this repo; no `pg`/`postgres.js` dependency is added; `STAGING_SUPABASE_DB_URL` remains a name nothing under `src/` or `tests/` reads.
5. STACK.md's walk recipe states that the sandbox exists and is the ONLY write surface a walk has. The interim `groups`/`idols` exception is not retired here — TASK-0041 already retired it on Ben's 2026-09-08 amendment — so this ticket adds no second retirement and restores no catalog-write branch. The amended launch line is pasted into a fresh shell and run before it is written down (LESSONS class 6).
6. `npm test` green, and `tests/live/edit.live.test.ts` green in whatever form TASK-0040 left it.

## Checks
```sh
bash -c 'npm run test:live -- tests/live/edit.live.test.ts'
bash -c 'npm test'
```

## History
- 2026-09-04T09:13:11Z [strategist] opened: Prove the walk sandbox on staging once Ben pastes it, and retire the interim walk-write exception
- 2026-09-09T01:09:25Z [strategist] criteria amended: Reconciled to Ben's 2026-09-08 vision amendment. Criterion 5 no longer retires the interim groups/idols walk-write exception — TASK-0041 does that now, on the amendment rather than on the paste — so this ticket only records that the sandbox is the sole write surface. Criterion 6 no longer assumes edit.live.test.ts still exercises a groups/idols write (TASK-0040 inverts it). [was: 1. (blocked until Ben pastes) The walk-sandbox record page at the seeded uuid renders its OK state with editable cells —… (1033 chars; full text in git history)]
- 2026-09-09T01:09:26Z [strategist] URGENCY, after the 2026-09-08 amendment: with groups/idols leaving EDIT_CONFIG (TASK-0040) and the interim catalog-row exception withdrawn (TASK-0041), walk_sandbox becomes the walkers' ONLY write surface. Until Ben pastes it, a save-path walk has no write surface at all and says so in its report as a narrowed walk — that is the honest state, not a blocker, and not a licence to work around anything. This ticket is still blocked on Ben and still P2 by the letter; the strategist cannot set priority and flags to the architect that P1 now looks right, because every save-path walk in M2 depends on it. The M2 endgame walks that grade FEAT-0011's edit-cell affordances are the ones that pay if it never lands.
- 2026-09-09T01:16:50Z [builder-121] claimed (attempt 1)
- 2026-09-09T01:26:27Z [architect] Two facts from 2026-09-08/09 that change this ticket's premise, recorded here rather than acted on: (1) `public.walk_sandbox` IS NOW ON STAGING with rows — Ben pasted the DDL 2026-09-08, builder-118 measured it 2026-09-09 (residue.live.test.ts swept it as 5 of 5 mapped tables, all seven columns present, the row-count floor passed). M2 precondition 3 is satisfied. (2) The interim walk-write exception is DEAD by construction, ahead of this ticket's schedule: Ben struck direct catalog editing on 2026-09-08 and TASK-0040 removes groups/idols from EDIT_CONFIG, so the one field of one groups/idols row that exception permitted can no longer be written by anything. What remains of this ticket is the STACK.md walk recipe pointing at the sandbox, and the retirement note. TASK-0052 carries the live round trip.
- 2026-09-09T01:31:48Z [builder-121] claimed → built: Sandbox proved on staging (ubfjjqlvnpnoborczbdb), rebased onto dc4aa7f (TASK-0041's STACK.md rewrite kept — both conflicts resolved by hand, neither retirement wording touched).

- WALK, production build of this tree on port 8791 (own detached worktree under the gitignored evidence dir, port free on 127.0.0.1/::1/0.0.0.0; server killed, worktree removed): /records/walk_sandbox/00000000-0000-4000-8000-000000000001 renders the OK state — 6 field lines, an edit control on each of the 5 editable columns and NONE on sandbox_id. note rewritten through the cell -> 200, survived reload; second reset restored all 3 rows byte-identical to sandbox-fixture.ts (compared column by column, created_at excepted — default-set, outside the map). Bonus: clearing label shows Postgres 23502 verbatim, reverts the cell, leaves the value intact. Reset itself: exit 0, deleted 3 seeded 3. Both refusals still exit 1 having written nothing (names unset; SUPABASE_URL at an undeclared host).
- CODE: tests/live/edit.live.test.ts gains a 'the walk sandbox' block at the foot (reset -> PATCH -> read back from DB and from the page -> reset in a finally -> assert fixture identity; plus a forged-column refusal). Its undo is resetSandbox, not withSweep — noted in the file header. Uses EDIT_CONFIG[table].editable only, so TASK-0040's regime change does not touch it. Red-then-green observed twice: dropping the restoring reset reddens the identity assert; adding sandbox_id to the control loop reddens the OK-state assert.
- TRAP for QA: the residue sweep now SCANS walk_sandbox (5 of 5 mapped tables, label=0 note=0) instead of skipping it — I proved both fixtures by writing the marker into walk_sandbox.note (sweep failed naming 'walk_sandbox.note: 1 row(s)') and resetting (passed). Staging is left at the fixture; final reset re-run after everything. STACK.md is at agenticflow/visions/admin-window/docs/STACK.md — the touch_scope path agenticflow/docs/vision/STACK.md is a symlink git refuses to add through.

Verified on the rebased tree: tsc 0, npm run lint 0, npm test 0 (2042+6), npm run test:live -- tests/live/edit.live.test.ts 0 (9/9), npm run test:live -- tests/live/residue.live.test.ts 0 (5/5).
- 2026-09-09T01:32:08Z [dispatcher] landed ticket/TASK-0037 onto run/admin-window as 1697bf8
- 2026-09-09T01:38:26Z [qa] built → qa: under attack
- 2026-09-09T01:53:44Z [receipt] RED receipt: failure signature 19912cf14e51 (same failure 1x)
- 2026-09-09T01:55:52Z [qa] qa → done: ATTACKED, nothing broke. Criteria re-run on the landed tree BEFORE reading the builder note; receipt GREEN (b522db1).

C1 (OK state): own detached worktree under the gitignored evidence dir, own production build (npm run build exit 0), own server on 8793 (probed free with real binds on 127.0.0.1/::1/0.0.0.0; killed, worktree removed, 0B left). GET /records/walk_sandbox/00000000-0000-4000-8000-000000000001 -> 200. aria-labels found: exactly {label,note,tally,is_flagged,observed_on} of walk_sandbox, one each; NONE for sandbox_id, and created_at appears nowhere in the markup. not_provisioned card absent, not-an-id state absent. Rows …0002/…0003 also 5 controls; …dead and "not-a-uuid" draw 0 controls.
C2: STACK.md §5 step 3 block pasted verbatim into a fresh shell -> exit 0, "deleted 3, seeded 3, read back and verified" — the exact string §5 quotes. Then note of …0001 rewritten THROUGH the record page cell (200), reset again, and an INDEPENDENT read (my own client, my own comparison, not the tool s) shows all 3 rows identical to sandbox-fixture.ts over SANDBOX_COLUMNS; the only extra column on the row is created_at.
C3: env -u SUPABASE_URL -u SUPABASE_SERVICE_ROLE_KEY -> exit 1 naming both names. SUPABASE_URL=https://zzzznotdeclared.supabase.co (DUMMY key, never the real one) -> exit 1 naming host and declared target. Unknown flag --dry-run -> exit 2. Nothing written in either.
C4: no DDL in src/tests; no pg/postgres.js in package.json; TASK-0021 s pin command exits 0; supabase/migrations still the 2 app-owned files.
C5: STACK.md says the sandbox IS on staging and is the ONLY write surface; the 2026-09-08 retirement text is attributed to Ben s amendment (TASK-0041), no second retirement added, no catalog-write branch restored.
C6: npm test 2096+6 green; test:live edit.live.test.ts 9/9 green.

ATTACK (integrated, one pass):
- Sabotage: in a scratch worktree copy I deleted the restoring reset from the finally -> "edits a mapped column … and a reset restores the fixture" goes RED on the fixture-identity assert. The pin is not vacuous. File restored, staging reset.
- Interrupt between PATCH and reset: planted "admin-window/QA-TASK-0037 planted residue" into note via the real HTTP PATCH and left it. residue.live.test.ts then FAILED: "walk_sandbox.note: 1 row(s)", after scanning 5 of 5 mapped tables / 58 columns. Next reset -> sweep GREEN, [label=0, note=0]. Both fixtures observed; the sweep genuinely scans this table.
- Forged columns over real HTTP with a minted cookie: sandbox_id -> 403 "sandbox_id is not an editable field of walk_sandbox"; created_at -> 403. Row unchanged.
- NOT NULL clear on label (null AND ""): 23502 surfaced verbatim, HTTP 500, stored value INTACT (page re-read shows "First sandbox row"). Coercion refusals also loud: tally "abc" 22P02, tally 1e20 22003, observed_on 2026-02-30 22008, is_flagged "maybe" 22P02 — none wrote.
- Seams: full npm run test:live 76/76 green; edit+residue run together 3x, 3x green (no in-flight-probe false red). Two concurrent resets: one exit 0, one exit 1 with the documented 23505 message; table left healthy at 3 rows.

RESIDUALS (not filed, dispatcher/architect call): (1) receipt npm test went RED once at 18:52 with ZERO failing tests — vitest "Timeout calling onTaskUpdate" during the isolated project under 5-lane load (one isolated test 53476ms); GREEN on immediate re-run. False-red receipt risk, not reproducible on demand, not TASK-0037 s. (2) tests/live/edit.live.test.ts:145 STILL writes groups and idols on staging (both tests observed passing), while STACK.md now states "No walk and no test writes a groups or idols row again" — TASK-0040 in flight; reported, not fixed. TASK-0037 s own docstring and the DECISIONS entry call the sandbox block "the live suite s ONLY proof a mapped column can be written", which is premature until TASK-0040 lands. (3) The 500 body echoes Postgres "Failing row contains (…)", i.e. the whole row incl. created_at, a column the map deliberately never draws — deliberate per route.ts and LOOK_AND_FEEL, admin-only audience, noted for the architect. (4) The concurrent-reset loser claims "the table is now EMPTY" when it is not (measured: 3 rows) — reset-tool wording, walker-facing only, no user path. [resolution: fixed] [receipt green: 2 cmd(s), tree 819aaf27]
