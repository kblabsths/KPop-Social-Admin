---
id: BUG-0086
type: bug
status: reopened
milestone: M2
priority: P2
opened_by: qa
parent: FEAT-0011
discovered_from: TASK-0053
children: []
assignee: null
claimed_at: null
depends_on: []
touch_scope: [src/components/EditableCell.tsx, src/components/records/record-fields.tsx, tests/offline/ui/editable-cell.test.ts]
force: false
attempts: 2
---
# Edit cell: opening one cell reflows the record table, so the next single click on another editable value is swallowed and the operator must click twice

## Description
Measured by QA on the TASK-0053 attack (2026-09-08), production build of
run/admin-window @ 98c69c0 on 127.0.0.1:8794, bundled Chromium, staging
`walk_sandbox`. NOT a TASK-0053 regression — the diff that landed there
(a text-decoration on the resting button, a ref on the field) cannot move a
box. It is a pre-existing defect of the shared edit cell that TASK-0053's
affordances make reachable and worth fixing: the underline now tells a
mouse-first operator which values they may click, and the second one they
click does nothing.

**Expected** — LOOK_AND_FEEL "Inputs and inline edit": "click a value, it
becomes an input". A click on an editable value opens that value, whether or
not another cell happens to be open. Correcting several fields of one record
is the edit surface's whole job (FEAT-0011).

**Found** — from an open cell, ONE click on another editable value does not
open it. The click focuses the target button and is then swallowed, because
the record table reflows out from under the pointer between mousedown and
mouseup.

Exact numbers, `/records/walk_sandbox/00000000-0000-4000-8000-000000000001`,
viewport 1400x950, light theme:

- at rest, the `note` resting button occupies x=459 y=238, 193x20;
- with `label` open it is at x=358 y=264 — the whole Value column moves
  **101px left and 26px down**, because the open field is full-width and the
  hint line ("Enter or leaving the field saves...") is added to the flow;
- a single human-timed click (mousedown, 120ms, mouseup) at the note button's
  then-current centre (455, 274) leaves `document.activeElement` =
  `BUTTON/note of walk_sandbox` — mousedown hit it — and
  `document.querySelectorAll('input,textarea')` EMPTY: nothing opened. The
  blur committed `label`, React swapped the input back for the button, the
  table snapped back, and by mouseup the pointer was over a different row:
  `document.elementFromPoint(455, 274)` is now `TD "7"` (the `tally` row);
- a SECOND click at the same coordinate opens nothing either and drops
  `activeElement` to `BODY` — that point is dead space once the table has
  snapped back;
- control, same click with nothing open: `note` opens, `activeElement` =
  `INPUT/note of walk_sandbox`. So the swallow is caused by the open cell,
  not by the click.

Reproduced identically with a 0ms click and with the `tally` cell as the
target (focus moved to `BUTTON/tally of walk_sandbox`, nothing opened).

**Risk this carries, stated as a risk and not as a measurement**: the mouseup
landed one row away, on a `<td>`, not on a button. A layout where it lands on
another editable value's BUTTON would open the WRONG field — and TASK-0053
now opens a field with its value selected, so the next keystroke replaces
that wrong field's value. That is the harm FEAT-0011's second affordance
exists to prevent ("a stranger wrote a wrong value into the shared catalog").

**Mechanism**, read off the source and consistent with the measurement:
`EditableCell`'s `onBlur` calls `commit()`, which calls `setEditing(false)`.
React flushes that discrete-event update synchronously, so the edit-mode
subtree — a field that claims the cell's full width plus the
`aria-describedby` hint span — is replaced by the intrinsic-width resting
button before the mouseup is dispatched. The cell's edit-mode and resting
renderings simply do not occupy the same box.

**On pinning it with a test.** This campaign's tiers structurally cannot see
it: `tests/offline` is environment node with `renderToStaticMarkup` and no
jsdom, and there is no browser tier (STACK.md §4 — "no jsdom, no Testing
Library, no Playwright dependency in this campaign"). It is the same class of
browser fact as BUG-0069's focus and TASK-0053's selection, and the house
answer to both is on record: extract the decision as a pure exported unit,
pin the unit offline, and measure the rendered fact in the walk
(`focusVerdict`, `selectOnOpen`). QA wrote no red test for this one and says
so rather than shipping a green one that pins nothing; the fix carries its
own pin plus a walk measurement of the box.

## Acceptance criteria
1. From a cell that is open, ONE click on another editable value on the same
   record opens that value: measured in a walk on a production build against
   staging `walk_sandbox`, `document.querySelectorAll('input,textarea')`
   carries the clicked field's `aria-label` after a single human-timed click,
   and the previously open cell has committed or reverted as it did before.
2. Opening a cell does not move the other cells: the resting bounding box of
   every other editable value on the record is unchanged (0px in x and y)
   while one cell is open, measured in the same walk in both themes.
3. Whatever decides it is a pure exported unit pinned in
   `tests/offline/ui/editable-cell.test.ts`, red against the current code —
   the `focusVerdict` / `selectOnOpen` precedent — rather than a class
   literal asserted out of the markup.
4. TASK-0053's two affordances are unchanged: the resting hairline underline
   still renders on editable values and none on read-only ones, and a cell
   still opens with its whole value selected (input and textarea alike).
5. `npm run lint`, `tsc --noEmit`, the targeted offline file and the record
   page's live suite are green.

## Checks
```sh
bash -c 'rm -rf .next/types && npm run lint && ./node_modules/.bin/tsc --noEmit'
./node_modules/.bin/vitest run --project=offline tests/offline/ui/editable-cell.test.ts tests/offline/records/page.test.ts
npm run test:live -- tests/live/edit.live.test.ts
```

## History
- 2026-09-09T03:34:54Z [qa] opened: Edit cell: opening one cell reflows the record table, so the next single click on another editable value is swallowed and the operator must click twice
- 2026-09-09T03:44:18Z [builder-144] claimed (attempt 1)
- 2026-09-09T04:17:14Z [builder-144] WALK MEASUREMENTS (production build of this branch on 127.0.0.1:8801, bundled Chromium, staging walk_sandbox row ...0001, viewport 1400x950, both themes; sandbox reset before and after).

Resting boxes, light theme, all five editable buttons at x=459.06: label y=205 133x20, note y=238 193x20, tally y=271 15x20, is_flagged y=304 41x20, observed_on y=337 74x20.

1. With `label` OPEN, every other editable button: dx=0, dy=0, dw=0, dh=0 — light and dark alike. Same zero deltas while typing into the open field (the state QA's 101px-left/26px-down was measured in).
2. document.elementFromPoint at note's resting centre (555.5, 248) WHILE label is open = BUTTON[note of walk_sandbox], not a td of another row.
3. ONE human-timed click (mousedown, 120ms, mouseup) there: document.querySelectorAll('input,textarea') = [note of walk_sandbox], activeElement = INPUT/note of walk_sandbox, selection 0..28 (whole value). Both themes.
4. The same click from an EDITED label (typed "First sandbox row edited"): note opens AND label committed — page said "saved", the button reads the new value.
5. The hint is out of the flow and inert: computed pointer-events=none; a click at the hint's own centre reaches TD[false] (the row underneath). Middle line (label): hint at y=231, x 459..852 — inside the Value column (td 451..1144), so the edited row's Provenance column (1144..1383) is never covered. Last line (observed_on): hintSide=above puts it at y=311, fully inside the table's clip box (y 143..363, overflow-y computed 'auto'); below it would have been cut.
6. TASK-0053 unchanged: computed text-decoration on an editable value = "underline 1px", on the read-only pk cell = "none"; input opens selected 0..len, textarea opens selected 0..30 over a 4-line value.
7. MULTILINE boundary, measured on purpose: a note carrying newlines opens a 199x70 textarea that floats over the two rows below and takes clicks inside its own box (elementFromPoint over tally = TEXTAREA), so one click on a value UNDER an open textarea does not open it. Nothing moves (dx=dy=0) and the panel is visible where the old rendering silently pushed those rows down. No walk_sandbox value is multiline; that state was created through the app's own PATCH route and restored to the fixture value.

Sandbox note for whoever walks next: a concurrent lane was writing walk_sandbox during this walk (its value "QA-TASK-0054-direct" was in `label` after my own reset), and one run of tests/live/edit.live.test.ts failed with "walk_sandbox holds no row ...0001" mid-reset; it passed 21/21 on re-run.
- 2026-09-09T04:17:32Z [builder-144] claimed → built: - Layout fix in the shared cell: an open cell puts NOTHING into the row's flow that a resting one does not. The resting value keeps its box (hidden, still sizing the row) and the field + hint are drawn over it, out of the flow; the hint is pointer-inert so a click aimed at the value it hangs over reaches that value, and it flips above on a table's last line where a float below is clipped by DataTable's overflow-x-auto. The pure units are cellLayout / occupiesFlow / hintSide.
- They live in a NEW file src/components/edit-cell-layout.ts, outside the touch scope and outside the "use client" boundary — the first cut had RecordFields (a server component) calling hintSide from EditableCell.tsx and the record page answered 500 ("Attempted to call hintSide() from the server"). tests/offline cannot see that boundary; the walk did. field-editor.tsx also touched (one pass-through prop).
- Proof: 11 new offline tests were RED against the pre-fix component (67 passed) and are green now. Walk on a production build against staging walk_sandbox, both themes: every other editable value moves 0px in x AND y while a cell is open and while it is typed into; one human-timed click on another value opens it with its value selected; label committed as before. Numbers, including the last-line hint's box against the table's clip box, are in the History comment above.
- Commands, all green on the rebased tree (onto a7641ec, TASK-0054 included): npm run lint; tsc --noEmit; vitest --project=offline tests/offline/ui/editable-cell.test.ts tests/offline/records/page.test.ts (171); npm run test:live -- tests/live/edit.live.test.ts (21); npm run test:http (16); full npm test from a detached worktree (66 files, 2436 + 6 isolated).
- Two things for QA. (1) A MULTILINE cell (a value carrying newlines) opens a 199x70 textarea that floats over the two rows below and takes clicks inside its own box, so a click on a value under it does not open that value — measured and documented, nothing moves, and the old rendering pushed those rows down instead. No walk_sandbox value is multiline by default. (2) The same control is the review close form's supplied-value cell (SuppliedControl), so its hint now floats over what is below it there too rather than pushing it down. (3) A concurrent lane was writing walk_sandbox during the walk; one live run failed mid-reset with "walk_sandbox holds no row ...0001" and passed 21/21 on re-run.
- 2026-09-09T04:17:49Z [dispatcher] landed ticket/BUG-0086 onto run/admin-window as 7af0810
- 2026-09-09T04:21:36Z [qa] built → qa: under attack
- 2026-09-09T04:44:16Z [qa] qa → reopened: REOPENED: acceptance criterion 1 is false on the path the ticket exists for. Attacked on the LANDED tree (run/admin-window d2e4623), production build on 127.0.0.1:8805, bundled Chromium, staging walk_sandbox (reset before every run and after the last), viewport 1400x950, both themes. Criteria run before reading the handoff.

WHAT HOLDS, measured myself:
- C2: all five cells opened in turn; every OTHER editable button dx=dy=dw=dh=0 while open AND while typing, light and dark.
- C3 non-vacuous: sabotaged cellLayout to the pre-fix shape {value:absent, field:flow, hint:flow} -> 4 of 78 tests in tests/offline/ui/editable-cell.test.ts fail; sabotaged hintSide to always "below" plus hint "float" -> 2 fail. Worktree restored clean.
- C4: computed text-decoration "underline 1px" on all five editable values, "none" on the read-only pk cell; every open field selects 0..len (input and textarea).
- hint: computed pointer-events none; a click at the overlap of label's hint and the note button (435, 245.5) OPENED note. Last line's hint y=311..333, inside the table clip box y=143..363.
- keyboard unchanged: Enter opens selected; Enter commits with "saving..." in flight; focus returns to the button when the write settles; Tab -> note -> tally; Escape reverts the value and returns focus.
- open cell's resting button: visibility hidden, keeps its box, b.focus() does not take it, absent from the table's ARIA snapshot, Tab from the field lands on the NEXT cell's button. No aria-hidden/inert needed.
- C5 checks: lint+tsc exit 0; targeted offline 171 passed; test:http 16; full npm test green.

WHY IT COMES BACK (C1 fails; repro is deterministic, both themes):
1. reset sandbox; /records/walk_sandbox/00000000-0000-4000-8000-000000000001. Resting: label x=459.06 y=205 120.2x20, note y=238 192.81x20, tally y=271 14.61x20, is_flagged y=304 41x20, observed_on y=337 74x20.
2. Click label, press End, type " edited by qa". While open every other box is dx=dy=0 - the fix works here.
3. ONE human-timed click (mousedown, 120ms, mouseup) at tally's resting centre (466.37, 281); elementFromPoint there before the press = BUTTON[tally of walk_sandbox].
Found: during the press every value in the column has moved 12.09px LEFT (label dw +85.8); elementFromPoint(466.37,281) is now TD "7"; on mouseup querySelectorAll('input,textarea') is EMPTY and activeElement = BUTTON/tally. Nothing opened - the operator must click twice. label did commit. Dark pass, label already longer: dx -48.76, efp TD "7", nothing opened; same against is_flagged (41px): dx -48.76, efp TD "false", nothing opened.

Mechanism: EditStatus is still IN the row's flow - the "saving..." span measures 46x16, position static, plus the 8px flex gap. At commit the cell's in-flow content grows ~54px and crosses the Value column's max-content (the 36-char sandbox_id row), the auto table layout re-apportions, the whole column shifts - inside the mousedown->mouseup window. Isolated: the same longer value written straight to the row (PATCH, no status on screen) leaves every box at x=459.06 (0px); a same-length mono edit with the status shown also shifts nothing and opens tally fine. So it is the transient status chrome, not the value.

cellLayout's rule is right and is not applied to the third thing the cell renders. EditStatus lives in EditableCell.tsx, inside touch_scope. Not a regression (pre-fix this failed too) - but C1 is the bar, and tally/is_flagged are integers and booleans, the narrow values a catalog is full of.

Accepted, not filed: the multiline textarea (193x70, opaque, pointer-events auto) covers the two rows below and a click inside it just moves the caret - nothing opens, nothing is written, rows below it are reachable once it closes. Residual: tests/live/edit.live.test.ts is not concurrency-safe (2 fail, then 21/21, then 20/21 with "walk_sandbox holds no row ...0001" while 12 vitest processes ran) - it can redden a receipt on unrelated code. Filed separately: BUG-0094 (a server component importing a "use client" export 500s the record page with every tier green).
- 2026-09-09T04:46:44Z [builder-156] claimed (attempt 2)
- 2026-09-09T05:05:02Z [builder-156] WALK 2 (attempt 2). Production build of the REBASED tree (ticket/BUG-0086 c351eaf onto run/admin-window 4ce78ff) on 127.0.0.1:8815 — port taken only after a real bind on 127.0.0.1, ::1 and 0.0.0.0 — bundled Chromium, staging walk_sandbox row ...0001, viewport 1400x950, light AND dark, sandbox reset before each theme's pass and after the last. Same walk was run once on the pre-rebase tree with identical numbers.

Resting boxes (identical in both themes, all five buttons at x=459.06): label y=205 120.20x20, note y=238 192.81x20, tally y=271 14.61x20, is_flagged y=304 41x20, observed_on y=337 74x20.

1. OPEN and TYPING, each of the five cells in turn: every OTHER editable button dx=dy=dw=dh=0.00 — light and dark.
2. QA's reopening repro, on a write slowed to ~1.6s by CDP latency (Network.emulateNetworkConditions latency=1500) so the saving window is wide: click label, End, type " edited by b156", then ONE human-timed click (mousedown, 120ms, mouseup) at tally's resting centre (466.37, 281). Measured INSIDE the press, at +120ms: status = role=status "saving…"; every other button [dx,dy,dw,dh] = [0,0,0,0]; the edited label button [0,0,+99.00,0] — its own width grew because its value did, it did not MOVE; elementFromPoint(466.37,281) still BUTTON[tally of walk_sandbox] (it was TD "7" before the fix). On mouseup: querySelectorAll('input,textarea') = [tally of walk_sandbox], activeElement = INPUT/tally, selection 0..1 over a 1-char value. Both themes. Re-measured at +420ms, still mid-write: all others 0.00.
3. The status box itself while up: [x=686.27, y=205, 54.20x20], computed position=absolute, pointer-events=none, visibility=visible; elementFromPoint at its own centre reaches the TD underneath, so it swallows nothing. Table container [scrollWidth, clientWidth, scrollHeight, clientHeight] = [1174,1174,220,220] before AND during — the float adds no scroll.
4. After the write settled: label committed (button now 219.20 wide, x/y unchanged), all others 0.00; focus back on BUTTON/tally; when the "saved" confirmation retired on its 1.5s clock, worst delta over every button = 0.00.
5. TASK-0053 unchanged: computed text-decoration "underline 1px" on all five editable values (rgb(209,213,220) light / rgb(54,65,83) dark), "none" on the read-only pk cell; note opens selected 0..28. BUG-0069 unchanged: Escape returns focus to BUTTON/note. Hint unchanged: pointer-events none, box [459.06, 264, 392.81x22].

RESIDUAL, measured on purpose and NOT fixed here — it is the VALUE, not chrome. From the fixture state, an edit that appends 61 characters (label dw +409.20) makes the edited value the widest content in the Value column, so the auto table layout re-apportions ON THE VALUE ALONE: during the same 120ms press every other button moves 97.89px left and the click opens nothing (open fields = []). This is the mechanism QA isolated as innocent at +99px (it fits under the 36-char sandbox_id row's max-content) and it bites once the growth clears that. No cell chrome is involved: the resting button IS the row's box, and it must be, or nothing holds the row's height. Removing it needs a decision this ticket does not carry — either the table stops sizing to content (DataTable-wide, table-fixed) or the cell opens on pointerdown instead of click, so a reflow between down and up cannot swallow anything. Both are outside this ticket's scope and touch scope.
- 2026-09-09T05:05:21Z [builder-156] claimed → built: - The rule now covers the THIRD thing the cell draws: cellLayout answers over (editing, statusShown) and places the status float-inert, so every state the cell can be in asks its row for the same box — the resting value and nothing else. EditStatus keeps its words, its role=status/role=alert live regions and its position beside the value (left-full ml-2), but absolute, opaque (it now hangs over what is beside it) and pointer-inert. Touch scope: EditableCell.tsx (in scope) + edit-cell-layout.ts (the pure model the last cut created, outside the listed scope and outside the "use client" boundary on purpose — see BUG-0094); record-fields.tsx untouched.
- Proof. Offline: 6 tests were RED under sabotage of exactly this fix (status:"flow" -> 4 fail; STATUS_BOX stripped of absolute/pointer-events-none/bg-surface -> 2 fail), green restored. Walk on a production build of the REBASED tree against staging walk_sandbox, both themes, reset before each pass and after the last: all five cells opened and typed into with every other button dx=dy=dw=dh=0; QA's exact repro (label + " edited by b156", ONE 120ms click at tally's resting centre during a write slowed to ~1.6s) OPENS tally with its value selected, elementFromPoint stays on the button through the press, and the edited cell's own button grows +99px in width without moving. Numbers, including the status box and the table's scrollWidth, are in the History comment above.
- Commands, all green on the rebased tree: npm run lint; tsc --noEmit; vitest --project=offline tests/offline/ui tests/offline/records (360); npm run test:live -- tests/live/edit.live.test.ts (22); npm run test:http (16); full npm test from a detached _receipt- worktree (67 files, 2525 + 6 isolated).
- Three things for QA. (1) RESIDUAL, measured and left: an edit that grows the value past the Value column's max-content (label dw +409px) re-apportions the column BY ITSELF — others move 97.89px left inside the press and the click opens nothing. No chrome is involved; the resting button is the row's box. Fixing it means either DataTable stops sizing to content or the cell opens on pointerdown — a decision above this ticket. At QA's own +99px repro it does not fire. (2) The status now hangs over whatever is beside the value instead of pushing it: on the record page that is empty space in the Value column, in the review close form (SuppliedControl) it is that row's own text. (3) tests/live/edit.live.test.ts is still not concurrency-safe — it failed 10/22 mid-reset with another lane writing walk_sandbox, then passed 22/22.
- 2026-09-09T05:05:43Z [dispatcher] landed ticket/BUG-0086 onto run/admin-window as 36ed54e
- 2026-09-09T05:10:23Z [qa] built → qa: under attack (batch qa-0086-0094)
- 2026-09-09T05:29:20Z [qa] qa → reopened: REOPENED (2nd reopen, after attempt 2): criterion 1 is false again, one mechanism further out — the VALUE itself, at 56 characters. Landed tree run/admin-window d3d217e, production build on 127.0.0.1:8823 (bind-probed on v4/v6/0.0.0.0 first), bundled Chromium, staging walk_sandbox reset before and after, viewport 1400x950, light AND dark. Criteria driven before reading either handoff.

WHAT HOLDS, measured myself (resting boxes match both builders': label 459.06/205 120.20x20, note /238 192.81x20, tally /271 14.61x20, is_flagged /304 41x20, observed_on /337 74x20):
- C2: all five cells opened in turn AND typed into — every other button dx=dy=dw=dh=0.00, both themes; 0.00 again after Escape.
- C1, original repro: label open, nothing typed, one 120ms click at note's centre -> efp stays BUTTON[note], note opens, activeElement=INPUT/note. Both themes.
- C1, the reopen repro: label + " edited by qa2", one 120ms click at tally's centre -> mid-press efp BUTTON[tally], others [0,0,0,0], label's own button +92.41px WIDE without moving, tally opens selected, label committed, exactly 1 PATCH.
- Refusal path (clearing not-null `tally`): role=alert 320x100, position absolute, pointer-events none, worst delta 0.00, efp at the alert's own centre reaches the TD under it, value reverts to 7, and the next click on `note` opens `note`.
- Double Enter in one open cell -> 1 PATCH. Double-click a value -> opens selected 0..28, 0 PATCH. Click on the open cell's own box -> hits its INPUT.
- C5: lint 0, tsc 0, offline 2590 + isolated 6 green from a detached _receipt- worktree.

WHY IT COMES BACK (C1; deterministic, 3/3 rounds from a reloaded fixture state, both themes): replace `note`'s fixture value (28 chars) with an ordinary correction of 56 — "Checked against the venue listing; corrected 2026-09-08." — then ONE human-timed click (mousedown, 120ms, mouseup) at tally's resting centre (466.37, 281). Before the press elementFromPoint = BUTTON[tally of walk_sandbox]. INSIDE the press every other value has moved 59.48px LEFT (note's own button +184.80 wide); elementFromPoint at the press point is TD "7"; on mouseup querySelectorAll('input,textarea') is EMPTY and activeElement = BUTTON/tally. Nothing opened — click twice. The note DID commit.
Ladder from the same start, one click each: 34 chars -> note +39.60 wide, others 0.00, tally OPENS; 56 -> -59.48, swallowed; 71 -> -86.51, swallowed; 122 -> -138.59, swallowed.

So attempt 2's "+409px / 61 appended chars" is not the threshold: it fires at a 56-character note, which is what free-text prose on a catalog record looks like, and `note` is the field an operator writes prose into. C1 carries no length clause.

Mechanism, unchanged in kind: the resting value IS the row's box, so at mousedown the field closes, the button reappears carrying the NEW value, the auto-layout column re-apportions, and the Value column moves inside the mousedown->mouseup window. No cell chrome is involved; cellLayout's rule is right and structurally cannot reach this.

Risk, stated as a risk: at these deltas mouseup landed on a <td>. A layout where it lands on another editable value's BUTTON opens the WRONG field, and a cell opens with its value selected — the harm FEAT-0011's second affordance exists to prevent.

No red offline pin, and deliberately: this is the browser fact criterion 3 already rules on, and the pure unit that would decide it does not exist until the design is chosen.

ATTEMPT 3: opening the cell on pointerdown instead of click lives in EditableCell.tsx — inside this ticket's touch scope — and kills the whole class (a reflow between down and up cannot swallow what already opened). DataTable-wide table-fixed does not. If neither is takeable here, escalate to the architect (split-or-escalate) rather than bounce a fourth time.
