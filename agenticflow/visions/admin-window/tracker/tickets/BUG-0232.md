---
id: BUG-0232
type: bug
status: open
milestone: M3
priority: P3
opened_by: qa
parent: null
discovered_from: BUG-0231
children: []
assignee: null
claimed_at: null
depends_on: []
touch_scope: [src/components/claims/claim-list.tsx, src/lib/url/narrowing.ts, src/components/claims/bucket-table.tsx]
force: false
---
# Three comments outside /claims still say the app issues a head: true count; the reads they describe are countRead

## Description
BUG-0231 closed the last 'head count' paraphrase in src/app/claims/page.tsx. Sweeping the rest of src/ for the same class (the sweep BUG-0231's file-scoped criteria could not make) finds three more sentences that describe reads THIS APP ISSUES TODAY as `head: true` counts. No actual `head: true` query exists anywhere in src/ — `grep -rn 'head: true' src` returns comment lines only (verified 2026-09-15 at b2d0e432).

FINDING 1 — src/components/claims/claim-list.tsx:76-77.
Expected (ARCHITECTURE §4.3 + BUG-0210): the list's `held` is `readClaimCount` -> `countRead`, a GET with `{ count: "exact" }` over `limit 0` (src/app/claims/page.tsx:1346 and :1649 `held: listCount.data`; src/lib/db/claims.ts:367-379; src/lib/db/result.ts:1430).
Found: "the read is a window now and `held` is a `head: true` count of its own" — present tense, naming the retired mechanism as the shipped one.

FINDING 2 — src/lib/url/narrowing.ts:45-46.
Expected: where fact 2 costs a query it is `readClaimCount(populationFilter)` -> `countRead` (src/app/claims/page.tsx:1378), same spelling everywhere.
Found: "where it does cost a query, that query is a bounded `head: true` count and never a row read".

FINDING 3 — src/components/claims/bucket-table.tsx:47-51.
Expected (src/lib/db/result.ts:1441-1455, admin-window/BUG-0224): the no-count arm is NOT reached only by a query written without the count option — 'the premise was false: a HOST answering 404 with zero bytes reaches it too'. The arm is the one rule for an answer this app cannot grade (`unreadableAnswer`).
Found: "a database that gave no count at all is exactly the response a query written without `{ head: true, count: "exact" }` returns" — stale on both counts: it states the false premise BUG-0224 removed, and it spells the required option with `head: true`, which ARCHITECTURE §4.3 retired.

SECONDARY, same class, test comments (no assertion depends on them): tests/offline/review/review-items.test.ts:111 and tests/offline/queues/page.test.ts:209 both say 'A head count returns no rows at all'.

NOT IN SCOPE, and must survive untouched — these say why the app does NOT use `head: true` and are true: src/lib/db/result.ts:1399 and :1405, src/lib/db/verdict.ts:251 and :256, src/lib/db/claims.ts:354, src/lib/db/review-items.ts:207, tests/live/parity.ts, tests/http/absence.http.test.ts, ARCHITECTURE.md §6 trap 12. A blanket ban on the literal across the repo is the wrong fix (LESSONS 4).

REPRO (static, no server), from the repo root:
  grep -rn 'head: true' src
  grep -n 'held: listCount.data' src/app/claims/page.tsx
  sed -n '74,79p' src/components/claims/claim-list.tsx
  sed -n '43,47p' src/lib/url/narrowing.ts
  sed -n '46,52p' src/components/claims/bucket-table.tsx
  sed -n '1441,1456p' src/lib/db/result.ts

IMPACT: prose only, no behaviour change, no user-visible defect. Filed because this class has now cost DEBT-0022 and BUG-0231 one lane each by being fixed file-by-file, and the next lane reading claim-list.tsx re-learns a mechanism the code retired. Fix is three sentences at their owners plus two test comments. No new test — grading prose with machinery is banned; the checks are the same grep form DEBT-0022 and BUG-0231 shipped, and each is RED on the current tree (verified). Run ./node_modules/.bin/vitest run --project=offline tests/offline/claims/page.test.ts before pushing (226 green at b2d0e432).

## Acceptance criteria
1. Every comment in src/ that describes a count read THIS APP ISSUES names the spelling the code has — `countRead`, `{ count: "exact" }` over `limit 0` — in the vocabulary src/app/claims/page.tsx:681-682 already uses. Concretely: claim-list.tsx's `held` sentence, narrowing.ts' fact-2 sentence, and bucket-table.tsx's no-count sentence each state their read as it is spelled today, and none of the three says the app issues a `head: true` count.
2. bucket-table.tsx's sentence additionally states the no-count arm as src/lib/db/result.ts:1441-1455 does: an answer this app cannot grade, whatever produced it — not 'the response a query written without the option returns'. Its surrounding rule (a refusal is rendered as a refusal, in that bucket's own row) is kept.
3. The contrastive mentions listed in the description (result.ts, verdict.ts, claims.ts, review-items.ts, tests/live/parity.ts, tests/http/absence.http.test.ts, ARCHITECTURE.md) are untouched: they explain why the app does NOT use head: true and are true.
4. The two test comments naming 'a head count' (tests/offline/review/review-items.test.ts:111, tests/offline/queues/page.test.ts:209) say what the stub actually returns; no assertion changes.
5. Prose only: no behaviour change, no new test, no sentence a user reads is touched. tsc --noEmit 0, npm run lint 0, tests/offline/claims/page.test.ts green with nothing rewritten.

## Checks
```sh
! grep -q 'head: true' src/components/claims/claim-list.tsx
! grep -q 'head: true' src/lib/url/narrowing.ts
! grep -q 'head: true' src/components/claims/bucket-table.tsx
grep -q 'countRead' src/components/claims/claim-list.tsx
./node_modules/.bin/tsc --noEmit
npm run lint
```

## History
- 2026-09-15T06:54:52Z [qa] opened: Three comments outside /claims still say the app issues a head: true count; the reads they describe are countRead
