---
id: BUG-0208
type: bug
status: done
milestone: M3
priority: P1
opened_by: verifier
parent: null
children: []
assignee: null
claimed_at: null
depends_on: []
touch_scope: [tests/live/edit.live.test.ts]
force: false
attempts: 1
resolution: fixed
---
# npm run test:live is red as invoked: one edit.live case exceeds its 60s timeout under the suite's own parallelism

## Description
M3 endgame verification, 2026-09-11, HEAD bed26500, staging ubfjjqlvnpnoborczbdb.

FROM A CLEAN START (repo root, nothing else touching staging):
  ( set -a && source .env && set +a && npm run test:live )
EXPECTED (M3 EC2, and the check block of every ticket that touches a page): exit 0.
OBSERVED: exit 1, twice out of two full-suite runs (12:10 and 12:16 local).
  FAIL |live| tests/live/edit.live.test.ts > a resolver-owned record page > shows the venue source on EVERY event the log holds a venue decision for, and moves no scalar's line
  Error: Test timed out in 60000ms.   (tests/live/edit.live.test.ts:598)
  Test Files 1 failed | 10 passed (11); Tests 1 failed | 120 passed (121)
Run 1 was under walk load; run 2 was clean, with only two idle local servers. Same failure both times.

THE SAME FILE ALONE IS GREEN:
  ( set -a && source .env && set +a && npm run test:live -- tests/live/edit.live.test.ts ) -> exit 0, 25 passed
  and that case takes 45,627 ms of its 60,000 ms budget with nothing else running - 76% of budget, solo.
So it is not a wrong assertion; the case walks every event the log holds a venue decision for (120 events on staging) as serial round trips, and under the eleven-file parallel run it crosses 60s.

WHY IT MATTERS: this is the command EC2 names and the command the campaign's tickets cite as their live gate. As invoked, the gate is red at the M3 close, and the next agent to run it will read a failure that says nothing about the tree. The six per-page parity suites themselves are all green (dashboard, cycles, browse, queues, sources, review-item, claims, runs, harness, residue - 10 files, 120 tests).

## Acceptance criteria
'( set -a && source .env && set +a && npm run test:live )' exits 0 on three consecutive runs on this machine with nothing else driving staging. The case that timed out either bounds its read (it proves a property over the venue-decision set, and a bounded sample or a single round trip carries the same property) or is given a stated timeout with the measured margin written beside it; its assertion is not weakened. Its solo wall time is recorded in the ticket in ms.

## Checks
```sh
( set -a && source .env && set +a && npm run test:live )
npm run lint
```

## History
- 2026-09-11T19:24:46Z [verifier] opened: npm run test:live is red as invoked: one edit.live case exceeds its 60s timeout under the suite's own parallelism
- 2026-09-11T19:46:44Z [builder-308] claimed (attempt 1)
- 2026-09-11T20:36:53Z [builder-308] claimed → built: tests/live/edit.live.test.ts only: the 'EVERY event' venue-source case now renders its 108-id population in BATCHES OF 2 instead of one page at a time, each batch awaited whole then graded IN ID ORDER (so what is asserted, and which id fails first, are the serial loop's answers); a new coverage assertion pins graded===ids so a batch walk that skips ids cannot pass by not looking; and the case carries a stated 180s budget with every measurement written beside it. No assertion weakened, population unchanged.
PROOF (rebased on run/admin-window): '( set -a && source .env && set +a && npm run test:live )' exit 0 THREE consecutive times - 11 files / 121 tests, edit case 41,919 / 45,606 / 44,524 ms under the suite. Same tree pre-change: exit 1 on 3 of 3, the ticketed 60s timeout. SOLO wall time of the case: 41,174 ms serial (before) -> 23,349 ms batched (after). 'npm run lint' exit 0, './node_modules/.bin/tsc --noEmit' exit 0, offline live-guard+toolchain 107 passed. Red-then-green: shortening the batch slice by one graded 81 of 108 ids and the new coverage assertion fired.
TRAPS FOR QA: (1) the fanout is 2 by MEASUREMENT, not taste - at 4 this case ran 23-35s under the suite but claims.live.test.ts's heaviest case went red on 3 of 9 full-suite runs with pending_claims refused blank (57014 statement timeout), against 0 of 5 pre-change and 0 of 4 for that file run alone; extra render concurrency here is paid for by another file's timeout. (2) Two of seven fanout-2 runs still took 60,938 and 66,006 ms, i.e. over the project's 60s live default, so the 180s budget is load-bearing, not decoration. (3) claims.live.test.ts is load-sensitive on this staging DB generally - grade a red there against machine load before blaming this diff. (4) The worktree carries a symlinked .env (per STACK) and its own node_modules.
- 2026-09-11T20:37:17Z [dispatcher] landed ticket/BUG-0208 onto run/admin-window as d35b1983
- 2026-09-11T20:38:09Z [qa] built → qa: under attack
- 2026-09-11T20:49:23Z [qa] qa → done: attacked: criteria re-run on the landed tree (d35b1983) BEFORE reading the builder's note.
EC1 - '( set -a && source .env && set +a && npm run test:live )' exit 0 on THREE consecutive runs from this checkout (69/68/65 s wall, 11 files / 121 tests each), and a 4th green under receipt.py. The ticketed case ran 44,077 / 42,988 / 39,432 ms under the suite - 22-24% of its 180s budget, never within 15s of the 60s default it used to blow.
EC2 - read the diff as a diff: every pre-change assertion survives verbatim (not-null; EM_DASH where the log holds no decision; 'admin-set' when admin_locked; the source NAME when source_id is set; dashed===[]) and the population read (CAP=200, count<=CAP, ids.length>0) is untouched. The only additions are the batching and the coverage assertion. Not weakened.
EC3 - solo wall time recorded as 23,349 ms; I measured 22,002 ms solo on this tree (file solo exit 0, 41s wall). The recorded figure is honest.
ATTACK (one integrated pass):
1. Coverage assertion watched-to-fail with MY mutation, not the builder's: loop bound 'ids.length - 1' stayed GREEN (the step is 2, so that bound skips nothing - the obvious probe is vacuous); 'ids.length - RENDER_FANOUT' went RED - "record pages rendered and graded: expected [ ...(106) ] to deeply equal [ ...(108) ]" at tests/live/edit.live.test.ts:731. The assertion is live, not decoration. Mutation reverted, tests/ clean.
2. Population verified independently of the test: read-only PostgREST count on staging ubfjjqlvnpnoborczbdb for entity_type=events, field='venue' returns count=108, 108 rows fetched, 108 distinct entity_id (events total 120). Matches the case's stated 108 exactly and is far below CAP=200, so "EVERY" is not a truncated read.
3. The seam the fix introduces (serial -> fanout 2): module-level mutable state on the render path is src/lib/db/client.ts:23 and src/lib/supabase.ts:6, both lazy client singletons, and nothing else; renderPage (tests/live/parity.ts:396) awaits the page fn then calls renderToStaticMarkup synchronously. No per-request state two concurrent renders could cross, so a batch cannot let one id's markup grade as another's.
4. Builder's trap 1 (render concurrency here paid for by another file's timeout) re-measured over my 3 runs: claims.live.test.ts file totals 59.3/51.5/51.7 s, but its heaviest SINGLE case is 12.8/11.9/13.9 s - every non-edit case in the suite is under 14s against the 60s per-case default. Nothing else is near a cliff at fanout 2.
5. './node_modules/.bin/tsc --noEmit' exit 0 (LESSONS 12 - a committed .ts nothing else type-checks) and 'npm run lint' exit 0. residue.live.test.ts green in all 3 runs: the case leaves nothing on staging.
RESIDUALS (noted, not tickets): (a) the case still costs 39-44s per suite run and the builder measured 2 of 7 runs at 60.9/66.0 s, so the 180s budget - not the batching - is what holds this green under load; (b) if the decided population ever exceeds CAP=200 the case goes red on the count assertion for a reason that is not the product; (c) the coverage assertion pins that every id was RENDERED, not that every id was GRADED - no path reaches that today. [resolution: fixed] [receipt green: 2 cmd(s), tree 3aa5d3db]
