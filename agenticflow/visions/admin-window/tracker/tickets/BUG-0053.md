---
id: BUG-0053
type: bug
status: done
milestone: M1
priority: P2
opened_by: designer
parent: null
discovered_from: M1-endgame-user-sim
children: []
assignee: null
claimed_at: null
depends_on: []
touch_scope: [src/app/records/[table]/[id]/page.tsx, src/components/records/record-fields.tsx, tests/offline/records/page.test.ts, tests/offline/records/provenance.test.ts]
force: false
siblings: [BUG-0052]
attempts: 1
resolution: fixed
---
# Event record page: a whole PROVENANCE column of dashes with no line saying what a dash means, while the pre-cutover page explains its own

## Description
Found INDEPENDENTLY by both document-blind user-sims (2026-09-03) — the only finding of the M1 endgame that two strangers reached from opposite goals. Confirmed live on :8771.

**Feel bar 5:** "Provenance shows at the fact, on review-item detail, claim detail and every editable field." **The Look, the evidence pair:** "The order and the anatomy never change between screens." **Voice register:** "the pipeline's engineer reading you its own logbook — plain, specific."

`/records/events/<id>` renders a FIELD / VALUE / PROVENANCE table. For an event whose fields carry no `field_provenance` row, every cell of the PROVENANCE column is `—`, and **nothing on the page says what that dash means**. The regime note above the table reads only:

    events is resolver-owned and read-only from Admin: its values change through
    the resolution pipeline, not by a direct edit.

Confirmed live, `/records/events/01a06573-e943-7f4e-8db9-db7a5742560e`: six rows, six dashes in PROVENANCE, no explanatory line.

**The asymmetry is the sharp part.** The pre-cutover branch of the same function DOES explain its dashes — `regimeNote()` in `src/app/records/[table]/[id]/page.tsx:82-89` says for `groups`/`idols`: "No field provenance is recorded for it, so no source is shown beside a value." The resolver-owned branch, which is the ONLY branch where a dash could mean several different things, says nothing. So the app explains the case that needs no explaining and stays silent on the case that does.

Tomas (`tomas.md`): "In a tool whose whole argument is that every fact is traceable, a record where nothing is traceable should say so out loud... I only worked it out by opening a second event where three of six fields say 'ticketmaster, applied 1d ago'. So the dash means 'this value has no recorded provenance', not 'provenance isn't wired up yet' — but I had to discover that by comparison, on a site that elsewhere goes out of its way to spell such things out." His stated condition for trusting the tool: "I'd treat **the record pages** as unverified until the blank provenance column says what it means."

Priya (`priya.md`): "A row of dashes under a column called PROVENANCE reads as 'we lost the data', not as 'nothing has been decided yet'."

Priya also names the true cause she had to leave the app to find: that event's performer claims are all sitting in `awaiting_link`, so nothing has been adjudicated onto it. The record page never says that.

**This is NOT a four-state violation.** The field table is in its OK state — it has rows. A `—` is the Look's mandated null rendering ("A null renders as `—` in disabled-gray — never blank, never `null`, `N/A` or `none`"), and the `empty:{holds,filledBy}` contract governs a surface with no rows, not a null cell inside a populated one. **Do not turn this table into an empty state.** The dashes are right; the missing thing is one sentence of the page's own voice, said once per record exactly as the pre-cutover note is (Ben's ruling on admin-window/TASK-0025: say it once, not on every line).

Evidence: `agenticflow/tracker/evidence/user-sim-m1/tomas-13-record-no-provenance.png` (all dashes), `tomas-11-record-provenance.png` (the contrast case, three of six fields sourced), `03-event-record.png`.

Related but distinct: BUG-0052 is the same file and also copy, but it is the EMPTY state's false pointer to Browse on a pre-cutover table; this is the OK state's unexplained null column on a resolver-owned table. Different branch, different bar.

## Acceptance criteria
A resolver-owned record page (`/records/events/<id>`, `/records/venues/<id>`) states, once per record and in the app's voice, what an empty PROVENANCE cell means: that no `field_provenance` row stands behind that value — distinguished from the page's existing not-provisioned and error states for the provenance leg, which keep their current renderings.

The sentence is said once, above the table, beside the regime note — never repeated per row, and never in place of the `—`.

The `—` in the PROVENANCE column is unchanged: still the disabled-gray em dash, never blank, never "N/A", and the field table never renders as an empty state while it has rows.

A record where SOME fields carry provenance and some do not reads correctly too — the sentence must not claim the whole record is unprovenanced when three of six fields are sourced (`tomas-11-record-provenance.png` is that case).

`tests/offline/records/page.test.ts` asserts, for a resolver-owned record with zero provenance rows, that the rendered page contains the explanatory sentence; and asserts for a partially-provenanced record that the rendering is not contradicted.

Human-check: open an event with an all-dash PROVENANCE column and read the page cold — it says what the dashes mean without opening a second event to compare. (human-checkable)

## Checks
```sh
npx vitest run --project=offline tests/offline/records/page.test.ts tests/offline/records/provenance.test.ts
./node_modules/.bin/tsc --noEmit
npm run lint
```

## History
- 2026-09-03T22:26:34Z [designer] opened: Event record page: a whole PROVENANCE column of dashes with no line saying what a dash means, while the pre-cutover page explains its own
- 2026-09-04T00:36:32Z [builder-84] claimed (attempt 1)
- 2026-09-04T00:46:14Z [builder-84] claimed → built: One legend line under the regime note on a resolver-owned record: "A — in Provenance means no field provenance is recorded for that field: the value has no source behind it, rather than a source this page failed to read." It is a statement about a CELL (never varies with the data), said once, never per row; the em dash, the rows and the four states are untouched. Suppressed when the provenance leg refused / found no table (LegNote owns those dashes), on pre-cutover tables (regimeNote already explains theirs), and where no field table renders.
- Verified rebased on 22436ff: offline records + tests/offline/ui/copy.test.ts 102 pass; tsc --noEmit clean; npm run lint exit 0; npm run test:live tests/live/edit.live.test.ts 7 pass (writes groups/idols only, swept by the suite; I made no DB writes).
- Delivered-HTML walk, prod build on my own port 8779 (killed my pid; no other port touched). The ticket's event 01a06573-e943-7f4e-8db9-db7a5742560e: 6 rows, 6 dashes, legend present. Contrast case 01a03f78-a34a-719d-829e-b132ad875b6c: 3 sourced (title/poster_url/starts_at, "ticketmaster, applied 1d ago") / 3 dashed, same legend, uncontradicted. Spacing survives the prod transform ('A — in Provenance means no field provenance…', no glued words) — inter-element spaces are explicit {" "} and tests/offline/ui/copy.test.ts covers the file.
- Two judgement calls QA should attack if wrong: (a) the legend says "field provenance" in PROSE, not the mono table name — on this page the literal field_provenance is what a FAILED leg prints, and tests/offline/records/page.test.ts:'tells an absent provenance table apart…' pins that signal; naming it in the ordinary case would have broken that test, so I matched the pre-cutover note's register instead of touching it. (b) The legend is unconditional for the regime, not gated on "at least one dash": stable anatomy, and the pk and any unmapped column carry no decision ever, so it is never vacuous (test 'still has dashes to explain on the most-sourced record possible'). Test hook is data-note="provenance-absence" (Empty's data-state precedent) so no test greps the sentence; red-then-green observed — 3 of the new tests fail on the pre-fix page.
- 2026-09-04T00:46:34Z [dispatcher] landed ticket/BUG-0053 onto run/admin-window as 6f70442
- 2026-09-04T00:47:15Z [qa] built → qa: under attack
- 2026-09-04T00:56:45Z [qa] qa → done: attacked: re-ran the criteria on the landed tree (6f70442), then drove the gate's whole truth table on real renders — prod build in my own detached worktree on port 8783 (node_modules + .env symlinked, never opened; killed pid 15003; 8771 and 8770 untouched).

DELIVERED HTML, prod build, staging rows. events 01a06573-e943-7f4e-8db9-db7a5742560e: 6 rows, 6 dashes (each carrying aria-label="no value"), legend said exactly once. events 01a03f78-a34a-719d-829e-b132ad875b6c: 3 sourced (title/poster_url/starts_at "ticketmaster, applied 1d ago") / 3 dashed, SAME legend, no contradiction. venues 01a03f78-67da-70d0-8feb-6fc70ef5edbd: legend present — the criterion's second named surface, which no test covered. Spacing survives the prod transform: 'A<!-- --> <!-- -->—<!-- --> <!-- -->in Provenance means…' → 'A — in Provenance means…'. No glued words.

GATE, all four states driven. record read ok + provenance leg answered → legend. Unknown id (…5742ffff, and a venue …ef5ffff) → data-state="empty", legend absent, 0 rows. Malformed id (/records/events/not-a-uuid) → two data-state="error", legend absent. Pre-cutover (groups) → no legend, regimeNote already explains its own dashes. Provenance leg refused (permissionDenied) and not-provisioned (schema-cache) → LegNote only, legend absent (offline render). Record leg refused while provenance answered → legend absent; I pinned that one, it was untested.

NOT VACUOUS. The provenance read filters .in("field", config.display) (lib/db/records.ts), so the pk is never asked about and always dashes: event_id "—" on the fully-sourced event, venue_id "—" on the venue with 3 of 4 display columns sourced. Legend can never caption an empty set.

JUDGEMENT CALLS — neither is a violation, no BUG filed.
(a) prose "field provenance", not the mono identifier. Forced, not preference: tests/offline/records/page.test.ts:897 (pre-existing, TASK-0029) asserts the ordinary render does NOT contain "field_provenance" — the literal is this page's failed-leg signal. And copy bar 5 bans prettifying an identifier into Title Case prose; the legend is lowercase prose, same register as regimeNote (page.tsx:86). Correct.
(b) unconditional per regime, not gated on a dash. The Look's "the order and the anatomy never change between screens" argues FOR unconditional, and the pk proof above makes it never vacuous. Correct.

PINS (committed 031873e, extend not spawn — added to the existing legend describe, no new file): legend over every resolver_owned table in the map + its pk dash; legend absent when the record read refused/found no table while the provenance leg answered. Both watched RED under a plausible wrong gate (config.table === "events", no record check): 3 failed / 64 passed.

VERIFIED GREEN: receipt exit 0 x3; offline records + tests/offline/ui/copy.test.ts 104 pass; full offline project 56 files / 1743 tests pass; npm run test:live tests/live/edit.live.test.ts 7 pass (writes one field of one existing groups/idols row, restored by withSweep, read back after); residue.live.test.ts 4 pass — 56 columns across 4 tables, zero campaign residue. Working HTML/cookie captures deleted via evidence_clean.py; worktree removed.

RESIDUALS (not tickets, no criterion or receipt path reaches them): (1) provenance.note folds TWO legs — if the decision log answers but the sources name lookup refuses, the legend is suppressed while the dashes still mean exactly what it says, and the operator gets a card about `sources` plus an uncaptioned dash column; same render also shows a raw source uuid as the authority (documented TASK-0029 behaviour). (2) The dash in a CELL carries aria-label="no value", so a screen reader hears "no value" and never the em dash the legend is about. [resolution: fixed] [receipt green: 3 cmd(s), tree ffa83c2b]
