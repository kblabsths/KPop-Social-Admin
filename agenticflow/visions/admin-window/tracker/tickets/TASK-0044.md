---
id: TASK-0044
type: task
status: done
milestone: M2
priority: P1
opened_by: architect
parent: FEAT-0011
children: []
assignee: null
claimed_at: null
depends_on: []
touch_scope: [agenticflow/tracker/for-human]
force: false
resolution: already_done
---
# ASK Ben: which columns of events and venues are editable at all

## Description
**SPEC named gap 7, filed blocked. Not a builder's pick** (spec §8 says the map is hand-written and says no more; AGENTS.md narrows it without fixing it). ARCHITECTURE §12 marker `EDIT_ALLOWLIST_EVENTS_VENUES`.

**What this decomposition added to the question, so Ben answers a closed list.** An admin edit is an observation through the gate, and the gate validates every claim against the registry schema for the domain (`domain_schema(p_domain, p_version)`). A column that is not a registry-declared field is refused by the database before it is refused by taste. Read 2026-09-08 from `kspace Scraper/supabase/migrations/20260829000004_events_take_up_the_third_schema.sql` (events v3) and `20260825000001_events_venues_reregister_with_real_facts.sql` (venues v2), the candidates are exactly:

- **events**: `title`, `event_type`, `status`, `starts_at`, `ends_at`, `time_precision`, `description`, `poster_url`, `ticket_url`
- **venues**: `name`, `address`, `city`, `country`, `latitude`, `longitude`, `timezone`, `website`, `image_url`

`venue` and `performers` are references, not cells: they are observed as `{"ref": …}` and resolved into `venue_id` / `event_performers` rows. `venue` is the picker's field (FEAT-0012); `performers` is out of scope for M2 and AGENTS.md forbids it as a field of `events` outright.

**The campaign's recommendation, so the answer can be one word.** Take the columns Ben already ruled visible on 2026-09-02 — events: `title`, `description`, `poster_url`, `starts_at`; venues: `name`, `city`, `country`, `address` — since those are the columns he said an operator comes to see, and every one of them is registry-declared. `event_type`, `status` and `time_precision` additionally carry CHECK constraints, so a free-text cell can produce a refusal the operator cannot predict; they are recommended OUT unless Ben wants them, in which case they want a fixed choice list rather than a cell (a widget this campaign has not costed).

**Deliverable**: the question written to `agenticflow/tracker/for-human/M2-edit-allowlist.md` — the two candidate lists, the recommendation, and the one-line consequence of each extra column — and this ticket blocked until Ben answers.

**What is NOT blocked**: everything in the override write path except the map's two entries. The write path, the route branch, the refusals and the absent-case rendering are all gradeable with an empty `editable` list, which is exactly what `events` and `venues` carry today.

## Acceptance criteria
1. **Ben's answer is recorded on this ticket** (2026-09-08): the editable columns are events `title`, `description`, `poster_url`, `starts_at`; venues `name`, `city`, `country`, `address` — the columns he ruled VISIBLE on 2026-09-02, now writable through the override path. `event_type`, `status` and `time_precision` stay OUT because all three are CHECK-constrained and the fixed choice list that would make them safe is a widget this campaign has not costed. He noted the list can be updated later.
2. The `EDIT_ALLOWLIST_EVENTS_VENUES` marker appears nowhere in `agenticflow/docs/vision/ARCHITECTURE.md`, History included — **the marker's absence is what closes the question** — and §9's `events`/`venues` bullet now carries the answer with its date, the rule that a column MOVES from `display` into `editable` rather than standing in both, and the one-edit widening rule.
3. `agenticflow/docs/DECISIONS.md` carries the dated paragraph: both lists, why the CHECK-constrained three are out, why "later" is one edit and not a second list, and the doors it closes.
4. **The answer reached the ticket that consumes it.** TASK-0054's criterion 8 writes both `editable` lists verbatim, alongside the `display` columns Ben ruled on 2026-09-02 and `reference: { field: "venue_id", domain: "venues" }` for the picker, so the override write path builds against a CLOSED list.
5. No column outside the answer enters `EDIT_CONFIG.editable`, and nothing in this campaign builds a choice-list widget for the three that stay out: widening is a one-entry change to the map when Ben rules it, never a builder's pick and never a second allowlist.
6. (human-checkable) Nothing further is owed: this ASK needed no code. QA closes it `blocked -> done` with resolution `already_done` — the architect has no such edge.

## Checks
```sh
bash -c '! grep -q EDIT_ALLOWLIST_EVENTS_VENUES agenticflow/docs/vision/ARCHITECTURE.md'
bash -c 'grep -qE "events: .title., .description., .poster_url., .starts_at." agenticflow/docs/vision/ARCHITECTURE.md'
bash -c 'grep -qE "venues: .name., .city., .country., .address." agenticflow/docs/vision/ARCHITECTURE.md'
bash -c 'grep -qF "the editable columns of" agenticflow/docs/DECISIONS.md'
bash -c 'grep -rqF "poster_url" agenticflow/visions/admin-window/tracker/ --include=TASK-0054.md'
```
## History
- 2026-09-09T01:14:53Z [architect] opened: ASK Ben: which columns of events and venues are editable at all
- 2026-09-09T01:15:00Z [architect] open → blocked: Blocked on Ben: SPEC named gap 7. The registry fixes the candidate set; which of those columns are editable is his call, not a builder's. Everything in the override path except the two map entries proceeds without the answer.
- 2026-09-09T03:27:06Z [architect] BEN'S ANSWER, 2026-09-08 — recorded verbatim in its parts. The editable columns are events: `title`, `description`, `poster_url`, `starts_at`; venues: `name`, `city`, `country`, `address`. `event_type`, `status` and `time_precision` stay OUT — all three are CHECK-constrained, so a free-text cell can produce a refusal the operator cannot predict, and the fixed choice list that would fix that is a widget this campaign has not costed. He noted the list can be updated later. That is exactly the campaign's recommendation taken whole: the eight columns are the ones he ruled VISIBLE on 2026-09-02, every one of them registry-declared (events v3, venues v2), so the gate accepts them. WHAT I DID WITH IT: the EDIT_ALLOWLIST_EVENTS_VENUES marker is gone from ARCHITECTURE.md (§12 and the 2026-09-08 History line both); §9's events/venues bullet carries the two lists with their date, plus two rules the answer forces — (a) a column MOVES from `display` into `editable` and never stands in both, since `decideEdit` reads `editable` alone and a column in both would be writable while the map called it read-only, so `events.display` becomes ["venue_id"] (the reference alone) and `venues.display` becomes empty, and because `mappedColumns` orders pk -> editable -> display the record pages draw the SAME lines in the SAME order as today; (b) 'updated later' is ONE edit to those two entries — never a second list, a per-column flag, or a 'future columns' scaffold. DECISIONS.md carries the dated paragraph. TASK-0054 gained criterion 8 (both entries verbatim, with the 2026-09-02 display columns and reference events.venue_id -> venues), criterion 9 (the one-edit rule) and three checks, and its dependency on this ticket is dropped because the edge waited on the ANSWER, which has landed. NOTHING FURTHER IS OWED HERE.
- 2026-09-09T03:27:27Z [architect] criteria amended: Re-aimed at the ANSWERED state, same reason as TASK-0043: the old criteria graded agenticflow/tracker/for-human/M2-edit-allowlist.md, a note the run never wrote (the question reached Ben through the blocked sweep's stub and the digest), and old check 3 asserted the marker was PRESENT, contradicting old criterion 4. The new bar grades the closure itself: the answer recorded, the marker gone from ARCHITECTURE.md, the ruling in DECISIONS.md, and both editable lists landed in TASK-0054's criteria. [was: 1. `agenticflow/tracker/for-human/M2-edit-allowlist.md` exists and carries: both registry-derived candidate lists with t… (720 chars; full text in git history)]
- 2026-09-09T03:27:28Z [architect] checks amended: Replaces the three old checks (one asserted the marker was PRESENT). All five dry-run at exit 0 on this tree as of 2026-09-08; the two list checks read ARCHITECTURE §9's own bullet lines, and the last is path-tolerant across tickets/ and archive/ so it survives TASK-0054 being archived. [was: bash -c 'test -f agenticflow/tracker/for-human/M2-edit-allowlist.md', bash -c 'grep -q "poster_url" agenticflow/tracker/… (266 chars; full text in git history)]
- 2026-09-09T03:27:51Z [architect] CLOSURE ROUTING — the ticket is answered and its bar is green, but the architect has no closing edge. Measured 2026-09-08: 'ticket.py transition TASK-0044 done --as architect --resolution already_done' -> ERROR: blocked -> done is not permitted for role architect (the (blocked, done) edge is QA's; wont_fix is strategist/human/toolsmith). Same shape as admin-window/TASK-0023 on 2026-09-02, which QA closed already_done after the architect's record landed. The ticket therefore stays blocked for exactly one action: QA runs receipt.py and closes it blocked -> done with --resolution already_done. All of its stored checks were dry-run VERBATIM on this tree today and every one exits 0. It is NOT waiting on Ben, and nothing downstream waits on it — TASK-0046 and TASK-0054 no longer carry the dependency edge, because the edge waited on the answer, not on the paperwork.
- 2026-09-09T03:33:35Z [qa] blocked → done: Attacked, not typed. Criteria re-run before reading the architect's story. C1 the eight columns are recorded verbatim (events title/description/poster_url/starts_at; venues name/city/country/address) with the CHECK-constraint reason for the three left out and Ben's 'can be updated later'. C2 EDIT_ALLOWLIST_EVENTS_VENUES appears nowhere in ARCHITECTURE.md, History included; I read Sec 12 to confirm the question was not re-posed under another name (it is stated as answered, unmarked) and Sec 9's events/venues bullet (lines 676-704) carries both lists with the 2026-09-08 date, the MOVES-from-display rule with the resulting events.display=[venue_id] / venues.display=[], and the one-edit widening rule. C3 DECISIONS.md carries the dated paragraph with both lists, the CHECK reason, why 'later' is one edit, and the doors closed. C4 the answer reached the consumer verbatim: TASK-0054 criterion 8 carries both entries with pk, regime, display and reference {field: venue_id, domain: venues}, plus the disjointness test on two fixtures and the pinned draw order. C5 nothing widened it: EDIT_CONFIG in src today has no events/venues editable column and no choice-list widget exists (TASK-0054 unbuilt), so no column outside the answer has entered. ADVERSARIAL EXTRA — the seam between this answer and the gate that must accept it: I checked all eight columns against the INSTALLED registry rather than the ticket's summary of it. All eight are declared — events v3 (20260829000004) declares title/starts_at/description/poster_url; venues v2 (20260825000001) declares name/address/city/country — so the gate will not refuse them as unknown fields (KS002), which is what would have made this answer unbuildable. FINDING, filed as a residual not a BUG because no criterion of this ticket or TASK-0054 is false and the ruling is Ben's: the stated rationale ('CHECK-constrained columns are out because a free-text cell can produce a refusal the operator cannot predict') does not hold uniformly for the in-list. Three admitted columns carry registry patterns/bounds that produce exactly that class of refusal: venues.country is pattern '^[A-Z]{2}$' maxLength 2 (typing 'USA' or 'us' is refused by the gate, not by the widget), events.poster_url is pattern '^https://' maxLength 2048, and every string field carries a maxLength (venues.name 200, city 120, address 500). Expected per the recorded reason: an editable column's value is accepted for anything an operator would type. Found: venues.country and events.poster_url are schema-pattern-constrained at kspace Scraper/supabase/migrations/20260825000001 lines 123-128 and 20260829000004 lines 86-93. Consequence is TASK-0054's/FEAT-0011's to carry — the gate refusal must render legibly at the field (its criterion 5 covers only columns absent from the map, a different path) — so it belongs in that ticket's brief, not in a bounce of a recorded human answer. Could not break the closure itself. No code owed, none written; closing already_done per the routing History. [resolution: already_done]
