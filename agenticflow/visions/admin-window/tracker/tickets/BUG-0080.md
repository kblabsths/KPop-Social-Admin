---
id: BUG-0080
type: bug
status: done
milestone: M2
priority: P2
opened_by: qa
parent: FEAT-0010
discovered_from: TASK-0047
children: []
assignee: null
claimed_at: null
depends_on: []
touch_scope: [src/lib/db/result.ts, tests/offline/db/result.test.ts, tests/fixtures/stub-client.ts]
force: false
attempts: 1
resolution: fixed
---
# a non-operator 42883 from a TABLE read renders a provisioned table as not_provisioned

## Description
**Expected** (TASK-0047 criterion 2, and the classifier's own doc comment): a code the app must not read as an absence "still classifies as `error` carrying the database's own message" — and above all, `not_provisioned` must never be a confident claim about an object that is right there (`src/lib/db/result.ts`, MISSING_OPERATOR's comment: "a false absence is a confident claim about a table that is right there").

**Found**: `classify()` returns `{kind:"not_provisioned", missing:"groups"}` for a 42883 raised by a plain TABLE read whose message is NOT the operator sentence. `groups` is provisioned and holds rows; the card would tell the operator to install it.

**Repro, measured read-only on the declared staging target 2026-09-08** (a SELECT; nothing written), from a detached worktree via `npm run test:live`:

    db.from("groups").select("id").filter("created_at", "fts", "x").limit(1)
      -> code=42883  message="function to_tsvector(timestamp with time zone) does not exist"
      -> classify(error, "groups").kind == "not_provisioned"     <-- WRONG

    db.from("groups").select("id").filter("id", "plfts", "x").limit(1)
      -> code=42883  message="function to_tsvector(uuid) does not exist"
      -> classify(error, "groups").kind == "not_provisioned"     <-- WRONG

    control, same table, same code, the one shape that IS guarded:
    db.from("groups").select("id").ilike("created_at", "%x%").limit(1)
      -> code=42883  message="operator does not exist: timestamp with time zone ~~* unknown"
      -> classify(...).kind == "error"                            <-- right

**Offline repro** (no network): `classify(missingFunctionOnTableRead(), T.groups).kind` is `"not_provisioned"`, expected `"error"`.

**Regression introduced by admin-window/TASK-0047** (`src/lib/db/result.ts:322`, the new `FUNCTION_ABSENT_CODES` arm). Before a4900e6 every 42883 fell through to `kind:"error"`; the builder's own diff shows it, having had to move an existing case's fixture from `42883` to `42501` because 42883 stopped being an error (`tests/offline/db/result.test.ts`, "carries message, details, hint and code, in that order").

**Why the guard is short, not wrong.** `MISSING_OPERATOR` is a sentence match, and the operator sentence is only ONE of the 42883s a provisioned table can raise. Postgres also says `function <name>(<types>) does not exist` for a missing OVERLOAD of a function the caller never named — `to_tsvector(timestamptz)` above. The absent function is real; the object `missing` names is not absent, and `missing` is what reaches the card.

**Not reachable from product code today**: no `.filter(...,"fts"|"plfts",...)`, `.ilike`, `.textSearch` or `.rpc` exists anywhere under `src/` (`tests/offline/edit/config.test.ts` bans `.rpc(` outright), so no user hits it at this commit. It becomes live the moment a search/filter control or the FEAT-0010 call seam lands — which is why it is filed rather than left as a note.

**Direction is the architect's, not prescribed here.** The clean fix is probably not a longer regex: a TABLE read can never be a function absence, so the KIND of object the caller asked for is the discriminator (a `readFunction`/`callFunction` seam, or a kind argument to `classify`). A wider sentence match would still be one sentence behind the next shape.

**The pin is a strict xfail.** `tests/offline/db/result.test.ts`, "never calls a PROVISIONED table absent because a TABLE read raised 42883", is committed as `it.fails(...)` — green while the divergence stands, RED the day it is fixed. **The fix flips it back to a plain `it(...)`**, and the check below then proves it. Watched red as a plain `it()` before filing: `expected 'not_provisioned' to be 'error'`. The measured fixture is `missingFunctionOnTableRead()` in `tests/fixtures/stub-client.ts`.

## Acceptance criteria
(to be written)

## Checks
```sh
./node_modules/.bin/vitest run --project=offline tests/offline/db/result.test.ts
```

## History
- 2026-09-09T01:49:03Z [qa] opened: a non-operator 42883 from a TABLE read renders a provisioned table as not_provisioned
- 2026-09-09T01:51:21Z [builder-127] claimed (attempt 1)
- 2026-09-09T02:04:38Z [builder-127] claimed → built: Fix: classify(error, missing, asked: AskedObject = "table") — the absence codes are partitioned by WHAT WAS ASKED, not by Postgres's prose. A table read may only claim a table/column absence (PGRST202/42883 -> error); a function call may only claim a function absence (42P01/PGRST204 -> error). New seam callFunction(fn, run, db?) is the only caller that says "function"; every existing read defaults to "table", so a call site that forgets gets an honest error, never a false absence. MISSING_OPERATOR survives on the function arm alone, where it can only refuse an absence claim, never make one.

Verified (rebased onto run/admin-window @fcc47c8, all green): QA's pin flipped from it.fails to it(...) and passes — watched red first as a plain it() (expected 'not_provisioned' to be 'error'); ./node_modules/.bin/vitest run --project=offline tests/offline/db/result.test.ts (88); tests/offline/db + tests/offline/absence (409); npm test (2139 offline + 6 isolated); tsc --noEmit; npm run lint; npm run test:live -- tests/live/harness.live.test.ts (6) and tests/live/residue.live.test.ts (6), read-only on the declared staging target, nothing written.

QA should know: (1) I touched tests/live/harness.live.test.ts, OUTSIDE touch_scope — its TASK-0047 case used readOne over an rpc, which now (correctly) cannot claim a function absence, so it calls through callFunction; I added a live case there that measures the real 42883 on staging (groups + fts filter on created_at -> code 42883; the app's readRows now returns error naming groups). groups' pk is 'id', not 'group_id'. (2) Three landed TASK-0047 offline cases moved from readOne/readCount/readRows-over-rpc to callFunction; no assertion was dropped, and each absence code is now graded in BOTH directions (own kind -> not_provisioned, other kind -> error). (3) A doc comment spelling '.rpc(' under src/ reddens tests/offline/browse/views.test.ts, which scans RAW text — callFunction's docs deliberately do not spell it. (4) Deliberate call: a 42883 raised inside a function body still names the caller's function (TASK-0047's landed judgement), so that residual absence claim stands.
- 2026-09-09T02:04:57Z [dispatcher] landed ticket/BUG-0080 onto run/admin-window as 75785aa
- 2026-09-09T02:16:06Z [qa] built → qa: under attack
- 2026-09-09T02:23:36Z [qa] qa → done: Attacked on the landed tree (result.ts/result.test.ts/stub-client.ts byte-identical to 75785aa at HEAD 234291f, so TASK-0048's later merge did not disturb the fix).

CRITERIA. The filed repro is dead: classify(missingFunctionOnTableRead(), T.groups) -> {kind:'error', reading:'groups'} carrying 'to_tsvector'; the strict pin is a plain it() at tests/offline/db/result.test.ts:275 and passes. Stored check re-run verbatim, exit 0 (receipt GREEN).

ATTACK 1 - full classify cross-product, 36 error shapes x both asked values (all 17 stub fixtures plus hostile ones: 42883 with no message / message:null / message:'' / uppercase OPERATOR / operator-sentence-in-details, PGRST202 for a table read, 42P01+PGRST205+PGRST204+42703 for a function call, numeric code 42883, code with padding, lowercase pgrst202, prototype-inherited code, null/undefined/string/array errors). Invariant held everywhere: asked='table' NEVER returns not_provisioned for a function-only code, asked='function' NEVER for a table/column code, and every not_provisioned names the string the caller passed. The two safe-direction misses I found are both toward error, never toward a false absence: ' 42883 ' (padded) and 'pgrst202' (lowercase) miss the exact-Set lookup - Postgres emits neither, and both land on error, so no false claim.

ATTACK 2 - the default at every call site. grep: classify( appears only inside result.ts (runQuery passes asked; readComplete:465,490 and readCount:551,563 hardcode the table default - correct, neither can be a function). No .rpc( outside the settlement seam. The one product caller of callFunction is src/lib/db/verdict.ts:154 (settleReviewItem); readSettlementReadiness:209 deliberately reads the verdicts TABLE via readRows, so its absence names verdicts, not a function. Nothing can borrow a function absence.

ATTACK 3 - callFunction on the stub: never throws for 10 failure shapes, a throwing callback, an unscripted name, or unset credentials (that last resolves 'SUPABASE_URL is not set' as error - it must not become 'the resolver is absent'); returns false/0/''/[]/{} verbatim rather than nulling them; records exactly one rpc call per invocation with its argument, tablesRead()==[].

ATTACK 4 - staging, read-only, from a detached worktree (.env and node_modules symlinked, removed after): npm run test:live -- tests/live/harness.live.test.ts, 6/6. TASK-0047's absence case still green (settle_review_item -> not_provisioned naming it), the new BUG-0080 case measures the real 42883 (fts on groups.created_at) and gets error naming groups while a plain groups read is ok. apply_resolution is named in the file but never called - presence is proved off the OpenAPI /rpc/ paths. No write verb in the file; nothing written.

The out-of-touch_scope edit (tests/live/harness.live.test.ts) dropped no assertion - readOne -> callFunction is the correct seam and the case is stronger than before.

FOUR TESTS ADDED (d946ca3), each watched RED first under an inverted expectation: callFunction joined the 'no exported read throws' sweep (all failure shapes, throwing callback, unset credential); a call site reaching the function through readOne/readRows/readCount/readComplete must get an error and never the not-provisioned card - the realistic way this regresses; falsy returns round-trip; a numeric code classifies as its string spelling does (errorCode's number branch was untested).

Green on the integrated tree: lint 0, tsc --noEmit 0, npm test 2191 offline + 6 isolated, result.test.ts 91.

Residual, not filed (lean, no path reaches it today): the mirror case survives on the function arm - an INSTALLED settle_review_item whose body raises a non-operator 42883 renders not_provisioned naming a function that is right there. TASK-0047's landed judgement, re-affirmed here. Trigger: the day Ben installs FEAT-0009's artifact. [resolution: fixed] [receipt green: 1 cmd(s), tree 88bae3c2]
