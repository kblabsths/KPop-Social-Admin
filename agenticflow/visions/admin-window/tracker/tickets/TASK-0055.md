---
id: TASK-0055
type: task
status: done
milestone: M2
priority: P2
opened_by: architect
parent: FEAT-0012
children: []
assignee: null
claimed_at: null
depends_on: [TASK-0042, TASK-0048, TASK-0054]
touch_scope: [src/components/records/entity-picker.tsx, src/lib/db/records.ts, tests/offline/records/entity-picker.test.ts, tests/offline/records/records-read.test.ts]
force: false
attempts: 1
resolution: fixed
---
# The reference field edits as an entity picker whose choice carries its confirmed match

## Description
**SPEC F12, the edit half.** A field whose kind is `reference` edits through an **entity picker**, never a free-text cell — the type of the thing being edited decides the widget (spec §8: "the widget follows the field's kind").

**What the choice carries, and why it is not a `venue_id`** (ARCHITECTURE §9.2, read from the installed registry 2026-09-08). Events v3 declares `venue` as `{"ref": "<the source's own id for the venue>"}`, and the link stage resolves `(source, 'venues', external_ref)` through `confirmed_matches` into `venue_id` — `venue_id` is produced, never observed. So the picker's choice lands as **the admin-tier observation PLUS its confirmed match**: the decision carries `value.field = "venue"` and `value.ref = <the chosen venue's id, used as the admin source's own external_ref>`, and `settle_review_item` writes the `confirmed_matches` row with `matched_by = 'verdict'` (a value that CHECK already admits) before applying. The apply therefore produces a `venue_id` row link and never a string. **No code path submits a reference field's value as text.**

**The picker's manners:**
- It searches within the referenced entity's own table, through a new read in `src/lib/db/records.ts` (the only layer that may spell a table name is `lib/db/tables.ts` — §4 rule 4). The read is a **named window** (§4.3: `readRows` with an explicit order and limit) and the surface says which window it is showing; it is not a complete-read claim about the whole table.
- It offers **only rows that exist**, and it **never creates an entity** — entity creation is the resolver's, not Admin's. Zero controls in the picker create, insert, or accept an unmatched value.
- **It is not a door** (SPEC F12's second hard limit): it is reachable only from a reference field on a record page already open. It is not a nav item, not a Browse view, not a global search box. ROADMAP question 1 is Ben's alone.
- Empty and null render honestly (LESSONS 1): no matches renders a labelled empty state saying what would fill it, and a row whose display name is null renders the dash with no qualifier rather than a blank line.

**The absence is the normal case and is graded FIRST.** `verdicts` and `settle_review_item` do not exist on staging and will not until Ben installs the handoffs — that is what `main` deploys against. The surface renders its not-provisioned state naming the missing object, offers no control it cannot perform, and nothing throws. **No Admin-side workaround** for the absent schema may be written (spec §10's one forbidden move).

With the function absent the picker **does not render at all**: the reference field is read-only with the reason named — the same degradation every override-regime field takes.

## Acceptance criteria
1. A `kind: reference` field renders a picker and never a text input; a structural test asserts no code path submits a reference field's value as a string (one probe it must flag, one shipped path it must not).
2. The picker's submission payload carries the chosen entity's id as `value.ref` and identifies the field as the registry's `venue`, asserted against the recording stub; the payload contains no free text for that field and no `value.value`.
3. The picker offers only rows the search read returned; it has zero create/insert affordances and refuses an unmatched typed value rather than submitting it.
4. The search read is a named window with an explicit order and limit, and the surface states the window it is showing; it makes no exactness or total claim (§4.3).
5. Zero matches renders a labelled empty state; a null display name renders the dash with no qualifier.
6. With `settle_review_item` absent: no picker control renders, the field is read-only with the reason named, the page answers 200 and nothing throws.
7. No listing page, no global search input, and no second Browse view is added: the sidebar still holds exactly six links and Browse still renders exactly one curated view.
8. `npm run lint`, `tsc --noEmit`, the targeted offline files and the record page's live suite are green.

## Checks
```sh
bash -c 'rm -rf .next/types && npm run lint && ./node_modules/.bin/tsc --noEmit'
./node_modules/.bin/vitest run --project=offline tests/offline/records/entity-picker.test.ts tests/offline/records/page.test.ts
npm run test:live -- tests/live/edit.live.test.ts
./node_modules/.bin/vitest run --project=offline tests/offline/shell/shell.test.ts
```

## History
- 2026-09-09T01:20:50Z [architect] opened: The reference field edits as an entity picker whose choice carries its confirmed match
- 2026-09-09T01:27:44Z [architect] depends_on: [TASK-0042, TASK-0048] → [TASK-0042, TASK-0048, TASK-0054]: Adds the override write path: the picker's submission IS an override decision, and both tickets write src/lib/db/records.ts and src/components/records/** — worktrees isolate builds, not landings (ARCHITECTURE §13.9).
- 2026-09-09T04:22:57Z [builder-152] claimed (attempt 1)
- 2026-09-09T05:06:23Z [builder-152] WALK (Playwright, production build of 60f9f99 on :8801 against staging ubfjjqlvnpnoborczbdb, 1440x900, minted cookie, 2026-09-09). Subject: /records/events/01a03f78-ab82-7df9-94af-6901a44a0a56 ('MAMAMOO 2026 US TOUR'), venue 01a03f78-a82b-71d8-b749-58587fdc7c83 ('accesso ShoWare Center'), found by clicking through Browse (50 event links). WITH settle_review_item ABSENT: pickers on the page = 0; controls inside [data-surface=fields] = 0; [data-window] elements = 0; one [data-note=override-unavailable] and one [data-state=not_provisioned] reading 'verdicts isn't in this database yet - it arrives with the scraper repo's migrations.'; the venue_id line renders 'accesso ShoWare Center' + the uuid with 1 link and 0 controls; 0 console errors or warnings. THE CHOICE AT THE WIRE: PATCH /api/admin/records/events/<id> {field:venue_id, ref:<venue uuid>} answered 503 {"error":"settle_review_item is not present in this database","missing":"settle_review_item"} - the not-provisioned answer, never a fake success. The SAME column as text, {field:venue_id, value:'Olympic Hall'}, answered 403 'venue_id is not an editable field of events'. After both, the reloaded field table was byte-identical to before (row_unchanged=true, 1 venue link). apply_resolution was never called; nothing was written anywhere.
- 2026-09-09T05:09:25Z [builder-152] claimed → built: The reference field (events.venue_id -> venues) draws an entity picker, never a cell: new src/components/records/entity-picker.tsx, a third question of the ONE map (decideReference in lib/edit/config.ts, registry field via BUG-0090's registryFieldOf), a named-window read (readReferenceChoices: order name then venue_id, .limit(ROW_CAP)) stated with a new 'alphabetical' arm of the shared WindowLine, and submitReferenceEdit sending {field, ref} with NO value key. The route builds one item-less override carrying value.ref under the REGISTRY field 'venue'; decideEdit still refuses venue_id as a value (403), unchanged.

Verified on the rebased tree (5930d8a onto 22f099e): lint + tsc clean; entity-picker+page 117 passed; shell 23 passed; full offline 2576 passed; npm run test:live -- tests/live/edit.live.test.ts 23 passed; npm run test:http 16 passed. Red-then-green: 8 sabotages each reddened their own test (registry field -> column: 3 red; ref sent as value: 1; picker never drawn: 7; picker drawn with the path closed: 1; search filter ignored: 3; window read loses its limit: 1; a 'column' key on the envelope: tsc error). Walk measurements are in History (Playwright, production build against staging): 0 pickers, 0 controls, 503 not-provisioned for the ref, 403 for the same column as text, row unchanged.

TRAPS FOR QA. (1) Touch scope: I went outside it. The picker needs the widget arm (components/records/fields.ts, record-fields.tsx), its submitter (submit.ts), the write arm (the PATCH route), the decision (lib/edit/config.ts) and a WindowLine arm (components/ui/window-line.tsx); tests went into the files that OWN each surface (edit/config, edit/records, edit/route, records/submit, records/page, ui/primitives) rather than into a new records-read.test.ts. (2) I CHANGED existing assertions that said the reference line offers no control when the path is open — that premise is what FEAT-0012 reverses. They now say: no CELL ever, a picker when the path is open, nothing at all when it is closed, discriminated by a new data-widget='picker' hook. (3) The picker's window line lives INSIDE the panel the operator opens, so the resting page publishes no data-window for it and claims no total; offline grades it by rendering PickerPanel directly (no jsdom). (4) The panel is drawn IN the row's flow, not floated like the cell's field: DataTable clips (overflow-x-auto), and the reference line is drawn last by mappedColumns, so nothing moves. (5) The choices read only fires when the override path is open, so on staging it never fires at all.
- 2026-09-09T05:09:45Z [dispatcher] landed ticket/TASK-0055 onto run/admin-window as 070820b
- 2026-09-09T05:10:29Z [qa] built → qa: under attack
- 2026-09-09T05:24:02Z [qa] qa → done: Attacked on the LANDED tree (detached _receipt worktree at run/admin-window d3d217e), stored checks re-run BEFORE reading the builder's story: lint+tsc exit 0; entity-picker+page 117 passed; edit.live 23 passed against staging; shell 23 passed. Receipt GREEN on the current tree (4/4 exit 0). Full gauntlet from my worktree: offline 2592 passed (69 files), isolated 6, test:http 17, all exit 0.

CRITERIA, each driven rather than read. (1) recordFields gives venue_id widget='picker' in every map state and never 'cell'; the structural rule flags a planted probe module and clears the shipped one. (2) Route builds ONE override whose value.field='venue' (registryFieldOf, not the column), value.ref=<chosen id>, value.value=null, no 'column' key anywhere in VerdictValue — matches the M2 handoff's reference arm (v_domain=events, v_field=venue -> v_ref_domain=venues, v_column=venue_id). (3) choose() re-looks-up the id in the window before sending, so a typed/unmatched value cannot be submitted; no create/insert control exists; the search box is in no form and has no Enter handler. (4/5) window line is the new 'alphabetical' arm inside the panel; MEASURED against staging (scratch live read, since deleted): 130 venues, held=130, limit=1000, truncated=false, 0 null names — so the cap claim is honest today and the truncation sentence is reachable only above 1000. (6) With settle_review_item absent: access is fail-closed (only settlement.kind==='ok' opens), no choices read is issued at all, widget undefined, editable false, one link on the line, zero [data-window] on the page. (7) shell: six links, one Browse view, unchanged. (8) above.

SCOPE DEVIATION — judged touch by touch, all NECESSARY, none a bounce: fields.ts/record-fields.tsx (criterion 1 cannot be met from entity-picker.tsx alone — the row has to draw the widget), submit.ts (the one submitter; the alternative was a second fetch spelling inside the widget), route.ts (criterion 2's registry-name transformation; without it a ref body 400s), config.ts/decideReference (only the map may spell venue_id/venue, ARCH §4 rule 4), window-line.tsx (the three existing arms state newest-first / complete-matched / catalog reads — reusing one would have stated a read that did not happen), page.tsx (the read leg). Tests went to the files that OWN each surface instead of the scoped new records-read.test.ts — the right call (extend, don't spawn). REWRITTEN ASSERTIONS still discriminate and are strictly sharper: the old 'editable === false' became 'widget === (isReference ? picker : undefined) AND editable === isReference' over every display column, plus 'no input/textarea inside [data-widget=picker] at rest' — a cell standing where a picker belongs now reddens, which the old form could not see.

BROKE IT ONCE -> BUG-0097 (P2, filed, pinned strict it.fails, commit 3b6951a): PickerOptions renders every option as an always-enabled button and choose() has no in-flight guard or edit epoch, so a second click during a save sends a second override PATCH for the same field and the last ANSWER wins the display rather than the last choice. The widget disables the control that cannot write (the toggle) and leaves live the ones that can — the exact rule EditableCell's reduceEdit was bought with (BUG-0069). Latent, not live: with the function absent no picker renders at all, so it reaches no user today. It does not defeat this ticket's criteria or its goal, so it is filed rather than bounced. [resolution: fixed] [receipt green: 4 cmd(s), tree 6519fb62]
