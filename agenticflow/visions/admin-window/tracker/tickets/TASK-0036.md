---
id: TASK-0036
type: task
status: open
milestone: patch
priority: P2
opened_by: strategist
parent: null
discovered_from: inbox:2026-09-03-walker-sandbox-table.md
children: []
assignee: null
claimed_at: null
depends_on: [TASK-0034]
touch_scope: [tests/walk, tests/offline/walk, tsconfig.json, agenticflow/docs/vision/STACK.md]
force: false
siblings: [TASK-0034]
---
# Reset the walk sandbox between walks: the seed fixture, the reset tool, and the walk recipe

## Description
Ben's inbox note, 2026-09-03: "a table that always exists which walkers can
interact with. **After a walk it should be reset for the next walk.**" This
ticket is the reset, the fixture it resets to, and the two paragraphs in
STACK.md that make a walker able to use both without reading this ticket.

**Blocked on TASK-0034**, which rules the mechanism (whether the tool creates
the table or only reseeds a table Ben created by hand), where the tool lives,
and the cadence. The scope on this ticket is a candidate, not a ruling — expect
it to be amended when TASK-0034 lands.

## What the tool does

Truncate the sandbox table and reseed it from a checked-in fixture, so two
runs in a row leave byte-identical rows and a walk that edited every editable
column leaves no trace of having happened. Nothing more: it is not a migration
runner and it is not a general staging fixture tool.

Four properties it must have, all of them borrowed rather than invented:

1. **It refuses against anything that is not the declared staging target.**
   `tests/live/staging-target.ts` already encodes that decision and
   `SERVICES.md` already declares the project — reuse them; do not write a
   second host check with its own idea of what staging is.
2. **It prints no value.** Host and declared target are identifiers and may
   appear in a refusal (that is the live setup's own rule); keys and DSNs
   never appear anywhere, in success or failure.
3. **The seed values carry no `admin-window` marker.** The moment TASK-0035
   lands, `tests/live/residue.live.test.ts` sweeps this table for that string
   and a fixture containing it reads as campaign residue on every run.
4. **It is not reachable from the app.** Nothing under `src/` imports it, and
   nothing it does goes through `src/lib/db/**`.

## The recipe

STACK.md §5 already tells a walker how to launch the instance and mint a
cookie (`tests/walk/session-cookie.mts`). This adds the third step beside
them, in the same voice: the one command that resets the sandbox, when to run
it (TASK-0034's cadence), the deep link a walk opens
(`/records/<table>/<key>`), and — plainly, because a walker will otherwise
file it as a bug — that a save 403s for the walker identity until Ben answers
the `admin_allowed_emails` question, so a read-and-render walk is what this
buys until then.

## What this replaces

Until this chain lands, walkers use the groups/idols exception: one field on
one existing row, the original value noted, restored before the walk ends,
residue sweep after. When the sandbox exists, STACK.md should say plainly that
the sandbox is the walk surface and the exception is retired — otherwise both
practices live on and the next walker picks the wrong one.

**Sibling to TASK-0034** (same inbox note, and both touch STACK.md). TASK-0034
decides *what may exist and by what mechanism* and writes that ruling into
ARCHITECTURE/DECISIONS; this ticket builds the tool and the fixture and writes
the walker-facing recipe into STACK.md §5. If they merge, the merged ticket
still owes both artifacts — a ruling nobody can run, and a tool nobody was
told about, each fail on their own.

## What the tool does

Truncate the sandbox table and reseed it from a checked-in fixture, so two
runs in a row leave byte-identical rows and a walk that edited every editable
column leaves no trace of having happened. Nothing more: it is not a migration
runner and it is not a general staging fixture tool.

Four properties it must have, all of them borrowed rather than invented:

1. **It refuses against anything that is not the declared staging target.**
   `tests/live/staging-target.ts` already encodes that decision and
   `SERVICES.md` already declares the project — reuse them; do not write a
   second host check with its own idea of what staging is.
2. **It prints no value.** Host and declared target are identifiers and may
   appear in a refusal (that is the live setup's own rule); keys and DSNs
   never appear anywhere, in success or failure.
3. **The seed values carry no `admin-window` marker.** The moment TASK-0035
   lands, `tests/live/residue.live.test.ts` sweeps this table for that string
   and a fixture containing it reads as campaign residue on every run.
4. **It is not reachable from the app.** Nothing under `src/` imports it, and
   nothing it does goes through `src/lib/db/**`.

## The recipe

STACK.md §5 already tells a walker how to launch the instance and mint a
cookie (`tests/walk/session-cookie.mts`). This adds the third step beside
them, in the same voice: the one command that resets the sandbox, when to run
it (TASK-0034's cadence), the deep link a walk opens
(`/records/<table>/<key>`), and — plainly, because a walker will otherwise
file it as a bug — that a save 403s for the walker identity until Ben answers
the `admin_allowed_emails` question, so a read-and-render walk is what this
buys until then.

## What this replaces

Until this chain lands, walkers use the groups/idols exception: one field on
one existing row, the original value noted, restored before the walk ends,
residue sweep after. When the sandbox exists, STACK.md should say plainly that
the sandbox is the walk surface and the exception is retired — otherwise both
practices live on and the next walker picks the wrong one.

## Acceptance criteria
tests/walk/reset-sandbox.mts exists, is run by node exactly as tests/walk/session-cookie.mts is, imports nothing from src/, and resets public.walk_sandbox to the checked-in fixture in tests/walk/sandbox-fixture.ts by deleting every row and re-inserting it through PostgREST with @supabase/supabase-js (no pg driver, no DDL, no TRUNCATE): run twice in a row it leaves identical rows, and run after a walk that edited every editable column it restores the fixture exactly. It takes SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY from its OWN process environment (mapped from the staging names on the command line, the STACK 5 launch idiom), loads no .env, and mentions no STAGING_ name anywhere — tests/live/setup.ts stays the one reader of those. It refuses non-zero having written nothing, through resolveStagingTarget from tests/live/staging-target.ts and SERVICES.md, against an unset name, an unparseable URL, no declaration, or any host that is not the declared staging target; no second host check is written. It prints no key and no DSN on either stream. Against a staging project where the table has not been pasted yet it exits non-zero naming walk_sandbox and saying it is created by hand (for-human/TASK-0034.md), never attempting DDL. tsconfig.json gains allowImportingTsExtensions: true so the .mts may import ../live/staging-target.ts by its real extension; ./node_modules/.bin/tsc --noEmit, npm run lint and npm run build all stay green. tests/offline/walk/sandbox-fixture.test.ts pins the fixture offline: it contains no "admin-window" substring, every fixture key is a mapped column of the EDIT_CONFIG walk_sandbox entry, the keys are walk-1/walk-2/walk-3, and at least one row has a null note and one a null observed_on. STACK.md section 5 carries the reset step beside the session-cookie step: the exact command, the cadence (immediately BEFORE every walk, mandatory — TASK-0034 item 4 — and again after a walk that wrote, when convenient), the deep link /records/walk_sandbox/walk-1, that clearing label, tally or is_flagged is refused by the database on purpose and is not a bug, and that the sandbox is now the walk-write surface and RETIRES the one-field-on-groups/idols exception the same section currently prescribes.

## Checks
```sh
npx vitest run --project=offline tests/offline/walk
./node_modules/.bin/tsc --noEmit
! grep -q "admin-window" tests/walk/sandbox-fixture.ts
grep -qF "resolveStagingTarget" tests/walk/reset-sandbox.mts
! grep -q "STAGING_SUPABASE" tests/walk/reset-sandbox.mts
grep -qF "reset-sandbox" agenticflow/docs/vision/STACK.md
grep -qF "/records/walk_sandbox/walk-1" agenticflow/docs/vision/STACK.md
```
## History
- 2026-09-03T21:35:12Z [strategist] opened: Reset the walk sandbox between walks: the seed fixture, the reset tool, and the walk recipe
- 2026-09-03T21:35:26Z [strategist] description amended: Sibling line added as the tool asked: TASK-0034 rules the mechanism into ARCHITECTURE/DECISIONS, TASK-0036 builds the tool and fixture and writes the walker recipe into STACK.md section 5. [was: Ben's inbox note, 2026-09-03: "a table that always exists which walkers can interact with. **After a walk it should be r… (573 chars; full text in git history)]
- 2026-09-03T21:35:27Z [strategist] depends_on: [] → [TASK-0034]: Blocked on the architect's ruling: whether the tool creates the table or only reseeds one Ben created by hand, where it lives (TASK-0021 pins STAGING_SUPABASE_DB_URL as a name nothing under src/ or tests/ reads), and the reset cadence are all TASK-0034's to settle.
- 2026-09-03T21:47:34Z [architect] touch_scope: [tests/walk, agenticflow/docs/vision/STACK.md] → [tests/walk, tests/offline/walk, tsconfig.json, agenticflow/docs/vision/STACK.md]: TASK-0034 ruling: the tool is tests/walk/reset-sandbox.mts beside the cookie mint (no fourth home for walk tooling, no pg driver, no DB URL). tests/offline/walk holds the fixture's offline pin. tsconfig.json gains allowImportingTsExtensions so the node-run .mts may import ../live/staging-target.ts by its real extension — measured today on this tree: tsc 0, lint 0, npm run build green, Next does not rewrite the flag.
- 2026-09-03T21:47:49Z [architect] criteria amended: Rewritten to TASK-0034's ruling: candidate A (Ben pastes the DDL; the tool only does DML), the tool's location and credential shape, the guard reuse, the before-every-walk cadence, the tsconfig flag with its measurement, and an offline pin for the fixture so the ticket has a bar every role can run without staging. The 403 caveat this ticket was told to repeat is now false and already corrected under BUG-0038 — say saves LAND, and that the sandbox retires the groups/idols exception. [was: A reset tool exists at the location TASK-0034 rules, outside src/ and importing nothing from src/lib/db/**, that resets … (867 chars; full text in git history)]
- 2026-09-03T21:47:58Z [architect] checks amended: The old single check (grep -qi sandbox STACK.md) went green today on an unrelated edit of mine, which is exactly the defect it is: it proved nothing about this ticket. Replaced with: the fixture's offline pin, tsc (which also proves the tsconfig flag and that the .mts compiles), the marker absence the residue sweep depends on, presence of the guard reuse, absence of a second STAGING_ reader, and the two recipe strings. Measured: tsc --noEmit is 0 on today's tree in ~1s; the vitest form reddens now because tests/offline/walk does not exist yet. [was: bash -c "grep -qi sandbox agenticflow/docs/vision/STACK.md"]
