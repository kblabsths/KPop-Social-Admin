---
id: BUG-0104
type: bug
status: done
milestone: M2
priority: P2
opened_by: qa
parent: null
discovered_from: BUG-0101
children: []
assignee: null
claimed_at: null
depends_on: []
touch_scope: [src/components/EditableCell.tsx, src/components/edit-cell-layout.ts, src/components/records/record-fields.tsx, src/components/ui/data-table.tsx, tests/offline/ui/editable-cell.test.ts, tests/offline/records/page.test.ts]
force: false
attempts: 1
resolution: fixed
---
# A tall refusal on the SECOND-TO-LAST field is drawn past the fields table's clipping container - the ordinal growth rule rescues only the last line

## Description
Measured 2026-09-08 by QA attacking BUG-0101 on the LANDED run branch (`run/admin-window` at 88da50e): production build served on 127.0.0.1:8793 (bind-probed, not 8770/8771) against staging (`ubfjjqlvnpnoborczbdb`), bundled Chromium, 1440x900, BOTH themes, `walk_sandbox` row `...0001`, `reset-sandbox` before AND after (both "deleted 3, seeded 3, read back and verified").

BUG-0101 fixed the LAST line and its criterion 1 is green (see that ticket). Its rule is ORDINAL - `statusGrowth(row, rows)` returns `up` only for `lastOfSeveral` (`src/components/edit-cell-layout.ts:191`), everything else `down`. But the container clips a box that exceeds the room BELOW ITS OWN ROW, not just below the last row. The fields table has 6 rows in 220px; the 23502 refusal this surface already produces is 120px tall. On the second-to-last row there are 62px of room. So the same defect, undiminished, sits one row up.

**Expected** (BUG-0098 criterion 1, restated by BUG-0101 criterion 1): a refused write renders BOTH halves at the field - the database refusal verbatim in mono AND the app-voice sentence - inside the fields table container, at rest, nothing scrolled.

**Found**: clearing `is_flagged` (row 5 of 6, the second-to-last) draws the refusal box y 304 -> 424 against a container of y 143 -> 363. Byte-identical in light and dark.
- container `div.overflow-x-auto`: y 143, h 220, bottom 363; `scrollHeight` 281 vs `clientHeight` 220 (the box adds 61px of hidden overflow).
- refusal box `role="alert"`: 304 -> 424, h 120 - **61px below the container edge**.
- mono half `null value in column "is_flagged" of relation "walk_sandbox" violates not-null constraint / Failing row contains (...)`: 306 -> 402, cut 39px - the screenshot shows it ending mid-word at `A note a` on its third line.
- app-voice half `is_flagged cannot be cleared - type a value into it.`: 404 -> 422, `fullyOutside: true`. **Not painted at all**, exactly as BUG-0101 measured for the last row. Setting the inner container `scrollTop = 100` (it clamps to 61) brings the box to 243 -> 363 and the sentence back - the only way to read it, and the table shows no scroll affordance for that axis.

Exact repro:
1. `node tests/walk/reset-sandbox.mts` with the STACK.md section 5 staging mapping.
2. `npm run build` + `npm run start -- --port <free>`; mint the walk cookie; open `/records/walk_sandbox/00000000-0000-4000-8000-000000000001` at 1440x900.
3. Click the `is_flagged` value (row 5 of 6), select-all, Delete, Enter. Park the pointer at 0,0 and read the boxes.

This is a supported path, not a contrived one: STACK.md section 5 calls clearing one of `label` / `tally` / `is_flagged` "a walkable error path put there on purpose", and `tests/live/edit.live.test.ts` pins all three refusals. `label` (row 2) and `tally` (row 4) have room and stay inside (205 -> 325 and 271 -> 327, re-measured on this tree); `is_flagged` is the one whose own row has less room than its own box.

**Not a regression of BUG-0101 and not BUG-0086.** Row 5 was told `down` before that fix and is told `down` after it, so this box is drawn exactly where it was drawn all along; BUG-0101 changed only the last line. And nothing reflows: rows sit at 169.5 / 198.5 / 231.5 / 264.5 / 297.5 / 330.5 and cells at x 209 / 459.3 / 1175.7 (w 250.3 / 716.41 / 247.3) before, during and after a commit, in both themes.

**Expected: both halves of a refusal painted inside the fields table container. Found: on `is_flagged` the app-voice half is drawn 41-59px below that container and is not painted at all** - `src/components/edit-cell-layout.ts:191` (`statusGrowth`, ordinal: only the last of several grows up) with the box height set by the database sentence in `src/components/EditableCell.tsx:490` (`max-w-xs`), inside `src/components/ui/data-table.tsx:112` (`overflow-x-auto`, whose other axis computes to `auto` and clips).

**QA honesty, same as BUG-0101 carried**: there is NO offline pin for this and QA deliberately invented none. `tests/offline` is plain node with `renderToStaticMarkup` and no jsdom (STACK.md section 4), so a bounding box is unobservable there, and the only offline-observable fact - that `RecordFields` tells `is_flagged` `down` - is CORRECT for an ordinal rule. Asserting `up` there would pin an arbitrary "last two lines" hack rather than the invariant. The invariant is: no refusal this surface can produce is painted outside the container. Its fix is plausibly geometric (measure the box and flip or clamp), or structural (the container stops clipping the y axis / the box is not a child of it) - and criterion 4 below asks the fixer to pin whatever DECISION their fix introduces, exactly as BUG-0101 pinned `statusGrowth`.

## Acceptance criteria
1. Clearing `is_flagged` on `/records/walk_sandbox/00000000-0000-4000-8000-000000000001` at 1440x900, in BOTH themes, on a production build, with the cell at rest and nothing scrolled: BOTH halves of the refusal are painted inside the fields table container (`div.overflow-x-auto`) - the app-voice sentence in full, the mono refusal not cut mid-line.
2. Every refusal already measured inside stays inside, unchanged: `observed_on` / 22007 on the last row (box 301 -> 357), `tally` / 22P02 (271 -> 327), `label` / 23502 (205 -> 325), against a container of 143 -> 363.
3. The status stays OUT of the row flow (BUG-0086, unchanged): committing a cell moves no row y and no cell x or width, and the box stays inert to the pointer.
4. Whatever DECISION the fix introduces is pinned in the offline suite the way `hintSide` and `statusGrowth` are (`tests/offline/ui/editable-cell.test.ts`), each rule proved on two fixtures - one it must answer each way. If the fix carries no expressible decision (a measured flip in the browser, a container that no longer clips), the ticket says so in one line and pins what IS observable: which element renders where in the tree, what class it carries, that both halves stay inside the one `role="alert"`.
5. The refusal is still the database words verbatim, both halves are still inside the one `role="alert"`, and the success path is untouched: `saving...` in secondary, `saved` in healthy for ~1.5s.
6. `npm run lint` and `tsc --noEmit` clean; the targeted offline files and the record page live suite green.

## Checks
```sh
bash -c 'rm -rf .next/types && npm run lint && ./node_modules/.bin/tsc --noEmit'
./node_modules/.bin/vitest run --project=offline tests/offline/ui/editable-cell.test.ts tests/offline/records/page.test.ts
npm run test:live -- tests/live/edit.live.test.ts
```

## History
- 2026-09-09T06:33:42Z [qa] opened: A tall refusal on the SECOND-TO-LAST field is drawn past the fields table's clipping container - the ordinal growth rule rescues only the last line
- 2026-09-09T06:37:57Z [builder-171] claimed (attempt 1)
- 2026-09-09T06:54:27Z [builder-171] Walk of criteria 1, 2, 3 and 5 on the REBASED tree (ticket/BUG-0104 at e1f75bf, rebased onto run/admin-window 88a404b). Production build (`npm run build` + `npm run start -- --port 8780`, bind-probed free port, never 8770/8771/8772), staging `ubfjjqlvnpnoborczbdb`, bundled Chromium, 1440x900, BOTH themes, `walk_sandbox` row ...0001, `reset-sandbox` before AND after (both "deleted 3, seeded 3, read back and verified"). Pointer parked at 0,0, nothing scrolled (`scrollTop` 0 in every reading below).

Container `div.overflow-x-auto` of the fields table: y 143 -> 363, clientHeight 220. Rows at 169.5 / 198.5 / 231.5 / 264.5 / 297.5 / 330.5. **Every number below is byte-identical in light and dark.**

CRITERION 1 - `is_flagged` (row 5 of 6), cleared, 23502, box h 120:
- box **243 -> 363** (was 304 -> 424), `transform: matrix(1,0,0,1,0,-61)` - the measured correction is exactly the 61px QA measured outside.
- mono half 245 -> 341, app-voice half `is_flagged cannot be cleared - type a value into it.` 343 -> 361. Neither is cut: `outside` 0px, `fullyOutside` false, for both halves, in both themes.
- container `scrollHeight` **220 == clientHeight 220** (was 281 vs 220): the box adds no hidden overflow at all now, so there is nothing left to scroll to.

CRITERION 2 - the three refusals QA measured inside, re-measured here, all with `transform: none` (`statusShift` answers 0, so they are drawn at the pixel they were drawn at):
- `label` / 23502, row 2: **205 -> 325**.
- `tally` / 22P02 (typed `seven`), row 4: **271 -> 327**.
- `observed_on` / 22007 (typed `nope`), last row, anchored `bottom-0`: **301 -> 357**.

One case beyond the four, because the class is not ordinal: `tally` CLEARED (23502, h 120 on row 4) was 271 -> 391, 28px outside; it is now 243 -> 363 with `transform: translateY(-28px)`. Same defect, another row, same fix - which is the whole argument for measuring rather than counting rows.

CRITERION 3 - no reflow, still inert. Rows before/during/after a commit: 169.5 / 198.5 / 231.5 / 264.5 / 297.5 / 330.5, identical in all five cases and both themes; cells at x 209 / 459.3 / 1175.7, w 250.3 / 716.41 / 247.3, identical. `document.elementFromPoint` 4px inside the refusal box's own top-left corner returns the BUTTON or the TD underneath, never the alert - the box is transparent to the pointer where it hangs over another row's control.

CRITERION 5 - success path untouched (light, same instance): editing `note` gave `saving...` at t+12ms in `text-ink-secondary`, `saved` at t+341ms in `text-healthy`, gone at t+1869ms - **1528ms of `saved`**. Both at 238 -> 258, `transform: none`, inside the clip; the typed value stood in the button afterwards.
- 2026-09-09T06:55:38Z [builder-171] claimed → built: - **The decision is `statusShift`** (`edit-cell-layout.ts`, pure, offline-pinned): given the box's uncorrected edges and the clipping container's, how far it must move. `0` for a box that already fits, `-overflow` for one past the floor, `+overflow` for one past the ceiling, top-aligned when it is taller than the container. `EditStatus` measures its own box before paint and applies the answer as a **transform**, so no flow changes and BUG-0086 is paid nothing. `statusGrowth` and `hintSide` are UNTOUCHED and still one predicate: the anchor still picks the direction, the measurement only corrects what the anchor cannot know (the box's height is the database's sentence, not the row's ordinal).
- **Verified**: red-then-green offline (with `statusShift` stubbed to the pre-fix `0`, the floor / ceiling / taller-than-container tests fail; restored, 122 pass). `npm run lint`, `tsc --noEmit`, `npm test` (2697+6), `npm run test:live -- tests/live/edit.live.test.ts` (25) all green on the REBASED tree. Walk of criteria 1/2/3/5 on a production build at 1440x900, both themes, staging sandbox reset before and after - full numbers in the History comment above. Headline: `is_flagged` box **243 -> 363** inside a container of 143 -> 363 (`translateY(-61px)`, both halves whole, `scrollHeight` now equals `clientHeight`), while `label` 205 -> 325, `tally` 271 -> 327 and `observed_on` 301 -> 357 carry `transform: none` and are drawn at exactly the pixels QA measured.
- **For QA**: `data-table.tsx` and `record-fields.tsx` were NOT touched - the container still clips exactly as it did, so BUG-0042's horizontal scroll is untouched by construction. Two traps worth knowing: the clipping ancestor is found by computed `overflow-y !== visible`, so a scroller added anywhere above the cell later will be honoured, and its PADDING box is the edge (`clientHeight` already excludes a horizontal scrollbar). A refusal TALLER than the whole container cannot be made to fit by moving - it is top-aligned by design and the container's own scroll reaches the rest; no refusal this surface produces today gets near that (the tallest is 120px against 220px). Also re-measured beyond the ticket: `tally` CLEARED (23502, row 4, h 120) was 28px outside and is now corrected too - the defect was never only row 5.
- 2026-09-09T06:56:00Z [dispatcher] landed ticket/BUG-0104 onto run/admin-window as 72d4af7
- 2026-09-09T06:56:47Z [qa] built → qa: under attack
- 2026-09-09T07:12:57Z [qa] qa → done: Attacked on the LANDED run branch (726e73b) in a detached `_receipt-` worktree, criteria re-run BEFORE reading the builder's walk. All three stored checks green there and again under `receipt.py` (lint+tsc exit 0; editable-cell.test.ts + page.test.ts 221 passed; edit.live.test.ts 25 passed).

Own walk: production build on 127.0.0.1:8795 (bind-probed, never 8770/8771/8772), staging ubfjjqlvnpnoborczbdb, bundled Chromium, 1440x900, BOTH themes, walk_sandbox ...0001, reset-sandbox before AND after (both "deleted 3, seeded 3, read back and verified").

C1 GREEN: is_flagged cleared -> box 243 -> 363 inside a container of 143 -> 363, transform matrix(1,0,0,1,0,-61); mono half 245 -> 341, app-voice half 343 -> 361, outsideTop/outsideBottom 0 and fullyOutside false for BOTH halves, container scrollHeight 220 == clientHeight (was 281 vs 220). Byte-identical in dark.
C2 GREEN, and I measured them myself rather than trusting the numbers: label 205 -> 325, tally/22P02 271 -> 327, observed_on/22007 301 -> 357, all `transform: none` — the measured shift moves nothing that already fits. tally CLEARED (a case beyond the ticket) 243 -> 363, translateY(-28px).
C3 GREEN: rows 169.5/198.5/231.5/264.5/297.5/330.5 and cells x 209/459.3/1175.7 w 250.3/716.41/247.3, identical before, during and after a commit, both themes. elementFromPoint 4px inside the lifted box returns BUTTON `note of walk_sandbox`, at its centre a TD — inert where it hangs over another row's control, and clicking that covered `note` button really opens the field.
C4 GREEN: six statusShift pins, each rule proved both ways. C5 GREEN: saving... at t+2ms, saved at t+176ms in healthy, gone at t+1689ms (~1513ms); refusal -> valid save clears the transform (saving/saved at 304 -> 324, transform none).

Attacks that found nothing: viewport resize while the refusal stands (1440x900 -> 1100x640 -> back; both halves inside at every step); container scrolled horizontally to its end with the refusal up (transform survives, box still inert); double and TRIPLE Enter inside one commit window (one PATCH, one alert, value stands); the identical refusal twice in a row; refusal on two fields at once (both correct, one shifted one not); SSR/hydration (no React warning in the server log, no pageerror, console carries only the 7 documented 500s of BUG-0068's refusal path); a refusal taller than the whole container — NOT constructible: with `note` at 349 chars of short words the box is 136px in a container that grew to 252, because Postgres caps the "Failing row contains" detail itself.

One divergence found and filed as BUG-0105 (P3, X axis, pre-existing — STATUS_BOX is untouched by e1f75bf): a >=180-char unbroken paste into observed_on makes the mono half's text paint 1117px past the container's right edge (mono scrollWidth 1987 vs box 312; container scrollWidth 2331 vs 1214). Not this ticket's defect and not a reason to bounce it — statusShift is vertical and answers 0 correctly there. [resolution: fixed] [receipt green: 3 cmd(s), tree c07280bd]
