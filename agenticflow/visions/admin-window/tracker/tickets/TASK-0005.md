---
id: TASK-0005
type: task
status: done
milestone: M1
priority: P1
opened_by: architect
parent: FEAT-0001
children: []
assignee: null
claimed_at: null
depends_on: [TASK-0004, TASK-0002]
touch_scope: [src/app, src/components, tests/http, tests/offline/shell]
force: false
attempts: 1
resolution: fixed
---
# Remove every deprecated surface; stand up the shell and the six routes

## Description
Module: `src/app/` (the shell and the route skeleton). Authority: spec section 3 and section 4; acceptance doc ground rule 'the build owns `src/` wholesale'; SPEC F1; M1 EC2, EC3, EC12; ARCHITECTURE section 2 (the exact survives/dies list) and section 5.

**Delete outright** — page, route, and any module whose only consumer was one of them:
`src/app/analytics/`, `src/app/database/`, `src/app/data-management/**` (including `completeness/`), `src/app/api/admin/events/[id]/route.ts`, `src/app/api/admin/groups/[id]/route.ts`, `src/app/api/admin/idols/[id]/route.ts`, `src/app/api/admin/catalog-search/route.ts`, `src/app/components/AdminNav.tsx`, and the whole `src/app/components/` directory once `EditableCell` has moved (TASK-0004). The events editor is **removed, not converted** — events stop being hand-editable until the verdict slice lands (spec section 3). Nothing is kept as dead code 'for reference': the contracts are the reference.

**Do not touch**: `src/middleware.ts`, `src/lib/auth.ts`, `src/lib/admin.ts`, `src/lib/supabase.ts`, `src/app/api/auth/[...nextauth]/route.ts`, `src/app/api/health/route.ts`, `src/app/login/page.tsx`, `railway.toml`, `supabase/migrations/`.

**Rewrite `src/app/layout.tsx`** as the shell: a 192px fixed left sidebar, chrome fill, hairline right edge, the six pages as **text labels with no icons** (the old unicode glyphs are dropped — one is a combining-mark hack), active item = chrome-inverse fill, sign-out at the bottom behind a hairline, content 16px-padded. **The layout performs no database read**: the 'Events: STALE/FRESH' strip and its `events` query are deleted (LOOK_AND_FEEL: no global status strip, the Dashboard owns health) — and a read in the root layout would take every page down when a table is absent, which acceptance test 9 forbids.

**Create the six routes** as real pages that render their `Page` shell, their title and their `Empty`/`NotProvisioned` state — no data yet, that is each page's own ticket:
`/` Dashboard, `/queues`, `/claims`, `/sources`, `/cycles`, `/browse`. Also create `/records/[table]/[id]` as a stub the edit tickets fill in. Each page function is `async` and is the only async component on its route (ARCHITECTURE section 5).

**Extend `tests/http/auth.http.test.ts`** to assert, for **every** route above plus `/queues/<id>` and `/records/groups/<id>`, that an unauthenticated request redirects to `/login`; and add the client-bundle scan: no service-role key material and no `SUPABASE_SERVICE_ROLE_KEY` value in any file `.next` emits for the client. Assert on the key NAME and on the shape of a key, never by printing a value.

Also assert the retired routes are **gone, not redirected**: a request to `/analytics`, `/database`, `/data-management` and `/data-management/completeness` returns 404.

## Acceptance criteria
[test 1, test 12; EC2, EC3, EC12] `npm run lint`, `./node_modules/.bin/tsc --noEmit` and `npm run build` exit 0. No retired page or route exists on disk and each retired path returns 404 rather than redirecting to a working page. No surviving module names `AdminNav`, and `src/app/components/` is gone. The six routes exist, each reachable from the sidebar, each rendering its title and a not-provisioned/empty state without touching the database from the layout. Every route and the two dynamic routes redirect an unauthenticated request to `/login`, and a scan of `.next`'s client output finds no service-role material. Sign-in, `src/lib/supabase.ts` and `railway.toml` are byte-unchanged. (human-checkable) The shell matches LOOK_AND_FEEL's frame — 192px sidebar, text labels, no icons, no status strip — at the close-review screenshots.

## Checks
```sh
npm run lint
./node_modules/.bin/tsc --noEmit
npm run build
bash -c "test ! -e src/app/analytics && test ! -e src/app/database && test ! -e src/app/data-management"
bash -c "test ! -e src/app/components && test ! -e src/app/api/admin/events && test ! -e src/app/api/admin/catalog-search"
bash -c "! grep -rq AdminNav src"
test -f src/app/queues/page.tsx && test -f src/app/claims/page.tsx && test -f src/app/sources/page.tsx && test -f src/app/cycles/page.tsx && test -f src/app/browse/page.tsx && test -f src/app/page.tsx
bash -c "grep -q getSupabaseAdmin src/lib/supabase.ts && grep -q SUPABASE_SERVICE_ROLE_KEY src/lib/supabase.ts"
bash -c "grep -q 'auth as middleware' src/middleware.ts && grep -q admin_allowed_emails src/lib/auth.ts"
bash -c "grep -q healthcheckPath railway.toml"
npm run test:http
```
## History
- 2026-09-02T05:36:26Z [architect] opened: Remove every deprecated surface; stand up the shell and the six routes
- 2026-09-02T05:36:56Z [architect] checks amended: replaced a git-diff-vs-HEAD pin (vacuous at receipt time: the ticket's own commit IS HEAD) with content pins on the carried-over files, and made the http suite self-contained by building first [was: npm run lint, ./node_modules/.bin/tsc --noEmit, npm run build, bash -c "test ! -e src/app/analytics && test ! -e src/app… (637 chars; full text in git history)]
- 2026-09-02T05:38:40Z [architect] checks amended: replaced for-loop presence checks with explicit test chains: the loop variable inside a double-quoted bash -c is expanded by the OUTER shell to empty, so the check was a guaranteed false-RED (dry-run 2026-09-01) [was: npm run lint, ./node_modules/.bin/tsc --noEmit, npm run build, bash -c "test ! -e src/app/analytics && test ! -e src/app… (779 chars; full text in git history)]
- 2026-09-02T06:09:56Z [dispatcher] Relayed from QA on TASK-0001 (residual, unfiled): the http harness strips *SUPABASE* names from the child env, but next start re-loads them from the repo's .env at runtime (loadEnvConfig on the repo root). The no-database guarantee is therefore unenforced; the first http test of a route that calls the service-role client is where it bites. Handle when extending the http suite to every route.
- 2026-09-02T07:28:52Z [builder-9] claimed (attempt 1)
- 2026-09-02T07:41:28Z [builder-9] claimed → built: Deleted Analytics/Database/Data Management/the three PATCH routes/catalog-search/AdminNav and all of src/app/components. Root layout is now the Frame only: synchronous, no DB read, no STALE/FRESH strip; 192px chrome sidebar (w-48 x --spacing:4px, checked in the emitted CSS), six text labels, active = chrome-inverse, sign-out behind a hairline. Content padding is the Page primitive's 16px — main deliberately carries none, or it would be 32px and off-scale. /login renders outside the Frame (Shell checks pathname), as before. Six async pages + /records/[table]/[id] stub, each Page + title + Empty.

Verified on the rebased tree, all 11 ticket checks PASS: lint, tsc, build, the not-exists chains, no AdminNav, six page files, the supabase.ts/middleware.ts/auth.ts/railway.toml content pins, npm run test:http (6 passed). npm test 178 passed. git diff run/admin-window..HEAD over the carried-over files shows only the four sanctioned api/admin deletions; supabase.ts, middleware.ts, auth.ts, admin.ts, login/page.tsx, railway.toml, supabase/ byte-unchanged.

TRAPS FOR QA. (1) Retired paths CANNOT 404 unauthenticated: Next 16's proxy runs before routing, so the gate redirects a stranger first. The http suite mints a session cookie with the harness's throwaway AUTH_SECRET (now exported) and asserts 404 signed-in, guarded by first asserting the same cookie gets 200 on the six routes — a broken cookie reddens instead of faking the 404. Observed red-then-green: restoring src/app/analytics/page.tsx gives '/analytics: expected 200 to be 404'. (2) Outside touch scope, as the ticket intended: tests/offline/db/layering.test.ts drops the three exemptions for files this ticket removed or rewrote, keeps src/lib/supabase.ts, and renames the now-stale constant DEPRECATED_UNTIL_TASK_0005 to CARRIED_OVER. No rule weakened; the ratchet still asserts every remaining entry exists. (3) The bundle scan asserts by NAME and SHAPE only, never a value, over .next/static plus the prerendered .html/.rsc, with a self-check that each pattern matches a sample. Honest but weakly loaded here: this worktree has no .env, so the build had no Supabase names to inline. (4) The .env-reload residual does not bite: nothing added here reads a database, and the layout no longer does either.
- 2026-09-02T07:41:51Z [dispatcher] landed ticket/TASK-0005 onto run/admin-window as dccf31c
- 2026-09-02T07:42:31Z [qa] built → qa: under attack
- 2026-09-02T07:49:23Z [qa] qa → done: Attacked on the landed tree (dccf31c) in the primary checkout. Receipt GREEN: all 11 stored checks exit 0, incl. npm run test:http. Full ci_command exit 0 (178 offline + 6 http).

CRITERIA, each driven rather than read. Retired surfaces gone from disk (three not-exists chains) AND 404 signed-in for /analytics, /database, /data-management, /data-management/completeness — answered identically to /no-such-surface-here, so none kept a special case on the way out. No AdminNav anywhere in src; src/app/components/ gone. Six routes + /records/[table]/[id] present, each 200 with an <h1>, each href in NAV_ITEMS pinned to an existing page.tsx by tests/offline/shell/shell.test.ts:46-49. Byte-unchanged vs main confirmed file-by-file: src/lib/supabase.ts, auth.ts, admin.ts, src/middleware.ts, src/app/login/page.tsx, api/auth/[...nextauth]/route.ts, api/health/route.ts, railway.toml, supabase/ (git diff --quiet, all clean). NO DB READ FROM THE LAYOUT: grep of src/app/layout.tsx + src/components/shell/ for lib/db|getSupabaseAdmin|supabase returns nothing, layout is synchronous, and all six routes serve 200 from a server whose env had every *SUPABASE* name deleted.

ATTACK 1 - the minted-cookie mechanism (the ticket's own trap). The throwaway secret does NOT reach the artifact: grep of .next for the literal finds nothing, AUTH_SECRET appears in no file under src/ and in no file under .next/static. The app accepts the cookie only because next start is handed it in env. Extended tests/http/auth.http.test.ts (commit 32bf94f) with six FORGERIES asserted across all 8 GATED_ROUTES plus a retired path: wrong secret, right secret bound to a different cookie name (salt), expired, tampered signature, malformed, empty. All six turned away to /login on every route, and a forgery gets the redirect at /analytics too — it never learns which paths exist. Watched red first: pointing the "wrong secret" variant at the real secret yields "/ with a token signed with a different secret: expected 200 to be greater than or equal to 300".

ATTACK 2 - the bundle scan, driven not trusted. Planted synthetic key-shaped strings directly into the emitted artifact and confirmed each redddens: JWT shape in .next/static/.../_ssgManifest.js (offset 83), sb_secret_ prefix in .next/server/app/claims.html (offset 9881), SUPABASE_SERVICE_ROLE_KEY name in .next/static/chunks/*.css (offset 24407). Failures name file+offset and never quote the match. Note this checkout HAS a repo-root .env, so my build loaded real names — the scan is meaningfully loaded here, unlike the builder's worktree where it was vacuous, and it is still clean.

ATTACK 3 - the new dynamic route. /records/<table>/<id> with 9 absurd inputs signed-in: script/img XSS payloads (React escapes; no raw injection in the body), %00, 2000-char segments, ..%2f traversal, unicode+emoji, SQL-ish. All 200, zero 500s; extra segment and missing id are 404.

OUT-OF-SCOPE EDIT, ruled: tests/offline/db/layering.test.ts is NECESSARY and MINIMAL. Its ratchet asserts every exempted path still exists, so deleting the three deprecated files would have reddened it. No rule weakened - the list shrank 4->1, which TIGHTENS it: src/app/page.tsx is no longer exempt, so the new Dashboard is now subject to the layering rules and passes on its merits. Not a bounce.

Zero bugs filed. Two residuals in the handoff (middleware matcher prefix lookahead; the .env-reload weakening of the "Supabase stripped" claim) - neither reachable from any path a criterion or receipt drives today. [resolution: fixed] [receipt green: 11 cmd(s), tree 5f20243b]
