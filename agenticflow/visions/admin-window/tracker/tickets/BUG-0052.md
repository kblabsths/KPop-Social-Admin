---
id: BUG-0052
type: bug
status: qa
milestone: M1
priority: P2
opened_by: designer
parent: null
discovered_from: M1-endgame-user-sim
children: []
assignee: builder-83
claimed_at: 2026-09-04T00:28:12Z
depends_on: []
touch_scope: [src/app/records/[table]/[id]/page.tsx, tests/offline/records/page.test.ts]
force: false
siblings: [BUG-0053]
attempts: 1
---
# Record page: the unknown-id empty state sends the operator to Browse, which does not list groups or idols

## Description
Found by the document-blind user-sim walk (Priya, 2026-09-03), `agenticflow/tracker/evidence/user-sim-m1/priya.md` Leg 2; confirmed live on :8771 and at source 2026-09-03.

**Voice copy bar 4:** "Every empty and not-provisioned state names what the surface holds and **what fills it**."

`/records/groups/<unknown-id>` and `/records/idols/<unknown-id>` render:

    No groups record with that id
    Browse lists the records that exist; check the id in the address bar.

**Browse does not list groups or idols.** It is the recent-events view and nothing else (SPEC F7: one curated view ships). So the empty state names a filler that cannot fill it — it points the operator at a page that structurally cannot help, in the one moment they most need help.

The sentence is a static literal, applied to every table the map carries, at `src/app/records/[table]/[id]/page.tsx:164`:

    filledBy="Browse lists the records that exist; check the id in the address bar."

For `events` it is true (Browse links to `/records/events/<id>`). For `groups` and `idols` it is false. One string, two regimes.

Priya's own words: "Browse does not list groups. The tool sent me to a page that cannot help me, in the one moment I most needed help. **This is where I would have closed the tab.**" She then left the app for a SQL client to obtain a group id.

Confirmed live 2026-09-03 on the walk instance:
`GET /records/groups/00000000-0000-0000-0000-000000000000` -> 200, body contains "No groups record with that id | Browse lists the records that exist; check the id in the address bar."

Evidence: `agenticflow/tracker/evidence/user-sim-m1/07-group-nonexistent-id.png`.

**Not a scope request.** That groups/idols have no listing or search is deliberately outside M1 (SPEC F7, and see `DESIGNER-JUDGEMENT.md`). This ticket is only that the copy must stop claiming a route the app does not have. What replaces it says what is true for a pre-cutover table — that these records are reached by id, and where an id comes from — without pointing at Browse.

## Acceptance criteria
The empty state on `/records/groups/<unknown-id>` and `/records/idols/<unknown-id>` no longer tells the operator that Browse lists the records that exist. What it says instead is true of a pre-cutover table: it names how such a record is reached (by id) and does not name a surface that cannot list it.

The `events` (and `venues`) empty state may keep naming Browse, because Browse does list events — the fix is regime- or table-aware copy, not a blanket deletion.

The delivered HTML of `/records/groups/<unknown-id>` contains no sentence claiming any in-app surface lists groups.

`tests/offline/records/page.test.ts` asserts the rendered empty-state copy for `groups` and for `events` separately, and asserts the groups copy does not contain "Browse".

Human-check: open `/records/groups/00000000-0000-0000-0000-000000000000` and follow the sentence's advice — it must lead somewhere that exists. (human-checkable)

## Checks
```sh
npx vitest run --project=offline tests/offline/records/page.test.ts tests/offline/records/provenance.test.ts
test "$(grep -cF "filledBy=\"Browse lists the records that exist" "src/app/records/[table]/[id]/page.tsx" || true)" -eq 0
./node_modules/.bin/tsc --noEmit
npm run lint
```
## History
- 2026-09-03T22:25:52Z [designer] opened: Record page: the unknown-id empty state sends the operator to Browse, which does not list groups or idols
- 2026-09-03T22:26:03Z [designer] checks amended: Source pin corrected: the criteria let the events empty state keep naming Browse, so pinning the sentence's total absence would go false-RED on a correct fix. Pin the UNCONDITIONAL form instead — the static 'filledBy="Browse ...' attribute must be gone; a regime-aware fix renders it through an expression. [was: npx vitest run --project=offline tests/offline/records/page.test.ts tests/offline/records/provenance.test.ts, test "$(gr… (268 chars; full text in git history)]
- 2026-09-04T00:28:12Z [builder-83] claimed (attempt 1)
- 2026-09-04T00:35:53Z [builder-83] claimed → built: Regime-aware empty state: `foundBy(config)` in src/app/records/[table]/[id]/page.tsx replaces the static filledBy literal. pre_cutover (groups/idols): 'Admin has no <table> listing: such a record is reached by its id alone. Check the id in the address bar, or take one from the catalog database.' resolver_owned (events/venues): 'Browse lists recent events, and an event's record links to its venue. Check the id in the address bar.' Keyed on regime, not table name (no table name spelled in the page).
Proof, all on the rebased tree: `npx vitest run --project=offline tests/offline/records/page.test.ts tests/offline/records/provenance.test.ts` 69 passed; grep pin 0; `npx tsc --noEmit`; `npm run lint` exit 0. Red-then-green: the two new tests fail against `git show HEAD:` page ('groups names Browse'; identical fillers) and pass after. `npm run test:live -- tests/live/edit.live.test.ts` (the live suite that renders RecordPage) 7 passed against staging, pre-rebase; the rebase base added tracker files only, no src. Delivered-HTML walk on my own prod build, port 8779 (built+started with the STACK.md mapping, minted cookie, killed my pid, port free): /records/{groups,idols,events,venues}/00000000-0000-0000-0000-000000000000 all 200; occurrences of 'Browse' in the delivered HTML: groups 1, idols 1 (the sidebar nav link only), events 3, venues 3 (nav + the copy, twice via the flight payload). Empty card renders 'No <!-- -->groups record with that id' — the inter-element space survives the prod transform (BUG-0045 trap re-checked; my copy is a plain string prop, no JSX spaces added).
QA notes: the guard is two-fixture on purpose — a blanket deletion of the Browse sentence reddens 'keeps naming Browse for a resolver-owned table'. The nav-label guard imports NAV_ITEMS, so adding a nav destination re-checks the copy automatically. Venues keeps the Browse sentence per the criteria, and it is true via the one-click path (Browse event row -> event record -> venue link); no in-app surface lists venues directly. This worktree has no npm install of its own, so tests/offline/toolchain.test.ts and one live-guard case fail here for want of node_modules/.bin — both pass in the primary checkout (verified: 92 passed), and nothing in my diff touches them.
- 2026-09-04T00:36:12Z [dispatcher] landed ticket/BUG-0052 onto run/admin-window as 70beeb0
- 2026-09-04T00:36:54Z [qa] built → qa: under attack
