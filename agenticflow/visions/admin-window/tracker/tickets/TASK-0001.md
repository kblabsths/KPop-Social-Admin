---
id: TASK-0001
type: task
status: built
milestone: M1
priority: P1
opened_by: architect
parent: FEAT-0002
children: []
assignee: builder-2
claimed_at: 2026-09-02T06:20:02Z
depends_on: [DEP-0001]
touch_scope: [package.json, package-lock.json, vitest.config.mts, eslint.config.mjs, tests, .env.example]
force: false
attempts: 2
empty_diffs: 1
---
# Toolchain: Vitest, the offline/live/http suites, and a lint that exits 0

## Description
Module: repo toolchain (no product behavior). Stands up the test harness every later ticket plugs into. Authority: `contracts/admin-build.md` test 1 and its ground rules; SPEC F2; `agenticflow/docs/vision/STACK.md` section 4 (the runner choice and the three suites); ARCHITECTURE section 10.

Do exactly this:

1. Install `vitest` and `vite-tsconfig-paths` as devDependencies **only after DEP-0001 has landed them in `agenticflow/docs/ALLOWED_DEPS.md`** (the supply-chain hook blocks anything else).
2. `vitest.config.mts` with `tsconfigPaths()` so `@/*` resolves, and three projects/globs:
   - offline: `tests/offline/**/*.test.ts` — the default
   - live: `tests/live/**/*.live.test.ts`
   - http: `tests/http/**/*.http.test.ts`
   The offline glob must be structurally unable to reach `tests/live` or `tests/http`.
3. `package.json` scripts: `test` = the offline project, `test:live` = the live project, `test:http` = **`next build` followed by** the http project (self-contained: any ticket can run `npm run test:http` as a check without a build step of its own), `typecheck` = `tsc --noEmit`. Do not change `dev`, `build`, `start`, or `engines`.
4. **`eslint.config.mjs`: add `agenticflow/**` to `globalIgnores`.** Measured 2026-09-01: `npm run lint` reports 2064 errors, every one of them inside `agenticflow/.venv-tools/.../playwright/driver/**` (the kit's Python venv ships bundled JS). With the ignore, lint exits 0 in 2.8s. Acceptance test 1 is unreachable without it. Do not touch any rule, and do not add ignores for product paths.
5. `.env.example`: add the NAMES `STAGING_SUPABASE_URL` and `STAGING_SUPABASE_SERVICE_ROLE_KEY` with empty values, next to the existing `SUPABASE_*` names. **Names only — never a value, in any file, commit, ticket or note.** Do not edit `.env`.
6. `tests/offline/toolchain.test.ts`: one real test asserting the offline project's include globs exclude `tests/live` and `tests/http` — the structural proof behind 'offline by default'.
7. `tests/http/auth.http.test.ts`: starts the built app on port **8772**, asserts `/api/health` returns 200 and `/` redirects to `/login` for an unauthenticated request, and stops the server in a `finally`. It must need no database. TASK-0005 extends it to every route.

Interface contract for every later ticket:

- offline tests: `tests/offline/**/*.test.ts`, no network, run by `npm test`
- live tests: `tests/live/**/*.live.test.ts`, run by `npm run test:live`
- http tests: `tests/http/**/*.http.test.ts`, run by `npm run test:http` (builds first), port **8772**
- shared fixtures: `tests/fixtures/` (TASK-0002 seeds the builders)

Out of scope: any `src/` change, any product behavior, any other dependency, `.env`, `railway.toml`, `next.config.ts`.

## Acceptance criteria
[test 1] `npm run lint` exits 0 with zero errors and zero warnings from product paths, and `npm test` passes with **no** `STAGING_SUPABASE_*` names in the environment. `npm run test:live` and `npm run test:http` both exist as scripts. `.env.example` carries both STAGING names and no value. The offline suite cannot see `tests/live` or `tests/http` by construction, and that is asserted by a test rather than by inspection. No file under `src/` changed.

## Checks
```sh
npm run lint
./node_modules/.bin/tsc --noEmit
npm test
bash -c "grep -q agenticflow eslint.config.mjs"
bash -c "grep -q STAGING_SUPABASE_URL .env.example && grep -q STAGING_SUPABASE_SERVICE_ROLE_KEY .env.example"
bash -c "grep -q test:live package.json && grep -q test:http package.json && grep -q typecheck package.json"
bash -c "test -f vitest.config.mts"
./node_modules/.bin/vitest run tests/offline/toolchain.test.ts
```

## History
- 2026-09-02T05:35:32Z [architect] opened: Toolchain: Vitest, the offline/live/http suites, and a lint that exits 0
- 2026-09-02T05:36:56Z [architect] description amended: made test:http self-contained (it builds the app itself) so any ticket can run it as a check without a build step of its own [was: Module: repo toolchain (no product behavior). Stands up the test harness every later ticket plugs into. Authority: `cont… (2629 chars; full text in git history)]
- 2026-09-02T05:54:14Z [builder-1] claimed (attempt 1)
- 2026-09-02T06:01:25Z [builder-1] claimed → built: Vitest 3.2.7 + vite-tsconfig-paths 6.1.1 installed lockfile-faithfully; vitest.config.mts defines offline/live/http projects whose include globs come from the new tests/suite-globs.ts (single source of truth for globs + port 8772 — later tickets import it, never retype the globs). Scripts: test=offline, test:live=live, test:http='next build && http', typecheck=tsc --noEmit; dev/build/start/engines untouched. eslint.config.mjs gains exactly one globalIgnores entry (agenticflow/**); .env.example gains STAGING_SUPABASE_URL and STAGING_SUPABASE_SERVICE_ROLE_KEY, values empty. No src/ change (git status --porcelain src = 0 lines).
Verified on the rebased tree (43a6a6f on c06bb1b): npm run lint exit 0 in 3.6s zero output; tsc --noEmit exit 0; env -u STAGING_SUPABASE_URL -u STAGING_SUPABASE_SERVICE_ROLE_KEY npm test = 3 passed; npm run test:http = 1 passed, port released after. Red-then-green: widening OFFLINE_INCLUDE to tests/**/*.test.ts reddened all 3 offline assertions and pulled the http test into the offline run; restored. Surface observed by hand: /api/health -> 200 {"ok":true}, / -> 307 location /login?callbackUrl=...
Three things for QA: (1) toolchain.test.ts proves the partition by shelling out to 'vitest list --project=X --filesOnly' (the runner's own discovery, no import, ~0.4s each) — if that CLI flag changes the test breaks loudly rather than silently passing. (2) npm run test:live exits non-zero TODAY because tests/live/ has no files yet ('No test files found'); the script exists and the project resolves — the loud-refusal-on-unset-STAGING-names behaviour is TASK-0002's setup file, not mine. (3) DEP-0001 predicted a THIRD install-script package (esbuild) and called a fourth 'a new finding': npm reports FOUR blocked install scripts after this install — sharp, unrs-resolver, esbuild@0.28.2, and fsevents@2.3.3 (optional darwin watcher via vite/chokidar). npm 12 blocks all of them by default so none executed, and vitest runs fine without esbuild's postinstall; flagging for the toolsmith, not fixing it here. debug resolves to 4.4.3 only (0 occurrences of 4.4.2 in the lockfile).
- 2026-09-02T06:01:47Z [dispatcher] landed ticket/TASK-0001 onto run/admin-window as 223861c
- 2026-09-02T06:02:22Z [qa] built → qa: under attack
- 2026-09-02T06:09:03Z [qa] qa → reopened: BUG-0002 filed; bouncing on the http harness, not on the criteria.

Criteria re-run on the landed tree (223861c, primary checkout, npm ci lockfile-faithful; npm ci also emits the 4 blocked install scripts the builder flagged - esbuild/fsevents/sharp/unrs-resolver, none executed): npm run lint exit 0, zero non-notice output lines (grep -v "npm notice" = 0). tsc --noEmit exit 0. env -u STAGING_SUPABASE_URL -u STAGING_SUPABASE_SERVICE_ROLE_KEY npm test exit 0, 3 passed. test:live/test:http/typecheck all present in package.json scripts; dev/build/start/engines unchanged. .env.example carries STAGING_SUPABASE_URL and STAGING_SUPABASE_SERVICE_ROLE_KEY, both ="" (names only, no value; .env never opened). Offline partition asserted by tests/offline/toolchain.test.ts via the runner own discovery, not by inspection. git diff --name-only 1d259cc..223861c -- src = 0 files; git status --porcelain src = 0 lines. Full ci_command (lint && tsc && npm test) exit 0 - which also means dispatcher BUG-0001 (CI red, "Cannot find module vitest") was a missing install in the primary checkout, not a code defect.

Attack (one pass, this ticket surface: the offline/live/http partition, the lint ignore, .env.example, and the port-8772 no-database http test):
1. PORT OWNERSHIP - BROKE IT. Expected: the http suite asserts against the app it starts on 8772. Found: a foreign listener on 127.0.0.1:8772 answering 200 on /api/health and 307 -> /login served ALL THREE requests the suite made and the suite passed green in 30ms, exit 0, with next start never binding. tests/http/auth.http.test.ts:41-49 (waitForReady polls the port, not the child) and :52-62. test:http is the self-contained check every later ticket runs, so two concurrent lanes = lane B certifying lane A build. BUG-0002, with a committed RED pin (1c46dfa) in the file that owns the surface: "refuses to certify a server it did not start when the port is already held" -> fails today with the exact message, passes when startServer rejects.
2. NO-DATABASE GUARANTEE - RESIDUAL, not filed (lean cap: one BUG). Expected (item 7): the http test needs no database. Found: serverEnv() (tests/http/auth.http.test.ts:14-30) deletes every *SUPABASE* name, but next start re-loads them from the repo .env at runtime - NextServer.loadEnvConfig, node_modules/next/dist/server/next-server.js:575, loadEnvConfig(this.dir) with this.dir = repo root, where a .env exists (test -f only; never opened). Measured on a FABRICATED env file in the scratchpad: after the test-style strip both names read absent, after loadEnvConfig both read present (names only, no values printed anywhere). Harmless today - /api/health and / touch no DB - but src/lib/supabase.ts:3-4 reads exactly SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY, so the moment TASK-0005 adds a data route to this suite it runs against whatever .env points at, against the FEAT staging-only/production-never rule. Trigger that makes it a ticket: the first http test of a route that calls getSupabaseAdmin().
3. Lint ignore: exactly one globalIgnores entry (agenticflow/**), no rule touched, no product path ignored - confirmed by reading eslint.config.mjs and by lint exiting 0 with empty output.
4. npm run test:live exits non-zero today ("No test files found") - correct per criteria: the criterion asks only that the script EXIST, and it does; tests/live is TASK-0002 seed. Not a defect.

No product source touched by QA. Only tests/http/auth.http.test.ts, committed as 1c46dfa.
- 2026-09-02T06:20:02Z [builder-2] claimed (attempt 2)
- 2026-09-02T06:22:43Z [builder-2] claimed → built: Re-verification only — EMPTY DIFF. ticket/TASK-0001 rebased onto run/admin-window and is identical to it (df2dbd2); the bounce's defect was already fixed and landed by BUG-0002 (3dbec35: tests/http/server-harness.ts + its two new tests in tests/http/auth.http.test.ts). Nothing was short of a criterion, so nothing was invented.

Verified on the rebased worktree after a lockfile-faithful npm ci (npm again blocks 4 install scripts — esbuild/fsevents/sharp/unrs-resolver; none executed, none needed): npm run lint exit 0, zero output lines after dropping 'npm notice'; ./node_modules/.bin/tsc --noEmit exit 0; 'env -u STAGING_SUPABASE_URL -u STAGING_SUPABASE_SERVICE_ROLE_KEY npm test' exit 0 with 0 STAGING_SUPABASE names in the child env, 3 offline tests passed; npm run test:http (next build + http project) exit 0, 3 passed, port 8772 free before and after; vitest run tests/offline/toolchain.test.ts 3 passed. Greps: eslint.config.mjs:17 'agenticflow/**' (one globalIgnores entry, no rule touched, no product path ignored); .env.example:8-9 STAGING_SUPABASE_URL="" and STAGING_SUPABASE_SERVICE_ROLE_KEY="" (names only); package.json has test:live, test:http, typecheck with dev/build/start/engines byte-identical to base. git diff --name-only 1d259cc..HEAD -- src = 0 files; git status --porcelain src = 0 lines.

BUG-0002's fix observed failing-correctly at its real surface, not just via its own test: with a foreign listener I spawned on 127.0.0.1:8772 (a 200 /api/health + 307 -> /login impostor, i.e. QA's exact false-pass shape), 'vitest run --project=http -t "serves health"' now FAILS in 11ms with 'port 8772 is already held by another process ... refusing to certify a server it did not start' — where before the fix that same setup went green in 30ms. My listener was killed; I killed no process I did not start.

For QA: (1) npm run test:live still exits non-zero ('No test files found') — tests/live/ is TASK-0002's seed; the criterion only asks the script exist. (2) QA's residual #2 stands unfixed and unfiled by design: 'next start' re-loads the repo .env via NextServer.loadEnvConfig, so stripping SUPABASE names in serverEnv() does not keep them out of the app under test. Harmless for /api/health and / (no DB touched), and choosing between 'unset' and 'set-to-empty' semantics is a real fork that FEAT-0002's 'an unset name is a refusal' rule belongs to — TASK-0005 owns it at the moment the http suite first exercises a route calling getSupabaseAdmin(). I did not guess it here. [EMPTY DIFF: no file changed under scope — 1 so far]
- 2026-09-02T06:23:04Z [dispatcher] nothing to land: ticket/TASK-0001 has no commits and a clean worktree — on to QA as-is
