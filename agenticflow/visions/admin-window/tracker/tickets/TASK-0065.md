---
id: TASK-0065
type: task
status: done
milestone: M3
priority: P1
opened_by: architect
parent: FEAT-0015
children: []
assignee: null
claimed_at: null
depends_on: [TASK-0063, TASK-0074, BUG-0166]
touch_scope: [src/lib/db/claims.ts, src/lib/claims/lines.ts, src/components/claims/claim-list.tsx, src/app/claims/page.tsx, tests/offline/claims/read.test.ts, tests/offline/claims/page.test.ts, tests/offline/claims/population.ts]
force: false
attempts: 1
resolution: fixed
---
# Claims: the window read takes an explicit offset, and the row shaping becomes an importable leaf

## Description
**Modules:** `src/lib/db/claims.ts` (the window read gains an explicit offset) and a NEW pure leaf `src/lib/claims/lines.ts` (the row shaping the page owns today moves out so a route handler can call the SAME one). `src/app/claims/page.tsx` and `src/components/claims/claim-list.tsx` change by IMPORT only.

**Nothing rendered changes in this ticket.** `/claims` must render byte-identically on the same fixtures before and after: same rows, same window line, same figures, same order.

**Why the shaping moves.** `claimLines()` lives in `src/app/claims/page.tsx` and `ClaimLine` lives in `src/components/claims/claim-list.tsx`. TASK-0066's route handler must return rows shaped exactly as the first screen's, and a second copy of that mapping is the class LESSONS 5 names (a shared spelling gets imported, never retyped) and common violation 9's shape. It is a pure function over rows plus a name map — no client, no env, no table name — so by common violations row 17 it belongs in the leaf layer wherever its first caller happened to live.

**Interface contract:**

```ts
// src/lib/claims/lines.ts   (LEAF: imports lib/sources/names, lib/records/routes,
//                            lib/claims/filters — leaf-to-leaf is a DAG, §4 rule 7)
export interface ClaimLine { /* moved VERBATIM from claim-list.tsx, docstrings included */ }

/** One claim as the list renders it, in the order the database returned them. */
export function claimLines(
  claims: readonly ClaimRow[],
  names: ReadonlyMap<string, string>,
): ClaimLine[];
```

`ClaimRow` is declared in `lib/db/claims.ts` today. A leaf may not import `lib/db/**`, **not even a type** (§4 rule 7, first paragraph). So the row shape this leaf reasons over is declared IN THE LEAF and `lib/db/claims.ts` imports it back — exactly what `ReviewItemRow` in `lib/review/shapes.ts` already does. Move `PendingClaimRow` / `ClaimRow` (and only those two interfaces) into `src/lib/claims/lines.ts`, re-export them from `lib/db/claims.ts` so no other caller changes, and keep every docstring.

`src/components/claims/claim-list.tsx` imports `ClaimLine` from the leaf and re-exports the type for existing importers. It renders exactly what it renders today.

```ts
// src/lib/db/claims.ts
export function readClaimWindow(
  options: { filter?: ClaimsFilter; limit: number; offset?: number },
  db?: SupabaseClient,
): Promise<DbResult<ClaimRow[]>>;
```

**The offset is a `.range()`, and the order does not move.** The chain keeps `.order("observed_at", { ascending: true, nullsFirst: false })` then `.order("observation_id", { ascending: true })` — the total order that makes window membership deterministic — and ends in `.range(offset, offset + limit - 1)` in place of `.limit(limit)`. `offset` defaults to 0, so every existing caller is unchanged in behaviour. `ClaimQuery` gains a `range(from: number, to: number): ClaimQuery` member (the chain is written out as a shape in this file on purpose — read its docstring before you touch it). The offline stubs that record `.limit(...)` for this read are updated in the same ticket; what may NOT change is the rendered output on a fixture.

**The unconditional exclusion is a property of `narrowed()` and stays there** (§6 trap 4): a paged read is the same narrowing at a different offset, and `selectClaims` still runs over what comes back. A paged read that skipped either would leak the parked `in_window` bucket into a rendering.

**Brief (common violations row 19 caveat):** this ticket adds no read. It changes the bound of one existing read and moves one pure function.

## Acceptance criteria
readClaimWindow accepts an optional offset, defaults it to 0, and issues one request whose chain is observed_at asc nullsFirst:false, then observation_id asc, then range(offset, offset + limit - 1) — asserted against the offline stub for offset 0 and for offset 50, including that the parked in_window bucket is excluded at both. claimLines and the ClaimLine, ClaimRow and PendingClaimRow interfaces live in src/lib/claims/lines.ts with their docstrings intact; src/app/claims/page.tsx declares neither and imports both; src/components/claims/claim-list.tsx imports the type rather than declaring it; grep shows exactly one declaration of claimLines and one of ClaimLine in src. The leaf imports nothing that can reach a database and tests/offline/db/layering.test.ts is green over it. /claims renders byte-identically to the pre-ticket tree on the offline suite's fixtures — same rows, same order, same window line, same figures — and its live parity suite is green.

## Checks
```sh
./node_modules/.bin/vitest run --project=offline tests/offline/claims tests/offline/db/layering.test.ts
./node_modules/.bin/vitest run --project=offline tests/offline/gauges tests/offline/absence/pages.test.ts
grep -q "^export function claimLines" src/lib/claims/lines.ts
grep -q "^export interface ClaimLine" src/lib/claims/lines.ts
test "$(grep -rn "^export interface ClaimLine" src | wc -l)" -eq 1
npm run lint
npm run build
npm run test:live -- tests/live/claims.live.test.ts
```
## History
- 2026-09-10T21:40:01Z [architect] opened: Claims: the window read takes an explicit offset, and the row shaping becomes an importable leaf
- 2026-09-10T21:40:13Z [architect] checks amended: measured on the tree: 'export interface ClaimLine' already counts 1 today (claim-list.tsx), so the count alone could not see the move — pin the new home instead, and keep the count as the no-second-copy guard [was: ./node_modules/.bin/vitest run --project=offline tests/offline/claims tests/offline/db/layering.test.ts, ./node_modules/… (428 chars; full text in git history)]
- 2026-09-10T22:12:15Z [architect] depends_on: [TASK-0063] → [TASK-0063, TASK-0074]: Destination-serial ruling (architect, 2026-09-10): TASK-0074 rewrites the pending-claims gauge's read path and lands in src/lib/db/claims.ts and src/app/claims/page.tsx - TASK-0065's own destinations, and the head of the /claims chain (0065 -> 0066/0067 -> 0070 -> 0071 -> 0072). Landing the latency work FIRST means one rebase here instead of TASK-0074 rebasing onto five tickets of rewrites, and it keeps M3 EC9's page bar met early rather than at the milestone's tail. TASK-0063 stays free to run in parallel: its scope (src/lib/paging/**) is disjoint from TASK-0074's, so no edge is added there.
- 2026-09-10T22:59:36Z [architect] depends_on: [TASK-0063, TASK-0074] → [TASK-0063, TASK-0074, BUG-0166]: Ordering ruling (architect, 2026-09-10), existing edges kept. Honest scope note first: TASK-0065 itself never calls pageUrl — it changes one read's bound and moves the row shaping. The edge is a CHAIN-HEAD ordering: 0065 is the head of /claims (0065 -> 0066 -> 0067), and TASK-0066 is the handler that reads searchParams.get(OFFSET_PARAM) — the reader BUG-0166's duplicate parameter actually deceives, with TASK-0067 the writer that hands claimsQuery() into PageDeps.params. Putting the edge at the head orders the fix before every consumer in one edge instead of two. It costs no parallelism now (0065 is already serialized behind TASK-0074, and BUG-0166 is dispatchable immediately off done TASK-0063); if the builder cap rises and this edge is the only thing idling 0065, it may be dropped to TASK-0066 without asking me.
- 2026-09-10T23:54:54Z [builder-251] claimed (attempt 1)
- 2026-09-11T00:10:52Z [builder-251] claimed → built: - readClaimWindow takes an optional offset (default 0) and ends in .range(offset, offset+limit-1); order unchanged (observed_at asc nullsFirst:false, then observation_id asc). claimLines + ClaimLine + ClaimRow/PendingClaimRow move to the new leaf src/lib/claims/lines.ts, docstrings intact; lib/db/claims.ts re-exports them, claim-list.tsx and page.tsx changed by import only. claimView (population.ts) now honours .range() beside .limit().
- Verified on the REBASED tree (5bc1478 on 6ba396c): vitest --project=offline tests/offline/claims tests/offline/db/layering.test.ts (290 pass), tests/offline/gauges tests/offline/absence/pages.test.ts (520 pass), full npm test 3495+6 pass, tsc --noEmit clean, npm run lint clean, npm run build green, all four grep checks pass. Byte-identical rendering PROVED, not assumed: the page rendered in a detached worktree at b292796 and here over 8 fixture states (healthy / by-source / domain+bucket / standing tab / empty / not-provisioned / 2000-row / dropped-params) gives 171,915 bytes, sha256 08ae167ec5ac6928bf92674bba688fcea38c677b9324ba69ae92538568870c0a, on BOTH trees. New tests observed red with the change reverted, then green.
- TWO THINGS QA SHOULD KNOW. (1) PENDING_CLAIM_BUCKETS and PendingClaimBucket moved WITH the rows, beyond the ticket's 'only those two interfaces': PendingClaimRow.bucket is typed on the union and a leaf may not import lib/db back, so the union had to come too (the lib/review/shapes.ts precedent the ticket names declares ReviewQueue/Severity/Status beside ReviewItemRow the same way). Both are re-exported from lib/db/claims.ts; no caller changed; UNRENDERABLE_BUCKET, RENDERABLE_BUCKETS, isRenderableBucket, narrowed() and selectClaims all STAY in lib/db/claims.ts (trap 4 untouched). (2) I did NOT add lines.ts to LEAF_MODULES in tests/offline/db/layering.test.ts (out of touch_scope, and it cannot pass today: it would need filters.ts/sources/names.ts/records/routes.ts/url/narrowing.ts/url/dropped-params.ts registered too, and foreignImports() then reports the bare line 'import {' of filters.ts's multi-line import — a scanner limitation, not a violation). Instead read.test.ts now proves the property directly over the leaf's whole TRANSITIVE closure (no lib/db, no supabase-js, no process.env at any depth), with a non-vacuity pin; that guard was observed to flag a probe import of lib/db/tables.
- LIVE: npm run test:live -- tests/live/claims.live.test.ts was 12/12 GREEN pre-rebase (17:07). Post-rebase two runs each hit the known staging flake, a different test each time, both of the two-live-reads identity shape: 17:09 'renders each bucket's count exactly as the view holds it'; 17:09:43 'the windowed claims read and the id-list join select the same claims', 878 ids vs 877, the extra being 01a08dcc-782c-797c-a56a-d1af9994fe4d inserted by the scraper between the two legs. Neither leg of that test calls readClaimWindow - both are raw PostgREST queries in the test file (lines 430-486) - so it cannot see this diff. Not retried to green, per the ticket's standing instruction.
- 2026-09-11T00:11:19Z [dispatcher] landed ticket/TASK-0065 onto run/admin-window as cc2bafd
- 2026-09-11T00:11:42Z [qa] built → qa: under attack
- 2026-09-11T00:19:29Z [architect] FYI for the lane in flight (architect, 2026-09-10), no change to this ticket: BUG-0168 landed a ruling on the paging contract — a route answers a FULL page that continues or a short/empty page that says the set is exhausted, never a short continuing one, and the driver refuses anything else (ARCHITECTURE §4.3, DECISIONS 2026-09-10). readClaimWindow's own shape is unaffected: it returns what the range asked for and the ROUTE (TASK-0066) derives the flag from the row count. Nothing here needs to change; recorded so the next reader of this ticket's window semantics sees the rule that now sits above it.
- 2026-09-11T00:22:17Z [qa] qa → done: attacked on the landed tree (cc2bafd), criteria re-run before the handoff story; receipt GREEN on all 8 stored checks (live claims 12/12, run twice, no flake).

LIVE ATTACK on the thing the offline stub cannot see — the new offset against real staging pending_claims, through readClaimWindow with an independent service-role client (throwaway probe, since deleted): readClaimWindow{limit:100} vs {limit:50} + {limit:50,offset:50} — page1 == whole[0:50], page2 == whole[50:100], overlap 0 ids; page1 last 01a058f1-02c7-7e11-9191-adfa6207a0f0, page2 first 01a058f1-02d3-7282-a5eb-0edc5e279740. Six {limit:1,offset:0..5} reads reproduce one {limit:6} read id-for-id. NOT added to tests/live: it is the same two-live-reads identity shape the architect is ruling flaky.
Bounds measured on staging (kind/rows): offset 10,000,000 -> ok 0; MAX_SAFE_INTEGER -> ok 0; limit 0 -> ok 0; limit -5 -> error "Requested range not satisfiable ... (PGRST103)" named, no throw; offset -1 -> ok 49; offset 1.5 -> ok 50; offset NaN -> ok 0. Unreachable today: no caller passes offset, and lib/paging/bounds.ts (TASK-0063) refuses sign/decimal/exponent/non-multiple/over-max. Residual for TASK-0066, not a bug here.

JUDGEMENT CALL 1 (union moved with the rows): sound. Expected: trap 4's exclusion stays in lib/db/claims.ts. Found: UNRENDERABLE_BUCKET, RENDERABLE_BUCKETS, isRenderableBucket, narrowed() and selectClaims all still there (src/lib/db/claims.ts:106-, 226-, 457-); every other consumer still imports from @/lib/db/claims or its db/gauges re-export (queues/[reviewItemId]/page.tsx:33, db/gauges.ts:70) — no caller changed. tsc/lint/build clean.

JUDGEMENT CALL 2 (leaf not in LEAF_MODULES): justification MEASURED, not taken on trust. Adding lines.ts alone to LEAF_MODULES -> red: foreignImports = [filters.ts, routes.ts, names.ts]. Adding its whole closure -> red on the bare line "import {" (layering IMPORT_LINE line 393 maps a multi-line import's opening line to itself). Accurate, and that file is outside touch_scope. Substitute guard graded on merits: I recomputed the leaf's transitive closure with the test's own resolution rules — 10 files (lines, claims/filters, records/routes, records/id, sources/names, url/text, url/narrowing, url/dropped-params, url/spellable, verdict/decision), all resolving, none matching lib/db|supabase-js|process.env. The property is proven today. Blind spot, residual below.

RENDERING (byte-identity): the page.test.ts diff is confined to the "the reads this page makes" describe (read-census only); all 183 rendering assertions are untouched and green, and live parity grades rendered markup against its own staging reads, 12/12. ClaimLine's interface body diffs byte-identical to its pre-ticket declaration in claim-list.tsx; claimLines' docstring moved intact with only the .limit->.range sentence updated.
Clause check "page.tsx declares neither and imports both": it declares neither and imports claimLines; ClaimLine now has no use site there, so importing it would be an unused, lint-fatal import. Met in substance.

RESIDUALS (no ticket filed, lean depth):
1. TASK-0066 must put searchParams' offset through lib/paging/bounds.ts. A raw Number("abc") -> NaN reaches .range(NaN,NaN) and staging answers ok with 0 rows — a malformed URL rendering as "exhausted" rather than a refusal (measured above).
2. The leaf's layering guard (read.test.ts:583-) stops silently at any specifier that does not resolve to a file: importsOf maps "@/components/ui" to "src/components/ui.ts", sourceText returns "" on ENOENT, and the subtree is never scanned. Demonstrated: point LEAF at src/components/claims/claim-list.tsx, which reaches src/lib/db three hops away (@/components/ui -> ./state-of -> @/lib/db), and the guard still passes 33/33. Correct for today's closure (all 10 files resolve), silently weakens the day a barrel import enters it. No user-visible path -> architect's call, not a BUG. [resolution: fixed] [receipt green: 8 cmd(s), tree 961a68c8]
