---
id: BUG-0202
type: bug
status: done
milestone: M3
priority: P3
opened_by: designer
parent: null
discovered_from: usersim:tomas
children: []
assignee: null
claimed_at: null
depends_on: []
touch_scope: [src/app/browse/page.tsx, tests/offline/browse/page.test.ts]
force: false
attempts: 1
resolution: fixed
---
# /browse discards a cols value that names no column, and every other parameter, in silence

## Description
Filed from the M3 user-sim walk of Tomas Berg (`agenticflow/tracker/for-human/M3-usersim-tomas.md`), 2026-09-11:

> "On **Browse**, the same games get nothing. `?cols=banana` renders all seven columns and says nothing. `?cols=` renders all seven columns and says nothing. `?table=venues` and `?q=BTS` are swallowed without a word. If I hand-edit a Browse URL and fat-finger a column name, I get the default view and no indication that what I asked for was discarded."

He shares a column view by URL — *"the column chips … survive in the URL so I can share the view"* — which is exactly the path on which a discarded `cols` matters: the recipient sees a different view from the sender's and nothing says so.

**Expected** — the same rule and the same module as the sibling ticket on `/sources`: `src/lib/url/dropped-params.ts`, authored for every route (BUG-0141) out of `/claims`' original (BUG-0123), rendered by `DroppedParamsLine`. LOOK_AND_FEEL bar 13 and the Look's *"no screen claims a mark it did not draw"*.

**Found** — `/browse` reads exactly one key and looks at no other: `const shown = shownColumns(view, params[COLUMNS_PARAM])` (`src/app/browse/page.tsx:124`, `COLUMNS_PARAM = "cols"`). `shownColumns` (`src/lib/browse/views.ts:154-170`) keeps only tokens that name a configured column and, when none does, silently returns the view's default set. `grep -rn "DroppedParamsLine\|droppedParams" src` returns three routes and `/browse` is not one of them.

**Deliberately NOT in this ticket: the partial case.** `?cols=title,banana` applied `title` and dropped one token. The shared rule's vocabulary is per KEY, not per token, and a token-level sentence would be a new kind of sentence on one page — the exact thing BUG-0141 moved the module to prevent. `cols` counts as applied when at least one of its tokens named a column; the fat-fingered token inside an otherwise-good list stays silent, on purpose, and that is recorded here rather than left for the next walker to re-find.

Provenance checked before filing: `git log src/app/browse/page.tsx` returns ticket-bearing commits only (BUG-0178, FEAT-0015, TASK-0068, BUG-0070 …). Not a human-lane divergence.

Sibling: BUG-0201 (`/sources`), same rule, disjoint files.

## Acceptance criteria
1. `/browse` names a parameter it did not apply, through the shared rule: the page calls `droppedParams` from `@/lib/url/dropped-params` and renders `DroppedParamsLine`. No second definition of the sentence.
2. `cols` is reported as APPLIED when at least one token it carried named a configured column, and as DROPPED when none did: `?cols=banana` and `?cols=nope,alsonope` are named; `?cols=title` and `?cols=title,banana` are not; `?cols=` (no value) is named by nobody, as the module already rules.
3. Every other key the URL carries — `?table=venues`, `?q=BTS`, anything — is named by the same line, verbatim in mono, under the module's existing allowlist and blank rules.
4. Nothing else moves: the column chips, their hrefs, the window line, the paged table, the row set and all four states render byte-identically to today for a bare `/browse` and for any URL whose `cols` applied.
5. The offline suite grades the new arm at the surface: a test named `names a cols value that chose no column as a parameter it did not apply` (that string, so the check below can find it) asserts the line is present for `?cols=banana`, absent for `?cols=title`, absent for `?cols=title,banana`, and absent for a bare `/browse`.
6. Human-check: at 1440x900, `/browse?cols=banana` says on screen that `cols` did not apply, and the seven default columns below it are then legible as the default rather than as the operator's choice. **(human-checkable)**

## Checks
```sh
npm run lint
npm run build
grep -q 'DroppedParamsLine' src/app/browse/page.tsx
grep -q 'names a cols value that chose no column as a parameter it did not apply' tests/offline/browse/page.test.ts
./node_modules/.bin/vitest run --project=offline tests/offline/browse/page.test.ts
npm run test:live -- tests/live/browse.live.test.ts
```

## History
- 2026-09-11T17:23:53Z [designer] opened: /browse discards a cols value that names no column, and every other parameter, in silence
- 2026-09-11T18:29:53Z [builder-302] claimed (attempt 1)
- 2026-09-11T18:37:08Z [builder-302] claimed → built: /browse now renders the shared DroppedParamsLine from droppedParams (no second sentence): cols is APPLIED when its value named at least one configured column, DROPPED when none did; every other key (table, q, tab, limit, ...) is named by the same line under the module's existing allowlist/blank rules; the line sits above the section so it renders over ok/refused/absent/empty reads alike.
Beyond touch_scope: src/lib/browse/views.ts — the parse shownColumns did inline is now the exported namedColumns, so the page asks the SAME function which columns a value chose rather than re-typing the split (LESSONS 5). shownColumns = namedColumns + the default; behaviour unchanged (views.test.ts 59 tests green untouched).
Verified: 6 new tests in tests/offline/browse/page.test.ts seen RED on the pre-fix tree (git checkout HEAD -- the two src files) and GREEN after — full offline suite 3968 pass, tsc clean, lint clean, npm run build green, npm run test:live -- tests/live/browse.live.test.ts 6 pass. Observed at the surface on a production build against staging (port 8789, own server, killed after): /browse -> no line, 8 th; ?cols=banana -> data-dropped-params=1, 'The URL carries cols, which this page did not apply: nothing below is narrowed by it.', still 8 th; ?cols=title and ?cols=title,banana -> no line, 2 th; ?table=venues&q=BTS -> total=2, names table and q.
For QA: (a) the partial case is deliberately silent per the ticket — ?cols=title,banana applies cols and says nothing about the bad token; (b) the shared sentence ends 'nothing below is narrowed by it', which on this route means columns, not rows — that wording is the shared module's and changing it here would be the second sentence criterion 1 forbids; (c) repeated ?cols=banana&cols=title applies title (shownColumns joins the values) and the line correctly stays silent, though droppedParams' own firstValue sees 'banana' — pinned by 'reads a repeated cols the same way the columns did'.
- 2026-09-11T18:37:38Z [dispatcher] landed ticket/BUG-0202 onto run/admin-window as 1ea821e7
- 2026-09-11T18:39:23Z [qa] built → qa: under attack
- 2026-09-11T18:44:59Z [qa] qa → done: Attacked on the landed tree (1ea821e7), criteria re-run before reading the builder's story. Receipt GREEN: lint 0, build 0, both greps, tests/offline/browse/page.test.ts 68 pass, test:live browse 6 pass. Full offline project 3968/80 green and tsc --noEmit clean (the out-of-scope views.ts edit is a shared lib, so the whole project is the integration check).

Criterion-by-criterion, measured: (1) one call of droppedParams from @/lib/url/dropped-params + DroppedParamsLine; grep 'The URL carries' returns ONE file (components/ui/dropped-params.tsx) — no second sentence. (2)(3) driven at the REAL surface, production build + minted cookie against staging on my own port 8793 (killed after): /browse -> 200, no line, 8 th; ?cols=banana -> 200, data-dropped-params=1 naming cols, still 8 th; ?cols=title -> 200, no line, 2 th; ?table=venues&q=BTS -> 200, data-dropped-params=2 naming table then q, in URL order. (4) the page under the line is unchanged: DroppedParamsLine returns null at total 0, so a bare /browse and any applied-cols URL are the old markup; proven again offline by the builder's strip-and-compare and by 3968 green.

Own attack, offline probe over 20 URL shapes (probe deleted, no file left behind): ?cols=<space>, ?cols=,,, ?cols=TITLE all correctly name cols while drawing the 7 defaults; ?cols=title,TITLE and ?cols=%20title correctly apply and stay silent; prototype-shaped keys ?constructor=1, ?toString=1, ?hasOwnProperty=1 are each named exactly once (Object.hasOwn holds); a 64-char key is spelled, a 65-char key is counted not spelled; ?offset=5 is named (the page reads no offset — paging is client-side, browseQuery only feeds the API). Ran the PAGED arm too (50-row window, provider+wrapper live): ?cols=banana -> 1 named, ?q=BTS -> 1 named, bare -> none; no duplicate line, nothing thrown.

Out-of-scope edit JUDGED OK: exporting namedColumns from src/lib/browse/views.ts. Re-typing the parse in the page is the LESSONS-5 defect the ticket's own criterion 1 is about, and the extraction is provably behaviour-preserving — inViewOrder cannot turn a non-empty configured list empty or an empty one non-empty, so moving the length-0 test after it changes nothing; one importer (browse/page.tsx), views.test.ts untouched and green.

Residual, NOT filed (P3, no criterion drives it, no app-generated URL produces it): a REPEATED cols whose FIRST value is blank is swallowed silently. Expected per criterion 2 (a cols value naming no configured column is named), Found: /browse?cols=&cols=banana -> 200, no dropped-params element, 8 th (measured over HTTP on the production build, and offline). Cause: the page reads the repeated key as one joined list (namedColumns) while droppedParams' firstValue judges 'banana' by the blank first value and takes the BUG-0127 blank exit. The builder's 'reads a repeated cols the same way the columns did' test covers only the non-blank-first direction. Trigger that would make it a ticket: any surface starting to emit a repeated cols, or the same firstValue-vs-joined split appearing on a route whose facet narrows ROWS. [resolution: fixed] [receipt green: 6 cmd(s), tree 1e486c3f]
