---
id: TASK-0060
type: task
status: done
milestone: patch
priority: P3
opened_by: strategist
parent: null
discovered_from: qa:BUG-0159
children: []
assignee: null
claimed_at: null
depends_on: [BUG-0194]
touch_scope: [src/components/sources/registry-table.tsx, src/components/sources/trends.tsx, src/lib/sources/names.ts, tests/offline/sources/names.test.ts, tests/offline/sources/page.test.ts]
force: false
attempts: 1
resolution: fixed
---
# Route the last two source-name renderers through lib/sources/names.ts — and close that class

## Description
BUG-0152 / 0154 / 0156 / 0158 / 0159 were FIVE P3 tickets on one state — a `sources.source` that exists but carries no ink — filed one site at a time, because each fix was a local `??` instead of a call to the one helper that already owns the question (`lib/sources/names.ts`: `sourceLabel`, and `hasVisibleContent` behind it). Two same-class sites were left unfiled at the M2 close:

- `src/components/sources/registry-table.tsx` — the name cell renders `row.source` with no fallback at all.
- `src/components/sources/trends.tsx` — the unnamed count is `split.source === null` (:233, :256), so a row that EXISTS with a blank name is not counted as unnamed while `sourceLabel` would still render it by id. The sentence at :333 then states a number that disagrees with the rows above it.

**Strategist's ruling at the M2 close: the class is paid for, and this is the last ticket in it.** No sixth per-site bug is filed for a blank source name, in M3 or after. The condition on that ruling is that BOTH sites go through the shared helper — not that both get their own local fallback. Note that staging's `CHECK (source ~ '^[a-z0-9_]+$')` forbids the state anyway; this is consistency work on a helper that already exists, not defence of a reachable bug.

## Acceptance criteria
Both sites call lib/sources/names.ts rather than answering 'is there anything readable here' themselves; grep finds no local blank-name fallback in either file. The trends unnamed count and sourceLabel agree on the same input: on a fixture holding a present-but-blank source name, the count at :333 and the rows above it state the same number, asserted on rendered HTML. Proved on two fixtures (LESSONS 8): one blank-name row that must be counted unnamed, one named row that must not. No other file gains a copy of the rule.

## Checks
```sh
./node_modules/.bin/vitest run --project=offline tests/offline/sources/names.test.ts tests/offline/sources/page.test.ts
npm run lint
```

## History
- 2026-09-10T21:23:04Z [strategist] opened: Route the last two source-name renderers through lib/sources/names.ts — and close that class
- 2026-09-10T21:46:07Z [architect] depends_on: [] → [TASK-0073]: destination collision, not a priority change: TASK-0073 rewrites src/components/sources/trends.tsx and tests/offline/sources/page.test.ts, which TASK-0060 also writes. M3 work runs first and TASK-0060 stays a patch-lane P3 behind it; worktrees isolate builds, not landings (ARCHITECTURE §13.9).
- 2026-09-11T14:37:12Z [architect] depends_on: [TASK-0073] → [BUG-0194]: Destination collision, not a priority change, and TASK-0073 (the previous edge) is done: BUG-0194 rewrites src/components/sources/trends.tsx and tests/offline/sources/page.test.ts, both of which TASK-0060 also writes. M3 truth lands first; TASK-0060 stays a patch-lane P3 behind it. Worktrees isolate builds, not landings (ARCHITECTURE 13.9). BUG-0194 takes no edge in return - nothing open blocks it.
- 2026-09-11T15:25:50Z [builder-296] claimed (attempt 1)
- 2026-09-11T15:38:31Z [builder-296] claimed → built: Both sites now ask lib/sources/names.ts: registry-table.tsx's name cell renders sourceLabel(names, row.source_id) (sourceColumns gained the names map the chips and both trends already take — one extra file, src/app/sources/page.tsx:300, passes it), and trends.tsx's unnamed count is !isSourceNamed(names, split.sourceId). isSourceNamed is a NEW third export of the leaf (the answer sourceLabel already computes, module-private nameIn behind both), so the leaf's 'exports exactly two' ratchet in names.test.ts was updated to three, and registry-table.tsx was added to its caller list.
Verified on the rebased tree (c73ef443): ./node_modules/.bin/vitest run --project=offline tests/offline/sources/names.test.ts tests/offline/sources/page.test.ts = 98 passed; npm run lint 0; ./node_modules/.bin/tsc --noEmit 0; whole offline project 3918 passed. Red-then-green observed: with src/ reverted and the new tests in place, 9 failed (3 registry-cell spellings, the 2 count cases, the leaf/ratchet ones). Surface observation on a blank-name fixture: the registry name cell reads '01920000-...-000000000101' instead of an empty link, the settled-values sentence reads 'The registry gave 1 of these sources no name in this read, so it is named by id.' and narrowed 'The registry gave this source no name in this read, so it is named by id.'
QA should know: (1) I CHANGED that sentence's copy — it said 'had no registry row in this read', which would now be false of a row that came back blank, and it now pluralises via pluralise() (it said 'they are' for 1 before). Nothing pins that copy; the new cases grade it by difference and by the figures it states. (2) data-source-name now carries the rendered LABEL, not the raw column — tests/live/sources.live.test.ts:233 still holds on staging, where CHECK (source ~ '^[a-z0-9_]+$') forbids a blank. runsHref(row.source) deliberately stays raw (a runs.source filter value, not a label; commented). (3) The same class survives at src/app/claims/page.tsx:1030, which counts gauge.unnamedSources from lib/gauges/standing-disagreements.ts (source === undefined only) while its rows label by sourceLabel — out of this ticket's touch_scope and its two named sites, flagged rather than silently widened. (4) The QA-flagged describe doc comment in page.test.ts is corrected: the settled-values scan is readRejectionStampGauge({ filter }) since BUG-0194 and its line names the narrowing.
- 2026-09-11T15:38:55Z [dispatcher] landed ticket/TASK-0060 onto run/admin-window as b2e4d861
- 2026-09-11T15:39:26Z [qa] built → qa: under attack
- 2026-09-11T15:45:58Z [qa] qa → done: attacked: criteria re-run first on the landed primary checkout (b2e4d861+), then the handoff read as a map of where the builder looked.

CRITERIA. (1) Both sites ask the leaf: registry-table.tsx:51 `const label = sourceLabel(names, row.source_id)` feeding BOTH the ink and data-source-name; trends.tsx:316 `!isSourceNamed(names, split.sourceId)`. grep over src/components/sources/*.tsx + src/app/sources/page.tsx finds no `??` blank-name fallback in either file (the only `??` hits are doc comments and unrelated `find(...) ?? null` type narrowing at trends.tsx:119/199/278). (2)+(3) graded on rendered HTML by the landed cases and by my own, below. (4) No new copy of the rule: every caller of names.ts is a sourceLabel/isSourceNamed/sourceNamesOf call, and the leaf's caller-list + "exports exactly three" ratchet in names.test.ts holds it.

CLASS CLOSED ON /sources. Four renderers of a source name, all through names.ts: chips (source-chips.tsx:61), registry name cell (registry-table.tsx:51), awaiting trend rows and settled-values trend rows (both sourceTrendLink -> sourceLabel, trends.tsx:56). The one surviving raw `row.source` (registry-table.tsx:175/176, runsHref + data-source-runs) is NOT a name renderer: the anchor's ink is the fixed word "runs" (confirmed in the browser), and the value is a `runs.source` match, so raw is right there.

MY ATTACK (new pin, tests/offline/sources/page.test.ts, "counts a blank-named source and a source with no registry row alike"). The landed cases prove each mechanism on a one-row fixture, where a count answering the question its own way can still agree with the rows by accident. Mine starves the gauge's OWN registry lookup two ways at once — ticketmaster blank ("   "), bandsintown absent — while the page's complete read still names both. Both trend rows then wear an id and the sentence must state 2. Watched RED against c73ef443^ (pre-fix src, this test file): `expected [ '1' ] to deeply equal [ '2' ]` while wearingAnId already returned both rows — the exact one-page-two-answers divergence. GREEN on the landed tree. Also pins that the sentence for blank-only and missing-only is IDENTICAL (the copy change's claim, graded by identity, no words pinned) and empty when all are named. Offline only, as the ticket says: staging's CHECK (source ~ '^[a-z0-9_]+$') forbids the blank.

SETTLED-VALUES SENTENCE. The new words are true of the count they state: `unnamed` and every row label now come out of one `nameIn` over one map, so "the registry gave N of these sources no name" covers both a missing row and an ink-less one — proved on the mixed fixture above. The old "had no registry row" would have been false of the blank leg.

STAGING WALK (real browser, playwright from .venv-tools, production build on port 8781 — 8770/8771/8772/8790 untouched; server killed, port released). /sources 200: 3 registry rows, ink == data-source-name for all three (test_harness, test_harness_control, ticketmaster), no uuid ink, 0 empty anchors, chips and runs hrefs unchanged (/cycles?source=ticketmaster). Narrowed to ticketmaster: 200, 1 row, same parity, no unnamed clause (correct — 0 unnamed). Nothing moved for named sources. tests/live/sources.live.test.ts 8 passed against staging, so data-source-name carrying the LABEL still satisfies the parity oracle's `toBe(source.source)` there.

CHECKS. Stored checks verbatim in the primary checkout: vitest 99 passed, lint 0. Plus ./node_modules/.bin/tsc --noEmit 0 (with my pin in the tree, LESSONS 12).

CARRIED, NOT GRADED (strategist's ruling: no sixth per-site bug): the same class survives one site further on /claims — gauge.unnamedSources in src/lib/gauges/standing-disagreements.ts counts `source === undefined` while its rows label through sourceLabel.

RESIDUALS (no path a criterion or receipt drives; not tickets): (a) narrowed + exactly one unnamed renders the weeks table, so the clause's "it is named by id" points at an id the section itself does not show — needs the gauge's join to disagree with the page's registry read; (b) sourceColumns now labels from a map keyed by source_id, so two registry rows sharing a source_id would render the later row's name in both cells — unreachable under the table's key. [resolution: fixed] [receipt green: 2 cmd(s), tree 3c5526b0]
