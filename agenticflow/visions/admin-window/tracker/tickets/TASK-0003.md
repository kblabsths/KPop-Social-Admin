---
id: TASK-0003
type: task
status: claimed
milestone: M1
priority: P1
opened_by: architect
parent: FEAT-0002
children: []
assignee: builder-10
claimed_at: 2026-09-02T07:42:42Z
depends_on: [TASK-0002]
touch_scope: [tests/live, tests/offline/live-guard.test.ts, agenticflow/docs/SERVICES.md]
force: false
attempts: 2
claim_tree_hash: 9d91049baed18cb044f73fc6947ee285d09f386868b0e4bff95bfe07b833deb7
---
# The live harness: staging-only credentials, the parity helper, the sweep helper

## Description
Module: `tests/live/` — the harness every page's parity test plugs into. Authority: acceptance doc ground rules ('Staging only', 'an unset name is a refusal, never a fallback') and tests 1, 2, 13; SPEC F2; ARCHITECTURE section 10.

Ships:

1. `tests/live/setup.ts` — the ONE place `STAGING_SUPABASE_URL` and `STAGING_SUPABASE_SERVICE_ROLE_KEY` are read. It loads `.env` explicitly with `dotenv` (Vitest does not read it), and it **refuses loudly** — non-zero, no fallback, no default — when either name is unset. It then makes the app's data layer point at staging for the live run by setting the process env the app reads, in this file only; product code never mentions a `STAGING_` name.
2. **A staging-target guard.** The host in `STAGING_SUPABASE_URL` must match the staging target declared in `agenticflow/docs/SERVICES.md`; no declaration, or a mismatch, is a refusal. This is what makes 'production is never a target' structural rather than a promise. Add the `## supabase` section to `SERVICES.md` **only** if Ben has already declared the target there — if the section is absent, do not invent it: say so on the ticket and leave the guard refusing.
3. `tests/live/parity.ts` — the reusable per-page parity mechanism: render a page function to markup, extract a named number from it, and compare it with a count the **test itself** issues. The comparison query must be written independently of the `lib/db` function the page used — two paths to one number, or it proves nothing.
4. `tests/live/sweep.ts` — record-and-restore for any row a live test touches, run in a `finally` so a failing assertion still sweeps (acceptance test 13). In M1 the only writer is the edit-surface test (TASK-0017/0018); the helper exists now so nobody hand-rolls one later.
5. `tests/offline/live-guard.test.ts` — an offline test proving the refusal path: with the names unset, the setup module throws/exits rather than falling back.

Note for whoever runs this: as of 2026-09-01 `.env` carries neither STAGING name (Ben is adding them) and `SERVICES.md` declares no service. Until both exist, `npm run test:live` refusing IS the correct, passing behavior of this ticket — do not weaken the guard to make a live test run.

Out of scope: writing any actual page parity test (each page ticket writes its own), any fixture rows in resolver-owned tables (that is a blocked question), any `src/` change.

## Acceptance criteria
[test 1, test 13] `npm test` stays green with the STAGING names unset. `npm run test:live` exits non-zero with the names unset and says which name is missing — it never falls back to `SUPABASE_*`. The staging-target guard refuses a host that is not the one declared in `agenticflow/docs/SERVICES.md`. The parity helper and the sweep helper are exported and documented, and the sweep runs in a `finally`. No file under `src/` mentions `STAGING_`.

## Checks
```sh
npm run lint
./node_modules/.bin/tsc --noEmit
npm test
bash -c "env -u STAGING_SUPABASE_URL -u STAGING_SUPABASE_SERVICE_ROLE_KEY npm run test:live && exit 1; exit 0"
./node_modules/.bin/vitest run tests/offline/live-guard.test.ts
bash -c "! grep -rq STAGING_ src"
bash -c "test -f tests/live/parity.ts && test -f tests/live/sweep.ts"
```

## History
- 2026-09-02T05:35:32Z [architect] opened: The live harness: staging-only credentials, the parity helper, the sweep helper
- 2026-09-02T07:13:57Z [builder-8] claimed (attempt 1)
- 2026-09-02T07:27:29Z [builder-8] claimed → built: Live harness shipped: tests/live/setup.ts (only reader of STAGING_SUPABASE_URL / STAGING_SUPABASE_SERVICE_ROLE_KEY; loads .env via dotenv, refuses naming every missing name, then points the app's SUPABASE_* names at staging), tests/live/staging-target.ts (the guard, kept PURE and separate so the offline suite can prove the refusal without the machine's .env deciding the answer), parity.ts (renderPage / readNumber / countRows / independentClient / assertParity — no way to ask lib/db for the expected number, by design), sweep.ts (record-and-restore, withSweep runs sweep in a finally; body+sweep double failure throws an AggregateError so residue is never swallowed), harness.live.test.ts (gives the live project a file so the refusal comes from the setup, not from 'no test files found'), and tests/offline/live-guard.test.ts (33 cases).

SERVICES.md is UNTOUCHED: it still declares no service, so the target guard refuses and that is the correct passing state — do not add a '## supabase' section without Ben's declaration. Outside touch_scope I added ONE line to vitest.config.mts: setupFiles: ["./tests/live/setup.ts"] on the live project, which is what makes the guard unskippable.

Verified on the rebased tree (run/admin-window @ c33860e): npm run lint 0; tsc --noEmit 0; npm test 164 passed (STAGING names unset); env -u STAGING_SUPABASE_URL -u STAGING_SUPABASE_SERVICE_ROLE_KEY npm run test:live exits 1 with LiveGuardError naming BOTH names; vitest run tests/offline/live-guard.test.ts 0; grep -rq STAGING_ src finds nothing. Red-then-green observed twice: my own test caught a real normalizeDeclared ordering bug (a backtick-then-period declaration failed to match), and sabotaging setup.ts with a '|| process.env.SUPABASE_URL' fallback turns the subprocess test red.

Traps QA should know: (1) the offline subprocess test sets the STAGING names to EMPTY rather than unsetting them — dotenv does not override a name already present, so that case is decided by the guard and not by whatever .env holds; unsetting them would be .env-dependent. (2) A declaration is accepted only as the project ref or the full host (equality, never a prefix) — a human-readable project name in SERVICES.md would be refused; I chose equality because a prefix match would let 'abc' match 'abc-production'. (3) harness.live.test.ts counts events; if staging lacks that canonical table the harness test fails loudly rather than silently.
- 2026-09-02T07:27:46Z [dispatcher] landed ticket/TASK-0003 onto run/admin-window as 9d0ea50
- 2026-09-02T07:28:19Z [qa] built → qa: under attack
- 2026-09-02T07:35:12Z [qa] qa → reopened: BUG-0007 filed — the parity mechanism can certify a page against a number the database never returned.

Criteria re-run on the landed tree (9d0ea50), before reading the handoff: npm run lint 0; tsc --noEmit 0; npm test 164 passed with both STAGING names unset; env -u STAGING_SUPABASE_URL -u STAGING_SUPABASE_SERVICE_ROLE_KEY npm run test:live exits 1 with LiveGuardError naming BOTH missing names; vitest run tests/offline/live-guard.test.ts 0; grep -rq STAGING_ src finds nothing; parity.ts and sweep.ts present. All seven stored checks pass.

Attacked: (1) live run with STAGING truly UNSET and decoy SUPABASE_URL/SUPABASE_SERVICE_ROLE_KEY present in the child env — exits 1, refusal names both STAGING names, zero mentions of the decoy host, no 'No test files found': no fallback. (2) live run with a fabricated STAGING URL and SERVICES.md declaring nothing — exits 1 on 'no staging target is declared', and the URL is not echoed anywhere in the output. (3) npm test with both STAGING names SET to fabricated values (the state this repo enters the moment Ben adds them) — still exit 0, 164 passed: the offline suite does not redden when the names arrive. (4) sweep: read as code plus the existing finally/AggregateError cases; body-throws still sweeps. All fabricated values, in child environments only; .env was never opened.

Broke it at the parity mechanism. Expected (tests/live/parity.ts:178-191 docstring; ARCHITECTURE.md 10 'two paths to one number, or it proves nothing'): countRows() throws rather than quietly comparing against 0 when its own query did not produce a count. Found: 'return count ?? 0' at tests/live/parity.ts:190 — a PostgREST answer with error null AND count null (exactly what a select written without { head: true, count: 'exact' } returns) becomes 0, and assertParity() then compares a page's rendered 0 against that fabricated 0 and RESOLVES. Repro is offline and databaseless: stubClient({ review_items: {} }) -> countRows(() => db.from('review_items').select('*')) resolves 0. The existing case 'returns zero only when the database actually counted zero' asserts count:0 only, so the property its name claims is not the property it tests.

Both halves are now pinned in tests/offline/live-guard.test.ts (lines 309, 325) as it.fails cases marked 'PIN admin-window/BUG-0007'; watched red unpinned, and the pin's strictness proven by flipping the assertion so the body passed — the file went red ('Tests 1 failed | 34 passed'). Branch stays green: npm test 166 passed, lint 0, tsc 0.

Ruling on the out-of-scope line: vitest.config.mts +4 (setupFiles on the live project only) is necessary and minimal — without it the guard is importable-and-skippable, and the offline and http projects are untouched (npm test still green). Not a bounce.

Residuals, neither filed (lean): (a) hostMatchesDeclaration matches the host's FIRST LABEL against a ref-only declaration, so a declaration of 'abc' also accepts 'abc.anything.tld' — production still cannot pass (different ref), so this only matters if STAGING_SUPABASE_URL is ever pointed at a foreign domain sharing the ref. (b) declaredStagingTarget does not track code fences; its docstring's claim that a template inside a code block 'cannot be mistaken for a declaration' holds today only because SERVICES.md's template is indented AND placeholder-shaped — an unindented filled-in EXAMPLE under a '## supabase' heading in that doc would be read as a real declaration.
- 2026-09-02T07:42:42Z [builder-10] claimed (attempt 2)
