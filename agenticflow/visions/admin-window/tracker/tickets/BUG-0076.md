---
id: BUG-0076
type: bug
status: done
milestone: M1
priority: P2
opened_by: qa
parent: null
discovered_from: BUG-0068
children: []
assignee: null
claimed_at: null
depends_on: []
touch_scope: [src/app/queues/[reviewItemId]/page.tsx, tests/offline/review-item/page.test.ts]
force: false
attempts: 1
resolution: fixed
---
# Queues item page: an address that is not a review-item id is reported as a failed database read, not as no such item

## Description
Found by QA attacking BUG-0068 (2026-09-04): the PATCH route and the record
page now both settle a malformed id BEFORE any read; the queues detail page is
the third and last address-taking surface, and it still does not.

**Expected**, by this app's own settled answer on the two surfaces beside it:
`review_items.review_item_id` is a `uuid` (scraper migration
`20260901000002_the_review_item_opens_once_per_subject.sql`, line 39), so a URL
segment that is not a uuid can equal no key in that table. "No such item" is
knowable with certainty and without a database — which is exactly what
`isRecordId` (`src/lib/db/records.ts`) carries, why BUG-0065 put it BEFORE the
record page's read, and why BUG-0068 put it before the PATCH route's write.

**Found**: `src/app/queues/[reviewItemId]/page.tsx:398-399` takes the segment
off `params` and hands it straight to `readReviewItem`, which builds
`.eq("review_item_id", <segment>)`. Postgres refuses the comparison with
`22P02 invalid input syntax for type uuid`, the data layer classifies that
(correctly, for a read it did make) as an arbitrary failure, and the page
renders the FAILED-READ state — the one whose recovery line is "reload".
Reloading re-sends the same segment forever, so that advice can never work: the
operator is stranded on unactionable advice, which is the same harm BUG-0065
was filed to remove one surface over.

**Measured** (2026-09-04, run/admin-window @ 2bbe138, offline tier with the
stub client scripted to answer `invalidUuidSyntax`):
  - reads issued: `[ { table: 'review_items' } ]` — expected none;
  - rendered states: `[ 'error' ]` — expected anything but the failed-read one.

**Reach**: an operator's own address bar, and the app invites it — the queues
empty state says "check the id in the address bar", and the review-item id is
hand-carried between surfaces. That is a wider path than BUG-0068's (which had
no UI path at all), which is why this is P2 and not P3.

**The fix**, if the architect agrees with the two rulings above: ask the id
question BEFORE the read and answer it with this page's own no-such-row state
(the `Empty` card it already renders for `item.data === null`), not with a new
sentence. Do NOT write a second id grammar: `isRecordId` is the one grammar in
this repo (ARCHITECTURE §9.1 item 9, DECISIONS 2026-09-04) and it takes the raw
segment and no config, so it applies to a `review_item_id` unchanged. The
error branch itself stays exactly as it is — a read that really happened and
really failed still reports what the database said (LOOK_AND_FEEL).

**Where it is pinned**: `tests/offline/review-item/page.test.ts`, describe "a
queues address that is not a review-item id". QA left it as a STRICT `it.fails`
pin naming this ticket, so it is green only while the divergence is live; the
fix must flip it back to a plain `it()` in the same commit (the convention this
repo already uses for admin-window/BUG-0013). Watched RED as a plain `it()` on
2bbe138, both halves, one at a time:
  "a segment that can be no review_item_id needs no read: expected
   [ { table: 'review_items', ... } ] to have a length of +0 but got 1"
  "the answer to a bad address is not a failed read: expected [ 'error' ] to
   not include 'error'"

## Acceptance criteria
A /queues/{reviewItemId} address whose segment is not a review-item id renders the page's own no-such-item state, having issued NO database read (observable as zero recorded calls on the stub client in tests/offline/review-item/page.test.ts), and the rendered surface carries no data-state="error" card. The page asks isRecordId from src/lib/db/records.ts — the same function the record page and the PATCH route ask; no second id grammar, regex or per-table key shape is written anywhere (ARCHITECTURE 9.1 item 9). Every other answer this page gives is unchanged: the database's own words for a read that really happened and failed, the not-provisioned state for an absent table, the no-such-row state for a well-formed id that matches nothing, and the item itself when it is there. The strict it.fails pin naming this ticket is flipped back to a plain it() by the fix.

## Checks
```sh
npx vitest run --project=offline tests/offline/review-item/page.test.ts
./node_modules/.bin/tsc --noEmit
npm run lint
```

## History
- 2026-09-04T05:55:12Z [qa] opened: Queues item page: an address that is not a review-item id is reported as a failed database read, not as no such item
- 2026-09-04T05:57:39Z [builder-109] claimed (attempt 1)
- 2026-09-04T06:07:58Z [builder-109] claimed → built: - The queues detail page now asks isRecordId (src/lib/db/records.ts — the one grammar, imported, not copied) BEFORE readReviewItem; a malformed segment returns the Empty card with zero queries, so the evidence, bucket and dial legs never run. The recovery sentence the two emptinesses share is factored to one const (FOUND_BY) so they cannot drift.
- COPY FORK, flagged: the ticket said 'not with a new sentence', but I gave the not-an-id card its OWN words rather than reusing the no-such-row copy. Reason: the record page's landed BUG-0065 test asserts exactly this (tests/offline/records/page.test.ts, 'does not answer %o with the unknown-id state' — emptyText(mistyped) !== emptyText(unknown)), the Look forbids two emptinesses sharing a rendering, and a trailing %20 is invisible without the grammar sentence. I read 'not a new sentence' as 'not a fifth STATE'. Overturn it in one string if the architect meant it literally; no test pins the copy.
- Verified on the rebased tree (5eda5f1 on run/admin-window): tsc --noEmit, npm run lint, npx vitest run --project=offline (58 files / 1983 tests), and npm run test:live -- tests/live/review-item.live.test.ts (7/7) + queues.live.test.ts (7/7). Watched RED first: the new block failed 13/13 against the pre-fix page ('expected [ { table: review_items } ] to have a length of +0 but got 1'). QA's it.fails pin is a plain it() and widened to the record page's fixture classes plus WELL_FORMED_IDS (canonical / uppercase / unhyphenated) which MUST still read.
- Production build against staging, port 8850 (8790 is a stranger's Python listener answering 'bad token' on IPv4 — my bind probe missed it because Next took IPv6; probe both stacks): /queues/not-an-id, /queues/<real minus last char>, /queues/<real>%20 each 200, states ['empty'], 0 evidence/dial nodes, identity echoed verbatim (the %20 shows as %20); the real item 01a06287-5b67-7d1f-b4a5-d90aa93cb01a renders 93 evidence/dial nodes and no state card; a well-formed absent uuid still reaches 'No row with that id in review_items' through a real read. Read-only, nothing written to staging.
- Trap for QA: node_modules and .env exist only in the primary checkout — symlink both into the worktree before running anything.
- 2026-09-04T06:08:18Z [dispatcher] landed ticket/BUG-0076 onto run/admin-window as 4902402
- 2026-09-04T06:08:56Z [qa] built → qa: under attack
- 2026-09-04T06:20:45Z [qa] qa → done: attacked on the landed tree (4902402): production build from source, driven against staging and two stub PostgRESTs; nothing to bounce.

CRITERIA (re-run before reading the builder's story):
- stored checks green: offline review-item/page.test.ts 63/63 with the it.fails pin now a plain it(); tsc --noEmit 0; lint 0 (receipt).
- ONE GRAMMAR: grep of src for any hex class returns only RECORD_ID (src/lib/db/records.ts:102). The page imports isRecordId (page.tsx:16) and calls it at :467, before readReviewItem at :479. No per-table key shape anywhere; the other hex regexes in the tree are test fixtures and colour-token tests.
- ZERO QUERIES, proven at HTTP: production build on :8802 pointed at a request-counting stub PostgREST (:8803, control :8804), tally reset per request. Nine malformed shapes (not-an-id, one-char-short, trailing-%20, {id} raw, %7Bid%7D, hyphen-after-6hex, "><script>, %00-suffixed, 4000 chars): each 200, pgRequests 0, states ["empty"], 0 data-evidence/data-evidence-view/data-dial nodes, id echoed verbatim, no unescaped script tag — so the evidence, bucket, dial and source-name legs are silent too, not just the item read. Well-formed spellings on the same stub: pgRequests 1, the recorded call being GET /rest/v1/review_items?...&review_item_id=eq.<id>.
- 26-SPELLING DIFFERENTIAL (BUG-0065's staging table) replayed against THIS page offline, stub scripted 22P02, 64 assertions: every spelling Postgres refuses -> 0 recorded calls + one empty card; every spelling it accepts (canonical, unhyphenated, both uppercases, mixed case, hyphen-after-8-16-20, hyphen-every-4, nil) -> a real read and the item, no state card. Guard never looser than Postgres. The two known guard-stricter braced spellings re-measured as unreachable on THIS route: /queues/{id} and /queues/%7Bid%7D both arrive as the literal %7B...%7D.
- EVERY OTHER ANSWER UNCHANGED, delivered HTML: staging (:8801) real item 01a06287-5b67-7d1f-b4a5-d90aa93cb01a renders 91 data-evidence + 1 data-evidence-view + 1 data-dial = 93 nodes, 0 state cards, both surface hooks (what_happened, evidence), and resolves identically in all three legal spellings. Well-formed absent uuid: 1 read, ["empty"], 'No row with that id in review_items'. Stub answering XX000: ["error"] after 1 read. Stub answering PGRST205: ["not_provisioned"] after 1 read. Malformed stays ["empty"] with 0 reads under both.
- the auth gate still fronts the new early return: no cookie and forged cookie both 307 to /login for /queues/not-an-id.
- extra probes that found nothing: traversal (..%2F..%2Fapi%2Fhealth), %20/%09/%0A/%25-only segments, urn:uuid:, fullwidth and Arabic-Indic digits, a Cyrillic lookalike, ;params, +suffix — all one empty card, zero reads; percent-encoded canonical (%30-prefixed and fully encoded) decodes to the same id and reads correctly.

JUDGEMENT on the builder's flagged COPY FORK: it holds; no BUG. The ticket's clause is about STATE and the state is unchanged — the not-an-id answer renders data-state="empty", this page's own no-such-item state, and the page's vocabulary is still empty/error/not_provisioned/item with no fifth. The words are the designer's jurisdiction, and the landed sibling test (tests/offline/records/page.test.ts, BUG-0065) requires the mistyped card's text to differ from the unknown-id card's, so reusing the copy would have split the two address-taking surfaces. Measured as delivered: the two cards differ and share exactly the recovery sentence, written once as FOUND_BY.

Residuals (digest only, no tickets): (1) the not-an-id sentence now exists once per surface (page.tsx:150 and records/[table]/[id]/page.tsx:221) — a designer/architect call, not a defect; (2) BUG-0069's evidence dir still holds a full worktree copy on disk. [resolution: fixed] [receipt green: 3 cmd(s), tree a4aea126]
