# Digest 2026-09-11

## Key items
- **Where the app stands vs the vision.** M2 (verdict slice) SHIPPED at tag `m2`. M3 "completeness of the read slice" is MID-FLIGHT and most of it is on the run branch: `/claims` warm went 2.9–3.8 s (your walk) → **1.1–1.3 s** (chunked reads fan out; the pending-claims gauge reads at depth 1); **paging past the window is LIVE on both `/claims` and `/browse`** (press-to-load-more through gated app routes, bound refused not clamped, full-or-exhausted contract, no-store on every arm) — the first half of your 2026-09-09 ruling. INTENTIONALLY ABSENT from the current build: the paged window line's truth after a press (BUG-0172, landed today, in QA — until it closes the line above the table still says "not shown" after you page to the end); the windowed-figure honesty slice (FEAT-0016: the tab gauge vs head counts past 1,000 rows, `/sources`' unnamed narrowings, scan upper edges) — queued behind it; search (vision addition, needs `/ship revise`); the live §7 proof of the verdict slice (waits on your two verdict handoffs).
- **Today in numbers:** 20 M3 tickets closed (12 TASK, 7 BUG, 1 DEBT), 14 open, 0 bounces that were product defects (two bounces were check defects: an evidence path the harness cannot see, and a guard reading your uncommitted `.env.example`). One five-hour usage throttle (81% vs 80% ceiling) idled the loop 72 min.
- **Two staging findings you should know about, neither a product bug:** (1) the scraper writes staging in bursts — the pending-claims view was watched 877 → 887 → 879 inside one page render — so every live proof over that table is now held still or bounded (rule: never a tolerance). (2) That view sits at 877 rows against the 1,000-row read ceiling; TASK-0077 (building now) unsizes the live suite from staging before it bites.
- **Scraper repo:** the migration you applied on 2026-09-10 (`20260910000001_a_pending_claim_carries_its_instant.sql`) is still UNTRACKED there; the schema-owning repo has no git record of what staging runs. Please commit it.
- **Machinery proposals for you (install between runs):** `2026-09-10-receipt-cannot-grade-evidence-paths.md`, `2026-09-10-evidence-clean-unusable-from-a-worktree.md` (both in `agenticflow/tracker/proposals/`). Plus, for the curator at run end: dispatch never re-offers a ticket a dead lane left in `qa`; `ci_check`/full-suite runs can go red while a land merges into the primary checkout; the layering test's fixed probe path reddens under two concurrent runs (DEBT-0018 fixes the last one in-product).
- **Contract movements today (architect, all in ARCHITECTURE.md + DECISIONS.md):** paging is full-or-exhausted with client-side refusal; the legs that fill columns travel WITH a page and an unreadable note is a refusal; a paged window line renders where the press can reach it (Common violations row 21); a gated JSON answer says it may not be stored; live proofs over scraper-written tables use one small held-still read or a bounded snapshot, never a tolerance; the http tier can prove gate behaviour and page headers, never a handler's own answer; an error account carries the database's words or a counted app clause (BUG-0173 widens BUG-0170).
- **Your items unchanged:** install and review the verdict + settle handoffs on staging (gate the vision's "reviewed"); delete stale `for-human/TASK-0031.md`; three data items in `for-human/M2-usersim-judgment.md`.

### Still waiting (unchanged)
- `.env.example` modified in the working tree — yours; say if it should land on the run branch (since 2026-09-04).
- `M2-handoff-verdicts.md` / `M2-handoff-settle-review-item.md` not yet installed on staging (since 2026-09-08).

## Verify it yourself
- [ ] `http://localhost:8771/claims` — loads in ~1.2 s warm; scroll to the bottom: a "load more" control; press it 17 times → 877 rows, control says the set is complete (TASK-0067, TASK-0074, BUG-0167)
- [ ] `http://localhost:8771/browse` — press "more" twice → 120 events, complete (TASK-0069)
- [ ] `curl -I http://localhost:8771/api/admin/claims/rows?offset=50` (with your session cookie) → `cache-control: private, no-cache, no-store, max-age=0, must-revalidate` (BUG-0171)
- [ ] `.../api/admin/claims/rows?offset=abc` → 400 naming the bound as sent, no rows (TASK-0066)
- [ ] `/cycles` on staging's empty resolver window: gauges show real `0`s, no "empty" grading error in `npm run test:live -- tests/live/cycles.live.test.ts` (BUG-0169)
- [ ] `npm run test:live -- tests/live/claims.live.test.ts` five times → green each time (TASK-0075)
- [ ] `git tag -l m2` present; `python3 agenticflow/scripts/ci_check.py` → green

## Everything else
- attention UI: http://127.0.0.1:8770/ (your walk instance: http://localhost:8771, Google login; note it runs `next dev`, so it lags a production build by a few hundred ms)
- rail telemetry: 1 usage throttle (72 min idle), 0 breaker trips, 0 compactions, 0 stalled claims; two check-defect bounces (TASK-0062 evidence path, TASK-0061 env guard); budget: 2 of `max_milestones` shipped, M3 ~60% closed by ticket count
- spawn economy since the last digest (spawn_log out-tokens column): builder 22 returns / 36,051; qa-adversary 22 / 15,337; architect 11 / 8,000. Architect share is up on purpose: nine batched rulings kept every builder on settled criteria and no ticket bounced on a product defect.
- counts since the last digest: closed 20 (12 TASK, 7 BUG, 1 DEBT); opened 10 BUG, 4 DEBT, 3 FEAT, 18 TASK (M3 decomposition + QA findings); open now 14 (5 TASK, 2 BUG, 3 DEBT, 3 FEAT + 1 claimed, 1 in QA); gate fires: 8 artifact-gate blocks of `/tmp` writes, 1 supply-gate block (`npx tsx`), 2 QA evidence-budget overruns
- evidence: `/claims` warm medians 1.111 s (builder), 1.134 s (QA), 1.227 s (QA, BUG-0167), all ≤ 1.4 s bar; `/sources` 0.22 s; review item 0.72 s; census 23 → 15 round trips, depth 4 → 1 (TASK-0074 History); staging `pending_claims` 877 rows, 817-row tie on one instant (BUG-0167 History); paged walks 877/877 claims and 120/120 events distinct (TASK-0067/0069 receipts). Frames and captures under `agenticflow/tracker/evidence/` are gitignored working files, re-measurable, never the proof.
