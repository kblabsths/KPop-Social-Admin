# Digest 2026-09-02

## Key items
- **Where the app stands vs the vision (end of 2026-09-02):** M1's read slice is BUILT and QA-closed on `run/admin-window` — all six pages (Dashboard, Queues + review-item detail, Claims, Sources, Cycles & runs incl. the adapter-runs half, Browse), the six gauges, the shell, and the edit surface (groups/idols direct edits; events/venues read-only with a display list, per-field provenance and a venue link). 72 tickets done. Live parity against real staging rows is GREEN for Dashboard, Queues, review-item, Cycles, runs, Browse and the edit write-and-sweep (0 residue). INTENTIONALLY ABSENT: the verdict slice (settle actions, overrides, the two handoff migrations) is M2; dials are deferred past M2 to Ben's own follow-up; the live proof of §7 actions (tests 6–8) is the deferred patch run.
- **The one M1 blocker is yours: TASK-0031.** The `pending_claims` view times out on staging for any read beyond `limit 1` (measured eight ways), so the Claims page and the Sources awaiting-row trend are honestly red and TASK-0032 (live oracles) is parked on it. The ticket carries the candidate index on `field_provenance` (`entity_type, entity_id, field, applied_at desc, provenance_id desc`), a `not materialized` fallback, target path and apply command — applied from the scraper repo. The M1 endgame (designer walk, user-sims, verifier, root-cause pass, tag) starts when it lands.
- **Decisions you made today:** env names (app reads SUPABASE_*, tests STAGING_*; no pg driver); staging writes on groups/idols approved, resolver-owned never; nine `runs` columns; display list on events/venues with provenance; "no provenance recorded" kept; dials deferred past M2; desktop-only, both themes; Railway recorded not declared (DEP-0002 done); MIGRATE_LATER fixture-table request added by you.
- **Open question for you:** `.env` again carries `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY` beside the STAGING names. Which project do they point at? If production, remove them (a local `npm run dev` would read prod with the service role); tests ignore them either way.
- **Staging facts, not defects:** one `review_item` exists (an entity_link signal), so decision-queue live parity is 0-vs-0 until the resolver escalates a real conflict; `verdicts` is absent (expected before M2).
- **Open BUGs:** BUG-0035 (P2, live-oracle classifier can grade an error page green — in build). Everything else is closed.

### Still waiting (unchanged)
- TASK-0031 index handoff (first appeared 2026-09-02 ~14:00Z).

## Verify it yourself
- [ ] `cd "kspace Admin" && git checkout run/admin-window && npm ci && npm run lint && npm test` — lint exits 0, offline suite green with no STAGING names set (TASK-0001)
- [ ] `npm run test:http` — builds the app, serves it on 8772, asserts `/api/health` 200 and `/` → `/login`, refuses if another process holds the port (TASK-0001, BUG-0002)
- [ ] `./node_modules/.bin/vitest run tests/offline/db` — the not-provisioned classification and the never-throws reads (TASK-0002)
- [ ] `grep -c "@theme" src/app/globals.css` — 1; the token layer and primitives under `src/components/ui` (TASK-0004)

## Everything else
- attention UI: http://127.0.0.1:8770/
- rail telemetry: watchdog releases 0; blocked ages: five ASK tickets since 2026-09-02 05:40Z; compactions 0; budget: M1 of max 6 milestones, 0 shipped
- spawn economy (output tokens by role, run to date): architect 54.7k (1 spawn), builder 52.5k (7), qa-adversary 23.5k (6), visionary 8.0k (1), toolsmith 6.9k (2), designer 4.5k (1)
- counts: opened — 8 FEAT, 25 TASK, 1 DEP, 5 BUG; closed done — TASK-0001, TASK-0002 (rebuild in flight), TASK-0004, DEP-0001, BUG-0001 (no change: stale primary node_modules), BUG-0002, BUG-0003; open BUGs — BUG-0004 (absence renders three ways, P2, pinned), BUG-0005 (credential guard misses destructured/optional-chained reads, P2, pinned); bounces: TASK-0001 ×1, TASK-0002 ×1
- CI: green at the last three checks; the first red (BUG-0001) was the primary checkout's stale install, not code
- evidence: receipts under `agenticflow/tracker/receipts/` for every closed ticket; contrast ratios quoted in TASK-0004's History
- factory discussion points (for the curator's proposals and Ben's factory thread, 2026-09-02): (1) harness permission prompts from subagents go to the human, not the dispatcher — an unanswered prompt stalls a lane until the stale timer; Ben wants dangerous-command review to stay human but readable: the kit should render the command in plain words on the prompt or route a first-pass review through the dispatcher; (2) `vision.py new` refuses fresh-install `.gitkeep` files as non-empty tracker dirs; (3) agent defs read `agenticflow/docs/STACK.md` while the architect writes `docs/vision/STACK.md` (symlinked by hand); (4) `compact_threshold_bytes` 8000 fired five raises in one day on verbatim-ruling Histories (raised to 16000 in run.yaml); (5) the SERVICES.md template invites a "name" where the live guard accepts only a project ref or host; (6) declaring a service whose CLI is installed globally opens it wholesale — the gate's prod fence is textual and Railway's default environment is named "production" (DEP-0002); (7) the attention UI does not surface ticket History, which is where every ruling and paste-ready artifact lands — Ben could not find DEP-0002's SERVICES.md text; (8) `git add <pathspec> && git commit` still commits everything another lane has STAGED in the shared checkout — two dispatcher tracker commits swept a QA lane's staged pin and its revert (net effect nil, history noisy); the dispatcher now uses `git commit -o <paths>`, and the skill text should say so; (9) the artifact gate blocks the sanctioned request path (`../MIGRATE_LATER.md`) mid-run, so cross-repo requests become paste-ready panel notes.
- kit note: `vision.py new` refused the fresh-install `.gitkeep` files as "non-empty" tracker dirs; migrated by hand (moved into the campaign folder) and activation re-run — a curator candidate
