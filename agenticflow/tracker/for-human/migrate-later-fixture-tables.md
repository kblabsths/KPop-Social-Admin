Feature request you asked for (2026-09-02): a set of test tables for live acceptance writes. The artifact gate forbids agents writing outside this repo mid-run, so paste this into ../MIGRATE_LATER.md yourself (under a new "## Test fixtures the live suites need" section):
- **A set of test tables for live acceptance writes.** Today the Admin campaign's live edit test writes one field on an existing staging `groups` row and restores it in a `finally` (approved 2026-09-02 as the interim practice; `idols` the same); resolver-owned tables are never written by Admin tests. `groups` and `idols` are slated for deletion once the catalog domains land, so the interim loses its target. *To design:* a dedicated fixture table (or a `fixture_*` namespace on staging) that a pre-cutover-style editable regime can point at, so live proofs of the edit surface, the sweep helper and residue checks never touch catalog rows. Cross-repo: schema in `kspace Scraper/supabase/migrations/`; the Admin edit map gains a fixture entry; the scraper's `synthetic_fixture_slices` is the shape to reuse.
Recommend: paste as-is; it is a design-queue item, not campaign work.
Unblocks: nothing now; it retires the groups/idols interim when the catalog domains land.
