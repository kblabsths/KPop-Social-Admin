DEP-0002 — Railway vet done. Nothing in M1 needs the railway CLI (deploys go by git push; the campaign never repoints). Declaring `## railway` in SERVICES.md would let agents run the globally installed CLI (/opt/homebrew/bin/railway) against the live deploy, whose Railway environment is named "production" — the gate's prod fence is textual and would not fire. Two paste-ready texts are on the ticket.
Recommend: Option A — a `### railway (recorded, deliberately NOT declared)` block in SERVICES.md; declare it (Option B, with both warnings) only when a ticket names a railway command worth the grant.
Unblocks: nothing in M1; closes DEP-0002 either way.
