TASK-0029 — ASK: what does a resolver-owned record page (events/venues) DISPLAY in M1? Spec §8 defines these tables only in their M2 override form; today /records/events/<id> (linked from every Browse row) renders the id and nothing else, because the one config map carries no display set for a read-only table.
Recommend: add a per-table display list to the same map (events: the Browse spot-verification columns — title, description, poster, starts_at, venue; venues: name, city, country, address), rendered read-only with provenance beside each; no second allowlist, no edit control.
Unblocks: TASK-0029 (a small builder ticket once ruled); the Browse → record link stops dead-ending.
